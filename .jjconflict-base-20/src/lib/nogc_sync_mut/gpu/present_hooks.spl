# Shared GPU presentation runtime hooks.

extern fn rt_cpu_present_pixels(pixels: [u32], w: i32, h: i32) -> bool

fn cpu_present_pixels(pixels: [u32], w: i32, h: i32) -> bool:
    rt_cpu_present_pixels(pixels, w, h)
