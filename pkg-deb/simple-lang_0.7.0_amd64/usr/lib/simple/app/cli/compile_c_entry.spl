# C Backend Compile Entry Point
#
# NOTE: The interpreter cannot call imported free functions, so this
# entry point currently acts as a fast-fail stub that falls through
# to the simple_codegen fallback (Path 3 in compile_c_backend).
#
# Once the bootstrap binary is compiled natively, this will be
# replaced with the full MIR C backend pipeline.

extern fn sys_get_args() -> [text]

fn main() -> i64:
    # Fast-fail: interpreter cannot call imported compiler functions
    # This allows the CLI to fall through to simple_codegen (Path 3)
    return 1
