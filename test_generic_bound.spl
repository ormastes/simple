# Test with generic parameter in bound

trait Iterator<T>:
    type Item

# Test bound with generic parameter (no associated type constraint yet)
trait FromIterator<T>:
    fn from_iter<I: Iterator<T>>(iter: I) -> Self
