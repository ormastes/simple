# platform = mac variant: fixed macOS build-target extensions.
# Resolved only when config selects platform: mac. No host detection.

pub fn target_lib_ext() -> text:
    ".dylib"

pub fn target_exe_ext() -> text:
    ""
