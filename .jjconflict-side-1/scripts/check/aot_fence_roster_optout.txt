# aot_fence_roster_optout.txt -- AOT/native/JIT fences deliberately NOT in the
# check-aot-lane-fences.shs roster.
#
# Format:  <script-basename-without-.shs><whitespace><why it is not in the roster>
# A reason is REQUIRED. check-aot-lane-fences.shs --roster rejects a bare
# basename: an unexplained omission is exactly the state this file exists to
# eliminate.
#
# WHY THIS FILE EXISTS
# --------------------
# check-aot-lane-fences.shs was written (2026-08-08) to end "fences that nothing
# runs". Its roster is a hand-maintained list, and nothing checked that the list
# matched the tree. On 2026-08-10 an audit at cc9302b4a25 found SEVENTEEN
# AOT/native/JIT fences on disk and absent from the roster -- including
# check-native-print-stdout-oracle.shs, the gate written specifically to catch
# the silent native-print drop, and all five fences that had just been given
# stdout oracles. The aggregator built to end unrun fences was itself leaving 17
# fences unrun, and its "PASS -- 14 fence(s) checked, all green" verdict said
# nothing about them.
#
# This file plus the --roster completeness oracle makes that drift FAIL instead
# of passing silently. Adding a new scripts/check/check-{native,aot,jit}-*.shs
# now forces a decision: put it in the roster, or write down here why it does not
# belong.
#
# DO NOT add a line here to make a red roster green. Add the fence to the roster
# instead. A line here is a written claim that the script is not an AOT-lane
# regression fence at all.
check-aot-lane-fences  the aggregator itself; listing it in its own roster would recurse
check-native-seed-parity  seed-vs-selfhost binary parity comparison, not an AOT codegen regression fence; long-running and needs two compiler binaries
check-native-parity-if-selfhosted  conditional wrapper around check-native-seed-parity; same rationale
check-jit-runtime-symbol-manifest  static manifest/symbol-table audit of the JIT runtime, not a native-build codegen fence
