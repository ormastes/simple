# Compatibility shim for legacy compiler_shared.interpreter.contracts imports.
#
# The canonical definitions live under `compiler.interpreter.contracts`.
# This module preserves the shared import path for compiler-facing code.

use compiler.interpreter.contracts.{
    ContractMode,
    ContractKind,
    ContractViolation
}

pub use ContractMode
pub use ContractKind
pub use ContractViolation
