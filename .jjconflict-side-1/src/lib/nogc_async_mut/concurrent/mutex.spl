"""
# No-GC Async Mutex Facade

Mutex operations are synchronous lock primitives, so the async family reuses
the no-GC sync runtime-hook owner instead of declaring duplicate hooks.
"""

export use std.nogc_sync_mut.concurrent.mutex.{Mutex, mutex_new}
