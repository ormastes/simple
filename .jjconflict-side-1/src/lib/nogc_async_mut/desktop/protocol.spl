"""
# No-GC async desktop protocol

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.desktop.protocol.{
    ProtocolConfig,
    register_protocol, unregister_protocol, is_protocol_registered
}
