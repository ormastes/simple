"""
# No-GC Async LSP Protocol

Compatibility facade over the no-GC sync LSP backend.
"""

export use std.nogc_sync_mut.lsp.lsp_protocol.{
    lsp_read_line_chars, lsp_read_chars, lsp_read_message,
    lsp_write_message, lsp_is_json_lines_mode, lsp_set_json_lines_mode,
    lsp_write_notification, lsp_log
}
