"""
# No-GC async remote DWARF debug

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.debug.remote.dwarf.{
    DwarfInfo, dwarf_load, dwarf_free, dwarf_addr_to_line,
    dwarf_line_to_addr, dwarf_function_at
}
