"""
# No-GC Async Service Lease Manager

Compatibility facade over the no-GC sync service lease-manager backend.
"""

export use std.nogc_sync_mut.service.lease_manager.{
    LEASE_EXCLUSIVE,
    LEASE_SHARED,
    LeaseEntry,
    LeaseResult,
    LeaseManager,
    lease_manager_new,
    try_acquire_exclusive,
    try_acquire_shared,
    release_lease,
    active_lease_count,
    has_active_exclusive
}
