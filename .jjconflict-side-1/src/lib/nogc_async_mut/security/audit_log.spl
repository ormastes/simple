"""
# No-GC async security audit log

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.security.audit_log.{
    log_security_event, format_audit_entry, event_type_name,
    event_to_fields, mask_value, severity_name,
    meets_severity, severity_rank
}
