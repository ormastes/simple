# UI Access Protocol — hub module
#
# Re-exports all public symbols from the access_* sub-modules.
# Consumers should import from this file for the full access API.

use common.ui.access_types.{
    UI_ACCESS_PROTOCOL_VERSION,
    UI_ACCESS_MAX_EVENTS,
    UiAccessProp,
    UiAccessNode,
    UiAccessSurface,
    UiAccessEvent,
    UiAccessSnapshot,
    UiAccessQueryResult,
    UiAccessEnsureResult,
    ui_access_canonical_id,
    ui_access_surface_id_from_canonical,
    ui_access_widget_id_from_canonical
}
use common.ui.access_snapshot.{
    ui_access_empty_snapshot,
    ui_access_snapshot_from_state,
    ui_access_snapshot_from_session,
    ui_access_snapshot_persistable,
    ui_access_recent_events,
    ui_access_recent_surface_events,
    ui_access_record_event,
    ui_access_record_action,
    ui_access_lifecycle_events
}
use common.ui.access_query.{
    ui_access_find_nodes,
    ui_access_find_node,
    ui_access_node_value,
    ui_access_supports_value,
    ui_access_value_to_json,
    ui_access_resolve_action_name,
    ui_access_snapshot_to_text,
    ui_access_snapshot_to_json,
    ui_access_surface_to_json,
    ui_access_query_to_json,
    ui_access_query_result,
    ui_access_ensure_result,
    ui_access_ensure_to_json,
    ui_access_events_to_json
}
