# JavaScript Engine — Standard Library Module Root
# Provides a complete JS engine usable by any Simple application.
#
# Usage: use std.js.types.js_types.{JsValue, JsObject, JsProperty}
#        use std.nogc_sync_mut.js.engine.interpreter.{JsInterpreter}
#        use std.js.engine.runtime.{JsRuntime}

# ── Type Definitions ───────────────────────────────────────────────────
use std.common.js.types.js_types.{JsValue, JsObject, JsProperty}
use std.common.js.types.ast_types.{JsExpression, JsStatement, SwitchCase}
use std.common.js.types.bytecode_types.{OpCode, BytecodeChunk, ConstantPool}
use std.common.js.types.gc_types.{Generation, GcHeader, GcObject, GcStats}

# ── Engine Components ──────────────────────────────────────────────────
use std.common.js.engine.js_error.{JsError, script_error, Logger, LogLevel}
use std.common.js.engine.lexer.{JsLexer}
use std.common.js.engine.parser.{JsParser}
use std.common.js.engine.vm.{Vm}
use std.common.js.engine.bytecode_compiler.{BytecodeCompiler}
use std.common.js.engine.runtime.{JsRuntime}
use std.common.js.engine.jit.{JitCompiler}
use std.common.js.engine.gc.{GcHeap}

# ── Builtins ───────────────────────────────────────────────────────────
use std.common.js.builtins.object.{object_create, object_keys, object_values, object_entries}
use std.common.js.builtins.json.{json_parse, json_stringify}
use std.common.js.builtins.promise.{promise_resolve, promise_reject}
use std.common.js.builtins.error.{create_error, create_type_error, create_reference_error}
