class StringBuilder:
    parts: [text]

impl StringBuilder:
    static fn new() -> StringBuilder:
        StringBuilder(parts: [])

    fn push(s: text):
        me.parts.push(s)

    fn push_line(s: text):
        me.parts.push(s)
        me.parts.push("\n")

    fn to_text() -> text:
        me.parts.join("")

    fn build() -> text:
        me.parts.join("")
