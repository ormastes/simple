"""
# Canonical package SFFI compatibility facade

The declaration owner is `std.nogc_sync_mut.package_sffi`. This module keeps
the historical namespace without duplicating raw declarations or Cargo ABIs.
"""

export use std.nogc_sync_mut.package_sffi.{
    calculate_checksum, create_tarball, extract_tarball, get_file_size,
    copy_file, mkdir_all, remove_dir_all, create_symlink, set_permissions,
    path_exists, is_directory
}
