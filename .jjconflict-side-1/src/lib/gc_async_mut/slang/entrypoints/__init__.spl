# @gc — module uses GC runtime
#
# entrypoints — mirrors vllm/entrypoints/
#
# Phase A6: the OpenAI-compatible HTTP surface lives under openai/.

export ApiResponse, ChatRequest, SlangModelEntry
export parse_chat_request, openai_error_response, openai_models_response
export read_model_entry, discover_models, model_ids, find_model
export slang_implemented_phase, slang_missing_inference_phases
export model_not_runnable_response, model_not_found_response, serve_chat_completions
export serve_models, slang_api_dispatch

# Offline (in-process) entrypoint — mirrors vllm/entrypoints/llm.py.
export SlangCompletion, slang_models, slang_generate, slang_release
