# GC async event loop — gc_async_mut.io.event_loop
#
# Re-exports Waker and EventLoop from the nogc_async_mut event_loop module.
# Consumers: nogc_sync_mut/io.spl

export use std.nogc_async_mut.io.event_loop.*
