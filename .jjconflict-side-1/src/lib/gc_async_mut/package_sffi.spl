"""
# GC Async Package SFFI

Compatibility facade over the no-GC sync package SFFI backend. Package archive
and filesystem operations are synchronous runtime calls, so the GC async family
reuses the shared no-GC declaration owner instead of redeclaring hooks.
"""

export use std.nogc_async_mut.package_sffi.*
