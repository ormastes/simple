# Canonical owned segmented-byte result shared by streaming emitters and the
# frame writer.  Segments are bounded by the producer that constructs them;
# total_bytes is the checked logical byte count.
struct ProviderSegmentedBytesV1:
    segments: [[u8]]
    total_bytes: i64

export ProviderSegmentedBytesV1
