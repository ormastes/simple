# Office App Themes
# Per-app accent colors building on the iOS base theme.

use common.ui.ios.theme.{ios_light, ios_dark}
use common.ui.style.{UITheme}

fn office_theme(app_name: text, dark: bool) -> UITheme:
    if dark:
        ios_dark()
    else:
        ios_light()
