"""
# sj fallback path — direct in-process request handling
"""

use std.io_runtime
use app.sj_daemon.request_handler

fn fallback_exec(handler: SjRequestHandler, argv: [text], pid: i64) -> CommandResponse:
    handle_cli_args(handler, argv, pid)
