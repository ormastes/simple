# Test: use with fully qualified local module path - access types
use app.io.mod (file_read, file_exists)
use app.ffi_gen.parser*

fn main():
    val source: text = "@Lib(lang: \"rust\", name: \"test\")\nextern class Test:\n"
    val lines = source.split("\n")
    print "Lines: {lines.len()}"

    # Test parser function
    val specs = parse_lib_externs(source, "test.spl")
    print "Specs: {specs.len()}"

main()
