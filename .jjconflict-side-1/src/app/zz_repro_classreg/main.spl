use app.zz_repro_classreg._SubStore.core.{Store, make_point_in_sub}
use app.zz_repro_classreg.plain_store.pcore.{PlainStore, make_point_in_plain}

fn main():
    val s = Store.new("a")
    val p = PlainStore.new("b")
    val q = make_point_in_sub()
    val r = make_point_in_plain()
    print "ok {s.root} {p.root} {q.x} {r.x}"
