# Display Service — owns GPU or framebuffer, exposes surface-based API via IPC.
#
# The display service mediates between the GPU driver (or framebuffer fallback)
# and clients like the compositor. Clients create surfaces, write pixels, and
# present damage regions through the DisplayService API.

use os.services.display.display_service.{DisplayService}
