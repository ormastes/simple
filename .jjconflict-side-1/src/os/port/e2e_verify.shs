#!/usr/bin/env sh
# Thin wrapper: bin/simple run src/os/port/e2e_verify.spl -- <args>
set -eu
REPO_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
cd "$REPO_ROOT"
exec bin/simple run src/os/port/e2e_verify.spl -- "$@"
