diff --git a/src/compiler/60.mir_opt/mir_opt/var_reassign_analysis.spl b/src/compiler/60.mir_opt/mir_opt/var_reassign_analysis.spl
index d3a39a72f95..687a490ecb7 100644
--- a/src/compiler/60.mir_opt/mir_opt/var_reassign_analysis.spl
+++ b/src/compiler/60.mir_opt/mir_opt/var_reassign_analysis.spl
@@ -96,81 +96,92 @@ fn local_alias_set(keys: [i64], values: [i64], local_id: i64, root_id: i64) -> (
 fn local_alias_reset(keys: [i64], values: [i64], local_id: i64) -> ([i64], [i64]):
     local_alias_set(keys, values, local_id, local_id)
 
-fn var_reassign_operand_local(operand: MirOperand) -> i64?:
+fn var_reassign_operand_local(operand: MirOperand) -> i64:
+    """Returns the local id read by `operand`, or -1 if it is a Const (not a
+    local reference at all). Uses a -1 sentinel instead of `i64?`/Option: this
+    interpreter's `.?` truthiness check on `Option::Some(0)` evaluates falsy
+    (it forwards the boxed payload as the condition instead of a real presence
+    tag), so any Option<i64> that can legitimately hold local id 0 silently
+    misreports "absent" for that one id. Local ids are always >= 0, so -1 is a
+    safe, collision-free sentinel that sidesteps the landmine entirely."""
     match operand.kind:
         case Copy(local):
-            Some(var_reassign_local_id_value(local))
+            var_reassign_local_id_value(local)
         case Move(local):
-            Some(var_reassign_local_id_value(local))
+            var_reassign_local_id_value(local)
         case Const(_, _):
-            nil
+            -1
 
 fn var_reassign_collect_escape_operand(escaped: [i64], operand: MirOperand) -> [i64]:
-    val local = var_reassign_operand_local(operand)
-    if local.?:
-        val local_id = local
+    val local_id = var_reassign_operand_local(operand)
+    if local_id != -1:
         local_list_push_unique(escaped, local_id)
     else:
         escaped
 
 fn var_reassign_collect_escape_operand_with_aliases(escaped: [i64], operand: MirOperand, alias_keys: [i64], alias_values: [i64]) -> [i64]:
-    val local = var_reassign_operand_local(operand)
-    if local.?:
-        val local_id = local
+    val local_id = var_reassign_operand_local(operand)
+    if local_id != -1:
         local_list_push_unique(escaped, local_alias_root(alias_keys, alias_values, local_id))
     else:
         escaped
 
-fn var_reassign_written_local(inst: MirInst) -> i64?:
+fn var_reassign_written_local(inst: MirInst) -> i64:
+    """Returns the local id written by `inst`, or -1 if `inst` writes no local
+    at all. Uses a -1 sentinel instead of `i64?`/Option -- see
+    `var_reassign_operand_local` for why: this interpreter's `.?` on
+    `Option::Some(0)` is falsy, so an instruction writing local id 0 (very
+    common -- it's often the first temp in a function) would be silently
+    misreported as "writes nothing" by every caller that gated on `.?`."""
     match inst.kind:
         case Const(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Copy(dest, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Move(dest, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case BinOp(dest, _, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case UnaryOp(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case CheckedBinOp(dest, _, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Alloc(dest, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Load(dest, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case GetElementPtr(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Aggregate(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case GetField(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Cast(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Bitcast(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case Call(dest, _, _):
             if dest.?:
                 val call_dest = dest
-                Some(var_reassign_local_id_value(call_dest))
+                var_reassign_local_id_value(call_dest)
             else:
-                nil
+                -1
         case CallIndirect(dest, _, _, _):
             if dest.?:
                 val indirect_dest = dest
-                Some(var_reassign_local_id_value(indirect_dest))
+                var_reassign_local_id_value(indirect_dest)
             else:
-                nil
+                -1
         case Intrinsic(dest, _, _):
             if dest.?:
                 val intrinsic_dest = dest
-                Some(var_reassign_local_id_value(intrinsic_dest))
+                var_reassign_local_id_value(intrinsic_dest)
             else:
-                nil
+                -1
         case Ref(dest, _, _):
-            Some(var_reassign_local_id_value(dest))
+            var_reassign_local_id_value(dest)
         case _:
-            nil
+            -1
 
 fn var_reassign_collect_inst_escapes(escaped: [i64], inst: MirInst) -> [i64]:
     var result = escaped
@@ -265,9 +276,8 @@ fn var_reassign_update_aliases_for_inst(inst: MirInst, alias_keys: [i64], alias_
         case Ref(dest, _, place):
             local_alias_set(alias_keys, alias_values, var_reassign_local_id_value(dest), local_alias_root(alias_keys, alias_values, var_reassign_local_id_value(place.local)))
         case _:
-            val written = var_reassign_written_local(inst)
-            if written.?:
-                val written_id = written
+            val written_id = var_reassign_written_local(inst)
+            if written_id != -1:
                 local_alias_reset(alias_keys, alias_values, written_id)
             else:
                 (alias_keys, alias_values)
@@ -283,9 +293,8 @@ fn analyze_var_reassign_blocks(blocks: [MirBlock]) -> VarReassignAnalysis:
 
     for block in blocks:
         for inst in block.instructions:
-            val written = var_reassign_written_local(inst)
-            if written.?:
-                val local_id = written
+            val local_id = var_reassign_written_local(inst)
+            if local_id != -1:
                 val local_root = local_alias_root(alias_keys, alias_values, local_id)
                 if local_count_value(local_ids, counts, local_id) > 0 and (local_list_contains(borrowed, local_id) or local_list_contains(borrowed, local_root)):
                     borrow_safe = false
diff --git a/src/compiler/60.mir_opt/mir_opt/var_reassign_ssa.spl b/src/compiler/60.mir_opt/mir_opt/var_reassign_ssa.spl
index 48831d7eded..63140194bc6 100644
--- a/src/compiler/60.mir_opt/mir_opt/var_reassign_ssa.spl
+++ b/src/compiler/60.mir_opt/mir_opt/var_reassign_ssa.spl
@@ -163,9 +163,8 @@ fn ssa_replace_operand_local(operand: MirOperand, original_id: i64, replacement_
             operand
 
 fn ssa_max_local_from_operand(current: i64, operand: MirOperand) -> i64:
-    val local = var_reassign_operand_local(operand)
-    if local.?:
-        val local_id = local.unwrap()
+    val local_id = var_reassign_operand_local(operand)
+    if local_id != -1:
         if local_id > current:
             local_id
         else:
@@ -175,9 +174,8 @@ fn ssa_max_local_from_operand(current: i64, operand: MirOperand) -> i64:
 
 fn ssa_max_local_from_inst(current: i64, inst: MirInst) -> i64:
     var max_id = current
-    val written = var_reassign_written_local(inst)
-    if written.?:
-        val written_id = written.unwrap()
+    val written_id = var_reassign_written_local(inst)
+    if written_id != -1:
         if written_id > max_id:
             max_id = written_id
     match inst.kind:
@@ -546,9 +544,8 @@ fn ssa_block_by_id(blocks: [MirBlock], id: i64) -> MirBlock?:
 fn ssa_block_written_locals(block: MirBlock) -> [i64]:
     var locals: [i64] = []
     for inst in block.instructions:
-        val written = var_reassign_written_local(inst)
-        if written.?:
-            val written_id = written.unwrap()
+        val written_id = var_reassign_written_local(inst)
+        if written_id != -1:
             locals = local_list_push_unique(locals, written_id)
     locals
 
@@ -950,9 +947,9 @@ fn ssa_reassigned_locals_for_blocks(blocks: [MirBlock]) -> [i64]:
     var counts: [i64] = []
     for block in blocks:
         for inst in block.instructions:
-            val written = var_reassign_written_local(inst)
-            if written.?:
-                val next_counts = local_count_increment(local_ids, counts, written.unwrap())
+            val written_id = var_reassign_written_local(inst)
+            if written_id != -1:
+                val next_counts = local_count_increment(local_ids, counts, written_id)
                 local_ids = next_counts[0]
                 counts = next_counts[1]
     var reassigned: [i64] = []
@@ -984,8 +981,8 @@ fn ssa_local_type_lookup(mir_locals: [MirLocal], local_id: i64) -> MirType:
 
 fn ssa_operand_push_local(locals: [i64], operand: MirOperand) -> [i64]:
     val lo = var_reassign_operand_local(operand)
-    if lo.?:
-        local_list_push_unique(locals, lo.unwrap())
+    if lo != -1:
+        local_list_push_unique(locals, lo)
     else:
         locals
 
@@ -1062,8 +1059,8 @@ fn ssa_collect_term_operand_locals(locals: [i64], term: MirTerminator) -> [i64]:
 fn ssa_local_defined_in_block(block: MirBlock, local_id: i64) -> bool:
     for inst in block.instructions:
         val written = var_reassign_written_local(inst)
-        if written.?:
-            if written.unwrap() == local_id:
+        if written != -1:
+            if written == local_id:
                 return true
     false
 
@@ -1291,16 +1288,21 @@ fn ssa_alloca_apply_def_store(inst: MirInst, slot_keys: [i64], slot_values: [i64
     """If `inst` defines a reassigned local, redirect its dest to a fresh
     temp and append a Store of that temp into the local's stack slot.
 
-    NOTE: written uses the POSITIVE `if written.?:` form only. The negated
-    `if not written.?: return ...` form mis-evaluates on struct-boxed
-    optionals in the bootstrap seed interpreter (documented quirk shared by
-    ssa_block_can_reach above and every MirBlock? check in this file): it
-    early-returned on EVERY def, so no Store was ever emitted -- 5 allocas +
-    5 loads + ZERO stores -- leaving each reassigned local's duplicate defs
-    intact for llc to reject ("multiple definition of local value")."""
-    val written = var_reassign_written_local(inst)
-    if written.?:
-        val written_id = written.unwrap()
+    NOTE: `var_reassign_written_local` returns a plain i64 (-1 = "no local
+    written"), NOT an `i64?`/Option. An Option<i64> here would hit a second,
+    worse landmine: this interpreter's `.?` on `Option::Some(0)` evaluates
+    falsy (it forwards the boxed payload as the truthiness check instead of a
+    real presence tag), in BOTH the positive and negated form -- so a def of
+    local id 0 (very common; it's often the function's first temp) would
+    silently skip the Store below, leaving that slot's alloca permanently
+    uninitialized (loads read stack garbage/zero). See
+    `var_reassign_operand_local` in var_reassign_analysis.spl for the same
+    landmine and fix pattern. The comment this replaced documented a *related*
+    negated-form bug (fixed by switching to the positive form); the -1 sentinel
+    fixes both that bug and the zero-payload one at the root, for every local
+    id including 0."""
+    val written_id = var_reassign_written_local(inst)
+    if written_id != -1:
         val idx = local_count_index(slot_keys, written_id)
         if idx >= 0:
             val slot_id = slot_values[idx]
