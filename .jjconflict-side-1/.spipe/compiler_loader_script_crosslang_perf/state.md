# Feature: compiler_loader_script_crosslang_perf

## Raw Request

`$sp_dev with pherallel agents with guide and higher model review complete the plan doc`

## Task Type

todo

## Refined Goal

Complete the canonical compiler-loader packed-byte performance plan so every acceptance item has an authoritative artifact, one-pass verification command or explicit blocker/resume contract, current operator guidance, and independent higher-capability review.

## Acceptance Criteria

- AC-1: `doc/03_plan/sys_test/compiler_loader_script_crosslang_perf.md` defines the full lane scope, requirement traceability, authoritative implementation/spec/manual artifacts, acceptance matrix, exact one-pass verification commands, three-cycle cap, and honest PASS/WARN/BLOCKED decision rules.
- AC-2: Every packed-byte boundary, mutator write-back, foreign-capability, loader negative-cache, provenance/mode, checksum/timing/RSS, compiler/core/lib, MCP/LSP, integration, and clean-sync item is classified as proved, contradicted, missing, or blocked from current evidence; no unchecked box is presented as completed.
- AC-3: Every unavailable gate retains its acceptance IDs plus prerequisite, exact resume command, expected retained artifact, owner, and final reviewer; unavailable self-hosted tooling is never replaced with the Rust seed.
- AC-4: The plan names the existing manual-facing `step("...")` phrases and canonical setup/checker helpers, requires any temporary placeholder to use `assert(false)` or `fail(...)`, and records generated-manual review ownership.
- AC-5: The plan gives every knowledge artifact an honest current disposition and blocker handoff: research/options and decision-ready architecture/design drafts are present; user-selected requirements, post-selection acceptance, and generated-manual evidence remain blocked rather than being called current. The system plan, guide, feature/layer expert knowledge, and concrete `doc/08_tracking/bug/` records identify exact owners, file/line anchors, and unblock conditions. Workflow skill/agent/command trees are `N/A` because this planning lane does not change their contract.
- AC-6: Parallel lower-model audits cover plan/evidence and guide/wiki freshness; the merge owner resolves their findings; a separate highest-capability reviewer accepts the merged plan, generated-manual disposition, exclusions, blockers, and done-state honesty.
- AC-7: Focused plan-quality checks, generated-spec layout guard, and direct-env runtime guards run once; only intentional lane files are committed and unrelated concurrent files remain untouched.

## Scope Exclusions

The current continuation explicitly excludes Stage 4 construction/execution and does not substitute Stage 2/3 or Rust-seed evidence for it. It does not claim missing runtime performance evidence, release, or absorb unrelated GUI/web/2D work. It may complete Rust-level semantic coverage and plan/research/design/manual metadata while retaining all deployed-CLI gates as blocked.

## Cooperative Review

- Lower-model sidecars: `plan_evidence_audit` and `guide_wiki_audit`.
- Merge owner: root Codex session for this worktree.
- Final reviewer: separate `gpt-5.6-sol` high-reasoning agent after merge.
- Shared feature/interface name: `compiler_loader_script_crosslang_perf`.
- Manual step helpers: the nine existing phrases in `test/05_perf/compiler_loader_script_crosslang_perf_spec.spl`; no new vocabulary without plan review.
- Existing setup/checker helpers: `check-cross-language-perf` and `check-file-exists-probe-c`. Historical `check-compiler-loader-perf` and `check-interpreter-packed-byte-rss` names are absent on current `main` and remain rejected operator commands unless implemented with fail-closed contracts.
- Fail-fast placeholders: `assert(false)` or `fail(...)` only.
- Generated-manual review owner: final higher-capability reviewer; the merge owner applies required corrections.

### Review receipts

The receipts through cycle 3 bind historical revision `3fdfa0d3351`. Fresh
parallel audits by `/root/traceability_audit` and `/root/operator_audit` found
post-review operational drift. Their findings were reconciled, two narrow
parallel checks accepted the command/status corrections, and
`/root/reconciled_plan_review` (`gpt-5.6-sol`, high) returned `ACCEPT` after one
host-matrix correction.

- `/root/plan_evidence_audit` (`gpt-5.6-luna`, high): read-only acceptance/evidence audit. Verdict: gaps found in Stage 4 admission, packed-byte coverage, traceability, and generated-manual provenance. Disposition: merged into the matrix and blocker ledger.
- `/root/guide_wiki_audit` (`gpt-5.6-luna`, high): read-only guide/wiki/manual audit. Verdict: stale completion language and incomplete manual status. Disposition: merged into the operator documents.
- `/root/high_model_plan_review` (`gpt-5.6-sol`, xhigh), cycle 1: `REJECT`. Required normalized evidence verdicts, literal commands, concrete tracking records, review receipts, and non-premature completion/manual wording. Disposition: corrections in progress; one bounded re-review required.
- `/root/high_model_plan_review` (`gpt-5.6-sol`, xhigh), cycle 2: `REJECT`. Four residual findings covered layer wording, exact packed-boundary anchors, a VCS-01 ledger row, and exact interactive research/design invocations. Disposition: all four corrected; final cycle-3 verdict pending.
- `/root/high_model_plan_review` (`gpt-5.6-sol`, xhigh), cycle 3: `ACCEPT`. Verdict: the plan is complete and honest as a planning document; feature verification and the named evidence/artifact gates remain BLOCKED.

## Phase

dev

## Log

- 2026-08-14: fresh `r4` stopped before Stage 2 admission because the
  formal-verification HIR consumer lacked its flat-AST contract producer. The
  coherent parser/AST/constructor slice is restored for the final bounded,
  source-frozen attempt; `r4` remains failed evidence.
- 2026-08-14: final bounded `r5` admitted Stage 2 (`858/0/0`, sanity pass,
  candidate SHA-256 `d2ed1d54673bc4cc848024ebbc229a873053dc315d8412613184bfdc5faec947`).
  Stage 3 again produced no candidate while one core and RSS grew to about
  19.8 GiB in 143 seconds. It was stopped before host OOM; Stage 4 and live
  feature evidence remain BLOCKED. Three fix cycles are exhausted: WARN.
- 2026-08-14 continuation: three distinct Stage 3 diagnostics showed the
  surface-registry class restoration and inline scalar lookup did not change
  the pre-HIR allocation slope. The final GDB sample proved a growing
  `convert_flat_stmt` recursion while parsing the 97-arm backend ASCII elif
  table. That table now uses the equivalent `char_code` range check, but the
  bounded-cycle guard defers its Stage 3 verification. Status remains WARN.
- 2026-08-14 continuation 2: an authorized three-cycle r5 resume showed that
  the ASCII rewrite and flattened 41-arm expression dispatcher do not clear
  parse progress 128. The admitted closure contains at least nine other
  19..52-arm chains; the durable owner is recursive if/elif parser/bridge
  representation and requires a fresh Stage 2. Stage 3/4 remain BLOCKED/WARN.

- dev: Created state file with 7 acceptance criteria (type: todo).
- plan: Merged lower-model plan/evidence and guide/wiki audits; at that historical revision research/requirements/architecture and packed-byte design/evidence were incomplete, and the deployed Stage 4 candidate plus manual docgen remained blocked.
- review-1: Highest-capability reviewer rejected the draft; applying all six correction groups before the single re-review.
- review-2: Highest-capability reviewer found four residual exactness gaps; corrected them and requested the final bounded cycle-3 review.
- review-3: At that historical review revision, highest-capability review accepted the corrected plan while operational gates, commit, and integration still remained.
- reconcile: Fresh parallel audits identified the lane-A/lane-B receipt error, missing Build11 Stage3 resume row, projected-place command gap, and conflated failure frontiers; all were corrected before final review.
- reconcile-review: Command/receipt check accepted; status check's one historical-wording finding was fixed; higher-capability review accepted the current reconciliation after the runnable Linux row required both GNU time and `timeout`.
- gates: SPipe wiring, `doc/06_spec` layout, and working/staged direct-env guards passed once. Workspace-root strict audit reported 137 pre-existing unrelated manifest violations; retained as WARN without broadening scope.
- post-sync-regression: Rebasing onto `7ac900316dd5` exposed a red packed-byte semantic test. Bare mutable packed bytes no longer enter the empty-projection place route, and the driver cases now use the direct interpreter rather than the SMF `run_code` path. At that point focused results were 1/1 and 4/4 while concat/clone/equality, projected-place, foreign-lifetime, and self-hosted gates remained blocked; the next log entry supersedes the first two gaps.
- stage4-excluded-continuation: At this historical revision PBL-01/02 remained evidence-process BLOCKED and PBL-03 remained blocked by the pointer-returning ABI. Later receipt and atomic-cutover entries supersede those dispositions. Research/options, an architecture draft, task breakdown, and expanded design/manual mapping existed; user selection and admitted docgen remained mandatory blockers. Three bounded Stage 3 recovery cycles still ended at exit 139 without an observed lowering-entry canary.
- continuation-review: Highest-capability review cycles 1-2 rejected missing red receipts and stale/factually inconsistent status text. Cycle 3 accepted the corrected continuation with PBL-01/02 evidence-process BLOCKED, PBL-03/DOC/Stage3/deployed-CLI blockers explicit, and no Stage 2/3 substitution for excluded Stage 4.
- pbl-red-evidence: PBL-01 and PBL-02 now retain named status-101 deliberate-red receipts in `doc/09_report/compiler_loader_packed_byte_deliberate_red_evidence_2026-08-14.md`; both oracle mutations were restored exactly and the authoritative green criteria were not rerun.
- post-integration: `0943ce963f05107046937456f8570b957aa939e5 WARN` was pushed under the lane lock and remains reachable from refreshed `origin/main`; two unrelated untracked GUI reports remain excluded.
- receipt-reconciliation: PBL-01/02 are now proved at the Rust interpreter boundary by restored green tests plus retained status-101 oracle receipts. The current report, PBL-03 inventory, and post-push wording require their own final commit/integration; the previous `0943ce... WARN` baseline remains reachable.
- receipt-review: Highest-capability cycle 1 rejected stale historical PBL and premature current-VCS wording; cycle 2 accepted the corrected Rust-boundary claims, atomic PBL-03 blocker, Stage-4 exclusion, and operationally pending integration.
- pbl03c: HashSet removed all three stored `rt_array_data_ptr_u8` addresses in favor of bounded occupancy indexing/clear. Stage 2 native-built the focused spec (3 compiled, 0 failed), but execution remains unavailable. At this historical point eight owners/23 uses remained; the later atomic-cutover entry supersedes that inventory.
- pbl03c-review: Highest-capability cycle 1 accepted bounded semantics/complexity, non-vacuous coverage, compile-only Stage-2 wording, the 8-owner/23-use/1-stored-address inventory, and absence of reverted PBL-03A/B prototypes.
- pbl03c-optimizer: One admitted Stage-2 attempt to native-build `src/app/optimize/main.spl` with isolated cache timed out at 180 seconds (exit 124), producing no candidate/output. It was not retried; optimizer/performance evidence remains blocked.
- pbl03c-optimizer-review: Highest-capability narrow cycle 2 accepted the timeout receipt and continued BLOCKED optimizer/performance disposition.
- lane-b-bootstrap-cycle-1: Fresh Rust authority completed, but Stage 2 exposed
  a partially removed retained-contract HIR producer/type slice. The rebase
  reconciles with upstream's complete producer and resolver behavior.
- lane-b-bootstrap-cycle-2: Stage 2 passed; Stage 3 rejected fourteen untyped
  bare-zero backend constants. Each now has an explicit `i64` annotation.
- lane-b-bootstrap-cycle-3: Stage 2 passed and the fourteen errors cleared;
  Stage 3 received external SIGTERM (143) after source indices 0..2. The wrapper
  has no Stage 3 timeout, so earlier bounded-timeout wording was unproved.
- pbl03-atomic: Eight owners/23 raw-pointer uses migrated to typed family adapters plus scoped DynLib dispatch; native codegen retains RuntimeValue owners, compatibility definitions/callers are removed, and the old escape symbol has zero positive non-document references. Scoped tests pass 4/4; native-only readback dispatch and restored owner/escape ABI guards pass 1/1 each. A retained status-101 inverted guard proves old-symbol rejection. Apple-target checking stopped before this crate in the Linux host C toolchain, so Metal remains WARN and Stage 4 remains excluded.
- pbl03-atomic-review: Highest-capability cycle 1 rejected clone-and-discard interpreter readback handlers. They were removed and explicitly kept native/JIT-only; cycle 2 ACCEPT found no remaining merge blocker and retained only the real-macOS compile WARN.
- pbl03-integration: Atomic implementation `2249dc49ac4e2d8f6d63626432fc8d7be6a12966` was pushed under `/tmp/simple-main-restart12-push.lock` after rebasing onto fetched main and is reachable from refreshed `origin/main`. Final bookkeeping follows the same locked sequence; unrelated GUI reports remain excluded.
- lane-b-stage3-long-resume: A detached resume stayed CPU-bound but grew from
  about 7.4 GiB to 20 GiB RSS after source 2 post-store with no further marker
  or I/O progress; it was stopped before host OOM. Localized the interval to
  aggregate-by-value module-surface lookup and replaced its normal path with
  scalar name/index resolution plus physical validation and a scalar-prefiltered
  compatibility fallback. Stage 3/4 remain pending the fresh source-frozen run.
- pbl03-integration: Final bookkeeping baseline `56448da2b25bbe90523ad672b25db2abaef74a67` was pushed under `/tmp/simple-main-restart12-push.lock` and remains reachable from refreshed `origin/main`; unrelated GUI reports remain excluded.
- pbl03-simpleos-extension: Refreshed-origin review found and removed the remaining SimpleOS filesystem/socket raw-byte pointer route. Eight operation-specific owner ABIs, production checked RuntimeValue decoding, atomic readback, caller migration, complete source/sysroot contracts, and compiler ABI guards are present. Stubbed cleanup/bounds and production-runtime decoding/copyback C tests pass; compiler/common guards pass 1/1 each. Stage 2 built all 18 pure-Simple core parts, and a host-target link selecting that archive's strong checked-byte owner passed the production runtime test. The final Stage-2 source-contract build completed 1 compiled/42 cached/0 failed. The broad Stage-2-generated archive still has legacy undefined pointer references in discarded sections, and an earlier intermediate artifact ended at known duck-dispatch status 132; therefore actual SimpleOS target archive/link/runtime evidence remains WARN. Final review accepted the scoped proof and integration completed at the later bookkeeping receipt.
- stage2-docgen: One bounded Stage-2 attempt failed immediately with `unknown command 'spipe-docgen'`; generated-manual provenance remains Phase-4-dependent and is not inferred.
- pbl03-simpleos-integration: `aef64fb1951136fbb98521ce1a67643207752a26` was rebased, pushed under `/tmp/simple-main-restart12-push.lock`, fetched again, and proved reachable from `origin/main`. Final bookkeeping and the lane WARN marker follow the same locked sequence.
- stage2-optimizer-audit: A distinct reduced entry closure built the real `optimize_full_analyze` path with admitted Stage 2 (55 compiled, zero failed, 13.5 seconds) and analyzed each of the four touched Simple files once. All runs exited 0; advisory scanner findings were dispositioned without unsafe source edits. This closes the optimizer-audit item only, not Stage 3/4 or live performance.
- stage3-receipt-prerequisite: Upstream bootstrap policy now requires a planner-produced receipt. The available release CLI exits 139 while producing it, so the stale receipt-less r3 command must not be repeated; focused planner-crash localization precedes one new debugger-backed Stage-3 diagnostic cycle.
- stage3-planner-localization: GDB localized the receipt crash before planner entry: hosted C argv belongs to a private registry, selected pure-Simple `rt_slice` returns nil `3`, and `handle_build` dereferences it. A three-cycle selected-owner bridge prototype was rejected because duplicate global new/push relocations could still bind to the C owner under whole-archive linking; it was removed. Next work requires private non-interposable pure-owner helpers plus a registry-sensitive production-link test.
- optimizer-planner-evidence-integration: `30f4de92f04d9c7717db9c3678eaa9f1bd7b9334` was rebased, pushed under `/tmp/simple-main-restart12-push.lock`, fetched again, and proved reachable from `origin/main`. It retains the completed Stage-2 optimizer audit and the exact unresolved Stage-3 planner prerequisite; the lane remains WARN.
- stage3-argv-private-owner-cycle: The private-helper argv bridge proved private ownership for empty/direct-push cases under production whole-archive linking, but populated argv still segfaulted because Stage 2 lowered both fail-closed push comparisons to undefined `rt_native_eq`. The 18-part archive built cleanly; the rejected prototype was removed at cycle 3. Next work must admit/prove `rt_native_eq` (or an equivalent already-supported fail-closed comparison) before reapplying the bridge.
- stage3-argv-blocker-integration: `a137d7ff2459a728e8891321bdde9b155ed32378` was integrated under the lane lock and is reachable from refreshed `origin/main`; it contains only the reviewed blocker evidence because the rejected prototype was removed.
- stage3-argv-owner-closure: A fresh 18-part Stage-2 archive and fail-closed whole-archive regression prove populated argv, exact strings, all count/raw/text/array aliases, private pure ownership, poisoned-public-provider rejection, and rooted `rt_native_eq`. The accepted implementation remains; non-ELF platforms are WARN.
- stage3-planner-cycle3: The current planner build fixed one Stage-2-incompatible mixed conditional, resumed 370 cached artifacts, compiled the full 856-file closure, then failed link on four undefined `defer_unsupported_marker` references. Inspection found its signature appended to a comment; the exact header is restored but not link-verified because the three planner-build cycles are exhausted. No binary or receipt exists.
- stage3-argv-owner-integration: `bfac2de350d5138ac62390c9afffbc13233bcfc6` was integrated under the lane lock and proved reachable from refreshed `origin/main`. It contains the accepted argv-owner/runtime regression and honest planner-link blocker evidence; the lane remains WARN.
- stage3-planner-authority-cycle: Three bounded current-planner cycles exhausted. Cycle 1 populated the refreshed cache and timed out without compiler error; cycle 2 reached link and exposed missing real memory-snapshot providers in the retained Rust bootstrap authority. The Linux Rust provider and focused test now pass, and its isolated archive exports all three symbols; non-Unix ABI behavior remains WARN. Cycle 3 advanced to one undefined `rt_array_data_ptr_u8`, emitted by the retained r2 compiler for the current linker byte-write path. The removed unsafe ABI was not restored. No planner, receipt, or Stage-3 run exists; next work requires one coherent compiler/runtime/source authority and a reviewed Stage-3-only stop path.
- stage3-coherent-ifchain-continuation: Coherent current Rust authority and the historical v1 Stage-3 receipt were proved before admission v2 superseded that receipt. The stop route's ordering and fail-closed guards are structurally accepted, but positive v2 execution is unproved. Pre-IfChain Stage 2 admitted 859/0, while two Stage-3 diagnostics hit the 24 GiB guard with exit 139. Per-file phase evidence localizes the active parse to the 28-arm pattern_dispatch chain; ast_stmt was only the last coarse completion label. Upstream flat IfChain directly targets the reverse-fold defect. Its broad focused seed test timed out and is not PASS; one final fresh current-source Stage2/3-only cycle remains.
- stage3-admission-v2-review: Upstream v2 policy invalidated the earlier v1 receipt before any new stage started, so the final Stage-3 cycle remains unused. A producer/replay prototype passed its focused contract but independent security review rejected its forgeable Stage-2 transcript authority and incomplete source freeze. The prototype was removed; a canonical independently verified Stage-2 admission manifest is now the prerequisite.
