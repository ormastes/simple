<!-- codex-research -->

# HTML/CSS Binary Caching Agent Tasks

1. Add shared render cache/profile helpers to `src/lib/common/ui/web_render_api.spl`.
2. Update shared web render unit coverage for static/dynamic classification and IPC full-HTML reuse.
3. Add optional persistent static-shell cache with hot memory hit path.
4. Add compact static-shell binary-plan artifact for the next DOM/style/layout encoding slice.
5. Add decoded layout payload fields for viewport, command slots, style slots, text runs, and payload byte estimate.
6. Add prepared SWBC reuse path for frame-hot static shells.
7. Add retained static draw-command list for prepared SWBC payloads.
8. Add GTK size/speed comparison script with graceful unavailable reporting, static-cache timing, prepared SWBC timing, retained command bytes, and static-shell size evidence.
9. Run targeted checks and record GTK comparison output.
10. Follow-up: design full binary DOM/style/layout storage and retained scene invalidation once this API slice is stable.
