# P2/P3 Bug Sweep — 2026-08-17

## Scope snapshot

Authority: `doc/08_tracking/bug/bug_db.sdn` at `ca1430bf6c` plus the pushed
off-host/Todo follow-up `e405a89ce6`. The structured `bugs_active` and `bugs`
tables contain nine valid P2/P3 rows: six fixed or resolved and exactly three
open P2 rows. They contain zero open P3 rows. Free-form `P2`/`P3` mentions in
the 2,651 historical Markdown records are not database status.

| Bug ID | Priority | Current status | Owner | Exact evidence | Lane |
|---|---:|---|---|---|---|
| `match_bare_ident_const_irrefutable` | P2 | open | MIR match lowering | tracked bug reproducer | `p2_match_const` |
| `text-split-limit-ignored` | P2 | open | text method dispatch across pure/native boundaries | `nested_string_split_spec.spl` | `p2_text_split` |
| `native_project_src_std_platform_alias_owner_loss_2026_08_03` | P2 | open | native-project import ownership | tracked bug reproducer | `p2_native_alias` |

## Resolved-row audit

The remaining six canonical P2/P3 rows are excluded from source edits because
their structured states are `fixed` or `resolved-duplicate`: Caret terminal
recovery, DevHub stdout color policy, Confluence real file I/O, nested JSON
duplicate classification, first-colon config parsing, and GPU backend evidence
reason preservation. Reopening any requires a contradictory current reproducer.

## Acceptance sequence

1. Claim each open record before editing and retain one exact failing result.
2. Prove the pure-Simple call path before editing Rust/runtime code.
3. Repair the first faulty owner, not a downstream symptom.
4. Add the exact regression and one adjacent/similar root-cause detector.
5. Update the structured BugDB state only after source and focused evidence land.
6. Refresh the matching bug record and feature/layer expert knowledge.
7. Merge owner runs scoped policy gates, rebases, and pushes one bounded batch.

## Cooperative ownership

- Sidecars: `p2_match_const`, `p2_text_split`, `p2_native_alias`.
- Merge owner: root Codex.
- Final reviewer: root Codex at normal/highest capability.
- Shared planning names: `P2P3BugInventoryEntry`,
  `p2_p3_bug_reproduction`, `p2_p3_adjacent_regression`.
- SSpec manual helpers: N/A unless a lane introduces a scenario-oriented spec.
- Fail-fast scaffolds: `assert(false)` or `fail(...)`; none may ship.

## External rows

N/A. The structured canonical inventory has no open P2/P3 row whose fix is
hardware-only. If a lane discovers an external acceptance dependency, it must
add a Todo DB row with the exact host, resume command, retained artifacts,
owner, and reviewer without closing the bug.
