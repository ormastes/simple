# mcpgdb Agent Tasks

1. Create self-contained MCP example package under `examples/mcpgdb`.
2. Implement shared JSON/protocol helpers and tool schemas.
3. Implement multi-session debug state and FIFO/log-backed backend control.
4. Implement gated raw-command rules and structured debug tools.
5. Implement workspace-scoped clangd launch and request handling.
6. Add unit/system tests and feature docs.
