<!-- codex-design -->
# SPipe Process Harness TUI Design

The user-facing TUI surface is a compact HUD line:

`model=<model> | jj=<worktree> | commit=<commit> | hr=<remaining> | week=<remaining> | goal=<goal>`

This can be embedded into existing CLI status surfaces without adding a full dashboard view.

