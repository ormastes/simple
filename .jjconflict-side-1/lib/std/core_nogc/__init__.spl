# Core NoGC Module - Variant-agnostic, explicit #[no_gc] utilities
# These types work without garbage collection

#[no_gc]
#[variant(any)]
mod core_nogc

export use arena.*
export use fixed_vec.*
export use fixed_string.*
export use bump.*
