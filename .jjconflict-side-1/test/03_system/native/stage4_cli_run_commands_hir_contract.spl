# Stage 4 HIR ownership regression for CLI run/fix command dependencies.

use app.io._CliCommands.run_commands.{cli_run_code}

extern fn rt_exit(code: i32)

fn main() -> i64:
    # Importing the real run-command owner lowers raw stdout and lazy fix-rule
    # dependencies without executing a child process in this probe.
    rt_exit(30)
    0
