struct Point:
    x: i64
    y: i64

fn main() -> i64:
    var m: Dict<text, Point> = {}
    m["k1"] = Point(x: 30, y: 40)
    var key_ok = 0
    for k in m.keys():
        if k == "k1":
            key_ok = 3
    return m["k1"].x + m["k1"].y + key_ok
