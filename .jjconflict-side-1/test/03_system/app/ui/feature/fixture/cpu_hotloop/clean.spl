# Fixture: the same loop shape, but annotated — must NOT be flagged.
fn paint_row(buf: [i32], w: i32):
    var x = 0
    while x < w:  # cpu-lane-loop-ok: fixture bulk-fill placeholder
        buf[x] = 0
        x = x + 1

fn no_loops_at_all(a: i32, b: i32) -> i32:
    a + b
