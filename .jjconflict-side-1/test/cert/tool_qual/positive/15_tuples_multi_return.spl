# Tool-qual positive case: tuple type + multi-value return + tuple destructure.
# Hand-computed: 17/5=3 remainder 17-3*5=2 -> divmod(17,5)=(3,2).
fn divmod(a: i64, b: i64) -> (i64, i64):
    (a / b, a % b)

fn main():
    val (q, r) = divmod(17, 5)
    print(q)   # 3
    print(r)   # 2
