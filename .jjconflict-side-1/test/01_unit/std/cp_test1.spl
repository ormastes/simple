use std.config_parser.{parse_sdn_config}

fn test_main():
    val content = "section1:\n  key1: val1"
    val result = parse_sdn_config(content)
    if result.sections.len() == 1:
        print "PASS: sections.len() == 1"
    else:
        print "FAIL: sections.len() == {result.sections.len()}"

test_main()
