use std.concurrent.cooperative_green.{cooperative_green_spawn}

fn main():
    var local_count = 0
    val handle = cooperative_green_spawn(\:
        local_count = local_count + 1
        local_count)
    print handle.join()
