extern fn stdin_read_char() -> text

fn main() -> i64:
    var input = ""
    while true:
        val ch = stdin_read_char()
        if ch == "":
            break
        input = input + ch
    print_raw("inherited:" + input)
    23
