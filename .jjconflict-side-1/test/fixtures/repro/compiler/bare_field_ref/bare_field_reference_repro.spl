class Desc:
    xlen: i32
    name: text

    me bare() -> i32:
        return xlen

    me qualified() -> i32:
        return self.xlen

fn main():
    val d = Desc(xlen: 32, name: "rv32")
    print("qualified=" + d.qualified().to_text())
    print("bare=" + d.bare().to_text())
