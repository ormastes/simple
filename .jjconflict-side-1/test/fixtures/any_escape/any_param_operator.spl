# Fixture: a REAL bare `Any` parameter compared with `!=` in a safe function.
# Neighbour of imported_optional_param: the same operator shape on a genuine
# erased value must still report the origin (001) and the operator escape (002).
fn is_set(raw: Any) -> bool:
    raw != nil
