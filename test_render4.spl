use common.ui.parse.sdn.{read_file, render_source}

fn main():
    val source = read_file("examples/ui/demo.ui.sdn")
    val result = render_source(source)
    match result:
        case Ok(html):
            print html
        case Err(e):
            print "Error: {e}"
