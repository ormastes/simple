use std.pure.tensor.{PureTensor}

fn main():
    val t = PureTensor.zeros([2, 3])
    print("zeros numel=" + t.numel().to_text())
    val u = PureTensor.from_data([1.0, 2.0, 3.0, 4.0], [2, 2])
    print("from_data numel=" + u.numel().to_text())
