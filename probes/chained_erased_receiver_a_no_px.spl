# Variant A-no-Px: identical to chained_erased_receiver_a_with_px.spl with the
# `Px` struct (and its `to_i32` method) deleted. Sole difference.
fn main():
    val s: text = "A5"
    val chained = s.char_code_at(0).to_i32()
    print("A chained=" + chained.to_text())
