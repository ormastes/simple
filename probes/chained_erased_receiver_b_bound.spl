# Variant B: typed intermediate binding instead of chaining, Px still present.
struct Px:
    value: i32

    fn to_i32() -> i32:
        self.value

fn main():
    val s: text = "A5"
    val code: i64 = s.char_code_at(0)
    val bound = code.to_i32()
    print("B bound=" + bound.to_text())
