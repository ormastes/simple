#!/bin/sh
set -eu

T32REM="${T32REM:-/opt/t32/bin/pc_linux64/t32rem}"
HOST="${1:-localhost}"
API_PORT="${2:-20000}"
GDB_PORT="${3:-2331}"

"$T32REM" "$HOST" "port=$API_PORT" "SETUP.API.GDB.Enable /PORT $GDB_PORT"
"$T32REM" "$HOST" "port=$API_PORT" "System.Option GDBSERVER ON"
