# jit_closure_blockers_baseline.txt — PRE-EXISTING debt, NOT approval.
#
# Format: `<path> <count>`. The count is COUNTED, not just allowed: finding
# MORE than <count> blockers in a listed file still FAILS. A fifth lambda added
# to a four-lambda file cannot hide behind this file.
#
# Removing a line is how debt is retired. Adding one requires editing this file
# in the same commit as the code, which is the reviewable signal.
#
# Do NOT add an entry to silence a new lambda you just wrote. Every entry needs
# a justification comment stating why no source-level remedy exists.

# 4 capturing lambdas building the `UiAccessPersistence` port.
#
# WHY THIS CANNOT BE FIXED IN SOURCE (verified 2026-08-17):
#   `UiAccessPersistence` is declared in `src/lib/nogc_sync_mut/ui/session.spl`
#   as a record of four function values — a deliberate DEPENDENCY-INVERSION
#   port. `session.spl` imports no Database/SQLite symbol (checked: none of its
#   27 `use` lines references `database` or `access_store`), and
#   `access_store.spl` imports `session.spl`. Holding the store handle directly
#   in the port would therefore require `session.spl` to import
#   `access_store.spl` — a circular import.
#   The four lambdas each capture the `store` handle, so they cannot be replaced
#   by named function references; and per the header of
#   `check-jit-closure-blockers.shs`, a named fn ref passed as a value SILENTLY
#   MISCOMPILES to an address-shaped garbage value under the JIT. Converting
#   would trade a performance blocker for a correctness bug.
#   The real defect is upstream: the Rust seed JIT refuses any module containing
#   a `MirInst::ClosureCreate`
#   (src/compiler_rust/compiler/src/codegen/jit.rs:196), per-MODULE and ignoring
#   reachability. Retire this entry by fixing that, not by editing the port.
#   See doc/08_tracking/bug/jit_closure_abi_refuses_lambdas_and_miscompiles_fn_refs_2026-08-06.md
src/lib/nogc_sync_mut/ui/access_store.spl 4
