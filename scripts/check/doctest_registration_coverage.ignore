# Ignore list for check-doctest-registration-coverage.shs
# Format: <glob><TAB><reason>. A reason is MANDATORY; an entry with none is a
# hard ERROR. Over-broad patterns (*, **, *.md, *.spl) are refused. An entry
# that matches nothing is STALE and FAILs the check -- ignore lists silently
# widen over time otherwise.
#
# (empty: no entries adjudicated illustrative yet)
