# Types checked by scripts/check/check-signature-type-import-provenance.shs
#
# Format: <TypeName> <declaring-module-path>
#
# A module that names one of these types in SIGNATURE / annotation position must
# reach it through a REAL module-level import statement -- not a glob (`use X.*`),
# not a line inside a docstring, not a commented-out line. The imported-surface
# projection accepts only a declaration, a re-export hop, or an explicit import as
# an ORIGIN; a glob is none of the three, so the projected signature carries no
# origin and the IMPORTER hard-errors with a synthesized span (span_file empty,
# span_line 0). See doc/08_tracking/bug/hir_unresolved_type_owner_missing_import_2026-08-22.md
# follow-up (i).
#
# Add a row here when a new type is found in this class. Rows are cheap; the scan
# is a few seconds.
HirModule compiler.hir.hir_types
CompiledModule compiler.backend.backend.backend_types
