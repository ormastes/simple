# Evidence receipts required for a CRITICAL release (hardening plan Phase 9).
#
# One check id per line. Each id must have a FRESH receipt in build/evidence/
# bound to the current artifact hash and seal hash, carrying that gate's own
# PASS verdict line. A missing entry is not a pass -- it is the Phase 9
# backlog, and check-critical-release-seal.shs names it.
#
# Ids that already have a gate wired to scripts/check/lib/emit_receipt.shs:
check-compiler-transition-coverage
check-critical-wildcard-ban
check-any-escape-census
check-post-mono-invariants
check-closed-match-coverage
check-completeness-seal
check-aspect-seal
check-compiler-schema-fresh
check-generated-visitors-fresh
#
# Release obligations whose gate is NOT YET wired to emit_receipt.shs (or does
# not exist at all). They are listed deliberately: a required receipt with no
# producer reports as `missing`, which is the honest state and keeps the
# obligation visible instead of letting the aggregate look green because
# nobody wrote — or nobody wired — the checker.
#
#   seed/self-host parity diff 0
#     Producer EXISTS but is unwired: scripts/check/check-native-seed-parity.shs
#     (owned by another lane; wiring it is one additive line).
check-native-seed-parity
#   interpreter/JIT/native diff 0
#     Producer EXISTS but is unwired: scripts/check/check-engine-differential.shs
#     (likewise owned elsewhere and under active edit).
check-engine-differential
#   reachable unsupported 0
#     No producer anywhere in scripts/check/ — this one still has to be written.
check-reachable-unsupported
