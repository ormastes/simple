# seed-native-build-invariant fixture, half 1 of 2 (owner module).
# The reader (main.spl) resolves this module-level global THROUGH a `use`
# import, so a correct seed must give the importer the SAME shared storage,
# not a fresh/empty instance. See owning bug:
# doc/08_tracking/bug/seed_interp_defer_lazy_imports_module_globals_2026-07-24.md

val ITEMS: [i64] = [2, 3]

fn owner_items() -> [i64]:
    ITEMS
