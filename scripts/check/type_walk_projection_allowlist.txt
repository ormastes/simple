# Parser TypeKind constructors that the MATERIALIZATION walk
# (`parser_type_named_dependencies`, 10.frontend/parser_types_expr.spl) handles
# and the PROJECTION walk (`imported_surface_type`,
# 20.hir/hir_lowering/_Items/module_callable_types.spl) deliberately does NOT.
#
# Every line is `Constructor  reason`. A constructor may sit here ONLY with a
# measured reason. Adding a constructor to the materialization walk without
# either projecting it or adding a line here is the exact failure this guard
# exists to stop -- see the "durable lesson" in
# doc/08_tracking/bug/hir_unresolved_type_owner_missing_import_2026-08-22.md.
#
# A line here whose constructor IS projected is a STALE allowlist and also
# fails: a list that no longer describes the tree is how a ratchet silently
# stops ratcheting.
Function    probed with the reproduce-spec shape, measured 0 unresolved-type errors (follow-up (c), 2026-08-22)
Optional    probed, 0 errors; a speculative fix measured byte-identical results and was reverted
Reference   probed, 0 errors
Atomic      probed, 0 errors
Isolated    probed, 0 errors
Projection  probed, 0 errors
Weak        HIR has no Weak kind; `lower_type` erases `-T` to Infer without erroring, so there is nothing to project into. Materialization side fixed 2026-08-22; owned .spl carries zero `-T` type positions.
