# Parser TypeKind constructors that the MATERIALIZATION walk
# (`parser_type_named_dependencies`, 10.frontend/parser_types_expr.spl) handles
# and the PROJECTION walk (`imported_surface_type`,
# 20.hir/hir_lowering/_Items/module_callable_types.spl) deliberately does NOT.
#
# Every line is `Constructor  reason`. A constructor may sit here ONLY with a
# measured reason. Adding a constructor to the materialization walk without
# either projecting it or adding a line here is the exact failure this guard
# exists to stop -- see the "durable lesson" in
# doc/08_tracking/bug/hir_unresolved_type_owner_missing_import_2026-08-22.md.
#
# A line here whose constructor IS projected is a STALE allowlist and also
# fails: a list that no longer describes the tree is how a ratchet silently
# stops ratcheting.
Optional    probed, 0 errors; a speculative fix measured byte-identical results and was reverted
Reference   probed, 0 errors
Atomic      probed, 0 errors
Isolated    probed, 0 errors
Projection  probed, 0 errors
Weak        HIR has no Weak kind; `lower_type` (20.hir/hir_lowering/types.spl) erases `-T` to Infer without erroring, so there is nothing to project into. NOTE 2026-08-23: the materialization-side `TypeKind.Weak` arm added on 2026-08-22 was lost with the ec13c319250 revert, so `Weak` is currently in NEITHER walk and this line is pre-emptive -- it keeps the reason attached for when that arm re-lands. Owned .spl carries zero `-T` type positions (measured 2026-08-22), so the gap stays latent.

# 2026-08-23: `Function` was REMOVED from this list. The guard's first run after
# being re-landed alone reported it as a stale line, and the source agrees:
# `imported_surface_type` projects Function at module_callable_types.spl:314-323
# (`parser_type_kind_is_function` / `_function_params` / `_function_return`).
# That arm landed separately and survived the ec13c319250 revert; the allowlist
# was written before it. Exactly the drift this guard exists to catch, caught on
# its own first honest run.
