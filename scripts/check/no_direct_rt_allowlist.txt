# Provider directories end in `/` and use prefix matching. Ordinary paths are
# exact files; `suffix:` entries are explicit suffix matches.
# Sanctioned runtime-boundary providers only. Adding a line here requires a
# doc/08_tracking record naming owner + reason.
src/lib/common/string_core.spl
src/lib/common/string_builder.spl
src/lib/nogc_sync_mut/ffi/
src/lib/nogc_sync_mut/sffi/
src/runtime/simple_core/
src/compiler/90.tools/sffi_gen/specs/
src/app/ffi_gen.specs/
# Baremetal/noalloc log boundary: rt_log_target_*/rt_simpleos_log_* are the
# device/semihost hardware write primitives — a genuine runtime boundary,
# not migratable product logic. Classified provider 2026-08-18
# (binary_runtime_hardening; see c_replaceable_bug_list.md taxonomy).
src/lib/nogc_async_mut_noalloc/log/
# GPU driver ABI boundary: rt_vulkan_*/rt_metal_* are tier-1 extern thunks into
# the installed Vulkan ICD / Apple Metal framework. No pure-Simple replacement
# exists for a vendor driver ABI. Classified provider 2026-08-18; owner+reason:
# doc/08_tracking/rt_boundary/provider_classification_2026-08-18.md
src/lib/nogc_sync_mut/io/vulkan_sffi.spl
src/lib/nogc_sync_mut/io/metal_sffi.spl
# Directory stream/status provider. Raw readdir/mkdir/remove ABI stays private;
# product consumers import the signature-preserving semantic wrappers only.
# Owner + reason: doc/08_tracking/rt_boundary/directory_entry_provider_2026-08-22.md
src/lib/nogc_sync_mut/io/dir_entry_ops.spl
# Loader exec-memory + file-I/O boundary: rt_mmap_raw/rt_mprotect/rt_ptr_read_u8/
# rt_ptr_write_u8/rt_ptr_write_bytes_raw/rt_array_data_ptr/rt_call_ptr_0 and
# rt_io_file_open/_seek/_read are the mmap, page-protection, raw-pointer and
# indirect-call primitives THEMSELVES. This is the layer that turns a file into
# executable memory and calls into it — there is nothing beneath it in Simple to
# delegate to, which is precisely the provider criterion (cf. the vulkan/metal
# vendor-ABI and baremetal-log entries above).
#
# Scoped to FILES, not the 99.loader/ directory, deliberately: the rest of the
# loader is ordinary product code and must stay under the ratchet. W^X is
# maintained inside these files (map RW, mprotect RX after write, never RWX).
# Classified provider 2026-08-19; owner+reason:
# doc/08_tracking/rt_boundary/provider_classification_2026-08-18.md
src/compiler/99.loader/smf_mmap_native.spl
src/compiler/99.loader/loader/smf_mmap_native.spl
src/compiler/99.loader/joinpoint_slots.spl
src/compiler/99.loader/aspect_pack_io.spl
# FFI-boundary modules BY NAME: a file named *_sffi.spl or sffi.spl declares
# itself the raw-FFI boundary for its subsystem — direct rt_* is its job.
# Classified provider 2026-08-18 (2,241 sites across 146 files at adoption).
suffix:_sffi.spl
suffix:/sffi.spl
# Second sffi-named tranche (2026-08-18): boundary modules whose names the
# first suffix pass missed. audit/sffi_*.spl are ANALYZERS of sffi usage,
# not boundaries — deliberately NOT listed.
suffix:_sffi_ops.spl
suffix:/sffi_common.spl
suffix:/sffi_signature.spl
suffix:/sffi_io.spl
suffix:/sffi_vulkan.spl
# libtorch binding facade (2026-08-18): the torch/ subtree IS the sanctioned
# third-party wrapper for rt_torch_*/rt_ps_torch_* (1,224 sites, 45 files at
# adoption; 98% torch-domain symbols, remainder is tensor-data marshalling
# pointer/alloc ops — boundary work). Taxonomy class: third-party wrapper,
# facade stays (c_replaceable_bug_list.md).
src/lib/common/torch/
src/lib/gc_sync_mut/torch/
src/lib/nogc_sync_mut/torch/
src/lib/nogc_async_mut/torch/
src/lib/gc_async_mut/torch/
# Memory/FFI binding packages (2026-08-18): the raw-pointer provider module,
# allocator binding, and third-party math/GPU binding facades (same class as
# the torch facade above). Also the async families' ffi/sffi dirs, which the
# original nogc_sync_mut-only entries missed.
src/lib/nogc_async_mut/sffi/
src/lib/nogc_async_mut/ffi/
src/lib/gc_async_mut/sffi/
src/lib/gc_async_mut/ffi/
src/lib/gc_sync_mut/sffi/
src/lib/gc_sync_mut/ffi/
src/lib/nogc_sync_mut/ptr/
src/lib/nogc_async_mut_noalloc/mimalloc.spl
src/lib/nogc_sync_mut/linalg/
src/lib/nogc_async_mut/linalg/
src/lib/gc_async_mut/linalg/
src/lib/nogc_async_mut/cuda/
src/lib/gc_async_mut/gpu_lane/
# Ptr-audit tranche (2026-08-18, 25-file audit: 0 product files, all FFI/
# device boundaries; cuda/ and linalg/ were already listed above — the
# audit's duplicates for those are intentionally not repeated):
src/lib/gc_async_mut/gpu/engine2d/
src/lib/nogc_sync_mut/gpu/
src/lib/nogc_async_mut/gpu/
src/lib/nogc_sync_mut/spec/evidence/counterpart/
src/lib/gc_async_mut/engine/audio/
src/lib/nogc_sync_mut/engine/audio/
src/lib/log.spl
src/lib/nogc_sync_mut/ui/gui_renderer.spl
src/lib/nogc_sync_mut/io/coverage_simple.spl
src/lib/scv/wasm_executor.spl
src/lib/common/ui/window_scene_draw_ir.spl
src/app/provider_cli/native_provider_v1.spl
src/app/simpleos_audio_host/daemon_runner.spl
src/app/io/feature_registry.spl
# Gate-lane ruling (2026-08-28, roots-widening adjudication): two common/
# boundary files that wrap a single class of raw rt_* primitive with no
# deeper Simple layer to delegate to (clock/time syscalls, raw binary byte
# I/O) — same provider criterion as the loader mmap/ptr entries above.
# Owner: gate lane. Record: doc/08_tracking/rt_boundary/provider_classification_2026-08-28.md
src/lib/common/time_utils.spl
src/lib/common/binary_io.spl
# By-kind heap-dump provider (2026-08-28): thin typed wrappers over
# rt_heap_* introspection externs, no deeper Simple layer underneath.
# Flagged by the compiler migration lane as provider-shaped, not migratable.
# Owner: gate lane. Record: doc/08_tracking/rt_boundary/provider_classification_2026-08-28.md
src/lib/nogc_sync_mut/mem/dump.spl
# Bundled/staged stdlib snapshot inside the simple-term-io / compiler_rust
# crate (2026-08-28, gate-lane roots-widening adjudication): 724 .spl files
# under src/compiler_rust/lib/ with a DIFFERENT directory layout than
# src/lib/ (std/src/, std/examples/, std/shaders/, std/report/ vs src/lib's
# common/nogc_sync_mut/gc_async_mut/... layers) and no sync script found
# under scripts/ referencing it. Excluded from the ratchet as
# derived/bundled crate content, same class as the vendor/** exclusions in
# CLAUDE.md's Owned-Code Scope — NOT verified as either a live sync target
# or a stale fork; see the bug record for the open question.
# Record: doc/08_tracking/bug/compiler_rust_lib_stdlib_snapshot_provenance_unknown_2026-08-28.md
src/compiler_rust/lib/
# Minimal-runtime-import contract boundary (2026-08-28): this exact file
# must keep a LITERAL rt_exit(1) — the worker must not pull std (no
# std.process.exit or similar), enforced by
# test/01_unit/app/native_build_worker_minimal_runtime_import_contract_spec.spl.
# Classified provider, not a migration target: the contract IS "call rt_*
# directly, never std".
src/app/cli/native_build_worker.spl
# ---- lib-lane residual-scope adjudication (2026-08-28, gate-lane ruling) ----
# Category (a): already-typed 1:1 wrapper modules sitting outside ffi/sffi/
# by filename convention only (each declares its own rt_* extern once and
# wraps it in a typed fn/class method — structurally identical to a sffi/
# provider file). Allowlisted IN PLACE, not relocated: relocation into
# ffi/sffi/ needs a cross-tree import-path audit (every absolute/relative
# importer across src/ and test/) that is out of scope for this pass and
# risks breaking working code for a purely cosmetic move. Owner: gate lane.
# Record: doc/08_tracking/rt_boundary/provider_classification_2026-08-28.md
src/lib/nogc_sync_mut/atomic.spl
src/lib/nogc_sync_mut/concurrent/
# rt_hal/ is a DIFFERENT case from the two entries above and is NOT a typed
# 1:1 extern wrapper module. Verified 2026-08-28: 9 files, exactly ONE
# `extern fn rt_*` in the whole directory, and ~160 regex matches that are
# the module's OWN pure-Simple functions named `rt_hal_*` (rt_hal_report,
# rt_hal_dispatch_one, rt_hal_digest256_equal, ...). Those are scanner
# false positives from `\brt_[a-z0-9_]*\(` colliding with the module's
# naming convention — not runtime calls. Exempted on that basis only.
# Known cost of the blanket entry: a genuine future rt_* runtime call added
# under rt_hal/ would also be excused. Preferred permanent fix is a scanner
# rule that excludes the `rt_hal_` prefix, after which this entry should be
# deleted. Dropping the entry today costs 160 forbidden sites
# (6685 -> 6845, still under baseline 7775).
src/lib/nogc_sync_mut/rt_hal/
src/lib/nogc_sync_mut/io/stderr_ops.spl
src/lib/nogc_sync_mut/io/env_ops.spl
src/lib/nogc_sync_mut/io/time_ops.spl
src/lib/nogc_sync_mut/io/file_ops.spl
src/lib/nogc_sync_mut/io/dir_ops.spl
src/lib/nogc_sync_mut/io/process_ops.spl
src/lib/nogc_sync_mut/io/sysinfo_ops.spl
src/lib/nogc_sync_mut/io/volatile_ops.spl
src/lib/nogc_sync_mut/io/debug_stubs.spl
src/lib/nogc_sync_mut/io/tls_common_hooks.spl
# Category (b): boundary-category rulings on the lib lane's residual list.
# GENUINE PROVIDERS (hardware/driver ABI, codegen-emitted names, or
# device-write primitives with no deeper Simple layer — same criterion as
# the pre-existing vulkan/metal/loader/baremetal-log entries above):
#   - GPU/graphics backend dispatch surfaces not already covered by the
#     existing src/lib/nogc_sync_mut/gpu/ and .../engine/audio/ entries:
src/lib/nogc_sync_mut/engine/render/
src/lib/nogc_sync_mut/engine/physics/backend_gpu/
src/lib/nogc_sync_mut/gpu_driver/
src/lib/nogc_sync_mut/gpu_runtime/
src/lib/nogc_sync_mut/gpu_profile/
src/lib/nogc_sync_mut/io/window_winit.spl
src/lib/nogc_sync_mut/io/simple_glfw.spl
src/lib/nogc_sync_mut/io/simple_sdl3.spl
src/lib/nogc_sync_mut/io/metal_ptr.spl
src/lib/nogc_sync_mut/desktop/display.spl
#   - ML/tensor: CUDA driver ABI under nogc_sync_mut/ (async-side
#     nogc_async_mut/cuda/ was already allowlisted; sync-side was not):
src/lib/nogc_sync_mut/cuda/
#     (linalg/ BLAS/LAPACK/CUDA-BLAS FFI files were already covered by the
#     pre-existing src/lib/nogc_sync_mut/linalg/ entry — no change needed.)
#   - JIT codegen: emits/consumes codegen symbol names, rule-3 boundary:
src/lib/nogc_sync_mut/jit/
#   - Baremetal device config/terminal/transport primitives, rule-3
#     boundary (same class as the existing noalloc log-boundary entry):
src/lib/nogc_sync_mut/baremetal/
#   - Debug/remote protocol: process-level debug primitives (ptrace, DWARF
#     unwinding consumed by a debugger, ST-Link/OpenOCD wire protocols).
#     Scoped to remote/ plus the two named agent files ONLY — NOT the whole
#     debug/ tree (see PRODUCT CODE below for two files deliberately
#     excluded):
src/lib/nogc_sync_mut/debug/remote/
src/lib/nogc_sync_mut/debug/native_agent.spl
src/lib/nogc_sync_mut/debug/smf_agent.spl
#
# PRODUCT CODE — NOT allowlisted, left forbidden (migration targets for a
# future lane), because each does real dispatch/parsing logic beyond the
# raw extern call and is not itself the boundary:
#   - src/lib/nogc_sync_mut/debug/interpreter_backend.spl (interpreter
#     command dispatch logic, not a raw primitive)
#   - src/lib/nogc_sync_mut/debug/formats/dsym_resolver.spl (dSYM format
#     parsing/lookup logic, not a raw primitive)
#   - src/lib/nogc_sync_mut/database/{core,mod,atomic,todo,checker,
#     nosql_offload}.spl, database/server/capability.spl,
#     database/sql/statement.spl, database/vector/distance.spl — DB engine
#     internals: storage/query logic that CALLS a raw rt_* primitive is not
#     the same thing as BEING the primitive; per two-kinds-of-simple-db-
#     naming these are the embedded-DB engine layer and should route
#     through a typed std.database wrapper like everything else, not be
#     exempted wholesale.
src/lib/nogc_sync_mut/rt_hal/virtual_device.spl
#
# --- rt API group providers, named 2026-09-06 (rt_api_group_policy.md Stage 1)
# Each entry below is an EXISTING boundary being NAMED, not a new exemption.
# Admission criterion, applied mechanically and recorded here so it can be
# re-checked: 100% of the group's direct rt_* call sites under src/ already
# live in this one file, AND the file's role is the ABI seam itself (a shim,
# a bridge, a hosted backend, a platform-event demultiplexer) rather than
# logic that merely happens to call the primitive. Groups that failed the
# criterion were left `unowned` on purpose — see the policy doc's Stage 1
# note for the ones deliberately NOT closed.
src/os/kernel/net/driver_shim.spl
src/os/compositor/hosted_backend_cocoa.spl
src/lib/editor/70.backend/gui_sdl_bridge.spl
src/lib/nogc_async_mut/io/platform_event.spl

# Differential effect-comparison probe. The rt_* call sites in this file are
# not incidental: the probe must read the filesystem through raw runtime
# externs precisely BECAUSE it is verifying wrapped implementations, and a
# wrapped reader would corroborate a mocked writer. The file's role is the ABI
# seam itself — two named providers plus the observation that discriminates
# them — not logic that happens to call a primitive. Owner + reason:
# doc/03_plan/runtime/native_to_pure_simple/native_surface_to_pure_simple_migration.md
# and doc/08_tracking/bug/io_runtime_file_write_resolves_to_retryless_twin_2026-09-06.md
# (lane C, 2026-09-06). Classified provider, baseline NOT bumped.
src/lib/nogc_sync_mut/dual_fs/
