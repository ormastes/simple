# rt_* externs declared by src/app/cli/bootstrap_main.spl that the
# core-c-bootstrap lane deliberately does NOT define, and cannot.
#
# rt_native_build IS the native-build driver. Its only implementation in the
# tree is src/compiler_rust/native_all/src/lib.rs:143 (hosted Rust runtime);
# there is no C body and there cannot be one. The seed already treats the
# bootstrap entry as authorized for the hosted libsimple_native_all.a
# (native_project/config.rs is_bootstrap_main_entry), so this is a lane-
# selection requirement, not a request to widen the Simple/C core ABI.
#
# This file is an explicit allowlist, never a wildcard: anything not listed
# here that the core lane does not define is a hard FAIL.
rt_native_build
