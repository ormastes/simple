# Method exists on NO class. Must fail closed, loudly -- never build
# and print a garbage value.
class Dog:
    fn speak(self) -> i64:
        return 7

fn main() -> i64:
    val d: Any = Dog()
    print "X={d.no_such_method()}"
    return 0
