name: Negative caller-supplied review head
on:
  workflow_dispatch:
    inputs:
      pull_request_number:
        required: true
        type: number
      session_id:
        required: true
        type: string
      head_sha:
        required: true
        type: string
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - run: echo forbidden
