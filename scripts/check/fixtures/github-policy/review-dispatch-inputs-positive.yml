name: Positive server-resolved review request
on:
  workflow_dispatch:
    inputs:
      pull_request_number:
        required: true
        type: number
      expected_head_sha:
        required: true
        type: string
      session_id:
        required: true
        type: string
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'server_head_sha: resolved-provider-value'
