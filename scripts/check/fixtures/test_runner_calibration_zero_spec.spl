# Calibration fixture — ZERO executed examples. It must be reported as an ERROR,
# never as a pass: a *_spec.spl that declares no `it` body executed nothing, and
# "nothing was checked" is not evidence of anything.
# Used only by scripts/check/check-test-runner-executes-bodies.shs. It lives under
# scripts/check/fixtures/ (not test/) so whole-suite discovery never picks it up.
use std.spec

fn main():
    print("calibration: this spec declares no it bodies")
