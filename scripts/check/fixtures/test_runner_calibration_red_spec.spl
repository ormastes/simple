"""
## Purpose and audience
Purpose: Calibration fixture — DELIBERATELY RED. It must FAIL. A runner that
reports this file as PASSED is load-only: it verified the file loads and
never executed the `it` body.
Audience: maintainers of scripts/check/check-test-runner-executes-bodies.shs.
"""

# Used only by scripts/check/check-test-runner-executes-bodies.shs. It lives under
# scripts/check/fixtures/ (not test/) so whole-suite discovery never picks it up.
use std.spec

describe "test-runner calibration (red)":
    it "executes an it body that fails":
        step("Run a trivially-false assertion inside the it body")
        # @req REQ-TEST-RUNNER-CALIBRATION-002
        # @capture(assertion): the runner's reported pass/fail for this body
        expect(1).to_equal(2)
