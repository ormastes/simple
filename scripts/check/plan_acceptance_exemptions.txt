# Named, reasoned exemptions for check-plan-acceptance-coverage.shs.
# Format: <slug>|<allowed_short>|<reason>
# `allowed_short` is how many open checkboxes may lack an `it`; `-1` means the
# plan needs no spec at all. A blanket exemption is not available: every line
# names a count and a reason, and the gate fails if the real shortfall EXCEEDS
# the number recorded here — so a new uncovered box still blocks.
process_safety|-1|Single open box is a manual monitoring observation ("after the next multi-day parallel-agent session, confirm no recurrence of the journal signature"). No promised function or artifact anywhere in the plan or tree; no script names `exit.target`. Inventing an oracle for it would be a fake test.
compiler_jit_rendering_loops|1|"All existing compiler tests continue to pass" is a blanket regression gate, not an interface. It is the suite's job, not a spec's.
sffi_universal_admission_next|13|Hardware/QEMU-gated evidence runs plus Rust-only unblocks with no promised Simple-side interface. Each of the 13 is named in the spec file itself.
perf_checklists|22|The plan repeats one checklist across many subjects; the spec is table-driven with one `it` per subject rather than 56 near-identical blocks. This grouping was the instruction to the writing lane, not a shortfall.
clang_board_bringup_x86_64_uefi|1|P4.1 (ssh clang -cc1 on the board, getfile over the real LAN, host byte-identity vs the OVMF reference, link -> exit 7) needs a purchased mini-PC on a physical LAN. Its only tree artifact is the P4.2 bring-up report, which P4.2's own `it` already asserts, so a separate `it` could only restate a literal. Named in the spec file at the point it was removed.
simpleos_nodejs_ai_cli_migration|1|"Binary size target: <2MB per arch" has nothing to measure: no QuickJS artifact is built for any *-simpleos triple, the plan names no output path or size-gate script, and AiCliRuntimeProfile carries no size-budget field. Named in the spec file at the point it was removed.
simpleos_production_master_plan_completion_status|1|LINUX-PERF is a process cap on how many times a human may iterate ("at most three correctness-preserving optimization cycles"). No cycle counter, budget constant or cycle-ledger artifact exists under src/ or config/. Named in the spec file, and still traced by the MATRIX-DOC census, which accepts a cell recorded here.
