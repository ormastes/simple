#!/bin/sh
set -eu

JS_RENDER_RUNTIME="${JS_RENDER_RUNTIME:-bun}"
JS_RENDER_RUNTIME_BIN="${JS_RENDER_RUNTIME_BIN:-bun}"
BUILD_DIR="${BUILD_DIR:-build/bun_simple_web_engine2d_bitmap_evidence}"
REPORT_PATH="${REPORT_PATH:-doc/09_report/bun_simple_web_engine2d_bitmap_evidence_$(date -u +%F).md}"
export JS_RENDER_RUNTIME JS_RENDER_RUNTIME_BIN BUILD_DIR REPORT_PATH

exec sh scripts/check-simple-web-engine2d-js-bitmap-evidence.shs
