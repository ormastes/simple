# Length-bearing baremetal HEAP_* tags that inline_runtime_len_value does NOT
# recognise, and which therefore answer the -1 sentinel for `.len()` on an
# ANY-erased receiver. FROZEN DEBT, not approval.
#
# Frozen 2026-09-01 while fixing HEAP_DICT=11 (the tag that actually blocked
# SimpleOS riscv64 row 2). These others are left uncovered deliberately: unlike
# HEAP_DICT, their in-memory layouts were NOT verified, and guessing a length
# offset would produce a confidently WRONG number, which is worse than the
# sentinel. Each needs its struct layout confirmed before it is covered.
#
# Note HEAP_STRING_BUILDER is defined with TWO different values in the tree
# (8 and 13), and 8 collides with HEAP_BTREEMAP. That conflict is itself a
# defect and must be resolved before either tag is covered.
#
# Adding a line here requires the same review as any other baseline update.
HEAP_BTREEMAP=8
HEAP_HASHMAP=9
HEAP_STRING_BUILDER=13
HEAP_STRING_BUILDER=8
