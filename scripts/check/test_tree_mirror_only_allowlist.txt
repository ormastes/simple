# Reviewed mirror-only exceptions for check-test-tree-divergence.shs.
# Format: label:relpath (label = unit|integration, relpath relative to the
# shadow tree root, e.g. unit:foo/bar_spec.spl for test/unit/foo/bar_spec.spl).
# HAND-MAINTAINED ONLY — never auto-generate this file to force a green run.
# Default posture: a mirror-only spec should be MIRRORED into the canonical
# tree (test/01_unit/ or test/02_integration/); allowlist only with a reason.
#
# unit:compiler/backend/llvm_ir_builder_incidental_probe.spl
#   Incidental probe, not a runnable spec: `bin/simple test` reports
#   zero-examples FAIL (executed=0). Mirroring it would duplicate a
#   FAIL-producing file into the canonical tree. Reviewed 2026-08-10.
unit:compiler/backend/llvm_ir_builder_incidental_probe.spl
#
# unit:runtime/time_epoch_convergence_spec.spl
#   Legitimate RED pin spec (executed=6 passed=3 failed=3) documenting the
#   known engine clock-epoch divergence (stream G2, 47adbf730ca). Mirroring
#   would double-count the same known defect as two failing files. Mirror it
#   once the divergence is fixed and it goes green. Reviewed 2026-08-10.
