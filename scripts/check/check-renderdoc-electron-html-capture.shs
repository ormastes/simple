#!/bin/sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
cd "$ROOT_DIR"

RDOC_OUTPUT_DIR="${RDOC_ELECTRON_HTML_OUT:-${RDOC_OUTPUT_DIR:-build/renderdoc/electron-html-capture}}" \
    scripts/tool/renderdoc-evidence.shs capture-electron-html
