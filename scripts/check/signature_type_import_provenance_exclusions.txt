# Names EXCLUDED from --auto mode, each for a stated reason.
#
# 1. The HIR type lowerer has BUILT-IN arms for these (20.hir/hir_lowering/types.spl,
#    lower_named_kind). A `struct Bool` really does exist in src/lib/*/ndarray and
#    `Option`/`Result`/`Dict` are recovered by name, so "declared exactly once in the
#    tree" does NOT make an explicit import the right repair -- adding one could
#    SHADOW the primitive. That risk is documented in the types.spl comments
#    themselves ("Placement follows the same load-bearing rule ... AFTER the symbol
#    lookup, so a DECLARED type of the same name still wins").
Option
Result
Dict
List
Bool
Int
Char
Any
Vec
Set
Map
HashMap
Float
String
Text
Array
Tuple
Self
Error
Ok
Err
Some
None
Nil
Weak
Box
Rc
Arc
Ref
Type
# 2. Names of one or two capitals are overwhelmingly TYPE PARAMETERS in this tree
#    (`fn walk_ast_asm_location<C>(node: AsmLocation, ctx: C, f: fn(..., C) -> C) -> C`),
#    not cross-module types. They are filtered by RULE in the awk, not listed here;
#    this comment records why, so the rule is not mistaken for an oversight.
