# Orphaned part-files: a `.spl` whose IMMEDIATE parent directory starts with `_`
# (the split-module convention) and whose module path no `use` anywhere names, so
# nothing ever compiles it.
#
# Frozen by scripts/check/check-orphan-part-files.shs. A NEW orphan fails. A row
# here that is no longer an orphan — it became imported, or the file was deleted —
# is a STALE baseline and ALSO fails: remove the row in the same change.
#
# Do NOT delete a baselined file without sweeping the WHOLE repository for pins on
# its path (gate baselines, specs, audit scripts), not just src/. Deleting one
# 717-line orphan touched nine such places, none of them under src/. See
# doc/08_tracking/bug/orphaned_part_files_never_imported_2026-09-18.md
src/app/_scratch_ed/main.spl
src/app/io/_CliCompile/native_build.spl
src/lib/gc_sync_mut/tls/_TlsUtilities/hex_encoding.spl
src/lib/gc_sync_mut/tls/_TlsUtilities/text_ops.spl
src/os/port/_initramfs_pack/archive.spl
src/os/services/evidence/_verifier_owner/verifier_transactions.spl
