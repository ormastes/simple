#!/bin/sh
# Turn the USB relay ON (close circuit, power the board).
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname "$0")" && pwd)"
. "$SCRIPT_DIR/relay_common.shs"

detect_relay_device
serial_send_noreply "$RELAY_CMD_ON"
relay_log "Power ON sent to $RELAY_DEVICE"
