# Project-specific secret patterns (GITIGNORED — do not commit)
# Use partial prefixes only — never store full keys here.
# Each line is a regex pattern checked against staged diffs.

# Anthropic API key (partial)
YthecG7S6h2p

# npm token (partial)
CSQHiRNN

# Azure DevOps PAT (partial)
3NJ2M69m

# OpenAI project key prefix
sk-proj-

# Google Gemini API key prefix
AIzaSy
