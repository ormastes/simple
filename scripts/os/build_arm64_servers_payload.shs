#!/bin/sh
# Compatibility entrypoint; the implementation is architecture-neutral.
set -eu
ROOT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
exec sh "$ROOT_DIR/scripts/os/build_simpleos_servers_payload.shs" arm64
