use std.spec.{describe, it, expect}

describe "while in it block":
    it "basic while loop":
        var count = 0
        while count < 5:
            count = count + 1
        expect(count).to_equal(5)
