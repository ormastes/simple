#!/usr/bin/env simple
# Test loading LazySeq__empty function

use app.interpreter.lazy.lazy_seq

fn main():
    print "Module loaded!"
    # Try calling the function directly via module path
    # val seq = app.interpreter.lazy.lazy_seq.LazySeq__empty()
    print "Test placeholder"
