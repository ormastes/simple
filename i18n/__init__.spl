# UI strings for the Simple compiler (English)
# Refactored into modules for maintainability

# Import all error message modules
use common.*
use parser.*
use runtime.*
use verification.*
use lint.*
use explanations.*

# Import error code mappings
use compiler.messages

# Re-export all constants (automatic from imports)
# All val declarations from imported modules are automatically available

# Export specific items if needed
export severity
export messages
