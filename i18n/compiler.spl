# Compiler error code mappings (refactored into modules)
# Combines semantic/codegen errors (E1xxx-E2xxx) and runtime errors (E3xxx+)

use error_codes_semantic.messages
use error_codes_runtime.runtime_messages

# Merge all error codes into single dictionary for backward compatibility
val all_messages = messages.merge(runtime_messages)

# Export merged dictionary as 'messages' (original name)
export all_messages as messages
export messages as error_messages  # Alias
