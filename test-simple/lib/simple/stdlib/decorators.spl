# Re-export test
use core.decorators.{cached, logged, deprecated}
export cached, logged, deprecated

fn test_marker():
    """This function only exists in re-export version"""
    print "Using RE-EXPORT version"
