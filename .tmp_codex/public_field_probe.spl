use compiler.driver.driver_public_api.{_parse_class_fields}

fn main() -> int:
    val lines = ["    mode: u8 @bits(4)", "    output: bool @bits(1)", "@export(\"C\")"]
    val parsed = _parse_class_fields(lines, 0)
    print parsed
    0
