extern fn rt_file_read_text(path: text) -> text

fn main() -> int:
    val source = rt_file_read_text("/tmp/sffi_driver_header_spec.spl") ?? ""
    val lines = source.split("\n")
    var i = 0
    while i < lines.len():
        print "<" + lines[i] + ">"
        i = i + 1
    0
