fn main() -> int:
    val raw = "    mode: u8 @bits(4)"
    var line = raw
    if line.contains("@bits("):
        val parts = line.split("@bits(")
        line = parts[0].trim_end()
    print "<" + line + ">"
    0
