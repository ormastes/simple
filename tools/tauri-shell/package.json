{
  "name": "simple-tauri-shell",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "prepare:dist": "mkdir -p dist && cp index.html dist/index.html",
    "desktop": "npm run prepare:dist && cargo run --manifest-path src-tauri/Cargo.toml -- ../../bin/simple ../../examples/ui/hello_tauri.ui.sdn",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
