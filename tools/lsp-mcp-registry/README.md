# Simple LSP MCP Server

Native-first LSP MCP bridge package for Simple.

```bash
npx @simple-lang/lsp-mcp-server
```

The package embeds the Linux x64 `simple_lsp_mcp_server` binary. To use a local
binary instead, set `SIMPLE_LSP_MCP_SERVER=/path/to/simple_lsp_mcp_server`.
