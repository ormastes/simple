# Simple MCP Server

Native-first MCP server package for Simple.

```bash
npx @simple-lang/mcp-server
```

The package embeds the Linux x64 `simple_mcp_server` binary. To use a local
binary instead, set `SIMPLE_MCP_SERVER=/path/to/simple_mcp_server`.
