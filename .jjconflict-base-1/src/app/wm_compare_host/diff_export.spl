# Host adapter: comparison-diff PPM export for wm_compare.
#
# Lives outside wm_lane_scope_dirs() (see
# src/compiler/90.tools/lint/_LintMain/wm_lane_boundary_lints.spl:51) so the
# os.compositor.diff_export import below is legal here. export_comparison_ppm
# is a stateless free function (writes a diagnostic artifact; no meaningful
# abstract port), so a facade re-export is the correct boundary, same as
# wave 1's stateless facades.
# See doc/03_plan/ui/wm_lane_boundary_ratchet_lanes.md (tranche W2, wave 2).

export use os.compositor.diff_export.{export_comparison_ppm}
