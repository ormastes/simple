# Host adapter: CLI log-option parsing for wm_compare.
#
# Lives outside wm_lane_scope_dirs() (see
# src/compiler/90.tools/lint/_LintMain/wm_lane_boundary_lints.spl:51) so the
# std.cli.log_modes import below is legal here.
# See doc/03_plan/ui/wm_lane_boundary_ratchet_lanes.md (tranche W2).

export use std.cli.log_modes.{parse_log_options, log_options_help, render_progress}
