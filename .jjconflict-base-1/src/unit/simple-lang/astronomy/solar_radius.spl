# Unit: SolarRadius
# Kind: Length
# Symbol: solar_radius
# Canonical scale: 695700000.0 m

class SolarRadius:
    value: f64

    fn to_f64() -> f64: self.value
    fn add(other: SolarRadius) -> SolarRadius: SolarRadius(value: self.value + other.value)
    fn sub(other: SolarRadius) -> SolarRadius: SolarRadius(value: self.value - other.value)

    static fn kind() -> text: "Length"
    static fn symbol() -> text: "solar_radius"
    static fn scale_to_base() -> f64: 695700000.0
    static fn base_unit() -> text: "m"
    static fn zero() -> SolarRadius: SolarRadius(value: 0.0)

export SolarRadius
