# No-GC Async 2D SIMD provider — facade over the no-GC sync canonical owner.

export use std.nogc_sync_mut.gpu.engine2d.simd_provider.{
    SimdHitCounts, make_simd_hit_counts_zero, reset_simd_hits, simd_hit_counts,
    record_simd_fill_hit, record_simd_copy_hit, record_simd_alpha_hit,
    record_simd_blit_hit, record_simd_scroll_hit, record_simd_vectorize_change,
    SimdProvider, register_simd_provider, registered_provider_name,
    has_registered_provider
}
