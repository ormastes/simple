"""
# No-GC async engine render shader compile

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.engine.render.shader_compile.{
    ShaderArtifact, ShaderCache, ShaderCompiler,
    shader_compiler_new, _hash_shader_source, _char_byte,
    shader_compiler_get_or_compile_spirv,
    shader_compiler_get_or_compile_wgsl
}
