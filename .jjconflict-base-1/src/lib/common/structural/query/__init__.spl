# ============================================================================
# common/structural/query — FROZEN QueryIR contract (QUERY lane)
#
# Architecture §7 (QueryIR: one engine, multiple dialects), §26 (contract
# freeze), §27 (QUERY lane owns structural/query). Contract document:
# doc/05_design/platform/structural_compute/query_contract_v1.md
#
# Explicit re-exports only — no `export use *`, which the linter flags for
# exposing unnecessary interface.
# ============================================================================

export use std.common.structural.query.query_contracts.{
    QUERY_DETERMINISM_MAX,
    QUERY_DIALECT_COUNT,
    QUERY_DIALECT_MAX,
    QUERY_OP_KIND_COUNT,
    QUERY_OP_KIND_MAX,
    QUERY_SCHEMA_VERSION,
    QueryDeterminism,
    QueryDialect,
    QueryOpKind,
    TAG_INDEX_DENSE_MARKER,
    TAG_INDEX_DENSE_SCALAR,
    TAG_INDEX_INVERTED_QUERY,
    TAG_INDEX_SET_EMPTY,
    TAG_INDEX_SET_KNOWN,
    TAG_INDEX_SET_LEN,
    TAG_INDEX_SMALL_SET,
    TAG_INDEX_SPARSE_RECORDS,
    query_determinism_from_u8,
    query_determinism_satisfies,
    query_determinism_to_u8,
    query_determinism_valid,
    query_dialect_equal,
    query_dialect_from_u8,
    query_dialect_to_u8,
    query_dialect_valid,
    query_op_kind_equal,
    query_op_kind_from_u8,
    query_op_kind_is_binary,
    query_op_kind_is_seed,
    query_op_kind_to_u8,
    query_op_kind_valid,
    tag_index_set_contains,
    tag_index_set_valid
}

export use std.common.structural.query.query_program.{
    CAPTURE_KIND_MAX,
    CAPTURE_SLOT_LEN,
    ENTITY_SET_ORDER_MAX,
    ENTITY_SET_VIEW_LEN,
    QUERY_NO_CONSTANT,
    QUERY_NO_INPUT,
    QUERY_OP_LEN,
    QUERY_U32_MAX,
    CaptureKind,
    CaptureSlot,
    EntitySetOrder,
    EntitySetView,
    QueryOp,
    QueryProgram,
    capture_kind_equal,
    capture_kind_from_u8,
    capture_kind_to_u8,
    capture_kind_valid,
    capture_schema_well_formed,
    capture_slot,
    capture_slot_equal,
    capture_zero_slot,
    entity_set_order_equal,
    entity_set_order_from_u8,
    entity_set_order_to_u8,
    entity_set_order_valid,
    entity_set_view,
    entity_set_view_empty,
    entity_set_view_equal,
    entity_set_view_well_formed,
    query_op,
    query_op_binary,
    query_op_equal,
    query_op_seed,
    query_op_unary,
    query_op_well_formed,
    query_program,
    query_program_capture_index,
    query_program_equal,
    query_program_valid,
    query_zero_op
}

export use std.common.structural.query.query_codec.{
    QUERY_PROGRAM_HEADER_LEN,
    CaptureSlotResult,
    EntitySetViewResult,
    QueryOpResult,
    QueryProgramResult,
    capture_slot_put,
    capture_slot_read,
    decode_capture_slot,
    decode_entity_set_view,
    decode_query_op,
    decode_query_program,
    encode_capture_slot,
    encode_entity_set_view,
    encode_query_op,
    encode_query_program,
    entity_set_view_put,
    entity_set_view_read,
    query_empty_program,
    query_magic_capture_slot,
    query_magic_entity_set_view,
    query_magic_op,
    query_magic_program,
    query_op_put,
    query_op_read
}
