# Parser model facade for wave-1 and future wave-2+ upgrades.
#
# For this milestone, the canonical fields are re-exported from parse_types
# so the new module graph exists without changing behavior.

use std.common.structural.parse.parse_types.{
    ParseActionSink,
    ParseCapacityManifest,
    ParseCapacityVerdict,
    ParseDiagnostic,
    ParseDiagnosticArena,
    ParseDialect,
    ParseLexProgram,
    ParseRequest,
    ParseResult,
    ParseSourceBlob,
    ParseToken,
    ParseTokenArena,
    parse_capacity_manifest_zero,
    parse_capacity_manifest,
    parse_capacity_verdict_accepted,
    parse_diagnostic_arena_empty,
    parse_diagnostic_arena_len,
    parse_diagnostic_at,
    parse_dialect_action_program,
    parse_dialect_grammar_program,
    parse_dialect_id,
    parse_dialect_incremental_policy,
    parse_dialect_lexical,
    parse_dialect_lex_program,
    parse_dialect_mapping_policy,
    parse_dialect_structure_program,
    parse_dialect_tag_schema,
    parse_request,
    parse_sink_emit_diagnostic,
    parse_sink_emit_tag,
    parse_sink_emit_token,
    parse_source_blob,
    parse_source_blob_empty,
    parse_source_blob_from_text,
    parse_source_blob_len,
    parse_token_arena_empty,
    parse_token_arena_len,
    parse_token_at
}

export ParseActionSink
export ParseCapacityManifest, ParseCapacityVerdict
export ParseDiagnostic, ParseDiagnosticArena
export ParseDialect, ParseLexProgram, ParseRequest, ParseResult
export ParseSourceBlob, ParseToken, ParseTokenArena
export parse_capacity_manifest_zero, parse_capacity_manifest
export parse_capacity_verdict_accepted
export parse_diagnostic_arena_empty, parse_diagnostic_arena_len, parse_diagnostic_at
export parse_dialect_action_program, parse_dialect_grammar_program
export parse_dialect_id, parse_dialect_incremental_policy
export parse_dialect_lexical, parse_dialect_lex_program
export parse_dialect_mapping_policy, parse_dialect_structure_program, parse_dialect_tag_schema
export parse_request
export parse_sink_emit_diagnostic, parse_sink_emit_tag, parse_sink_emit_token
export parse_source_blob, parse_source_blob_empty, parse_source_blob_from_text, parse_source_blob_len
export parse_token_arena_empty, parse_token_arena_len, parse_token_at
