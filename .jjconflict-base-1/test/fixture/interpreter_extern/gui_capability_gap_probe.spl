# Fixture for test/01_unit/compiler/interpreter_extern/capability_gap_spec.spl.
# Calls rt_gui_present_html, a real rt_gui_* extern with no entry in
# EXTERN_DISPATCH and no native implementation anywhere (M3 capability gap).
# NOTE: rt_gui_get_glyph_8x16 is a DIFFERENT rt_gui_* name that IS registered
# (conversion::rt_gui_get_glyph_8x16_fn) — do not reuse it here, it would not
# exercise the capability-gap arm.

extern fn rt_gui_present_html(html: text)

fn main() -> i64:
    rt_gui_present_html("<html></html>")
    print "unexpected success: rt_gui_present_html"
    0
