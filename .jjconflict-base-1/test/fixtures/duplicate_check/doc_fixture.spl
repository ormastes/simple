# Compute a score for an input value.
fn documented_score(input: i64) -> i64:
    input + 1

# Compute a score for an input value.
fn duplicated_doc(input: i64) -> i64:
    input + 2

fn undocumented_helper(input: i64) -> i64:
    input - 1
