# Fixture f08 — a lambda RETURNED from a function (escaping closure).

fn make_adder(n: i64) -> (i64) -> i64:
    \x: x + n

fn main():
    val f = make_adder(1)
    print("f08 marker result={f(41)}")
