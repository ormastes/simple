use std.common.compress.{decompress_bytes}

fn zstd_decompress(compressed: [u8]) -> Result<[u8], text>:
    val result = decompress_bytes(compressed, nil)
    if result.is_err():
        return Err(result.unwrap_err().message())
    Ok(result.unwrap())
