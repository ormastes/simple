# Test keyword traits

# Not trait - logical not operator keyword
trait Not:
    type Output
    fn not() -> Self::Output

# From trait - conversion keyword
trait From<T>:
    fn from(value: T) -> Self

# Into trait - should work without special handling
trait Into<T>:
    fn into() -> T
