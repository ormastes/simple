"""
Simple module with doctest examples

This demonstrates doctest syntax patterns:
- Functions/classes: """ triple-quote blocks (Python-style)
- Modules: /// doc comments (for package overview)

Examples:
```sdoctest
>>> import example_module
>>> example_module.factorial(5)
120
```
"""

"""
Computes factorial of n

Examples:
```sdoctest
>>> factorial 5
120
>>> factorial 0
1
>>> factorial 1
1
```
"""
fn factorial(n: Int) -> Int:
    if n <= 1:
        return 1
    return n * factorial(n - 1)

"""
Stack data structure

Examples:
```sdoctest
>>> s = Stack.new()
>>> s.push(42)
>>> s.pop()
42
>>> s.is_empty()
True
```
"""
struct Stack:
    items: List<Int>

    fn new() -> Stack:
        return Stack(items: [])

    fn push(item: Int):
        self.items.push(item)

    fn pop() -> Int:
        return self.items.pop()

    fn is_empty() -> Bool:
        return self.items.len == 0

"""
Range iterator

Examples showing multi-line statements:
```sdoctest
>>> r = Range.new(1, 3)
>>> for x in r:
...     print x
1
2
3
```
"""
struct Range:
    start: Int
    end: Int

    fn new(start: Int, end: Int) -> Range:
        return Range(start: start, end: end)
