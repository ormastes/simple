"""
# Database SDN Table Import/Export

## Overview
Simple DB supports reading and writing table data as SDN named tables.
The SDN file is human-readable and uses the table's column names as fields.

## SDN Format
```sdn
users |id, name, active|
    1, "Alice", true
    2, "Bob", false
```

## API Shape
```simple
db.export_table_sdn("users", "users.sdn")
db.import_table_sdn("users", "users.sdn")
```
"""

class Database:
    fn export_table_sdn(self, table: str, path: str):
        pass

    fn import_table_sdn(self, table: str, path: str) -> i64:
        return 0

#[id("700")]
describe "Database SDN table import/export":
    ## Test: Export table to SDN

    """
    ### Scenario: Export a table as SDN

    When a table is exported, the SDN file contains a named table whose
    fields match the column names, and rows are written in order.
    """
    fn export_users(db: Database):
        db.export_table_sdn("users", "users.sdn")

    #[id("700.1")]
    test "Export users table to SDN":
        assert_compiles()

    ## Test: Import table from SDN

    """
    ### Scenario: Import a table from SDN

    When a table is imported, each SDN row is inserted as a database row.
    The import operation returns the number of rows inserted.
    """
    fn import_users(db: Database) -> i64:
        return db.import_table_sdn("users", "users.sdn")

    #[id("700.2")]
    test "Import users table from SDN":
        assert_compiles()
