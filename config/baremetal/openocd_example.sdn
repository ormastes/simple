# Baremetal Host Communication - OpenOCD Example
# Connect via OpenOCD semihosting.

connection:
    transport: openocd_semihost
    timeout_ms: 10000

openocd:
    host: localhost
    telnet_port: 4444
    cfg: board/stm32f4discovery.cfg

options:
    log_enabled: true
