# Baremetal Host Communication - SSH Example
# Connect to a baremetal device accessible via SSH.

connection:
    transport: ssh
    timeout_ms: 10000

ssh:
    host: 192.168.1.100
    port: 22
    user: root
    key_path: ~/.ssh/id_rsa
