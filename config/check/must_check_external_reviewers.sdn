# Production external must-check reviewer trust roots. Provisioning a key is a
# reviewed repository change; test keys and private keys must never be listed.
must_check_external_reviewers:
  schema: "simple.must-check-reviewer-policy/v1"
trusted_reviewers |key_id, public_key_path, public_key_sha256|
