# Curated spec set for scripts/check/check-native-interp-differential.shs, covering
# the Dict `set` -> `contains_key` read-back shapes that every compiler memo uses.
#
#   sh scripts/check/check-native-interp-differential.shs \
#     --specs-file config/check/native_interp_differential_dict_shapes.txt
#
# Rationale: doc/08_tracking/bug/dict_memo_contains_key_native_shapes_2026-09-14.md.
# The interpreter is the oracle; all six rows are GREEN there (measured 2026-09-14
# on the Rust seed). A row that goes RED on the native lane is a self-hosting
# blocker, because the compiler's recursion breakers are exactly these shapes.
test/01_unit/lib/common/dict_native_shape_differential_spec.spl
