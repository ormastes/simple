# Specs the native-vs-interpreter differential census ALWAYS compares.
#
# The default curation is a stride sample across two roots, which is right for
# surveying the unknown and wrong for a spec written to pin one specific
# divergence: whether that spec is examined would depend on its alphabetical
# position modulo the stride. Rows listed here are compared on every run and
# then de-duplicated against the stride sample.
#
# Add a row only for a spec that pins a native/interpreter divergence whose
# regression would otherwise be silent. A path that no longer exists is skipped;
# it cannot make the run vacuous, because an empty spec set is still ERROR.

# Option<T> None discrimination when absence is only knowable at runtime.
# doc/08_tracking/bug/option_none_promoted_to_some_by_static_nil_provenance_2026-09-14.md
test/01_unit/compiler/option_none_runtime_discriminant_spec.spl
