{
  "_comment": "MCP Servers Configuration (Windows)",

  "mcpServers": {
    "simple-mcp": {
      "_info": "69 tools. Wrapper runs source mode by default (native simple_mcp_server.exe core-dumps on tools/call; set SIMPLE_MCP_PREFER_NATIVE=1 to opt in once fixed).",
      "command": "bin/simple_mcp_server.cmd",
      "args": [],
      "cwd": ".",
      "env": {
        "SIMPLE_LOG": "error",
        "RUST_LOG": "error"
      }
    },
    "simple-lsp-mcp": {
      "_info": "14 language-server MCP tools for Simple source navigation and editor features",
      "command": "bin/simple_lsp_mcp_server.cmd",
      "args": [],
      "cwd": ".",
      "env": {
        "SIMPLE_LOG": "error",
        "RUST_LOG": "error"
      }
    }
  }
}
