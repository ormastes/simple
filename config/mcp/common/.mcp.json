{
  "_comment": "MCP Servers Configuration (Linux/macOS)",

  "mcpServers": {
    "simple-pipe-mcp": {
      "_info": "SPipe-linked Simple MCP alias. Wrapper runs the cached native server by default and probes it before launch.",
      "command": "bin/simple_mcp_server",
      "args": [],
      "cwd": ".",
      "env": {
        "SIMPLE_LOG": "error",
        "RUST_LOG": "error"
      }
    },
    "simple-mcp": {
      "_info": "69 tools. Wrapper runs the cached native simple_mcp_server by default; source fallback is explicit debugging only.",
      "command": "bin/simple_mcp_server",
      "args": [],
      "cwd": ".",
      "env": {
        "SIMPLE_LOG": "error",
        "RUST_LOG": "error"
      }
    },
    "simple-lsp-mcp": {
      "_info": "LSP MCP tools. Wrapper runs source mode by default (native fails all tools/call; lsp_diagnostics gated off in source mode, 10/11 tools work). Set SIMPLE_LSP_MCP_PREFER_NATIVE=1 to opt in once fixed.",
      "command": "bin/simple_lsp_mcp_server",
      "args": [],
      "cwd": ".",
      "env": {
        "SIMPLE_LOG": "error",
        "RUST_LOG": "error"
      }
    },
    "stitch": {
      "_info": "Google Stitch MCP — UI design generation, site building, screen code retrieval",
      "command": "bash",
      "args": ["-lc", "if [ -f ~/.security/env.sh ]; then . ~/.security/env.sh; fi; exec npx -y @_davideast/stitch-mcp proxy"],
      "cwd": "."
    }
  }
}
