# Web WM — Window Manager via web server backend
#
# Launches the WM layout in a web browser using the HTTP+WebSocket server.
# Open http://localhost:3000 in a browser to view.

use app.ui.web.server.{run_web}

fn main() -> i64:
    return run_web("examples/ui/hello_electron.ui.sdn", 3333)
