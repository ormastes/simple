app:
  title: "Minimal"
  theme: dark

keybindings:
  normal:
    q: quit

layout:
  type: vbox
  children:
    - type: panel
      id: main
      title: Hello
      flex: 1
      children:
        - type: text
          id: greeting
          content: "Hello from Simple UI!"
    - type: statusbar
      id: status
      left: "{app.mode} MODE"
      right: "{app.title}"
