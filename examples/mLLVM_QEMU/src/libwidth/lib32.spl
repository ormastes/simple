# 32-bit Width Support Library (Stub)
#
# Placeholder for future ARM Cortex-M / RISC-V RV32 support.
# All functional methods use pass_todo to indicate unimplemented status.

use core.types.{GuestArch}
use guest.cpu_def.{GuestCpuDef, GuestState}

# ============================================================================
# Width32 Library (Stub)
# ============================================================================

class Width32Lib:
    """32-bit width support library stub.

    Future targets: ARM Cortex-M (Thumb-2), RISC-V RV32I.
    Currently unimplemented -- all factory methods use pass_todo.
    """
    _placeholder: i64

impl Width32Lib:
    static fn new() -> Width32Lib:
        Width32Lib(_placeholder: 0)

    fn word_size() -> i64:
        32

    fn arch_name() -> text:
        "arm32/riscv32 (stub)"

    fn create_emulator() -> Width32Stub:
        """Factory: create a 32-bit emulator (not yet implemented)."""
        pass_todo

    fn supported_targets() -> [text]:
        """List of target architectures planned for 32-bit support."""
        ["arm-cortex-m0", "arm-cortex-m4", "riscv32i"]

# ============================================================================
# 32-bit Emulator Stub
# ============================================================================

class Width32Stub:
    """Placeholder emulator for 32-bit architectures."""
    _placeholder: i64

impl Width32Stub:
    static fn new() -> Width32Stub:
        Width32Stub(_placeholder: 0)

    fn word_size() -> i64:
        32

    me step() -> i64:
        pass_todo

    me run(max_cycles: i64):
        pass_todo

    me load_program(addr: i64, data: [i64]):
        pass_todo

    me reset():
        pass_todo

    fn stats_text() -> text:
        "32-bit emulator: not yet implemented"
