/**
 * @file host_memory_buffer_impl.h
 * @brief Implementation details for host memory buffer pool
 *
 * This file contains inline/template implementations for host memory buffer
 * pool operations. It should be included at the end of host_memory_buffer.h.
 */

#pragma once

// This file is intentionally minimal - actual implementations are in host/host_memory_buffer.cpp
// Inline implementations can be added here if needed for performance-critical paths.

// Example inline helper (if needed):
// inline bool host_pool_is_valid(Pool* p) {
//     return p && p->dev && p->cap > 0;
// }
