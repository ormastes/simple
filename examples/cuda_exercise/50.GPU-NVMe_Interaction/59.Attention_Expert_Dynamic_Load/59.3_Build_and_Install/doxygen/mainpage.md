# Module 59.3 Build & Install

The build recipes defined here turn the feature capabilities (Module 59.2) into
actionable sequences for CMake + Python packaging. Every step references the
feature matrix so future automation stays consistent with the advertised support.
