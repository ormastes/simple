"""
NVFS native driver — FsDriver implementation for DriverInstance::Nvfs.

Phase 9 B1 deliverable.

Submodules:
  - capabilities    — nvfs_native_capabilities() CapabilitySet (16 caps, no PosixCompat).
  - extensions      — impl blocks for SnapshotExt, DedupExt, ChecksumExt, SendRecvExt,
                      ScrubExt, CompressExt, EncryptExt, ReflinkExt.
  - fs_driver_impl  — NvfsDriver struct + FsDriver impl (all 23 ops).

Scope boundary:
  - POSIX shim (NvfsPosixDriver) lives in examples/nvfs/src/posix/ — agent B2.
  - I/O backends (io_uring_cmd, vfio) are future milestones.
"""

# All submodules are automatically available.
