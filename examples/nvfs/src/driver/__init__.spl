"""
NVFS driver shim — comment-only stub.

Phase 5 does not provide a driver impl. Real NVMe submission lives in
`src/lib/nogc_async_mut/` and will arrive in later milestones. This
directory exists so the submodule layout matches the design doc.

Planned submodules (post-Phase-5):
  - io_uring_cmd   — Linux io_uring passthrough submission path.
  - vfio           — user-space VFIO driver for bare-metal.
  - probe          — capability probe implementation feeding std.storage.capability.
"""
