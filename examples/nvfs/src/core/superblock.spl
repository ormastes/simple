# NVFS core — superblock reader/writer.
#
# Implements std.fs.nvfs.superblock_if.SuperblockReader. Lives in the
# META_DURABLE class with the checkpoint ring.
#
# Source: doc/05_design/nvfs_design.md §3.

struct SuperblockDevice:
    """Backing device reference for the superblock."""
    arena_id: i64
    block_size: i32

fn sb_read() -> i64:
    """Read and verify the canonical superblock."""
    pass_todo

fn sb_write(generation: i64) -> bool:
    """Emit a new superblock image at the given checkpoint generation."""
    pass_todo
