# NVFS core — metadata intent log.
#
# Small append-only log that captures directory-tree / extent-map
# mutations so crash recovery can replay them against the last durable
# checkpoint.

fn intent_append(payload: [u8]) -> i64:
    """Append one intent record; return its sequence number."""
    pass_todo

fn intent_flush(up_to: i64) -> bool:
    """Force-flush the intent log to and including sequence `up_to`."""
    pass_todo

fn intent_replay_from(checkpoint_gen: i64) -> i64:
    """Replay intent records past `checkpoint_gen`; return count replayed."""
    pass_todo
