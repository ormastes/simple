# NVFS core — arena_* concrete impl.
#
# Implements std.storage.arena.Arena. Real backend chooses ZNS zones /
# FDP streams / plain bands per CapabilityProbe.

fn arena_create_impl(class_tag: i32, hint_bytes: i64) -> i64:
    """Provision a new arena; returns opaque handle id."""
    pass_todo

fn arena_append_impl(arena_id: i64, data: [u8], durability_tag: i32) -> i64:
    """Append `data`; returns bytes_written."""
    pass_todo

fn arena_readv_impl(arena_id: i64, off: i64, buf: [u8]) -> i64:
    """Read into `buf`; returns bytes_read."""
    pass_todo

fn arena_seal_impl(arena_id: i64, gen: i64) -> bool:
    """Seal at generation `gen`."""
    pass_todo

fn arena_discard_impl(arena_id: i64) -> bool:
    """Discard the arena (honor DSM trim where probed)."""
    pass_todo

fn arena_clone_range_impl(src: i64, src_off: i64, dst: i64, dst_off: i64, len: i64) -> bool:
    """Copy byte range; prefer Simple Copy Command when CopyOffload probed."""
    pass_todo

fn arena_preferred_granule_impl(arena_id: i64) -> i64:
    """Preferred I/O granule for the arena's class."""
    pass_todo
