/**
 * Publication capability is owned by the parent commit-publisher composition
 * root. Consumers can name the store but cannot construct it or mint a permit.
 */
export { TargetInventoryStoreV1 } from "../core/knowledge_compiler_commit_publisher.js";
