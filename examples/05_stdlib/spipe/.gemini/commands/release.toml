description = "Prepare a protected stable or prerelease release from an immutable admitted candidate."
prompt = """
Read doc/00_llm_process/skill_command/command/release.md completely and follow it as the semantic authority. Require an isolated release branch/worktree, canonical version projections, verified evidence, an immutable build-once candidate, and exact promote-without-rebuild publication. Beta maintenance accepts only a caller-selected reviewed bug-fix commit with exact provenance and renewed result-revision evidence. Ask before external push or publication. Never directly update protected refs, automatically select fixes, broadly push tags, move/delete/reuse a published tag, or accept fallback artifacts.
"""
