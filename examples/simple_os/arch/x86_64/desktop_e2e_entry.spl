# x86_64 Desktop E2E Baremetal Entry
#
# Minimal wrapper that boots the baremetal runtime, runs the desktop E2E
# test harness, and then exits QEMU through ISA debug-exit.

extern fn serial_println(msg: text)
extern fn rt_port_outb(port: u16, value: u8)

use os.test.desktop_e2e_test.{test_main}

fn spl_start():
    serial_println("")
    serial_println("=== SimpleOS Desktop E2E ===")
    serial_println("[desktop-e2e] Starting desktop_e2e_test.test_main()")

    test_main()

    serial_println("[desktop-e2e] test_main() returned")
    rt_port_outb(0xf4, 0)
