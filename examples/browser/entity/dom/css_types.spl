#![allow(REQC001)]
#![allow(REQC004)]
#![allow(unnamed_duplicate_typed_args)]
# Compatibility re-export for legacy examples.browser CSS type imports.

use std.common.color.types.{Color, from_rgba}
use common.render_scene.css_types.{
    CSSValue,
    CSSDeclaration,
    SelectorPart,
    Selector,
    CSSRule,
    Specificity
}
