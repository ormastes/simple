#![allow(REQC001)]
#![allow(REQC004)]
#![allow(unnamed_duplicate_typed_args)]
# Compatibility re-export for legacy examples.browser error imports.

use common.web.error.{
    BrowserErrorKind,
    BrowserError,
    config_error,
    ipc_error,
    network_error,
    parse_error,
    dom_error,
    layout_error,
    paint_error,
    script_error,
    sandbox_error,
    io_error
}
