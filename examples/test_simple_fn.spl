# Test without decorator
use spec.*

describe "simple function":
    it "works":
        fn square(x: i64) -> i64:
            return x * x

        result = square(5)
        expect(result).to_equal(25)
