#![allow(REQC001)]
#![allow(REQC004)]
#![allow(unnamed_duplicate_typed_args)]
# Compatibility re-export for legacy examples.browser DOM node type imports.

use common.web.node_types.{
    NodeKind,
    DomNode,
    Element,
    TextNode,
    Comment,
    DocumentFragment
}
