"""
spostgre business layer — MDSOC+ ECS slice.

11 POD components + 8 systems following spostgre_design.md §3.

Submodules:
  - components: Relation, PageDescriptor, Tuple, WalRecord, Txn,
                Checkpoint, VacuumTask, BufferFrame, PmapBinding,
                BlobRef, CapabilityView.
  - systems:    sys_commit, sys_wal_flush, sys_checkpoint, sys_vacuum,
                sys_buffer_evict, sys_pmap_publish, sys_blob_gc,
                sys_capability_probe.
"""

# All submodules are automatically available.
