# spostgre engine — vacuum / space reclamation.
#
# Walks pmap, discards dead page versions, drops orphaned arena ranges.

use std.spostgre_if.types.{Rel, BlkNo}

fn vacuum_range(rel: Rel, lo: BlkNo, hi: BlkNo) -> i64:
    """Reclaim dead versions in [lo, hi]. Returns bytes freed."""
    pass_todo

fn vacuum_scan_dead(rel: Rel) -> i64:
    """Count dead tuples across the relation without freeing."""
    pass_todo
