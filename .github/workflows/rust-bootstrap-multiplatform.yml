name: Rust Bootstrap Multiplatform

on:
  push:
    paths:
      - 'src/compiler_rust/**'
      - 'src/compiler/**'
      - 'src/app/cli/**'
      - 'src/app/mcp/**'
      - 'src/app/simple_lsp_mcp/**'
      - 'src/lib/**'
      - 'src/runtime/**'
      - 'scripts/bootstrap/bootstrap-from-scratch.sh'
      - 'scripts/check/cert/redeploy_gate/candidate_frontend_admission.shs'
      - 'scripts/check/cert/redeploy_gate/fixtures/p2_add.spl'
      - 'scripts/check/check-bootstrap-portability.shs'
      - 'scripts/check/check-llvm-simd-row-native-arch.shs'
      - 'scripts/check/check-mcp-native-smoke.shs'
      - 'scripts/check/check-cranelift-aot-aggregate-cross.shs'
      - 'scripts/check/check-native-option-try-target-fail.shs'
      - 'scripts/check/check-native-seed-parity.shs'
      - 'scripts/check/check-stage4-hosted-dynload-provider-archive.shs'
      - 'scripts/check/check-ui-slim-closure.shs'
      - 'scripts/check/check-pure-simple-lint-runnable.shs'
      - 'src/app/ui.tui/async_app.spl'
      - 'scripts/check/check-ui-layout-branch-coverage.shs'
      - 'test/unit/app/ui/layout_spec.spl'
      - 'src/app/ui.tui/backend.spl'
      - 'src/app/ui.tui_web/backend.spl'
      - 'scripts/check/validate_stage4_hosted_dynload_provider_archive.spl'
      - 'scripts/check/native-smoke-matrix.shs'
      - 'scripts/setup/platform-detect.shs'
      - 'test/fixtures/compiler/llvm_simd_row_native_probe.spl'
      - 'test/fixtures/compiler/stage4_struct_enum_array_probe.spl'
      - 'test/fixtures/compiler/native_option_try_*_loud.spl'
      - 'test/fixtures/compiler/native_option_uniform_tagged_abi_repro.spl'
      - 'test/fixtures/compiler/native_class_array_param_field_mutation.spl'
      - 'test/fixtures/browser/event_loop_set_timeout_standalone.spl'
      - 'test/fixtures/native_nested_struct_array_text_projection/**'
      - 'test/fixtures/native_draw_ir_command_text_projection/**'
      - 'test/fixtures/native_engine2d_draw_ir_param_projection/**'
      - 'test/fixtures/native_web_draw_ir_provenance/**'
      - 'test/fixtures/native_crossmodule_result_u8/**'
      - 'test/01_unit/compiler/backend/hosted_c_target_guard_spec.spl'
      - '.github/workflows/freebsd-build.yml'
      # These workflows must remain absent; trigger this guard if someone
      # restores one so portability contracts reject the regression.
      - '.github/workflows/windows-tests.yml'
      - '.github/workflows/test-isolation.yml'
      - '.github/workflows/cross-platform.yml'
      - '.github/workflows/simple-llvm-cross.yml'
      - '.github/workflows/rust-bootstrap-multiplatform.yml'
  pull_request:
    paths:
      - 'src/compiler_rust/**'
      - 'src/compiler/**'
      - 'src/app/cli/**'
      - 'src/app/mcp/**'
      - 'src/app/simple_lsp_mcp/**'
      - 'src/lib/**'
      - 'src/runtime/**'
      - 'scripts/bootstrap/bootstrap-from-scratch.sh'
      - 'scripts/check/cert/redeploy_gate/candidate_frontend_admission.shs'
      - 'scripts/check/cert/redeploy_gate/fixtures/p2_add.spl'
      - 'scripts/check/check-bootstrap-portability.shs'
      - 'scripts/check/check-llvm-simd-row-native-arch.shs'
      - 'scripts/check/check-mcp-native-smoke.shs'
      - 'scripts/check/check-cranelift-aot-aggregate-cross.shs'
      - 'scripts/check/check-native-option-try-target-fail.shs'
      - 'scripts/check/check-native-seed-parity.shs'
      - 'scripts/check/check-stage4-hosted-dynload-provider-archive.shs'
      - 'scripts/check/check-ui-slim-closure.shs'
      - 'scripts/check/check-pure-simple-lint-runnable.shs'
      - 'src/app/ui.tui/async_app.spl'
      - 'scripts/check/check-ui-layout-branch-coverage.shs'
      - 'test/unit/app/ui/layout_spec.spl'
      - 'src/app/ui.tui/backend.spl'
      - 'src/app/ui.tui_web/backend.spl'
      - 'scripts/check/validate_stage4_hosted_dynload_provider_archive.spl'
      - 'scripts/check/native-smoke-matrix.shs'
      - 'scripts/setup/platform-detect.shs'
      - 'test/fixtures/compiler/llvm_simd_row_native_probe.spl'
      - 'test/fixtures/compiler/stage4_struct_enum_array_probe.spl'
      - 'test/fixtures/compiler/native_option_try_*_loud.spl'
      - 'test/fixtures/compiler/native_option_uniform_tagged_abi_repro.spl'
      - 'test/fixtures/compiler/native_class_array_param_field_mutation.spl'
      - 'test/fixtures/browser/event_loop_set_timeout_standalone.spl'
      - 'test/fixtures/native_nested_struct_array_text_projection/**'
      - 'test/fixtures/native_draw_ir_command_text_projection/**'
      - 'test/fixtures/native_engine2d_draw_ir_param_projection/**'
      - 'test/fixtures/native_web_draw_ir_provenance/**'
      - 'test/fixtures/native_crossmodule_result_u8/**'
      - 'test/01_unit/compiler/backend/hosted_c_target_guard_spec.spl'
      - '.github/workflows/freebsd-build.yml'
      # These workflows must remain absent; trigger this guard if someone
      # restores one so portability contracts reject the regression.
      - '.github/workflows/windows-tests.yml'
      - '.github/workflows/test-isolation.yml'
      - '.github/workflows/cross-platform.yml'
      - '.github/workflows/simple-llvm-cross.yml'
      - '.github/workflows/rust-bootstrap-multiplatform.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # Native builds — compile and test on each platform
  native:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: Linux x86_64
          - os: macos-latest
            name: macOS aarch64
          - os: macos-13
            name: macOS x86_64
          - os: windows-latest
            name: Windows x86_64
            backend: llvm
          - os: windows-latest
            name: Windows x86_64
            backend: llvm
          - os: windows-latest
            name: Windows x86_64
            backend: cranelift
    runs-on: ${{ matrix.os }}
    name: Native — ${{ matrix.name }}
    steps:
      - uses: actions/checkout@v4

      - name: Validate bootstrap portability contracts
        if: runner.os == 'Linux' && matrix.backend == 'llvm'
        run: sh scripts/check/check-bootstrap-portability.shs

      # spl_fonts declares an UNCONDITIONAL #[link(name = "freetype")]
      # (spl_fonts/src/lib.rs:134), so any link of that crate -- including its
      # `lib test` harness under `cargo test --workspace --lib` -- needs
      # freetype on the link path. This job installed no system deps, so the
      # macOS rows failed with `ld: library 'freetype' not found` while the
      # Build step still passed. Mirrors the working pattern already in
      # .github/workflows/rust-tests.yml. macOS-gated: other rows unchanged.
      - name: Install freetype (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install freetype
          echo "LIBRARY_PATH=$(brew --prefix freetype)/lib:$LIBRARY_PATH" >> "$GITHUB_ENV"
          echo "CPATH=$(brew --prefix freetype)/include:$CPATH" >> "$GITHUB_ENV"

      - name: Setup MSVC
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Fix MSVC linker PATH
        if: runner.os == 'Windows'
        shell: bash
        run: |
          if [ -f /usr/bin/link ]; then
            mv /usr/bin/link /usr/bin/link.bak
          fi

      - uses: dtolnay/rust-toolchain@nightly

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src/compiler_rust/target
          key: ${{ runner.os }}-${{ runner.arch }}-bootstrap-${{ hashFiles('src/compiler_rust/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-bootstrap-

      - name: Build
        working-directory: src/compiler_rust
        run: cargo build --profile bootstrap -p simple-driver

      - name: Test
        working-directory: src/compiler_rust
        run: cargo test --workspace --lib

      # Slim-UI import-closure gate (NFR-UI-SLIM-002,
      # doc/02_requirements/nfr/ui_slim_kernel_plugin.md; plan Wave 0 / A01).
      # Runs `deps fast` on the ordinary-mode TUI entry through the seed just
      # built above and fails if any file in its closure lives under one of the
      # five forbidden prefixes the NFR names. Needs a built seed, which is why
      # it runs here and not in repo-hygiene.yml (which only runs --selftest).
      # Skipped on Windows: the guard is POSIX sh and the seed there is
      # simple.exe, untested under Git Bash.
      - name: Slim-UI closure gate
        if: runner.os != 'Windows'
        run: |
          sh scripts/check/check-ui-slim-closure.shs \
            --seed src/compiler_rust/target/bootstrap/simple \
            src/app/ui.tui/async_app.spl \
            src/os/compositor src/os/drivers src/os/kernel \
            src/lib/skia src/lib/gc_async_mut/gpu

      # Pure-Simple lint runnable pin: the lint program (src/app/cli/lint_entry.spl)
      # must EXECUTE through the seed just built and its findings must
      # discriminate (clean fixture -> "Lint passed", dirty fixture ->
      # RAW-RT-001). Its --selftest also needs the binary, so it cannot run in
      # repo-hygiene.yml; it was baselined as unwired debt until wired here.
      - name: Pure-Simple lint runnable gate
        if: runner.os != 'Windows'
        run: |
          sh scripts/check/check-pure-simple-lint-runnable.shs \
            --binary src/compiler_rust/target/bootstrap/simple

      # Shared UI layout-engine gate (landed with 0f7492dfb32; the guard header
      # cites no plan/NFR doc, the commit is its authority). Two blocking halves:
      # ui.tui/backend.spl and ui.tui_web/backend.spl must still import the same
      # common.ui.layout.{compute_layout}, and running test/unit/app/ui/layout_spec.spl
      # under SIMPLE_COVERAGE=1 must leave >= 90% of the decisions in
      # src/lib/common/ui/layout.spl covered on BOTH outcomes (0 recorded
      # decisions is ERROR, never a pass). Unlike the two gates above there is
      # no repo-hygiene --selftest half: its selftest itself runs the
      # interpreter, so without a binary it exits 2. Not parameterized; it
      # probes bin/simple (untracked, absent here) and falls back to the seed
      # built above at src/compiler_rust/target/bootstrap/simple.
      - name: Shared UI layout-engine coverage gate
        if: runner.os != 'Windows'
        run: |
          sh scripts/check/check-ui-layout-branch-coverage.shs

      - name: Upload binary
        uses: actions/upload-artifact@v4
        if: github.ref == 'refs/heads/main'
        with:
          name: bootstrap-${{ matrix.name }}-${{ github.sha }}
          path: |
            src/compiler_rust/target/bootstrap/simple*
          retention-days: 7

  # Cross-compilation builds from Linux x86_64
  cross:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-unknown-linux-gnu
            name: Linux aarch64
            pkg: gcc-aarch64-linux-gnu
          - target: riscv64gc-unknown-linux-gnu
            name: Linux riscv64
            pkg: gcc-riscv64-linux-gnu
          - target: x86_64-pc-windows-gnu
            name: Windows x86_64 (MinGW)
            pkg: gcc-mingw-w64-x86-64
    name: Cross — ${{ matrix.name }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@nightly
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation toolchain
        run: sudo apt-get update && sudo apt-get install -y ${{ matrix.pkg }}

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src/compiler_rust/target
          key: cross-${{ matrix.target }}-${{ hashFiles('src/compiler_rust/Cargo.lock') }}
          restore-keys: |
            cross-${{ matrix.target }}-

      - name: Build
        working-directory: src/compiler_rust
        run: cargo build --profile bootstrap -p simple-driver --target ${{ matrix.target }}

  # FreeBSD build (VM-based)
  freebsd:
    runs-on: ubuntu-latest
    name: Native — FreeBSD x86_64
    steps:
      - uses: actions/checkout@v4

      - name: Build on FreeBSD
        uses: vmactions/freebsd-vm@v1
        with:
          usesh: true
          prepare: |
            pkg install -y curl bash
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly
          run: |
            . "$HOME/.cargo/env"
            cd src/compiler_rust
            cargo build --profile bootstrap -p simple-driver
            cargo test --workspace --lib
