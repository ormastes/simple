# platform = windows variant: fixed Windows build-target extensions.
# Resolved only when config selects platform: windows. No host detection.

pub fn target_lib_ext() -> text:
    ".dll"

pub fn target_exe_ext() -> text:
    ".exe"
