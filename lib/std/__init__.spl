# Simple Language Standard Library
# Default variant: async + no_gc
#
# Lint policy:
# - #[deny(primitive_api)] - No bare primitives in public APIs
# - #[deny(bare_string)] - No bare str/String in public APIs

#[deny(primitive_api)]
#[deny(bare_string)]
mod std

# Prelude - auto-imported into every file
export use prelude.*

# Core modules (variant-agnostic, pure)
common use core.*
common use core.option.*
common use core.result.*

# Unit type definitions
common use units.*

# Default async_nogc profile
use host.async_nogc.*
