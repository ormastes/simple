# Transition table: MirTypeKind -> C backend type mapper
#
# Boundary: `CTypeMapper.map_type`,
# src/compiler/70.backend/backend/c_type_mapper.spl
# Producer universe: `enum MirTypeKind` (29 registry entries -- the integer and
# float families share one registry id each), enumerated from the generated
# registry spec/compiler_schema/registry/compiler.mir.MirTypeKind.sdn.
#
# Lane C7 (backend coverage, hardening plan 2026-08-21 sections 11.3-11.6).
# Before C7 eight variants fell to `case _: "int64_t"` -- the worst shape in the
# file, because int64_t is a perfectly valid C type: a 256-bit vector, a
# scalable vector, an arbitrary-width Bits or a Result silently became a 64-bit
# integer and the generated C compiled and RAN with the wrong representation.
#
# Recorded state verified by source inspection 2026-08-21.

table: mir_type_to_c_backend
producer: MirTypeKind
consumer: CTypeMapper
source: src/compiler/70.backend/backend/c_type_mapper.spl

universe:
  - ActorType
  - Array
  - Bits
  - Bool
  - Char
  - Dict
  - Enum
  - F32
  - FuncPtr
  - Generator
  - I8
  - Never
  - Opaque
  - Promise
  - Ptr
  - Ref
  - Result
  - ScalableVec
  - Slice
  - Struct
  - Tuple
  - U8
  - Union
  - Unit
  - Vec4d
  - Vec4f
  - Vec4i
  - Vec8f
  - Vec8i

transition:
  from: MirTypeKind.ActorType
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps ActorType to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Array
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Array to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Bits
  to: CTypeMapper
  state: implemented
  reason: "explicit arm added by lane C7; Bits now maps to a real C representation instead of int64_t"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Bool
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Bool to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Char
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Char to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Dict
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Dict to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Enum
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Enum to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.F32
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps F32 to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.FuncPtr
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps FuncPtr to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Generator
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Generator to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.I8
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps I8 to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Never
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Never to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Opaque
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Opaque to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Promise
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Promise to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Ptr
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Ptr to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Ref
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Ref to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Result
  to: CTypeMapper
  state: implemented
  reason: "explicit arm added by lane C7; Result now maps to a real C representation instead of int64_t"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.ScalableVec
  to: CTypeMapper
  state: unsupported
  reason: "explicit arm raises the named E-BACKEND-C-TYPE-ScalableVec"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Slice
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Slice to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Struct
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Struct to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Tuple
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Tuple to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.U8
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps U8 to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Union
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Union to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Unit
  to: CTypeMapper
  state: implemented
  reason: "dedicated arm maps Unit to a C type"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Vec4d
  to: CTypeMapper
  state: implemented
  reason: "explicit arm added by lane C7; Vec4d now maps to a real C representation instead of int64_t"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Vec4f
  to: CTypeMapper
  state: implemented
  reason: "explicit arm added by lane C7; Vec4f now maps to a real C representation instead of int64_t"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Vec4i
  to: CTypeMapper
  state: implemented
  reason: "explicit arm added by lane C7; Vec4i now maps to a real C representation instead of int64_t"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Vec8f
  to: CTypeMapper
  state: implemented
  reason: "explicit arm added by lane C7; Vec8f now maps to a real C representation instead of int64_t"
  test: mir_type_to_c_backend_coverage_spec

transition:
  from: MirTypeKind.Vec8i
  to: CTypeMapper
  state: implemented
  reason: "explicit arm added by lane C7; Vec8i now maps to a real C representation instead of int64_t"
  test: mir_type_to_c_backend_coverage_spec

