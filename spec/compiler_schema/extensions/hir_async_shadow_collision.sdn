# Admission fixture (hardening plan §7.2): a DIFFERENT provider claiming a
# constructor the frozen seal already holds. Complete in every other respect —
# the only defect is the stable-identity collision, so nothing else can be
# blamed for the rejection.
extension:
  owner_enum: compiler.hir.HirExprKind
  constructor: async.Await
  closure: complete
  payload_schema_hash: "aa11bb22cc33dd44"
  module_abi_hash: "44dd33cc22bb11aa"
  required_core_abi_hash: "core-abi-v1"
  provider_module: "hir_async_shadow"
  local_ordinal: 1
  schema_abi_version: 2

  provides:
    verify: shadow.verify_await
    type_check: shadow.type_await
    effects: shadow.effects_await
    visit_children: shadow.visit_await
    lower_mir: shadow.lower_await
    print: shadow.print_await
    hash: shadow.hash_await
    serialize: shadow.serialize_await

module:
  id: hir_async_shadow
  linkage: dynload
  closure: complete
  activation: startup

  extends:
    - enum: compiler.hir.HirExprKind
      constructor: async.Await
      schema_abi: 2

  provides:
    - HirVerify
    - HirEffects
    - HirVisit
    - HirLowerMir
    - HirPrint
    - HirSerialize
