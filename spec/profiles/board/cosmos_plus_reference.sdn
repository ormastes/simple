# BoardProfile — CosmosPlusReference.
# Apertures, PCI identity and the bitstream SHA are MEASURED, from
# doc/**/cosmos_openssd_port_2026-06-30.md. Capacity/buffer figures are
# ILLUSTRATIVE and derived only to make the closure equations checkable.
#
# EMISSION IS BLOCKED: origin ledger row CosmosPlusReference is `pending`
# (PL bitstream redistribution unresolved). The profile still VALIDATES.
schema_version: 1
board:
  id: CosmosPlusReference
  revision: "cosmos-plus-8ch8way-v3.0.0"
  controller: CosmosPlusZynq7000
  media: ToshibaOnfiToggle
  offload: CosmosNfcEccDma
  origin: CosmosPlusReference
  # transport_profile: DECLARED, unbound
  transport_profile: board
  bound: 0
  wiring:
    channels: 8
    ways_per_channel: 8
    nfc_aperture_base: 0x43C00000
    nfc_aperture_stride: 0x10000
    pcie_aperture_base: 0x83C00000
    pcie_aperture_span: 0x10000
  identity:
    pci_vendor: 0x10EE
    pci_device: 0x7028
    pci_class: 0x010802
    bar0_bytes: 8192
    bitstream_sha256: "66e863b2ff2c0190928e3e71aeba9725551584cffc32854928946b1720cbf5c2"
    bitstream_contract: COSMOS_PCIE_CONTRACT_8CH8WAY_V300
  # namespace_capacity_lbas: ILLUSTRATIVE
  namespace_capacity_lbas: 1000000000
  # write_buffer_pool: ILLUSTRATIVE
  write_buffer_pool: 512
  # max_inflight_writes: ILLUSTRATIVE
  max_inflight_writes: 128
  # per_write_buffers: ILLUSTRATIVE
  per_write_buffers: 4
  # reserved_oob_bytes: ILLUSTRATIVE
  reserved_oob_bytes: 64
  # metadata_bytes_per_page: ILLUSTRATIVE
  metadata_bytes_per_page: 64
  evidence_required: C5
  certification_current: C0
  unsupported:
    - sgl
    - zns
    - controller_memory_buffer
