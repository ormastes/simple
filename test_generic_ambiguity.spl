# Test case: does the parser correctly distinguish?
class Box<T>:
    value: T

fn compare(x: i64) -> bool:
    if x < 5:
        return true
    return false

fn generic_and_compare<T>(x: i64, data: T) -> bool:
    if x < 10:
        return true
    return false

print "Test completed"
