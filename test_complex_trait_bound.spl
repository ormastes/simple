# Test complex trait bounds with generics

trait Iterator:
    type Item

# Test: Trait bound with associated type constraint
trait FromIterator<T>:
    fn from_iter<I: Iterator<Item=T>>(iter: I) -> Self
