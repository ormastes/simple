# ASan suppressions for Simple compiler bootstrap build
# The memtrack hash table uses raw malloc intentionally (avoids recursion).
# Usage: ASAN_OPTIONS=suppressions=src/compiler_cpp/sanitizers/asan.supp
