"""
# RV32 PMP
"""

enum Rv32AccessType:
    Fetch
    Load
    Store

class Rv32Pmp:
    allow_all: bool

    static fn create() -> Rv32Pmp:
        Rv32Pmp(allow_all: true)

    fn allow(addr: i64, width: i64, access: Rv32AccessType) -> bool:
        self.allow_all and addr >= 0 and width > 0
