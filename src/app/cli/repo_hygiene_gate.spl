# Repo hygiene gate wrapper used by `simple check`.

use app.io.mod.{process_run}

fn run_repo_hygiene_gate() -> i64:
    val (_stdout, _stderr, exit_code) = process_run("sh", ["scripts/check/check-repo-hygiene.shs"])
    exit_code
