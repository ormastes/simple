# Bootstrap overrides for missing stdlib symbols

use std.database.feature.{Feature}

fn sort_features_by_id(features: [Feature]) -> [Feature]:
    # Stable, no-op sort for bootstrap
    features

fn find_duplicate_ids(features: [Feature]) -> [Feature]:
    val _ = features
    []

fn mark_orphaned_features(features: [Feature], ids: [text]) -> [Feature]:
    features
