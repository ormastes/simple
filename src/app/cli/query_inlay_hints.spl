# Compatibility module. The canonical implementation and outline index live in
# query_tokens so query and direct module consumers share one behavior owner.
export use app.cli.query_tokens.{query_inlay_hints}
