# Bootstrap stub for `simple check`
# In bootstrap builds we skip full type-checking to avoid parser/type dependencies.

fn run_check(args: [text]) -> i64:
    print "[bootstrap] check skipped"
    0

export run_check
