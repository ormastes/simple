# MCP Module Compatibility Shim
#
# Keeps `app.mcp.*` import paths valid after MCP sources moved under
# `src/lib/nogc_async_mut/mcp`.
#
# Intentionally no exports here: concrete APIs are provided by submodules.
