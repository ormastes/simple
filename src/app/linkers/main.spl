# Simple CLI - linkers command
# Delegates to Rust runtime via FFI

use std.cli.cli_util (get_cli_args)
use app.io.mod (cli_handle_linkers)

fn print_help():
    print "Usage: simple linkers"
    print "Lists available native linkers"
    print ""
    print "Run 'simple linkers --help' for Rust-level help."

fn main() -> i64:
    val all_args = get_cli_args()
    var i = 0
    while i < all_args.len():
        if all_args[i] == "--help" or all_args[i] == "-h":
            print_help()
            return 0
        i = i + 1
    cli_handle_linkers()
