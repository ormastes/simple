# Opt-in Calc semantic UI access server.
#
# The server mounts the existing /api/test/ui surface used by `simple ui`.
# It is not SGTTI and it does not define an Office-specific protocol.

use app.office.sheets.calc_session_host.{run_calc_session_host}

fn run_calc_access_server(port: i32, in_path: text) -> i32:
    if port <= 0 or port > 65535:
        print "Error: Calc UI access port must be between 1 and 65535"
        return 1
    # One host owns both the terminal and the semantic UI service.  Do not
    # start a second workbook/session for --ui-access-port.
    run_calc_session_host(port, in_path)

export run_calc_access_server
