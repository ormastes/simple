# Leak Check App Shim
#
# Source shim so `app.leak_check` resolves to the current leak-check
# implementation instead of stale compiled `.smf` artifacts.

use compiler.tools.leak_check.main.{run_leak_check}

export run_leak_check
