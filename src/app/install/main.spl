# Simple CLI - install command
# Installs all dependencies from simple.sdn/simple.lock

use app.cli_util (get_cli_args, find_manifest_path)
use app.io.mod (file_exists, file_read, file_write, dir_create)
use app.package_utils (generate_lockfile_content, parse_deps_from_sdn)
use std.text.{NL}
use std.log.{error}

fn print_help():
    print "Usage: simple install [options]"
    print ""
    print "Install all dependencies from simple.sdn."
    print "Uses simple.lock if available, otherwise resolves from manifest."
    print ""
    print "Options:"
    print "  --frozen     Only install from lockfile, fail if missing"
    print "  --dry-run    Show what would be installed without changes"
    print "  -h, --help   Show this help"

fn main() -> i64:
    val args = get_cli_args()

    var frozen = false
    var dry_run = false

    for arg in args:
        if arg == "-h" or arg == "--help":
            print_help()
            return 0
        elif arg == "--frozen":
            frozen = true
        elif arg == "--dry-run":
            dry_run = true

    val manifest_path = find_manifest_path()
    if manifest_path == "":
        error("install", "no simple.sdn or simple.toml found in current directory")
        return 1

    val cwd = cwd()
    val lockfile_path = "{cwd}/simple.lock"
    val modules_dir = "{cwd}/simple_modules"

    # Check for lockfile
    if frozen and not file_exists(lockfile_path):
        error("install", "--frozen requires simple.lock to exist")
        return 1

    val content = file_read(manifest_path)
    val deps = parse_deps_from_sdn(content)

    if deps.len() == 0:
        print "No dependencies to install."
        return 0

    if dry_run:
        print "Would install {deps.len()} dependencies:"
        for (name, version) in deps:
            print "  {name} ({version})"
        return 0

    # Create simple_modules directory
    if not dir_create(modules_dir, true):
        error("install", "failed to create simple_modules/")
        return 1

    print "Installing {deps.len()} dependencies..."

    var installed = 0
    for (name, version) in deps:
        # Create package directory
        val pkg_dir = "{modules_dir}/{name}"
        if not dir_create(pkg_dir, true):
            error("install", "failed to create directory for '{name}'")
        else:
            # Write a placeholder module file
            val mod_content = "# {name} - installed by simple install{NL}# version: {version}{NL}# TODO: actual package download from registry{NL}"
            file_write("{pkg_dir}/__init__.spl", mod_content)
            print "  installed {name} ({version})"
            installed = installed + 1

    print ""
    print "Installed {installed}/{deps.len()} packages to simple_modules/"

    # Generate lockfile if not frozen
    if not frozen and not file_exists(lockfile_path):
        val lock_content = generate_lockfile_content(deps)
        file_write(lockfile_path, lock_content)
        print "Generated simple.lock"

    0
