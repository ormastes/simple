import intern_codegen

fn main():
    val source: text = "InternFnSpec(name: \"test\", category: \"io\")"
    print "Source length: {source.len()}"
    val specs = intern_codegen.parse_intern_specs(source)
    print "Specs: {specs.len()}"

main()
