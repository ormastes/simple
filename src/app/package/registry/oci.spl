# OCI Client
# GHCR push/pull operations via oras CLI.

use app.package.registry.types (PublishResult)
use app.package.registry.auth (get_token)

fn oras_available() -> bool:
    pass_todo

fn print_oras_install_help():
    print "The 'oras' CLI is required to publish packages."
    print "Install it from: https://oras.land/docs/installation"

fn push(spk_path: text, oci_ref: text) -> PublishResult:
    pass_todo

fn pull(oci_ref: text, dest_dir: text) -> bool:
    pass_todo

fn exists_in_registry(oci_ref: text) -> bool:
    pass_todo
