# Direct FFI Test - Call extern functions directly
import std.package_ffi

print "Testing FFI Functions Directly..."

# Test calling extern function directly
# TODO: Replace direct FFI call with wrapper (package_exists) from app.io or compiler.ffi
val result = rt_package_exists("/tmp")
# TODO: Replace direct FFI call with wrapper (package_exists) from app.io or compiler.ffi
print "rt_package_exists(\"/tmp\") = " + result.to_string()

if result == 1:
    print "✓ /tmp exists"
else:
    print "✗ Failed"
