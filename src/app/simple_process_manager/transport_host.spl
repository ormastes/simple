#![allow(unnamed_duplicate_typed_args)]
"""transport_host — flat alias for `transport.spm_transport_host`.

Specs import `app.simple_process_manager.transport_host.{SpmTransportHost}`.
The authoritative class lives under the `transport/` subdir; we pub-use
it here so the flat spec-import path resolves.

Feature: spm-winfs-llm-suite AC-3.
"""

pub use app.simple_process_manager.transport.spm_transport_host.{SpmTransportHost}
