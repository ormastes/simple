#![allow(unnamed_duplicate_typed_args)]
"""transport — flat alias for `transport.spm_transport`.

Specs import `app.simple_process_manager.transport.{SpmRequest, SpmResponse}`.
Authoritative definitions live in `transport/spm_transport.spl`; re-exported
here so the flat spec-import path resolves.

Feature: spm-winfs-llm-suite AC-3.
"""

pub use app.simple_process_manager.transport.spm_transport.{SpmTransport, TransportStatus, SpmRequest, SpmResponse, spm_encode_request, spm_decode_request, spm_encode_response, spm_decode_response}
