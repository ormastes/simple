# File I/O Extern Functions
#
# Comprehensive filesystem and terminal I/O operations.
# Port of rust/compiler/src/interpreter_native_io.rs (631 lines)
#
# Provides Simple wrappers for:
# - Filesystem operations (read, write, directories, metadata)
# - File handle operations (open, seek, flush, close)
# - Terminal operations (stdin/stdout/stderr, raw mode, TTY)
#
# This module has been refactored into smaller submodules:
# - file_io/ffi_fs.spl - Filesystem FFI declarations
# - file_io/ffi_file.spl - File handle FFI declarations
# - file_io/ffi_term.spl - Terminal FFI declarations
# - file_io/fs.spl - Filesystem wrapper functions
# - file_io/file.spl - File handle wrapper functions
# - file_io/term.spl - Terminal wrapper functions

# Import and re-export filesystem operations
use app.interpreter.extern.file_io.fs.{
    fs_exists, fs_read, fs_read_string, fs_write_string, fs_write, fs_append,
    fs_create_dir, fs_remove_file, fs_remove_dir, fs_rename, fs_copy,
    fs_metadata, fs_read_dir, fs_open
}

# Import and re-export file handle operations
use app.interpreter.extern.file_io.file.{
    file_read, file_write, file_flush, file_seek, file_sync, file_close
}

# Import and re-export terminal operations
use app.interpreter.extern.file_io.term.{
    stdin, stdout, stderr, is_tty,
    enable_raw_mode, disable_raw_mode, get_term_size,
    term_write, term_read, term_read_timeout, term_flush, term_poll
}

# Re-export all functions
export fs_exists, fs_read, fs_read_string, fs_write_string, fs_write, fs_append
export fs_create_dir, fs_remove_file, fs_remove_dir, fs_rename, fs_copy
export fs_metadata, fs_read_dir, fs_open
export file_read, file_write, file_flush, file_seek, file_sync, file_close
export stdin, stdout, stderr, is_tty
export enable_raw_mode, disable_raw_mode, get_term_size
export term_write, term_read, term_read_timeout, term_flush, term_poll
