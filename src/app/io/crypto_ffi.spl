# Compatibility facade: application crypto I/O uses the canonical library owner.
#
# Keep this module path for older imports without redeclaring raw providers or
# maintaining a second set of sentinel-prone wrappers.
export use std.nogc_sync_mut.io.crypto_sffi.*
