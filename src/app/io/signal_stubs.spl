# Signal Handler SFFI Stubs
#
# Temporary stub implementations until SFFI is added to runtime.
# These allow the signal handling code to compile and run (but do nothing).

# ============================================================================
# Stub Implementations (return false/do nothing)
# ============================================================================

fn rt_signal_handler_available() -> bool:
    """
    Stub: Always returns false until SFFI is implemented.

    TODO: Replace with actual SFFI implementation in seed/runtime.c
    """
    false

fn rt_signal_handler_install(signal: i64, handler: fn()):
    """
    Stub: Does nothing until SFFI is implemented.

    TODO: Replace with actual SFFI implementation in seed/runtime.c
    """
    pass_do_nothing

fn rt_atexit_register(handler: fn()):
    """
    Stub: Does nothing until SFFI is implemented.

    TODO: Replace with actual SFFI implementation in seed/runtime.c
    """
    pass_do_nothing

# ============================================================================
# Exports
# ============================================================================

export rt_signal_handler_available
export rt_signal_handler_install
export rt_atexit_register
