# Compatibility module for the canonical compiler ABI owner.
#
# Keep this path because app.io.__init__ and older callers import it, but never
# fabricate LLVM IR here. The runtime/provider boundary is declared once in
# cli_ops; callers must validate that the selected runtime implements it.

export use app.io.cli_ops.{rt_compile_to_llvm_ir}
