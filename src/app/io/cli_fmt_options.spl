# Shared closed option validation for both pure-Simple fmt CLI owners.

pub fn fmt_option_error(args: [text]) -> text:
    for arg in args:
        if arg == "fmt" or arg == "--check" or arg == "--write" or arg == "--diff" or arg == "--help" or arg == "-h":
            continue
        if arg.starts_with("-"):
            return "unknown option: {arg}"
    ""
