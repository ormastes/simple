# Compatibility facade: application TLS I/O uses the canonical library owner.
#
# Keep this module path for older imports without duplicating 35 raw providers
# and their wrapper semantics.
export use std.nogc_sync_mut.io.tls_sffi.*
