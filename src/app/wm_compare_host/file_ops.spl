# Host adapter: file read/write/exists primitives for wm_compare.
#
# Lives outside wm_lane_scope_dirs() (see
# src/compiler/90.tools/lint/_LintMain/wm_lane_boundary_lints.spl:51) so the
# nogc_sync_mut.io.file_ops import below is legal here.
# See doc/03_plan/ui/wm_lane_boundary_ratchet_lanes.md (tranche W2).

export use std.nogc_sync_mut.io.file_ops.{file_write, file_read_text, file_exists}
