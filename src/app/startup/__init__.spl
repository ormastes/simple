# Startup policy module.

export use app.startup.launch_metadata.{
    LaunchMetadata,
    StartupLaunchPlan,
    launch_metadata_default,
    launch_metadata_for_path,
    launch_metadata_for_native_build,
    launch_metadata_for_simpleos_path,
    launch_metadata_for_simpleos_baremetal_path,
    launch_metadata_sidecar_path,
    render_launch_metadata_sidecar,
    render_native_launch_metadata_trailer,
    parse_launch_metadata_sidecar,
    parse_launch_metadata_from_smf_bytes,
    parse_launch_metadata_from_native_bytes,
    has_native_launch_metadata_trailer,
    native_launch_metadata_trailer_magic,
    startup_detect_launch_kind,
    startup_normalize_program_args,
    startup_plan_from_metadata,
    startup_feature_summary
}

export use app.startup.load_policy_wiring.{
    StartupLoadDecision,
    load_policy_preload_mode,
    startup_load_decision,
    load_decision_from_plan
}

export use app.startup.segment_load_plan.{
    StartupSegmentRow,
    StartupSegmentTable,
    SegmentPlanRow,
    SegmentLoadPlan,
    segment_action_map_read_only,
    segment_action_read_ahead,
    segment_action_skip,
    parse_segment_table,
    segment_load_plan
}

export use app.startup.load_plan_receipt.{
    LoadPlanV1,
    LoadReceiptV1,
    LoadReceiptCheck,
    load_plan_hash,
    load_plan_seal,
    load_plan_to_sdn,
    load_receipt_to_sdn,
    load_receipt_record,
    load_receipt_check_against_plan
}

export use app.startup.receipt.{
    StartupReceiptV1,
    startup_evidence_none,
    startup_evidence_compact,
    startup_evidence_full,
    startup_receipt_record,
    render_startup_receipt_sdn
}

export use app.startup.dynsmf_autoload.{
    dynsmf_startup_session_for_manifest_from_values,
    dynsmf_startup_session_from_values,
    dynsmf_startup_session,
    dynsmf_startup_summary
}

export use app.startup.host_startup.{
    StartupArgumentValue,
    StartupFileArguments,
    StartupBeforeMainResult,
    startup_parse_file_arguments,
    startup_before_main
}
