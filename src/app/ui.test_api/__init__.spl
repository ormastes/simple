# UI Test API - Re-exports
# Handler
export handle_test_request

# JSON helpers
export element_to_json, elements_to_json, extract_json_field, escape_json, state_to_json

# Transport-neutral mount helpers (shared by host WM, TUI web, standalone,
# and SimpleOS compositor bridge — see src/app/ui.test_api/mount.spl).
export TEST_API_PREFIX, test_api_matches, dispatch_test_api, format_http_response, dispatch_and_format
