# UI State — backward-compat shim
# Canonical source: src/lib/common/ui/state.spl

use common.ui.state.{init_state, init_state_with_mode, update_state,
    update_tree, is_quit_event, is_quit_command}
