# UI Core Types — backward-compat shim
# Canonical source: src/lib/common/ui/widget.spl

use common.ui.widget.{WidgetKind, LayoutKind, UIMode, UIEvent,
    WidgetNode, UITree, WidgetRect, UIState,
    parse_widget_kind, parse_layout_kind}
