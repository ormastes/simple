use app.zz_repro_classreg.types.{Point}

class PlainStore:
    root: text

impl PlainStore:
    static fn new(root: text) -> PlainStore:
        PlainStore(root: root)

fn make_point_in_plain() -> Point:
    Point(x: 8)
