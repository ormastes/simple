# LSP MCP Module
# Exposes Language Server Protocol functionality via MCP protocol

# TODO: [parser][P2] deferred: explicit re-export list below is maintained manually until
# parser support for `export *` lands — requires changes in the declaration parser
# (src/compiler_rust/compiler/src/parser/) to recognise the `export *` form and expand
# it to all public symbols of the current module's sibling files at compile time.

# Re-exported from mod.spl
export Position
export Range
export Location
export LspSymbolKind
export SymbolInfo
export HoverContents
export HoverInfo
export DiagnosticSeverity
export Diagnostic
export ReferenceContext
export ReferenceLocation
export LspDefinitionHandler
export LspReferencesHandler
export LspHoverHandler
export LspSymbolsHandler
export LspDiagnosticsHandler
