# GUI - Graphical UI Renderer
#
# This module provides graphical UI rendering that shares the same
# Element/Node types as TUI. Supports both HTML output (for web/SSR)
# and native window rendering (for desktop applications).
#
# Renderers:
#   - HtmlRenderer: Outputs HTML/CSS for web browsers
#   - NativeRenderer: Uses framebuffer FFI for desktop windows
#   - VulkanRenderer: GPU-accelerated renderer using Vulkan API (sync)
#   - VulkanAsyncRenderer: Async/await Vulkan renderer (JavaScript-style)

mod gui

# HTML/CSS renderer for web output
export html.*

# Native window renderer for desktop
export native.*

# Vulkan renderer for GPU-accelerated rendering (synchronous)
export vulkan.*

# Vulkan async renderer for GPU-accelerated rendering (async/await)
export vulkan_async.*

# Vulkan core types (device, swapchain, render pass)
export vulkan_types.*

# Vulkan extended types, FFI, constants
export vulkan_types_ext.*

# Vulkan FFI bindings (extern fn declarations)
export vulkan_ffi.*

# Vulkan high-level wrapper types (VkDevice, VkBuffer, VkWindow, VkSwapchain)
export vulkan_ffi_calls.*

# Vulkan shader compilation (SPIR-V loading)
export vulkan_shaders.*

# Vulkan graphics pipeline (with smart defaults)
export vulkan_pipeline.*

# Vulkan buffers (vertex, index buffers)
export vulkan_buffers.*

# Vulkan uniform/storage buffers, FFI, constants
export vulkan_buffers_ops.*

# Vulkan command buffers and recording (ElementProcessor, core commands)
export vulkan_commands.*

# Vulkan synchronization and advanced commands (Frame, FrameData, ResourceManager, BufferManager)
export vulkan_commands_sync.*

# Vulkan GUI renderer (RenderBackend implementation)
export vulkan_renderer.*

# Vulkan renderer layout, drawing, vertex management (core draw calls)
export vulkan_renderer_draw.*

# Vulkan renderer extended utilities (PipelineBuilder, Attributes, parsers, event converters)
export vulkan_renderer_draw_ext.*

# Vulkan font rendering (text atlas, TTF loading)
export vulkan_font.*

# Vulkan font rendering helpers, FFI, TextLine, FontWeight
export vulkan_font_render.*

# Hot reload support for GUI applications
export hot_reload.*

# Hot reload implementation, file system monitoring, FFI
export hot_reload_apply.*

# ImGui - immediate mode GUI type definitions
export imgui.*

# ImGui widget implementations
export imgui_widgets.*

# Electron desktop renderer
export electron.*

# Electron native APIs, data types, FFI
export electron_bridge.*

# GUI-specific widgets with graphical styling (Card, Chip, Avatar, Badge)
export widgets.*

# GUI widgets - Tooltip and Divider
export widgets_impl.*

# Theme and styling for GUI
export theme.*
