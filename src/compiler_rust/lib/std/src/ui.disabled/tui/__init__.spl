# TUI - Terminal UI Renderer
#
# This module provides a terminal-based UI renderer using
# the existing terminal functionality from host/io/term.spl.
# Elements are rendered using box-drawing characters and ANSI styles.

mod tui

# Main TUI renderer
export renderer.*

# TUI async renderer - drawing, buffer management, FFI
export renderer_async_draw.*

# TUI-specific widgets
export widgets.*
