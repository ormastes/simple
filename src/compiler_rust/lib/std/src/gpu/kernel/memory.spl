# GPU Kernel Memory - Global/shared/local memory access primitives

# Memory fence and barriers
extern fn barrier()
extern fn mem_fence()
extern fn barrier_and_fence()

# Shared memory declaration (compile-time sized)
# Usage: shared val local_data: [f32; 256]
