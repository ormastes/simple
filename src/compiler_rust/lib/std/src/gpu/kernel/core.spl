# GPU Kernel Core - Thread/block/grid indices and intrinsics

# Thread identification
extern fn global_id() -> u32
extern fn global_id_dim(dim: u32) -> u32
extern fn local_id() -> u32
extern fn local_id_dim(dim: u32) -> u32
extern fn group_id() -> u32
extern fn group_id_dim(dim: u32) -> u32

# Size queries
extern fn global_size() -> u32
extern fn global_size_dim(dim: u32) -> u32
extern fn local_size() -> u32
extern fn local_size_dim(dim: u32) -> u32
extern fn num_groups() -> u32
extern fn num_groups_dim(dim: u32) -> u32

# Warp/subgroup intrinsics
extern fn warp_size() -> u32
extern fn lane_id() -> u32
