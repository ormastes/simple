
use verification.regenerate.nogc_compile as regen_module

fn main() -> Int:
    val content = regen_module.regenerate_nogc_compile()
    print("FILE:src/verification/nogc_compile/src/NogcCompile.lean")
    print("LENGTH:" + str(len(content)))
    print(content)
    print("END_FILE")
    0
