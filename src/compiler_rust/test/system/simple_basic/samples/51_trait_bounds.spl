# 51_trait_bounds.spl - Polymorphism over traits

trait Named:
    fn name() -> text

struct Project:
    title: text

impl Named for Project:
    fn name() -> text:
        return self.title

fn greet(x: Named) -> text:
    return "hello {x.name()}"

print greet(Project(title: "simple"))

main = 0
