# Generic Type Representation
#
# Structs for representing generic types in the type system.
# Used by monomorphization and type inference passes.

struct GenericParam:
    name: text
    constraint: text

struct GenericInst:
    base_type: i64
    type_args: [i64]

export GenericParam, GenericInst
