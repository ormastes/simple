# Diagnostics Formatters Module
# @tag:api
#
# Provides formatters for diagnostic output in various formats.
#
# This module contains formatter implementations that convert Diagnostic
# objects into human-readable text, machine-readable JSON, or simplified
# output formats. Each formatter is optimized for different use cases:
# terminal display, IDE integration, or simple logging.
#
# Public API:
#   - TextFormatter - Rich terminal output with colors and source context
#   - JsonFormatter - Structured JSON for IDE/tool integration
#   - SimpleFormatter - Plain text for logs and non-terminal output
#
# Example: Terminal output with TextFormatter
#
#     use diagnostics.formatters.{TextFormatter}
#     use diagnostics.{Diagnostic, Span}
#
#     val source = "val x = 1 + true"
#     val span = Span(start: 12, end: 16, line: 1, column: 12)
#
#     val diag = Diagnostic.error("type mismatch")
#         .with_span(span)
#         .with_label(span, "expected i64, found bool")
#
#     # Create formatter with color support
#     val formatter = TextFormatter.new()
#     formatter.enable_colors = true
#     formatter.show_source = true
#
#     val output = formatter.format(diag, source)
#     print output
#
# Example: JSON output for language servers
#
#     use diagnostics.formatters.{JsonFormatter}
#     use diagnostics.{Diagnostic}
#
#     val diag = Diagnostic.warning("unused variable")
#         .with_code("W0612")
#
#     val formatter = JsonFormatter.new()
#     val json = formatter.format(diag, "")
#     # Returns: {"severity":"warning","code":"W0612","message":"unused variable"}
#
# Example: Simple formatter for logs
#
#     use diagnostics.formatters.{SimpleFormatter}
#     use diagnostics.{Diagnostic}
#
#     val diag = Diagnostic.error("file not found")
#         .with_code("E0001")
#
#     val formatter = SimpleFormatter.new()
#     val output = formatter.format(diag, "")
#     # Returns: "error[E0001]: file not found"

# Re-export formatters
export text_formatter.TextFormatter
export json_formatter.JsonFormatter
export simple_formatter.SimpleFormatter
