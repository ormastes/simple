# Unit: SolarLuminosity
# Kind: Power
# Symbol: solar_luminosity
# Canonical scale: 3.828e+26 W

class SolarLuminosity:
    value: f64

    fn to_f64() -> f64: self.value
    fn add(other: SolarLuminosity) -> SolarLuminosity: SolarLuminosity(value: self.value + other.value)
    fn sub(other: SolarLuminosity) -> SolarLuminosity: SolarLuminosity(value: self.value - other.value)

    static fn kind() -> text: "Power"
    static fn symbol() -> text: "solar_luminosity"
    static fn scale_to_base() -> f64: 3.828e+26
    static fn base_unit() -> text: "W"
    static fn zero() -> SolarLuminosity: SolarLuminosity(value: 0.0)

export SolarLuminosity
