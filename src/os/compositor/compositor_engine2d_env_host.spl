# Hosted Engine2D compositor environment policy.

extern fn rt_env_get(key: text) -> text?

fn engine2d_compositor_env_get(key: text) -> text?:
    rt_env_get(key)

export engine2d_compositor_env_get
