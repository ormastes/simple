# Explicit x86 compatibility capability for the baremetal WM input loop.
# Architecture-neutral shell code may call this only after the x86 composition
# root grants the matching capability.

use os.kernel.boot.cpu.{port_inb}

fn legacy_ps2_input_ready() -> bool:
    ((port_inb(0x64) as i32) & 1) != 0
