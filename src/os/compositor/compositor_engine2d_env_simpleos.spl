# SimpleOS has no process environment in the compositor lane. Backend policy
# is fixed by the guest build and device receipts rather than host variables.

fn engine2d_compositor_env_get(_key: text) -> text?:
    nil

export engine2d_compositor_env_get
