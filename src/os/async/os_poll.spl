# SimpleOS future poll result.

enum OsPoll<T>:
    Ready(value: T)
    Pending

impl OsPoll<T>:
    fn is_ready() -> bool:
        match self:
            case OsPoll.Ready(_): true
            case OsPoll.Pending: false

    fn is_pending() -> bool:
        not self.is_ready()
