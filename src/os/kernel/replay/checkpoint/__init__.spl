# SimpleOS Kernel — Container Checkpoint/Replay
#
# Snapshot and restore entire containers: process tree, memory,
# registers, file descriptors, filesystem overlay, IPC state.

export ContainerCheckpoint, ProcessSnapshot, FdEntry, FdKind
export IpcSnapshot, QueueSnapshot, FsOverlayEntry
export KernelObjectCheckpoint
