# Canonical provider for the optional RV32 NVMe firmware boot action.
#
# Stock RV32 boots have no optional firmware payload. Firmware-specific builds
# replace this module at composition time while boot code keeps the stable HAL.

pub fn hal_rv32_boot_optional_nvme_fw_selftest() -> i64:
    0
