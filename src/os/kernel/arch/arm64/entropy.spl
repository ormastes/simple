# ARM64 early-boot entropy mixer.

@cfg(arm64)
extern fn rt_rndr() -> u64
extern fn rt_time_now_unix_micros() -> i64
use os.kernel.arch.common.entropy_mix.{mix64}

fn entropy_seed_u64() -> u64:
    mix64(rt_rndr() ^ rt_time_now_unix_micros().to_u64() ^ 0x41524D3634)
