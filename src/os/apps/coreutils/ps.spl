# coreutils/ps — list processes
#
# Canonical process tool entry delegates to the userlib list_tasks owner.

use os.tools.proc.ps_tool.{run_ps}
#
# MDSOC: feature dimension, coreutils layer

pub fn main_ps(args: [text]) -> i32:
    run_ps(args)
