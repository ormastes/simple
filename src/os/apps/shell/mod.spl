# SimpleOS Shell — Module Root
#
# Interactive command shell with job control, pipes, and redirection.

use os.apps.shell.shell_app.{ShellApp}
use os.apps.shell.shell_job.{JobTable, Job, JobState}
use os.apps.shell.shell_pipe.{parse_pipeline, execute_pipeline, has_pipeline}
use os.apps.shell.shell_redirect.{parse_redirects, apply_redirects, restore_redirects, ParsedCommand}
use os.apps.shell.shell_async.{CommandFuture, spawn_command_async}
use os.apps.shell.shell_tools.{tool_touch, tool_head, tool_tail, tool_wc, tool_sort, tool_uniq, tool_which, tool_date, tool_true, tool_false, tool_tee, tool_ln, tool_chmod, tool_stat}
