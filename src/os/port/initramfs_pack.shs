#!/usr/bin/env sh
# Thin wrapper: bin/simple run src/os/port/initramfs_pack.spl -- <args>
set -eu
REPO_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
cd "$REPO_ROOT"
exec bin/simple run src/os/port/initramfs_pack.spl -- "$@"
