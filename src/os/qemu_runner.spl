# SimpleOS QEMU Runner — Multi-Architecture Boot & Test
#
# Provides build + run + test functionality for all 6 architectures.
# Usage:
#   bin/simple os build --arch=x86_64
#   bin/simple os run --arch=riscv64
#   bin/simple os test --arch=all

# Split from src/os/qemu_runner.spl to keep module cohesion under the 800-line source limit.
export use os.qemu_runner_part1.*
export use os.qemu_runner_part2.*
export use os.qemu_runner_part3.*
export use os.qemu_runner_part4.*
export use os.qemu_runner_part5.*
