# POSIX Pipes — implemented over IPC port pairs
#
# NOT YET IMPLEMENTED — needs IPC endpoint create syscall.
# Returns -ENOSYS on all calls.

val ENOSYS: i32 = 38

fn posix_pipe(fds_out_read: u64, fds_out_write: u64) -> i32:
    """Create a pipe. NOT YET IMPLEMENTED. Returns -ENOSYS."""
    0 - ENOSYS
