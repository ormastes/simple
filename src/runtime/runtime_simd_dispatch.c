/*
 * SIMD Text Dispatch — compilation unit for dispatch table support.
 * The actual dispatch table (g_simd_text) and init are in runtime_simd_utf8.c.
 * This TU exists so the build system sources list has a matching .c file.
 */
#include "runtime_simd_dispatch.h"
