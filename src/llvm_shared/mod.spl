# LLVM Shared Infrastructure
#
# Shared LLVM backend infrastructure for Core Simple and Full Simple compilers.
# Eliminates code duplication between compiler/ and compiler_core/ LLVM backends.
#
# Bootstrap-compatible: No generics, no Option types, seed_cpp compilable.

# Re-export all shared modules
export llvm_shared.tools.*
export llvm_shared.target.*
