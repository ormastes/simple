# Diagnostics Core Module
#
# Minimal diagnostic system without i18n or formatters.
# Used by the parser and compiler to avoid circular dependencies.
#
# This module provides only the core diagnostic types:
# - Severity levels
# - Source spans
# - Labels
# - Basic diagnostic creation
#
# For full diagnostics with i18n and formatters, use the lib.diagnostics module.
#
# Usage:
#     use compiler_shared.diagnostics.{Diagnostic, Severity, Span}
#
#     val span = Span(start: 0, end: 5, line: 1, column: 1)
#     val diag = Diagnostic.error("unexpected token")
#         .with_code("E0001")
#         .with_span(span)
#         .with_label(span, "expected identifier")

# Re-export core types
export severity.Severity
export span.Span
export label.Label
export diagnostic.Diagnostic
