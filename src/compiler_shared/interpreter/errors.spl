# Compatibility shim for legacy compiler_shared.interpreter.errors imports.
#
# The canonical definitions live under `compiler.interpreter.errors`.

use compiler.interpreter.errors.{ErrorExplanation, ErrorRegistry}

pub use ErrorExplanation
pub use ErrorRegistry
