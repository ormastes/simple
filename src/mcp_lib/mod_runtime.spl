# MCP Library - Runtime-Compatible Module
#
# Simplified version without export syntax for runtime compatibility
# Direct imports only - no re-exports

# This file exists for documentation purposes
# Runtime users should import directly from submodules:
#   use mcp_lib.helpers.{jp, js, make_result_response, ...}
#   use mcp_lib.core.{McpState, create_mcp_state, ...}
#   use mcp_lib.schema.{get_tool_schema, get_all_tool_schemas, ...}

# Runtime does NOT support `export module.{...}` syntax
# Users must import from specific modules
