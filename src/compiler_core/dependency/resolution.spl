# Dependency Resolution (delegates to compiler_shared)
use compiler_shared.dependency.resolution.{Segment, ModPath, FileKind, ResolutionResult}
use compiler_shared.dependency.resolution.{FileSystem, filesystem_new, filesystem_from_files}
use compiler_shared.dependency.resolution.{to_file_path, to_dir_path, resolve, is_well_formed}
export Segment, ModPath, FileKind, ResolutionResult
export FileSystem, filesystem_new, filesystem_from_files
export to_file_path, to_dir_path, resolve, is_well_formed
