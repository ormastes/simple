# Predicate Matching (delegates to compiler_shared)
use compiler_shared.predicate.{PredicateContext, Selector, SignaturePattern, ArgPatterns}
use compiler_shared.predicate.{Predicate, MatchContext}
export PredicateContext, Selector, SignaturePattern, ArgPatterns
export Predicate, MatchContext
