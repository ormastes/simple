# HIR Lowering Module
#
# Lowers AST to High-Level IR (HIR), performing desugaring and transformations.
# Handles async/await, pattern matching, comprehensions, and other high-level constructs.
#
# Public API:
#   - use compiler_core.hir_lowering.items.{lower_item}
#   - use compiler_core.hir_lowering.expressions.{lower_expr}
#   - use compiler_core.hir_lowering.statements.{lower_stmt}
#   - use compiler_core.hir_lowering.async.{lower_async_fn}

# All submodules are automatically available.
