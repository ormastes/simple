# Borrow Checking (delegates to compiler_shared)
use compiler_shared.borrow_check.lifetime.{Lifetime, Region}
use compiler_shared.borrow_check.lifetime.{ConstraintKind, LifetimeConstraint}
use compiler_shared.borrow_check.lifetime.{LifetimeEnv, LifetimeInference}
use compiler_shared.borrow_check.borrow_graph.{PlaceBase, PlaceElem, Place}
use compiler_shared.borrow_check.borrow_graph.{BorrowKind, Borrow, BorrowSet}
use compiler_shared.borrow_check.borrow_graph.{BorrowError, BorrowGraph}
use compiler_shared.borrow_check.nll.{BasicBlock, Terminator, ControlFlowGraph}
use compiler_shared.borrow_check.nll.{LivenessResult, LivenessAnalysis}
use compiler_shared.borrow_check.nll.{SpanLabel, NLLError, NLLChecker}
use compiler_shared.borrow_check.mod.{BorrowCheckResult, BorrowChecker}
use compiler_shared.borrow_check.mod.{check_mir_function, check_mir_module}
export Lifetime, Region
export ConstraintKind, LifetimeConstraint
export LifetimeEnv, LifetimeInference
export PlaceBase, PlaceElem, Place
export BorrowKind, Borrow, BorrowSet
export BorrowError, BorrowGraph
export BasicBlock, Terminator, ControlFlowGraph
export LivenessResult, LivenessAnalysis
export SpanLabel, NLLError, NLLChecker
export BorrowCheckResult, BorrowChecker
export check_mir_function, check_mir_module
