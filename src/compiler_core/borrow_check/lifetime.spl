# Borrow Check Lifetimes (delegates to compiler_shared)
use compiler_shared.borrow_check.lifetime.{Lifetime, Region}
use compiler_shared.borrow_check.lifetime.{ConstraintKind, LifetimeConstraint}
use compiler_shared.borrow_check.lifetime.{LifetimeEnv}
use compiler_shared.borrow_check.lifetime.{InferenceError, LifetimeInference}
use compiler_shared.borrow_check.lifetime.{Region__create, Region__universal}
use compiler_shared.borrow_check.lifetime.{LifetimeConstraint__outlives, LifetimeConstraint__equals}
use compiler_shared.borrow_check.lifetime.{LifetimeConstraint__is_static}
use compiler_shared.borrow_check.lifetime.{LifetimeEnv__create}
export Lifetime, Region
export ConstraintKind, LifetimeConstraint
export LifetimeEnv
export InferenceError, LifetimeInference
export Region__create, Region__universal
export LifetimeConstraint__outlives, LifetimeConstraint__equals
export LifetimeConstraint__is_static
export LifetimeEnv__create
