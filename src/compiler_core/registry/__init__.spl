# Registry Module
#
# Unified registry for compiler components: types, symbols, and metadata.
# Provides centralized access to compilation artifacts.
#
# Public API:
#   - use compiler_core.registry.mod.{Registry}
#   - use compiler_core.registry.unified_registry.{UnifiedRegistry}

# All submodules are automatically available.
