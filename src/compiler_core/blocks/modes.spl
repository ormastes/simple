# Block Lexer Modes (delegates to compiler_shared)
use compiler_shared.blocks.modes.{LexerMode, LexerConfig, SyntaxFeatures, Span}
use compiler_shared.blocks.modes.{TextSpan, PreLexInfo}
use compiler_shared.blocks.modes.{BlockSkipPolicy, BlockOutlineInfo}
use compiler_shared.blocks.modes.{BlockToken, BlockTokenKind}
export LexerMode, LexerConfig, SyntaxFeatures, Span
export TextSpan, PreLexInfo
export BlockSkipPolicy, BlockOutlineInfo
export BlockToken, BlockTokenKind
