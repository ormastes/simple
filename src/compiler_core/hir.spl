# HIR - High-level Intermediate Representation
#
# Delegates to src/compiler_shared/hir.spl

use compiler_shared.hir.{HirModule, HirImport, HirImportItem}
use compiler_shared.hir.{SymbolId, Symbol, SymbolKind, ScopeId, Scope, ScopeKind, SymbolTable}
use compiler_shared.hir.{MethodResolution}
use compiler_shared.hir.{HirType, HirTypeKind}
use compiler_shared.hir.{DimExpr, DimExprKind, DeviceType, LayerType, ParamShape}
use compiler_shared.hir.{Effect, EffectKind}
use compiler_shared.hir.{HirFunction, HirTypeParam, HirParam}
use compiler_shared.hir.{HirClass, HirStruct, HirField}
use compiler_shared.hir.{HirEnum, HirVariant, HirVariantKind}
use compiler_shared.hir.{HirTrait, HirTraitBound, HirImpl, HirConst}
use compiler_shared.hir.{HirExpr, HirExprKind, HirInterpolation, HirCallArg, HirMatchArm}
use compiler_shared.hir.{HirEnumPayload, HirComprehensionKind, HirCompClause, HirCompClauseKind}
use compiler_shared.hir.{HirBinOp, HirUnaryOp}
use compiler_shared.hir.{HirPattern, HirPatternKind, HirPatternPayload}
use compiler_shared.hir.{HirStmt, HirStmtKind, HirAssignOp, HirBlock}
use compiler_shared.hir.{HirLowering, LoweringError, LoweringErrorKind}

export HirModule, HirImport, HirImportItem
export SymbolId, Symbol, SymbolKind, ScopeId, Scope, ScopeKind, SymbolTable
export MethodResolution
export HirType, HirTypeKind
export DimExpr, DimExprKind, DeviceType, LayerType, ParamShape
export Effect, EffectKind
export HirFunction, HirTypeParam, HirParam
export HirClass, HirStruct, HirField
export HirEnum, HirVariant, HirVariantKind
export HirTrait, HirTraitBound, HirImpl, HirConst
export HirExpr, HirExprKind, HirInterpolation, HirCallArg, HirMatchArm
export HirEnumPayload, HirComprehensionKind, HirCompClause, HirCompClauseKind
export HirBinOp, HirUnaryOp
export HirPattern, HirPatternKind, HirPatternPayload
export HirStmt, HirStmtKind, HirAssignOp, HirBlock
export HirLowering, LoweringError, LoweringErrorKind
