# Backend API (delegates to compiler_shared)
use compiler_shared.backend.backend_api.{BackendKind, CodegenTarget, OptimizationLevel, BuildMode}
use compiler_shared.backend.backend_api.{CompileOptions, CompiledModule, CompiledSymbol, SymbolKind}
use compiler_shared.backend.backend_api.{CompileError}
use compiler_shared.backend.backend_api.{format_mir_module, select_backend, select_backend_with_mode}
use compiler_shared.backend.backend_api.{available_backends, gpu_backends, backend_for_name}
use compiler_shared.backend.backend_api.{compile_module_with_backend, get_effective_backend_name}
use compiler_shared.backend.backend_api.{Backend}
export BackendKind, CodegenTarget, OptimizationLevel, BuildMode
export CompileOptions, CompiledModule, CompiledSymbol, SymbolKind
export CompileError
export format_mir_module, select_backend, select_backend_with_mode
export available_backends, gpu_backends, backend_for_name
export compile_module_with_backend, get_effective_backend_name
export Backend
