# Desugaring Module
#
# Desugars high-level async/await syntax into state machines.
# Analyzes suspension points and generates poll-based async implementations.
#
# Public API:
#   - use compiler_core.desugar.mod.{desugar_async}
#   - use compiler_core.desugar.poll_generator.{generate_poll_fn}
#   - use compiler_core.desugar.state_enum.{generate_state_enum}

# All submodules are automatically available.
