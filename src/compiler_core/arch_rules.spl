# Architecture Rules (delegates to compiler_shared)
use compiler_shared.arch_rules.{RuleAction, ArchRule, ArchRulesConfig}
use compiler_shared.arch_rules.{DependencyKind, Dependency, ArchViolation, ArchRulesEngine}
export RuleAction, ArchRule, ArchRulesConfig
export DependencyKind, Dependency, ArchViolation, ArchRulesEngine
