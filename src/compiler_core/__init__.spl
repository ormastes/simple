# Simple Self-Hosting Compiler Module
#
# This file declares the `compiler` module namespace.
# All files in this directory can be imported as `compiler.module_name`.

mod compiler

# Public modules
mod lexer
mod treesitter
mod parser
mod hir
mod mir
mod codegen
mod driver
mod backend
mod resolve
mod type_infer
mod dim_constraints
mod blocks
mod init
mod mod

# Compilation context and pipeline
mod compilation_context
mod instantiation
mod pipeline_fn

# Shared modules (used by compiler, loader, linker)
mod inference

# Internal modules
mod config
mod di
mod aop

# Common imports for all modules
common use crate.std.*

# Export the main entry points
use driver.{CompileResult, CompilerDriver, CompileMode, CompileOptions}
use init.{CompilerContext, init_compiler, get_context}
