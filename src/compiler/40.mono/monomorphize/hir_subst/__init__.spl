# HIR body substitution (critical hardening Phase 3, lane M3).
# Entry point: subst_hir_body(body, subst_map) in body_subst.spl.

export use compiler.monomorphize.hir_subst.body_subst.{HirSubstReport, subst_hir_body, subst_hir_body_with_report, subst_hir_expr, subst_hir_stmt, subst_hir_pattern, subst_hir_block}
