# Re-exported from mod.spl
export ExecutionManager, CodeInfo, LocalExecutionManager
export set_jit_backend, get_jit_backend

# Tiered JIT (pure Simple policy, Rust only for codegen)
export TieredJitManager, TieredJitConfig, JitTier
export FunctionProfile, JitStats
export default_config, release_config, eager_config
