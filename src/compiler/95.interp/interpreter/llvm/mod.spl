# LLVM Shared Infrastructure
#
# Shared LLVM backend infrastructure for Core Simple and Full Simple compilers.
# Used by compiler LLVM backend components to avoid internal duplication.
#
# Bootstrap-compatible subset: no generics and no Option types in shared bootstrap-sensitive code.

# Re-export all shared modules
