#![allow(export_outside_init)]
# Forwarding module: compiler.backend_port -> compiler.backend.backend_port
#
# Tests import `compiler.backend_port.{BackendPort}`. The actual definition lives
# in `compiler/70.backend/backend_port.spl` which resolves as
# `compiler.backend.backend_port`. This file re-exports for the shorter path.

use compiler.backend.backend_port.{BackendPort}

export BackendPort
