# Desugaring Stage Ports
#
# Input/output port contracts for the desugaring pipeline stage.
# Desugaring transforms source-level syntax sugar into canonical forms.
# Operates on source text (5 passes).

struct DesugarInputPort:
    """Source text to desugar."""
    source_text: text
    module_name: text

struct DesugarOutputPort:
    """Desugared source text with injected declarations."""
    desugared_source: text
    injected_fn_names: [text]
    pass_count: i64

export DesugarInputPort, DesugarOutputPort
