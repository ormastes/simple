# Lexing Stage Ports
#
# Input/output port contracts for the lexing pipeline stage.
# The lexer takes source text and produces a token stream.
#
# Design: doc/research/compiler_mdsoc_design.md Section 2.2

struct LexerInputPort:
    """Source text provider for the lexer."""
    source_text: text

struct LexerOutputPort:
    """Token stream produced by the lexer (parallel arrays)."""
    token_tags: [i64]
    token_texts: [text]
    token_lines: [i64]
    token_cols: [i64]
    token_count: i64

export LexerInputPort, LexerOutputPort
