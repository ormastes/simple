# Transition-completeness model (hardening plan section 11.2-11.4).
# Explicit re-exports, matching the convention in compiler/00.common/__init__.spl.

export use compiler.common.transition.coverage_state.{
    CoverageState,
    coverage_state_tag, coverage_state_is_handled,
    coverage_state_describe, coverage_state_from_fields, split_state_payload
}

export use compiler.common.transition.transition_table.{
    TransitionRow, TransitionTable,
    parse_transition_table, variant_name_of,
    unquote_scalar, split_key_value
}

export use compiler.common.transition.validator.{
    CoverageReport, validate_transition_table,
    coverage_report_is_clean, coverage_report_handled
}
