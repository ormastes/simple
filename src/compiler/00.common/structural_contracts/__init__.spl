# WS3-style explicit re-exports — no wildcard hub.
# Each export use points to the specific child module that defines the symbol.

# Re-exported from sidecars.spl
export use compiler.common.structural_contracts.sidecars.{
    COMPILER_SIDECAR_CONTRACT_VERSION, CompilerIrKind, ShardRef,
    TagShardRef, OriginShardRef, QueryIndexShardRef, ProfileShardRef, DiagnosticArenaRef,
    TagShard, OriginShard, QueryIndexShard, MirProfileShard
}

# Re-exported from ports.spl
export use compiler.common.structural_contracts.ports.{
    COMPILER_PORT_CONTRACT_VERSION, AstView, MirOptView, ObjectFileView,
    ParsingOutputPort, OptimizationInputPort, MutationReceipt, OptimizationOutputPort,
    PlacementHintShardRef, LinkingInputPort,
    TransformBoundary, SidecarErrorCode, SidecarError, SidecarBundle, SidecarMapper
}

# Re-exported from aop.spl
export use compiler.common.structural_contracts.aop.{
    COMPILER_AOP_CONTRACT_VERSION, TYPE_PREDICATE_BYTECODE_VERSION,
    TypePredicateInstruction, TypePredicateBytecode, TypePredicateDescriptor,
    FacetRepresentation, FacetMultiplicity, FacetAccess, FacetBindingPlan,
    PointcutQueryRef, PointcutSelectorKind,
    AdviceKind, AdviceTemplateRef, AopRule, WeavingReceipt
}

# Re-exported from optimizer.spl
export use compiler.common.structural_contracts.optimizer.{
    COMPILER_OPTIMIZER_CONTRACT_VERSION, BackendPolicy, EffectSummary,
    OptimizationPassContract, OptimizationRemark
}

# Re-exported from offload_profile.spl
export use compiler.common.structural_contracts.offload_profile.{
    COMPILER_OFFLOAD_PROFILE_CONTRACT_VERSION, CompilerStage, OffloadMode, OffloadFallbackPolicy,
    CompilerOffloadProfile, OffloadDecision,
    validate_offload_profile, cpu_only_profile
}
