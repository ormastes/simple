# Completeness seal — semantic admission layer above the aspect-pack loader.
# Hardening plan 2026-08-21 §13.1, §13.2, §16.4, §17.4, §17.6.
#
# Identity, closure/linkage/activation and dense tags come from A4's
# `compiler.common.dynamic_identity`; import them from there, not from here.

export use compiler.loader.completeness_seal.axis_parse.{
    closure_from_text,
    linkage_from_text,
    activation_from_text,
    first_use_obligation_met
}

export use compiler.loader.completeness_seal.manifest.{
    TransitionDecl,
    ExtensionManifest,
    ExtendsDecl,
    ModuleManifest,
    ManifestDoc,
    ManifestError,
    manifest_error_text,
    parse_manifest_text
}

export use compiler.loader.completeness_seal.required_interfaces.{
    required_operations,
    required_module_interfaces,
    operation_interface_name,
    not_applicable_operations,
    effective_required_operations,
    missing_operations,
    missing_module_interfaces
}

export use compiler.loader.completeness_seal.seal.{
    SealError,
    SealConfig,
    CompletenessSeal,
    seal,
    identity_of,
    default_capacity,
    seal_error_code,
    seal_error_text
}
