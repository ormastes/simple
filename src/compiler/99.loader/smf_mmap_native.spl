# Compatibility SMF mmap/native execution shim.

fn native_alloc_exec_memory(size: i64) -> i64:
    0

fn native_write_exec_memory(addr: i64, code: [u8], offset: i64) -> i64:
    0

fn native_make_executable(addr: i64, size: i64) -> bool:
    false

fn native_call_function_0(addr: i64) -> i64:
    0

fn native_free_exec_memory(addr: i64, size: i64) -> bool:
    true

export native_alloc_exec_memory, native_write_exec_memory
export native_make_executable, native_call_function_0, native_free_exec_memory
