# Compatibility facade. The shared contract owns the executable-memory
# transaction so root and nested loader surfaces use one authority.
export use compiler.loader.shared_contract.executable_mapping.*
