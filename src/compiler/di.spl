#![allow(export_outside_init)]
# Forwarding module: compiler.di -> compiler.common.di
#
# Tests import `compiler.di.{DiContainer, ...}`. The actual definition lives in
# `compiler/00.common/di.spl` which resolves as `compiler.common.di`.
# This file re-exports for the shorter path.

use compiler.common.di.{
    DiContainer,
    Binding,
    di_is_system_test_locked,
    set_container,
    get_container
}

export DiContainer
export Binding
export di_is_system_test_locked
export set_container
export get_container
