# HIR Lowering - AST to HIR Transformation (Facade)
#
# This is a facade module that re-exports functionality from the hir_lowering/ subdirectory.
# The implementation has been split into focused modules:
#
# - hir_lowering/types.spl       - Type definitions, error handling, type lowering
# - hir_lowering/expressions.spl - Expression and pattern lowering
# - hir_lowering/statements.spl  - Statement lowering
# - hir_lowering/items.spl       - Item lowering (functions, classes, structs, enums, traits, impls)
# - hir_lowering/async.spl       - Async/await support (Future<T>, validation)
#
# This facade pattern keeps imports stable while improving code organization.

# Re-export main types
use hir_lowering.types.{HirLowering, LoweringError, LoweringErrorKind}
use hir_lowering.async.{AsyncFunctionCheck}
use hir_lowering.async_errors.{
    AsyncErrorCode,
    AsyncError,
    AsyncErrorCollector
}

export HirLowering, LoweringError, LoweringErrorKind, AsyncFunctionCheck
export AsyncErrorCode, AsyncError, AsyncErrorCollector
