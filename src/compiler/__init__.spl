# Simple Self-Hosting Compiler Module
#
# This file declares the `compiler` module namespace.
# All files in this directory can be imported as `compiler.module_name`.

mod compiler

# Public modules
pub mod lexer
pub mod treesitter
pub mod parser
pub mod hir
pub mod mir
pub mod codegen
pub mod driver
pub mod backend
pub mod resolve
pub mod type_infer
pub mod dim_constraints
pub mod blocks
pub mod init
pub mod mod

# Compilation context and pipeline
pub mod compilation_context
pub mod instantiation
pub mod pipeline_fn

# Shared modules (used by compiler, loader, linker)
pub mod inference

# GC/NoGC configuration
pub mod gc_config
pub mod gc_boundary

# Internal modules
mod config
mod di
mod aop

# Common imports for all modules
common use crate.std.*

# Export the main entry points
export use driver.{CompileResult, CompilerDriver, CompileMode, CompileOptions}
export use init.{CompilerContext, init_compiler, get_context}
