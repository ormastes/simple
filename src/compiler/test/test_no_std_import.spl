# Only import compiler.driver, not std.spec
use compiler.driver.driver.*

describe "Test without explicit std import":
    it "works":
        val mode = CompileMode.Interpret
        expect(mode.to_text()).to_equal("interpret")
