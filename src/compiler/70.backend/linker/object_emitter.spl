# Object Emitter - stubbed for bootstrap
# Original backed up to /tmp/object_emitter.spl.bak

struct ObjectCodeUnit:
    name: text
    code: [u8]

fn assemble_code_units(units: [ObjectCodeUnit], obj_path: text, verbose: bool) -> Result<text, text>:
    Err("object_emitter stubbed for bootstrap")

export assemble_code_units
export ObjectCodeUnit
