# Leak Check Module
#
# Unified memory leak detection tool covering both internal (GC-level)
# and external (ASan/Valgrind via C backend) leak detection.
#
# Usage: bin/simple leak-check [options] <source.spl>
