# Core duplication detection engine split facade

export use compiler.tools.duplicate_check.detector_part1.{
    TokenInterner, new_token_interner, intern_token,
    log_progress, log_verbose, progress_on, progress_interval, rss_mb, progress_tick,
    DuplicateBlock, DuplicateGroup, HashScanSummary, TokenWindowCandidate, SimilarityPrep,
    rolling_hash, extract_code_snippet, extract_code_snippet_by_line,
    block_line_count, total_lines_for_blocks, _merge_blocks, sort_blocks_by_file_and_line,
    collapse_overlapping_blocks, build_duplicate_group, duplicate_block_signature,
    materialize_block_code, has_signal_word, count_executable_lines, count_nonempty_lines,
    is_low_signal_block, compute_token_code, token_kind_code, power_mod, read_detector_source,
    load_tokens_for_file, build_token_codes, collect_token_window_candidates,
    collect_hash_counts_in_file, collect_repeated_hashes, find_duplicates_in_file,
    find_duplicates_in_file_with_hash_filter, bucket_exact_blocks, contains_high_signal_block,
    add_exact_groups, group_exact_blocks, _merge_groups, sort_by_impact
}
export use compiler.tools.duplicate_check.detector_part2.{
    collect_files, sample_blocks_for_similarity, load_tokens_with_line_indexes,
    build_similarity_adjacency, build_similarity_groups, refine_groups_with_similarity,
    parse_idx, scan_duplicate_blocks, find_duplicates, find_duplicates_with_options
}
