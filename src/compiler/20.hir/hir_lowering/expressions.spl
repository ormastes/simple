# HIR expression lowering umbrella.
#
# Implementations live in cohesive extension modules under _Expressions so
# callers retain the stable compiler.hir.hir_lowering.expressions surface.

export use compiler.hir.hir_lowering._Expressions.expression_support.*
export use compiler.hir.hir_lowering._Expressions.expression_core.*
export use compiler.hir.hir_lowering._Expressions.expression_components.*
export use compiler.hir.hir_lowering._Expressions.match_desugaring.*
export use compiler.hir.hir_lowering._Expressions.block_and_asm_lowering.*
