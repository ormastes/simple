# Remote cache namespace policy + client protocol.
#
# namespace_policy.spl — the §15.4 read/write matrix, enforced in code.
# remote_client.spl    — the remote protocol seam plus a local-filesystem fake.
#
# Nothing in this directory may enter an ActionDigest: it is all PROVENANCE or
# LOCAL_STATE per the C0 classification table.
