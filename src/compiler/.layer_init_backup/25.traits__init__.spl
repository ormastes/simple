# Traits Layer — Layer 25
#
# Trait definition, implementation, solving, coherence, and validation.

mod traits

arch {
    tier = "core"
    layer = 25
}

friend types
friend mir
friend backend

pub mod traits
pub mod trait_def
pub mod trait_impl
pub mod trait_solver
pub mod trait_coherence
pub mod trait_validation
pub mod trait_method_resolution

export TraitDef, TraitImpl, TraitSolver
