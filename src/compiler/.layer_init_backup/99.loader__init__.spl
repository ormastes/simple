# Loader Layer — Layer 99
#
# Module resolver and loader. Highest layer — can import from all others.

mod loader

arch {
    tier = "core"
    layer = 99
}

pub mod loader
pub mod module_resolver

export ModuleResolver, ModuleLoader
