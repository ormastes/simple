# HIR Layer — Layer 20
#
# High-level Intermediate Representation types, definitions, and lowering.

mod hir

arch {
    tier = "core"
    layer = 20
}

friend types
friend traits
friend mir

pub mod hir
pub mod hir_types
pub mod hir_definitions
pub mod hir_lowering
pub mod inference

export HirModule, HirExpr, HirStmt
internal_export HirLowering, HirBuilder
