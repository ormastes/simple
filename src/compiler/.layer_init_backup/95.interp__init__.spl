# Interpreter Layer — Layer 95
#
# Interpreter, MIR interpreter, and execution engine.

mod interp

arch {
    tier = "core"
    layer = 95
}

pub mod interpreter
pub mod mir_interpreter
pub mod execution

export Interpreter
