# Block System Module - Delegates to compiler_shared
use compiler_shared.blocks.mod.{BlockDefinition, BlockRegistry, block_registry}
use compiler_shared.blocks.mod.{init_blocks, reset_blocks}
use compiler_shared.blocks.mod.{LexerMode, LexerConfig, SyntaxFeatures}
use compiler_shared.blocks.mod.{BlockContext, BlockError, BlockValue}
use compiler_shared.blocks.mod.{enable_block, disable_block, list_blocks, BlockInfo}
use compiler_shared.blocks.mod.{define_block, define_const_block, validated_block}
use compiler_shared.blocks.mod.{block, block_with_validation, const_block}
use compiler_shared.blocks.mod.{BlockBuilder}
use compiler_shared.blocks.mod.{register_block, unregister_block, is_block, with_block}

export BlockDefinition, BlockRegistry, block_registry
export init_blocks, reset_blocks
export LexerMode, LexerConfig, SyntaxFeatures
export BlockContext, BlockError, BlockValue
export enable_block, disable_block, list_blocks, BlockInfo
export define_block, define_const_block, validated_block
export block, block_with_validation, const_block
export BlockBuilder
export register_block, unregister_block, is_block, with_block
