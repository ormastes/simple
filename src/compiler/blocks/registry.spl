# Block Registry (delegates to compiler_shared)
use compiler_shared.blocks.registry.{BlockRegistry, RegistryBuilder}
use compiler_shared.blocks.registry.{block_registry, register_block, unregister_block, is_block}
use compiler_shared.blocks.registry.{get_block, list_blocks, is_block_registered, with_block}
export BlockRegistry, RegistryBuilder
export block_registry, register_block, unregister_block, is_block
export get_block, list_blocks, is_block_registered, with_block
