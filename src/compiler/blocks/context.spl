# Block Context - Delegates to compiler.blocks for compatibility
use compiler.blocks.context.{BlockContext, BlockError, BlockErrorKind}
use compiler.blocks.context.{Diagnostic, DiagnosticLevel}

export BlockContext, BlockError, BlockErrorKind
export Diagnostic, DiagnosticLevel
