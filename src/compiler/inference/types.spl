# Type Inference Types (delegates to compiler_shared)
use compiler_shared.inference.types.{Type, TypeVarId, SkolemId, DeferredTypeId}
use compiler_shared.inference.types.{Field, Variant, Constraint, TypeScheme, TypeEnv}
use compiler_shared.inference.types.{UnifyError, InferError}
export Type, TypeVarId, SkolemId, DeferredTypeId
export Field, Variant, Constraint, TypeScheme, TypeEnv
export UnifyError, InferError
