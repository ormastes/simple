# Aspect-Oriented Programming (delegates to compiler_shared)
use compiler_shared.aop.{PointcutKind, Pointcut, AdviceKind, AdviceForm, Advice}
use compiler_shared.aop.{LogAspect, TracingAspect, ContractAspect}
use compiler_shared.aop.{JoinPointKind, JoinPoint, JoinPointContext}
use compiler_shared.aop.{MatchedAdvice, WeavingRule, WeavingConfig, WeavingResult}
use compiler_shared.aop.{WeavingDiagnostic, DiagnosticLevel}
use compiler_shared.aop.{AopWeaver}
export PointcutKind, Pointcut, AdviceKind, AdviceForm, Advice
export LogAspect, TracingAspect, ContractAspect
export JoinPointKind, JoinPoint, JoinPointContext
export MatchedAdvice, WeavingRule, WeavingConfig, WeavingResult
export WeavingDiagnostic, DiagnosticLevel
export AopWeaver
