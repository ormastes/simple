# Codegen Stage Ports
#
# Input/output port contracts for the codegen pipeline stage.
# Codegen maps MIR to target-specific output via a BackendPort.

struct CodegenInputPort:
    """MIR program view from the mir_to_backend transform."""
    function_count: i64
    extern_fn_count: i64
    string_literal_count: i64

struct CodegenOutputPort:
    """Backend output for the linker."""
    object_byte_count: i64
    symbol_count: i64
    target_triple: text
    success: bool

export CodegenInputPort, CodegenOutputPort
