# HIR Lowering Stage — HirLowerOutputPort
#
# HIR module produced by lowering.

use compiler.mdsoc.feature.hir_lowering.app.hir_function.{HirFunction}

struct HirLowerOutputPort:
    """HIR module produced by lowering."""
    functions: [HirFunction]
    struct_count: i64
    error_count: i64

export HirLowerOutputPort
