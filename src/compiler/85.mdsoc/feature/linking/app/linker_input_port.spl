# Linking — LinkerInputPort
#
# Object files and libraries ready for linking.

use common.compute.placement_contracts.handles.{ArenaResidency}
use common.compute.placement_contracts.semantic.{AnyObject}

struct LinkerInputPort:
    """Object files and libraries ready for linking."""
    object_file_count: i64
    library_count: i64
    output_type: text
    output_path: text
    input_residency: ArenaResidency<AnyObject>
