# Linking — LinkerOutputPort
#
# Result of linking.

use common.compute.placement_contracts.handles.{ArenaResidency}
use common.compute.placement_contracts.semantic.{AnyObject}

struct LinkerOutputPort:
    """Result of linking."""
    output_path: text
    output_size_bytes: i64
    success: bool
    error_message: text
    output_residency: ArenaResidency<AnyObject>
