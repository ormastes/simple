# Parsing Stage — ParserInputPort
#
# Token stream from the lexer.

struct ParserInputPort:
    """Token stream from the lexer."""
    token_tags: [i64]
    token_texts: [text]
    token_lines: [i64]
    token_cols: [i64]
    token_count: i64
    source_text: text

