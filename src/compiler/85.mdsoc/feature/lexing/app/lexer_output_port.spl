# Lexing Stage — LexerOutputPort
#
# Token stream produced by the lexer.

struct LexerOutputPort:
    """Token stream produced by the lexer (parallel arrays)."""
    token_tags: [i64]
    token_texts: [text]
    token_lines: [i64]
    token_cols: [i64]
    token_count: i64

export LexerOutputPort
