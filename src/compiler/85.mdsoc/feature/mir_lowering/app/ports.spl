# MIR Lowering Stage Ports
#
# Input/output port contracts for the MIR lowering pipeline stage.
# MIR lowering converts HIR structured control flow into flat basic blocks.

struct MirLowerInputPort:
    """HIR view from the hir_to_mir transform."""
    module_name: text
    function_count: i64

struct MirLowerOutputPort:
    """MIR program produced by lowering."""
    function_count: i64
    extern_fn_names: [text]
    string_literal_count: i64
    error_count: i64

export MirLowerInputPort, MirLowerOutputPort
