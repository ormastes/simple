# MDSOC Module — Multi-Dimensional Separation of Concerns
#
# Provides manifest-composed virtual capsules for the Simple compiler.
#
# Modules:
#   types.spl             — Core type definitions (structs, enums)
#   construct_types.spl   — Construct dimension types (second axis)
#   config.spl            — SDN config parser for capsule.sdn
#   layer_checker.spl     — Layer dependency enforcement
#   construct_checker.spl — Construct tier enforcement (second dimension)
#   cross_query.spl       — Cross-dimension queries (feature x construct)
#
# Usage:
#   use compiler.mdsoc.types.{MdsocManifest, CaretId, LayerDef, ...}
#   use compiler.mdsoc.config.{load_mdsoc_config, parse_mdsoc_sdn}
#   use compiler.mdsoc.layer_checker.{LayerChecker, check_layer_dep, ...}
#   use compiler.mdsoc.construct_checker.{ConstructLayerChecker}
#   use compiler.mdsoc.cross_query.{query_cross_dimension, query_by_construct}

# Re-export core types
export CaretId, CaretMapping
export DimensionDef, DimensionMap
export LayerDirection, LayerDef
export VirtualCapsule, SurfaceBinding, CapsuleExport
export CapsuleVisibility
export BypassGrant, BypassUsage
export CapsuleRules, MdsocManifest

# Re-export construct dimension types
export ConstructKind, ConstructTier
export SharedBinding, ConstructCapsule
export CrossDimensionQuery, CrossDimensionResult
