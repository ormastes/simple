# HWIR module exports.

export use compiler.mir.hwir.types.{
    HwModule, HwPort, HwSignal, HwRegister, HwCombOp,
    HwMemory, HwFsm, HwPipeline, HwClockDomain, HwResetPolicy,
    HwResourceBinding, HwSourceMap, HwCostModel, CoreConfig, HwNodeId,
    HwOrigin, HwConstant, HwCompareOp, HwSelectOp,
    HwModuleDef
}
export use compiler.mir.hwir.mir_to_hwir.{
    HwirLowerInput, HwirLowerResult,
    lower_mir_summary_to_hwir, lower_strict_hwir_and_module,
    lower_strict_mir_function_to_hwir,
    hwir_default_clock_domain,
    hwir_input_port, hwir_output_port,
    hwir_local_to_signal, hwir_local_to_register
}
export use compiler.mir.hwir.sequential.{
    HwStateRegister, HwSeqGuard, HwSeqValue, HwSeqAssignment, HwSeqRule,
    HwSeqOutputBinding, HwSeqInstancePin, HwSequentialPlan
}
export use compiler.mir.hwir.riscv_scalar_core.{
    RiscvGen2ScalarElaboration, RiscvGen2ScalarDispatchPlan,
    riscv_gen2_scalar_elaboration, riscv_gen2_scalar_dispatch
}
export use compiler.mir.hwir.aspects.{
    HwAspectManifest, HwAspectLockEntry, HwAspectLock, HwAspectApplication,
    HwAspectPlan, HwAspectProbe, HwAspectWeaveResult, hwir_aspect_plan_absent,
    hwir_aspect_lock_for_plan, hwir_aspect_plan_lock_diagnostic,
    hwir_aspect_lock_sha256,
    weave_hwir_observational_ports, weave_hwir_observational_ports_locked
}
