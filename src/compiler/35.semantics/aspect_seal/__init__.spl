# Aspect completeness seal — hardening plan 2026-08-21 Phase 6 (§13.3-§13.5).
#
# Scope note: fixtures only. The weaver, witness/sidecar ABI and any signature
# verifier are UNLANDED; nothing exported here verifies a signature.

export use compiler.semantics.aspect_seal.facet_model.{
    AdviceMode,
    advice_mode_from_text,
    advice_mode_text,
    advice_mode_critical_safe,
    FacetActivation,
    facet_activation_from_text,
    activation_text,
    Pointcut,
    FacetDecl,
    AdviceDecl,
    AspectDecl,
    FacetBinding,
    AspectBindingSet,
    BindingReport,
    binding_completeness,
    sorted_text,
    canonical_lines,
    binding_set_hash,
    AspectDocError,
    aspect_doc_error_text,
    AspectDoc,
    parse_aspect_doc
}

export use compiler.semantics.aspect_seal.seal.{
    AspectSealReason,
    aspect_seal_reason_code,
    aspect_seal_reason_text,
    AspectSealProfile,
    profile_tag,
    AspectSeal,
    aspect_seal_check,
    post_weave_recheck
}
