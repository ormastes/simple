# semantic_api — recursive primitive-API semantic checker (mission-critical lane A1)
#
# Standalone library; NOT wired into the live lint pipeline yet (integration
# is a later serialized step).

export type_walk.*
export checker.*
export alias_registry.*
