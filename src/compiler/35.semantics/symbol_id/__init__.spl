# Stable SymbolId foundation — re-exports.
# See stable_id.spl (id derivation) and index.spl (short-form index).

export use compiler.semantics.symbol_id.stable_id.{
    SymbolComponents, symbol_id_schema_version,
    stable_symbol_id, short_fingerprint, short_fingerprint_n,
    default_short_len, clamp_short_len, format_short_link,
    normalize_signature, find_short_collisions, check_short_forms_unique
}

export use compiler.semantics.symbol_id.index.{
    SymbolIndex, build_symbol_index, build_symbol_index_with_short_len
}
