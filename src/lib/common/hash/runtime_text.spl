# Stable Pure-Simple text hash for dynSMF source and interface sidecars.
# Keep the same UTF-8 FNV-1a/64 bytes and signed wrap as rt_hash_text.

use std.hash.{rt_hash_text}

pub fn runtime_text_hash_v1(value: text) -> i64:
    rt_hash_text(value)
