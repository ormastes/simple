# Common GPU Types
#
# Shared GPU type definitions usable by both gc_async_mut and nogc_sync_mut GPU modules.
# Follows the common/torch/interface.spl pattern.
#
# Public API:
#   use std.common.gpu.device.{GpuBackend, Gpu, gpu_cuda, gpu_vulkan, gpu_none}

# All submodules are automatically available.
