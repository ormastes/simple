# Shared DBFS/VFS mount capability vocabulary.
#
# Kernel-side publication and userspace syscall decoding intentionally remain
# in their respective owners; this module only freezes the wire-state type.

pub enum DbfsVfsMountCapabilityV1:
    Unavailable
    DbfsMountedRecoveryCompleteDurableTransactionalReplace
