# Migration shim — src/lib/common/units/ (deprecated)
#
# Moved to: unit.simple-lang.*
# This file re-exports from the new tree so existing `use std.common.units.*`
# imports keep working through one release cycle.
#
# @deprecated("moved to unit.simple-lang; remove in 0.11.0")

# Re-export the catalog model (still physically located at
# src/lib/common/units/model/world_units.spl — Track A's registry reuses it).
use std.common.units.model.world_units.{
    QuantityDomain,
    UnitClass,
    ExactRatio,
    UnitFactor,
    UnitExpression,
}
export QuantityDomain
export UnitClass
export ExactRatio
export UnitFactor
export UnitExpression
