# Doctest Parser
#
# Parses docstring code examples for testing.
# TODO: [doc][P2] Full implementation of doctest parsing.

enum Expected:
    Output(value: text)
    Empty
    Exception(exc_type: text, msg: text?)
