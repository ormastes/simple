# Common validation utilities

fn clamp(value: f64, min_val: f64, max_val: f64) -> f64:
    if value < min_val:
        return min_val
    if value > max_val:
        return max_val
    value

export clamp
