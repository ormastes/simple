# Reproducer: cross-module call to a fn that uses `text.from_char_code`

fn _uses_text_static_method(n: int) -> text:
    return text.from_char_code(n)

fn _no_text_static() -> text:
    return "literal"
