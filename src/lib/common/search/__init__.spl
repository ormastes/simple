# ============================================================================
# common/search — Phase-1 search/pattern-matching package facade
#
# Behavior-carrying search custom types + the Phase-1 algorithm set, built on
# the common/bytes foundation. Core rule: prefilter -> candidate -> verify -> rank.
#
# Plan: doc/03_plan/lib/search/custom_type_alpha_search_team_plan_2026-06-15.md
# ============================================================================

# --- Type barrier (AC-1): always exported ---
export use std.common.search.types.{Haystack, Pattern, MatchPos, MatchSpan, Score, PostingList, Embedding}

# --- Phase-1 algorithm modules (wired by orchestrator after fan-out) ---
export use std.common.search.exact.*
export use std.common.search.multi.*
export use std.common.search.prefilter.*
export use std.common.search.simd_scan.*

# --- Phase-2 index / rank / ANN / bitmap modules ---
export use std.common.search.inverted_index.*
export use std.common.search.ranking.*
export use std.common.search.ann.*
export use std.common.search.roaring.*

# --- Canonical deterministic lexical search contract (Wave 4) ---
export use std.common.search.analyzer.*
export use std.common.search.document.*
export use std.common.search.corpus_stats.*
export use std.common.search.query.*
export use std.common.search.top_k.*
export use std.common.search.explain.*
export use std.common.search.snapshot.*
export use std.common.search.provider.*

# --- Frozen Wave-4 Package 0 contracts (2026-08-31) ---
export use std.common.search.analyzer_contract.*
export use std.common.search.fusion_types.*
export use std.common.search.contract_ids.*
export use std.common.search.canonical_json.*
