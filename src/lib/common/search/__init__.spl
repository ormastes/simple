# ============================================================================
# common/search — Phase-1 search/pattern-matching package facade
#
# Behavior-carrying search custom types + the Phase-1 algorithm set, built on
# the common/bytes foundation. Core rule: prefilter -> candidate -> verify -> rank.
#
# Plan: doc/03_plan/lib/search/custom_type_alpha_search_team_plan_2026-06-15.md
# ============================================================================

# --- Type barrier (AC-1): always exported ---
export use std.common.search.types.{Haystack, Pattern, MatchPos, MatchSpan, Score, PostingList, Embedding}

# Phase-1 modules wired by orchestrator after fan-out:
#   The four algorithm files below do NOT exist yet — they are produced by the
#   parallel domain agents. Once they land, the orchestrator uncomments these
#   lines so the package re-exports them. Domain agents must NOT edit this file.
#
# export use std.common.search.exact.*
# export use std.common.search.multi.*
# export use std.common.search.prefilter.*
# export use std.common.search.simd_scan.*
