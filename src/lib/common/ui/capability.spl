# UI backend capability flags and unsupported-operation marker.

enum Capability:
    Mouse
    Color
    Images
    Touch
    NativeDialogs
    Notification

class NotSupported:
    operation: text

    static fn new(operation: text) -> NotSupported:
        return NotSupported(operation: operation)
