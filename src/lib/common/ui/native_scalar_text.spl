# Native-safe scalar text projection for operational UI and WM identities.

extern fn rt_raw_i64_to_string(value: i64) -> text

fn ui_native_i64_text(value: i64) -> text:
    rt_raw_i64_to_string(value)
