# Variant-neutral address for the pure-Simple normalization layers.
#
# The implementation lives in `std.gc_async_mut.pure.nn.norm`; this module
# only forwards it so consumers outside the `gc_async_mut` variant can name
# it without hard-coding that variant. No definitions live here.
pub use std.gc_async_mut.pure.nn.norm.*
