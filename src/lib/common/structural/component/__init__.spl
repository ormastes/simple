# common/structural/component — component descriptor contract (Phase B).
# Explicit re-exports only — no `export use *`, which the linter flags.
export use std.common.structural.component.descriptor.{
    ComponentDescriptor,
    ComponentParseOutcome,
    ComponentValidation,
    component_presence_values,
    component_placement_values,
    component_activation_values,
    component_axis_value_valid,
    component_descriptor_default,
    component_list_value_parse,
    component_descriptor_parse_sdn,
    component_catalog_validate,
    component_effective_dynload_setting
}
