# ============================================================================
# common/structural/identity — FROZEN identity contract (ID-TAG lane)
#
# Architecture §4 (stable identity model), §26 (contract freeze),
# §27 (ID-TAG owns structural/identity). Contract document:
# doc/05_design/platform/structural_compute/identity_tagmap_contract_v1.md
#
# Explicit re-exports only — no `export use *`, which the linter flags for
# exposing unnecessary interface.
# ============================================================================

export use std.common.structural.identity.entity_id.{
    ARTIFACT_ID_LEN,
    ENTITY_KEY_LEN,
    ENTITY_REF_LEN,
    HASH128_LEN,
    IDENTITY_SCHEMA_VERSION,
    SEMANTIC_ENTITY_KEY_LEN,
    SNAPSHOT_ID_LEN,
    SOURCE_ANCHOR_LEN,
    ArtifactId,
    EntityKey,
    EntityRef,
    Hash128,
    RevisionPair,
    SemanticEntityKey,
    SnapshotId,
    SourceAnchor,
    artifact_id,
    artifact_id_equal,
    entity_key,
    entity_key_equal,
    entity_key_resolvable_in,
    entity_ref,
    entity_ref_equal,
    entity_ref_stale,
    hash128,
    hash128_equal,
    revision_pair,
    semantic_entity_key,
    semantic_entity_key_equal,
    snapshot_id,
    snapshot_id_equal,
    snapshot_supersedes,
    source_anchor,
    source_anchor_equal
}

export use std.common.structural.identity.identity_codec.{
    EntityKeyResult,
    EntityRefResult,
    SemanticEntityKeyResult,
    SnapshotIdResult,
    SourceAnchorResult,
    artifact_id_put,
    artifact_id_read,
    decode_entity_key,
    decode_entity_ref,
    decode_semantic_entity_key,
    decode_snapshot_id,
    decode_source_anchor,
    encode_entity_key,
    encode_entity_ref,
    encode_semantic_entity_key,
    encode_snapshot_id,
    encode_source_anchor,
    entity_key_put,
    entity_key_read,
    entity_ref_put,
    entity_ref_read,
    hash128_put,
    hash128_read,
    identity_zero_artifact,
    identity_zero_hash,
    semantic_entity_key_put,
    semantic_entity_key_read,
    snapshot_id_put,
    snapshot_id_read,
    source_anchor_put,
    source_anchor_read
}
