# ============================================================================
# common/structural/resolve — FROZEN resolve contract (LINK lane)
#
# Architecture Part VII §18 (GraphResolveCore), §19 (SMF link pipeline),
# §29 Wave 7. Contract document:
# doc/05_design/platform/structural_compute/link_manager_contract_v1.md
#
# Explicit re-exports only — no `export use *`, which the linter flags for
# exposing unnecessary interface.
# ============================================================================

export use std.common.structural.resolve.resolve_types.{
    DEFINITION_RECORD_LEN,
    LINK_MUTATION_KIND_MAX,
    LINK_TAG_HOT_ORDER,
    LINK_TAG_ICF_CANDIDATE,
    LINK_TAG_OUTPUT_RANGE,
    LINK_TAG_REACHABLE,
    LINK_TAG_RELOCATION_KIND,
    LINK_TAG_SECTION_ALIGNMENT,
    LINK_TAG_SECTION_KIND,
    LINK_TAG_SYMBOL_BINDING,
    LINK_TAG_SYMBOL_RESOLUTION,
    LINK_TAG_SYMBOL_VISIBILITY,
    REFERENCE_RECORD_LEN,
    RESOLUTION_RECORD_LEN,
    RESOLVE_KEY_LEN,
    RESOLVE_MODE_MAX,
    RESOLVE_REASON_MAX,
    RESOLVE_SCHEMA_VERSION,
    RESOLVE_STATUS_MAX,
    SMF_LINK_STAGE_L0_DISCOVER,
    SMF_LINK_STAGE_L1_DECODE,
    SMF_LINK_STAGE_L2_ARENAS,
    SMF_LINK_STAGE_L3_INTERN_SORT,
    SMF_LINK_STAGE_L4_SELECT,
    SMF_LINK_STAGE_L5_ARCHIVE_FIXPOINT,
    SMF_LINK_STAGE_L6_REACHABILITY,
    SMF_LINK_STAGE_L7_ADDRESS_LAYOUT,
    SMF_LINK_STAGE_L8_RELOCATION,
    SMF_LINK_STAGE_L9_OUTPUT_ASSEMBLY,
    SMF_LINK_STAGE_L10_PROVENANCE,
    SMF_LINK_STAGE_L11_SSD_WRITE,
    SMF_LINK_STAGE_L12_MANIFEST_COMMIT,
    ConstraintBatch,
    DefinitionRecord,
    DomainPlacementRef,
    LinkMutationKind,
    MutationPlanRef,
    ReferenceRecord,
    ResolutionRecord,
    ResolveGroupResult,
    ResolveGroupView,
    ResolveInputRef,
    ResolveKey,
    ResolveMode,
    ResolveProfile,
    ResolveReason,
    ResolveResultRef,
    ResolveStatus,
    definition_record,
    definition_record_equal,
    link_mutation_kind_from_u8,
    link_mutation_kind_to_u8,
    link_mutation_kind_valid,
    reference_record,
    reference_record_equal,
    resolution_definition_present,
    resolution_record,
    resolution_record_equal,
    resolve_key,
    resolve_key_equal,
    resolve_mode_from_u8,
    resolve_mode_to_u8,
    resolve_mode_valid,
    resolve_reason_from_u8,
    resolve_reason_to_u8,
    resolve_reason_valid,
    resolve_status_from_u8,
    resolve_status_to_u8,
    resolve_status_valid
}

export use std.common.structural.resolve.resolve_codec.{
    DefinitionRecordResult,
    ReferenceRecordResult,
    ResolutionRecordResult,
    decode_definition_record,
    decode_reference_record,
    decode_resolution_record,
    definition_record_put,
    definition_record_read,
    encode_definition_record,
    encode_reference_record,
    encode_resolution_record,
    reference_record_put,
    reference_record_read,
    resolution_record_put,
    resolution_record_read,
    resolve_key_put,
    resolve_key_read,
    resolve_magic_definition,
    resolve_magic_reference,
    resolve_magic_resolution,
    resolve_zero_entity_ref
}
