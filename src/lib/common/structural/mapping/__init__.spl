# ============================================================================
# common/structural/mapping — FROZEN mapping contract (MAP lane)
#
# Architecture §6 (MappingGraph and provenance), §26 (contract freeze),
# §27 (MAP lane owns structural/mapping). Contract document:
# doc/05_design/platform/structural_compute/mapping_contract_v1.md
#
# Explicit re-exports only — no `export use *`, which the linter flags for
# exposing unnecessary interface.
# ============================================================================

export use std.common.structural.mapping.contracts.{
    STRUCTURAL_MAPPING_CONTRACT_VERSION,
    MappingKind,
    LayoutMapping,
    MappingGraph,
    mapping_graph,
    layout_of_mapping
}

export use std.common.structural.mapping.mapping_edge.{
    MAPPING_EDGE_LEN,
    MAPPING_FLAGS_KNOWN,
    MAPPING_FLAGS_LEN,
    MAPPING_FLAG_DISCARDED,
    MAPPING_FLAG_NONE,
    MAPPING_FLAG_SYNTHETIC,
    MAPPING_FLAG_WEIGHT_VALID,
    MAPPING_KIND_COUNT,
    MAPPING_KIND_MAX,
    MAPPING_KIND_SET_ALL,
    MAPPING_KIND_SET_EMPTY,
    MAPPING_KIND_SET_LEN,
    MAPPING_SCHEMA_VERSION,
    ORIGIN_POLICY_MAX,
    MappingEdge,
    MappingShard,
    OriginPolicy,
    mapping_build_reverse,
    mapping_edge,
    mapping_edge_equal,
    mapping_edge_weight_or_default,
    mapping_flags_has,
    mapping_flags_valid,
    mapping_forward_edges,
    mapping_kind_bit,
    mapping_kind_equal,
    mapping_kind_from_u8,
    mapping_kind_set_add,
    mapping_kind_set_contains,
    mapping_kind_set_of,
    mapping_kind_set_valid,
    mapping_kind_to_u8,
    mapping_kind_valid,
    mapping_offsets_well_formed,
    mapping_reverse_edges,
    mapping_shard,
    mapping_shard_equal,
    mapping_shard_forward,
    mapping_shard_valid,
    mapping_zero_edge,
    origin_policy_from_u8,
    origin_policy_to_u8,
    origin_policy_valid
}

export use std.common.structural.mapping.mapping_codec.{
    MappingEdgeResult,
    MappingKindSetResult,
    MappingShardResult,
    decode_mapping_edge,
    decode_mapping_kind_set,
    decode_mapping_shard,
    encode_mapping_edge,
    encode_mapping_kind_set,
    encode_mapping_shard,
    mapping_edge_put,
    mapping_edge_read,
    mapping_empty_shard,
    mapping_magic_edge,
    mapping_magic_kind_set,
    mapping_magic_shard
}
