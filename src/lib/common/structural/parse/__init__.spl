# ============================================================================
# common/structural/parse — public PARSE framework surface (wave 1).
#
# Plan: doc/03_plan/platform/structural_compute/parser_framework_plan.md
# Explicit re-exports only — no `export use *`, which the linter flags.
# ============================================================================

export use std.common.structural.parse.parse_types.{
    PARSE_BACKEND_CPU_REFERENCE,
    PARSE_DIAG_CAPACITY_EXCEEDED,
    PARSE_DIAG_NONE,
    PARSE_DIAG_UNRECOGNIZED_BYTE,
    PARSE_FALLBACK_MODE_UNIMPLEMENTED,
    PARSE_FALLBACK_NONE,
    PARSE_MODE_CPU_REFERENCE,
    PARSE_MODE_HYBRID_VECTOR_GPU,
    PARSE_MODE_RESIDENT_GPU,
    PARSE_NO_ID,
    PARSE_NO_ID_I64,
    PARSE_OUTPUT_DIAGNOSTICS,
    PARSE_OUTPUT_NONE,
    PARSE_OUTPUT_TOKENS,
    PARSE_REJECT_CAPACITY_EXCEEDED,
    PARSE_REJECT_CAPACITY_UNSET,
    PARSE_REJECT_LEX_PROGRAM_MALFORMED,
    PARSE_REJECT_UNKNOWN_MODE,
    PARSE_SCHEMA_ID,
    PARSE_SCHEMA_VERSION,
    PARSE_STAGE_ID,
    PARSE_STATE_FLAG_NONE,
    PARSE_STATE_FLAG_SKIP,
    PARSE_TAG_DEMAND_ALL,
    PARSE_TAG_DEMAND_OFF,
    PARSE_TOKEN_NONE,
    ParseActionSink,
    ParseCapacityManifest,
    ParseCapacityVerdict,
    ParseDialect,
    ParseDiagnostic,
    ParseDiagnosticArena,
    ParseLexProgram,
    ParseRequest,
    ParseResult,
    ParseSourceBlob,
    ParseToken,
    ParseTokenArena,
    parse_action_sink_new,
    parse_capacity_manifest,
    parse_capacity_manifest_zero,
    parse_capacity_verdict_accepted,
    parse_diagnostic_arena_empty,
    parse_diagnostic_arena_len,
    parse_diagnostic_at,
    parse_dialect_action_program,
    parse_dialect_grammar_program,
    parse_dialect_id,
    parse_dialect_incremental_policy,
    parse_dialect_lex_program,
    parse_dialect_lexical,
    parse_dialect_mapping_policy,
    parse_dialect_structure_program,
    parse_dialect_tag_schema,
    parse_lex_program,
    parse_lex_program_valid,
    parse_lex_transition,
    parse_request,
    parse_sink_emit_diagnostic,
    parse_sink_emit_tag,
    parse_sink_emit_token,
    parse_source_blob,
    parse_source_blob_empty,
    parse_source_blob_from_text,
    parse_source_blob_len,
    parse_token_arena_empty,
    parse_token_arena_len,
    parse_token_at
}
export use std.common.structural.parse.parse_cpu_reference.{
    parse_capacity_verify,
    parse_deterministic_hash,
    parse_run,
    parse_run_cpu_reference
}
