# Frozen ownership-transfer contract surface. Runtime codecs and task transport
# remain private to their future owners; this module exports vocabulary only.

export use std.common.structural.transfer.transfer_contracts.{
    PARALLEL_DOMAIN_COUNT,
    PARALLEL_DOMAIN_MAX,
    PARALLEL_TRANSFER_MODE_COUNT,
    PARALLEL_TRANSFER_MODE_MAX,
    PARALLEL_TRANSFER_PAYLOAD_COUNT,
    PARALLEL_TRANSFER_PAYLOAD_MAX,
    TRANSFER_SCHEMA_VERSION,
    ParallelExecutionDomain,
    ParallelTransferMode,
    ParallelTransferPayload,
    TransferEnvelopeV1,
    parallel_execution_domain_to_u8,
    parallel_execution_domain_valid,
    parallel_transfer_mode_consumes_source,
    parallel_transfer_mode_is_scoped_loan,
    parallel_transfer_mode_to_u8,
    parallel_transfer_mode_valid,
    parallel_transfer_payload_allows_raw_pointer,
    parallel_transfer_payload_to_u8,
    parallel_transfer_payload_valid,
    transfer_envelope_v1,
    transfer_envelope_v1_boundary_allowed,
    transfer_envelope_v1_well_formed
}

export use std.common.structural.transfer.transfer_codec.{
    TRANSFER_ENVELOPE_BODY_LEN,
    TRANSFER_ENVELOPE_WIRE_LEN,
    TransferEnvelopeResult,
    decode_transfer_envelope,
    encode_transfer_envelope,
    transfer_envelope_zero,
    transfer_magic
}

export use std.common.structural.transfer.process_frame_codec.{
    MAX_PROCESS_TRANSFER_BYTES,
    PROCESS_TRANSFER_FRAME_HEADER_LEN,
    PROCESS_TRANSFER_TEXT_MAX_LINE_BYTES,
    PROCESS_TRANSFER_TEXT_PREFIX,
    ProcessTransferFrameResult,
    ProcessTransferFrameV1,
    ProcessTransferTextLineResult,
    decode_process_transfer_frame,
    decode_process_transfer_text_line,
    encode_process_transfer_frame,
    process_transfer_frame_v1,
    process_transfer_frame_zero,
    process_transfer_text_line_encode,
    process_transfer_payload_checksum,
    process_transfer_route_allowed
}
