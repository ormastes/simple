"""Canonical structural artifact identity used by placement contracts."""

export use common.structural.identity.entity_id.{
    ArtifactId, Hash128, artifact_id, artifact_id_equal, hash128
}

fn artifact_id_valid(id: ArtifactId) -> bool:
    id.schema_version > 0

export artifact_id_valid
