# Window Protocol — shared window manager request, response, and event types.
# Used by compositor, window manager, and client applications.

use common.window_protocol.window_protocol
use common.window_protocol.geometry
