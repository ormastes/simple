# Simple Standard Library Configuration
# Merged: former src/std/ and src/lib/ into unified src/lib/

project:
  name: simple-std
  version: 0.4.0
  type: library
  language: Simple

  root: .
  source_dir: src
  test_dir: test

  dependencies:
    - project: ../../rust
