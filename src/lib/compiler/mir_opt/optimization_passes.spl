# Stub re-export for std.compiler.mir_opt.optimization_passes
# Routes to the canonical implementation in src/compiler/60.mir_opt/optimization_passes.spl

export use compiler.mir_opt.optimization_passes.{
    OptimizationLevel,
    OptimizationStats,
    OptimizationEngine,
    optimize_write_coalesce,
    optimize_read_ahead_hoist,
    optimize_syscall_batch
}
