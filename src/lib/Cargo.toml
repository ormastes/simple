[package]
name = "simple-term-io"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lints]
workspace = true

[dependencies]
simple-native-loader = { path = "../native_loader" }
libc = "0.2"

[build-dependencies]
cc = "1"
