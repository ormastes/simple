# Editor Layout — top-level arrangement of panes and dock zones.
#
# Provides: EditorLayout, LayoutPane, layout_new.

use std.editor.view.dock_zone.{DockZone, DockPosition, dock_zone_new}

enum LayoutOrientation:
    Horizontal
    Vertical

struct LayoutPane:
    pane_id: i64
    path: text
    width: i64
    height: i64

struct EditorLayout:
    panes: [LayoutPane]
    active_pane_id: i64
    sidebar: DockZone
    panel: DockZone
    orientation: LayoutOrientation

fn layout_new() -> EditorLayout:
    EditorLayout(
        panes: [],
        active_pane_id: 0,
        sidebar: dock_zone_new(1, DockPosition.Left),
        panel: dock_zone_new(2, DockPosition.Bottom),
        orientation: LayoutOrientation.Horizontal)
