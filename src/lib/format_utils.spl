# Shim: re-exports from lib/common/format_utils.spl
# Allows `use std.format_utils.{...}` to resolve at runtime
use lib.common.format_utils.*

export format_int, format_hex, format_binary
export format_size_bytes, format_duration_ms, format_percentage
export align_left, align_right, align_center
export format_table_row, format_table_divider
export format_bullet_list, format_numbered_list
export wrap_text, wrap_text_hard
export indent, indent_block, dedent
export box_text, border_text
