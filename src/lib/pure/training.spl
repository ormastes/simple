# Stubs for pure training primitives
# These are placeholder implementations; real training uses torch backends.

struct TrainingHistory:
    losses: [f64]
    val_losses: [f64]

fn mse_loss(pred: any, target: any) -> f64:
    0.0

class SGD:
    learning_rate: f64
    parameters: [any]

    fn zero_grad():
        ()

    fn step():
        ()

class Adam:
    learning_rate: f64
    parameters: [any]
    beta1: f64
    beta2: f64

    fn zero_grad():
        ()

    fn step():
        ()

export TrainingHistory, mse_loss, SGD, Adam
