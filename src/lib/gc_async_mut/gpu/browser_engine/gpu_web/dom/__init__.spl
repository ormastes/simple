# gpu_web/dom — HTML-DOM lane, parse-side canonical data model (wave 1).
#
# Plan: doc/03_plan/platform/structural_compute/html_css_parser_plan.md
#
# Explicit re-exports only — no `export use *`, which the linter flags for
# exposing unnecessary interface.

export use std.gc_async_mut.gpu.browser_engine.gpu_web.dom.dom_arena_types.{
    DOM_NODE_KIND_CDATA,
    DOM_NODE_KIND_COMMENT,
    DOM_NODE_KIND_DOCTYPE,
    DOM_NODE_KIND_DOCUMENT,
    DOM_NODE_KIND_ELEMENT,
    DOM_NODE_KIND_NONE,
    DOM_NODE_KIND_PROCESSING_INSTRUCTION,
    DOM_NODE_KIND_TEXT,
    DOM_NO_ID,
    DOM_NO_ID_I64,
    GPU_WEB_DOM_ARENA_SCHEMA_ID,
    GPU_WEB_DOM_ARENA_SCHEMA_VERSION,
    DomArena,
    DomAttributeArena,
    DomNodeId,
    DomSnapshot,
    DomSpan,
    DomStringTable,
    dom_arena_attribute_index,
    dom_arena_attribute_span,
    dom_arena_attribute_value,
    dom_arena_canonical_lines,
    dom_arena_canonical_text,
    dom_arena_children,
    dom_arena_depth,
    dom_arena_empty,
    dom_arena_in_range,
    dom_arena_kind,
    dom_arena_name,
    dom_arena_node_count,
    dom_arena_node_id,
    dom_arena_parent,
    dom_arena_value,
    dom_attribute_arena_empty,
    dom_node_id,
    dom_node_id_equal,
    dom_snapshot,
    dom_snapshot_id_live,
    dom_snapshot_supersedes,
    dom_span,
    dom_span_equal,
    dom_string_count,
    dom_string_table_empty,
    dom_string_text
}

export use std.gc_async_mut.gpu.browser_engine.gpu_web.dom.dom_arena_build.{
    DOM_BUILD_ERR_ATTRIBUTE_WITHOUT_ELEMENT,
    DOM_BUILD_ERR_CAPACITY,
    DOM_BUILD_ERR_UNBALANCED_CLOSE,
    DOM_BUILD_ERR_UNCLOSED_ELEMENTS,
    DomBuildResult,
    DomBuilder,
    DomInternResult,
    dom_build_result_snapshot,
    dom_builder_attribute,
    dom_builder_capacity_plan,
    dom_builder_cdata,
    dom_builder_close,
    dom_builder_comment,
    dom_builder_doctype,
    dom_builder_finish,
    dom_builder_intern,
    dom_builder_new,
    dom_builder_open_depth,
    dom_builder_open_document,
    dom_builder_open_element,
    dom_builder_processing_instruction,
    dom_builder_text
}
