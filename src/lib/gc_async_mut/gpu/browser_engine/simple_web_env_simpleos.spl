# SimpleOS web-render policy is build/device driven; it has no host process
# environment. Optional debug/tuning probes therefore remain unavailable.
fn simple_web_env_get(key: text) -> text?:
    nil

export simple_web_env_get
