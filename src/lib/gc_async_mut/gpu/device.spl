# GPU device facade for gc_async_mut tier.

export use std.common.gpu.device.{GpuBackend, Gpu, gpu_cuda, gpu_vulkan, gpu_rocm, gpu_oneapi, gpu_metal, gpu_none}
export use std.nogc_sync_mut.gpu.device.{detect_backends, preferred_backend}
