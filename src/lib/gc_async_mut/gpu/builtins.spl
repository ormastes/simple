# GC async GPU intrinsic facade.
#
# Builtin GPU intrinsic declarations are no-GC async compatible; native GPU
# backends lower them, while interpreter fallback behavior lives in the no-GC
# async backend surface.

export use std.nogc_async_mut.gpu.builtins.*
