@cfg("os", "simpleos")
use std.gc_async_mut.gpu.engine2d.draw_ir_env_simpleos.{draw_ir_env_get}

@cfg(macos or linux or windows or freebsd or openbsd or netbsd or android)
use std.gc_async_mut.io.mod_stub.{env_get as draw_ir_env_get}

export draw_ir_env_get
