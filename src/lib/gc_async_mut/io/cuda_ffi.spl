# GC async CUDA I/O FFI facade.
#
# CUDA runtime operations are synchronous backend calls; the no-GC sync I/O
# owner keeps the legacy Simple-friendly ABI and this compatibility family
# re-exports it without local runtime-hook declarations.

export use std.nogc_sync_mut.io.cuda_ffi.*
