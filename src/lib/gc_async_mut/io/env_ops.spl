"""
# GC Async Environment Operations

Compatibility facade over the no-GC sync environment backend.
"""

export use std.nogc_async_mut.io.env_ops.*

# Stable uniquely-named owner for older bootstrap compilers whose native
# codegen cannot preserve a renamed function import at the call site.
fn gc_env_get(key: text) -> text:
    env_get(key)
