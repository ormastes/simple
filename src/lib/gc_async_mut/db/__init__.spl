# GC-async tier wrapper for std.gc_async_mut.db.
#
# Re-exports all symbols from the canonical nogc_async_mut.db tier.
# GC callers import from this path; the implementation lives in nogc_async_mut.

export use nogc_async_mut.db.*
