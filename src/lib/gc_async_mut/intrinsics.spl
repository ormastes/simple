# GC async compile-time intrinsic facade.
#
# Intrinsic helpers are compiler/evaluator owned. Route through the no-GC async
# family so GC compatibility code does not duplicate placeholder bodies.

export use std.nogc_async_mut.intrinsics.*
