# GC UDP Socket Operations
#
# GC networking keeps this module as a compatibility facade over the no-GC
# async surface. Runtime-facing UDP ownership stays in the no-GC family.

export use std.nogc_async_mut.net.udp.{UdpSocket}
