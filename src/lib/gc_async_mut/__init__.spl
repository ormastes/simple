@gc

# GC async mutable stdlib family.
#
# This root manifest marks the family as GC-capable for manifest-backed runtime
# checks. Module exports remain owned by the concrete submodules so the GC async
# compatibility surface can preserve family-local types where needed.
