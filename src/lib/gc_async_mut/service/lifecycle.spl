"""
# GC Async Service Lifecycle

Compatibility facade over the no-GC async service lifecycle surface.
"""

export use std.nogc_async_mut.service.lifecycle.{
    PidFileState,
    acquire_pid_file,
    release_pid_file,
    read_pid_file,
    is_daemon_alive
}
