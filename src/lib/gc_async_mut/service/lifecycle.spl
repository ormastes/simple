"""
# GC Async Service Lifecycle

Compatibility facade over the no-GC sync service lifecycle backend.
"""

export use std.nogc_sync_mut.service.lifecycle.{
    PidFileState,
    acquire_pid_file,
    release_pid_file,
    read_pid_file,
    is_daemon_alive
}
