"""
# GC Async Service Request Queue

Compatibility facade over the no-GC async service request-queue surface.
"""

export use std.nogc_async_mut.service.request_queue.{
    QueueEntry,
    RequestQueue,
    request_queue_new,
    enqueue,
    dequeue,
    queue_len,
    queue_is_empty,
    queue_high_len,
    queue_low_len,
    queue_drain
}
