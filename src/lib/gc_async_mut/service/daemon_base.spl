"""
# GC Async Service Daemon Base

Compatibility facade over the no-GC async service daemon surface.
"""

export use std.nogc_async_mut.service.daemon_base.{
    DaemonConfig,
    DaemonBase,
    daemon_config_new,
    daemon_base_new,
    daemon_base_start,
    daemon_base_stop,
    daemon_base_is_running
}
