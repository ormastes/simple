# @gc — module uses GC runtime
#
# engine/ — mirrors vllm/engine/. Master plan A5.
#
# Present: llm_engine (model residency, admission, request-level generation).
# Absent: async_llm_engine, continuous batching and metrics, which need the A4
# paged KV cache underneath them.

export engine_set_lib_path, engine_set_n_ctx, engine_lib_path, engine_backend_available
export engine_loaded_model, engine_unload, engine_ensure_loaded, engine_generate, engine_complete
