# Canonical self-hosted allocation SFFI surface.
#
# Keep compiler imports compatible with the bootstrap `std.alloc.sffi` API
# while the raw runtime declarations remain owned by the no-GC SFFI layer.
use nogc_sync_mut.sffi.alloc*
