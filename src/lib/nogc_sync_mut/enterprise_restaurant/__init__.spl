# Enterprise Restaurant — table-service proving vertical over the enterprise
# store and the sale catalog (lane .spipe/simple_enterprise_suite, W2-E).
export use std.nogc_sync_mut.enterprise_restaurant.restaurant.*
