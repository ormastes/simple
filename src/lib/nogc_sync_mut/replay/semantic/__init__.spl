# SReplay — Semantic Trace Module
#
# High-level semantic trace events for replay debugging:
#   function enter/exit, variable writes, object lifecycle,
#   ownership/borrow tracking, async timeline.

export SemanticEventKind, SemanticEvent
export TraceWriter, init_global_writer, emit_global, finalize_global_writer
export read_semantic_trace
export ObjectId, ObjectRegistry
