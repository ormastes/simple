# SReplay — Simple Replay Debugger Library
#
# Deterministic replay as a platform capability:
#   checkpoint + event log + semantic trace + reverse debugger

export ReplayMode, ReplayConfig, ReplaySessionInfo, Address, TargetDesc, Arch, Endian
export ReplayBackend
