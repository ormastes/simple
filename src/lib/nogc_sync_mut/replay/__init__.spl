# SReplay — Simple Replay Debugger Library
#
# Deterministic replay as a platform capability:
#   checkpoint + event log + semantic trace + reverse debugger

export ReplayMode, ReplayConfig, ReplaySessionInfo, Address, TargetDesc, Arch, Endian
export ReplayBackend

# Track 3: Process-level replay
export ProcessEventKind, ProcessReplayEvent
export ProcessRecorder, ProcessReplayer
export DirtyPage, PageCheckpoint

# Track 4: Semantic trace
export SemanticEventKind, SemanticEvent
export TraceWriter, ObjectId, ObjectRegistry

# Track 6: VM replay
export VmConfig, VmState, VmReplayMode
export QemuBridge
