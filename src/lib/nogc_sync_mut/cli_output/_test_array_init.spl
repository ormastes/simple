# Shared module for module-level array-initialisation import tests.
# Consumed by test/system/module_import/fixtures/array_init_import.spl
# and baseline_direct_import.spl. Mirrors
# src/compiler_rust/lib/std/src/cli_output/_test_array_init.spl.

fn test_i64_arr() -> i64:
    val values = [1, 2, 3]
    values.len()

fn test_text_arr() -> i64:
    val values = ["one", "two"]
    values.len()
