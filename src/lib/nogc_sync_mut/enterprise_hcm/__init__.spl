# Enterprise HCM — employee/contract/attendance/time/leave vertical over the
# enterprise store (lane .spipe/simple_enterprise_suite, W6-A). Payroll is a
# boundary export only — no payroll/tax calculation lives here.
export use std.nogc_sync_mut.enterprise_hcm.hcm.*
