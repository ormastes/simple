# compression/brotli.spl — REPLACED
#
# The brotli decoder has moved to:
#   src/lib/nogc_sync_mut/compression/brotli/  (decoder.spl, bit_reader.spl, mod.spl)
#
# Import:
#   use std.nogc_sync_mut.compression.brotli.decoder.{brotli_decode}
#
# The old file contained only misnamed Huffman frequency-analysis stubs
# that had no relationship to RFC 7932; they have been removed.
