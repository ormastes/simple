# std.game2d.app.config — re-export GameConfig surface (Wave C, AC-1/AC-6)
#
# `GameConfig` / `WindowConfig` / `RuntimeConfig` live under
# `game2d/config/game_sdn.spl`. This module forwards them so callers
# can `use std.nogc_sync_mut.game2d.app.config` without reaching into
# the SDN-loader-specific path.

use std.nogc_sync_mut.game2d.config.game_sdn.{
    GameConfig, WindowConfig, RuntimeConfig, load_game_config
}

export GameConfig
export WindowConfig
export RuntimeConfig
export load_game_config
