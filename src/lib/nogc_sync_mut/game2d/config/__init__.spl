# std.game2d.config — re-exports (Wave B, AC-6)

use std.nogc_sync_mut.game2d.config.game_sdn.{WindowConfig, RuntimeConfig, GameConfig, load_game_config}

export WindowConfig
export RuntimeConfig
export GameConfig
export load_game_config
