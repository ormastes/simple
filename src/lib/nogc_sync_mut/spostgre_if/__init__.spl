"""
spostgre_if — Trait contracts for the spostgre DB engine.

@tag:stdlib
@tag:api

Public trait surface consumed by the spostgre engine implementation in
`examples/spostgre/src/engine/` and by any integration test / alternative
backend. Concrete impls NEVER live here.

Submodules:
  - types:        opaque aliases (Rel, BlkNo, Lsn, TxnId, PhysPtr).
  - storage_api:  trait bundle — buf/wal/pmap/temp/checkpoint/blob/vacuum.

See:
  - doc/05_design/spostgre_design.md §2.1 (MDSOC), §5 (API), §6 (WAL).
"""

# All submodules are automatically available.
