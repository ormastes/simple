# Daemon SDK — Reusable daemon infrastructure with file-based IPC
#
# Provides generic daemon infrastructure: PID lock singleton,
# key=value serialized request/response protocol, poll-loop runner,
# and submit-and-poll client API.

export std.daemon_sdk.types.*
export std.daemon_sdk.lock.*
export std.daemon_sdk.protocol.*
export std.daemon_sdk.daemon.*
export std.daemon_sdk.client.*
