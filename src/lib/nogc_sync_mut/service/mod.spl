"""
# Service Base Library — Reusable daemon infrastructure
#
# Provides generic daemon building blocks: PID-file lifecycle,
# lease management, request queue, audit log, and UDS externs.
# VCS-specific and other service daemons compose on top.
#
# MDSOC+ architecture: outer concern boundary (lifecycle, transport)
# with ECS inner layer for state management in concrete services.
"""

use std.io_runtime

# Re-export sub-modules
use std.service.traits
use std.service.lifecycle
use std.service.lease_manager
use std.service.request_queue
use std.service.audit_log
use std.service.daemon_base
use std.service.extern
