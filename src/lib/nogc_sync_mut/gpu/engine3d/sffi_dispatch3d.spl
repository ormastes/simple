"""SFFI Dispatch Layer — Static/Dynamic Dual-Path for 3D Engine

@tag:stdlib
@tag:gpu

Provides the dual-path SFFI dispatch infrastructure for 3D GPU backends.
Each backend gets a *Ffi3D class that holds either static (extern fn) or
dynamic (DynLib dlopen) function pointers. At init time, hosted OSes
try dynamic first; SimpleOS always uses static.

Usage:
    val mode = default_sffi_mode_3d()
    val vk = VulkanFfi3D.create(mode)  # auto-selects path
"""

use std.sffi.dynamic.{DynLib, DynLoader, sffi_call}

# ============================================================================
# SFFI Mode Enum
# ============================================================================

enum GpuFfiMode3D:
    Static
    Dynamic

impl GpuFfiMode3D:
    fn to_text() -> text:
        match self:
            case Static: "Static"
            case Dynamic: "Dynamic"

# ============================================================================
# Platform Detection
# ============================================================================

use std.io_runtime.{env_get}
use std.nogc_sync_mut.env.platform.{detect_os}

fn is_simpleos_3d() -> bool:
    val os = env_get("SIMPLE_OS_KERNEL")
    os == "simpleos"

fn default_sffi_mode_3d() -> GpuFfiMode3D:
    if is_simpleos_3d():
        GpuFfiMode3D.Static
    else:
        GpuFfiMode3D.Dynamic

# ============================================================================
# FfiDispatchBase3D Trait
# ============================================================================

trait FfiDispatchBase3D:
    fn mode() -> GpuFfiMode3D
    fn is_available() -> bool

# ============================================================================
# Dynamic Library Path Resolution
# ============================================================================

fn gpu_lib_path_3d(lib_name: text) -> text:
    # SIMPLE_OS_TYPE is never set by any runtime or CLI entry in this repo, so
    # the old env gate probed Linux sonames even on macOS. detect_os() consults
    # the in-process rt_platform_name() primitive first and needs no env.
    # See doc/08_tracking/bug/gpu_ffi_loader_probes_linux_sonames_on_macos_2026-09-19.md.
    val os = detect_os()
    if os == "windows":
        "{lib_name}.dll"
    elif os == "macos":
        "lib{lib_name}.dylib"
    else:
        "lib{lib_name}.so"

fn gpu_lib_candidates_3d(base_name: text) -> [text]:
    val os = detect_os()
    var paths: [text] = []
    if os == "windows":
        paths.push("{base_name}.dll")
    elif os == "macos":
        paths.push("lib{base_name}.dylib")
        paths.push("lib{base_name}.1.dylib")
        # dyld does not search Homebrew's lib dir for bare sonames, so probe
        # the standard prefixes directly. Absent files are rejected by the
        # file_exists pre-probe in std.sffi.dynamic before any dlopen attempt.
        paths.push("/opt/homebrew/lib/lib{base_name}.dylib")
        paths.push("/usr/local/lib/lib{base_name}.dylib")
    else:
        paths.push("lib{base_name}.so.1")
        paths.push("lib{base_name}.so")
    paths

fn try_load_gpu_lib_3d(base_name: text) -> DynLib?:
    val candidates = gpu_lib_candidates_3d(base_name)
    var i = 0
    while i < candidates.len():
        val lib = DynLib.load(candidates[i])
        if lib != nil:
            return lib
        i = i + 1
    nil

# ============================================================================
# SFFI Mode Resolution Helper
# ============================================================================

fn resolve_sffi_mode_3d(backend_name: text) -> GpuFfiMode3D:
    val mode = default_sffi_mode_3d()
    mode
