# MSan Types — Memory State Sanitizer State
# @tag:internal

struct MemRegion:
    var_id: text
    size: i64
    initialized: bool
    freed: bool

fn mem_region(var_id: text, size: i64) -> MemRegion:
    MemRegion(var_id: var_id, size: size, initialized: false, freed: false)

export MemRegion, mem_region
