# ASan Types — Address Sanitizer State
# @tag:internal

struct AsanAllocRecord:
    id: i64
    size: i64
    tag: text
    freed: bool

fn asan_alloc_record(id: i64, size: i64, tag: text) -> AsanAllocRecord:
    AsanAllocRecord(id: id, size: size, tag: tag, freed: false)

export AsanAllocRecord, asan_alloc_record
