# LSan Types — Leak Sanitizer State
# @tag:internal

struct LeakCheckpoint:
    name: text
    snapshot_id: i64

fn leak_checkpoint(name: text, snapshot_id: i64) -> LeakCheckpoint:
    LeakCheckpoint(name: name, snapshot_id: snapshot_id)

export LeakCheckpoint, leak_checkpoint
