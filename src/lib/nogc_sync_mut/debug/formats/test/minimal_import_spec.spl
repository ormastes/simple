"""Minimal test."""

use std.binary_io.{BinaryReader}

describe "Reader":
    it "reads u8":
        val data: [u8] = [0x42]
        var reader = BinaryReader.new(data)
        val v = reader.read_u8()
        expect(v.?).to_equal(true)
