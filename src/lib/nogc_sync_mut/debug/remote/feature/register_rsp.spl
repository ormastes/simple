# GDB RSP Feature Registration
# Registers GDB Remote Serial Protocol features for direct RSP connections.

use std.nogc_sync_mut.debug.remote.feature.features.{FeatureId, FeatureRank}
use std.nogc_sync_mut.debug.remote.feature.registry.FeatureRegistry

fn register_rsp_features(registry: FeatureRegistry):
    registry.register(FeatureId.RspConnect, FeatureRank.NATIVE())
    registry.register(FeatureId.RspPacketSend, FeatureRank.NATIVE())
    registry.register(FeatureId.Halt, FeatureRank.BRIDGE())
    registry.register(FeatureId.Resume, FeatureRank.BRIDGE())
    registry.register(FeatureId.SingleStep, FeatureRank.BRIDGE())
    registry.register(FeatureId.ReadMemory, FeatureRank.BRIDGE())
    registry.register(FeatureId.WriteMemory, FeatureRank.BRIDGE())
    registry.register(FeatureId.ReadAllRegisters, FeatureRank.BRIDGE())
    registry.register(FeatureId.SetBreakpoint, FeatureRank.BRIDGE())
    registry.register(FeatureId.ClearBreakpoint, FeatureRank.BRIDGE())
    registry.register(FeatureId.SetWatchpoint, FeatureRank.BRIDGE())
