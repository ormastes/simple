# HTTP Request Limits — thin delegation to the shared core.
# Canonical implementation: src/lib/common/net/http_core.spl
export use std.common.net.http_core.{
    HttpLimits, http_limits_default,
    HttpParseError, http_parse_error_status, http_parse_error_message,
    check_request_line, check_header_count, check_header_size, check_body_size
}
