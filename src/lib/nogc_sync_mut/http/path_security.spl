# HTTP Path Security — thin delegation to the shared core.
# Canonical implementation: src/lib/common/net/http_core.spl
export use std.common.net.http_core.{
    is_safe_static_path, contains_null_byte, contains_traversal,
    contains_backslash, starts_with_slash, normalize_path, validate_static_path
}
