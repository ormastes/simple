# SimpleOS selected fonts are supplied as already-loaded VFS bytes. There is
# no host path or runtime font handle in this target lane.

use std.nogc_sync_mut.io.font_sffi.{FontHandle}

fn load_font(path: text) -> FontHandle?:
    nil

fn free_font(font: FontHandle):
    val _unused = font.handle
