# Enterprise Subscriptions — recurring-billing vertical over the enterprise
# store (lane .spipe/simple_enterprise_suite, W22-D). Reuses the frozen `sales`
# role for RBAC via a local gate; does not touch foundation.spl or
# rbac_registry.spl.
export use std.nogc_sync_mut.enterprise_subscription.subscription.*
