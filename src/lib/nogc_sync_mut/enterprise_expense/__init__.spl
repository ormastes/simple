# Enterprise Expense — expense claims, per-category caps, tiered approval limits
# over the durable enterprise store (lane .spipe/simple_enterprise_suite, W23-FAN).
export use std.nogc_sync_mut.enterprise_expense.expense.*
