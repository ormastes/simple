# Compatibility facade for the canonical CLI SFFI owner.
export use std.nogc_sync_mut.sffi.cli.*

fn cli_run_ffi_gen(args: [text]) -> i64:
    cli_run_sffi_gen(args)

fn cli_ffi_gen(args: [text]) -> i64:
    cli_run_sffi_gen(args)
