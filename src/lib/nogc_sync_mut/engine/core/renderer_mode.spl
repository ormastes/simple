# Renderer mode selection shared by engine-loop surfaces.

enum RendererMode:
    Software
    Gpu

impl RendererMode:
    fn to_text() -> text:
        match self:
            Software: "Software"
            Gpu: "Gpu"
