# Enterprise Assets — fixed-asset register + straight-line depreciation over the
# durable enterprise store (lane .spipe/simple_enterprise_suite, W23-FAN).
export use std.nogc_sync_mut.enterprise_assets.assets.*
