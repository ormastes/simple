# Shim: std.nogc_sync_mut.torch.dyn_ffi -> std.common.torch.dyn_ffi_ops
#
# The shared implementation lives in src/lib/common/torch/dyn_ffi_ops.spl and
# src/lib/common/torch/dyn_ffi_tensor_ops.spl.
# All three torch variants (nogc_sync_mut, nogc_async_mut, gc_async_mut) delegate here.

use std.common.torch.dyn_ffi.*
use std.common.torch.dyn_ffi_ops.*
use std.common.torch.dyn_ffi_tensor_ops.*
