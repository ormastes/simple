"""
# No-GC async OIDC discovery

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.oidc.discovery.{
    OidcConfig, OidcDiscovery,
    extract_json_string, extract_json_array
}
