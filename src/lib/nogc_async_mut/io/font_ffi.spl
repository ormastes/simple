"""
# No-GC async font FFI

Compatibility facade over the no-GC sync backend boundary.
"""

export use std.nogc_sync_mut.io.font_ffi.{
    FontHandle, GlyphMetrics, GlyphBitmap,
    load_font, free_font, get_glyph, free_glyph_bitmap, get_line_height
}
