"""
# No-GC async io signal handlers

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.io.signal_handlers.{
    exit,
    SIGHUP, SIGINT, SIGTERM, SIGKILL,
    default_cleanup_handler, install_signal_handlers,
    on_sigint, on_sigterm, on_sighup, on_normal_exit,
    create_signal_cleanup_handler, signal_handlers_available, signal_name
}
