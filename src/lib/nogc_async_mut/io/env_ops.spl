"""
# No-GC Async Environment Operations

Compatibility facade over the no-GC sync environment backend.
"""

export use std.nogc_sync_mut.io.env_ops.{
    env_get, env_set, cwd, home, env_vars, host_arch, host_os
}
