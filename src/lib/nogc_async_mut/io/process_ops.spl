"""
# No-GC Async Process Operations

Compatibility facade over the no-GC sync process backend.
"""

export use std.nogc_sync_mut.io.process_ops.{
    ProcessResult,
    process_run, process_run_timeout_unix, process_run_timeout_windows,
    process_run_timeout, process_run_with_limits, process_output,
    shell, shell_bool, shell_output, shell_output_trimmed, shell_lines,
    shell_int, process_spawn_async, process_wait, process_is_running,
    process_kill, eprint
}
