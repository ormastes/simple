"""
# No-GC async font SFFI

Compatibility facade over the no-GC sync backend boundary.
"""

export use std.nogc_sync_mut.io.font_sffi.{
    FontHandle, GlyphMetrics, GlyphBitmap,
    load_font, free_font, get_glyph, glyph_index, free_glyph_bitmap, get_line_height
}
