"""
# No-GC async DMA I/O

Compatibility facade over the no-GC sync backend boundary.
"""

export use std.nogc_sync_mut.io.dma.{
    DmaDir, DmaCachePolicy, DmaBuffer, DmaOwner, SharedDmaBuffer,
    DmaReleaseRequest, dma_alloc, dma_buffer_to_shared,
    validate_shared_dma_release, dma_free, dma_sync_for_device,
    dma_sync_for_cpu, dma_shared_sync_cpu_to_device,
    dma_shared_sync_device_to_cpu, dir_to_raw, dma_cache_line_size
}
