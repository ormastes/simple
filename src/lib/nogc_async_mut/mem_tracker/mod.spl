"""
# No-GC Async Memory Tracker

Compatibility facade over the no-GC sync memory tracker backend.
"""

export use std.nogc_sync_mut.mem_tracker.mod.{
    mem_enable, mem_disable, mem_is_enabled, mem_snapshot,
    mem_capture_snapshot, mem_dump_leaks, mem_live_count, mem_live_bytes,
    mem_reset, mem_count_since, mem_bytes_since, mem_phase, mem_diff,
    mem_diff_quick, mem_group_by_tag, mem_build_report, mem_print_report,
    parse_leak_dump, mem_set_listener, mem_clear_listener
}
