"""
# No-GC async js engine interpreter

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.js.engine.interpreter.{
    PendingAsyncRequest, PendingPromiseTask, PromiseHandlerRecord,
    PromiseHandlerRegistration, PendingTimerTask, JsInterpreter,
    _statement_name, filter_response_headers_for_script,
    parse_non_negative_i64_text, text_equals_exact,
    _simple_fetch_marker, _simple_fetch_base_url,
    _simple_fetch_cookie_header, _simple_fetch_state,
    _simple_fetch_value, _simple_fetch_handlers,
    _scheme, _origin, _without_fragment, _without_query,
    _base_dir, _resolve_fetch_url, _simple_fetch_sync
}
