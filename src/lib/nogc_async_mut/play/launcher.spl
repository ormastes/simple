"""
# No-GC async play launcher

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.play.launcher.{
    PLAY_ARTIFACTS_ROOT, DEVTOOLS_MARKER,
    launcher_artifacts_dir, extract_ws_url_from_text, poll_for_ws_url,
    launcher_next_session_id, launcher_resolve_executable,
    launcher_build_args, launcher_launch, probe_browser_ws, launcher_kill
}
