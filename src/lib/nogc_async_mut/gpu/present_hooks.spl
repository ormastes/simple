# No-GC async GPU presentation hooks facade.
#
# Presentation runtime hooks are synchronous backend operations. Keep ownership
# in the no-GC sync backend and expose this async-family facade for higher
# compatibility layers.

export use std.nogc_sync_mut.gpu.present_hooks.{cpu_present_pixels}
