"""
# No-GC Async CUDA FFI

Compatibility facade over the no-GC sync CUDA FFI backend. CUDA runtime calls
are synchronous backend operations; async-family CUDA layers reuse the shared
no-GC declaration owner instead of redeclaring hooks.
"""

export use std.nogc_sync_mut.cuda.ffi.*
