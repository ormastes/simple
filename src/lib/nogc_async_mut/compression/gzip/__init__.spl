export use nogc_async_mut.compression.gzip.compress.{
    gzip_compress, gzip_decompress,
    gzip_compress_default, gzip_compress_best, gzip_compress_fast, gzip_compress_none,
    gzip_validate, gzip_get_metadata, gzip_is_compressed, gzip_extract_payload,
    gzip_compression_ratio, gzip_space_savings, gzip_efficiency,
    gzip_header_overhead, gzip_footer_overhead, gzip_total_overhead,
    gzip_analyze_data, gzip_suggest_level, gzip_estimate_size
}
export use nogc_async_mut.compression.gzip.crc.{
    crc32_calculate, crc32_verify, crc32_update, crc32_finalize, crc32_chunked
}
