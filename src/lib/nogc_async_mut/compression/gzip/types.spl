export use nogc_sync_mut.compression.gzip.types.{
    GZIP_MAGIC1, GZIP_MAGIC2, CM_DEFLATE,
    OS_FAT, OS_UNIX, OS_NTFS, OS_UNKNOWN,
    FLAG_TEXT, FLAG_HCRC, FLAG_EXTRA, FLAG_NAME, FLAG_COMMENT,
    WINDOW_SIZE, MIN_MATCH, MAX_MATCH, MAX_DISTANCE,
    MAX_BITS, MAX_LITERALS, MAX_DISTANCES,
    BLOCK_STORED, BLOCK_FIXED, BLOCK_DYNAMIC,
    LEVEL_NONE, LEVEL_FAST, LEVEL_DEFAULT, LEVEL_BEST
}
