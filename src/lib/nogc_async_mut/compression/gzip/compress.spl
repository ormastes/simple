export use nogc_sync_mut.compression.gzip.compress.{
    gzip_compress, gzip_decompress,
    gzip_compress_default, gzip_compress_best, gzip_compress_fast, gzip_compress_none,
    gzip_validate, gzip_get_metadata, gzip_is_compressed, gzip_extract_payload,
    gzip_wrap_deflate, gzip_compression_ratio, gzip_space_savings, gzip_efficiency,
    gzip_header_overhead, gzip_footer_overhead, gzip_total_overhead,
    gzip_analyze_data, gzip_find_patterns, gzip_suggest_level, gzip_estimate_size,
    gzip_compare_levels, gzip_benchmark, compress_gzip, decompress_gzip,
    compress_gzip_level, calculate_crc32, verify_crc32, validate_gzip,
    get_gzip_metadata, stream_compress_gzip, get_compression_ratio,
    get_space_savings, analyze_data, find_patterns, compression_efficiency,
    split_into_blocks, merge_blocks, is_gzip_compressed, extract_gzip_payload
}
