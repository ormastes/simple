# Default-tier wrapper: `use std.enterprise_channel` resolves here.
# Implementation lives in the sync tier alongside the enterprise store.
export use std.nogc_sync_mut.enterprise_channel.channel_hub.*
