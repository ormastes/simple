"""
# No-GC Async Daemon SDK Client

Compatibility facade over the no-GC sync daemon SDK backend.
"""

export use std.nogc_sync_mut.daemon_sdk.client.{
    daemon_ensure_running, daemon_submit, daemon_poll_result,
    daemon_submit_and_wait, daemon_stop
}
