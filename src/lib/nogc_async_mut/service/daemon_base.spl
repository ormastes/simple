"""
# No-GC Async Service Daemon Base

Compatibility facade over the no-GC sync service daemon backend.
"""

export use std.nogc_sync_mut.service.daemon_base.{
    DaemonConfig,
    DaemonBase,
    daemon_config_new,
    daemon_base_new,
    daemon_base_start,
    daemon_base_stop,
    daemon_base_is_running
}
