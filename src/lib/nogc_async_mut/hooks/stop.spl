"""
# No-GC Async Stop Hooks

Compatibility facade over the no-GC sync hooks backend.
"""

export use std.nogc_sync_mut.hooks.stop.{
    IncompleteSummary,
    detect_incomplete_work, show_incomplete_summary, interactive_prompt,
    stop_hook_callback, register_stop_hooks, maybe_run_stop_hooks,
    run_stop_hooks, print_msg, read_line
}
