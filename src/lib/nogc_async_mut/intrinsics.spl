# No-GC async compile-time intrinsic facade.
#
# These helpers are compiler/evaluator intrinsics with no async runtime state.
# Reuse the no-GC sync declarations so the async family does not own duplicate
# placeholder bodies.

export use std.nogc_sync_mut.intrinsics.*
