# No-GC Async UDP Socket Operations
#
# The `net.udp` surface is synchronous. In the no-GC async family it is a
# compatibility facade over the no-GC synchronous backend, while true async
# datagram I/O remains under `io.udp.AsyncUdpSocket`.

export use std.nogc_sync_mut.net.udp.{UdpSocket}
