"""
# No-GC async versioned dynamic FFI

Compatibility facade over the no-GC sync backend boundary.
"""

export use std.nogc_sync_mut.ffi.dynamic_versioned.{
    LibVersion, VersionedDynLib, MultiVersionLoader,
    lib_version, lib_version_any, lib_version_string,
    build_candidate_paths, library_search_paths
}
