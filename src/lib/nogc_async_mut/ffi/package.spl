"""
# No-GC async FFI package

Compatibility facade over the no-GC sync backend boundary.
"""

export use std.nogc_sync_mut.ffi.package.{
    package_exists, package_is_dir, package_file_size, package_copy_file,
    package_chmod, package_mkdir_all, package_remove_dir_all,
    package_create_symlink, package_create_tarball, package_extract_tarball,
    package_sha256, package_free_string,
    cargo_build, cargo_check, cargo_test, cargo_test_doc, cargo_clean,
    cargo_fmt, cargo_lint
}
