"""
# No-GC async FFI error

Compatibility facade over the no-GC sync backend boundary.
"""

export use std.nogc_sync_mut.ffi.error.{
    error_type_mismatch, error_arg_count, error_division_by_zero,
    error_index_oob, error_index_out_of_bounds, error_undefined_var,
    error_semantic, error_message, error_throw, error_free
}
