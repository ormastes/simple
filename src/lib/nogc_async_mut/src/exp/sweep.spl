"""
# No-GC async experiment sweep

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.src.exp.sweep.{
    random_uniform, random_randint,
    TrialStatus, Trial, SamplerKind, Sampler, PrunerKind, Pruner,
    prune_by_median, prune_by_halving, StudyConfig, Study
}
