"""
# No-GC async experiment artifact

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.src.exp.artifact.{
    ArtifactEntry, ArtifactStore,
    save_artifacts, load_artifacts, file_write, gc_artifacts,
    extract_artifacts_from_sdn
}
