"""
# No-GC async security types

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.security.types.{
    SecurityEvent, SecuritySeverity, AuditEntry, SecurityContext, AuditConfig,
    severity_for_event, generate_correlation_id, time_now_ms, random_hex,
    _zero_hash_32
}
