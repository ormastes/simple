"""
# No-GC async security types

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.security.types.{
    SecurityEvent, SecuritySeverity, AuditEntry, SecurityContext, AuditConfig,
    PermissionSnapshot, RemoteSecurityContextHeaders, RemoteSecurityContext,
    RemoteSecurityValidatedIdentity,
    remote_security_headers_from_pairs, anonymous_remote_security_context,
    remote_security_session_id_from_pairs, remote_security_bearer_token,
    reconstruct_remote_security_context, sign_remote_security_token,
    validate_remote_security_token, validate_remote_security_context,
    severity_for_event, generate_correlation_id, time_now_ms, random_hex,
    _zero_hash_32
}
