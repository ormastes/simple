"""
# No-GC Async QMP Client

Compatibility facade over the no-GC sync QMP backend.
"""

export use std.nogc_sync_mut.qemu.qmp_client.{
    QmpClient,
    qmp_connect, qmp_disconnect, qmp_send, qmp_send_check,
    qmp_savevm, qmp_loadvm, qmp_query_status, qmp_system_reset, qmp_screendump
}
