"""
# No-GC async desktop global shortcut

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.desktop.global_shortcut.{
    Shortcut,
    shortcut_to_text, register_global_shortcut,
    unregister_global_shortcut, unregister_all_shortcuts
}
