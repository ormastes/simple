"""
# No-GC async desktop lifecycle

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.desktop.lifecycle.{
    LifecycleEvent, AppPhase, AppLifecycle,
    lifecycle_event_name, acquire_single_instance_lock,
    release_single_instance_lock, is_single_instance
}
