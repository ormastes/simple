"""
# No-GC async database core

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.database.core.{
    StringId, file_read, file_write, file_exists,
    StringInterner, SdnRow, SdnTable, SdnDatabase,
    split_sdn_row, strip_quotes, quote_if_needed,
    parse_sdn_table, interner_from_sdn,
    new_sdn_database, load_sdn_database
}
