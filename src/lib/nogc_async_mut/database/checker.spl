"""
# No-GC async database checker

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.database.checker.{
    file_exists,
    DbIssue, DbCheckReport, DbFixResult,
    check_sdn_parse, check_required_fields, check_empty_required,
    check_duplicate_ids, check_valid_column, check_orphan_refs,
    check_file_refs, check_interner_bounds, check_enum_values,
    fix_remove_duplicates, fix_invalid_valid_col, fix_stale_running,
    fix_trim_fields, count_by_severity, fixable_issues, format_issue
}
