# Default-tier wrapper: `use std.enterprise_outbox` resolves here.
# Implementation lives in the sync tier alongside the enterprise store.
export use std.nogc_sync_mut.enterprise_outbox.outbox_worker.*
