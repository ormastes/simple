"""
# No-GC async LSP WASI main

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.lsp.main_wasi.{
    wasi_read_line, wasi_write, wasi_log,
    read_lsp_message, write_lsp_message,
    run_wasi_lsp, extract_id, make_initialize_response, main
}
