"""
# No-GC Async String Benchmark

Compatibility facade over the no-GC sync benchmark backend.
"""

export use std.nogc_sync_mut.benchmark.string_bench.{
    now_ns, repeat_text, make_ascii_corpus, make_mixed_utf8_corpus,
    make_adversarial_corpus, bench_op, run_count_codepoints_bench,
    run_is_ascii_bench, run_validate_bench, run_search_bench,
    run_case_bench, main
}
