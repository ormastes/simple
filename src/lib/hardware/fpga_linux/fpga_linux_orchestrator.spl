# capsule: vhdl.emit.control
# FPGA Linux orchestration capsule.
#
# Bundle generation remains in the data capsule during this compatibility
# extraction.  This owner records the control boundary and is the extension
# point for board execution without changing the public module path.

pub fn fpga_linux_orchestrator_capsule_name() -> text:
    "fpga_linux_orchestrator"
