# Shim: re-exports from common/validation.spl for backward compatibility.
# For new code, prefer importing from std.common.validation directly.

export use std.common.validation.{
    is_positive, is_negative, is_non_negative, is_zero,
    is_in_range, is_outside_range,
    is_not_empty, is_empty,
    is_divisible, is_multiple_of
}
