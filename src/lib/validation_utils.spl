# Validation Utilities Module (Compatibility Wrapper)
#
# This module has been consolidated into validation.spl.
# All functions now delegate to the canonical implementations.
#
# For new code, prefer importing from std.validation directly.

# Import from canonical module
use std.validation.{is_positive, is_negative, is_non_negative, is_zero}
use std.validation.{is_in_range, is_outside_range}
use std.validation.{is_not_empty, is_empty}
use std.validation.{is_divisible, is_multiple_of}

# ============================================================================
# Exports (unchanged for backward compatibility)
# ============================================================================

export is_positive, is_negative, is_non_negative, is_zero
export is_in_range, is_outside_range
export is_not_empty, is_empty
export is_divisible, is_multiple_of
