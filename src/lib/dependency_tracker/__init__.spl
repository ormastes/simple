# Dependency Tracker
#
# Module dependency resolution, import graph, visibility rules, and symbol tables.
# Port of rust/dependency_tracker/

from graph import {ImportGraph, ImportEdge, ImportKind, CyclicDependencyError}
from resolution import {ModPath, Segment, FileKind, ResolutionResult, resolve, well_formed}
from symbol import {SymbolTable, SymbolEntry, SymbolKind, SymbolConflictError, ProjectSymbols}
from visibility import {Visibility, Symbol, SymbolId, ModDecl, DirManifest, ModuleContents}
from visibility import {visibility_meet, ancestor_visibility, effective_visibility, effective_visibility_with_glob}

export ImportGraph, ImportEdge, ImportKind, CyclicDependencyError
export ModPath, Segment, FileKind, ResolutionResult, resolve, well_formed
export SymbolTable, SymbolEntry, SymbolKind, SymbolConflictError, ProjectSymbols
export Visibility, Symbol, SymbolId, ModDecl, DirManifest, ModuleContents
export visibility_meet, ancestor_visibility, effective_visibility, effective_visibility_with_glob

from macro_import import {SymKind, sym_kind_of, MacroSymbol, AutoImport, MacroExports}
from macro_import import {is_auto_imported, auto_imported_macros, partition_exports}
from macro_import import {glob_import, explicit_import, combine_exports, MacroDirManifest}

export SymKind, sym_kind_of, MacroSymbol, AutoImport, MacroExports
export is_auto_imported, auto_imported_macros, partition_exports
export glob_import, explicit_import, combine_exports, MacroDirManifest
