# Simple Additional Libraries Module

# Re-export pure DL library
export PureTensor, compute_strides
export add, sub, mul, div, matmul, relu, sigmoid, tanh
export Linear, ReLU, Sigmoid, Tanh
export Variable, backward
export train_step, evaluate

# Re-export database library
export StringInterner, SdnTable, SdnRow, SdnDatabase
export BugDatabase, TestDatabase, FeatureDatabase

# Re-export JSON library
export JsonBuilder, JsonArrayBuilder, escape_json

use lib.pure.mod (
    PureTensor, compute_strides,
    add, sub, mul, div, matmul, relu, sigmoid, tanh,
    Linear, ReLU, Sigmoid, Tanh,
    Variable, backward,
    train_step, evaluate
)

use lib.database.mod (
    StringInterner, SdnTable, SdnRow, SdnDatabase,
    BugDatabase, TestDatabase, FeatureDatabase
)

use lib.json.mod (
    JsonBuilder, JsonArrayBuilder, escape_json
)

# Re-export pure utilities
export is_digit, is_alpha, is_alnum, is_whitespace
export is_identifier_start, is_identifier_char
export split_lines, trim, starts_with, ends_with, contains
export basename, dirname, join_paths, get_extension

use lib.pure.char_utils (
    is_digit, is_alpha, is_alnum, is_whitespace,
    is_identifier_start, is_identifier_char
)

use lib.pure.string_utils (
    split_lines, trim, starts_with, ends_with, contains
)

use lib.pure.path_utils (
    basename, dirname, join_paths, get_extension
)
