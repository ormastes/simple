# Verification Models
# Formal models for memory capabilities, contracts, and memory ordering.
#
# These models are designed to match the corresponding Lean 4 definitions
# in the verification/ directory, enabling formal verification of Simple programs.

# Import and export tensor_dimensions module
use tensor_dimensions.{Dim, DimVar, DimConstraint, TensorShape, ShapeEnv, DimInferenceContext, ShapeError, unify_dims, unify_shapes, infer_matmul_shape, infer_broadcast_shape, verify_reshape, verify_shape_at_runtime, estimate_tensor_memory, max, min}

pub use Dim, DimVar, DimConstraint, TensorShape, ShapeEnv
pub use DimInferenceContext, ShapeError
pub use unify_dims, unify_shapes, infer_matmul_shape, infer_broadcast_shape
pub use verify_reshape, verify_shape_at_runtime, estimate_tensor_memory
pub use max, min

# NOTE: Other modules commented out to avoid circular import issues
# Users should import specific modules directly:
#   import verification.models.contracts as contracts
# pub use memory_capabilities.*
# pub use contracts.*
# pub use memory_model_drf.*
# pub use type_inference.*
# pub use async_effects.*
