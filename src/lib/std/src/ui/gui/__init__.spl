# GUI - Graphical UI Renderer
#
# This module provides graphical UI rendering that shares the same
# Element/Node types as TUI. Supports both HTML output (for web/SSR)
# and native window rendering (for desktop applications).
#
# Renderers:
#   - HtmlRenderer: Outputs HTML/CSS for web browsers
#   - NativeRenderer: Uses framebuffer FFI for desktop windows
#   - VulkanRenderer: GPU-accelerated renderer using Vulkan API (sync)
#   - VulkanAsyncRenderer: Async/await Vulkan renderer (JavaScript-style)

mod gui

# HTML/CSS renderer for web output
pub use html.*

# Native window renderer for desktop
pub use native.*

# Vulkan renderer for GPU-accelerated rendering (synchronous)
pub use vulkan.*

# Vulkan async renderer for GPU-accelerated rendering (async/await)
pub use vulkan_async.*

# Vulkan core types (device, swapchain, render pass)
pub use vulkan_types.*

# Vulkan shader compilation (SPIR-V loading)
pub use vulkan_shaders.*

# Vulkan graphics pipeline (with smart defaults)
pub use vulkan_pipeline.*

# Vulkan buffers (vertex, index, uniform, storage)
pub use vulkan_buffers.*

# Vulkan command buffers and recording
pub use vulkan_commands.*

# Vulkan GUI renderer (RenderBackend implementation)
pub use vulkan_renderer.*

# Vulkan font rendering (text atlas, TTF loading)
pub use vulkan_font.*

# GUI-specific widgets with graphical styling
pub use widgets.*

# Theme and styling for GUI
pub use theme.*
