# Tree-sitter Parser Module for Simple Language
# Pure Simple implementation for LSP/Editor support

# Parser
export TreeSitterParser from parser

# Tree representation
export Tree, Node, NodeId, Span, NodeArena, TreeCursor, Range, ParentEntry from tree

# Lexer
export Lexer from lexer

# Grammar and tokens
export TokenKind, Token, Grammar, GrammarRule, build_simple_grammar from simple_grammar

# Query system
export Query, QueryMatch, Capture, QueryPattern, QueryCursor from query
export build_highlight_patterns, capture_to_token_type from query
export SYNTAX_HIGHLIGHTING_QUERY, LOCALS_QUERY from query

# Incremental parsing edits
export InputEdit, Point, DiffOp from edits
export compute_edits, find_affected_nodes, find_reparse_boundary from edits
export apply_edits_to_span, count_lines, compute_end_point from edits

# Error recovery
export ErrorRecovery, RecoveryStrategy, SyncPoint, ErrorInfo, ParserContext from error_recovery
export RecoveryAction, recover_from_error from error_recovery
