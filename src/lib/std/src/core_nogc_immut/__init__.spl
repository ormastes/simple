# Variant-agnostic: no_gc + immutable
# Pure functional utilities without heap allocation
# Ideal for: GPU kernels, baremetal, embedded, real-time

#[variant(any)]
#[no_gc]
#[immutable]
mod core_nogc_immut

export functional.*
export static_vec.*
export static_string.*
