# Tree-sitter Parser Module for Simple Language
# Pure Simple implementation for LSP/Editor support

# Parser
pub use TreeSitterParser from parser

# Tree representation
pub use Tree, Node, NodeId, Span, NodeArena, TreeCursor, Range, ParentEntry from tree

# Lexer
pub use Lexer from lexer

# Grammar and tokens
pub use TokenKind, Token, Grammar, GrammarRule, build_simple_grammar from simple_grammar

# Query system
pub use Query, QueryMatch, Capture, QueryPattern, QueryCursor from query
pub use build_highlight_patterns, capture_to_token_type from query
pub use SYNTAX_HIGHLIGHTING_QUERY, LOCALS_QUERY from query

# Incremental parsing edits
pub use InputEdit, Point, DiffOp from edits
pub use compute_edits, find_affected_nodes, find_reparse_boundary from edits
pub use apply_edits_to_span, count_lines, compute_end_point from edits

# Error recovery
pub use ErrorRecovery, RecoveryStrategy, SyncPoint, ErrorInfo, ParserContext from error_recovery
pub use RecoveryAction, recover_from_error from error_recovery
