# Host Variant: async + gc + immutable
# Functional programming style with garbage collection
# All data structures use immutable interfaces (return new instances)

#[variant(platform_host, "async", gc, immutable)]
mod async_gc_immut

pub use use io.*
pub use use net.*
