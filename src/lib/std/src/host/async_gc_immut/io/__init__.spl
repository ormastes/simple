# I/O module for async + gc + immutable variant
# Functional style I/O with immutable data structures

#[variant(platform_host, "async", gc, immutable)]
mod io

pub use use fs.*
pub use use buf.*
pub use use term.*
