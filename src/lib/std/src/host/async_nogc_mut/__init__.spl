# Default Host Variant: async + no_gc + mutable
# Async I/O with manual memory management

#[variant(platform_host, "async", no_gc, mutable)]
mod async_nogc_mut

pub use use io.*
pub use use net.*
pub use use sys.*
pub use use datetime.*
