# HIR - High-level Intermediate Representation
#
# Typed expression tree between AST (CST) and MIR.
# Provides single lowering point for interpreter and compiler.
#
# Architecture:
#   Parser (CST) -> HIR Lowering -> HIR -> Interpreter (eval)
#                                      -> MIR Lowering -> Codegen
#
# Modules:
#   types  - Core HIR types (TypeId, BinOp, HirExprKind, HirStmtKind)
#   module - HirFunction, HirModule definitions (Phase 2)
#   lower  - CST to HIR lowering (Phase 3)
#   eval   - HIR interpreter (Phase 4)

# Re-export types module
pub use use types.*

# Re-export module module (Phase 2)
pub use use module.*

# Lowering exports (Phase 3)
pub use use lower.*

# Eval exports (Phase 4)
pub use use eval.*
