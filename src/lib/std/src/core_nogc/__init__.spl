# Core NoGC Module - Variant-agnostic, explicit #[no_gc] utilities
# These types work without garbage collection

#[no_gc]
#[variant(any)]
mod core_nogc

pub use use arena.*
pub use use fixed_vec.*
pub use use fixed_string.*
pub use use bump.*
