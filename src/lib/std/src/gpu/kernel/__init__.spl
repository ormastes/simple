# GPU Kernel (single variant: async + no_gc + immutable)
# Device-side GPU kernel primitives

#[variant(platform_gpu, "async", no_gc, immutable)]
#[gpu_kernel]
mod kernel

export use core.*
export use memory.*
export use simd.*
export use math.*
export use atomics.*
export use reduce.*
