# Graphics Render Module
#
# 3D rendering system using Vulkan:
# - Device management (shared device singleton)
# - Renderer3D (main rendering loop)
# - Pipeline (forward rendering with Phong lighting)
# - Buffer management (vertex, index, uniform)
# - Texture management (2D, cubemap)
#
# Based on: doc/plans/floating-booping-coral.md

mod render

# Re-export all render types
pub use use device_manager.*
pub use use renderer.*
pub use use pipeline.*
pub use use buffer.*
pub use use texture.*
pub use use batching.*
pub use use shadow.*
pub use use postprocess.*
pub use use debug.*
pub use use ibl.*
