# High-performance collection types using Rust std.collections
#
# This module provides fast, production-ready collection types:
#
# - HashMap: O(1) hash table for fast key-value lookups
# - BTreeMap: O(log n) ordered map with sorted keys
# - HashSet: O(1) hash set for fast membership testing
# - BTreeSet: O(log n) ordered set with sorted elements
#
# These collections use Rust's battle-tested implementations via FFI,
# providing 10-100x speedup over the basic RuntimeDict for large collections.
#
# Example:
#   import collections.{HashMap, HashSet}
#
#   val map = HashMap.new()
#   map.insert("name", "Alice")
#
#   val set = HashSet.new()
#   set.insert(1)
#   set.insert(2)

use hashmap.{HashMap}
use btreemap.{BTreeMap}
use hashset.{HashSet}
use btreeset.{BTreeSet}
