@allow(star_import) # reason: compatibility facade, re-exports backing family symbols
# Compatibility facade: std.gc_sync_mut.security.enforcement.gate -> std.gc_async_mut.security.enforcement.gate
export use std.gc_async_mut.security.enforcement.gate.*
