# Shim: re-exports from lib/nogc_sync_mut/process_monitor.spl
# Allows `use std.process_monitor.{...}` to resolve at runtime
use lib.nogc_sync_mut.process_monitor.*

export ProcessMetrics
export sample_process, monitor_until_exit, exceeds_limits
export is_linux, is_macos, is_windows
