# Shim: std.io_runtime → nogc_sync_mut.io_runtime
# Created for interpreter-mode module resolution (binary can't search lib/*/ subdirs)
export use nogc_sync_mut.io_runtime.{
    shell, shell_output, shell_bool, shell_output_trimmed, ShellResult, ProcessResult,
    file_read, file_write, file_exists, file_delete, file_copy, file_size, file_hash_sha256,
    file_read_bytes, file_write_bytes, file_append_text,
    read_file, read_file_text, write_file, remove_file_if_exists, file_size_raw,
    dir_create, dir_create_all, is_dir, is_file, is_char_device, dir_list, dir_walk, dir_remove, dir_remove_all, dir_exists,
    list_dir, mkdir_p,
    env_get, env_set, cwd, home, platform_name,
    getpid, process_kill, process_run, process_run_bounded, process_spawn_async, process_is_running,
    time_now_unix_micros, time_now_monotonic_ms, get_args, cli_arg_count, cli_arg_at, exit, thread_sleep,
    hash_text, file_rename, shell_exec, process_run_timeout,
    rt_file_write_text, rt_file_read_text,
    host_os, host_arch
}
