# Internationalization (i18n) and Localization (l10n) Utilities Module
# Provides comprehensive locale management, message translation, number/date formatting,
# pluralization, text direction, and collation support.
#
# ARCHITECTURE:
# - Pure Simple implementation (no FFI dependencies)
# - Tuple-based data structures (no runtime generics)
# - Option/nil error handling (no exceptions)
# - No method chaining (intermediate variables)
# - Facade pattern: delegates to organized category modules
#
# LOCALE FORMAT: BCP 47 (language-region-script-variant)
# Example: "en-US" (English, United States), "zh-Hans-CN" (Chinese, Simplified, China)
#
# ORGANIZATION:
# This module is organized using the facade pattern:
# - locale/types.spl: Locale creation, parsing, validation
# - locale/formatting.spl: Message translation, pluralization, text direction
# - locale/numbers.spl: Number and percentage formatting
# - locale/dates.spl: Date, time, and relative time formatting
# - locale/currency.spl: Currency formatting and parsing
# - locale/collation.spl: String comparison and sorting
# - locale/utilities.spl: Text padding and list formatting
# - locale/mod.spl: Main facade that re-exports all functions

import locale

# ============================================================================
# RE-EXPORT ALL FUNCTIONS FROM FACADE
# ============================================================================

# Locale Types and Creation (24 functions)
fn create_locale(language: text, region: text) -> (text, text, text, text):
    locale::create_locale(language, region)

fn create_locale_full(language: text, region: text, script: text, variant: text) -> (text, text, text, text):
    locale::create_locale_full(language, region, script, variant)

fn parse_locale(locale_code: text) -> (text, text, text, text):
    locale::parse_locale(locale_code)

fn locale_to_code(loc: (text, text, text, text)) -> text:
    locale::locale_to_code(loc)

fn default_locale() -> (text, text, text, text):
    locale::default_locale()

fn get_language(loc: (text, text, text, text)) -> text:
    locale::get_language(loc)

fn get_region(loc: (text, text, text, text)) -> text:
    locale::get_region(loc)

fn get_script(loc: (text, text, text, text)) -> text:
    locale::get_script(loc)

fn get_variant(loc: (text, text, text, text)) -> text:
    locale::get_variant(loc)

fn locale_equals(locale1: (text, text, text, text), locale2: (text, text, text, text)) -> bool:
    locale::locale_equals(locale1, locale2)

fn locale_en_us() -> (text, text, text, text):
    locale::locale_en_us()

fn locale_en_gb() -> (text, text, text, text):
    locale::locale_en_gb()

fn locale_es_es() -> (text, text, text, text):
    locale::locale_es_es()

fn locale_es_mx() -> (text, text, text, text):
    locale::locale_es_mx()

fn locale_fr_fr() -> (text, text, text, text):
    locale::locale_fr_fr()

fn locale_fr_ca() -> (text, text, text, text):
    locale::locale_fr_ca()

fn locale_de_de() -> (text, text, text, text):
    locale::locale_de_de()

fn locale_it_it() -> (text, text, text, text):
    locale::locale_it_it()

fn locale_pt_br() -> (text, text, text, text):
    locale::locale_pt_br()

fn locale_pt_pt() -> (text, text, text, text):
    locale::locale_pt_pt()

fn locale_ja_jp() -> (text, text, text, text):
    locale::locale_ja_jp()

fn locale_zh_cn() -> (text, text, text, text):
    locale::locale_zh_cn()

fn locale_zh_tw() -> (text, text, text, text):
    locale::locale_zh_tw()

fn locale_ko_kr() -> (text, text, text, text):
    locale::locale_ko_kr()

fn locale_ar_sa() -> (text, text, text, text):
    locale::locale_ar_sa()

fn locale_ar_ae() -> (text, text, text, text):
    locale::locale_ar_ae()

fn locale_ru_ru() -> (text, text, text, text):
    locale::locale_ru_ru()

fn locale_hi_in() -> (text, text, text, text):
    locale::locale_hi_in()

fn locale_nl_nl() -> (text, text, text, text):
    locale::locale_nl_nl()

fn locale_sv_se() -> (text, text, text, text):
    locale::locale_sv_se()

fn locale_pl_pl() -> (text, text, text, text):
    locale::locale_pl_pl()

fn is_valid_language_code(code: text) -> bool:
    locale::is_valid_language_code(code)

fn is_valid_region_code(code: text) -> bool:
    locale::is_valid_region_code(code)

fn is_valid_locale(loc: (text, text, text, text)) -> bool:
    locale::is_valid_locale(loc)

# Formatting and Translation (5 functions)
fn load_messages(loc: (text, text, text, text), messages: text):
    locale::load_messages(loc, messages)

fn add_message(loc: (text, text, text, text), key: text, value: text):
    locale::add_message(loc, key, value)

fn translate(key: text, loc: (text, text, text, text)) -> text:
    locale::translate(key, loc)

fn translate_with_args(key: text, args: (text, text, text, text, text), loc: (text, text, text, text)) -> text:
    locale::translate_with_args(key, args, loc)

fn translate_with_count(key: text, count: i64, loc: (text, text, text, text)) -> text:
    locale::translate_with_count(key, count, loc)

# Pluralization (2 functions)
fn plural_rule(count: i64, loc: (text, text, text, text)) -> text:
    locale::plural_rule(count, loc)

fn pluralize(count: i64, forms: (text, text, text, text, text, text), loc: (text, text, text, text)) -> text:
    locale::pluralize(count, forms, loc)

# Text Direction (3 functions)
fn text_direction(loc: (text, text, text, text)) -> text:
    locale::text_direction(loc)

fn is_rtl(loc: (text, text, text, text)) -> bool:
    locale::is_rtl(loc)

fn is_ltr(loc: (text, text, text, text)) -> bool:
    locale::is_ltr(loc)

# Number Formatting (6 functions)
fn get_decimal_separator(loc: (text, text, text, text)) -> text:
    locale::get_decimal_separator(loc)

fn get_thousands_separator(loc: (text, text, text, text)) -> text:
    locale::get_thousands_separator(loc)

fn format_number(num: i64, loc: (text, text, text, text)) -> text:
    locale::format_number(num, loc)

fn format_decimal(num: i64, decimals: i64, loc: (text, text, text, text)) -> text:
    locale::format_decimal(num, decimals, loc)

fn format_percent(ratio: i64, loc: (text, text, text, text)) -> text:
    locale::format_percent(ratio, loc)

fn parse_percent(text: text, loc: (text, text, text, text)) -> i64:
    locale::parse_percent(text, loc)

# Currency (5 functions)
fn currency_symbol(currency: text, loc: (text, text, text, text)) -> text:
    locale::currency_symbol(currency, loc)

fn currency_decimals(currency: text) -> i64:
    locale::currency_decimals(currency)

fn currency_position(loc: (text, text, text, text)) -> text:
    locale::currency_position(loc)

fn format_currency(amount: i64, currency: text, loc: (text, text, text, text)) -> text:
    locale::format_currency(amount, currency, loc)

fn parse_currency(text: text, currency: text, loc: (text, text, text, text)) -> i64:
    locale::parse_currency(text, currency, loc)

# Date and Time (9 functions)
fn month_name(month: i64, loc: (text, text, text, text)) -> text:
    locale::month_name(month, loc)

fn month_name_short(month: i64, loc: (text, text, text, text)) -> text:
    locale::month_name_short(month, loc)

fn day_name(day: i64, loc: (text, text, text, text)) -> text:
    locale::day_name(day, loc)

fn day_name_short(day: i64, loc: (text, text, text, text)) -> text:
    locale::day_name_short(day, loc)

fn format_date(year: i64, month: i64, day: i64, pattern: text, loc: (text, text, text, text)) -> text:
    locale::format_date(year, month, day, pattern, loc)

fn format_time(hour: i64, minute: i64, second: i64, pattern: text, loc: (text, text, text, text)) -> text:
    locale::format_time(hour, minute, second, pattern, loc)

fn relative_time_seconds(seconds: i64, loc: (text, text, text, text)) -> text:
    locale::relative_time_seconds(seconds, loc)

fn time_ago(seconds: i64, loc: (text, text, text, text)) -> text:
    locale::time_ago(seconds, loc)

fn time_until(seconds: i64, loc: (text, text, text, text)) -> text:
    locale::time_until(seconds, loc)

# Collation (5 functions)
fn compare_strings(s1: text, s2: text, loc: (text, text, text, text)) -> i64:
    locale::compare_strings(s1, s2, loc)

fn sort_strings(items: (text, text, text, text, text), loc: (text, text, text, text)) -> (text, text, text, text, text):
    locale::sort_strings(items, loc)

fn to_upper_locale(text: text, loc: (text, text, text, text)) -> text:
    locale::to_upper_locale(text, loc)

fn to_lower_locale(text: text, loc: (text, text, text, text)) -> text:
    locale::to_lower_locale(text, loc)

fn to_title_case(text: text, loc: (text, text, text, text)) -> text:
    locale::to_title_case(text, loc)

# Utilities (3 functions)
fn pad_left(text: text, width: i64, pad_char: text) -> text:
    locale::pad_left(text, width, pad_char)

fn pad_right(text: text, width: i64, pad_char: text) -> text:
    locale::pad_right(text, width, pad_char)

fn format_list(items: (text, text, text, text, text), loc: (text, text, text, text)) -> text:
    locale::format_list(items, loc)

# ============================================================================
# MODULE EXPORTS SUMMARY
# ============================================================================

# This module provides 80+ functions for comprehensive i18n/l10n support:
#
# Locale Management (24 functions):
#   - create_locale, create_locale_full, parse_locale, locale_to_code
#   - get_language, get_region, get_script, get_variant, locale_equals
#   - default_locale, is_valid_language_code, is_valid_region_code, is_valid_locale
#   - 20 common locale constructors (en_us, fr_fr, es_es, de_de, ja_jp, zh_cn, etc.)
#
# Translation (5 functions):
#   - load_messages, add_message, translate, translate_with_args, translate_with_count
#
# Pluralization (2 functions):
#   - plural_rule, pluralize
#
# Text Direction (3 functions):
#   - text_direction, is_rtl, is_ltr
#
# Number Formatting (6 functions):
#   - format_number, format_decimal, get_decimal_separator, get_thousands_separator
#   - format_percent, parse_percent
#
# Currency (5 functions):
#   - format_currency, parse_currency, currency_symbol, currency_decimals, currency_position
#
# Date/Time (9 functions):
#   - month_name, month_name_short, day_name, day_name_short
#   - format_date, format_time, relative_time_seconds, time_ago, time_until
#
# Collation (5 functions):
#   - compare_strings, sort_strings, to_upper_locale, to_lower_locale, to_title_case
#
# Utilities (3 functions):
#   - pad_left, pad_right, format_list
#
# Total: 80+ functions organized into 7 category modules via facade pattern
