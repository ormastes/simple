# Base64 Encoding/Decoding Utilities Module (Facade)
#
# Provides comprehensive Base64, Base32, hex, and binary encoding/decoding
# utilities following RFC 4648 standards.
#
# FEATURES:
# - Standard Base64 encoding/decoding
# - URL-safe Base64 variants
# - Base32 encoding/decoding (standard and extended hex)
# - Hexadecimal encoding/decoding
# - Binary string encoding/decoding
# - Custom alphabet support
# - Chunked/multiline encoding
# - Validation utilities
# - Padding control
# - Constant-time comparison for security
#
# IMPLEMENTATION NOTES:
# - Pure Simple implementation (no FFI)
# - No generics (uses concrete types)
# - No try/catch (uses Option/nil pattern)
# - No chained methods (uses intermediate variables)
# - Tuple-based data structures
#
# ARCHITECTURE:
# This is a facade module that re-exports functionality from organized submodules:
# - base64/types: Constants, alphabets, and type definitions
# - base64/encode: All encoding operations
# - base64/decode: All decoding operations
# - base64/validation: Input validation utilities
# - base64/variants: Custom alphabet support
# - base64/utilities: Low-level helpers

# ============================================================================
# RE-EXPORTS FROM BASE64 MODULE
# ============================================================================

# Import and re-export all public functionality from the base64 module
use base64::types::{
    BASE64_ALPHABET,
    BASE64_URL_ALPHABET,
    BASE32_ALPHABET,
    BASE32_HEX_ALPHABET,
    HEX_ALPHABET_LOWER,
    HEX_ALPHABET_UPPER,
    PADDING_CHAR,
    get_base64_alphabet,
    get_base64_url_alphabet,
    get_base32_alphabet,
    get_base32_hex_alphabet,
    get_hex_alphabet_lower,
    get_hex_alphabet_upper
}

use base64::encode::{
    encode_base64,
    encode_base64_no_pad,
    encode_base64_with_alphabet,
    encode_base64_url,
    encode_base64_url_no_pad,
    encode_base32,
    encode_base32_hex,
    encode_base32_no_pad,
    encode_base32_with_alphabet,
    encode_hex,
    encode_hex_upper,
    encode_hex_lower,
    encode_hex_with_alphabet,
    encode_binary,
    encode_int,
    encode_chunked,
    encode_stream
}

use base64::decode::{
    decode_base64,
    decode_base64_url,
    decode_base64_with_alphabet,
    decode_base32,
    decode_base32_hex,
    decode_base32_with_alphabet,
    decode_hex,
    decode_binary,
    decode_to_int,
    decode_chunked,
    decode_stream
}

use base64::validation::{
    is_valid_base64,
    is_valid_base64_url,
    is_valid_hex,
    is_valid_base32
}

use base64::variants::{
    create_alphabet,
    encode_with_alphabet,
    decode_with_alphabet
}

use base64::utilities::{
    text_to_bytes,
    bytes_to_text,
    compare_constant_time,
    strip_whitespace,
    estimate_encoded_size,
    estimate_decoded_size,
    encode_multiline,
    char_code,
    char_from_code,
    is_whitespace_char,
    is_base64_char,
    text_char_at,
    text_length,
    text_substring,
    text_substring_helper,
    text_concat,
    byte_to_bits,
    bits_to_byte,
    list_append,
    list_get,
    list_length,
    char_to_base64_index,
    base64_index_to_char,
    calculate_padding,
    add_padding,
    remove_padding,
    count_padding,
    find_char_in_alphabet,
    int_to_text,
    text_to_int
}
