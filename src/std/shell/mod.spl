# Shell Module - Shell utilities facade
#
# Module index for shell/ subdirectory

# Import env module functions
use env.{get, set, cwd}

# Create env namespace with functions
val env = {
    "get": get,
    "set": set,
    "cwd": cwd
}

export env
