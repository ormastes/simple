# GPU Module - Unified GPU Interface
#
# Provides backend-agnostic GPU computing with automatic device selection,
# memory management, and stream-based async operations.
#
# Quick Start:
#   use std.gpu.{Context}
#
#   val ctx = Context.default()  # Auto-detect GPU
#   val arr = ctx.alloc_upload([1.0, 2.0, 3.0])
#   val result = arr.download()

# Re-export from submodules
use std.gpu.device.{GpuBackend, Gpu, detect_backends, preferred_backend, gpu_cuda, gpu_vulkan, gpu_none}
use std.gpu.memory.{GpuArray, gpu_alloc, gpu_alloc_upload, gpu_alloc_zeros}
use std.gpu.context.{Context, create_context_from_config}

# ============================================================================
# Exports
# ============================================================================

# Device types and functions
export GpuBackend, Gpu
export detect_backends, preferred_backend
export gpu_cuda, gpu_vulkan, gpu_none

# Memory types and functions
export GpuArray
export gpu_alloc, gpu_alloc_upload, gpu_alloc_zeros

# Context API (main interface)
export Context
export create_context_from_config
