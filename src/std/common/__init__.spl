# Common Utilities
#
# Configuration, diagnostics, and target architecture abstractions.
# Port of rust/common/

from config_env import {ConfigEnv}
from diagnostic import {Severity, Span, Label, EasyFix, Replacement, FixConfidence}
from diagnostic import {Diagnostic, Diagnostics, SourceRegistry}
from target import {TargetArch, PointerSize, TargetOS, TargetConfig, Target, WasmRuntime, Endian}
from file_reader import {ReadStrategy, MappedFile, FileReader, read_to_string, read_to_bytes}
from runtime_symbols import {AbiVersion, CURRENT_ABI, RuntimeSymbolProvider, RUNTIME_SYMBOL_NAMES, has_runtime_symbol}
from fault_detection import {FaultKind, check_faults}
from fault_detection import {
    set_stack_overflow_detection_enabled, is_stack_overflow_detection_enabled,
    set_max_recursion_depth, get_max_recursion_depth, reset_recursion_depth,
    increment_recursion_depth, decrement_recursion_depth, current_recursion_depth,
    is_timeout_exceeded, set_timeout_exceeded, reset_timeout
}
from easy_fix_rules import {levenshtein, suggest_typo_fix, suggest_type_coercion_fix, suggest_keyword_reorder}
from fix_applicator import {FixError, FixReport, apply_fixes, apply_to_disk, filter_by_confidence, filter_by_id}

export ConfigEnv
export Severity, Span, Label, EasyFix, Replacement, FixConfidence
export Diagnostic, Diagnostics, SourceRegistry
export TargetArch, PointerSize, TargetOS, TargetConfig, Target, WasmRuntime, Endian
export ReadStrategy, MappedFile, FileReader, read_to_string, read_to_bytes
export AbiVersion, CURRENT_ABI, RuntimeSymbolProvider, RUNTIME_SYMBOL_NAMES, has_runtime_symbol
export FaultKind, check_faults
export set_stack_overflow_detection_enabled, is_stack_overflow_detection_enabled
export set_max_recursion_depth, get_max_recursion_depth, reset_recursion_depth
export increment_recursion_depth, decrement_recursion_depth, current_recursion_depth
export is_timeout_exceeded, set_timeout_exceeded, reset_timeout
export levenshtein, suggest_typo_fix, suggest_type_coercion_fix, suggest_keyword_reorder
export FixError, FixReport, apply_fixes, apply_to_disk, filter_by_confidence, filter_by_id
