# Tree Utilities
#
# Binary tree, BST, and heap implementations.
# Pure Simple implementation - no external dependencies.
#
# Represents trees using tuples: (value, left, right) for nodes, nil for empty.
# Heaps use array-based representation for efficiency.
#
# Functions:
# - Binary Tree: create_node, insert_level_order, traversals (inorder, preorder, postorder)
# - Tree Properties: height, size, is_balanced, is_complete, leaf_count
# - BST: bst_insert, bst_search, bst_delete, bst_min, bst_max, bst_validate
# - Tree Utilities: lowest_common_ancestor, find_path, mirror, diameter
# - Heaps: heapify, heap_insert, extract_min/max, is_heap, heap_sort
# - Conversions: tree_to_array, array_to_tree

# ============================================================================
# Imports
# ============================================================================

from types import create_node, create_leaf, node_value, node_left, node_right, is_leaf
from create import insert_level_order, array_to_tree, bst_from_sorted
from traverse import inorder, preorder, postorder, level_order
from modify import bst_insert, bst_delete, mirror
from modify import heap_parent, heap_left, heap_right
from modify import min_heapify_down, max_heapify_down, min_heapify_up, max_heapify_up
from modify import build_min_heap, build_max_heap
from modify import heap_insert_min, heap_insert_max, extract_min, extract_max
from modify import heap_sort
from query import height, size, leaf_count, is_balanced, is_complete, diameter
from query import bst_search, bst_min, bst_max, bst_validate
from query import find_path, lowest_common_ancestor, are_identical, is_subtree
from query import peek_min, peek_max, is_min_heap, is_max_heap

# ============================================================================
# Exports
# ============================================================================

# Types
export create_node, create_leaf, node_value, node_left, node_right, is_leaf

# Create
export insert_level_order, array_to_tree, bst_from_sorted

# Traverse
export inorder, preorder, postorder, level_order

# Modify
export bst_insert, bst_delete, mirror
export heap_parent, heap_left, heap_right
export min_heapify_down, max_heapify_down, min_heapify_up, max_heapify_up
export build_min_heap, build_max_heap
export heap_insert_min, heap_insert_max, extract_min, extract_max
export heap_sort

# Query
export height, size, leaf_count, is_balanced, is_complete, diameter
export bst_search, bst_min, bst_max, bst_validate
export find_path, lowest_common_ancestor, are_identical, is_subtree
export peek_min, peek_max, is_min_heap, is_max_heap
