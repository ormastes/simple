# Compression Module
#
# Compression and decompression utilities (gzip, zlib, etc.).
#
# Public API:
#   - use std.compress.{compress, decompress}
#   - use std.compress.{gzip_compress, gzip_decompress}

# All submodules are automatically available.
