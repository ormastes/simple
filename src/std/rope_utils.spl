# Rope Data Structure Module (Facade)
# Efficient string manipulation using tree-based rope structure
# Provides O(log n) operations for large text documents
#
# This is a facade module that re-exports all rope functionality
# from categorized submodules in src/std/rope/

# ============================================================================
# IMPORT SUBMODULES
# ============================================================================

# Import type definitions and constants
import "rope/types.spl"

# Import creation operations
import "rope/create.spl"

# Import concatenation operations
import "rope/concat.spl"

# Import splitting operations
import "rope/split.spl"

# Import search and comparison operations
import "rope/search.spl"

# Import modification operations
import "rope/modify.spl"

# Import balancing operations
import "rope/balance.spl"

# Import utility operations
import "rope/utilities.spl"

# ============================================================================
# EXPORTS (for documentation)
# ============================================================================
# Core operations: 79 functions
# - Creation: rope_create_empty, rope_create_from_string, rope_create_leaf, rope_create_concat, rope_clone
# - Properties: rope_is_leaf, rope_is_concat, rope_is_empty, rope_length, rope_weight, rope_depth
# - Concatenation: rope_concat, rope_concat_string, rope_prepend_string, rope_merge_chunks
# - Indexing: rope_char_at, rope_substring
# - Splitting: rope_split_at, rope_split_at_multiple, rope_split_into_chunks
# - Insertion: rope_insert_at, rope_insert_rope_at, rope_insert_line
# - Deletion: rope_delete_range, rope_delete_at, rope_delete_line
# - Replacement: rope_replace, rope_replace_range, rope_replace_line
# - Rebalancing: rope_needs_rebalancing, rope_rebalance, rope_force_rebalance, rope_to_leaf_list, rope_from_leaf_list, rope_is_balanced, rope_compact, rope_optimize_for_append, rope_optimize_for_prepend
# - Conversion: rope_to_string, rope_to_string_builder, rope_to_lines
# - Iteration: rope_iter_chars, rope_iter_leaves, rope_for_each_char, rope_map_chars
# - Search: rope_find, rope_find_all, rope_contains, rope_starts_with, rope_ends_with
# - Comparison: rope_equals, rope_compare
# - Utilities: rope_reverse, rope_trim, rope_trim_start, rope_trim_end, rope_to_upper, rope_to_lower
# - Statistics: rope_stats, rope_print_stats
# - Line operations: rope_line_count, rope_get_line
# - Word operations: rope_word_count, rope_split_words, rope_join_words
# - Filtering: rope_filter_lines, rope_filter_chars
# - Slicing: rope_slice, rope_take, rope_drop, rope_take_while, rope_drop_while
# - Aggregation: rope_fold_left, rope_fold_right, rope_reduce
# - Partitioning: rope_partition, rope_span
# - Formatting: rope_pad_left, rope_pad_right, rope_center
# - Validation: rope_validate, rope_debug_print, rope_indent_str
