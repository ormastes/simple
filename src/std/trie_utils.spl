# Trie (Prefix Tree) Implementation
#
# A trie is a tree data structure used for efficient string storage and retrieval.
# Each node represents a character, and paths from root to nodes form strings.
# Ideal for autocomplete, spell checking, and prefix-based searches.
#
# Examples:
#   trie = trie_create()
#   trie_insert(trie, "cat")
#   trie_insert(trie, "car")
#   trie_insert(trie, "card")
#   trie_contains(trie, "cat")  # true
#   trie_starts_with(trie, "ca")  # true
#   trie_find_all_with_prefix(trie, "ca")  # ["cat", "car", "card"]
#   trie_suggest(trie, "ca", 2)  # ["cat", "car"]

# Import all trie modules
import trie/types
import trie/insert
import trie/search
import trie/delete
import trie/prefix
import trie/traversal
import trie/utilities

# Re-export all public functions from submodules
# This maintains backward compatibility with existing code

# Types (from trie/types.spl)
fn trie_create(): types.trie_create()
fn trie_node(): types.trie_node()

# Insert operations (from trie/insert.spl)
fn trie_insert(trie, word): insert.trie_insert(trie, word)
fn trie_insert_all(trie, words): insert.trie_insert_all(trie, words)

# Search operations (from trie/search.spl)
fn trie_search(trie, word): search.trie_search(trie, word)
fn trie_contains(trie, word): search.trie_contains(trie, word)
fn trie_starts_with(trie, prefix): search.trie_starts_with(trie, prefix)
fn trie_find_node(trie, str): search.trie_find_node(trie, str)
fn trie_wildcard_search(trie, pattern): search.trie_wildcard_search(trie, pattern)
fn trie_words_with_pattern(trie, pattern): search.trie_words_with_pattern(trie, pattern)
fn trie_words_ending_with(trie, suffix): search.trie_words_ending_with(trie, suffix)
fn trie_words_containing(trie, substring): search.trie_words_containing(trie, substring)
fn trie_find_anagrams(trie, word): search.trie_find_anagrams(trie, word)
fn trie_fuzzy_search(trie, word, max_distance): search.trie_fuzzy_search(trie, word, max_distance)
fn trie_contains_batch(trie, words): search.trie_contains_batch(trie, words)
fn trie_contains_any(trie, words): search.trie_contains_any(trie, words)
fn trie_contains_all(trie, words): search.trie_contains_all(trie, words)

# Delete operations (from trie/delete.spl)
fn trie_delete(trie, word): delete.trie_delete(trie, word)
fn trie_delete_all(trie, words): delete.trie_delete_all(trie, words)
fn trie_remove_if(trie, predicate): delete.trie_remove_if(trie, predicate)

# Prefix operations (from trie/prefix.spl)
fn trie_find_all_with_prefix(trie, prefix): prefix.trie_find_all_with_prefix(trie, prefix)
fn trie_count_with_prefix(trie, prefix): prefix.trie_count_with_prefix(trie, prefix)
fn trie_longest_common_prefix(trie): prefix.trie_longest_common_prefix(trie)
fn trie_longest_prefix(trie, str): prefix.trie_longest_prefix(trie, str)
fn trie_all_prefixes(trie, word): prefix.trie_all_prefixes(trie, word)
fn trie_is_prefix_of_word(trie, word): prefix.trie_is_prefix_of_word(trie, word)
fn trie_words_with_prefix_only(trie, prefix): prefix.trie_words_with_prefix_only(trie, prefix)
fn trie_suggest(trie, prefix, max_results): prefix.trie_suggest(trie, prefix, max_results)
fn trie_complete_word(trie, prefix): prefix.trie_complete_word(trie, prefix)
fn trie_suggest_scored(trie, prefix, max_results, scorer): prefix.trie_suggest_scored(trie, prefix, max_results, scorer)
fn trie_suggest_by_frequency(trie, prefix, frequencies, max_results): prefix.trie_suggest_by_frequency(trie, prefix, frequencies, max_results)
fn trie_most_common_prefix_length(trie): prefix.trie_most_common_prefix_length(trie)

# Traversal operations (from trie/traversal.spl)
fn trie_all_words(trie): traversal.trie_all_words(trie)
fn trie_all_unique_prefixes(trie): traversal.trie_all_unique_prefixes(trie)
fn trie_all_paths(trie): traversal.trie_all_paths(trie)
fn trie_filter(trie, predicate): traversal.trie_filter(trie, predicate)
fn trie_map(trie, mapper): traversal.trie_map(trie, mapper)

# Utility operations (from trie/utilities.spl)
fn trie_size(trie): utilities.trie_size(trie)
fn trie_is_empty(trie): utilities.trie_is_empty(trie)
fn trie_depth(trie): utilities.trie_depth(trie)
fn trie_min_depth(trie): utilities.trie_min_depth(trie)
fn trie_shortest_word(trie): utilities.trie_shortest_word(trie)
fn trie_longest_word(trie): utilities.trie_longest_word(trie)
fn trie_merge(trie1, trie2): utilities.trie_merge(trie1, trie2)
fn trie_from_list(words): utilities.trie_from_list(words)
fn trie_to_list(trie): utilities.trie_to_list(trie)
fn trie_clear(trie): utilities.trie_clear(trie)
fn trie_branch_count(trie): utilities.trie_branch_count(trie)
fn trie_words_of_length(trie, length): utilities.trie_words_of_length(trie, length)
fn trie_words_in_range(trie, min_len, max_len): utilities.trie_words_in_range(trie, min_len, max_len)
fn trie_node_count(trie): utilities.trie_node_count(trie)
fn trie_to_string(trie): utilities.trie_to_string(trie)
fn trie_from_string(str): utilities.trie_from_string(str)
fn trie_average_word_length(trie): utilities.trie_average_word_length(trie)
fn trie_length_distribution(trie): utilities.trie_length_distribution(trie)
fn trie_intersection(trie1, trie2): utilities.trie_intersection(trie1, trie2)
fn trie_union(trie1, trie2): utilities.trie_union(trie1, trie2)
fn trie_difference(trie1, trie2): utilities.trie_difference(trie1, trie2)
fn trie_symmetric_difference(trie1, trie2): utilities.trie_symmetric_difference(trie1, trie2)
fn trie_is_subset(trie1, trie2): utilities.trie_is_subset(trie1, trie2)
fn trie_equals(trie1, trie2): utilities.trie_equals(trie1, trie2)
fn trie_is_valid(trie): utilities.trie_is_valid(trie)
fn trie_memory_usage(trie): utilities.trie_memory_usage(trie)
fn trie_compression_ratio(trie): utilities.trie_compression_ratio(trie)
