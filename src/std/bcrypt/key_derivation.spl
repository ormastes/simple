# bcrypt Key Derivation (Blowfish/Eksblowfish)
import "std/bcrypt/core" as core

fn test_blowfish_encrypt(left: i64, right: i64) -> list[i64]:
    core.test_blowfish_encrypt(left, right)

fn get_blowfish_state() -> list[list[i64]]:
    core.get_blowfish_state()
