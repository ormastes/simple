[package]
name = "simple-stub"
version = "0.1.0"
edition = "2021"
description = "Minimal executable stub for Simple settlement executables"

[lints]
workspace = true

[[bin]]
name = "simple_stub_old"
path = "src/main.rs"

# Main stub is now Simple: src/app/stub/main.spl

[dependencies]
simple-loader = { path = "../loader" }
