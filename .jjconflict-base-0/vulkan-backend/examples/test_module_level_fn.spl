# Test with module-level function
use spec.*

fn module_square(x: i64) -> i64:
    return x * x

describe "module level function":
    it "works":
        result = module_square(5)
        expect(result).to_equal(25)
