# Files rt_migrate must never rewrite (reported as `SKIP <path> reason=skip-list`).
# One relative path per line; `#` comments. A file can also opt out on its own
# with a `# rt-migrate: skip` comment line (reported as reason=marker).
#
# Contract specs lock the literal rt_* source text of these files
# (compiler lane finding, 2026-08-28):
src/compiler/80.driver/cache/cas_store.spl
src/compiler/80.driver/driver_hir_cache.spl
src/app/cli/native_build_worker.spl
