#!/bin/sh
set -eu

module=src/compiler/50.mir/_MirLoweringExpr/literals.spl

if rg -q '^extern fn rt_' "$module"; then
    echo "MIR literal lowering must not retain unused raw SFFI authority" >&2
    exit 1
fi
