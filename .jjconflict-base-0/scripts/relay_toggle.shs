#!/bin/sh
# Toggle the USB relay (flip current state).
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname "$0")" && pwd)"
. "$SCRIPT_DIR/relay_common.shs"

detect_relay_device
serial_send_noreply "$RELAY_CMD_TOGGLE"
relay_log "Toggle sent to $RELAY_DEVICE"
