# DType module
mod dtype

pub enum DType:
    Float32
    Float64
    Int32
    Int64

impl DType:
    pub fn to_string(self) -> text:
        match self:
            case Float32: "float32"
            case Float64: "float64"
            case Int32: "int32"
            case Int64: "int64"
