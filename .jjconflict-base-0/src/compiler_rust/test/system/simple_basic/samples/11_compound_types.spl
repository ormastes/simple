# 03_compound_types.spl - Tuples, lists, dicts

# Tuple
pair = (1, "hello")
print "pair = {pair}"

# List
list = [1, 2, 3, 4, 5]
print "list = {list}"
print "list.len() = {list.len()}"

# Dictionary
m = {"a": 1, "b": 2, "c": 3}
print "m = {m}"

# Exit with list length
main = list.len()
