# Watcher Module — Singleton watcher service with file-based IPC
#
# Provides a background watcher daemon that monitors source files,
# generates SHB/SMF cache artifacts on demand, and coordinates
# with the compiler/interpreter/loader via file-based protocol.

