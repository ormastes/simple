# Virtual Memory Manager public facade.
#
# Keep the dependency graph one-way: leaf modules import vmm_core, while this
# facade aggregates the core and leaf APIs for existing callers.

use os.kernel.memory.vmm_core
use os.kernel.memory.vmm_vma
use os.kernel.memory.vmm_address_space
use os.kernel.memory.vmm_copy
