# Unit: Grain
# Kind: Mass
# Symbol: grain
# Canonical scale: 6.47989e-05 kg

class Grain:
    value: f64

    fn to_f64() -> f64: self.value
    fn add(other: Grain) -> Grain: Grain(value: self.value + other.value)
    fn sub(other: Grain) -> Grain: Grain(value: self.value - other.value)

    static fn kind() -> text: "Mass"
    static fn symbol() -> text: "grain"
    static fn scale_to_base() -> f64: 6.47989e-05
    static fn base_unit() -> text: "kg"
    static fn zero() -> Grain: Grain(value: 0.0)

export Grain
