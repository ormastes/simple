# Unit: Pps
# Kind: NetPacketRate
# Symbol: pps
# Canonical scale: 1.0 pps

class Pps:
    value: f64

    fn to_f64() -> f64: self.value
    fn add(other: Pps) -> Pps: Pps(value: self.value + other.value)
    fn sub(other: Pps) -> Pps: Pps(value: self.value - other.value)

    static fn kind() -> text: "NetPacketRate"
    static fn symbol() -> text: "pps"
    static fn scale_to_base() -> f64: 1.0
    static fn base_unit() -> text: "pps"
    static fn zero() -> Pps: Pps(value: 0.0)

export Pps
