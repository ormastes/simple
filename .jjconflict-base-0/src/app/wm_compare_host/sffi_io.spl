# Host adapter: sffi.io fs primitives (dir/file read+write via sffi) for
# wm_compare.
#
# Lives outside wm_lane_scope_dirs() (see
# src/compiler/90.tools/lint/_LintMain/wm_lane_boundary_lints.spl:51) so the
# nogc_sync_mut.sffi.io import below is legal here.
# See doc/03_plan/ui/wm_lane_boundary_ratchet_lanes.md (tranche W2).

export use std.nogc_sync_mut.sffi.io.{
    dir_create_all, file_write_text, file_read_bytes, file_write_bytes, file_exists
}
