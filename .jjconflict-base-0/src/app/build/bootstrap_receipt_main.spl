# Minimal bootstrap-only receipt entrypoint.

use app.build.bootstrap_receipt_planner.{
    normalize_bootstrap_receipt_args, plan_bootstrap_authorization
}

fn main() -> i64:
    plan_bootstrap_authorization(normalize_bootstrap_receipt_args(get_args()))
