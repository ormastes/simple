"""
# Application window SFFI compatibility facade

The canonical SDL/native ABI is owned by `std.nogc_sync_mut.sffi.window_abi`.
Application code consumes the pure-Simple window product surface.
"""

@allow(star_import) # reason: compatibility facade, re-exports the canonical product surface
export use std.nogc_sync_mut.io.window_sffi.*
