# Leak Check App Shim - Main
#
# Delegates the app-facing `app.leak_check.main` module to the current
# implementation under compiler tooling.

use compiler.tools.leak_check.main.{run_leak_check}

export run_leak_check
