use app.compile.c_codegen (generate_c_code)
extern fn rt_file_read_text(path: text) -> text

fn main():
    val source_raw = rt_file_read_text("examples/native_v2_test.spl")
    val source = source_raw ?? ""
    val c_code = generate_c_code(source)
    print c_code
