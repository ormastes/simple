# Chromium Shell — Re-exports (V3 simple_browser shell — M1)

# Backend adapter
export ChromiumBackend

# Application
export ChromiumApp, chromium_main, parse_chromium_backend

# Tab manager (M6 — multi-window / tabs)
export BrowserTab, TabManager
export DEFAULT_TAB_WIDTH, DEFAULT_TAB_HEIGHT, DEFAULT_TAB_CLEAR

# Interactivity (M7 — hotkeys + tab-strip hit testing)
export chromium_hotkey_action, apply_hotkey_action, hit_test_tab_strip
export TAB_STRIP_HEIGHT
