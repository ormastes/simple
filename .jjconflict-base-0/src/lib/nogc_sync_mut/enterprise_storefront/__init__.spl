# Enterprise Storefront — customer browse->cart->checkout->receipt vertical over
# the enterprise store (lane .spipe/simple_enterprise_suite, W21-B). Reuses the
# frozen `sales` role via a local gate; does not touch foundation.spl or
# rbac_registry.spl.
export use std.nogc_sync_mut.enterprise_storefront.storefront.*
