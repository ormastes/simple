"""
# GC Async Platform Helpers

Compatibility facade over the no-GC sync platform backend. Host platform
detection and path helper operations are synchronous runtime calls, so the GC
async family reuses the shared no-GC declaration owner.
"""

export use std.nogc_async_mut.platform.*
