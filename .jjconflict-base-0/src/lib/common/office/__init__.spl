# Office Framework - Re-exports
#
# Shared utilities for the Simple Office suite.

# Undo/Redo (undo.spl)
export UndoEntry, UndoStack

# Clipboard (clipboard.spl)
export ClipboardContent, OfficeClipboard

# Search (search.spl)
export SearchMatch, SearchOptions
export find_all, find_next, replace_first, replace_all, default_search_options

# File Operations (file_ops.spl)
export RecentFile, RecentFileStore
export file_extension_for_app, app_name_for_extension
