# Iterator reduction functions

use std.iterator.create.{Iterator}

fn iter_collect<T>(iter: Iterator<T>) -> [T]:
    """Collect all elements of an iterator into an array."""
    []

export iter_collect
