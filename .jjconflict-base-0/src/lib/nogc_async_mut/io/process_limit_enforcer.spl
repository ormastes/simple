"""
# No-GC Async Process Limit Enforcer

Compatibility facade over the no-GC sync process-limit backend.
"""

export use std.nogc_sync_mut.io.process_limit_enforcer.{
    build_ulimit_command, build_timeout_wrapper, detect_violation,
    detect_violation_from_profile, format_violation_message,
    supports_ulimit, get_ulimit_flags, bytes_to_kb, ms_to_seconds,
    profile_to_ulimit_params
}
