"""
# No-GC async profiler_simple

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.io.profiler_simple.{
    ProfileTimer,
    profiler_active, profiler_call, profiler_return,
    profile_function, timer_start, timer_end,
    profile_begin, profile_end, with_profiling
}
