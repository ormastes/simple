# Default-tier wrapper: `use std.enterprise_sale` resolves here.
# Implementation lives in the sync tier alongside the enterprise store.
export use std.nogc_sync_mut.enterprise_sale.foundation.*
export use std.nogc_sync_mut.enterprise_sale.goods_sale.*
