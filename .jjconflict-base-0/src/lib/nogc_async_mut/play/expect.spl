"""
# No-GC async play expectations

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.play.expect.{
    PageExpect, LocatorExpect,
    expect_page, expect_locator, expect_page_with, expect_locator_with
}
