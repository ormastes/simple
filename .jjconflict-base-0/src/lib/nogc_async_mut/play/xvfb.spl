"""
# No-GC async play Xvfb

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.play.xvfb.{
    is_linux, is_macos, is_windows, display_is_set,
    needs_xvfb, xvfb_run_available, wrap_cmd, maybe_wrap_cmd
}
