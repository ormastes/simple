"""
# No-GC Async I/O Runtime

Compatibility facade over the no-GC sync I/O runtime backend.
"""

export use std.nogc_sync_mut.io_runtime.{
    ShellResult,
    shell, shell_output, shell_output_trimmed, shell_bool,
    file_read, read_file, file_write, write_file, file_exists, file_delete,
    remove_file_if_exists, file_copy, file_read_bytes, file_size,
    file_size_raw, process_run, env_get, env_set, cwd, home, getpid,
    time_now_unix_micros, get_args, exit, dir_create, dir_create_all,
    mkdir_p, is_dir, is_file, list_dir, dir_remove, host_os, host_arch, read_file_text
}
