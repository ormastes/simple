"""
# No-GC Async Daemon SDK Types

Compatibility facade over the no-GC sync daemon SDK backend.
"""

export use std.nogc_sync_mut.daemon_sdk.types.{
    DAEMON_IDLE, DAEMON_POLLING, DAEMON_PROCESSING, DAEMON_SHUTTING_DOWN,
    DaemonConfig, DaemonState, DaemonRequest, DaemonResponse,
    daemon_config_new, daemon_state_new,
    daemon_request_new, daemon_response_new
}
