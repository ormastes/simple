"""
# No-GC Async Daemon SDK Lock

Compatibility facade over the no-GC sync daemon SDK backend.
"""

export use std.nogc_sync_mut.daemon_sdk.lock.{
    try_acquire_daemon_lock, release_daemon_lock, is_daemon_running,
    read_daemon_pid, is_pid_alive, lock_path_parent
}
