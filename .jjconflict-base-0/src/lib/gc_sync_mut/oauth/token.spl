@allow(star_import) # reason: compatibility facade, re-exports backing family symbols
# Compatibility facade: std.gc_sync_mut.oauth.token -> std.gc_async_mut.oauth.token
export use std.gc_async_mut.oauth.token.*
