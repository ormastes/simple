@allow(star_import) # reason: compatibility facade, re-exports backing family symbols
# Compatibility facade: std.gc_sync_mut.oidc.id_token -> std.gc_async_mut.oidc.id_token
export use std.gc_async_mut.oidc.id_token.*
