# Mission-Critical Infrastructure Hardening V2 — Wave 1 RED

Date: 2026-08-11
Status: OPEN (P2)
Status re-verified 2026-08-17 by source inspection (triage shard 02).
Owner: mission-critical-infra-hardening-v2 merge owner

## Exact evidence

- `test/01_unit/os/sosix/certified_manifest_spec.spl`: PASS, 11/11.
- `test/01_unit/compiler/mission_critical/compiler_admission_spec.spl`: FAIL,
  6/10. One fixture performs an unsupported complex indexed-field assignment.
  Three negative fixtures mutate hash-bound receipt fields but continue to
  expect a later semantic rejection; canonical receipt recomputation correctly
  rejects them first as `run_correlation_mismatch`.
- `test/01_unit/lib/common/mission_critical/draw_ir_generation_arena_v3_spec.spl`:
  FAIL, 3/8. Exact retirement does not persist the expected generation advance;
  active/admitted state is not observed by several scenarios; failed-seal abort
  and terminal advance scenarios consequently fail.

The deployed binary warns that it is Rust bootstrap-seed lineage, so these are
development diagnostics, not production pure-Simple release evidence.

## Required fixes

1. Compiler spec: replace the unsupported indexed-field mutation with a
   supported whole-fixture reconstruction. For tests intended to reach later
   validation stages, recompute `receipt_hash` and update the independently
   frozen run expectation; retain one mutation-without-rehash test proving the
   canonical hash catches tampering first.
2. DrawIR arena: isolate whether nested mutable method calls or match-arm
   binding causes `_advance_generation`/admission state loss. Add the smallest
   direct method reproducer, fix the pure-Simple owner path, then require exact
   capacity, active-generation refusal, forged-plan refusal, abort recovery,
   and terminal non-wrap scenarios to pass.
3. Replace the allocation profile rolling modulo fingerprint with canonical
   SHA-256; the current multiply may overflow before modulo.
4. Add canonical SimpleOS manifest serialization/content hash, artifact-level
   payload hashes, stress receipt identity, and reject unselected cells that
   retain PASS-like evidence.
5. Expand DrawIR-v3 from admission metadata to the designed packed storage,
   fixed slots/ring, encoding cursor/content hashes, immutable publication, and
   Engine2D queued handoff before claiming full REQ-MCI-005 coverage.
6. Move missing-receipt aggregate indexing into an explicit guarded branch;
   chained boolean evaluation must never evaluate `receipts[-1]`.

## Resume commands

After changing the corresponding owner, run each focused spec once:

```text
SIMPLE_TIMEOUT_SECONDS=180 bin/simple test test/01_unit/compiler/mission_critical/compiler_admission_spec.spl --mode=interpreter
SIMPLE_TIMEOUT_SECONDS=180 bin/simple test test/01_unit/lib/common/mission_critical/draw_ir_generation_arena_v3_spec.spl --mode=interpreter
SIMPLE_TIMEOUT_SECONDS=180 bin/simple test test/01_unit/lib/nogc_sync_mut/mission_critical/domain_arena_v1_spec.spl --mode=interpreter
SIMPLE_TIMEOUT_SECONDS=180 bin/simple test test/01_unit/lib/nogc_sync_mut/mission_critical/bounded_process_policy_spec.spl --mode=interpreter
SIMPLE_TIMEOUT_SECONDS=180 bin/simple test test/01_unit/lib/nogc_sync_mut/mission_critical/mci_evidence_manifest_v1_spec.spl --mode=interpreter
SIMPLE_TIMEOUT_SECONDS=180 bin/simple test test/03_system/infra/mission_critical_infra_hardening_v2_spec.spl --mode=interpreter
```

## Stale evidence producer map (2026-08-11)

The baseline's nine stale reports must be regenerated by their owning wrappers;
changing a report timestamp is not evidence.  Use the proven pure-Simple
candidate `release/x86_64-unknown-linux-gnu/simple` where `SIMPLE_BIN` is shown.
`bin/simple` and `bin/release/x86_64-unknown-linux-gnu/simple` currently identify
themselves as Rust bootstrap seeds and are forbidden for these claims.

| Stale report | Authoritative regeneration command | Current prerequisite / disposition |
|---|---|---|
| `shared_wm_renderer_unification_evidence_2026-07-05.md` | `SIMPLE_BIN=release/x86_64-unknown-linux-gnu/simple BUILD_DIR=build/shared_wm_renderer_unification_evidence_20260811 REPORT_PATH=doc/09_report/shared_wm_renderer_unification_evidence_$(date -u +%F).md sh scripts/check/check-shared-wm-renderer-unification-evidence.shs` | Proven non-seed Simple executable; wrapper checks and runs `shared_wm_renderer_unification_spec.spl` plus the host compositor source contract. |
| `cpu_simd_engine2d_evidence_current_2026-07-02.md` | `SIMPLE_BIN=release/x86_64-unknown-linux-gnu/simple BUILD_DIR=build/cpu-simd-engine2d-evidence-20260811 REPORT_PATH=doc/09_report/cpu_simd_engine2d_evidence_$(date -u +%F).md sh scripts/check/check-cpu-simd-engine2d-evidence.shs` | Candidate must export all three `rt_engine2d_simd_*_row_u32` symbols and pass the wrapper's interpreter smoke; `nm`, timeout support, and native architecture execution are required. |
| `simpleos_llvm_port_evidence_current_2026-07-02.md` | No single authoritative report wrapper exists. Resume with `SIMPLE_LIB=src release/x86_64-unknown-linux-gnu/simple run src/os/port/llvm/build.spl -- --cross --status`, then rerun the target-specific `src/os/port/llvm/build.shs`/`sysroot.shs` commands and the two integration specs enumerated in the report before producing a new dossier from retained logs. | Requires LLVM/Clang/CMake/Ninja host tools, target sysroots, substantial disk/time, and target-specific artifacts. A status-only run cannot refresh the dossier. Add a canonical producer before release. |
| `gui_renderdoc_feature_coverage_status_2026-07-03.md` | `BUILD_DIR=build/gui_renderdoc_feature_coverage_status_20260811 REPORT_PATH=doc/09_report/gui_renderdoc_feature_coverage_status_$(date -u +%F).md sh scripts/check/check-gui-renderdoc-feature-coverage-status.shs` | Non-launching aggregate; upstream RenderDoc, browser, production parity, and performance evidence must exist and be current. Fresh run on 2026-08-11 correctly failed `missing-behavior-evidence`; see `doc/09_report/gui_renderdoc_feature_coverage_status_2026-08-11.md`. |
| `layered_simple_gui_web_engine2d_bitmap_evidence_2026-07-05.md` | `SIMPLE_BIN=release/x86_64-unknown-linux-gnu/simple BUILD_DIR=build/layered_simple_gui_web_engine2d_bitmap_evidence_20260811 REPORT_PATH=doc/09_report/layered_simple_gui_web_engine2d_bitmap_evidence_$(date -u +%F).md sh scripts/check/check-layered-simple-gui-web-engine2d-bitmap-evidence.shs` | Non-seed Simple plus Node, Bun, Electron/browser tooling, current scene fixtures, and live bitmap comparisons; minimum 25 bitmap iterations. |
| `production_gui_web_renderer_parity_evidence_2026-06-29.md` | `SIMPLE_BIN=<provenanced-stage4-simple> BUILD_ROOT=build/production_gui_web_renderer_parity_evidence_20260811 REPORT_PATH=doc/09_report/production_gui_web_renderer_parity_evidence_$(date -u +%F).md sh scripts/check/check-production-gui-web-renderer-parity-evidence.shs` | Requires an explicit stage-4 candidate with a valid adjacent provenance receipt. Also requires fresh Electron matrix/layout, live Tauri+Chrome surface captures, device readback, font offload, event routing, and platform-specific Metal inputs; reused evidence is limited to 72 hours. Current release candidate has no adjacent provenance receipt. |
| `simple_web_engine2d_js_bitmap_evidence_2026-07-03.md` | `SIMPLE_BIN=release/x86_64-unknown-linux-gnu/simple JS_RENDER_RUNTIME=node BUILD_DIR=build/simple_web_engine2d_js_bitmap_evidence_20260811 REPORT_PATH=doc/09_report/simple_web_engine2d_js_bitmap_evidence_$(date -u +%F).md sh scripts/check/check-simple-web-engine2d-js-bitmap-evidence.shs` | Non-seed Simple, Node, canonical JS fixture, exact ARGB comparison, and performance trials. |
| `bun_simple_web_engine2d_js_bitmap_evidence_2026-07-05.md` | `SIMPLE_BIN=release/x86_64-unknown-linux-gnu/simple BUILD_DIR=build/bun_simple_web_engine2d_bitmap_evidence_20260811 REPORT_PATH=doc/09_report/bun_simple_web_engine2d_bitmap_evidence_$(date -u +%F).md sh scripts/check/check-bun-simple-web-engine2d-bitmap-evidence.shs` | Same exact bitmap contract as Node, with `bun` installed (currently `/home/ormastes/.bun/bin/bun`). |
| `qemu_gtk_wm_capture_evidence_2026-07-05.md` | `SIMPLE_BIN=release/x86_64-unknown-linux-gnu/simple QEMU_AUTO_LAUNCH_SIMPLEOS_DESKTOP=1 BUILD_DIR=build/qemu_gtk_wm_capture_evidence_20260811 REPORT_PATH=doc/09_report/qemu_gtk_wm_capture_evidence_$(date -u +%F).md sh scripts/check/check-qemu-gtk-wm-capture-evidence.shs` | Requires `qemu-system-x86_64`, a current bootable SimpleOS desktop image, QMP/live screendump, GUI SMF artifact contract, and guest-origin performance marker. Do not promote fake-QMP, host-only, or missing-guest rows. |

After all nine owners produce current, passing artifacts, rerun exactly once:

```sh
BUILD_DIR=build/simpleos_hardening_evidence_matrix_20260811_refresh \
REPORT_PATH=doc/09_report/simpleos_hardening_evidence_matrix_$(date -u +%F).md \
sh scripts/check/check-simpleos-hardening-evidence-matrix.shs
```

Do not rerun the already-green unchanged SimpleOS manifest spec.

## Host-independent storage-formal refresh — 2026-08-11

Exactly one ready host-independent evidence producer was run in this refresh:

```sh
sh scripts/check/check-simpleos-storage-formal-proofs.shs
```

Result: **PASS** (exit 0). The DB-storage, FAT32, and NVFS Lean projects built
and checked 5, 3, and 6 files respectively with zero trust bypasses, and the
wrapper ended with `STATUS: PASS simpleos-storage-formal-proofs`.

- Current report:
  `doc/09_report/simpleos_storage_formal_proofs_2026-08-11.md`
- Retained log:
  `build/evidence/mci-v2/formal-storage-20260811/storage_integrity_formal.log`
- Retained log SHA-256:
  `b3de3ccd35b170d5abebe4210d660178bf7682f0ada3cbd285ae59714991544a`
- Classification: the `formal_storage_integrity` row has current standalone
  PASS evidence. This does not refresh any of the nine static report owners,
  does not cover native storage execution, and does not justify rerunning or
  promoting the release aggregate.

## Allocation fault-injection increment — 2026-08-11

The arena now registers one-shot pre-cursor and pre-publication fault boundaries,
with typed allocation exhaustion, fail-before-publication behavior, saturating
telemetry, rollback, and cross-domain isolation coverage. The focused bootstrap-
seed interpreter run reached 8/9: both injected failures and all isolation/
rollback checks behaved as designed, but the newly appended telemetry field was
observed as zero instead of two. The field was moved into the stable telemetry
portion of the class layout after that run; this layout adjustment remains
unverified in this bounded session. This is development evidence only and does
not unblock external host, QEMU, hardware, or 24-hour certification rows.

## Host-independent memory-safety formal refresh — 2026-08-11

Exactly one additional ready host-independent evidence producer was run in
this refresh:

```sh
sh scripts/check/check-simpleos-memory-safety-formal-proofs.shs
```

Result: **PASS** (exit 0). GC reachability, GC/no-allocation boundary,
GC manual-borrow, manual-pointer-borrow, and no-GC compilation Lean projects
built and checked 1, 4, 2, 2, and 2 files respectively with zero trust
bypasses. The wrapper ended with
`STATUS: PASS simpleos-memory-safety-formal-proofs`.

- Current report:
  `doc/09_report/simpleos_memory_safety_formal_proofs_2026-08-11.md`
- Retained log:
  `build/evidence/mci-v2/formal-memory-20260811/memory_safety_formal.log`
- Retained log SHA-256:
  `c72caf9a01ae23fdac34560a452632170a0ee732fa4f6e281ce5968bcf7dd81b`
- Classification: the standalone `formal_memory_safety` modeled-property gate
  has current PASS evidence. This is not runtime/codegen correspondence,
  concurrent execution, QEMU/hardware, aggregate, or release evidence.

## Host-independent critical-concurrency formal refresh — 2026-08-11

Exactly one producer was executed once:
`sh scripts/check/check-simpleos-critical-formal-proofs.shs`. It passed with 85
required theorem declarations across five Lake projects and 14 Lean files, with
zero reported trust bypasses. The retained log SHA-256 is
`bb14f78dd57aba33f354cdbd76ab2edce74abff246d3e17be6a94f7b7407e27a`;
the receipt is under
`build/evidence/mission_critical_infra_hardening_v2/critical_concurrency_20260811/`.

Classification: this refresh advances only the formal scheduler, actor-channel,
DRF memory-model, and capability-model evidence. Model-to-implementation/native
runtime correspondence, deployed race freedom, QEMU/hardware, aggregate, and
release evidence remain unresolved.

## Host-independent CPU/SIMD refresh — 2026-08-11

Exactly one canonical producer was executed once:

```sh
SIMPLE_BIN=release/x86_64-unknown-linux-gnu/simple \
SIMPLE_BIN_SOURCE=mission-critical-explicit-release \
BUILD_DIR=build/cpu-simd-engine2d-evidence-20260811 \
REPORT_PATH=doc/09_report/cpu_simd_engine2d_evidence_2026-08-11.md \
sh scripts/check/check-cpu-simd-engine2d-evidence.shs
```

Result: **FAIL** (exit 1), `simple-bin-simd-smoke-failed`. The candidate passed
the producer's initial identity and required-symbol checks but the mandatory
interpreter smoke terminated with a segmentation fault. Consequently the
canonical bitmap workload and facade spec did not execute; native SIMD hits,
bit-exact parity, and all mismatch rows remain unproven.

- Report: `doc/09_report/cpu_simd_engine2d_evidence_2026-08-11.md`
- Hash-bound receipt and raw logs:
  `build/cpu-simd-engine2d-evidence-20260811/receipt.md`
- Candidate SHA-256:
  `04a38e21d6fbd86149d46d3ee2d761349f8ad29b02c5037a8eb589b6a1b9e4e0`
- Classification: current CPU/SIMD row remains RED. This result provides no
  GPU, QEMU, hardware, aggregate, or release evidence.

## Host-independent RISC-V dual-track refresh — 2026-08-11

Exactly one authoritative producer was executed once:
`sh scripts/check/check-riscv-formal-dual-track.shs`. It **FAILed closed**
(exit 1). The sidecar contract negative-control self-test first passed all eight
fixtures, then the production lane rejected `bin/simple` because it identifies
as a Rust bootstrap seed. BYL/Lean proof checking did not begin.

- Receipt:
  `build/evidence/mission_critical_infra_hardening_v2/riscv_dual_track_20260811/receipt.md`
- Log SHA-256:
  `7057820a95e1e794eeae410d89f184fe8b6234dbd2ab068c704bb78b380ce381`
- Selected compiler SHA-256:
  `df2da4952028ebbe3e89d0a2255d34c93e63522ad779d3444c5a0c82d3a0f5a0`
- Classification: current fail-closed compiler-admission evidence only. The
  RISC-V dual-track formal row remains RED. No RTL/SBY execution, QEMU, FPGA,
  hardware, aggregate, or release evidence is claimed.
- Unblock: rerun the same producer once with an exact-current admitted
  pure-Simple compiler selected by the canonical sidecar contract.

## Re-triage 2026-08-17 (m9a_tests lane) — SPLIT REQUIRED

This is a **three-spec umbrella** and cannot be closed or worked as one row.
The three subjects are independent and were located on disk:

1. `test/01_unit/compiler/mission_critical/compiler_admission_spec.spl`
2. `test/01_unit/lib/common/mission_critical/draw_ir_generation_arena_v3_spec.spl`
   (the "draw_ir arena v3" spec — the triage row did not carry its path; this
   is the only match for `*draw_ir*arena*` / `*arena_v3*` under `test/`)
3. `test/01_unit/os/sosix/certified_manifest_spec.spl`

All three files are present. A single umbrella verdict over three unrelated
specs is exactly the shape that lets one genuine RED hide behind two stale
ones, so this doc should be split into three rows before any of them is
actioned.

**Not reproduced from this lane** — see the parent reports Unproven list.
