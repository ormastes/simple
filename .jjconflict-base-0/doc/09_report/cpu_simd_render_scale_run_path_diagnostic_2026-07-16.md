# CPU SIMD Render Scale Run-Path Diagnostic

- Date: 2026-07-16
- Worktree: `/tmp/simple_simd_origin_rebased`
- Scope: focused 16x16 software row generated by the 4K/8K contract
- Status: resumed after the initial three-cycle cap

## Symptom

`cpu_simd_render_scale_contract_spec.spl` reached the executable scenario but
timed out after 120 seconds. The first nested wrapper row was
`4k_software`; no SDN output was produced.

The generated entry is retained at:

```text
build/check/cpu-simd-render-scale/tiny-run-diagnose/4k_software.entry.spl
```

It calls `software_only_render_loop_export_values` with the scalar `software`
backend, 16x16 pixels, 300 DPI, one warmup, and one sample. The wrapper invokes:

```sh
SIMPLE_LIB=src "$SIMPLE_BIN" run \
  build/check/cpu-simd-render-scale/tiny-run-diagnose/4k_software.entry.spl \
  --mode=native
```

## Root-Cause Chain

The process was CPU-bound while the interpreter loaded the broad renderer
closure. The initial retained run consumed approximately 1.47 GiB RSS. JIT
lowering failed first, so the full interpreter path was a consequence rather
than the primary defect.

### Cycle 1: Vulkan mutable receivers

Initial error:

```text
cannot modify self in immutable fn method 'VulkanSession.init_device'
```

Five methods in
`src/lib/nogc_sync_mut/gpu/engine2d/vulkan_session.spl` wrote session fields
while declared with immutable `fn` receivers. They now use `me`:

- `init_device`
- `create_command_pool`
- `create_pipeline_cache`
- `load_shader`
- `release`

Retained diagnostic:

```text
build/native_probe/cpu-simd-run-path/after-mutability.stderr
```

JIT then advanced to the DirectX backend.

### Cycle 2: DirectX mutable receipt

Next error:

```text
cannot modify self in immutable fn method 'DirectXBackend._native_execute_once'
```

`_native_execute_once` updates native receipt state and now uses `me`. The
public readback trait remains unchanged.

Retained diagnostic:

```text
build/native_probe/cpu-simd-run-path/after-directx.stderr
```

JIT then advanced to Arabic shaping.

### Cycle 3: font shaping capability and symbol collisions

Next error:

```text
mutation without mut capability while lowering selected_arabic_glyphs
```

The immutable `for glyph in working` binding was replaced by indexed mutation
through the mutable `working` array. The same compilation also reported
cross-module private helper collisions. The helpers now have module-owned
names:

- `_arabic_coverage_index`, `_arabic_single`
- `_devanagari_coverage_index`, `_devanagari_single`
- `_layout_coverage_index`

Retained diagnostic and resource evidence:

```text
build/native_probe/cpu-simd-run-path/after-font-fixes.stderr
build/native_probe/cpu-simd-run-path/after-font-fixes.time
```

Observed bounded result:

```text
exit=124
elapsed=20.17
user=17.46
sys=2.69
maxrss_kb=1473168
```

JIT advanced again and exposed the remaining error:

```text
Memory safety error [W1003]: mutable binding with shared type (found):
shared pointers cannot be reassigned; use val instead of var at 96:5
```

The matching source is
`src/lib/nogc_sync_mut/text_layout/font_renderer.spl` in
`_resolved_font_metric_cached`: `var found: ResolvedFontMetrics? = nil` is
reassigned from a shared cache entry.

## Resumed Cycle 1: shared font-metric cache value

`_resolved_font_metric_cached` now unlocks and returns the matched cache value
directly. The mutable shared optional was removed; the miss path unlocks and
returns `nil`. The focused source contract guards this capability fix.

The next bounded diagnostic advanced to `Engine2D._poison_vulkan_font_surface`,
where an immutable optional binding directly wrote
`VulkanBackend.completion_unknown`. `VulkanBackend.mark_completion_unknown()`
now owns that mutation; Engine2D retains the updated backend exactly as before.

The following diagnostic advanced to direct mutation of
`VulkanFontCompositeEvidence.fallback_pixels` through an immutable binding.
`clear_fallback_pixels()` now owns that transfer cleanup; the fallback buffer
is still released immediately after poisoning the Vulkan surface.

## Resumed Cycles 2-3: Vulkan capability boundaries

`Engine2D._poison_vulkan_font_surface` directly wrote
`VulkanBackend.completion_unknown` through an immutable optional binding.
`VulkanBackend.mark_completion_unknown()` now owns that transition.

The next diagnostic found the same pattern for
`VulkanFontCompositeEvidence.fallback_pixels`. The new
`clear_fallback_pixels()` method owns the transfer cleanup without changing
when the buffer is released.

Retained final diagnostic:

```text
build/native_probe/cpu-simd-run-path/after-evidence-clear.stderr
build/native_probe/cpu-simd-run-path/after-evidence-clear.time
```

Observed bounded result:

```text
exit=124
elapsed=20.07
user=15.74
sys=4.24
maxrss_kb=574764
jit_failure_markers=0
interpreter_fallback_markers=0
hir_error_markers=0
```

This is a material transition: RSS fell from approximately 1.47 GiB to
575 MiB and JIT reached code generation. The remaining diagnostic is:

```text
[CODEGEN-AMBIGUOUS-METHOD] in 'emu_draw_image_scaled_blend' bare method
'draw_image_blend' has 30 candidates
```

The owner is
`src/lib/gc_async_mut/gpu/engine2d/backend_emu_adv.spl`. Its `core` parameter
is typed as `RenderBackend`, but codegen still resolves the method by bare name.
The next cycle must use the repository's existing explicit trait-dispatch form
or otherwise qualify that receiver; it must not select one concrete backend.

## Test Hardening

The system spec no longer embeds `/home/ormastes/dev/pub/simple/bin/simple`.
Nested runs use `CPU_SIMD_RENDER_SCALE_TEST_SIMPLE_BIN`, with portable
`bin/simple` fallback. Source assertions guard every receiver and font-helper
fix above. The mirrored SPipe manual is updated.

## SIMD Evidence Unaffected

Before this run-path diagnosis:

- 4K fill improved from 47 ms to 20 ms.
- 8K fill improved from 215 ms to 79 ms.
- Copy measured 6 ms versus 6 ms at 4K and 22 ms versus 24 ms at 8K.
- Full buffers matched exactly.
- Exhaustive packed-ARGB color/transparency and span ABI checks passed.
- x86_64, AArch64, and RISC-V target binaries passed their canonical runners.
- GPU backend selection and implementations were not replaced by CPU paths.

## Resumed Cycles 4-6: Explicit backend routing

A same-function `RenderBackend` local did not disambiguate trait-object method
dispatch. The retained `after-typed-render-backend` diagnostic still reported
`emu_draw_image_scaled_blend`, then the three existing `Engine2D` blend calls,
and `Engine2D.draw_image_scaled`; RSS returned to 1.70 GiB after interpreter
fallback.

The scaled-blend helper now calls the existing free alpha-blend implementation.
That implementation moved unchanged from `backend_emu.spl` to
`backend_emu_adv.spl` and remains re-exported through its original module API.
The retained `after-free-blend-dispatch` diagnostic proves the helper ambiguity
is gone and advances to `Engine2D`; RSS was 568,824 KiB.

`Engine2D.draw_image_blend` now routes explicitly to the only distinct paths:

- Vulkan and OpenCL retain their accelerated implementations.
- Software, CPU, and DirectX retain their optimized concrete CPU implementations.
- Backends whose implementation already called `emu_draw_image_blend` use that
  same shared helper directly.

Software, CPU, and DirectX constructor paths retain their concrete backend in
an optional capability field. Vulkan scaled-image routing now uses the same
explicit optional extraction pattern as other checked GPU operations. No
backend is replaced by a CPU path.

Retained final diagnostic:

```text
build/native_probe/cpu-simd-run-path/after-engine2d-explicit-routing.stderr
build/native_probe/cpu-simd-run-path/after-engine2d-explicit-routing.time
```

Observed bounded result:

```text
exit=124
elapsed=20.09
user=16.27
sys=3.81
maxrss_kb=572568
codegen_ambiguous_method_markers=0
jit_failure_markers=1
interpreter_fallback_markers=1
```

All ambiguous-method diagnostics are gone. The sole remaining native blocker is:

```text
Cranelift JIT compile: Module error: unresolved external symbol 'rt_len'
would NULL-jump in JIT; deferring to interpreter
```

The three-cycle cap stopped this continuation before symbol-table work. The
diagnostic used a 45,716,912-byte CLI dated 2026-07-04. Current source already
registers `rt_len` in the static JIT provider and has a focused regression in
`src/compiler_rust/compiler/src/codegen/jit_tests.rs`; that fix landed on
2026-07-13. The isolated worktree's 42,477,824-byte pure-Simple CLI at
`release/x86_64-unknown-linux-gnu/simple` is dated 2026-07-15 and exposes both
`run` and `test`.

The next cycle must therefore rerun the tiny native entry once with the
isolated worktree release CLI. Only modify symbol registration if that current
binary reproduces the failure. Do not run the full SPipe contract while JIT
still falls back.

## Resumed Cycles 7-9: Current-source CLI qualification

The isolated 42,477,824-byte release CLI accepts `--help`, but both the old
`run <file>` command form and its documented direct `<file.spl>` form terminate
with `SIGSEGV` before command diagnostics:

```text
exit=139
elapsed=1.15-1.19
maxrss_kb=7168-7424
stdout_bytes=0
stderr=timeout: the monitored command dumped core
```

A current-source full CLI was then built once with the isolated Stage3 compiler
and a preserved cache:

```text
build/native_probe/simple-current
build/native_probe/cpu-simd-current-cli/full-cli-build.log
build/native_probe/cpu-simd-current-cli/full-cli-build.time
```

Build evidence:

```text
exit=0
elapsed=254.23
maxrss_kb=811440
1149 compiled, 0 cached, 0 failed
```

The linked 31 MiB CLI is not runnable: both `run <file>` and direct
`<file.spl>` reproduce the same immediate 7 MiB-RSS `SIGSEGV`. The build log
explains why the successful link is not production evidence: it generated 71
unresolved-symbol stubs, including startup-path/runtime helpers such as
`path_extension`, `path_filename`, `path_parent`, `panic`, and `val_kind`.

This supersedes the stale-`rt_len` hypothesis as the current blocker. The
current source has the `rt_len` registration, but no current pure-Simple CLI
can reach source execution. The next cycle must fix or eliminate the first
startup-reachable generated stub using the existing Stage3 cache, then rerun
only the tiny entry. The focused SPipe contract remains deferred until a
current CLI executes without a generated-stub crash.

## Resumed Cycles 10-12: Supported runtime lane and direct exporter

A bounded GDB run identified the startup null call exactly:

```text
#0 0x0
#1 io__cli_ops___cli_current_exe_path
#2 io__cli_ops___cli_driver_binary
#3 io___CliCommands__run_commands__cli_run_file
```

Disassembly showed the null target was `rt_path_absolute` inside
`_cli_resolve_symlink`. Source declarations and the Rust runtime implementation
already existed; the first current-source CLI had been linked without the
supported runtime archive. `rust-hosted` is intentionally rejected by current
tooling. Rebuilding with `--runtime-bundle core-c-bootstrap --runtime-path
src/compiler_rust/target/bootstrap` is the supported Stage4 lane.

Retained corrected build:

```text
build/native_probe/simple-current-core-c
build/native_probe/cpu-simd-current-cli/full-cli-core-c-build.log
build/native_probe/cpu-simd-current-cli/full-cli-core-c-build.time
```

Evidence:

```text
exit=0
elapsed=175.54
maxrss_kb=356304
2 compiled, 1147 cached, 0 failed
rt_path_absolute definitions=1
```

The corrected CLI no longer crashes and reaches HIR. It then exposed a
generated-wrapper import failure for
`software_only_render_loop_export_values`, even though that public function
exists in `backend_measurement_software_export.spl`. The wrapper was redundant:
the exporter is already an executable entrypoint with the required CLI options,
so direct invocation was tested as a simplification. That path forces the
current CLI to scan the broad `src/app` tree and did not reach the focused
exporter reliably; the experiment was reverted. The narrow generated wrapper
remains the canonical contract shape.

A 16x16 focused contract smoke proves both earlier blockers are gone, but the
first row now reaches a new runtime failure:

```text
cpu_simd_render_scale_contract_status=fail
cpu_simd_render_scale_contract_reason=4k_run_failed_exit_132
runtime error: field access on nil receiver
elapsed=2.74
maxrss_kb=90640
```

Retained evidence:

```text
build/native_probe/cpu-simd-run-path/current-cli-contract-smoke.stdout
build/native_probe/cpu-simd-run-path/current-cli-contract-smoke.stderr
build/check/cpu-simd-render-scale/current-cli-smoke/4k.err
```

The nil-receiver observation came from the reverted broad direct-entry
experiment. A zero-render bisect spent its full 30-second bound in unrelated
`src/app` parser scans, confirming that this is not a viable contract path.
Changing the narrow wrapper import from explicit item syntax to wildcard syntax
still failed immediately on the same public function name, so spelling/rename
is not the issue.

The current blocker is the self-hosted HIR import/export path failing to expose
the public function from the narrow wrapper. The next cycle must reproduce that
import with the smallest two-module fixture and fix the shared import owner.
Do not rerun the full-resolution contract until the 16x16 wrapper exits zero.

## Resumed cycles 13-15: minimal import fixture and HIR diagnostics

The import failure was reduced to two files under
`build/native_probe/import-fixture`: a provider exporting `pub fn answer()` and
an entry importing `provider.{answer}`. This removes Engine2D, SIMD, generated
wrapper, and broad application-tree behavior from the failing path.

The first current-source run trapped with `field access on nil receiver`.
GDB localized the trap to
`HirLowering_dot_lower_hir_expr`. The one-argument primitive-cast probe read
`call_args_t[0].has_name` directly before materializing a typed `CallArg`.
`src/compiler/20.hir/hir_lowering/expressions.spl` now indexes once into a typed
local before reading the field. A source regression was added to
`test/01_unit/compiler/hir/bootstrap_expr_args_source_spec.spl`.

After rebuilding, the fixture stopped trapping and reported the underlying
semantic failure cleanly: `unresolved name: answer`. Inspection of
`register_imported_symbol` found six optional `Dict.get()` reads of parser
struct values. These were changed to `has()` plus typed indexed reads in
`src/compiler/20.hir/hir_lowering/_Items/module_lowering.spl`, covering classes,
structs, enums, traits, functions, and constants. A semantic import regression
was added to `test/01_unit/compiler/hir/resolve_import_symbols_spec.spl`.

The retained current-source rebuild succeeded:

```text
build/native_probe/simple-current-core-c-importfix
3 compiled, 1146 cached, 0 failed
elapsed=146.80
maxrss_kb=382584
```

The minimal fixture still exits 1 after that change:

```text
HIR lowering error in build.native_probe.import-fixture.entry:
unresolved name: answer
```

Therefore the nil-receiver defect is fixed, but `Dict.get()` was not the sole
cause of public import registration. The three-cycle limit has been reached.
The next cycle must trace module-name lookup and imported-symbol insertion in
the minimal fixture before running either the 16x16 or full SIMD contract.
Retained stdout/stderr are
`build/native_probe/import-fixture/importfix.stdout` and
`build/native_probe/import-fixture/importfix.stderr`.

## Resumed cycles 16-18: external interpret closure and symbol display

Three small-model read-only reviews traced independent parts of the import
path. Two identified the decisive mismatch: the fixture imports the short
module name `provider`, while source collection keys the provider file as
`build.native_probe.import-fixture.provider`. Existing working imports either
use an exact dotted module path or have an explicit alias. The third review
confirmed that, once registration runs, `define` stores the local `answer`
binding and normal lookup can resolve it.

The first experiment replaced `modules_by_name.get()` with `has()` plus indexed
reads. It was non-causal because the requested short key was absent, and was
fully reverted. The existing entry-closure alias block in
`src/compiler/80.driver/driver.spl` already creates the required short key, but
was limited to AOT native builds. Direct CLI execution falls back to
`interpret_file` when no sibling driver exists; that path used
`CompileMode.Interpret`, loaded only the entry file, and never entered the
closure block.

The driver now reuses the existing closure resolver for one external direct
interpret input. No second resolver or import-specific fallback was added. A
source contract in
`test/01_unit/compiler/driver/native_build_cache_plumbing_spec.spl` keeps AOT
and direct interpretation on that shared path.

Retained builds:

```text
build/native_probe/simple-current-core-c-entryaliasfix
build/native_probe/simple-current-core-c-interpretclosurefix
build/native_probe/cpu-simd-current-cli/full-cli-entryaliasfix-build.{log,time}
build/native_probe/cpu-simd-current-cli/full-cli-interpretclosurefix-build.{log,time}
```

The final build succeeded with `3 compiled, 1146 cached, 0 failed`, elapsed
153.14 seconds, and 383000 KiB maximum RSS. The fixture no longer reports
`unresolved name: answer`; it reaches expression lowering and traps with a nil
receiver instead. GDB localizes the new failure after import lookup:

```text
#0 SymbolTable_dot_symbol_display_name
#1 HirLowering_dot_lower_hir_expr
#8 HirLowering_dot_lower_function
#10 HirLowering_dot_lower_parser_module_unstub
#11 CompilerDriver_dot_lower_and_check_impl
```

This proves external interpret closure loading and the short-module alias are
now active. The remaining blocker is the stored-symbol read in
`SymbolTable.symbol_display_name`, currently `self.symbols[raw]`, on the
self-hosted runtime. The three-cycle limit is reached, so no further mutation
or rebuild was attempted. Next cycle: reduce that read to the established
single-read optional idiom, rerun this fixture once, then resume the 16x16 SIMD
contract only after exit zero.

Retained evidence:

```text
build/native_probe/import-fixture/entryaliasfix.{stdout,stderr}
build/native_probe/import-fixture/interpretclosurefix.{stdout,stderr,gdb}
```

## Resumed cycles 19-21: nil argument, not SymbolTable storage

Two small-model read-only reviews checked the sole production caller of
`SymbolTable.symbol_display_name` and the direct regression surface. The first
working hypothesis was indexed optional corruption inside that method. A
current-source build with `self.symbols.get(raw)` still trapped at the identical
instruction. Changing the read-only method receiver from `me` to `fn` also
left the same frame and instruction unchanged. Both experiments were reverted.

Disassembly then showed the trap occurs before any dictionary access. The
generated function checks its `SymbolId` argument for nil at entry and emits
`field access on nil receiver` before destructuring it. A GDB breakpoint at
`SymbolTable_dot_symbol_display_name` captured:

```text
rdi = 0x248b181  # SymbolTable receiver
rsi = 0x0        # found_symbol argument
rdx = 0x240d161  # fallback text
```

Reserving symbol ID zero was tested as the final hypothesis, but the argument
remained nil even with `next_symbol_id` starting at one. That experiment and
its test edits were fully reverted. The branch is entered because the known
self-hosted `if val` defect can bind nil despite the explicit source guard; the
underlying lookup still returned nil.

The causal dirty change is now isolated in
`register_imported_symbol`: cycles 13-15 replaced the original six single-read
`Dict.get()` optionals with `has()` plus indexed parser-struct reads. The
external interpret closure fix now supplies the correct `provider` module, but
the changed registration path does not create the `answer` symbol. The next
cycle must restore the original single-read values in that shared function,
retain the existing explicit-import semantic regression, rebuild once, and
rerun the two-file fixture. Do not modify `SymbolTable` again unless new
evidence contradicts the entry-register capture.

Retained builds and diagnostics:

```text
build/native_probe/simple-current-core-c-symdisplayfix
build/native_probe/simple-current-core-c-symdisplayfn
build/native_probe/simple-current-core-c-symbolid1
build/native_probe/import-fixture/symdisplayfix.{stdout,stderr,gdb}
build/native_probe/import-fixture/symdisplayfn.{stdout,stderr,gdb}
build/native_probe/import-fixture/symbolid1.{stdout,stderr,gdb}
build/native_probe/import-fixture/symbolid1-entry-registers.gdb
```

## Resumed cycles 22-24: direct symbols clear HIR

The six imported parser-item reads were restored to their original single-read
`Dict.get()` implementation; `module_lowering.spl` now has no diff from the
base registration code. Breakpoints proved that `register_imported_symbol` is
reached with `provider`, `answer`, and local name `answer`, and that
`SymbolTable.define` runs first for `answer`, then for `main`, on the same
receiver.

The decisive retained ID-1 experiment showed `lookup("answer")` itself returns
a non-nil optional object. The nil appears only when the compiled `if val`
extracts its `SymbolId` payload. The previous explicit nil comparison therefore
cannot protect this path. Identifier lowering now uses the existing
`lookup_or_invalid()` API, eliminating optional extraction, and real SymbolTable
IDs start at one because `Dict.get()` cannot distinguish a stored integer zero
from nil. The secondary SymbolTable constructor in `compile_c_entry.spl` uses
the same invariant. The explicit-import regression now includes a local
`main()` that calls `answer()`, covering both consecutive root-scope bindings
and the identifier-lowering path.

Retained current-source build:

```text
build/native_probe/simple-current-core-c-directsymbol
build/native_probe/cpu-simd-current-cli/full-cli-directsymbol-build.log
build/native_probe/cpu-simd-current-cli/full-cli-directsymbol-build.time
4 compiled, 1145 cached, 0 failed
elapsed=165.01
maxrss_kb=383540
```

The minimal fixture still exits 132, but the failure boundary has moved past
HIR lowering into execution:

```text
#0 InterpreterBackendImpl_dot_eval_block
#1 InterpreterBackendImpl_dot_eval_expr
#2 InterpreterBackendImpl_dot_eval_block
#3 InterpreterBackendImpl_dot_call_hir_function
#4 InterpreterBackendImpl_dot_process_module
#5 InterpreterBackendImpl_dot_interpret_hir_module
#6 CompilerDriver_dot_interpret_pipeline
```

This is concrete progress: module aliasing, imported registration, identifier
lookup, HIR function lowering, and interpreter function dispatch all complete.
The next cycle must reduce the nil receiver in `eval_block` using this same
two-file fixture. Do not run the 16x16 SIMD wrapper until the fixture exits
zero. Retained evidence is
`build/native_probe/import-fixture/directsymbol.{stdout,stderr,gdb}`.

## Resumed cycles 25-27: conditional multi-field payload boundary

The post-HIR trap was reduced to the nested branch block in the fixture's
`if answer() == 42: 0 else: 1`. The call stack is outer function
`eval_block -> eval_expr(If) -> inner eval_block`. A breakpoint on the inner
call captured `rsi=3`, so the callee receives an immediate nil value instead
of a `HirBlock` and traps on its first field read.

Three existing-pattern experiments were tested: a typed `HirBlock` rebind at
the call site, copying all `If` payloads before recursively evaluating the
condition, and separately re-matching the original enum once per payload field
(the workaround used by three-field statement assignment lowering). Every
build still passed `rsi=3` to the inner block. All interpreter experiments were
reverted; `interpreter.spl` has no retained diff from these cycles.

The retained explicit-import regression keeps the exact conditional body, so
future fixes cover the complete fixture rather than only import lookup.

Follow-up disassembly corrected the initial field attribution. The true branch
uses a valid heap pointer held in `rbx`. The condition evaluates false, and the
else path calls the generic `Option.unwrap()` for `HirBlock?`; that unwrap
returns `3`. The invalid value is therefore the large-struct Optional payload,
not the direct second `If` field. This matches the historical enum
struct-payload extraction family. `hir_lowering/statements.spl` already avoids
the same compiled pattern-extraction bug by calling `rt_enum_payload`
directly. The next cycle must use that shared runtime extraction path for the
optional else block, then determine why `answer() == 42` selected else. Do not
add more typed rebinds.

Retained evidence:

```text
build/native_probe/simple-current-core-c-ifblocktyped
build/native_probe/simple-current-core-c-ifpayloadcopy
build/native_probe/simple-current-core-c-iffieldprojection
build/native_probe/import-fixture/ifblocktyped.{stdout,stderr,gdb}
build/native_probe/import-fixture/ifpayloadcopy.{stdout,stderr,gdb}
build/native_probe/import-fixture/iffieldprojection.{stdout,stderr,gdb}
build/native_probe/import-fixture/iffieldprojection-inner-block.gdb
build/native_probe/import-fixture/directsymbol-if-enum-memory.gdb
build/native_probe/import-fixture/directsymbol-hir-enum-new.gdb
```

## Resumed cycle 28: retained else-payload probe and root-cause boundary

The direct `rt_enum_payload(else_)` experiment was built into
`build/native_probe/simple-current-core-c-elsepayload` with the supported
`core-c-bootstrap` runtime. The build passed (`3 compiled, 1146 cached,
0 failed`; 158.26 s elapsed; 371944 KiB max RSS). A distinct retained fixture
run then failed:

- command output: `build/native_probe/cpu-simd-current-cli/elsepayload-retained.{stdout,stderr,rc}`
- exit status: `132` (`SIGILL`)
- diagnostic: `runtime error: field access on nil receiver`
- backtrace: `build/native_probe/cpu-simd-current-cli/elsepayload-retained.gdb`
- register/disassembly probe: `build/native_probe/cpu-simd-current-cli/elsepayload-registers.gdb`

The backtrace still enters `InterpreterBackendImpl.eval_block` from the false
arm of `HirExprKind.If`. The call-site disassembly shows the optional payload
extractor feeding the invalid block to `eval_block`, so the direct runtime
payload call is not a fix and has been reverted.

The larger root cause is now bounded: direct interpret closure loading resolves
the imported `answer` symbol, but `interpret_pipeline` executes each `HirModule`
independently and `process_module` creates a fresh `EvalContext` containing only
that module. User-function lookup searches only `ctx.module.functions`.
Consequently an imported function body is unavailable while evaluating the
entry module; the call cannot produce `42`, the condition takes the false arm,
and the existing large-struct `Option<HirBlock>` extraction defect then traps.
Correct execution requires a linked multi-module interpreter context (including
owner-module symbol maps), not another optional-payload workaround.

The current regression tests prove import symbol registration and driver source
closure plumbing only. They do not execute an imported call. No claim of an
end-to-end fixture pass is valid until a focused interpreted cross-module call
test exits zero. The retained `next_symbol_id: 1` invariant is separate: integer
zero stored in `Dict` is nil-like and cannot be distinguished by `get`.

Small-model review independently found the same evidence gap and the stale
report wording. This section supersedes earlier statements that the complete
fixture was covered or that all SymbolId changes were reverted.

## Resumed cycles 29-31: owner-aware interpreter registry rejected

Two small-model read-only reviews confirmed that no reusable HIR linker exists.
They identified `CompileContext.hir_modules` as the authoritative module set and
warned that flattening modules is invalid because each module allocates its own
`SymbolId` range. The proposed minimal design therefore kept modules separate,
resolved an imported symbol through `HirSymbol.defining_module`, and constructed
the callee `EvalContext` from the owning module's symbol map.

Three current-source binaries were retained:

- `simple-current-core-c-multimodule`: initial module registry, build PASS
- `simple-current-core-c-multimodule-owner`: owner-aware name resolution, build PASS
- `simple-current-core-c-multimodule-mut`: mutating (`me`) registry setup, build PASS

The owner-aware build completed with `3 compiled, 1146 cached, 0 failed` in
147.01 s (373596 KiB max RSS). Its fixture still exited `132`. The trace at
`build/native_probe/cpu-simd-current-cli/multimodule-owner-trace.{stdout,stderr,rc}`
proved that closure loading and all three HIR modules completed, but the
`answer` call missed the user-function fast path, fell through the unreliable
`Value.Function` payload path, evaluated the comparison false, and trapped in
the false branch. Changing registry setup from `fn` to mutating `me` produced a
clean rebuild (`3 compiled, 1146 cached, 0 failed`, 146.55 s, 380996 KiB max
RSS), but the retained fixture
`build/native_probe/cpu-simd-current-cli/multimodule-mut-fixture.*` still exited
`132` with the same nil-receiver diagnostic.

The complete registry experiment was reverted because it did not pass its
single required execution check. No interpreter, interpreter-call, closure
execution, or registry source change from these cycles remains. The retained
driver import-closure and symbol-table fixes remain independently justified,
but the end-to-end imported call is still unresolved. Per the mandatory
three-cycle cap, no fourth implementation attempt or SIMD contract run was
started in this turn.

## Resumed cycles 32-34: context-carried module set rejected

The next design removed mutable backend state and carried `[HirModule]` in
`EvalContext`, which already flows through every interpreted call. Three
current-source binaries compiled successfully:

- `simple-current-core-c-context-modules`: context-carried module set
- `simple-current-core-c-context-imports`: bootstrap HIR import preservation
- `simple-current-core-c-context-symbol-owner`: direct `NamedVar` SymbolId owner lookup

The first trace proved the module set survived (`modules=3`) but target
resolution fell back to the entry path. Preserving imports in bootstrap HIR did
not change the runtime read. The final variant used the `NamedVar` SymbolId
directly; its trace reported `id=8` but still selected the entry path because
the compiled symbol-table value scan did not recover `defining_module`.

All three fixture runs exited `132` with `field access on nil receiver` after
the imported call missed the user-function fast path and the comparison became
false. Authoritative traces:

- `build/native_probe/cpu-simd-current-cli/context-modules-fixture.*`
- `build/native_probe/cpu-simd-current-cli/context-imports-fixture.*`
- `build/native_probe/cpu-simd-current-cli/context-symbol-owner-fixture.*`

The small-model review additionally identified missing glob-import, shadowing,
alias, method/static-call, and callee-scope isolation coverage. Because the
minimal explicit-import fixture remained red, none of those broader changes
were attempted. The complete context/module/import experiment and its source
contract assertions were reverted. No unproven interpreter changes remain.

## Resumed cycles 35-37: direct native contract runner rejected

The contract requires a native SIMD binary, so this turn tested direct
`native-build --entry-closure` execution instead of the broken interpreter. A
fresh self-hosted CLI was built at
`build/native_probe/simple-current-native-contract-runner` (`2 compiled, 1147
cached, 0 failed`; 172.98 s; 357652 KiB max RSS).

Three retained 16x16 attempts failed before binary emission:

1. `cpu-simd-native-contract-16`: broad `--source src` parsed unrelated files.
2. `cpu-simd-native-contract-16-v2`: explicit app/lib roots still forced
   whole-library parsing.
3. `cpu-simd-native-contract-16-v3`: entry-only closure loading reached the
   transitive renderer and exposed the same source parser failure.

The authoritative final log is
`build/native_probe/cpu-simd-native-contract-16-v3/4k.native-build.log`. The
self-hosted parser loses the function body after this compact initializer:

```simple
fn css_bytes_find(cb: [i64], n: i32, ch: i64, from: i32) -> i32:
    var i = if from < 0: 0 else: from
    while i < n:
```

It reports `while` and following statements as unexpected class-body tokens at
`simple_web_html_layout_renderer.spl:4077`. The `src/lib` and `src/std` copies
are byte-identical. The next root fix is an ordinary initialization plus a
guarded normalization, followed by the entry-only native contract rerun.

The experimental runner was reverted because it never produced a binary.
Small-model review confirmed scheduling and flag compatibility and identified
strict gates for self-hosted provenance, native object format, clean build logs,
and linked SIMD externs. Per the mandatory three-cycle cap, the renderer source
edit was not started in this turn.

## Resumed cycles 38-40: renderer rewrite rejected; class-boundary parser repro

The compact initializer was first expanded to an ordinary assignment and
negative-value guard in the byte-identical `src/lib`/`src/std` renderer copies.
The exact entry-only native build still failed at the first function-body
statement, now the guard itself, proving the inline conditional was not the
cause. Retained evidence:

- `build/native_probe/cpu-simd-native-direct-after-compact/native-build.log`
- `build/native_probe/cpu-simd-native-direct-after-compact/native-build.rc`

A minimal fixture containing only the exact `Rules` and `RuleBuckets` class
fields followed by `css_bytes_find` reproduced the failure immediately. The
parser accepts the function declaration but reports its four-space-indented
body as `unexpected token in class body`. Renaming the `from` parameter to
`start` produced the identical result. Retained reducer evidence:

- `build/native_probe/rule-buckets-parser-fixture.log`
- `build/native_probe/rule-buckets-parser-fixture.rc`
- `build/native_probe/rule-buckets-parser-fixture-start.log`
- `build/native_probe/rule-buckets-parser-fixture-start.rc`

Both speculative renderer edits and the temporary fixture source were removed.
The failed native caches were also deleted; the small diagnostic logs remain.
The next root investigation belongs in class-member/dedent parsing, using the
reducer shape, rather than in renderer semantics. Per the three-cycle cap, no
fourth parser change or contract run was attempted.

## Resumed cycle 41: generic-close dedent root fixed

Four reduced fixtures isolated the class-boundary failure to a final generic
field type such as `Dict<text, i32>`. Scalar and nested-list fields did not
produce the class-body parser error. Two independent small-agent traces agreed
on the token path:

1. The closing generic `>` is represented by `TOK_GT`.
2. `token_requires_rhs(TOK_GT)` is intentionally true so a relational
   expression may continue on the next line.
3. `CoreLexer.handle_indentation` therefore suppressed the newline and dedent
   after the generic field.
4. The following top-level `fn` was consequently parsed as a class method, and
   its body statements were reported as unexpected class-body tokens.

Globally removing `TOK_GT` from `token_requires_rhs`, or making continuation
indentation-sensitive, would regress the established G27 trailing-operator
contract. The minimal context-aware fix instead has `parser_expect(TOK_GT)`
mark the already-confirmed generic close as complete before advancing the
lexer. All current `parser_expect(TOK_GT)` callers close generic type or type
parameter lists; comparison expressions do not use this path.

Regression coverage was added for both sides of the contract:

- a class ending in `Dict<text, i32>` dedents to a following top-level function;
- a relational `>` at end of line still continues to its right-hand side.

A fresh self-hosted CLI was built at
`build/native_probe/simple-current-parser-generic-close`: `5 compiled, 1144
cached, 0 failed`, 145.9 s total, no swap. The detailed build output, timing,
RSS, and exit status are retained under
`build/native_probe/cpu-simd-current-cli/parser-generic-close/`.

The fresh CLI's direct check reports the original `Dict` reducer as `OK`, with
evidence under `build/native_probe/class-dedent-dict-check/`. Its overall exit
status is 1 only because the post-check hygiene hook expects a `simple_seed`
sibling next to an isolated probe binary. The prior class-body parser errors
are absent.

Native-build and test-command execution remain independently blocked: the
minimal native-build process terminates with signal 11 before compiler output,
and the fresh CLI's test fallback attempts to execute
`src/app/test_runner_new/test_runner_client.spl` directly and receives
permission denied. The canonical release wrapper also terminates with signal
11 at startup. Evidence is retained under
`build/native_probe/class-dedent-dict-fixed/`,
`build/native_probe/parser-generic-close-test/`, and
`build/native_probe/parser-generic-close-release-test/`. These failures are
separate from the now-green parser check and still block the native 16x16 SIMD
contract probe.

The entry-only 16x16 renderer native build was then rerun once with the fixed
CLI. It passed the former renderer line-4077 failure and continued for 5:28,
proving the generic-close fix advances the real closure. It stopped later in
phase 2 while parsing `src/std/common/gpu/font_atlas_composite.spl`, where
embedded shader text leaked into parser tokens (`uint` and repeated synthetic
`Indent` errors), and finally at
`src/std/gc_async_mut/gpu/engine2d/draw_ir_adv.spl:587`, where the parser
rejected `var offscreen: Engine2D` without an initializer. Peak RSS was
2628856 KiB and no binary was emitted. Full output, timing, RSS, and status are
retained under
`build/native_probe/cpu-simd-native-contract-16-generic-close/`.

High-capability review required the generic regression to prove declaration
ownership rather than only absence of parser errors. The test now asserts that
the module has two top-level declarations and that the second is `read`. It
also covers `Dict<text, Option<i32>>`; the active `CoreLexer` emits adjacent
`TOK_GT` tokens rather than `TOK_SHR`, and a direct nested-generic reducer check
reports `OK`. Evidence is retained under
`build/native_probe/class-dedent-nested-generic-check/`; temporary reducer
sources were removed after capture.

The next valid-source blocker at `draw_ir_adv.spl:587` was fixed without a
placeholder engine or redundant surface allocation. The backend branches now
produce one `Result<Engine2D, text>`; an error returns immediately and the
successful value initializes `offscreen` through `unwrap()`. A direct check of
the updated source reports `OK`, with evidence under
`build/native_probe/draw-ir-offscreen-initializer-check/`. The overall isolated
probe command again exits 1 only in its post-check missing-seed hygiene hook.
The remaining native-build blocker is closure-parser state contamination in
otherwise standalone-green shader/string modules.

## Resumed cycles 42-44: speculative diagnostics fixed; late HIR move isolated

The shader diagnostics were not cross-module lexer leakage. During flat-AST
conversion, `flat_bridge_build_string_interps` probes every `{...}` region in a
string as a possible Simple interpolation. OpenCL, Metal, GLSL, CSS, and JSON
brace bodies therefore enter `flat_bridge_parse_interp_inner`; invalid
fragments correctly fall back to plain text, but their speculative parser
errors were printed before the sticky error flag was restored.

The parser now owns a nest-safe diagnostic suppression flag. The flat bridge
saves it, suppresses output only around the speculative fragment parse,
restores both the prior error flag and suppression state, and keeps the same
clean-expression/fallback decision. The existing interpolation sticky-error
spec now also checks that suppression is restored. Two independent small-agent
traces confirmed that full-module lexer/parser reset is correct and that this
probe is the sole source of the retained shader diagnostics.

A fresh self-hosted CLI was built at
`build/native_probe/simple-current-interp-probe-silent`: `7 compiled, 1142
cached, 0 failed`, 166.9 s total, no swap. Build output, timing, RSS, and status
are retained under
`build/native_probe/cpu-simd-current-cli/interp-probe-silent/`.

The exact entry-only 16x16 renderer build then passed the former phase-2
failures with zero parser diagnostics and ran for 8:21. It terminated later
with `runtime error: field access on nil receiver` / SIGILL (exit 132), used
3173652 KiB peak RSS, and emitted no binary. Evidence is retained under
`build/native_probe/cpu-simd-native-contract-16-interp-silent/`.

The built-in `SIMPLE_COMPILER_PHASE_PROFILE=1` path itself crashed at startup
with SIGSEGV, so it could not identify the late phase. A final normal-path GDB
run reproduced the late trap and captured this stack:

1. `HirLowering.try_register_local_struct_type`
2. `HirLowering.lower_hir_stmt`
3. `HirLowering.lower_hir_stmt_multi`
4. `HirLowering.lower_hir_block`
5. `HirLowering.lower_function`
6. `HirLowering.lower_module`

The disassembly proves the `init_expr` argument is nil at function entry. High
review of the caller assembly narrowed this to the initialized `Var` path: the
known-broken `if val vr_init_present = vr_init` binding explicitly produces a
nil payload before calling the registration helpers. The `Val` path passes its
initializer after `lower_hir_expr`, so initializer consumption is not the
cause. Reordering would also risk changing RHS shadowing/self-reference lookup.
The exact next fix is to keep the current order and replace that binding with
an explicit `if vr_init.?:` plus `val vr_init_present = vr_init ?? sk_dummy`,
matching the already-correct optional handling immediately above. GDB output
and disassembly are retained under
`build/native_probe/cpu-simd-native-contract-16-gdb/`.

High review also found that speculative diagnostic suppression currently
covers parser errors but not `parser_warn`; acceptance must extend the same
guard to warnings and verify that rejected brace fragments leave neither
warnings nor printed parser diagnostics.

Per the mandatory three-cycle cap, that HIR ordering change was not applied or
rerun in this turn.

## Resumed cycles 45-47: initialized Var fixed; Symbol optional payload isolated

High review corrected the prior tentative ordering diagnosis. The first GDB
trap came from the initialized `Var` branch's known-broken compact binding:
`if val vr_init_present = vr_init` produced nil before calling local
struct/tuple registration. It was replaced by the existing explicit optional
idiom (`if vr_init.?:` followed by `vr_init ?? sk_dummy`) without changing RHS
lowering or symbol-definition order. A source regression rejects the broken
form. Speculative parser suppression was also extended to `parser_warn`, and
the sticky regression now checks that no warning state remains.

A fresh CLI at `build/native_probe/simple-current-var-init-unwrap` built cleanly
(`4 compiled, 1145 cached, 0 failed`, 123.3 s, no swap). The 16x16 closure then
advanced past `try_register_local_struct_type` but trapped after 5:07 in
`SymbolTable.symbol_display_name`; its parser/warning diagnostic count remained
zero. Evidence is retained under
`build/native_probe/cpu-simd-native-contract-16-var-init-unwrap/` and the GDB
stack under `build/native_probe/cpu-simd-native-contract-16-var-init-gdb/`.

That second trap exposed the same compact optional-binding form around HIR
symbol lookups. Four exact `if val ... = found` sites in `hir_types.spl` and
bootstrap module lowering were changed to explicit presence checks and
`unwrap()`, with a source invariant rejecting both forms. A second fresh CLI at
`build/native_probe/simple-current-hir-found-unwrap` built cleanly (`4 compiled,
1145 cached, 0 failed`, 128.3 s, no swap).

The next 16x16 run still trapped after 5:11 in
`SymbolTable.symbol_display_name`, with zero parser/warning diagnostics and no
binary. GDB and disassembly show that the dictionary lookup passes
`rt_is_some`, but `found.unwrap()` yields nil and the subsequent `sym.name`
field read traps. This is no longer a pattern-binding failure; the next fix
must avoid the broken optional-struct payload extraction while preserving the
stored renamed symbol name used by cross-module closure emission. Evidence is
retained under `build/native_probe/cpu-simd-native-contract-16-hir-found-unwrap/`
and `build/native_probe/cpu-simd-native-contract-16-hir-found-gdb/`.

Per the mandatory three-cycle cap, no further SymbolTable change or contract
rerun was started in this turn.

High review resolved the next implementation choice: native `rt_index_get`
returns the stored `HirSymbol` pointer directly (or nil), not a wrapped
`Some(Symbol)`. The generated `found.?` check accepts that non-nil pointer, but
`Option.unwrap` then misinterprets it and returns nil. The minimal next change
is therefore a typed direct narrowing (`val sym: Symbol = found` and
`var renamed: Symbol = found`) after the existing presence check. The two
bootstrap `lookup()` results remain true `Some(SymbolId)` values and should keep
their explicit unwraps. A behavioral regression must define, rename, and read
back a qualified symbol name; source-text exclusion alone is insufficient.

## Resumed cycles 48-50: Symbol narrowing fixed; later HIR nil remains

The dictionary-backed `HirSymbol` sites now use typed direct narrowing after
their existing presence checks. A behavioral regression defines a symbol,
renames it, and verifies the qualified display name. The bootstrap `lookup()`
sites that return genuine `Some(SymbolId)` values retain explicit optional
unwrapping.

A fresh self-hosted CLI at
`build/native_probe/simple-current-symbol-direct-narrow` built cleanly (`3
compiled, 1146 cached, 0 failed`). Its GDB contract run advanced beyond
`SymbolTable.symbol_display_name` and trapped later in
`HirLowering.lower_hir_expr`. Disassembly mapped the new trap to `.id` on the
nil payload produced by compact optional binding of `fld_base_sym_opt`. The
same invalid SymbolId binding form was removed from field lowering, initialized
variable tuple destructuring, async future lookup, and bootstrap module lookup;
each now performs a presence check followed by explicit unwrap.

The resulting CLI at
`build/native_probe/simple-current-hir-symbolid-unwrap` built cleanly (`5
compiled, 1144 cached, 0 failed`, 118.8 s, no swap). The final allowed 16x16
contract run still terminated with `runtime error: field access on nil
receiver` / SIGILL (exit 132) after 6:15.80. It used 3177480 KiB peak RSS,
performed no swaps, emitted no parser output and produced no fixture binary.
The compiler progressed beyond both previously proven HIR traps, but this run
did not produce a core dump or stack identifying the later nil receiver.
Complete output, timing, status, and cache evidence are retained under
`build/native_probe/cpu-simd-native-contract-16-hir-symbolid-unwrap/`.

This is the third verification/fix cycle for the resumed lane. Per the hard
iteration cap, no speculative fourth patch or duplicate native build was
started. The next session must begin by obtaining a stack for this later trap,
then fix only the proven receiver before repeating the 16x16 contract. Full
4K/8K and cross-architecture acceptance remains blocked on that contract.

## Resumed cycles 51-53: facade imports and imported return inference

The first diagnostic rerun used `SIMPLEOS_FOCUSED_HIR_DIAG=1` and retained the
messages that preceded the former formatter trap. The first fatal message was
`unresolved name: get_args`; later messages included `reset_simd_hits` and
`simd_hit_counts`. All three were imported by
`backend_measurement_software_export.spl` through facade-only modules whose
transitive re-exports are not registered by the entry-closure HIR importer.
The exporter now imports the canonical `nogc_sync_mut` owners directly. This
preserves the singleton SIMD counters and does not initialize or select a GPU.

GDB proved the prior SIGILL occurred in `_format_hir_lowering_error` while
reading a nil `LoweringError.span`. The first guard used `and`; high review
correctly rejected it because Simple evaluates both boolean operands eagerly.
The formatter now uses separate control flow, typed direct narrowing of the
present span, and an unlocated fallback message. The first rebuilt CLI passed
(`3 compiled, 1146 cached, 0 failed`), but its closure run changed the failure
to SIGSEGV after 4:59.42, with 3191540 KiB peak RSS and no swap. Evidence is in
`build/native_probe/cpu-simd-native-contract-16-import-owner-span-guard/`.

After rebuilding with the separated guard (`3 compiled, 1146 cached, 0
failed`), GDB located that SIGSEGV in
`HirLowering.scan_ast_block_for_returns`, called by
`infer_untyped_return_type` from `register_imported_symbol`. The only newly
direct-imported function lacking a return annotation was `reset_simd_hits`.
Its canonical definition now declares the established unit type `-> ()`, so
import registration does not scan its body to infer a return type. GDB evidence
is retained under
`build/native_probe/cpu-simd-native-contract-16-import-owner-safe-span-gdb/`.

The final CLI again built cleanly (`3 compiled, 1146 cached, 0 failed`, 2:22.92,
367528 KiB peak RSS, no swap). The final allowed 16x16 closure run advanced
beyond the return scanner, then terminated later with `runtime error: field
access on nil receiver` / SIGILL after 5:21.17. It used 3191192 KiB peak RSS,
performed no swaps, and emitted no fixture. Evidence is retained under
`build/native_probe/cpu-simd-native-contract-16-import-owner-explicit-unit/`.

Per the mandatory three-cycle cap, no fourth GDB run, patch, or native build
was started. The next session must capture the later SIGILL stack from the
`simple-current-import-owner-explicit-unit` CLI before editing. Full 4K/8K and
x86/AArch64/RISC-V acceptance remains blocked on a successful 16x16 closure.

## Resumed cycles 54-56: span-free HIR diagnostics expose local return scan

GDB first confirmed that the `LoweringError.span` payload remains nil after the
separated formatter guard. High review selected the existing flat diagnostic
representation: `LoweringError` now stores a `location: text` before its nested
span, and the driver formats only `err.location` plus `err.message`. A focused
native fixture containing `nosuchfn(1)` exits normally with code 1 and reports
`HIR lowering error ... unresolved name: nosuchfn` instead of SIGILL.

The first full closure run with this layout advanced to 7:10.73 before another
nil-receiver SIGILL, using 3189456 KiB peak RSS and no swap. GDB proved that
this later trap was inside `HirLowering.error` itself: the caller supplied a
nil `Expr.span`, so computing the flattened location from `span.file` was also
unsafe. The flat bridge does not provide meaningful file/line data on this
path, so HIR errors now retain the original span field for API compatibility
but set the flat location to empty without dereferencing the unavailable span.
The tiny unresolved-name fixture again exits cleanly with code 1.

The span-free CLI built cleanly (`3 compiled, 1146 cached, 0 failed`, 2:35.06,
368412 KiB peak RSS, no swap). Its 16x16 closure still terminated with a
nil-receiver SIGILL after 5:21.02, using 3192852 KiB peak RSS and no swap. The
final GDB run identified the remaining owner:

1. `HirLowering.scan_ast_block_for_returns`
2. `HirLowering.scan_ast_expr_for_returns`
3. `HirLowering.scan_ast_stmt_for_returns`
4. `HirLowering.scan_ast_block_for_returns`
5. `HirLowering.infer_untyped_return_type`
6. `HirLowering.declare_module_symbols`
7. `HirLowering.lower_module`

The scanner receives a nil local function block while predeclaring an untyped
function. This is distinct from the imported `reset_simd_hits` inference path
fixed previously. Any fix changes general untyped-return inference semantics,
so no speculative fallback-to-unit patch was made in the third cycle. Evidence
is retained under
`build/native_probe/cpu-simd-native-contract-16-span-free-hir-errors-gdb/`;
the next session must identify the exact local function and preserve inferred
value returns while avoiding scans of malformed/absent blocks.

Per the mandatory three-cycle cap, no fourth patch or build was started. Full
4K/8K and x86/AArch64/RISC-V acceptance remains blocked on the 16x16 closure.

## Resumed cycles 57-59: missing spans removed; unsafe-block payload isolated

The nested `LoweringError.span` remained nil after array storage even with
separated formatter control flow. High review selected the existing flat
diagnostic representation: `LoweringError` now carries a `location: text`
before its retained span field, and the driver formats only the flat location
and message. A focused native `nosuchfn(1)` fixture exits normally with code 1
and an unresolved-name diagnostic instead of SIGILL.

The first full closure then advanced to 7:10.73 before SIGILL. GDB proved that
some `Expr.span` values are already nil when `HirLowering.error` is called, so
producer-side `span.file` access was unsafe too. Because the flat bridge does
not supply meaningful source locations on this path, HIR errors now retain the
span field for API compatibility but use an empty flat location without
dereferencing it. The focused diagnostic fixture still exits normally.

The span-free CLI built cleanly (`3 compiled, 1146 cached, 0 failed`, 2:35.06,
368412 KiB peak RSS, no swap). Its full closure terminated after 5:21.02 with
SIGILL, 3192852 KiB peak RSS, and no swap. The final GDB stack returned to the
untyped-return scanner:

1. `scan_ast_block_for_returns`
2. `scan_ast_expr_for_returns`
3. `scan_ast_stmt_for_returns`
4. `scan_ast_block_for_returns`
5. `infer_untyped_return_type`
6. `declare_module_symbols`

Caller disassembly at return address `0xb8f290` initially appeared to map the
nil block argument to the fourth consecutive direct block-payload branch in
source order. That interpretation was provisional and was disproved in the
next resumed cycle by inspecting the dispatch prologue: the branch at
`0xb8f269` is reached by a constant-true test, with no discriminant check.
Evidence is retained under
`build/native_probe/cpu-simd-native-contract-16-span-free-hir-errors-gdb/`,
including `scan-expr-caller.log`.

The actual defect is the compiler's documented struct-shadowed variant-pattern
hazard: `ExprKind.Block` shares its name with `struct Block`, so the bootstrap
compiler lowers that case as an irrefutable struct pattern and then extracts an
enum payload from whichever later expression reached it. For a non-Block
variant the extraction returns nil and the scanner traps when reading
`b.stmts`. The unsafe/loss/nograd flat-AST gaps remain real, but they are not
the cause of this crash. A blanket nil guard or Unit fallback would hide the
bad dispatch and inferred value returns, so the next cycle must predispatch
`Block` by its runtime discriminant, matching the established repair in
`expressions.spl`.

## Resumed cycles 60-62: return-scan dispatch repaired and logged

Full disassembly of `scan_ast_expr_for_returns` corrected the previous branch
mapping. Its dispatch prologue jumps to `0xb8f269` from a literal true
condition; that target calls `rt_enum_payload` and passes the result directly
to `scan_ast_block_for_returns`. This is the documented bootstrap seed hazard
for a variant whose name also names a struct. The existing expression lowerer
already avoids it by predispatching `ExprKind.Block` using runtime discriminant
`1138084884` (`hash("Block")`). The return scanner now does the same and keeps
the shadowed arm out of its general match.

Detailed, opt-in logging was added through the existing
`hir_module_phase_probe` gate. It reports each untyped function scan as
`return-scan:function:<name>` and reports entry into the explicitly dispatched
`Block` and `Field` branches. `SIMPLE_BOOTSTRAP_DIAG=1` is the usable narrow
gate. `SIMPLE_COMPILER_PHASE_PROFILE=1` is not usable for this evidence: it
segfaults independently at address zero in
`driver_log_helpers._phase_elapsed_ms`, before HIR lowering. The GDB proof is
stored in
`build/native_probe/cpu-simd-native-contract-16-block-predispatch-gdb/gdb.log`.

The first updated pure-Simple CLI built successfully with `3 compiled, 1146
cached, 0 failed` in 2:25.38 at 380940 KiB peak RSS. Its strict 16x16 closure
reached `_web_budget_begin` at 5:23.13 and then trapped on a nil field receiver.
High-capability review identified the paired hazard: `ExprKind.Field` is also
shadowed by `struct Field`; the established expression lowerer predispatches
both names. The scanner now predispatches `Field` with discriminant `21232742`
and recursively scans only its base expression.

The second updated CLI also built successfully (`3 compiled, 1146 cached, 0
failed`, 2:30.49, 383808 KiB peak RSS). The final strict closure advanced past
the previous `_web_budget_begin` point and logged the next function,
`fb_put`, before another `runtime error: field access on nil receiver` at
5:14.06 and 3192448 KiB peak RSS. No `return-scan:ExprKind.Block` or
`return-scan:ExprKind.Field` entry immediately preceded that failure, so the
remaining nil receiver is a later scanner/lowering access and is not evidence
that either repaired payload path regressed. Logs are retained under:

- `build/native_probe/cpu-simd-current-cli/block-predispatch/`
- `build/native_probe/cpu-simd-native-contract-16-block-predispatch/`
- `build/native_probe/cpu-simd-current-cli/block-field-predispatch/`
- `build/native_probe/cpu-simd-native-contract-16-block-field-predispatch/`

The source regression records both explicit discriminants and both opt-in log
markers. The normal `test` frontend remains unavailable in this worktree: it
attempts to execute `src/app/test_runner_new/test_runner_client.spl` directly
and exits 126 with permission denied. The two cache-backed CLI builds are the
successful compiler checks; the strict closure remains the behavioral failing
gate. Per the mandatory three-cycle limit, no fourth diagnostic/fix attempt was
started. Full 4K/8K and x86/AArch64/RISC-V acceptance therefore remains blocked
on the 16x16 closure.

## Resumed cycles 63-65: `fb_put` reduced to a one-second reproducer

GDB on the final cycle-62 CLI located the remaining trap at the entry of
`scan_ast_expr_for_returns`: the caller's `ExprKind.If` arm passed a nil
expression, and the callee trapped while reading `e.kind`. Caller disassembly
proved the sequence was the If scanner (`condition`, then block, optional else)
and that payload slot zero was loaded through `rt_enum_payload` plus
`rt_index_get` before the recursive call. The full stack and instruction
evidence is retained under
`build/native_probe/cpu-simd-native-contract-16-block-field-predispatch-gdb/`.

A small-model collision inventory confirmed `Block` and `Field` are the only
ExprKind names colliding with imported struct names; both predispatch repairs
are present. It also reduced the failing function to ordinary `If`, `Binary`,
`Index`, identifier, assignment, and bare-return shapes. The exact source body
is now preserved as `build/native_probe/return-scan-fb-put-fixture.spl`.
Native-building this fixture reproduces the failure in about one second and
roughly 11 MiB RSS, replacing the previous 5-6 minute, 3.19 GiB diagnostic
loop.

Typed local rebinds for the If payload compiled successfully in a new
pure-Simple CLI (`3 compiled, 1146 cached, 0 failed`, 2:29.69, 382172 KiB peak
RSS), but the full strict closure still trapped immediately after the new
`return-scan:ExprKind.If` marker. It reached that point after 6:31.63 rather
than the previous 5:14 failure, with 3192144 KiB peak RSS.

Because return-type inference only needs returns in the then/else bodies, the
condition recursion was removed and the pattern changed to
`ExprKind.If(_, then_, else_)`, avoiding intentional condition extraction.
That CLI also built cleanly (`3 compiled, 1146 cached, 0 failed`, 2:49.78,
376148 KiB peak RSS), but the one-second fixture still traps after the If
marker. Therefore at least one remaining then/else payload access is malformed;
the condition was not the sole invalid slot. The strict full closure was not
rerun because the focused behavioral gate already failed.

Evidence directories:

- `build/native_probe/cpu-simd-current-cli/if-payload-rebind/`
- `build/native_probe/cpu-simd-native-contract-16-if-payload-rebind/`
- `build/native_probe/return-scan-fb-put-current/`
- `build/native_probe/return-scan-fb-put-current-gdb/`
- `build/native_probe/cpu-simd-current-cli/if-body-only-scan/`
- `build/native_probe/return-scan-fb-put-if-body-only/`

Per the mandatory three-cycle cap, no further source patch or behavioral retry
was started. The new seconds-scale fixture is the first gate for the next turn;
the 16x16/full-resolution/architecture matrix remains pending until it passes.

High-capability review then identified the exact remaining owner without
another build: `scan_ast_stmt_for_returns` begins its broad match with
`StmtKind.Expr(expr)`, while `struct Expr` is imported. This is the same
struct-shadowed variant-pattern defect already proven for `Block` and `Field`.
The seed treats a nested bare `StmtKind.Return(nil)` as the always-true `Expr`
arm, extracts its nil payload, and calls `scan_ast_expr_for_returns(nil, ...)`.
That explains why the failure occurs after entering the valid If then block.
The next patch must predispatch `StmtKind.Expr` by runtime discriminant, remove
it from the broad match, and restore condition scanning in the If arm; the
current body-only If change is diagnostic and not acceptable as the final
semantics. The one-second fixture is the required behavioral regression.

## Resumed cycles 66-68: scanner fixed; failure advanced into MIR prescan

The reviewed `StmtKind.Expr` root cause was repaired by runtime-discriminant
predispatch in `scan_ast_stmt_for_returns`; the shadowed arm is no longer in
the broad match, and extracted payloads are explicitly rebound to `Expr`.
Full If condition scanning was restored. The rebuilt pure-Simple CLI completed
with `3 compiled, 1146 cached, 0 failed` in 2:25.33 at 379996 KiB peak RSS.

The one-second `fb_put` fixture then logged both statement-expression entries,
entered the If scanner, and completed the HIR declaration pass. It next trapped
in `SymbolTable.get_symbol` while lowering `fb_put`: the remaining compact
`if val found_symbol = fn_symbol_lookup` in `lower_function` produced a nil
symbol id. Replacing it with the established `.?, unwrap()` pattern built a
clean CLI (`3 compiled, 1146 cached, 0 failed`, 2:33.52, 375552 KiB peak RSS)
and advanced the fixture through `phase3:hir:function:done ...fb_put`.

The next GDB stack moved entirely into MIR:

1. `MirLowering.prescan_expr_for_struct_types`
2. `MirLowering.prescan_stmt_for_struct_types`
3. `MirLowering.prescan_block_for_struct_types`
4. `MirLowering.lower_module`

Because `HirStmtKind.Expr` is shadowed by `HirExpr`, the MIR struct-type
prescan was given the same runtime-discriminant predispatch, explicit
`HirExpr` rebind, and opt-in `[mir-prescan] HirStmtKind.Expr` marker. That CLI
also built cleanly (`3 compiled, 1146 cached, 0 failed`, 2:34.19, 378632 KiB
peak RSS), but the focused fixture still segfaulted after HIR completion and
did not emit the MIR marker. Therefore this final patch has not yet proved it
owns the active MIR fault; the strict closure was not rerun.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/stmt-expr-predispatch/`
- `build/native_probe/return-scan-fb-put-stmt-expr-predispatch-gdb/`
- `build/native_probe/cpu-simd-current-cli/function-symbol-unwrap/`
- `build/native_probe/return-scan-fb-put-function-symbol-unwrap-gdb/`
- `build/native_probe/cpu-simd-current-cli/mir-stmt-expr-predispatch/`
- `build/native_probe/return-scan-fb-put-mir-stmt-expr-predispatch/`

Per the three-cycle cap, no fourth patch, GDB run, or full closure was started.

Final high-capability review found two concrete defects in the unverified MIR
patch. First, its hardcoded `hash("Expr")` value does not match the existing
HIR-statement runtime discriminator evidence (`823890719`); the expected tag
must be derived from a constructed dummy variant instead of duplicated as a
magic number. Second, `prescan_expr_for_struct_types` still keeps shadowed
`HirExprKind.Block` in its broad match before `If`, reproducing the same
always-true struct-pattern hazard one level deeper. The established MIR
expression dispatcher already predispatches Block safely and should be reused
as the model. The absent marker is not proof that statement predispatch was
skipped because it used buffered `print`; crash diagnostics must use `eprint`.
These reviewed corrections are the next turn's first patch, followed by the
one-second fixture.

## Resumed cycles 69-71: MIR prescan fixed; real lowering reaches nested Binary

The MIR prescan patch now derives both tags from constructed, typed HIR values:
`HirStmtKind.Expr(nil_expr)` and `HirExprKind.Block(empty_hir_block)`. The
shadowed Block arm was removed from the broad expression match, and crash-path
markers use `eprint`. Its pure-Simple CLI built cleanly (`3 compiled, 1146
cached, 0 failed`, 2:37.53, 378928 KiB peak RSS). The focused fixture emitted
`[mir-prescan] HirStmtKind.Expr`, proving the derived statement dispatch, then
advanced into real MIR function lowering.

The next fault occurred at `lower_function:param-map-start fb_put count=6`.
`HirParam.symbol` is a required `SymbolId`, but the loop incorrectly consumed
it through compact optional binding. Replacing that branch with a typed direct
assignment and local-map insertion built cleanly (`3 compiled, 1146 cached, 0
failed`, 2:32.37, 382896 KiB peak RSS). The fixture then completed parameter
mapping, entered the body, and failed on statement zero.

GDB showed the body failure in four recursive `MirLowering.lower_expr` frames
under `lower_if`, matching the four-level `or`/comparison Binary tree in
`fb_put`. The Binary arm was given explicit `HirBinOp` and `HirExpr` rebinds,
and all operator/recursive uses were routed through those typed locals. That
CLI also built cleanly (`3 compiled, 1146 cached, 0 failed`, 2:34.63, 406104
KiB peak RSS), but the one-second fixture failed at the same body statement;
typed rebinds alone did not move the active fault.

Evidence directories:

- `build/native_probe/cpu-simd-current-cli/mir-derived-dispatch/`
- `build/native_probe/return-scan-fb-put-mir-derived-dispatch-gdb/`
- `build/native_probe/cpu-simd-current-cli/required-param-symbol/`
- `build/native_probe/return-scan-fb-put-required-param-symbol-gdb/`
- `build/native_probe/cpu-simd-current-cli/binary-payload-rebind/`
- `build/native_probe/return-scan-fb-put-binary-payload-rebind/`

The three-cycle cap was reached, so no Binary predispatch or further full
closure was started. Small-model PC mapping and high-capability patch review
were launched in parallel against the retained one-second reproducer.

## Resumed cycles 72-74: Binary and If pass; assignment payload remains

Small-model PC mapping and high-capability review agreed that Binary dispatch
was valid. The trap was the first compact `if val` around
`local_mir_type_of(left_local)`, which bound nil and dereferenced `.kind`.
All six sibling local-type probes in the Binary arm now use explicit `.?` plus
`unwrap()`, and the ineffective typed-Binary rebind experiment was removed.
The rebuilt CLI completed with `3 compiled, 1146 cached, 0 failed` in 2:28.37
at 399656 KiB peak RSS. The fixture then lowered the complete nested If
condition and then block and advanced to body statement one.

GDB mapped statement one's first fault to `lower_expr` entered from real
statement lowering. The Index arm received typed `base`/`index` locals and its
four compact option consumers were converted to explicit checks. That CLI
built cleanly (`3 compiled, 1146 cached, 0 failed`, 2:25.74, 411772 KiB peak
RSS), but the fixture remained at statement one, proving the nil was supplied
before Index processing.

The canonical MIR statement lowerer still began its broad match with shadowed
`HirStmtKind.Expr`; an Assign statement could therefore be misread as Expr.
Expr now uses a constructed-discriminant predispatch beside the existing Let
predispatch and is absent from the broad match. The CLI again built cleanly
(`3 compiled, 1146 cached, 0 failed`, 2:25.19, 374924 KiB peak RSS), but the
fixture still failed at statement one. The remaining active path is therefore
the broad `HirStmtKind.Assign` payload extraction (target/op/value), not the
removed Expr arm or the hardened Index internals.

Evidence directories:

- `build/native_probe/cpu-simd-current-cli/binary-option-unwrap/`
- `build/native_probe/return-scan-fb-put-binary-option-unwrap-gdb/`
- `build/native_probe/cpu-simd-current-cli/index-payload-options/`
- `build/native_probe/return-scan-fb-put-index-payload-options-gdb/`
- `build/native_probe/cpu-simd-current-cli/mir-real-stmt-expr-predispatch/`
- `build/native_probe/return-scan-fb-put-mir-real-stmt-expr-predispatch/`

The three-cycle cap prevented another assignment-payload patch or full closure
run. High-capability final review was requested against this exact boundary.

Final review mapped the remaining nil to `HirStmtKind.Assign` payload slot two,
`value`, before the target Index is consumed. Plain assignment deliberately has
a nil middle `op`, and the seed's broad three-field extraction passes nil
instead of the expected `NamedVar(color)` value. The next patch must derive the
Assign discriminator from `HirStmtKind.Assign(fallback_expr, nil,
fallback_expr)`, extract target/op/value in separate qualified matches, type
target and value explicitly, lower the value, call `lower_assign`, return, and
remove Assign from the broad match. Review also rejected the current Index
typed/options experiment as non-causal; it should be removed. The Binary option
fixes and real Expr predispatch are proven and remain.

## Resumed cycles 75-77: assignment/index lowering completes; diagnostics formatter remains

The reviewed assignment repair was implemented at both ownership boundaries.
HIR statement lowering now extracts the raw `ExprKind.Assign` payload and
constructs `HirStmtKind.Assign`; MIR statement lowering predispatches the
derived Assign discriminator, extracts target/op/value independently, and
returns before the broad match. A high-capability GDB review then identified a
second enum-shadow collision: the indexed assignment target was entering the
earlier `HirExprKind.Field` arm. `lower_assign` now predispatches Field and
removes it from the broad match. The rebuilt CLI completed cleanly with `3
compiled, 1146 cached, 0 failed` in 2:26.52 at 377016 KiB peak RSS.

That fixture advanced into `emit_bounds_check_for_index`. Its compact optional
bind accepted a nil `base_type`; the helper now uses an explicit nil guard and
an explicit result presence/unwrap check. The next pure-Simple CLI also built
cleanly (`3 compiled, 1146 cached, 0 failed`, 2:28.17, 405264 KiB peak RSS).
The fixture then completed both MIR function bodies, proving that the original
If, Binary, Assign, Field/Index, parameter, and bounds paths all lower. It next
trapped while collecting a nonfatal MIR diagnostic.

GDB mapped the post-lowering trap to `_format_mir_error`. Its broad match on
`Span?` bound a nil payload as `Some(sp)`. Replacing that match with explicit
presence/unwrap handling and adding the durable
`mir:diagnostics count=<n>` phase marker produced another clean CLI rebuild:
`3 compiled, 1146 cached, 0 failed`, 2:27.32, 377596 KiB peak RSS. The focused
fixture still trapped in `_format_mir_error`; therefore this native compiler
also miscompiles the explicit optional-span path. No fixture binary was
produced, and the strict 16x16, 4K/8K, AArch64, and RISC-V gates were not run.

Small-model review independently confirmed that the nil receiver is the local
formatted span rather than `MirError` itself. The next bounded repair is to
make `_format_mir_error` message-only (`MIR lowering error: <message>`) on this
path, preserving the original diagnostic at the cost of optional location
metadata. This is smaller and safer than another optional representation
workaround. The mandatory three-cycle cap was reached, so that repair is
deferred to the next turn rather than applied without verification.

Final high-capability review accepted the HIR Assign, MIR Assign/Field, and
bounds-check changes as causal and appropriately narrow. It classified the
raw HIR `[Any]` payload extraction as ABI-sensitive but supported by the
observed forward progress, so it should not be refactored without contrary
evidence. The most likely hidden warning is the existing recoverable
`unsupported MIR type kind: Infer` with a nil span.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/bounds-base-nil-guard/`
- `build/native_probe/return-scan-fb-put-bounds-base-nil-guard/`
- `build/native_probe/simple-current-mir-error-option-log`
- `build/native_probe/return-scan-fb-put-mir-error-option-log-gdb/`

## Resumed cycles 78-80: formatter fixed; parameter reads reach MIR type lookup

The reviewed message-only `_format_mir_error` removed all compiled `Span?`
handling while retaining the durable `mir:diagnostics count=<n>` phase marker.
Its pure-Simple CLI rebuilt cleanly (`3 compiled, 1146 cached, 0 failed`,
2:27.42, 372680 KiB peak RSS). The focused fixture no longer trapped in the
formatter and exposed the hidden fatal diagnostics: all six `fb_put`
parameters (`fb`, `fbw`, `fbh`, `x`, `y`, and `color`) were reported as
undefined.

Local and small-model analysis found that the real `NamedVar` arm discarded
the symbol already embedded by HIR and attempted a fresh name lookup after the
function scope had been popped. It now uses the embedded symbol ID. The next
CLI rebuilt cleanly (`3 compiled, 1146 cached, 0 failed`, 2:25.67, 407132 KiB
peak RSS). This cleared the undefined-variable diagnostics and advanced into
Binary operand type probing, where GDB mapped a new nil trap to
`MirLowering.local_mir_type_of`.

High-capability review additionally required explicit `HirParam`, `SymbolId`,
and `text` rebinds across the compiled array/enum payload boundaries. Those
rebinds were applied, and `local_mir_type_of` now explicitly rebinds each
`MirLocal` array element. The third CLI rebuilt cleanly (`5 compiled, 1144
cached, 0 failed`, 2:27.50, 424844 KiB peak RSS), but the focused fixture still
trapped in `local_mir_type_of` with the same recursive Binary/If stack.

The unchanged program counter after typing the scanned `MirLocal` indicates
the nil is more likely the `LocalId` argument retrieved from the parameter
map, not the array element. The next bounded repair should delete the
shadow-prone `LocalKind.Arg` scan and temporary `arg_locals` dictionary in
`lower_function`: `MirBuilder.begin_function` creates parameter locals in
signature order, and `parameter_local_offset` already derives the only valid
layout difference (bootstrap has no return slot; normal lowering may have
one). Mapping each parameter directly to `LocalId(pli + offset)` removes the
only remaining optional/dictionary value between the parameter and type scan.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/mir-error-message-only/`
- `build/native_probe/return-scan-fb-put-mir-error-message-only/`
- `build/native_probe/cpu-simd-current-cli/namedvar-embedded-symbol/`
- `build/native_probe/return-scan-fb-put-namedvar-embedded-symbol-gdb/`
- `build/native_probe/cpu-simd-current-cli/typed-param-namedvar-local-scan/`
- `build/native_probe/return-scan-fb-put-typed-param-namedvar-local-scan-gdb/`

The three-cycle cap was reached. No fourth patch, strict 16x16 closure, or
architecture matrix was started.

## Resumed cycles 81-83: parameter layout and local scan eliminated

The shadow-prone `LocalKind.Arg` scan and temporary `Dict<i64, LocalId>` were
removed from parameter setup. `lower_function` now maps each `HirParam`
directly to the dense local layout created by `MirBuilder.begin_function`,
using the already-derived return-slot offset. The pure-Simple CLI rebuilt
cleanly (`3 compiled, 1146 cached, 0 failed`, 2:35.14, 375292 KiB peak RSS),
but the focused fixture remained in `local_mir_type_of`; this disproved the
temporary argument dictionary as the source.

Allocation review confirmed that active builder local IDs are dense array
indices: `begin_function` resets both sequences, `new_local` assigns the next
ID and appends once, and `set_local_name` preserves order. The struct-array
scan in `local_mir_type_of` was therefore replaced by a bounds-checked indexed
read. That CLI rebuilt cleanly (`3 compiled, 1146 cached, 0 failed`, 2:31.69,
377548 KiB peak RSS). GDB then moved the trap into
`mir_stmt_local_id_value`, showing that its struct-pattern validation was
itself unsafe on this compiled path.

The final cycle removed that pattern helper and read the typed `LocalId.id`
directly before indexing. The CLI again rebuilt cleanly (`3 compiled, 1146
cached, 0 failed`, 2:28.50, 376868 KiB peak RSS), but GDB still trapped at the
direct `local.id` projection. This proves the argument passed to
`local_mir_type_of` is genuinely nil; neither the parameter layout nor the
locals array is responsible.

The remaining carrier is `self.local_map: Dict<i64, LocalId>`. NamedVar lookup
finds the parameter key (the undefined-variable diagnostics are gone), then
returns a nil struct value from the dictionary. The next turn must audit all
`local_map` reads/writes and choose the smallest representation-safe repair,
preferably storing raw `i64` local IDs and reconstructing `LocalId` at the
boundary if the usage count remains bounded. A name-based side map or local
scan would reintroduce shadowing and is rejected.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/direct-param-local-layout/`
- `build/native_probe/return-scan-fb-put-direct-param-local-layout-gdb/`
- `build/native_probe/cpu-simd-current-cli/indexed-local-type/`
- `build/native_probe/return-scan-fb-put-indexed-local-type-gdb/`
- `build/native_probe/cpu-simd-current-cli/direct-local-id-index/`
- `build/native_probe/return-scan-fb-put-direct-local-id-index-gdb/`

The three-cycle cap was reached. No fourth representation patch or broader
closure was started.

## Resumed cycles 84-86: raw local-map values isolate the write boundary

`MirLowering.local_map` and lambda capture snapshots were converted from
`LocalId` struct values to raw `i64` local IDs. Twelve writes now store `.id`,
the seven value-consuming reads reconstruct `LocalId`, and lambda call/snapshot
maps copy raw IDs. Compact optional payload writes in enum-match lowering were
also changed to explicit presence/unwrap checks before reading `.id`. The first
pure-Simple CLI rebuilt cleanly (`7 compiled, 1142 cached, 0 failed`, 2:28.79,
478608 KiB peak RSS).

Parallel audit found one non-`self` write in the fresh lambda lowerer. It now
stores `qi + offset` directly, completing the representation conversion. The
second CLI rebuilt cleanly (`3 compiled, 1146 cached, 0 failed`, 2:31.98,
368132 KiB peak RSS), but the focused fixture still passed a null `LocalId` to
`local_mir_type_of`.

All raw map reads were then given explicit typed `i64` rebinds before
`LocalId` reconstruction, eliminating generic Dict return typing as a cause.
The third CLI rebuilt cleanly (`5 compiled, 1144 cached, 0 failed`, 2:33.64,
425076 KiB peak RSS), but the fixture retained the same stack. Disassembly of
`local_mir_type_of` shows its entry guard testing the second argument and
branching directly to the nil-receiver trap; no helper body instruction runs.
The caller therefore supplies a null `LocalId`.

For this fixture, parameter setup remains the earliest raw-map write:
`local = LocalId(id: pli + parameter_local_offset)` followed by
`local_map[symbol] = local.id`. The read path is now proven typed and the map
representation is no longer struct-valued, so the remaining minimal repair is
to store `pli + parameter_local_offset` directly and reserve the constructed
`LocalId` only for `set_local_name`. This avoids projecting an immediately
constructed single-field struct through the same compiled representation
hazard. If the null survives that direct write, the next diagnostic must log
the raw mapped integer at NamedVar return and the Binary operand depth before
any `LocalId` construction.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/raw-local-map/`
- `build/native_probe/cpu-simd-current-cli/raw-local-map-complete/`
- `build/native_probe/return-scan-fb-put-raw-local-map-complete-gdb/`
- `build/native_probe/cpu-simd-current-cli/raw-local-map-typed-reads/`
- `build/native_probe/return-scan-fb-put-raw-local-map-typed-reads-gdb/`

The three-cycle cap was reached. No direct-integer parameter write, strict
closure, or architecture matrix was started.

## Resumed cycles 87-89: parameter writes proven; broad NamedVar read remains

Parameter setup now stores `pli + parameter_local_offset` directly instead of
constructing and immediately projecting `LocalId`. The pure-Simple CLI rebuilt
cleanly (`3 compiled, 1146 cached, 0 failed`, 2:32.68, 370504 KiB peak RSS),
but the focused fixture retained the same `local_mir_type_of` stack. A GDB
breakpoint proved that the helper's first invocation receives a zero second
argument; disassembly of the caller shows this is the return from
`lower_expr(left)` before the right operand's type probe.

Opt-in parameter-write and NamedVar-read logs were added. Reusing the broad
`SIMPLE_COMPILER_TRACE` switch activated unrelated legacy paths and caused an
earlier SIGSEGV, producing no useful MIR evidence. The corresponding CLI had
nevertheless rebuilt cleanly (`4 compiled, 1145 cached, 0 failed`, 2:26.81,
414156 KiB peak RSS).

The two breadcrumbs were then isolated behind
`SIMPLE_MIR_LOCAL_MAP_TRACE=1`. That CLI rebuilt cleanly (`4 compiled, 1145
cached, 0 failed`, 2:27.69, 425212 KiB peak RSS). The focused fixture recorded
all six expected writes exactly:

- `fb`: symbol 3, local 0
- `fbw`: symbol 4, local 1
- `fbh`: symbol 5, local 2
- `x`: symbol 6, local 3
- `y`: symbol 7, local 4
- `color`: symbol 8, local 5

No discriminant-predispatch NamedVar read marker appeared before the trap.
Therefore parameter creation, offsets, raw map storage, and write-side typing
are proven correct, while the predispatch path is not active. The first
comparison reaches the broad `NamedVar` arm, whose inline `LocalId(id:
mapped_local_id)` still returns null. The next bounded repair should log that
broad read and return the existing typed builder local ID from
`self.builder.locals[mapped_local_id].id`, with a bounds check, instead of
constructing another single-field `LocalId`. If validated, the same
index-to-existing-local conversion should be shared by the remaining six map
consumers.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/direct-param-id-write/`
- `build/native_probe/return-scan-fb-put-direct-param-id-write-gdb/`
- `build/native_probe/cpu-simd-current-cli/local-map-id-trace/`
- `build/native_probe/return-scan-fb-put-local-map-id-trace/`
- `build/native_probe/cpu-simd-current-cli/local-map-dedicated-trace/`
- `build/native_probe/return-scan-fb-put-local-map-dedicated-trace/`

The three-cycle cap was reached. No builder-local reconstruction patch or
broader closure was started.

## Resumed cycles 90-92: NamedVar discriminator matches; nested pattern fails

All seven raw local-map consumers now retrieve the existing typed `LocalId`
from `builder.locals[mapped_id].id`; no consumer constructs a new single-field
`LocalId`. The stale source contract was updated to require direct raw
parameter-ID storage, and a distinct broad-NamedVar read marker was added. The
pure-Simple CLI rebuilt cleanly (`5 compiled, 1144 cached, 0 failed`, 2:25.77,
451596 KiB peak RSS), but the focused fixture still trapped before any Var or
NamedVar marker.

Dedicated markers were then added to both predispatch and broad `Var` reads.
That CLI rebuilt cleanly (`3 compiled, 1146 cached, 0 failed`, 2:28.97, 436500
KiB peak RSS). The fixture again emitted only the six valid parameter writes;
neither variable variant's payload arm executed.

The final cycle logged each actual HIR expression discriminator beside
constructed Var and NamedVar exemplars. The CLI rebuilt cleanly (`3 compiled,
1146 cached, 0 failed`, 2:29.93, 429972 KiB peak RSS). The focused trace was:

1. four nested Binary expressions (`1764299124`), matching the chained bounds
   condition;
2. a left operand with actual discriminator `1337030607`;
3. the constructed NamedVar discriminator was also `1337030607`;
4. no NamedVar read marker followed;
5. the right IntLit (`1201078288`) lowered, then the first type probe received
   null.

This proves the outer discriminator guard is correct and the inner
`match expr_value.kind: case HirExprKind.NamedVar(...)` is the failing
boundary. It does not extract the payload despite an exact discriminator
match, then the broad match also returns a null local. The next bounded repair
must declare/reuse `rt_enum_payload`, extract the guarded NamedVar payload as
`[Any]`, explicitly rebind slot 0 to `SymbolId` and slot 1 to `text`, and run
the existing map/builtin logic without another enum pattern. Var should remain
unchanged until evidence exercises it.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/builder-local-map-reads/`
- `build/native_probe/return-scan-fb-put-builder-local-map-reads-gdb/`
- `build/native_probe/cpu-simd-current-cli/var-local-map-trace/`
- `build/native_probe/return-scan-fb-put-var-local-map-trace/`
- `build/native_probe/cpu-simd-current-cli/expr-disc-trace/`
- `build/native_probe/return-scan-fb-put-expr-disc-trace/`

The three-cycle cap was reached. No raw NamedVar payload extraction, strict
closure, or architecture matrix was started.

Small-model and high-capability review confirmed the payload ABI and scope:
multi-field `HirExprKind.NamedVar(symbol, name)` lowers in declaration order,
so slot 0 is `SymbolId` and slot 1 is `text`. The guarded raw-payload branch
must own every NamedVar outcome (mapped local, global, `OUTPUT_LIMIT`,
`DEFAULT_TIMEOUT_MS`, `None`, and diagnostic) and return without falling into
the known-broken broad match. `Var` remains unchanged because this trace did
not exercise it. The focused source contract should assert the extern, typed
slot extraction, and removal of the inner NamedVar match.

## Resumed cycles 93-95: raw payload works; SymbolId scalar remains tagged

The nested NamedVar match was replaced by `rt_enum_payload` extraction with
typed slot 0 `SymbolId` and slot 1 `text`. The guarded branch now owns and
returns every NamedVar outcome: constants, mapped locals, fallback resolution,
`None`, globals, and diagnostics. Focused source assertions cover the extern,
payload slots, direct parameter storage, and absence of the guarded nested
match. The pure-Simple CLI rebuilt cleanly (`3 compiled, 1146 cached, 0
failed`, 2:26.98, 402480 KiB peak RSS).

This eliminated the nil trap and lowered the full fixture, but all parameter
references were diagnosed undefined. A dedicated payload marker was added.
The next CLI rebuilt cleanly (`3 compiled, 1146 cached, 0 failed`, 2:29.27,
411424 KiB peak RSS), and the trace showed:

- `fb`: payload symbol 24, parameter key 3
- `fbw`: payload symbol 32, parameter key 4
- `fbh`: payload symbol 40, parameter key 5
- `x`: payload symbol 48, parameter key 6
- `y`: payload symbol 56, parameter key 7
- `color`: payload symbol 64, parameter key 8

Every payload symbol is exactly the intended ID shifted left three bits. The
raw enum payload preserves RtCore's tagged-integer scalar, while assigning it
to `SymbolId` and reading `.id` does not untag it.

Routing the typed slot through `mir_expr_symbol_id_value` did not decode it.
That third CLI rebuilt cleanly (`3 compiled, 1146 cached, 0 failed`, 2:29.55,
403676 KiB peak RSS) and reproduced the same tagged values. Repository runtime
code provides the canonical decoder: public `rt_value_as_int`, implemented as
an arithmetic right shift by three in both native C and pure-Simple core. The
next bounded repair should declare `extern fn rt_value_as_int(value: i64) ->
i64`, decode `symbol_t.id` before local-map lookup, and assert that source
contract. A raw hardcoded shift is rejected because the runtime owns tagging.

Evidence is retained under:

- `build/native_probe/cpu-simd-current-cli/namedvar-raw-payload/`
- `build/native_probe/return-scan-fb-put-namedvar-raw-payload/`
- `build/native_probe/cpu-simd-current-cli/namedvar-payload-id-trace/`
- `build/native_probe/return-scan-fb-put-namedvar-payload-id-trace/`
- `build/native_probe/cpu-simd-current-cli/namedvar-symbol-decoder/`
- `build/native_probe/return-scan-fb-put-namedvar-symbol-decoder/`

The three-cycle cap was reached. No runtime-tag decode, strict closure, or
architecture matrix was started.

## Resumed cycles 96-98: SymbolId decode passes; inliner nil trap advances

The raw `NamedVar` payload now crosses the runtime boundary through the
canonical `rt_value_as_int` decoder. The first clean pure-Simple CLI build
completed with `3 compiled, 1146 cached, 0 failed` in 2:29.04 at 424064 KiB
peak RSS. Its fixture trace proves the behavioral result, not only the source
shape: parameter keys are written as IDs 3 through 8, payloads decode to those
same IDs, every lookup reports `has=true`, and every read resolves to locals 0
through 5.

Evidence:

- `build/native_probe/cpu-simd-current-cli/namedvar-runtime-untag/build.log`
- `build/native_probe/return-scan-fb-put-namedvar-runtime-untag/build.log`

After correct MIR lowering, native optimization reached a separate existing
failure: `field access on nil receiver`, emitted as `SIGILL`. GDB located the
first trap in `InlineCostAnalyzer.extract_operand_function_name`. That helper
required `type_.kind == FuncPtr` even though the repository's named-call MIR
convention is value-based: `Const(Str(name), _)`. The type check was removed,
and a focused unit case now verifies extraction from
`Const(Str("self"), MirType.unit())`. The second clean CLI build completed with
`3 compiled, 1146 cached, 0 failed` in 2:30.84 at 375128 KiB peak RSS.

Evidence:

- `build/native_probe/cpu-simd-current-cli/inliner-callee-shape/build.log`
- `build/native_probe/return-scan-fb-put-inliner-callee-shape/build.log`

The next debugger trace showed progress into the duplicate
`ModuleInliner.extract_func_name` helper. All three driver callee extractors
were aligned with the same call-operand convention: `Str` carries a name and
`Int` carries a symbol ID; the unused `MirType.kind` dereferences were removed.
The third clean CLI build completed with `3 compiled, 1146 cached, 0 failed`
in 2:32.78 at 375276 KiB peak RSS.

Evidence:

- `build/native_probe/cpu-simd-current-cli/inliner-callee-driver-shape/build.log`
- `build/native_probe/return-scan-fb-put-inliner-callee-driver-shape/build.log`

The final fixture still terminates with `SIGILL` after the same successful
NamedVar/local-map trace and reports `field access on nil receiver`. Per the
three-cycle guard, no fourth debugger/build attempt was made. The next scoped
session must obtain the new post-driver backtrace, repair that later optimizer
boundary, and produce the fixture binary before running the retained strict
16x16 entry. The strict 16x16 closure, 4K/8K render contract, and strict
x86_64/AArch64/RISC-V target matrix were not started in these cycles.

The focused optimizer unit source was checked once with the new self-hosted
CLI. The command exited 1 because `check` rejects the pre-existing
`it "...":` test syntax throughout `inlining_spec.spl`; it did not isolate a
new-case type or assertion failure. The retained log is
`build/native_probe/cpu-simd-focused-checks/inlining-spec-check.log`.
Repository diff whitespace validation passed after all edits.

## Resumed cycles 99-101: collection text probe and free-function transit

The fresh post-inliner GDB trace reached
`collection_opt_patterns.colopt_local_is_text`; all earlier NamedVar and
inliner stages remained green. The original probe nested
`match loc.type_.kind: case Ptr(pointee, _)` and then dereferenced
`pointee.kind`, reproducing the same bootstrap enum-payload binding hazard.

A type-owner helper, `mir_type_is_text`, now checks the `Ptr` discriminant,
extracts its boxed multi-field payload through `rt_enum_payload`, rebinds slot
zero to `MirType`, and compares the pointee discriminant with `I8`. Both
collection text probes delegate to it. The active collection optimizer spec
now covers direct true/false type classification and an end-to-end
`Ptr(I8)` concat rewrite to `spl_str_append`. High-capability review found no
blocking issue in the payload ABI, mutability handling, or scope.

The first clean CLI rebuild completed with `5 compiled, 1144 cached, 0 failed`
in 2:30.37 at 389620 KiB peak RSS. The fixture still trapped in
`colopt_local_is_text`. A nil guard was added at the shared helper boundary;
the second rebuild completed with `3 compiled, 1146 cached, 0 failed` in
2:29.01 at 357932 KiB peak RSS, but the trap remained.

Disassembly then identified the actual receiver: the generated
`colopt_local_is_text` checked its `MirFunction` argument before reading
`.locals`, found it nil, printed `field access on nil receiver`, and executed
`ud2`. The concat helpers only need local metadata, so the optimizer now
extracts `func.locals` once from the valid method argument and passes
`[MirLocal]` through the free-function chain instead of repeatedly passing the
large `MirFunction` value. The third rebuild completed with `4 compiled, 1145
cached, 0 failed` in 2:29.59 at 398068 KiB peak RSS.

The final fixture still exits with `SIGILL` after the successful NamedVar
trace. Per the three-cycle guard, no fourth debugger run was made, so the new
post-`[MirLocal]` trap location is intentionally not guessed. Evidence:

- `build/native_probe/cpu-simd-current-cli/colopt-text-raw/build.log`
- `build/native_probe/return-scan-fb-put-colopt-text-raw/build.log`
- `build/native_probe/cpu-simd-current-cli/colopt-text-nil-guard/build.log`
- `build/native_probe/return-scan-fb-put-colopt-text-nil-guard/build.log`
- `build/native_probe/cpu-simd-current-cli/colopt-locals-array/build.log`
- `build/native_probe/return-scan-fb-put-colopt-locals-array/build.log`

The next scoped session must take one backtrace with
`simple-current-colopt-locals-array`, repair that later boundary, and produce
the one-second fixture binary. Strict 16x16, 4K/8K, and the strict
x86_64/AArch64/RISC-V target matrix remain unstarted.

## Resumed cycles 102-104: local transit isolated; unsafe rewrite removed

The first fresh backtrace remained in `colopt_local_is_text`. Disassembly of
the `[MirLocal]` version showed that the array itself was valid but typed
`for loc in locals` produced a nil `MirLocal` before field access. A direct
dense-ID lookup was tried, but sidecar audit found multiple optimizer passes
with sparse or unregistered local IDs. That diagnostic CLI rebuilt cleanly;
the implementation was therefore corrected to an index-driven `while` scan
that preserves sparse-ID lookup while avoiding typed iterator binding.

The corrected CLI rebuilt with `4 compiled, 1145 cached, 0 failed` in 2:33.44
at 387116 KiB peak RSS. Its fixture still trapped in the same helper. The next
disassembly identified a different nil receiver: the `LocalId?` reported
present, but `unwrap()` passed a nil `LocalId` struct. The matcher was changed
to carry the existing scalar `i64?` local ID instead. That CLI rebuilt with
`3 compiled, 1146 cached, 0 failed` in 2:35.04 at 373820 KiB peak RSS. The
fixture advanced from deliberate nil-field `SIGILL` to a later `SIGSEGV`,
proving the optional-struct boundary was removed.

High-capability review then found why execution segfaulted: the now-reachable
string-concat rewrite encoded `spl_str_append` with `MirType.unit()` instead
of a function-pointer type, so native lowering would call string-data memory.
More importantly, replacing value-semantic concat with in-place `realloc`
was unsound without ownership and alias proofs and could realloc static string
storage. The minimal correct repair is deletion. The collection optimizer's
string-concat-to-append pattern and its local-transit plumbing were removed;
array concat and the rest of collection optimization remain unchanged.

Evidence:

- `build/native_probe/cpu-simd-current-cli/colopt-direct-local/build.log`
- `build/native_probe/cpu-simd-current-cli/colopt-indexed-scan/build.log`
- `build/native_probe/return-scan-fb-put-colopt-indexed-scan/build.log`
- `build/native_probe/cpu-simd-current-cli/colopt-scalar-local-id/build.log`
- `build/native_probe/return-scan-fb-put-colopt-scalar-local-id/build.log`

The safety deletion was applied after the three-cycle cap and is intentionally
not rebuilt in this session. The next scoped session must build that deletion
once and rerun the fixture. Strict 16x16, 4K/8K, and the strict architecture
matrix remain pending.

## Resumed cycles 105-107: MIR clears; rebuilt CLI lacks Cranelift SFFI

The ownership-unsafe collection string rewrite was rebuilt successfully with
`4 compiled, 1145 cached, 0 failed` in 2:34.59 at 377736 KiB peak RSS. The
fixture now clears MIR optimization and reaches `[cranelift-direct] target`,
then segfaults at address zero in
`nogc_sync_mut.sffi.codegen.cranelift_new_aot_module`.

Binary inspection proved this is a link-composition defect, not fixture MIR:
the rebuilt CLI contains undefined `rt_cranelift_new_aot_module` with no
dynamic relocation, while the stage3 compiler defines it and
`src/compiler_rust/target/bootstrap/deps/libsimple_compiler.a/.so` export it.
`LD_PRELOAD` cannot repair an absolute zero call site.

Two existing linker configurations were tried once each. Pointing
`--runtime-path` at the `deps` directory while retaining
`--runtime-bundle core-c-bootstrap` rebuilt cleanly (`2 compiled, 1147 cached,
0 failed`, 2:32.60, 351480 KiB) but still omitted the symbol. Switching the
bundle selector to `auto` also rebuilt cleanly (`2 compiled, 1147 cached, 0
failed`, 2:32.40, 359080 KiB) and still produced the same undefined symbol.

Evidence:

- `build/native_probe/cpu-simd-current-cli/colopt-drop-unsafe-string/build.log`
- `build/native_probe/return-scan-fb-put-colopt-drop-unsafe-string/build.log`
- `build/native_probe/return-scan-fb-put-colopt-drop-unsafe-string-preload/build.log`
- `build/native_probe/cpu-simd-current-cli/colopt-drop-unsafe-string-full-sffi/build.log`
- `build/native_probe/return-scan-fb-put-colopt-drop-unsafe-string-full-sffi/build.log`
- `build/native_probe/cpu-simd-current-cli/colopt-drop-unsafe-string-auto-sffi/build.log`

The next scoped session must repair the native linker/runtime-bundle
composition so compiler CLI closures link the existing
`libsimple_compiler.a` SFFI definitions, then rebuild once and rerun the
fixture. Strict 16x16 and all architecture/render matrices remain pending.

## Resumed cycles 108-110: Stage-4 backfill works; text ABI double expansion isolated

The prior missing-symbol diagnosis was refined rather than papered over. The
bootstrap native-project linker intentionally authorizes the dedicated
compiler backfill only for exact stage-4 compiler entries. Building
`src/app/cli/main.spl` with both `SIMPLE_BOOTSTRAP=1` and
`SIMPLE_BOOTSTRAP_STAGE4=1` linked a real definition of
`rt_cranelift_new_aot_module`; `nm` and a debugger breakpoint both proved the
constructor was present and called. That canonical CLI completed with
`3 compiled, 1147 cached, 0 failed` in 2:35.25 at 391332 KiB peak RSS.

The fixture then exposed compact optional pattern binding in `SymbolTable`.
The lookup family now uses explicit presence checks and typed unwraps instead
of binding through `if val`. The next canonical CLI completed with
`3 compiled, 1147 cached, 0 failed` in 2:31.71 at 348696 KiB peak RSS. Its
fixture cleared HIR/MIR lowering and reached the Cranelift constructor without
a trap, but the constructor returned zero.

Bounded archive inspection and two independent sidecar audits proved the
backfill is complete, retains its Cranelift state/dependency closure, and is
not stubbed. A direct archive probe returned valid handles for target codes
0, 1, 2, and 99. GDB then captured the decisive ABI evidence at the failing
call:

```text
rdi=0 rsi=-1 rdx=14
```

The required raw ABI is `(name_ptr, name_len, target)`, so the expected values
were `(valid pointer, 14, 0)`. Disassembly of the generated Simple wrapper
showed why: it evaluated the explicit `name.ptr()`/`name.len()` wrapper and
then inserted another `rt_string_data`/`rt_string_len` expansion before the
raw call. The compiler's native and LLVM text-argument tables incorrectly
listed four raw Cranelift APIs:

- `rt_cranelift_new_module`
- `rt_cranelift_new_aot_module`
- `rt_cranelift_begin_function`
- `rt_cranelift_get_function_ptr`

High-capability review rejected changing their Simple declarations to logical
`text`, because interpreter handlers and compatibility wrappers implement the
established raw pointer/length ABI. The accepted shared repair removes those
four entries from both Rust codegen expansion tables and leaves the raw Simple
wrappers intact. The direct adapter log now records AOT module name and target
and emits a value-bearing failure line. A focused source contract prevents
the four mappings from returning while retaining all raw wrapper calls.

The corrected Rust bootstrap driver rebuilt successfully in 3:42.01 at
2348316 KiB peak RSS. It was used only for the bootstrap step that produces a
pure-Simple stage-4 compiler. The final stage-4 attempt did not converge: its
native-build worker consumed one full CPU for 21 minutes, reached about
1.4 GiB RSS, emitted no object, and stopped updating its log after source
warnings. Per the three-cycle/runaway guard it was terminated once with
status 143; no retry was started and no cache was deleted.

Evidence:

- `build/native_probe/cpu-simd-current-cli/stage4-colopt-drop-unsafe-string/build.log`
- `build/native_probe/return-scan-fb-put-stage4-symbol-unwrap/build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-symbol-unwrap/build.log`
- `build/native_probe/cpu-simd-current-cli/rust-text-abi/seed-build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-cranelift-text-abi/build.log`
- `build/native_probe/cranelift-text-abi-source-check.log`
- `build/native_probe/cranelift-text-abi-stage4-check.log`

The one-second fixture therefore still lacks a binary, and strict 16x16,
4K/8K, x86_64/AArch64/RISC-V, and render performance matrices remain pending.
The next scoped session must diagnose the cold bootstrap worker stall from the
retained log/cache before any further stage-4 build attempt.

## Resumed cycles 111-113: worker routing fixed; AOT creation clears

The 21-minute worker run was not a compiler deadlock. The rebuilt Rust seed
had been invoked without `SIMPLE_NATIVE_BUILD_RUST=1`, so command dispatch
launched `native_build_worker.spl` through the interpreter. Its parent waited
normally while the worker eagerly loaded the full compiler graph and emitted
162116 diagnostic lines. The repository's canonical bootstrap script already
selects the direct Rust bootstrap handler with that environment flag; no new
worker abstraction or loader workaround was needed.

With canonical routing restored, the full stage-4 CLI completed successfully:
`1150 compiled, 0 cached, 0 failed` in 3:48.30 at 815504 KiB peak RSS. This
produced a pure-Simple executable. The one-second fixture then proved the text
ABI repair end to end:

```text
[cranelift-direct] create AOT name=bootstrap_main target=0
[cranelift-direct] module
[cranelift-direct] declare fb_put
[cranelift-direct] compile fb_put
```

Module creation no longer returns zero. The fixture instead reached a later
nil-field trap inside `cl_translate_instruction`. GDB and disassembly showed
that `MirInstKind.Const` completed `cl_translate_const_value`, then trapped on
the destructured `dest.id`. A discriminant/payload predispatch was added for
the colliding `Const` variant. That CLI rebuilt with
`3 compiled, 1147 cached, 0 failed` in 2:32.45 at 320992 KiB peak RSS.

The first payload version still rebound slot zero as `LocalId`; disassembly
proved that single-field struct binding was nil. It now decodes the payload
directly to scalar `dest_id` with `rt_value_as_int`, matching the established
map-key ABI. The second CLI rebuilt with `3 compiled, 1147 cached, 0 failed`
in 2:35.92 at 299988 KiB peak RSS. The fixture still trapped at the same gate.

High-capability review found the remaining generated-code hazard: the old
`case Const(dest, value, type_)` arm remained below the predispatch. It was
removed so `Const` has exactly one path and cannot re-enter broken pattern
destructuring. Per the three-cycle guard, that final deletion is intentionally
not rebuilt in this turn.

Evidence:

- `build/native_probe/cpu-simd-current-cli/stage4-cranelift-text-abi-rust-worker/build.log`
- `build/native_probe/return-scan-fb-put-stage4-cranelift-text-abi/build.log`
- `build/native_probe/return-scan-fb-put-stage4-cranelift-text-abi/gdb-sigill.log`
- `build/native_probe/cpu-simd-current-cli/stage4-const-predispatch/build.log`
- `build/native_probe/return-scan-fb-put-stage4-const-predispatch/build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-const-scalar-id/build.log`
- `build/native_probe/return-scan-fb-put-stage4-const-scalar-id/build.log`

The next scoped session starts with one cached rebuild of the duplicate-arm
deletion and one fixture run. Strict 16x16, 4K/8K, and architecture matrices
remain pending until the fixture produces an executable.

## Resumed cycles 114-116: Const clears; GEMM operand ABI repaired

The cached rebuild of the deleted legacy `Const` arm completed with
`3 compiled, 1147 cached, 0 failed` in 2:32.47 at 321884 KiB peak RSS. The
fixture still emitted the managed nil-field diagnostic, but GDB moved the
failure out of `cl_translate_instruction` and identified the exact frame:

```text
operand_copy_local_id
operand_uses_local
terminator_operand_use_count
local_use_count_in_function
detect_gemm_add_pair
compile_function
```

`operand_copy_local_id` destructured the single-field `Copy(LocalId)` and
`Move(LocalId)` variants through the same bootstrap-unsafe struct pattern path.
It was replaced with discriminant/payload decoding. The first version treated
the payload as `[Any]`; the next cached CLI build again completed with
`3 compiled, 1147 cached, 0 failed` in 2:31.35 at 320776 KiB peak RSS, and the
fixture changed from a managed nil trap to `SIGSEGV` in the same helper.

The second GDB trace proved why. `rt_enum_payload` returns a wrapper array only
for multi-field variants. For a single-field variant it returns the field
directly. Indexing that direct tagged `LocalId` as an array produced an invalid
data pointer (`r11 = 0x31`). The helper now converts the direct payload with:

```text
Some(rt_value_as_int(rt_enum_payload(kind)))
```

The final cached CLI rebuild completed with `3 compiled, 1147 cached, 0 failed`
in 2:36.45 at 322076 KiB peak RSS. This repair cleared both GEMM-analysis
crashes and advanced the fixture into `main` compilation. The remaining
deterministic failure is later and distinct:

```text
[cranelift-direct] compile main
cranelift: AOT declare_function error: Backend error: Symbol "..." has a null byte
Simple runtime error: function not found: void.unwrap_err
```

The three-cycle cap was reached, so the corrupted AOT function-name/result
path was not modified or rebuilt in this session. The next scoped session
starts by tracing the `main`-time declaration caller and the value passed as
its name, then performs at most one focused repair before rerunning this
fixture. Strict 16x16, 4K/8K, and x86_64/AArch64/RISC-V matrices remain gated
on producing the fixture executable.

Evidence:

- `build/native_probe/cpu-simd-current-cli/stage4-const-no-legacy-arm/build.log`
- `build/native_probe/return-scan-fb-put-stage4-const-no-legacy-arm/build.log`
- `build/native_probe/return-scan-fb-put-stage4-const-no-legacy-arm/gdb-sigill.log`
- `build/native_probe/cpu-simd-current-cli/stage4-gemm-operand-payload/build.log`
- `build/native_probe/return-scan-fb-put-stage4-gemm-operand-payload/build.log`
- `build/native_probe/return-scan-fb-put-stage4-gemm-operand-payload/gdb-sigsegv.log`
- `build/native_probe/cpu-simd-current-cli/stage4-gemm-direct-scalar-payload/build.log`
- `build/native_probe/return-scan-fb-put-stage4-gemm-direct-scalar-payload/build.log`

### Frontier review after cycle 116

The read-only high-capability review accepted both payload repairs and found
two concrete follow-ups for the next bounded session:

1. `cranelift_codegen_adapter.spl` passes `emit_name` through the raw text ABI
   again when defining the completed AOT function, although Rust retained the
   valid name at `begin_function`. The corrupted symbol keeps a plausible
   13-byte length but has a stale or invalid pointer. Because the Rust wrapper
   reconstructs this text with unchecked raw parts, this is a memory-safety
   concern, not only an error-reporting defect. Compare the pointer/length at
   begin and define; if only the second pointer is corrupt, remove the redundant
   name transport and use the retained finished-function name.
2. `compile_module_with_backend` lacks an explicit result return type while its
   caller invokes `unwrap_err()`. That directly explains the independent
   `void.unwrap_err` resolution failure and should be typed consistently with
   the bootstrap helper.

The existing source contracts are useful bootstrap guards but are not
behavioral ABI proof. The retained fixture remains the release gate after
these two focused repairs.

## Resumed cycles 117-119: AOT ownership fixed; begin-time name remains corrupt

The AOT define path now keeps the public four-argument runtime ABI but ignores
the legacy name pointer/length. Both Simple facades pass zeroes and Rust uses
the name owned by `FinishedFunc`, which was cloned by `begin_function`. The
established wrapper name was retained because the async SFFI facade re-exports
it. `compile_module_with_backend` also now declares its actual
`Result<CompiledModule, CompileError>` return type, eliminating the independent
`void.unwrap_err` type defect.

The Rust `simple-driver` seed rebuilt successfully in 3:12.47 at 2350300 KiB
peak RSS. The first stage-4 rebuild necessarily invalidated the native cache
and completed with `1150 compiled, 0 cached, 0 failed` in 3:44.30 at
843996 KiB peak RSS. Its fixture exposed a bootstrap dispatch regression in
the optional GEMM fusion scanner: scalar `Mul` was treated as `MatMul`, and the
`Ret(MirOperand?)` destructuring passed nil to `operand_copy_local_id`.

The fusion detector now checks the outer `BinOp` and nested `MatMul` /
`BroadcastAdd` discriminants before scanning uses. Its `Ret` path decodes the
single-field terminator payload and optional payload explicitly. The cached
stage-4 rebuild completed with `3 compiled, 1147 cached, 0 failed` in 2:28.16
at 324176 KiB peak RSS. The fixture no longer crashed; it reached AOT define,
which returned false without a Cranelift diagnostic.

GDB proved the call used `(module=1, name_ptr=0, name_len=0, ctx=5)` and that
the runtime function returned zero. This identified a deployment omission:
the seed had been rebuilt, but stage 4 linked the stale
`libsimple_compiler_backfill` implementation, which rejected an empty name
before consulting `FinishedFunc`. The canonical `simple-compiler-backfill`
package rebuilt successfully in 17.67 seconds at 442076 KiB peak RSS. The next
stage-4 link completed with `2 compiled, 1148 cached, 0 failed` in 2:28.76 at
324836 KiB peak RSS.

With the correct backfill linked, Rust reached `declare_function` but reported
that the name retained by `begin_function` already contained NUL bytes. This
narrows the remaining defect to the earlier Simple text lifetime boundary:
`compile_function` saves `emit_name`, builds the signature, then passes the
saved text to `cranelift_begin_function`. The declaration loop succeeds because
it consumes the name immediately. The next scoped session should avoid keeping
that derived text across `build_signature`: build the signature first, then
call begin with the `"__simple_main"` literal or `func.name` directly, or reuse
the declared-name registry if frontier review finds that stronger.

The three-cycle cap was reached. No further source repair or rebuild was run.
Strict 16x16, 4K/8K, and x86_64/AArch64/RISC-V matrices remain gated on the
fixture executable.

Evidence:

- `build/native_probe/cpu-simd-current-cli/rust-aot-finished-name/seed-build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-aot-finished-name-result-type/build.log`
- `build/native_probe/return-scan-fb-put-stage4-aot-finished-name-result-type/build.log`
- `build/native_probe/return-scan-fb-put-stage4-aot-finished-name-result-type/gdb-sigill.log`
- `build/native_probe/cpu-simd-current-cli/stage4-gemm-explicit-dispatch/build.log`
- `build/native_probe/return-scan-fb-put-stage4-gemm-explicit-dispatch/build.log`
- `build/native_probe/return-scan-fb-put-stage4-gemm-explicit-dispatch/gdb-aot-define-return.log`
- `build/native_probe/cpu-simd-current-cli/rust-aot-finished-name-backfill/backfill-build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-aot-backfill-gemm-dispatch/build.log`
- `build/native_probe/return-scan-fb-put-stage4-aot-backfill-gemm-dispatch/build.log`

## Resumed cycles 120-122: dynamic text ABI fixed; object emission reached

Passing `func.name` directly after signature construction did not repair AOT
declaration. GDB then proved declaration itself had always returned handle
zero: Rust received `name_len=6`, but the supplied bytes began with a tagged
heap-object address rather than UTF-8. The Simple log could print `fb_put`, yet
`text.ptr()` exposed the runtime value/header for dynamic text.

An intermediate numeric-handle path was added to `begin_function`. Rust
validates the handle against the requested module before recovering the
registry-owned name. The Rust seed and compiler backfill rebuilt successfully
in 3:22.79 and 19.13 seconds respectively. The resulting stage-4 CLI completed
with `1150 compiled, 0 cached, 0 failed` in 4:09.33 at 895988 KiB peak RSS.
Its fixture showed `name_or_handle=0`: declaration still failed before the
handle path could help.

The repository's existing LLD bridge identified the canonical shared repair:
dynamic runtime text must cross raw SFFI boundaries through
`rt_string_data(text)` and `rt_string_len(text)`, not `.ptr()` and `.len()`.
Both no-GC Cranelift facades now use those accessors for module creation,
function begin/declaration/lookup, string data declaration, function-address
lookup, and object output paths. This prevents every sibling Cranelift text
operation from restoring the same tagged-pointer bug.

The cached stage-4 rebuild completed with `3 compiled, 1147 cached, 0 failed`
in 2:42.29 at 323896 KiB peak RSS. The fixture then cleared every previous
gate:

```text
[cranelift-direct] declare __simple_main
[cranelift-direct] declare fb_put
[cranelift-direct] compile main
[cranelift-direct] compile fb_put
[cranelift-direct] emit /tmp/simple_cranelift_build.native_probe.return-scan-fb-put-fixture.o
```

It segfaulted immediately after object emission and produced no final fixture
executable. The three-cycle cap was reached, so the post-emission crash was not
traced in this session. The next scoped session starts with one GDB trace at
`rt_cranelift_emit_object_raw`, `cranelift_free_module`, and
`rt_file_read_bytes` to distinguish module cleanup from dynamic output-path
readback. Strict 16x16, 4K/8K, and architecture matrices remain gated on the
fixture executable.

Evidence:

- `build/native_probe/cpu-simd-current-cli/stage4-begin-direct-name/build.log`
- `build/native_probe/return-scan-fb-put-stage4-begin-direct-name/build.log`
- `build/native_probe/cpu-simd-current-cli/rust-begin-declared-handle/seed-build.log`
- `build/native_probe/cpu-simd-current-cli/rust-begin-declared-handle/backfill-build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-begin-declared-handle/build.log`
- `build/native_probe/return-scan-fb-put-stage4-begin-declared-handle/build.log`
- `build/native_probe/return-scan-fb-put-stage4-begin-declared-handle/gdb-begin-return.log`
- `build/native_probe/return-scan-fb-put-stage4-begin-declared-handle/gdb-declare-return.log`
- `build/native_probe/cpu-simd-current-cli/stage4-runtime-text-accessors/build.log`
- `build/native_probe/return-scan-fb-put-stage4-runtime-text-accessors/build.log`

## Resumed cycles 123-125: cache-write ABI repaired; hosted link path remains blocked

GDB isolated the post-object-emission crash to the native build cache write:
the generated compiler called `rt_file_write_text` with the length-aware
four-argument text ABI, while the C runtime still exported the legacy
two-pointer wrapper. `runtime.h` and `runtime_native.c` now expose the
four-argument write/append ABI and share the binary-safe
`rt_core_file_write_data` implementation. The native runtime archive and a
cached pure-Simple stage-4 compiler rebuilt successfully. The fixture then
emitted its object without crashing.

A trace-enabled fixture run proved the next failure is in hosted linking. The
driver reaches `link_to_native`, but its output path is already represented as
`<value:0xea43411>` instead of the requested path. The direct linker returns
without creating a file, and native-build's output guard correctly fails. The
The compact `text? ?? "a.out"` extraction was investigated as a candidate.
A frontier review rejected replacing it with `Option<text>.unwrap()`: the
current lowering can still pass the tagged Option container without first
extracting `rt_enum_payload`, and a source-spelling contract would not prove
linker execution or output creation. That speculative patch was removed.

Verification did not converge within the mandatory three-cycle limit. The
pure-Simple cached stage rebuild timed out after 10 minutes at 70 MiB RSS with
no compiler output, consistent with the known worker-routing stall. A focused
pure-Simple driver check also timed out. The final bootstrap-only Rust driver
attempt compiled for 3:29.68 but selected `core-c-bootstrap`; its link failed
on hosted-only runtime symbols, including the Cranelift SFFI owners. The next
session must use `SIMPLE_BOOTSTRAP=1` and `--runtime-bundle rust-hosted`; the
Rust CLI permits that hosted bundle only in bootstrap mode. No fourth build or
fixture attempt was started. The exact tagged output-path payload fix remains
pending, and the strict 16x16/render-scale and x86_64/AArch64/RISC-V matrices
remain gated.

Evidence:

- `build/native_probe/return-scan-fb-put-stage4-runtime-text-accessors/gdb-post-emit-fresh.log`
- `build/native_probe/cpu-simd-current-cli/runtime-file-text-abi/native-all-build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-runtime-file-text-abi/build.log`
- `build/native_probe/return-scan-fb-put-stage4-runtime-file-text-abi/build.log`
- `build/native_probe/return-scan-fb-put-stage4-runtime-file-text-abi-trace/build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-output-option-unwrap/build.log`
- `build/native_probe/cpu-simd-current-cli/stage4-output-option-unwrap/driver-check.log`
- `build/native_probe/cpu-simd-current-cli/stage4-output-option-unwrap-rust-bootstrap/build.log`

## Resumed cycles 126-128: shared null-coalesce payload fix; bootstrap lane wall

The retained trace was traced past the driver call site into shared MIR
lowering. `CompileOptions.output_file` is `text?`; `Some(path) ?? "a.out"`
checked `rt_is_some` but copied the tagged enum container into the present
branch. The linker's `<value:0xea43411>` output is the runtime formatter's
representation of that leaked container.

The `NullCoalesce` present branch now calls the existing
`rt_unwrap_or_self` owner before merging. The core C implementation of that
helper, previously an identity function, now matches the Simple-core and Rust
owners by returning an enum payload and preserving non-enum raw values. The
runtime call retains its real `i64 -> i64` ABI; text results normalize through
the existing `rt_interp_cstr` decoder and aggregate handles are explicitly
retyped before the merge. The focused MIR source spec checks lowering and C
ownership. This is the
shared fix for all `Option<T> ?? default` users; no driver/linker special case
was retained.

Executable verification did not converge within three cycles:

1. Passing `rust-hosted` directly to the Rust seed was rejected because hosted
   bundles are removed from the full `main.spl` native-project lane.
2. The canonical `--full-bootstrap --full-cli` sequence produced and verified
   pure-Simple stage2/stage3, then failed stage4 because `core-c-bootstrap`
   lacks hosted full-CLI symbols (`spl_dlopen`, `rt_process_run_timeout`, GUI
   owners, and others).
3. A warm-cache focused stage2 rebuild omitted the wrapper's exported
   `SIMPLE_BOOTSTRAP=1`; bootstrap-main therefore selected core-only runtime
   and failed on `rt_native_build`/Cranelift SFFI symbols.

No fourth build was started. The next scoped session should repeat only the
focused stage2 command with `SIMPLE_BOOTSTRAP=1`, then use that pure-Simple
binary to build and execute the retained fixture. Full-CLI runtime ownership
is a separate blocker and is not required for that fixture gate. Strict
16x16/render-scale and x86_64/AArch64/RISC-V matrices remain pending.

Frontier review also identified two pre-existing nullable semantics outside
this output-path fix: explicit ordinal `None` is not recognized by the C
runtime's hash-based predicate, and raw integer value `3` collides with the nil
sentinel. Neither has executable evidence in this cycle, so neither was mixed
into the payload patch.

Evidence:

- `build/native_probe/cpu-simd-current-cli/stage4-rust-hosted-bootstrap/build.log`
- `build/native_probe/cpu-simd-current-cli/bootstrap-simd-resume/build.log`
- `build/bootstrap-simd-resume/logs/x86_64-unknown-linux-gnu/stage2-native-build.log`
- `build/bootstrap-simd-resume/logs/x86_64-unknown-linux-gnu/stage3-native-build.log`
- `build/bootstrap-simd-resume/logs/x86_64-unknown-linux-gnu/stage4-native-build.log`
- `build/native_probe/cpu-simd-current-cli/stage2-null-coalesce-v2/build.log`

## Resumed cycles 129-131: fixture and architecture matrix pass

The focused stage2 rebuild was repeated with the missing canonical
`SIMPLE_BOOTSTRAP=1` environment and completed successfully from the warm
cache. That new pure-Simple compiler built the retained return-scan/fb-put
fixture in 1.1 seconds. The resulting 4.4 MiB executable ran and exited zero,
proving the cache-write ABI and shared null-coalesce payload repair through
final hosted linking and execution.

The strict architecture matrix then passed. Runtime owners compiled for
x86_64, AArch64, RISC-V, and RISC-V RVV. Target C kernel binaries for x86_64,
AArch64, and RISC-V all built and ran with row scheduling (cross targets under
QEMU). Matrix status is `pass`, reason `target-binary-builds-pass`.

The strict 16x16/32x32 render-scale run did not pass. Its first
`4k_software` arm timed out with exit 124 before writing SDN output; stderr
contains `simple: seed sibling not found, skipping delegation`. The retained
full stage4 CLI therefore remains unsuitable for this run path even though it
contains the required Engine2D externs. No retry was issued after the third
cycle. The next scoped session must repair or provide the source-matched seed
sibling/delegation path, then rerun this render contract once. Architecture
matrices do not need repeating.

Evidence:

- `build/native_probe/cpu-simd-current-cli/stage2-null-coalesce-bootstrap/build.log`
- `build/native_probe/return-scan-fb-put-stage2-null-coalesce-bootstrap/build.log`
- `build/native_probe/return-scan-fb-put-stage2-null-coalesce-bootstrap/run.log`
- `build/native_probe/return-scan-fb-put-fixture-null-coalesce-bootstrap-bin`
- `build/check/cpu-simd-arch-matrix-post-fixture/evidence.env`
- `build/check/cpu-simd-arch-matrix-post-fixture/report.md`
- `build/check/cpu-simd-render-scale-post-fixture.stderr.log`
- `build/check/cpu-simd-render-scale-post-fixture/4k_software.err`
- `build/check/cpu-simd-render-scale-post-fixture/4k_software.time`

## Resumed cycles 132-134: native render entry builds; core runtime owners missing

Installing the canonical adjacent `simple_seed` did not repair delegation for
the retained full CLI because that older binary fails to resolve its relative
`argv[0]`; `_cli_seed_sibling_path` receives an empty executable path. A
10-second `strace` confirmed the failed lookup. Setting the existing
`SIMPLE_BOOTSTRAP_DRIVER` override to the absolute seed path corrected the
child `execve`, but the Rust seed still spent the full 900-second render bound
compiling the benchmark import graph and produced no SDN output.

The proven pure-Simple stage2 compiler then native-built the generated 16x16
software benchmark entry directly in 41.38 seconds. This removes CLI
delegation from the render hot path. The resulting 13 MiB executable started
in 1.18 seconds but failed with `field access on nil receiver` and SIGILL.
Its build log explains why: 217 unresolved-symbol stubs were generated,
including all required `rt_engine2d_simd_*_row_u32` and
`rt_engine2d_simd_*_span_u32` owners. This artifact is not valid render
evidence.

No fourth cycle was started. The next session must rerun this focused native
entry build with `SIMPLE_NO_STUB_FALLBACK=1`, repair the core runtime bundle so
`runtime_simd_dispatch.c` supplies the Engine2D SIMD owners, and execute the
same 16x16 artifact. The already-passing architecture matrix must not be
repeated.

Evidence:

- `build/native_probe/render-seed-delegation.strace`
- `build/native_probe/render-seed-override.strace`
- `build/check/cpu-simd-render-scale-seed-override.stderr.log`
- `build/check/cpu-simd-render-scale-seed-override/4k_software.time`
- `build/native_probe/render-scale-native-4k-software/build.log`
- `build/native_probe/render-scale-native-4k-software/run.err`
- `build/native_probe/render-scale-native-4k-software/run.time`

## Resumed cycle 135: strict link isolates shared Cranelift sections

The focused benchmark was rebuilt with `SIMPLE_NO_STUB_FALLBACK=1` and retained
native objects. This corrected the prior hypothesis: the fresh
`runtime_simd_dispatch.o` exports the required Engine2D row/span symbols as
strong text owners, and none appears in the strict linker's unresolved list.
The bootstrap runtime archive also contains all eight Engine2D SIMD owners.

The strict link instead failed on 116 hosted optional-backend symbols. Object
diagnostics showed that all Cranelift functions in each imported module shared
`.text.subsection`, so one reachable function retained every optional function
and its CUDA, ROCm, OneAPI, OpenGL, WebGPU, and dynamic-SFFI references. The
normal Cranelift AOT constructor already enables per-function/data-object
sections; the SFFI AOT constructor did not. The focused repair enables the same
settings in `rt_cranelift_new_aot_module` and adds a direct two-function object
test. The render contract's binary gate now also requires strong `T` symbols,
rejecting the weak `W` stubs that made the earlier invalid binary look linked.

Evidence:

- `build/native_probe/render-scale-native-4k-software-no-stub/build.log`
- `build/native_probe/render-scale-native-4k-software-no-stub/build.time`

### Cycle 135 correction and stop condition

The per-function-section hypothesis above was tested and falsified. A rebuilt
Rust seed, native-all archive, and pure-Simple stage2 produced the same strict
benchmark failure: 116 unresolved symbols before and 116 after, with identical
symbol sets. Cranelift's object backend names distinct function subsections
`.text.subsection`, so the repeated printed section name was not evidence that
functions shared one section. The experimental SFFI builder change and test
were removed.

The remaining failure is entry-closure/runtime-lane ownership: the canonical
renderer closure retains optional hosted GPU and dynamic-SFFI modules that the
explicit `core-c-bootstrap` runtime does not own. Required Engine2D SIMD
row/span symbols remain strongly resolved. The render contract's strong-symbol
gate and `fill_rows` coverage remain as valid independent fixes. Per the
three-cycle guard, no further render rebuild, audit, commit, rebase, or push was
attempted in this session.

Additional evidence:

- `build/native_probe/cranelift-sffi-function-sections/rust-seed-build.log`
- `build/native_probe/cranelift-sffi-function-sections/rust-native-all-build.log`
- `build/native_probe/cranelift-sffi-function-sections/stage2-build-bootstrap.log`
- `build/native_probe/render-scale-native-4k-software-function-sections/build.log`
- `build/native_probe/render-scale-native-4k-software-function-sections/build.time`

## Resumed cycles 136-138: CPU Draw IR closure links; execution traps early

The renderer closure was narrowed at its architectural boundary instead of
implementing optional GPU backends. The benchmark now asks the canonical HTML
layout owner for a `DrawIrComposition`, then executes its current rect/text
command set directly through `CpuBackend.create()` or
`CpuBackend.create_simd()`. The narrow module does not import `engine.spl` or
`draw_ir_adv.spl`, so CUDA, ROCm, OneAPI, OpenGL, WebGPU, and Vulkan are no
longer retained by this CPU-only entry.

Cycle 136 used an explicit bootstrap runtime path and reduced the unresolved
set from 116 to 17. That path preferred the stale
`target/bootstrap/deps/libsimple_runtime.a`, which omitted the current SIMD and
legacy-font C owners. Cycle 137 removed the override and built the canonical
fresh core-C bundle. All SIMD and legacy-font symbols then resolved, leaving
seven tagged-value ABI owners: `rt_string_rfind`, mutex lock/unlock, and the
four dynamic-SFFI calls used by canonical font resolution.

Cycle 138 added real core-C tagged-value implementations in
`runtime_native.c`. The implementation reuses the registered core string and
array representations, provides protected-value mutex operations, and converts
tagged strings to bounded NUL-terminated copies before `dlopen`/`dlsym`.
`runtime_native.c` compiles cleanly and exports all new owners. The final strict
native build passed with no fallback stubs: 1 module compiled, 76 cached, zero
failed, and a 3.0 MiB executable.

The only execution attempt ran that exact artifact once for `cpu_simd` and
once for `software` at 16x16. Both exited 132 (`SIGILL`) before writing output;
their logs are empty. ELF metadata requires only x86-64-baseline, excluding an
AVX host mismatch. The kernel journal retained the fault address even though
core dumps were disabled: `0x655fea` resolves to `reset_simd_hits`, immediately
after its six counter stores, where native codegen emitted `ud2` for void-body
fallthrough. The function now ends with explicit `return ()`, and the source
contract pins that terminator. This final source correction is not executed in
this turn because the three-cycle cap was reached. Checksum, native-hit, and
full render-scale evidence therefore remain unproven; no commit, rebase, or
push followed.

Evidence:

- `build/native_probe/cpu-draw-ir-cycle1/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle2/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle3/runtime-native-compile.log`
- `build/native_probe/cpu-draw-ir-cycle3/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle3/cpu_simd.run.log`
- `build/native_probe/cpu-draw-ir-cycle3/software.run.log`

## Resumed cycles 139-141: native traps narrowed to layout font metrics

Cycle 139 rebuilt after adding the explicit unit return to `reset_simd_hits`.
The prior trap disappeared. Execution advanced to `Mutex.lock`, where the
font-metric cache lock was a nil receiver because call-valued class globals are
not represented by the current native module-init format.

Cycle 140 tested explicit runtime lock initialization and corrected the new
core-C mutex ABI so `lock` remains held until `unlock`; it also serialized the
mutex registry and retained POSIX `dlsym(NULL, name)`. Execution advanced past
the lock and trapped in selected-font asset validation. That showed vector-face
selection was unnecessary hosted work for the benchmark's final bitmap
`CpuBackend.draw_text` executor.

Cycle 141 added a vector-disabled DrawIR command lane in the canonical HTML
semantic owner. The normal DrawIR entry remains vector-enabled. The final run
still trapped in `Mutex.lock`: HTML layout itself resolves font metrics at
`simple_web_html_layout_renderer.spl:5797` before DrawIR command emission, so a
command-only switch is too late. The next implementation must carry a
font-metrics policy through layout and use bitmap advances for this CPU lane;
it must not use mutable process-global render mode.

All three strict native builds linked without fallback stubs. No SDN was
produced, checksum/SIMD-hit evidence remains missing, and the three-cycle cap
ended the turn without commit, rebase, or push.

Evidence:

- `build/native_probe/cpu-draw-ir-cycle4/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle4/cpu_simd.run.log`
- `build/native_probe/cpu-draw-ir-cycle5/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle5/cpu_simd.run.log`
- `build/native_probe/cpu-draw-ir-cycle6/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle6/cpu_simd.run.log`

## Resumed cycles 142-144: CPU Draw IR native evidence

Cycle 142 carried the vector-font policy into `compute_styles` and routed the
CPU bitmap composition through that policy. The strict native artifact built
and ran without a trap. Its first manual CLI probe was invalid evidence because
the exporter expects `--software-render-backend`, `--warmup-count`, and
`--sample-count`; the shorthand flags selected the default software backend.
The corrected CLI probe then exposed a separate native `text.to_i64` issue
(`16 -> 2`, `220 -> 27`, `1 -> 0`), so CLI-derived dimensions were excluded
from render acceptance evidence.

Cycle 143 compiled the checker's exact value-based API with literal dimensions.
The 16x16 CPU-SIMD run selected `cpu_simd`, completed one warmup and one sample,
and proved 49 provider hits plus 16 native row hits. Only 33 of 256 pixels were
nonzero because the CPU bitmap Draw IR composition omitted the CSS canvas
background.

Cycle 144 prepended the semantic canvas-background command before HTML box/text
commands. The strict value-based native run selected `cpu_simd`, completed one
warmup and one sample, recorded 65 provider hits and 32 native row hits, and
returned `sum32:137369747233` with `nonzero_pixels:256`. The architecture matrix
remains the previously proven PASS and was not rerun. The mandatory three-cycle
cap prevented another compiled scalar harness; full 4K/8K retained checksums and
an independently executed scalar checksum remain pending final review.

Evidence:

- `build/native_probe/cpu-draw-ir-cycle7/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle7/cpu_simd_corrected.sdn`
- `build/native_probe/cpu-draw-ir-cycle8-values/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle8-values/probe.sdn`
- `build/native_probe/cpu-draw-ir-cycle9-values/native-build.log`
- `build/native_probe/cpu-draw-ir-cycle9-values/probe.sdn`

## Final native parity and canonical Draw IR baseline

The native span ABI originally wrote raw `u32` values into Simple arrays whose
elements use tagged integers. Reduced evidence made the corruption visible as
an almost exact 8x checksum gap between CPU-SIMD and scalar output. The native
fill entry points now box pixels before SIMD stores; blend unboxes inputs and
boxes its result; copy remains a bit-exact tagged-slot copy. Exporter numeric
arguments now use `parse_int()` rather than the character-code `to_i64()` API.

The focused C kernel, tagged span, and row-scheduling gates pass. On x86_64 the
row scheduling probe measured 4K fill at 42.501 ms serial / 11.154 ms parallel
and 8K at 175.150 ms serial / 35.670 ms parallel.

The canonical Draw IR producer now excludes `visibility:hidden` nodes and
always emits its CSS canvas background. This intentionally moves the retained
benchmark baseline from the legacy direct-paint values to the canonical Draw
IR values. A strict native full-resolution run proved scalar/SIMD parity:

- 4K: `sum32:35624123900197965`, all 8,294,400 pixels nonzero, 7,469 provider
  hits and 4,320 native hits.
- 8K: `sum32:142496654044810320`, all 33,177,600 pixels nonzero, 11,792 provider
  hits and 8,640 native hits.
- Scalar controls produced the same checksums with zero SIMD/native hits.
- Full run: 8.31 seconds elapsed, 2,603,264 KiB max RSS.

Evidence:

- `build/native_probe/cpu-draw-ir-resume-c-kernels-fixed/`
- `build/native_probe/cpu-draw-ir-resume-cycle2-parity/`
- `build/native_probe/cpu-draw-ir-resume2-c-kernels/`
- `build/native_probe/cpu-draw-ir-resume2-full-final/`

## Mainline integration verification

Cherry-picking the verified lane onto current mainline exposed two integration
regressions. First, the raw C blend helper inherited tagged-value conversions
even though its public array wrapper already unboxes and reboxes pixels. This
broke the standalone C kernel contract; restoring raw lanes made both the C
kernel and in-place span ABI checks pass. Second, narrowing parsed CLI integers
with `as i32` unboxed values twice (`3840 -> 480`, `300 -> 37`, `1 -> 0`). The
numeric `to_i32()` conversion preserves the native value and restores full-size
render arguments.

The native link also exposed the pre-existing undefined `hash_to_upper_hex`
call in `sha1_hex_upper`. It now composes the existing lowercase encoder with
the standard text uppercase operation. The rebuilt exporter has no unresolved
application symbols; the linker preview contains only compiler CPU metadata
symbols (`__cpu_indicator_init` and `__cpu_model`).

Final integrated native scalar/SIMD parity:

- 4K: `sum32:35493787911101004`, all 8,294,400 pixels nonzero, 4,599 provider
  hits and 4,438 native hits on SIMD; scalar has zero hits.
- 8K: `sum32:141975144273648204`, all 33,177,600 pixels nonzero, 8,919 provider
  hits and 8,758 native hits on SIMD; scalar has zero hits.
- Direct environment guards pass for working and staged changes; no executable
  specs exist under `doc/06_spec`; staged and full commit whitespace checks pass.
- The focused SHA-1 test runner is blocked before test execution by the existing
  test-runtime error `unknown extern function: rt_cli_arg_count`. Native linking
  verifies that the removed SHA-1 symbol is no longer unresolved.

Integration evidence is under `build/native_probe/integrated/`; these build
artifacts are intentionally untracked.

Final high-capability review found that partial-alpha native blending still
assumed an opaque destination and that the new uppercase SHA-1 assertion lacked
its selective import. Native src-over now carries destination alpha through the
straight-alpha formula on scalar and AArch64 lanes, including a guarded zero
denominator before zero-alpha lane restoration. The C baseline now covers
transparent destination pixels, the kernel/span checker passes, both SHA-1
specs import `sha1_hex_upper`, and the reviewer confirmed both findings resolved.
