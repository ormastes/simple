# Pure-Simple tooling hardening status — current snapshot 2026-07-24

No row is release-qualified until an admitted current Stage 4 runs its listed
evidence. Rust-seed or stale deployed output is bootstrap/debug evidence only.

| Tool | Current status | Known bug or risk | Root solution / proving evidence |
|---|---|---|---|
| Test runner | Structured evidence, indexed-u8 failure gates, canonical direct summaries, daemon routing, atomic request publication, and timeout classification are source-fixed. Explicit missing/non-test/empty targets exit 4; implicit discovery, `--list`, and `--spl-doctest` retain valid zero-selection behavior. Invalid/bare values exit 2, and JSON mode owns one aggregate envelope with worker diagnostics on stderr. | Fresh qualification is blocked by retained native crashes. A deployed run returned `0x1fffffffffffffff` for a SIGILL child status; `process_run_bounded` was already corrupt before the runner formatted it. See `test_runner_native_signal_exit_status_tag_corruption_2026-07-24.md`. | Fix native signed process-result tuple preservation and add signal-status parity evidence; then admit one incremental Stage 4 and run the focused/essential gates once. |
| Lint | Parser-backed ARG/COLL/STUB/W0404/W0406/W0407, PARSE001, invalid options/config, mixed help, JSON/fix/dry-run, recursive directory expansion, empty-directory denial, and canonical overlap deduplication are source-fixed. | The retained wrapper crashes before some AST contracts, and no fresh Stage-4 candidate has qualified the full surface. | Run the focused CLI/config/AST contracts and aggregate essential-tools gate once against the exact admitted candidate. |
| Formatter/fix | Atomic writes and rewrite admission are source-fixed. Malformed fmt/fix options and mixed help exit 2 before I/O; dry-run is nonmutating; directories, missing files, and read failures fail closed. | The retained runtime can crash at the stale `rt_env_set` ABI boundary before owner contracts; no fresh Stage-4 behavior proof exists. | Qualify the live fmt/fix grammar and mutation contracts on the admitted candidate; keep unimplemented migration behavior out of scope. |
| Duplicate checker | Missing targets, malformed options, clone discovery, and relative walking are source-fixed. Incremental token/cosine grouping is non-cyclic; its versioned UTF-8-safe cache validates content hashes, rejects malformed snapshots, and prunes deleted files. Interpreted semantic imports are lazy and no longer pull MCP helpers. The external-CWD gate covers uncached/create/reuse/change/delete/disabled cache JSON behavior. | Fresh Stage-4 qualification is missing. Native one-binary closure still includes semantic HTTP and the standalone `core-c-bootstrap` link exposes missing Dict/string/HTTP symbols; see `duplicate_check_eager_semantic_native_closure_2026-07-24.md`. | Repair the native closure/runtime owner, incrementally build one admitted candidate, then run the focused phase-2 spec and bounded essential-tools gate once. |
| Native build/cache | Monitor classification, output publication, BuildCache warm-path hazards, and removed runtime-bundle rejection are source-fixed. Bootstrap and full-CLI owners reject all six hosted aliases before build work; a fresh Stage 2 built and ran the tiny capability artifact. | The synchronized supported-lane bootstrap closure reused 675 cached objects but remained CPU-bound for 900 seconds with no log or artifact; the single bounded attempt was terminated. | Instrument or bound entry-closure discovery, resume the preserved cache, produce one admitted full CLI, then run the bounded essential-tools smoke. |
| Bootstrap admission | Candidate `00431ce5…` passed delegated stdout/stderr/exit fidelity and the isolated official source-check. Stage 4 now runs the exact candidate through redeploy and essential-tools gates before deployment. | Admission rejected that candidate on p2 native-build SIGILL and bounded test-child result loss. Both roots are source-fixed (`2f6430a87c8`, `bd9de761c11`), but no successor candidate exists. | Produce one cache-preserving successor from a verified pure-Simple stage, then run each admission gate once and record hash, revision, elapsed time, and RSS. |
| Pure-Simple runtime ABI | Tuple row-length repair and locally-defined runtime-call resolution are source-fixed | post-fix exact Rust archive qualification has not run because the existing Rust test binary failed to link; a package-scoped one-job rebuild timed out before the test; runtime success remains unclaimed | finish the incremental Rust test build, then rerun the exact archive probe once |
| `check` | Unknown/missing/invalid output options reject before discovery. Directories expand recursively, canonical overlaps deduplicate, and explicit empty directories fail instead of reporting `OK`; `run` preserves all post-entry program arguments. | Evidence is source/temporary-bootstrap only; no successor Stage 4 contains both the p2 lowering and bounded-output fixes. | Run the focused option, directory, source-check, and run-argument contracts once through the admitted candidate. |
| MCP server | native-first source exists; qualification pending | wrapper/registry can select stale or source fallback; startup/request budgets unproven | exact native artifact admission, stdio integration, warm startup/latency/RSS evidence |
| LSP MCP server | diagnostics uses group-aware bounded execution; correlated `lsp_symbols` wrapper admission is fixed in source | installed candidate provenance is missing and its exact native smoke remains unqualified | regenerate the wrapper, invalidate stale stamps, then rerun the exact-artifact native smoke with hash, revision, time, and RSS evidence |
| SPipe/docgen | source/manual present; generated evidence blocked | argv/runtime crashes or stalls can prevent truthful manual generation | admitted runtime, bounded docgen, generated/manual parity and zero placeholders |
| Test daemon | absolute-expiry protocol and responsive production routing gates fixed and unit-tested; Stage 4 lifecycle/ownership evidence pending | old path-only requests could let a fixed 600s daemon child outlive its client and block later work; CLI ownership still needs qualification | tagged absolute expiry, dequeue-time spawn refusal, and remaining-time group-aware execution; then prove start/status/run/stop plus stale-state recovery |
| Essential-tools smoke | The bounded identity preflight rejects Rust seed/debug binaries. The external-CWD matrix proves real/red/zero/forged runner outcomes, lint clean/deny/directory/strict JSONL, and duplicate token/cosine cache lifecycle with JSON stream purity. | No freshly admitted Stage-4 CLI has passed the expanded aggregate. | Admit the exact candidate, then run the bounded aggregate once. |
| Codex session guard | Duplicate guarded resumes use a rollout flock plus live-owner detection; default thresholds refuse resumes over 20M tokens and terminate live rollouts over 50M | direct Codex invocation outside `bin/codex` remains intentionally unguarded | launch through repo `bin/codex` or put repo `bin/` first on `PATH` |

## Next admission sequence

1. Produce one incremental Stage 4 candidate; do not run a full bootstrap.
2. Admit its provenance, then run the essential-tools aggregate once.
3. Run focused lint, duplication, atomic-provider, compiler/lib, MCP, and LSP MCP
   checks once each; generate the SPipe manual only after those pass.
4. Record command, candidate hash, exit, elapsed time, and max RSS, then review,
   sync, and push.
