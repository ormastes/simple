# WM Glass Theme Domain Research — TLDR

- Stitch Glass is a material contract: translucency, blur, radius, borders,
  depth, typography, and active/inactive state.
- `aetheric_dark` already contains most source tokens, but current adapters
  discard them.
- Repository history leaves three plausible visual identities, so the user
  must select the intended product direction.

```text
Stitch reference
  -> typed glass material
     -> capability-aware lowering
        -> equal semantic fingerprint on host and SimpleOS
```
