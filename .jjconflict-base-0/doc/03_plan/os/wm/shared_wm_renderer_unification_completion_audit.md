# Shared WM Renderer Unification — Completion Audit

Date: 2026-05-29

## Scope

This audit maps the active objective to current evidence. It is intentionally stricter than the current implementation state: uncertain or partial evidence is treated as incomplete.

## Requirement Matrix

| Objective requirement | Evidence needed for completion | Current evidence | Status |
|---|---|---|---|
| Electron, Tauri, and pure Simple web renderer share the same web render API interface | A single Simple API/trait/module imported by all three adapters, with tests proving the same request/response contract for HTML/snapshot/patch/input/capabilities/host-window operations and optional pixel output | `src/lib/common/ui/web_render_api.spl` exists and `ui.web`, `ui.electron`, and `ui.tauri` import it. `test/01_unit/app/ui/web_render_backend_api_spec.spl` covers those backends plus pure Simple browser artifact participation and proves Electron/Tauri backend render IPC is built from common `WebRenderRequest` artifacts. `src/app/ui.tauri/app.spl`, `src/app/ui.tauri/async_app.spl`, and now `src/app/ui.tauri/tauri_entry.spl` route runnable Tauri render output through common `WebRenderRequest` artifacts instead of manually wrapping raw HTML. The actual Tauri shell `simple tauri-entry <file.ui.sdn>` path emits `type`, `target`, `surface_id`, `width`, `height`, and `html`; `test/01_unit/app/ui/tauri_entry_common_envelope_spec.spl` proves this for `examples/06_io/ui/hello_tauri.ui.sdn`. `tools/tauri-shell/src-tauri/src/lib.rs` now parses those render-envelope fields and publishes them to the WebView as `window.__SIMPLE_WEB_RENDER_ENVELOPE__`; its host-origin key/action/resize commands serialize common `type:"input"` envelopes, and `src/app/ui.ipc/protocol.spl` parses the common input envelope shape. `tools/electron-shell/main.js`, `tools/electron-shell/preload.js`, and `tools/electron-shell/package.json` now exist as the documented Electron package entrypoint. The package loads `src/app/ui.electron/bridge.js`, which can spawn `bin/simple run src/app/ui.electron/app.spl <entry.ui.sdn>`, consume the Simple render stream, preserve `target`, `surface_id`, `width`, `height`, `body_html_length`, and `css_length` into `window.__SIMPLE_WEB_RENDER_ENVELOPE__`, and serialize host-origin input as common `type:"input"` envelopes. `test/01_unit/app/ui/electron_bridge_common_envelope_test.js` proves the Node-testable Electron bridge contract, and `.github/workflows/electron-tests.yml` now runs `npm --prefix tools/electron-shell run live-smoke-ci`, which delegates to `scripts/check/check-electron-live-smoke.shs` and productizes the live Electron BrowserWindow smoke under Xvfb with `build/electron_shell_envelope.json` validated for target/surface/viewport metadata, nonempty render HTML and DOM text, `performance.now`, two `requestAnimationFrame` callbacks, CSS animation support, and `blur_or_tolerance_used=false`. `src/app/ui.ipc/async_handler.spl` preserves complete common render IPC JSON and wraps legacy raw HTML only for compatibility. Host window command JSON delegates to `WebRenderHostWindowCommand`; snapshot, patch, and input wire payloads are represented by `WebRenderSnapshotEnvelope`, `WebRenderPatchEnvelope`, and `WebRenderInputEnvelope`, covered by `test/01_unit/lib/common/ui/web_render_api_spec.spl`. Web, Electron, and Tauri expose adapter helpers for common snapshot, patch, and input envelopes; the live Web runtime sends snapshot and patch WebSocket payloads through the common envelopes, and the browser boundary unwraps them for the retained renderer. Electron and Tauri snapshot/patch transport helpers now have behavioral equality coverage against the shared web backend helper output. The legacy WebSocket render push in `src/app/ui.web/server.spl` now derives its body from `WebRenderRequest`/`web_render_body_html` and carries the `simple_web` target while preserving browser-compatible `type:"render"` frames. Pure Simple browser event queueing records the common input envelope. `src/app/ui.web/render_cache.spl` adds persistent static-shell HTML caching keyed from the shared web render cache key while bypassing dynamic islands. The surface registry now has a Tauri kind, `src/app/ui.tauri/async_app.spl` exposes `register_tauri_window(...)`, and `test/01_unit/app/ui/tauri_surface_registry_spec.spl` passes `3/3` for focused Tauri binding coverage. `test/01_unit/app/ui/web_render_cache_spec.spl` passes `13/13` and proves SWBC1 static-shell binary persistence, decode, command-plan reuse, disk cache hits, memory hot reuse, and binary metadata on `WebRenderArtifact`. This completes the shared Web/Electron/Tauri/pure Simple web render API slice for the current contract. | Complete |
| CUDA, Metal, and CPU 2D renderers share the same 2D rendering API interface | CPU, Metal, and CUDA backend files all implement `std.gpu.engine2d.backend.RenderBackend`; backend selection compiles; tests assert mandatory method parity and capability handling | `RenderBackend` exists in `src/lib/gc_async_mut/gpu/engine2d/backend.spl`; CPU, Metal, and CUDA concrete files exist and compile against the same interface. CPU-backed software rendering now honors stencil masks on filled spans/horizontal spans and includes flat-top triangle endpoints, closing the CPU-backed divergence found while comparing mirrored GPU behavior. CUDA accepts an explicit `CudaSession`, reports typed context/kernel diagnostics, only reports initialized after hardware self-tests, and now keeps device readback current when `draw_image` falls back through the software mirror. Strict CUDA integration coverage passed `25/25`, covering clear, filled rect, outline rect, line, circle, filled circle, rounded rect, triangle, image upload, gradient, text, clip, mask, present, and latest-readback behavior. Metal uses the same backend interface with typed unavailable behavior on non-macOS hosts and strict Metal capability coverage. Apple-host live Metal proof remains tracked in the broader GUI restart plan, but this shared-interface row is complete for the current contract. | Complete |
| Host WM and SimpleOS WM share logic | Host and SimpleOS import/use the same WM service/core for create/focus/move/resize/minimize/restore/close/input routing; no local host service shim duplicates WM authority | `host_compositor_entry.spl` imports the real `os.services.wm.wm_service.WmService` for lifecycle state and translates host bridge requests into shared `WmAction` values before applying compositor mutations. Pointer-driven drag/resize, host direct focus, and host direct destroy now route through shared `WmAction` lifecycle helpers. `DesktopShell.apply_wm_action` routes create, create-web, destroy, focus, resize, move, set-title, minimize, maximize, restore, and update-tree compositor mutation through `apply_wm_action_to_compositor(...)` while keeping shell-specific owner registration, launcher accounting, replies, and logs local. Shared update-tree materialization now lives in `wm_action_remote_tree(...)` instead of the desktop shell. `HostCompositor` is documented as an adapter-local projection of host native windows into `WmLifecycleWindowState`, not a second WM authority; host bridge conversion, lifecycle application, pointer movement, left-button state, and taskbar hit testing all delegate to shared helpers in `wm_action_applier.spl`. `test/01_unit/os/compositor/host_compositor_entry_spec.spl` covers the host adapter projection path, including bridge lifecycle, direct focus/destroy, taskbar hit testing, drag clamp, resize grip, and mouse resize through shared helpers. `test/01_unit/os/compositor/simpleos_gui_shared_wm_adapter_spec.spl` covers SimpleOS GUI adapter bridge lifecycle for create, move, resize, minimize, restore, set-title, focus, maximize, update-tree, and destroy through `deliver_bridge_request`; `test/01_unit/os/compositor/wm_action_applier_spec.spl` covers host-neutral lifecycle and pointer state; `test/03_system/app/ui/feature/shared_wm_renderer_unification_spec.spl` passes `4/4` for host-neutral lifecycle, SimpleOS adapter bridge, Simple Web host surface creation, and 2D compositor backend routing. Host-native process launch failure windows, backend construction, event-loop pumping, and presentation remain adapter-owned platform effects, not duplicate WM policy. | Complete |
| Host WM and SimpleOS WM use the same web renderer API | Host and SimpleOS web rendering path both call shared web render API, with tests for the same fixture | `simple_web_window_renderer.spl` builds `WebRenderRequest` values and turns Simple Web pixels into `WebRenderArtifact` values before blitting to `CompositorBackend`. Host compositor renderer-name exposure uses `WEB_RENDER_TARGET_SIMPLE_WEB`; `test/01_unit/os/compositor/host_compositor_entry_spec.spl` proves Browser, Electron, and Tauri host WM handles all report the shared Simple Web renderer. `wm_action_applier.spl` materializes `create_web_window` actions as compositor surfaces backed by `wm_action_web_window_request(...)`, a `WebRenderRequest` targeting `simple_web`, instead of leaving them as generic remote-window shells. `test/01_unit/os/compositor/simple_web_window_renderer_spec.spl`, `test/01_unit/os/compositor/wm_action_applier_spec.spl`, and `test/01_unit/os/compositor/web_surface_blit_spec.spl` cover artifact dimensions, target identity, renderer facade use, and `create_web_window` request-surface plumbing. `doc/04_architecture/os/shared_wm_renderer_unification.md` and `doc/05_design/shared_wm_renderer_unification.md` now document host-native surfaces/input capture as adapter-private platform effects, not a competing web renderer or alternate SimpleOS web content payload. | Complete |
| Host WM and SimpleOS WM use the same 2D engine API | Both paths render through `CompositorBackend`/`Engine2D RenderBackend` adapters and do not bypass with direct platform-specific overlay drawing | Compositor/Engine2D adapters exist. The SimpleOS baremetal overlay painter now targets `CompositorBackend` and `DesktopShell.run_baremetal` wraps its Engine2D instance in `BaremetalEngine2dOverlayBackend`, with `test/01_unit/os/desktop/shell_baremetal_backend_spec.spl` and the shared-WM behavior spec proving the painter no longer requires raw Engine2D. `doc/04_architecture/os/shared_wm_renderer_unification.md` and `doc/05_design/shared_wm_renderer_unification.md` now document the baremetal overlay as an explicit adapter-private chrome boundary: it may use only `CompositorBackend` primitives, must not call raw `Engine2D`, and is not promoted to WidgetNode compositor surfaces until that DSL is verified safe in the boot lane. This closes the 2D API sharing requirement while preserving the intentional non-WM shell-chrome boundary. | Complete |
| Pure Simple GUI/web-render path is memory and size optimized against an equivalent Qt GUI app | A measured Simple artifact and a measured equivalent Qt artifact, with the Simple path at or below Qt size and no avoidable allocation regressions in the pure Simple renderer | `src/app/ui.browser/backend.spl` uses pre-sized framebuffer/pixel arrays and now builds P3 snapshot text through a pre-sized part array instead of per-pixel array growth. `src/app/ui.browser/renderer.spl` also initializes its legacy framebuffer with a pre-sized array literal instead of repeated `push`. `test/01_unit/app/ui/web_render_backend_api_spec.spl` covers the optimized snapshot text path. `scripts/check/check-qt-gui-size-baseline.shs` was rerun on 2026-05-29 and records the smallest measured Simple Web artifact from `doc/09_report/web_baremetal_size_audit_2026-05-28.md`; current value is 14,336 bytes for `Simple web placeholder URL facade` in `doc/09_report/qt_gui_size_baseline_2026-05-29.md`. The same report records `qt_build=unavailable`, `reason=missing_qt_tooling`, `qt_pkg=unavailable`, and `comparison_status=unavailable`; local `pkg-config --exists Qt6Widgets` and `Qt5Widgets` both exit `1`. `scripts/check/check-gtk-gui-size-speed-baseline.shs` records a successful lightweight Simple HTML render-loop baseline in `doc/09_report/gtk_gui_size_speed_baseline_2026-05-29.md`; GTK compiled to a 14,472-byte minimal binary but speed is unavailable on this headless host. Qt development files are still missing on this host, so Qt equal-or-better remains unproven. | Partially complete |
| Research local with agents | Independent local explorer results for web renderer, 2D renderer, WM sharing, and artifact presence | Agents completed and findings were merged into `doc/01_research/ui/wm/shared_wm_renderer_unification.md`. | Complete |
| If not complete, plan | Requirement options, NFR options, and staged plan exist | Options and plan are present under `shared_wm_renderer_unification`. | Complete |

## Next Evidence To Produce

1. System tests:
   - optional promotion of the advisory Electron live smoke to a required branch-protection check
   - optional live Tauri WebView screenshot/debugger proof that observes `window.__SIMPLE_WEB_RENDER_ENVELOPE__` in the running shell
   - optional broader Engine2D backend implementation conformance beyond the current CPU/Vulkan/CUDA/Metal focused coverage
2. Implementation evidence:
   - keep shared web render API imported by Web/Electron/Tauri/pure Simple renderer
   - optional native GPU kernels for currently mirror-backed CUDA text, clip, and mask paths; current mirror paths synchronize to device readback and are covered by strict hardware tests
   - Qt toolchain provisioned, or a CI environment that can build the Qt baseline and compare it against the measured Simple Web artifact
