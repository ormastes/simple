# Editor IDE Production Matrix - 2026-05-13

Purpose: track production parity for the Markdown/wiki editor, VSCode-style
debugging, and LSP surfaces. A row is complete only when implementation, UI
wiring, and behavior verification exist.

## Status Legend

- PASS: behavior works end-to-end and is covered by tests.
- PARTIAL: some implementation exists, but parity is incomplete.
- MISSING: no production implementation was found.
- BLOCKED: depends on architecture/runtime support that is not present yet.

## Obsidian / Wiki Parity

| Feature | Status | Evidence / Required Work |
| --- | --- | --- |
| Live Markdown preview | PASS | Preview pane exists, toggles through the shared preview command, scroll-syncs to the source cursor, refreshes through Markdown edits, renders in TUI and GUI right docks, and now updates the preview model with the cursor's active block so live preview preserves editable raw source for the active block while inactive blocks remain rendered. Verified with `src/compiler_rust/target/bootstrap/simple check src/lib/editor/view/preview_pane.spl src/app/editor/md_dispatch.spl src/app/editor/tui_shell.spl src/app/editor/gui_shell.spl test/03_system/editor_markdown_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_markdown_spec.spl --mode=interpreter --fail-fast` (84 passed), `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed). |
| Inline visual editing | PASS | Active-block source preservation exists; live preview rendering now uses the active buffer to map the source cursor line into rendered output, activates the cursor's block, preserves raw Markdown for that active block, and highlights cursor/selection positions inside the rendered preview line while leaving surrounding blocks rendered. Behavior coverage verifies source-line-to-render-line mapping, scroll position, raw source preservation, cursor marker, and selection marker. Evidence: `src/compiler_rust/target/bootstrap/simple check src/lib/editor/view/preview_pane.spl src/app/editor/md_dispatch.spl src/app/editor/tui_shell.spl src/app/editor/gui_shell.spl test/03_system/editor_markdown_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_markdown_spec.spl --mode=interpreter --fail-fast` (84 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed). |
| Headings / outline | PASS | Outline and heading commands exist; `outline` opens the Markdown outline panel through command/palette routing, the right dock persists `md-outline`, `j`/`k` selection and Enter jump behavior are controller-tested against a real Markdown file, and LSP document symbols can hydrate the active Markdown outline. |
| Links | PASS | Markdown links, LSP definition, `open-link`, normal-mode `gx`, GUI link-click events, and GUI Ctrl+Click cursor hit testing can open file/relative links with behavior coverage; HTTP(S) links hand off through `SIMPLE_EDITOR_BROWSER_COMMAND`, `BROWSER`, or `xdg-open`, with configured-command behavior coverage. External opener failures now report the attempted command and exit code with controller behavior coverage. |
| Wiki links | PASS | `md_wiki_index` indexes `[[wikilink]]`, aliases, resolved/unresolved links, MCP can create notes for link targets, and `open-link`/`gx` plus GUI link-hit metadata resolve open-note wiki links by title. Unresolved wiki links create a heading-seeded Markdown note before opening it with behavior coverage, and controller-created notes update the vault search/index state immediately so the new note is discoverable without waiting for a watcher refresh. |
| Backlinks | PASS | `md_wiki_backlinks`, `editor.wiki_backlinks`, disk-backed `editor.wiki_vault_backlinks`, command/palette routing, renderable TUI/GUI right-dock panel rows, keyboard selection, selected-row jump, and GUI row click-through are behavior-tested. Controller wiki panels now merge fresh vault-search index documents from watched file changes, so backlink results update without reopening the changed source note. |
| Embeds / transclusion | PASS | `md_wiki_index` parses `![[note]]` embeds, resolution, graph edges, and open-note plus disk-backed vault MCP queries; full-line Obsidian embeds classify as rendered embed blocks with TUI/GUI behavior coverage, resolved note embeds can render target document content in wiki-aware TUI preview and GUI transclusion surfaces while stripping frontmatter, nested resolved note embeds render recursively with a depth cap, Markdown image embeds render actual GUI image preview markup, inline Markdown images inside mixed GUI paragraph content render as preview elements while preserving escaped surrounding text, attachment picker command/GUI paths list vault candidates and insert attachment embeds, and Quick Switch now exposes an Embed action that inserts `![[note]]` transclusions at the active cursor through the shared `wiki-embed-insert` command. Evidence: `simple check src/app/editor/commands.spl src/app/editor/editor_controller.spl src/app/editor/gui_shell.spl test/03_system/editor_controller_spec.spl test/03_system/editor_gui_spec.spl`, `simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed), `simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed). |
| Tables | PASS | Insert table command exists; GUI preview rendering emits structured editable table rows/cells with row/column edit controls; row/column insertion commands plus GUI event routing mutate the Markdown source; GUI table cell commits update the backing Markdown; table-cell navigation is command/palette/Tab covered for same-row next, previous, and cross-row separator-skipping next/previous movement; and the TUI renderer now highlights the active Markdown table cell from the cursor column. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/editor/tui_shell.spl test/03_system/editor_controller_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed). |
| Tasks | PASS | Toggle task plus `md_wiki_tasks`, status and text-query filtered task views, open-note MCP, disk-backed vault task queries, command/palette routing, renderable TUI/GUI right-dock task panel rows, GUI task checkbox rendering/toggle events, GUI task query/filter/count controls, active-buffer batch mark-done/open operations, and explicit vault-root task batch mark-done/open workflows with open-buffer refresh are behavior-tested. |
| Tags | PASS | Inline and frontmatter tags are indexed and exposed through open-note/disk-backed vault MCP queries plus command/palette-routed TUI/GUI right-dock tag panel rows; selected tag rows navigate to the source file and line with behavior coverage. Controller wiki panels now merge fresh vault-search index documents from watched file changes, so tag results update without reopening the changed note. |
| Properties/frontmatter | PASS | Frontmatter key/value indexing plus open-note and disk-backed vault MCP listing exist; `editor.wiki_set_property` can update YAML properties on disk; command/palette-routed TUI/GUI right-dock property panel rows plus active-note `wiki-property-set` editing are behavior-tested; property schema diagnostics report missing/duplicate/invalid values through the Problems panel and persist schema payload for GUI rendering; GUI property panels render schema-aware select controls for allowed values; `wiki-vault-property-review` previews affected vault notes before writing; and `wiki-vault-property-set` can bulk-set across a vault root or selectively apply to named files while refreshing open buffers. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/editor/commands.spl src/app/editor/editor_controller.spl src/app/editor/gui_shell.spl src/lib/editor/70.backend/gui_backend.spl test/03_system/editor_controller_spec.spl test/03_system/editor_gui_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed). |
| Callouts | PASS | Obsidian callout markers including folded `> [!TYPE]-` are indexed and exposed through open-note and disk-backed vault MCP queries; block parsing classifies callouts; TUI/GUI renderers show typed callout blocks; GUI callout markup includes type classes, fold state, source from/to lines, title text styling hooks, and per-body-line metadata for visual edit round-tripping; command/palette routing toggles the active callout source marker between folded and unfolded states while preserving body text exactly; and folded callouts hide body content in TUI/GUI rendering with GUI fold button metadata. Evidence: `simple check src/lib/editor/70.backend/gui_backend.spl test/03_system/editor_controller_spec.spl test/03_system/editor_gui_spec.spl`, `simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed), `simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed). |
| Graph view | PASS | Wiki graph edge model covers links/embeds for open notes and disk-backed vault roots, with command/palette-routed TUI/GUI right-dock graph panel rows, resolved graph rows target linked notes so panel selection jumps to the destination note instead of only the source, and GUI graph panels render clickable graph-node markup with deterministic spatial node/edge coordinates and layout metadata covered by GUI tests. |
| Vault search | PASS | Markdown search exists for one document, wiki MCP can crawl markdown vault roots for note lookup/graph/backlinks, vault-wide content search exposes MCP results plus a command/palette-routed right-dock panel, and the vault search index supports refresh/update/remove, watched-change invalidation, controller file-change updates, deterministic relevance ranking with score exposure in panels/MCP output, `.simple-vault-search.cache` save/load reuse from controller and MCP paths, persisted-cache freshness checks against current vault documents before reuse, `path:` plus `kind:`/`block:` result filters, GUI Vault Search kind-filter controls, and GUI path-filter controls backed by query-rewrite behavior tests. |
| Quick switcher | PASS | `md_wiki_quick_switch`, `editor.wiki_quick_switch`, disk-backed `editor.wiki_vault_quick_switch`, fuzzy-ranked query matching, command/palette-routed TUI/GUI right-dock quick-switch rows, selected-row jump, watched vault-search index invalidation, stored query state, empty-query all-notes listing, and GUI picker controls for filter input/count/all-notes behavior are covered by controller and GUI tests. |
| Templates | PASS | Template variable expansion for `{{title}}`, `{{date}}`, and caller-provided custom variables exists with MCP preview, template-folder listing, disk-backed insertion, command/palette routing, a wiki-panel template picker, query filtering, selected-row insertion, and GUI picker controls for query/title/date variables plus direct insert events are behavior-tested. |
| Daily notes | PASS | Daily-note path/content helpers, configurable folder/title-format/extension workspace options, MCP preview/create-open flow, command/palette create routing, selectable daily-note picker command, selected-row create behavior, and GUI date-picker form submission that creates/opens the requested note are behavior-tested. |
| Attachments | PASS | Wiki index tracks image/attachment embeds from `![[asset]]` and `![alt](path)` with open-note/disk-backed vault MCP exposure, plus MCP and command/palette insertion into notes; full-line image attachments classify as rendered embed blocks with TUI/GUI behavior coverage, Markdown image embeds render actual GUI image preview markup, inline mixed-content Markdown images render as GUI preview spans, command/palette attachment picker scans vault roots into selectable wiki panel rows with query filtering, selected picker rows can insert into supplied target notes, command/MCP insertion can copy source assets into note-local storage with deterministic conflict suffixing, and GUI picker controls for query/target/storage plus direct insert events are behavior-tested. |
| Plugin extension points | PASS | Extension host supports two-phase manifest loading, activation events, contribution registration, contexts, event subscriptions, workspace state, debug adapter lookup, and policy-gated runtime command dispatch; Markdown activation/command dispatch and wiki-style plugin command activation/runtime queue contracts are behavior-tested in `test/03_system/editor_extension_spec.spl`. |

## VSCode Debug Parity

| Feature | Status | Evidence / Required Work |
| --- | --- | --- |
| Debug adapter discovery | PASS | Simple adapter contribution is found after language activation. |
| Start debugging shortcut | PASS | `F5` routes to `debug-process-start`, creates controller debug session state, starts the Simple adapter through native stdio transport, sends `initialize`/`launch`, uses nearest `.vscode/launch.json` when present through `debug-start` and launch picker paths, TUI/GUI frame loops poll DAP messages, `keyboard-shortcuts` opens a right-dock discovery panel for default shortcuts, and the source-backed DAP adapter now supplies live launch/stop/step/evaluate/breakpoint behavior for the shortcut path. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/dap/simple_dap_main.spl src/app/editor/debug_process_command_smoke.spl test/03_system/dap_protocol_live_spec.spl test/03_system/editor_keybinding_spec.spl test/03_system/editor_gui_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_keybinding_spec.spl --mode=interpreter --fail-fast` (38 passed), `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed), `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed), and `python3 scripts/smoke/dap_protocol_smoke.py` (`STATUS: PASS dap_protocol_smoke`). |
| Launch configuration | PASS | `EditorDebugLaunchConfig` parses VSCode-style launch JSON, including args, `preLaunchTask`, string-valued `env`, and `compounds`; controller start discovers/selects the nearest matching `.vscode/launch.json` config; `${workspaceFolder}`/`${file}`/`${fileDirname}`/`${fileBasename}` substitution is behavior-tested across program/cwd/args/task/env; `debug-configs` opens a selectable panel of matching configs and compounds before launch; selected compounds start each referenced configuration as a separate debug session; and in-memory plus process-backed DAP launch helpers now hand args/task/env to the runtime adapter. Verified with `src/compiler_rust/target/bootstrap/simple check src/lib/editor/services/debug_session.spl src/app/editor/editor_controller.spl test/03_system/editor_debug_session_spec.spl test/03_system/editor_controller_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_debug_session_spec.spl --mode=interpreter --fail-fast` (9 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Breakpoints | PASS | Protocol smoke validates `setBreakpoints`, invalid lines are rejected, controller state tracks breakpoint metadata, the debug model builds DAP `setBreakpoints` requests with condition/log/hit metadata, controller breakpoint changes queue DAP client frames through the stable helper API, and the source-backed DAP adapter now enforces verified breakpoint stops with updated stack lines. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/dap/simple_dap_main.spl`, `python3 scripts/smoke/dap_protocol_smoke.py` (`STATUS: PASS dap_protocol_smoke`), `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Conditional breakpoints | PASS | Debug model/controller track condition, log message, and hit condition metadata; DAP requests serialize those fields; the source-backed adapter parses breakpoint objects, verifies invalid lines, reports condition/hit metadata in breakpoint responses, enforces simple expression conditions against in-scope values, and honors numeric hit conditions before stopping. Verified with `python3 scripts/smoke/dap_protocol_smoke.py` (`STATUS: PASS dap_protocol_smoke`) and `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed). |
| Continue / pause / step | PASS | Debug model/controller track continue, pause, and step commands; command/palette routing exposes continue, pause, step-over, step-into, and step-out with behavior coverage; the debug model builds DAP continue/pause/step requests; controller actions queue those request frames through the editor-side DAP client and write them to native stdio when `debug-process-start` is used; DAP `stopped`, `continued`, `terminated`, and `exited` events hydrate active debug status and the Debug Session panel; and the live source-backed adapter smoke proves `next` advances the reported stack line while `continue` emits continued, breakpoint, exception, and terminated events. Verified with `python3 scripts/smoke/dap_protocol_smoke.py`, `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Stack frames | PASS | Smoke stack exists; the debug model builds DAP `stackTrace` requests, parses DAP stack response source/name/line/column data, command/palette routing can queue stack requests through the editor-side DAP client, and the controller hydrates those precise locations into the Debug Session panel with behavior coverage. The live source-backed DAP adapter reports updated stack lines after launch, continue-to-breakpoint, step, and exception stops. Verified with `python3 scripts/smoke/dap_protocol_smoke.py`, `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Scopes / variables | PASS | Source-backed variables exist; the debug model builds DAP `scopes`/`variables` requests, command/palette routing can queue those requests through the editor-side DAP client, parses DAP scope/variable response bodies, and the controller hydrates those responses into the Debug Session panel with behavior coverage. The live source-backed adapter now scopes variables and evaluate results to the current stopped line so reported values follow the active stack position. Verified with `python3 scripts/smoke/dap_protocol_smoke.py`, `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Watch expressions | PASS | Debug model/controller track watch expressions; `debug-watch` command/palette routing adds watches with behavior coverage; a renderable `debug-console` Debug Session panel shows watch state; adding a watch queues a DAP `evaluate`; `evaluate` responses hydrate pending watch rows through controller state; and DAP `stopped` events automatically queue watch refreshes while preserving the stopped status. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/editor/editor_controller.spl test/03_system/editor_controller_spec.spl` and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed). |
| Debug console / REPL | PASS | Debug model/controller track evaluate requests and console transcript entries; `debug-evaluate` command/palette routing records evaluate requests and queues DAP client evaluate frames with behavior coverage; process-backed mode writes those frames to native stdio; the `debug-console` Debug Session panel shows console rows; DAP `evaluate` response bodies parse result text; controller hydration appends evaluate results to the Debug Session panel; and the source-backed adapter evaluates in-scope names at the current stopped line with unavailable-result handling. Verified with `python3 scripts/smoke/dap_protocol_smoke.py`, `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Exception breakpoints | PASS | Debug model/controller store exception breakpoint filters, command/palette routing can set filters with behavior coverage, the debug model builds DAP `setExceptionBreakpoints` requests, controller filter changes queue DAP client frames, and the Debug Session panel renders configured filters; the Simple DAP adapter advertises caught/uncaught filters; live protocol smoke covers `setExceptionBreakpoints`; and the source-backed adapter stops with `reason: exception` when execution reaches a Simple panic/raise line while exception filters are active. Verified with `python3 scripts/smoke/dap_protocol_smoke.py`, `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Restart / terminate | PASS | Disconnect/terminate smoke exists; controller can mark a session terminated, queue DAP `disconnect`, stop the editor-side DAP client/process binding, and restart by queuing `restart`, `initialize`, and `launch` frames; `debug-restart`/`debug-terminate` command routes update the active debug session lifecycle with model/controller behavior coverage; DAP terminated/exited events hydrate active session status; and the live source-backed adapter responds to `restart`, `terminate`, and `disconnect` with entry/terminated lifecycle events. Verified with `python3 scripts/smoke/dap_protocol_smoke.py`, `src/compiler_rust/target/bootstrap/simple test test/03_system/dap_protocol_live_spec.spl --mode=interpreter --fail-fast` (1 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed). |
| Multi-session debug | PASS | `EditorDebugRegistry` supports multiple sessions with IDs; `debug-sessions` opens a selectable Debug Sessions panel; Enter switches the active session; `debug-switch <id>` switches directly; the controller now keeps per-session DAP client bindings and restores the active DAP client when switching sessions; and compound launch configurations create separate debug sessions with separate DAP launch clients while preserving aggregate launch evidence for the picker flow. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/editor/editor_controller.spl src/lib/editor/services/debug_session.spl test/03_system/editor_controller_spec.spl test/03_system/editor_debug_session_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (92 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_debug_session_spec.spl --mode=interpreter --fail-fast` (9 passed). |

## LSP Parity

| Feature | Status | Evidence / Required Work |
| --- | --- | --- |
| Completion | PASS | Active LSP request and `Ctrl+Space` route covered. |
| Hover | PASS | Request path exists, normal-mode `K` triggers hover through the active LSP with default keybinding coverage, GUI source rows emit `editor-hover` metadata, active GUI source characters expose per-column hover targets, GUI hover events move the cursor and request LSP hover, repeated GUI hover over the already-visible popup position is debounced, async `textDocument/hover` responses hydrate into the shared TUI/GUI LSP results panel, controller state exposes a hover popup model at the source position, GUI rendering emits escaped hover popup markup, TUI rendering overlays a styled hover popup near the editor with behavior coverage, and `hover-dismiss`/Escape/GUI close events hide it with behavior coverage. `hover_delay_ms` is now configurable through editor config/settings schema, GUI render surfaces expose `data-hover-delay-ms`, GUI hover events with elapsed hover time are gated until the configured threshold, and decorated GUI source lines preserve per-column hover targets across selection, semantic token, inlay hint, and Markdown link render modes. Verified by `src/compiler_rust/target/debug/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast`, `src/compiler_rust/target/debug/simple test test/03_system/editor_settings_schema_spec.spl --mode=interpreter --fail-fast`, and `src/compiler_rust/target/debug/simple check src/lib/editor/00.common/config.spl src/lib/editor/00.common/settings_schema.spl src/lib/editor/70.backend/gui_backend.spl src/app/editor/gui_shell.spl`. |
| Go to definition | PASS | Request, `F12`, MCP dispatch, parsed location rows, TUI/GUI right-dock result panel, normal-mode result selection, selected-result jump command, frame-loop async response hydration, behavior-tested `lsp-back` location stack, and GUI LSP navigation history controls that render and dispatch back navigation are covered. |
| Diagnostics | PASS | Poll/store exists with command/palette-routed TUI/GUI right-dock Problems panel, normal-mode row selection, jump-to-diagnostic coverage, active-file stale diagnostic clearing on text change, tested severity-priority gutter markers wired into TUI single-pane, TUI split-pane, and GUI source render paths, LSP `publishDiagnostics` notifications replace diagnostics for the published URI, ignore older versioned publishes, and clear on empty arrays, `diagnostics-filter` narrows Problems rows by severity/source, `diagnostics-group` groups Problems rows by severity/source, `diagnostics-sort` sorts Problems rows by severity/source/path, GUI Problems filter/group/sort controls dispatch through those commands, selected Problems view is reapplied after diagnostic refreshes, and GUI Problems table rendering exposes severity/message/source/file/line cells with behavior coverage. |
| Code actions / quick fix | PASS | Markdown quick-fix apply plus active-document and multi-file LSP WorkspaceEdit application work; disk-backed sibling files are updated when not open, already-open sibling buffers receive the edit in memory without saving unrelated dirty state, command-only and edit-plus-command LSP code actions route through `workspace/executeCommand`, async LSP code-action responses hydrate into a selectable right-dock Code Actions panel, and behavior coverage exists for those paths. The Code Actions panel now supports all/quickfix/source filtering, kind grouping, GUI controls for those modes, and apply dispatch preserves the original code-action index after filtering/group rows. Verified by `src/compiler_rust/target/debug/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast`, `src/compiler_rust/target/debug/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast`, and `src/compiler_rust/target/debug/simple check src/app/editor/editor_controller.spl src/app/editor/gui_shell.spl src/lib/editor/00.common/config.spl src/lib/editor/00.common/settings_schema.spl src/lib/editor/70.backend/gui_backend.spl`. |
| References | PASS | Client, controller, command, `editor.lsp_references` MCP path, parsed location rows, TUI/GUI right-dock result panel, normal-mode result selection, selected-result jump command, frame-loop async response hydration, and GUI result-row rendering/click selection for result-list focus management are behavior-tested. |
| Selection ranges | PASS | `textDocument/selectionRange` client/controller/command/MCP request path exists, async responses hydrate into the shared TUI/GUI LSP results panel as nested selection rows, `selection-expand`/panel Enter apply the selected range into active buffer selection state, `selection-shrink` applies the previous returned range, and default `Shift+Alt+Right` / `Shift+Alt+Left` chords route through controller/GUI/SDL paths. Buffer state now supports multiple active selection ranges while preserving the existing primary `selection_range` compatibility field; active range helpers normalize, deduplicate, count, query, and render multiple ranges; LSP row activation updates the primary selection without dropping secondary selections; `selection-apply-all` applies all LSP selection rows as active selections; TUI/GUI renderers highlight all selected spans; and insert/delete/backspace replace or clear all active selections in right-to-left offset order to avoid coordinate drift. Evidence: `src/compiler_rust/target/debug/simple check src/lib/editor/10.buffer/buffer.spl src/app/editor/editor_controller.spl src/app/editor/commands.spl src/app/editor/tui_shell.spl src/lib/editor/70.backend/gui_backend.spl`; `src/compiler_rust/target/debug/simple test test/03_system/editor_buffer_spec.spl --mode=interpreter --fail-fast` (65 passed); `src/compiler_rust/target/debug/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (88 passed); `src/compiler_rust/target/debug/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (78 passed). |
| Rename | PASS | Client/controller/command/MCP paths exist for `textDocument/prepareRename` and `textDocument/rename`; `:rename <newName>` and palette payload routing request rename, async rename responses apply WorkspaceEdit results into the active buffer/disk-backed sibling files, `rename-preview <newName>` plus `rename-confirm` provide behavior-tested pending WorkspaceEdit review before mutation, shared WorkspaceEdit preview rows render before/after diff labels for rename edits, and missing-file rename edits are marked as conflicts with a GUI conflict badge before confirmation. Evidence: `simple check src/lib/editor/view/lsp_result_panel.spl src/app/editor/editor_controller.spl src/app/editor/gui_shell.spl test/03_system/editor_controller_spec.spl test/03_system/editor_gui_spec.spl`, `simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed), `simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (80 passed). |
| Document symbols | PASS | Client, controller, command, MCP request path, parsed symbol rows, TUI/GUI right-dock result panel, and frame-loop async response hydration exist. LSP document symbols now populate the existing outline panel for both Markdown and non-Markdown active documents; non-Markdown symbol hydration initializes outline state, preserves symbol nesting depth, switches the right dock to `md-outline`, and GUI frames render the resulting outline pane for Simple files. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/editor/editor_controller.spl src/app/editor/gui_shell.spl test/03_system/editor_controller_spec.spl test/03_system/editor_gui_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (89 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (79 passed). |
| Workspace symbols | PASS | Client/controller/command/MCP request path plus parsed symbol rows, TUI/GUI right-dock result panel, frame-loop async response hydration, and behavior-tested query payload routing exist. The controller now owns a workspace symbol index populated from an explicit workspace root rebuild and workspace-folder add notifications; `workspace-symbols <query>` still sends the normal LSP `workspace/symbol` request, then hydrates indexed cross-file matches immediately into the shared Workspace Symbols panel when available. Behavior coverage proves a query from an active file returns a matching symbol from a sibling file outside the active buffer with stable path/detail metadata. Verified with `src/compiler_rust/target/bootstrap/simple check src/app/editor/editor_controller.spl test/03_system/editor_controller_spec.spl` and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (90 passed). |
| Formatting | PASS | Document/range formatting request paths exist, async `textDocument/formatting` and `textDocument/rangeFormatting` TextEdit responses apply into the active buffer through the shared edit applier, `formatting-preview`/`range-formatting-preview` can review returned TextEdit results before confirmation, GUI preview panels expose Apply/Cancel controls through the shared WorkspaceEdit confirmation/cancel path, and explicit `formatting-save` / `range-formatting-save` commands save applied formatting edits to disk with behavior coverage. WorkspaceEdit preview rows now include explicit selected replacement ranges plus replacement text payloads, and GUI preview rows render side-by-side `diff-before` / `diff-after` blocks next to Apply/Cancel for formatting, range-formatting, rename, and generic WorkspaceEdit previews. Verified with `src/compiler_rust/target/bootstrap/simple check src/lib/editor/view/lsp_result_panel.spl src/app/editor/gui_shell.spl src/app/editor/editor_controller.spl test/03_system/editor_controller_spec.spl test/03_system/editor_gui_spec.spl`, `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (90 passed), and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (79 passed). |
| Semantic tokens | PASS | `textDocument/semanticTokens/full` request path exists, async responses decode LSP delta token data into the shared TUI/GUI LSP results panel with token legend/modifier names when supplied, token ranges apply to buffer state, GUI source rendering emits `semantic-token-*` spans, TUI rendering applies config-theme ANSI token styles with horizontal-scroll coverage, and edit-time invalidation is behavior-tested. Verified with `src/compiler_rust/target/bootstrap/simple check src/lib/editor/view/lsp_result_panel.spl src/app/editor/editor_controller.spl` and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (88 passed). |
| Inlay hints | PASS | `textDocument/inlayHint` request path exists, async responses hydrate into the shared TUI/GUI LSP results panel, hints are applied to buffer state, TUI/headless render paths include inline labels, GUI render paths emit styled `.inlay-hint` spans with click metadata, GUI inlay clicks move the cursor to the hint anchor, edit-time invalidation is behavior-tested, and edits schedule inlay hint refresh when hints or the inlay panel are active. Edit-triggered inlay refresh is now debounced and cancellable: controller state tracks pending refresh, elapsed time, and delay; repeated edits reset elapsed time and replace the pending refresh; explicit `inlay-hints` requests cancel pending refresh; TUI/GUI frame loops flush pending refreshes through `_poll_inlay_hint_refresh_after(100)`; and settings schema exposes `inlay_hint_refresh_delay_ms`. Evidence: `src/compiler_rust/target/debug/simple check src/app/editor/editor_controller.spl src/app/editor/tui_shell.spl src/app/editor/gui_shell.spl src/lib/editor/00.common/settings_schema.spl`; `src/compiler_rust/target/debug/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (88 passed); `src/compiler_rust/target/debug/simple test test/03_system/editor_settings_schema_spec.spl --mode=interpreter --fail-fast` (24 passed). |
| Signature help | PASS | Signature help request path exists, insert-mode `(`/`,` trigger characters request signature help after updating the buffer, async `textDocument/signatureHelp` responses hydrate all returned overloads into selectable shared TUI/GUI LSP result rows, `activeSignature` selects the initial overload, `activeParameter` highlights the active argument with fallback for inactive overloads, controller/palette commands `signature-next` and `signature-prev` wrap among overloads and refresh the popup label/detail/active parameter, LSP panel `j`/`k` selection refreshes the signature popup, GUI popup controls emit next/previous/dismiss events, TUI rendering overlays a styled signature popup with ANSI-highlighted active parameter, and `signature-dismiss`/Escape/GUI close events hide it. Evidence: `src/compiler_rust/target/debug/simple check src/lib/editor/view/lsp_result_panel.spl src/app/editor/editor_controller.spl src/app/editor/commands.spl src/app/editor/gui_shell.spl`; `src/compiler_rust/target/debug/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (88 passed); `src/compiler_rust/target/debug/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (78 passed). |
| Folding ranges | PASS | Markdown folding exists, `textDocument/foldingRange` request path is wired through client/controller/command/MCP, async responses hydrate into the shared TUI/GUI LSP results panel, returned ranges apply to buffer fold state, TUI/GUI/headless render paths skip folded interior lines, fold creation remaps cursors hidden by the new range back to the fold header, folded header lines show `+N folded | za` markers in TUI/headless output and GUI `.fold-badge` metadata, GUI fold badges dispatch `fold-toggle` events that unfold the exact represented range, normal-mode `za` toggles the current Markdown section fold with behavior coverage, fold ranges serialize/restore through buffer state, session DB tab records persist `folds=`, session restore reapplies them after reopen, and `fold-all`/`unfold-all`/`fold-toggle` plus Markdown `foldAll`/`unfoldAll` commands are behavior-tested. GUI source rendering now also marks foldable-but-open Markdown headers with `foldable-header` and a `fold-badge-open` button using the same `fold-toggle` event path and target end-line metadata, so folded and unfoldable visual states both have deterministic affordance coverage. Verified with `src/compiler_rust/target/bootstrap/simple check src/lib/editor/70.backend/gui_backend.spl src/app/editor/gui_shell.spl` and `src/compiler_rust/target/bootstrap/simple test test/03_system/editor_gui_spec.spl --mode=interpreter --fail-fast` (78 passed). |
| Document links | PASS | Document link request path exists, async `textDocument/documentLink` responses hydrate into the shared TUI/GUI LSP results panel with selectable targets, Markdown/wiki cursor hit testing opens local targets through `open-link`/`gx`, GUI Markdown rendering exposes link hit-test metadata, GUI link-click/Ctrl+Click paths open local targets, and HTTP(S) links hand off through `SIMPLE_EDITOR_BROWSER_COMMAND`, `BROWSER`, or `xdg-open` with configured-command and failure behavior coverage. |
| Code lens | PASS | `textDocument/codeLens` and `codeLens/resolve` request paths exist, async code lens and resolved code lens responses hydrate into the shared TUI/GUI LSP results panel with command metadata, single and multi-string command arguments plus structured object/array/primitive arguments are preserved in command payloads, result rows show argument summaries before execution, buffer state stores inline code-lens labels, TUI/GUI/headless render paths expose inline lens labels, GUI inline lens buttons expose `data-args`/title metadata and can execute commands, `code-lens-run` dispatches the selected lens command with behavior coverage, and GUI LSP result rows render command-specific argument form controls for command name/payload plus Run dispatch with GUI spec coverage. |
| Call hierarchy | PASS | Prepare, incoming, and outgoing call hierarchy request paths exist, async `textDocument/prepareCallHierarchy`, `callHierarchy/incomingCalls`, and `callHierarchy/outgoingCalls` responses hydrate into the shared TUI/GUI LSP results panel, incoming/outgoing rows preserve hierarchy depth and render indented direction-labeled rows, prepared call rows retain their original LSP item payload, selected root expansion requests incoming calls and merges returned children beneath the selected root in the Call Hierarchy panel, selected call rows expose direct incoming/outgoing request shortcuts, collapsed rows hide descendants, call-row expansion state persists per call-hierarchy panel across async refresh/reconstruction, and behavior coverage verifies mixed incoming/outgoing composition at nested depths. Evidence: `simple check test/03_system/editor_controller_spec.spl src/app/editor/editor_controller.spl src/lib/editor/view/lsp_result_panel.spl`, `simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (91 passed). |
| Workspace folders / file watching | PASS | File watcher surface exists, activated editor documents and non-broad parent directories are watched, TUI/GUI frame loops poll for external changes without clobbering unchanged status, changed open buffers refresh from disk, create/change/delete events are detected from file hashes plus directory snapshots, noisy `.git`, `node_modules`, and `.simple-vault-search.cache` paths are ignored by default, vault-search state is invalidated through the file-change path, and active LSP clients receive watched-file plus workspace-folder notifications with controller/frame-loop behavior coverage. File watcher policy is now configurable: `configure_policy` updates debounce interval and user ignore globs, watched directory snapshots are rebuilt under the new policy, custom globs are applied in addition to safe defaults, `check_events_debounced` gates polling by elapsed milliseconds, controller helper `_poll_file_watcher_after` applies debounced events through the same notification path, and desktop settings expose `file_watcher_debounce_ms` plus `file_watcher_ignore_globs`. Evidence: `src/compiler_rust/target/debug/simple check src/lib/editor/services/file_watcher.spl src/app/editor/editor_controller.spl src/lib/editor/00.common/settings_schema.spl`; `src/compiler_rust/target/debug/simple test test/03_system/editor_lsp_spec.spl --mode=interpreter --fail-fast` (18 passed); `src/compiler_rust/target/debug/simple test test/03_system/editor_settings_platform_spec.spl --mode=interpreter --fail-fast` (9 passed); `src/compiler_rust/target/debug/simple test test/03_system/editor_controller_spec.spl --mode=interpreter --fail-fast` (88 passed). |
