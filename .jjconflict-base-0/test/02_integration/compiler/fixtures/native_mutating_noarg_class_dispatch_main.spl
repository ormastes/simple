use native_mutating_noarg_class_owner.{NativeNoArgDispatchFixture}

fn main() -> i64:
    var instance = NativeNoArgDispatchFixture.create()
    val status = instance.initialize()
    if status != 0 or instance.value != 7:
        return 1
    0
