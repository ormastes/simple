# CI test: verify basic path normalization
# Simple normalization without imports

fn normalize(path: text) -> text:
    var result = path
    # Replace double slashes
    while result.contains("//"):
        result = result.replace("//", "/")
    result

val p = normalize("/usr//bin//local")
print p
