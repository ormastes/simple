#!/bin/sh
set -eu
mkdir -p "$BUILD_DIR"
{
  echo "metal_engine2d_framebuffer_readback_status=unavailable"
  echo "metal_engine2d_framebuffer_readback_reason=metal-requires-macos"
  echo "metal_engine2d_framebuffer_blur_or_tolerance_used=false"
} >"$BUILD_DIR/evidence.env"
