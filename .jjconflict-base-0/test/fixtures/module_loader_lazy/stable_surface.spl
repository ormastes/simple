"""Stable scanner fixture with a fixed declaration surface."""

fn stable_escape(s: text) -> text:
    """Stable scanner fixture: quote-heavy body remains local to this spec."""
    s.replace("\\", "\\\\").replace("\"", "\\\"")

fn stable_identity(value: i64) -> i64:
    value
