# Adds one
fn add_one(value: i64) -> i64:
    value + 1

# Shared data
struct SharedState:
    count: i64

# Handles work
class Worker:
    me run():
        nil

# Outcome values
enum Outcome:
    Ok
    Err
