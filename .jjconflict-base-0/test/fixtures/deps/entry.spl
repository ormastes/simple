# Entry point fixture for deps tool tests
# Direct imports: module_a (-> common, cycle_a->cycle_b->cycle_c->cycle_a) and module_b (-> common)
# common.spl is shared between module_a and module_b
use .module_a.{module_a_fn}
use .module_b.{module_b_fn}

fn entry_fn() -> text:
    "entry"
