# Fixed compiler-admission oracle: parsing, function emission, call, arithmetic.
fn mci_admission_add(left: i64, right: i64) -> i64:
    left + right

fn main():
    print "mci-compiler-admission:" + mci_admission_add(19, 22).to_text()
