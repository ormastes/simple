struct ImportedAggregatePoint:
    left: i64
    right: i64

fn make_point(seed: i64) -> ImportedAggregatePoint:
    ImportedAggregatePoint(left: seed, right: seed + 1)

export ImportedAggregatePoint, make_point
