# BAD INPUT on purpose: reads an undefined variable. The interpreter
# diagnoses (`variable 'undefined_var_xyz' not found`); the seed JIT
# currently FAILS OPEN and prints OK — a silent wrong answer.
fn main():
    var y = undefined_var_xyz
    print("SHOULD_NOT_PRINT")
main()
