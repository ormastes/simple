# Fixture: struct + impl living in a DIFFERENT module than the trait it
# implements (xmod_trait_def.spl). Overrides only farewell; greet should
# dispatch through the trait's default body across the module boundary.

use test.fixtures.cross_module_trait_default.xmod_trait_def.{XModDftGreeter}

struct XModDftTarget:
    n: i64

impl XModDftGreeter for XModDftTarget:
    fn farewell() -> text:
        "override-farewell"

export XModDftTarget
