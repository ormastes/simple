# Soundness corpus: generic identity function over two instantiations.
# expected stdout:
# 7
# gg
fn id<T>(x: T) -> T:
    x

fn main():
    print(id(7))      # 7   (i64 instantiation)
    print(id("gg"))   # gg  (text instantiation)
