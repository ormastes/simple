# OPEN DEFECT repro (NOT gated by the runner). See README.md.
# `add/2` called with 1 argument SHOULD be rejected (TOR-FM-02). On the
# deployed binary it is silently ACCEPTED: prints 1, exit 0.
fn add(a: i64, b: i64) -> i64:
    a + b

fn main():
    print(add(1))
