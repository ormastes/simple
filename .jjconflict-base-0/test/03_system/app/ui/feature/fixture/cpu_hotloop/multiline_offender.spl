# Fixture: a multi-line parenthesized loop condition — the `:` is on the
# continuation line, not the `while` line. Must still be flagged LOOP (new=1).
fn scan(a: i32, b: i32, n: i32):
    var i = 0
    while (i < n and
           a < b):
        i = i + 1
