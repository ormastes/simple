# Purpose and audience: executable specification evidence for the owning engineering team.
# @req REQ-SPKC-011
# research: doc/01_research/domain/sspec_documentization_maintenance.md ; plan: doc/03_plan/sspec_modernization_plan.md ; architecture: doc/04_architecture/sspec_documentization_maintenance.md ; design: doc/05_design/infra/sspec/modern_sspec_typed_evidence_design.md

# codex-system-test
# @evidence-display: links
# FAIL-FAST GATE: the canonical evidence consumer is closed, but producers are incomplete.
# Requirements: REQ-SPKC-011
# Matrix: W4-SRCH-01..27
use std.spec.step

use std.spec.*

"""
# SPipe knowledge-compiler provider parity

This executable manual owns the Wave 4 provider-parity acceptance surface.
Every scenario invokes the same locked `check_spipe_provider_parity` gate.
The JavaScript provider and focused Simple/DBFS production fixtures execute.
The Node acceptance runner rejects a successful spec verdict unless the fixture
also emits exactly one `SPIPE_WAVE4_CONFORMANCE=` JSON record conforming to
`conformance_evidence_schema.json`. Simple and DBFS do not yet emit canonical five-field roots,
scores, statistics, explanations, and deltas for every applicable matrix row.
Until they do, the gate fails with `NOT-EVIDENCE`; source presence and a
narrower green fixture are never provider-parity evidence.

Fixture oracle:
`examples/05_stdlib/spipe/test/fixture/wave4_search/fixture_manifest.json`.
"""

describe "SPipe knowledge compiler provider parity":
    # @manual scenario evidence
    it "should return identical golden ordering and scores across fallback and Simple providers":
        # @req: REQ-SPKC-011, REQ-SPKC-012, REQ-SPKC-013
        step("Search and trace artifacts")
        check_spipe_provider_parity()

    it "should keep exact identity dominant and break lexical ties by public document ID":
        # @req: REQ-SPKC-011, REQ-SPKC-012
        step("Search and trace artifacts")
        check_spipe_provider_parity()

    it "should reject phrase queries and apply metadata equality filters identically in version 1":
        # @req: REQ-SPKC-011, REQ-SPKC-012
        step("Search and trace artifacts")
        check_spipe_provider_parity()

    it "should return bounded canonical explanations for every ranked hit":
        # @req: REQ-SPKC-013
        step("Search and trace artifacts")
        check_spipe_provider_parity()

    it "should make mixed incremental deltas equal a clean rebuilt snapshot":
        # @req: REQ-SPKC-012, REQ-SPKC-014
        step("Search and trace artifacts")
        check_spipe_provider_parity()

    it "should degrade process and semantic provider failures without a false semantic pass":
        # @req: REQ-SPKC-013, REQ-SPKC-015, REQ-SPKC-016, REQ-SPKC-017, REQ-SPKC-018
        step("Search and trace artifacts")
        check_spipe_provider_parity()

    it "should reject every query and response resource boundary at limit plus one":
        # @req: REQ-SPKC-011, REQ-SPKC-012, REQ-SPKC-013
        step("Search and trace artifacts")
        check_spipe_provider_parity()

    it "should meet qualified warm-query and incremental-update latency gates":
        # @req: REQ-SPKC-011, REQ-SPKC-012, REQ-SPKC-014
        step("Search and trace artifacts")
        check_spipe_provider_parity()

fn check_spipe_provider_parity():
    fail("NOT-EVIDENCE: npm test:wave4-conformance requires one closed SPIPE_WAVE4_CONFORMANCE record from each executable producer with actual roots/scores/order/stats/explanations/deltas")
