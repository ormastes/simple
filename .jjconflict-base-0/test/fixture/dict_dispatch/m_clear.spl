# Fixture: Dict.clear() must route to rt_dict_clear, not str.clear.
fn main():
    var d: Dict<i64, i64> = {}
    d[1] = 1
    d[2] = 2
    d.clear()
    print("dict_dispatch: method=clear ok=1 len=" + d.len().to_text())
