# Fixture for test/01_unit/compiler/interpreter_extern/capability_gap_spec.spl.
# Calls a real rt_webgpu_* extern (declared in
# src/lib/nogc_sync_mut/gpu/engine2d/webgpu_ffi.spl) that has no entry in
# EXTERN_DISPATCH and no native implementation anywhere (M3 capability gap),
# so the parent spec can capture the structured capability-gap error text
# from a child process under the interpreter.

extern fn rt_webgpu_adapter_count() -> i64

fn main() -> i64:
    val n = rt_webgpu_adapter_count()
    print "unexpected success: rt_webgpu_adapter_count={n}"
    0
