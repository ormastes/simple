(x25519mlkem768_three_oracle_manifest
  format_version: 6
  status: "active-single-external-oracle"
  fixture: "test/fixtures/crypto/x25519mlkem768/mlkem_native_fd58_vectors.sdn"
  scope: "one independently maintained external implementation plus a local implementation and a pinned fixture"
  primary: "Pure-Simple pinned-vector implementation (local, not independent external evidence)"
  secondary: "pq-code-package/mlkem-native fd58 vector fixture (pinned test data, not an executable oracle)"
  external_independent_implementation_count: 1
  legacy_filename: "three_oracle is retained for stable references; it does not claim three independent external implementations"
  independent: (name: "Cloudflare CIRCL" license: "BSD-3-Clause" version: "v1.6.4"
    runner: "test/oracle/x25519mlkem768_circl/run.sh"
    scope: "full EK, DK, CT, shared-secret byte equality and decapsulation")
  policy: "offline only; an unavailable cached module, exact local toolchain, or readonly dependency graph is a hard failure")
