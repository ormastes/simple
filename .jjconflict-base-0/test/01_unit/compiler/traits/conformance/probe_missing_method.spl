# FIXTURE (not a spec). Deliberately ill-formed: the impl omits the required
# trait method `farewell` entirely. A conforming compiler must reject this file.

trait Greeter:
    fn greet(name: text) -> text
    fn farewell(name: text) -> text

struct Partial:
    tag: text

impl Greeter for Partial:
    fn greet(name: text) -> text:
        "hi " + name

fn main():
    val p = Partial(tag: "x")
    print(p.greet("bob"))
    print("FIXTURE_RAN_MISSING_METHOD")
