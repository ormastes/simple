use std.spec.*
use os.kernel.memory.vmm.{PAGE_SIZE, VMM_MAX_VMAS}

describe "VMM native facade imports":
    it "resolves core constants through the public facade":
        expect(PAGE_SIZE).to_equal(4096)
        expect(VMM_MAX_VMAS).to_equal(256)
