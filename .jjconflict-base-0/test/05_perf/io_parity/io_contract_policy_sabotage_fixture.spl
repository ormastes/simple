use std.spec.*
use io_contract_policy.{io_contract_ok}

describe "I/O contract mutation sentinel":
    it "IO_PARITY_MUTATION_MUST_FAIL rejects a changed partial length":
        print("IO_PARITY_MUTATION_SENTINEL")
        expect(io_contract_ok(true, true, 11, 0, true, true)).to_be(true)
