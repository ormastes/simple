# Network module for async + gc + immutable variant
# Functional style networking with immutable data structures

#[variant(platform_host, async, gc, immutable)]
mod net

export use tcp.*
export use udp.*
export use http.*
