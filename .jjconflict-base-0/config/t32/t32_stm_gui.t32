; TRACE32 visible GUI config for STM32 via Power Debug II USB
; Use with the X11-enabled container helper when a visible PowerView session is needed.

OS=
SYS=/opt/t32
TMP=/tmp

PBI=
USB

RCL=
NETTCP
PORT=20000
