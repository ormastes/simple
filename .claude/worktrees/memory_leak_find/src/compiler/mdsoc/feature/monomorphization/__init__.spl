# Monomorphization Feature Module
#
# Pipeline stage: Typed AST with generics → Monomorphic AST
# Layer: app
# Dimension: feature

arch {
  dimension = "feature"
  layer = "app"

  imports {
    allow = [
      "core/entity/ast/**",
      "core/entity/hir/**",
      "shared/**"
    ]
    deny = [
      "compiler/feature/type_checking/**",
      "compiler/feature/hir_lowering/**",
      "compiler/feature/mir_lowering/**"
    ]
  }

  exports {
    expose = ["./app/ports"]
  }
}
