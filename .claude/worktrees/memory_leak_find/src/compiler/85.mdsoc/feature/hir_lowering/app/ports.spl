# HIR Lowering Stage Ports
#
# Input/output port contracts for the HIR lowering pipeline stage.
# HIR lowering converts a typed AST into HIR (High-level IR).

struct HirLowerInputPort:
    """Typed AST context from the typing_to_hir transform."""
    module_name: text
    typed_decl_count: i64
    symbol_count: i64

struct HirFunction:
    """A HIR-level function definition."""
    name: text
    param_count: i64
    return_type: i64
    body_inst_count: i64

struct HirLowerOutputPort:
    """HIR module produced by lowering."""
    functions: [HirFunction]
    struct_count: i64
    error_count: i64

export HirLowerInputPort, HirFunction, HirLowerOutputPort
