# GPU Runtime Module (NoGC)
#
# GPU runtime support for executing compute kernels.
# Integrates with PyTorch and native GPU backends.
# NoGC variant — no borrowed-view pattern, direct FFI calls.
#
# Public API:
#   - use std.nogc_sync_mut.gpu_runtime.mod.{gpu_available, gpu_tensor_zeros}

# All submodules are automatically available.
