"""
# MIR Enum Access Specification

**Feature IDs:** #TBD
**Category:** Compiler
**Status:** In Progress

## Overview

Tests that MIR and HIR enum variants can be accessed as values.
"""

use std.spec
use compiler.hir_types.{SymbolKind}
use compiler.mir_data.{MirTypeKind}


describe "MIR Enum Access":
    it "accesses MirTypeKind.Unit":
        val v = MirTypeKind.Unit
        expect(v != nil).to_equal(true)

    it "accesses SymbolKind.Variable":
        val v = SymbolKind.Variable
        expect(v != nil).to_equal(true)
