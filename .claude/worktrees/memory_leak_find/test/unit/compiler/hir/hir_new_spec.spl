"""
# HIR Lowering Initialization Specification

**Feature IDs:** #TBD
**Category:** Compiler
**Status:** In Progress

## Overview

Tests that HirLowering can be created with no initial errors.
"""

use std.spec
use compiler.hir_lowering.*


describe "HirLowering Creation":
    it "creates HirLowering with no errors":
        var lowering = HirLowering.new()
        expect(lowering.errors.len()).to_equal(0)
