# Fully Documented Module - 100% Coverage
#
# This module demonstrates perfect documentation coverage.
# All functions have inline comments, docstrings, and sdoctests.

# Calculate the sum of two numbers
fn add(a: i64, b: i64) -> i64:
    """
    Adds two integers and returns the result.

    sdoctest:
        val result = add(2, 3)
        expect(result).to_equal(5)
    """
    a + b

# Multiply two numbers
fn multiply(x: i64, y: i64) -> i64:
    """
    Multiplies two integers.

    sdoctest:
        val product = multiply(4, 5)
        expect(product).to_equal(20)
    """
    x * y

# A well-documented struct
struct Point:
    """
    Represents a 2D point with x and y coordinates.
    """
    x: i64
    y: i64

export add, multiply, Point
