<!-- codex-design -->
# Stage4 re-export resolver NFR options

All options must preserve semantic results for positive and negative re-export
lookups. For the R6 representative `compiler.10.frontend.core.__init__`
façade, the second identical resolution pass must add zero graph traversals;
the test harness must expose a counter. A failed lookup must not grow the
active-path set after return. The true Stage4 full-CLI build remains the
release evidence gate.
