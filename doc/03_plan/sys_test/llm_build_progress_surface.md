# System Test Plan: LLM Build Progress

The executable SPipe specification verifies full-record round trip, stale writer rejection, sequence/time monotonicity, terminal handoff to a new build, and concise rendering. Integration qualification additionally runs native-build with centralized roots and observes `current.sdn` while retaining the event log.

