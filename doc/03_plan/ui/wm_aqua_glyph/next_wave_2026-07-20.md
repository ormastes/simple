# Next-wave plan: SimpleOS WM / theme / 2D interactivity (post Aqua+glyph campaign)

Date: 2026-07-20. Source: the 2026-07-19/20 campaign (glyph text visible under
real firmware, Aqua theme + shared gradient landed, CSS-file theming landed,
first 2D spatial API landed). Each item: Research (evidence we hold), Change
(what to do), Verdict (readiness/priority).

## Summary table

| # | Item | Verdict |
|---|------|---------|
| 1 | WM mouse routing via compositor_pick_topmost | READY — next 2D tranche |
| 2 | Theme single-source unification (tokens -> defaults) | READY — refactor, value-identical |
| 3 | Guest /theme.css wiring land + gradient on-screen proof | BLOCKED-SHORT — needs one clean-boot window |
| 4 | Browser launch from FS + page render | IN-FLIGHT — lane owns VFS experiment |
| 5 | Window title text not rendered | READY — probe recipe in bug doc |
| 6 | 2D backend coverage deepening | PARTIAL — Vulkan/lavapipe first, SIMD wiring |
| 7 | P1 framebuffer goldens | BLOCKED — kernel bug filed (HHDM) |
| 8 | Compiler root fixes (retire workarounds) | READY — seed work, bootstrap-validated |
| 9 | Infra: self-hosted redeploy + daemon wall | PREREQ for all spec-run verification |
| 10 | Physical-board evidence | BLOCKED-HW — needs a real board session |

## 1. WM mouse-event routing (2D interactivity, tranche 2)
Research: `compositor_pick_topmost`/`layer_rects_overlap` landed (e033a95fe7c,
probe 38/38). Audit (cae7285842578) found NO spatial mouse routing anywhere:
wm_service/wm_world/wm_action do id/port bookkeeping only; Compositor is
orphaned from the live Engine2D facade.
Change: wire pointer events (the WM event gate path proven 2026-07-11) to
pick_topmost against the window/layer set; click -> focus/raise + deliver to
owner; add hover/press/release spec tranche mirroring the landed test style.
Verdict: READY. Highest-leverage interactivity item; API + tests exist.

## 2. Theme single-source unification
Research: 4 theme modules (wm_chrome_theme, glass/theme, glass/numeric_tokens,
theme_registry[widget], ios/theme). wm_chrome_theme_defaults() MIRRORS
AQUA_LIGHT_* by copy (comment admits it); values currently match (verified
2026-07-20) — drift is one edit away. CSS-file theming (39a45034d07,
9351608392d) consumes the defaults at runtime.
Change: derive wm_chrome_theme_defaults() FROM AQUA_LIGHT_* tokens (u64
0x00RRGGBB + alpha -> u32 helper), keep field order and byte-identical
values (clock-region sha 9512f562 must not change); fold glass aqua_light()
to the same tokens; document token->role mapping in the wiki entry.
Verdict: READY. Pure refactor with pinned-sha invariance as the gate.

## 3. Guest CSS theme + gradient on-screen proof
Research: guest /theme.css wiring written + build-verified (667/0, symbols
linked) but held; every boot from the shared WC currently faults inside the
in-flight VFS mount experiment (confounded isolation test documented — NOT a
mainline regression). Shared titlebar gradient is landed on the live MDI
chrome but no post-land boot has captured it yet. A dependency-free FAT32
injector (scratchpad fat32_inject_root_file.py) exists for theme disks.
Change: once the VFS lane settles: clean-boot A/B (aqua vs slate disk),
pixel tables for desktop/titlebar/body, land guest wiring, capture gradient
shade count (expect ~27 in titlebar strip vs flat 6), refresh clock pin only
if the region actually changes.
Verdict: BLOCKED-SHORT. Hours-scale once the WC quiets; all assets staged.

## 4. Browser from FS + page render
Research: mount skip guard = blockdevice-dispatch-codegen-bug; three seed
codegen defects fixed 2026-07-19 may have cured the root (hypothesis under
live test by the opus lane, which owns the current boot fault). Fallback
truth: readiness marker overstates web state (bug filed b69e5469531);
browser_demo frozen-placeholder bug (2026-07-12) caps the ladder at chrome
until fixed.
Change: per lane outcome — either remove the guard (bug genuinely fixed,
mount verified) or root-fix dispatch; build app disk; climb mount ->
materialize -> chrome -> page-content with per-rung evidence; make the
readiness line state-computed.
Verdict: IN-FLIGHT. Do not double-assign; review the lane's report.

## 5. Window title text
Research: bug 23713c829db — all 512 text px are X/labels/clock; title
contributes zero while sibling calls on the same path render. Suspects:
title-fit max_chars math, title string integrity (boxing landmine family),
or draw-order/clip.
Change: one gated probe boot (title.len(), first bytes, max_chars, draw
entry) then a boring-construct fix; regression = titlebar band gains dark
pixels left of the close rect.
Verdict: READY. Small; fold into the first clean-boot cycle alongside #3.

## 6. 2D backend coverage
Research: audit truth-table (cae7285842578): software=full raster ground
truth; cpu_simd = dishonest alias (open bug 2026-05-31, externs unwired);
vulkan largest real backend, lavapipe headless is the one real-device path
on this host; metal/directx/cuda/rocm honest ceiling = contract tests;
dual Engine2D classes (stub fakes success); test-dir triplication.
Change (ordered): (a) wire cpu_simd externs or rename the alias honestly;
(b) Vulkan lavapipe device tranche (raster golden vs software backend);
(c) consolidate/deprecate the stub Engine2D facade; (d) collapse test dirs;
(e) CI hardware for metal/directx or mark contract-only in docs.
Verdict: PARTIAL. (a)+(b) are actionable now; (e) needs infra decisions.

## 7. P1 framebuffer goldens
Research: generator desktop_e2e_entry crashes (paging _alloc_table_page, no
Limine HHDM — bug filed 2026-07-20); spec baseline path mismatch; stand-in
golden rejected at review (goldens change only via their own generator).
Change: fix the HHDM/paging path (or the entry's boot protocol), fix the
spec path, regenerate both goldens via UPDATE_BASELINE, close task #6.
Verdict: BLOCKED on the kernel fix; recipe fully documented in the READMEs.

## 8. Compiler root fixes (retire the workarounds)
Research: 12 bugs filed this campaign; fixed in seed: char_code_at untag,
enum-eq discriminant (improved upstream to typed PatternTest). Open with
landed workarounds: for-in-text corrupted bindings (repro probe landed),
zero-arg receiver not marshalled (rocm sibling latent), class-default text
fields not applied (transform_identity), module-init text globals, tuple
spill clobber, string-interp verbatim, LogLevel -kernel frames.
Change: fix each in seed with the filed repro as the regression test;
bootstrap-validate; then sweep out the call-site workarounds (grep the bug
IDs in comments) in a follow-up so canonical code returns.
Verdict: READY as a dedicated seed campaign; highest long-term payoff.

## 9. Infra prerequisite: self-hosted redeploy
Research: test daemon wall systemic (control spec proves it); seed trim
recursion blocks bin/simple run on browser_engine graph; bin/simple was
observed swapped to a broken seed mid-session by a parallel lane.
Change: land the self-hosted redeploy (memory: redeploy wall notes), then
re-run every spec deferred this campaign (theme, vector-font, e2d tranche,
CSS parser) and convert DAEMON-TIMEOUT rows to real verdicts.
Verdict: PREREQ. Every "runs blocked" row in this plan resolves through it.

## 10. Physical-board evidence
Research: OVMF pflash proxy fully green (gate landed + 2G readiness fix
8eb968a3799); board-runnable.md requires board identity + real serial/SSH
transcript — impossible from this environment.
Change: board session: boot the same BOOTX64.EFI + NVMe layout, capture
serial transcript, add the board-evidence row to the gate doc.
Verdict: BLOCKED-HW. Explicit, tracked, not glossed.

## Process notes (carried lessons)
- Serialize QEMU lanes: shared harness paths (QMP/serial/elf) clobber under
  parallel boots; one QEMU owner per round.
- Land via plumbing onto fetch-fresh tips; verify origin CONTENT after every
  push; goldens change only via their own generators.
- Small-model lanes + coordinator review caught 3 overclaims this campaign
  (colors=14 "glyphs", byte-order desktop false alarm, coincidental-cancel
  fix) — keep the independent-verification step.
