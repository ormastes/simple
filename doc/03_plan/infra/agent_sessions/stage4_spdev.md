# Lane: stage4 / $sp_dev remake plan (ex-codex 019f9c04)
Goal: "$sp_dev remake plan; do all item tasks in parallel."
Last state: found active stage4 native-build from previous attempt; patching parser-sensitive helper forms to explicit match/if+return to avoid parser ambiguity.
Next: re-check stage4 build result; land parser-sensitive form patches; re-derive remaining plan items ($sp_dev).
