# Parser Framework Agent Tasks — TLDR

Interfaces are frozen in `doc/05_design/parser_framework.md`. Root Codex is merge owner and final reviewer.

```text
A contracts/model -> B scalar/runtime/Simple adapter -> C SIMD -> D GPU + E incremental/auto -> G evidence
```

- A owns common immutable identities, segmented model, output plans.
- B owns default-tier sink/runtime/scalar and the single Simple grammar cutover.
- C owns structural-index boundary parity.
- D owns bounded total-table GPU count/scan/emit through existing GPU/MMU owners.
- E owns edit lineage, stabilization, segment reuse, mappings/invalidation, measured auto.
- G owns four focused specs, generated manual, N2 evidence, guide freshness.
- Agents never edit unrelated dirty files; placeholders fail explicitly.
