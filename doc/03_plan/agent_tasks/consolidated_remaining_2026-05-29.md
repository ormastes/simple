# Consolidated Remaining Work Plan — 2026-05-29

## Completed and Closed

| Track | Status | Evidence |
|-------|--------|----------|
| JIT hotspot interpreter cost | Fixed | `doc/08_tracking/bug/jit_hotspot_optimization_plan_interpreter_cost_2026-05-28.md` |
| JIT hotspot shared-policy plan | Fixed | `doc/08_tracking/bug/jit_hotspot_shared_policy_plan_cost_2026-05-28.md` |
| Predicate-policy SSA/escape analysis | Merged | 19 commits cherry-picked to main (suppress zero-count reassignment → escape analysis gating) |
| Web baremetal size restart candidates | Done | 10 commits merged; audit at `doc/09_report/web_baremetal_size_audit_2026-05-28.md` |
| Crash-safe 24h guardrails | Superseded | Operational wisdom preserved; no active items |

## Track 1 — JIT Hotspot Plugin (Next Phase)

Source: `doc/03_plan/agent_tasks/optimization_plugin_jit_hotspot.md`

Remaining:

1. Add native backend specialization benchmarks after a concrete JIT hotspot
   provider consumes `JitHotspotPlan`.
2. Add automatic MIR-level specialization providers once semantic proof is
   generated by analysis instead of supplied explicitly.

Prerequisites: tiered JIT compile-decision consumer and opt-in hotspot
specialization providers (both landed).

## Track 2 — Security Convention-First Architecture (Next Phase)

Source: `doc/03_plan/agent_tasks/security_convention_first_architecture.md`

Remaining:

1. Add remote `SecurityContext` key rotation, persistent session lookup,
   refresh, and revocation.
2. Enforce lowered sandbox plans in runtime/kernel/VM backends.
3. Run all HTTP/event-loop handler execution under scheduler/fiber-owned
   task-id propagation (not worker/fd fallback).
4. Provider-side GitHub environment/OIDC trust setup (deployment-specific).

Prerequisites: convention-first coordinate inference, SEC101/SEC501, sandbox
lowering, and HMAC-signed bearer validation (all landed).

## Track 3 — Workspace Root Write Guard (Not Started)

Source: `doc/03_plan/agent_tasks/workspace_root_write_guard.md`

Six phases — none started:

1. **Policy and Audit** — Parse `FILE.md` for allowed root entries, implement
   audit + dry-run output, add tests with synthetic workspaces.
2. **Fix/Quarantine** — `--fix` mode moves violations to
   `build/workspace_quarantine/<timestamp>/`, write manifest.
3. **Lint Integration** — Wire audit into lint, emit stable diagnostics,
   fail lint on misplaced root entries.
4. **Commit Hook** — Block commits that introduce root-policy violations.
5. **Lock/Unlock** — Runtime freeze/unfreeze for workspace root policy.
6. **Tool-Server Protection** — Prevent MCP/LSP tool-servers from writing
   outside allowed paths.

Recommended build order: phases 1–3 first, then 4, then 5, then 6.

## Track 4 — Web & Baremetal Size (Stabilization)

Source: `doc/03_plan/agent_tasks/web_baremetal_size_restart_2026-05-28.md`

The three restart candidates (script-render closure, baremetal semihost/root
API closure, ARM interrupt-control sharing) are done and verified. Remaining
stabilization:

1. Re-run full size audit gate (`scripts/check-web-baremetal-size-audit.shs`)
   after the 29-commit merge to confirm no regression.
2. Verify web rendering, SimpleOS/baremetal examples, semihost stdout, startup,
   and interrupt handling are still minimized and covered by stable gates.
3. Push evidence to origin/main after verification passes.
