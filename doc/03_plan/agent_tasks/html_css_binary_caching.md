<!-- codex-research -->

# HTML/CSS Binary Caching Agent Tasks

1. Add shared render cache/profile helpers to `src/lib/common/ui/web_render_api.spl`.
2. Update shared web render unit coverage for static/dynamic classification and IPC full-HTML reuse.
3. Add GTK size/speed comparison script with graceful unavailable reporting.
4. Run targeted checks and record GTK comparison output.
5. Follow-up: design persistent binary artifact storage and retained scene invalidation once this API slice is stable.
