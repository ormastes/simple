# Simple Language Production Audit

Date: 2026-05-13

## Objective Restatement

Make Simple production-grade outside the library parity lane: keep docs current,
make the directory/file structure clean, prevent generated clutter, define a
Ruby-like short but expressive public API style, and standardize error/warning
messages.

## Prompt-to-Artifact Checklist

| Prompt requirement | Evidence | Status |
| --- | --- | --- |
| Production-level language posture | This audit maps the broad objective to verifiable deliverables. | Partial |
| Libraries handled in other agents | `doc/03_plan/gc_nogc_runtime_completion_audit_2026-05-13.md` records the current library parity closure and remaining compiler/noalloc gaps. | Delegated/covered elsewhere |
| Make docs | API placeholder pages replaced with production contracts: naming, Ruby-inspired features, errors. | Complete for this slice |
| Directory/file structure cleanup | `doc/04_architecture/file_class_structure.md` remains the main inventory; `scripts/audit/repo_hygiene_policy.json` defines source roots and generated-path policy; `scripts/audit/repo_hygiene_audit.py` adds a repeatable clutter/empty-dir gate; four empty source directories were removed. | Complete for this slice |
| Prevent dirty generated files | Hygiene policy and script check unignored caches/temp files and empty source dirs without deleting files. | Complete for this slice |
| API consistency | `doc/01_research/local/api_design/naming.md` defines canonical verbs, conversion prefixes, predicate policy, alias policy, diagnostic field names, and the predicate migration gate; `scripts/audit/api_consistency_audit.py` checks public-ish function/method declarations against hard-rejected names and fails if predicate-prefix debt exceeds `scripts/audit/api_consistency_baseline.json`. | Complete for this slice |
| Ruby-like short expressive API | `doc/01_research/local/api_design/ruby_features.md` defines which Ruby ergonomics Simple adopts and rejects. | Complete for this slice |
| Error/warning messages | `doc/01_research/local/api_design/errors.md` defines diagnostic shape, code ranges, message style, Result policy, and warning promotion policy; query diagnostics now use `E0001`/`E1003`, cycle soft warnings use `W0420`, async lowering diagnostics moved to `E0801-E0810` so they no longer overload VHDL `E0701`/`E0710`, `simple check` preserves parser diagnostic codes/help, reports basic annotation/literal and return/literal mismatches as `E1003`, and keeps warning-only status/help in text and JSON output without warning on normal `std` namespace imports; `simple check` also borrows the workspace resolver for check-only import validation, resolves numbered-layer alias children in intermediate segments, explicit `NN.layer` path segments, relative dot imports, compiler-root legacy bare imports, `compiler_shared.interpreter.*`, stdlib package aliases including `std.lib.*`, `src.std.*`, `common.*`, and `app.mcp_jj.*`, target-as-module legacy imports such as `import aes/utilities`, current-package prefixes from package facades such as `rules.impl`, and skips legacy string-import AST nodes with empty module paths, cutting false import-warning volume on broad source checks; `simple run` now prints stable codes/help for common run-path failures instead of exposing `semantic:` phase text. | Complete for this slice |
| Diagnostic regression gate | `scripts/audit/diagnostic_code_audit.py` scans compiler/app and Rust CLI public diagnostic code literals for stable code shapes. | Complete for this slice |
| Diagnostic catalog coverage | `scripts/audit/diagnostic_catalog_audit.py` verifies assigned stable codes, including enum-based and Rust CLI diagnostic codes, have useful catalog entries with title, description, causes, and fixes; the catalog now includes file-read, parse, name/type, runtime division, dimension, async, VHDL, import-warning, and cycle codes found by the audit. | Complete for this slice |
| Diagnostic formatter behavior coverage | `test/unit/compiler/diagnostic_formatter_contract_spec.spl` locks code, span column, label, note, and help rendering for Simple syntax, JSON, and terminal text formatters. | Complete for this slice |
| End-to-end diagnostic CLI coverage | `test/integration/app/diagnostics/check_diagnostics_contract_spec.spl` runs real `bin/simple check` on invalid source, annotation and return type mismatches, warning-only source, and a missing file; it verifies `error[E0002]`, `error[E1003]`, `error[E0001]`, expected/found details, `warning[W0410]`, warning exit code 0, warning summary, JSON `"status": "warning"`, and help in both text and JSON output. `test/integration/app/diagnostics/run_diagnostics_contract_spec.spl` runs real `bin/simple run` on undefined-function, division-by-zero, and missing-file cases; it verifies `error[E1002]`, `error[E2001]`, `error[E0001]`, help text, and no leaked `semantic:` phase prefix. | Complete for this slice |
| API migration debt | `doc/03_plan/api_predicate_prefix_migration_2026-05-13.md` records the scoped migration rules; `python3 scripts/audit/api_consistency_audit.py` reports 0 hard violations across 36,243 scanned declarations and freezes the 1,427 advisory legacy `is_`/`has_` predicate-prefix declarations at baseline counts: `src/app` 204, `src/compiler` 600, `src/lib/common` 623. | Tracked with regression gate |
| Import warning debt | `doc/03_plan/import_warning_debt_2026-05-13.md` records the remaining unresolved-import warning buckets after resolver cleanup, separates resolver-complete cases from library parity and source cleanup ownership, and lists the current `simple check src/compiler` / `simple check src/lib` counts. | Tracked with owner split |
| Verification evidence | `python3 -m py_compile scripts/audit/repo_hygiene_audit.py scripts/audit/diagnostic_code_audit.py scripts/audit/api_consistency_audit.py scripts/audit/diagnostic_catalog_audit.py` passed; `python3 -m json.tool scripts/audit/repo_hygiene_policy.json >/dev/null` passed; `python3 -m json.tool scripts/audit/api_consistency_baseline.json >/dev/null` passed; `python3 scripts/audit/repo_hygiene_audit.py` passed; `python3 scripts/audit/diagnostic_code_audit.py` passed with 2,267 scanned source files; `python3 scripts/audit/api_consistency_audit.py` passed with 36,243 scanned declarations, 0 hard violations, and predicate-prefix debt at the checked baseline; `python3 scripts/audit/diagnostic_catalog_audit.py` passed with 37 assigned stable codes and 45 useful catalog entries; `cargo test --manifest-path src/compiler_rust/Cargo.toml -p simple-compiler module_resolver --lib` passed with 49 tests; `cargo test --manifest-path src/compiler_rust/Cargo.toml -p simple-driver cli::check --lib` passed with 16 tests; `cargo test --manifest-path src/compiler_rust/Cargo.toml -p simple-driver cli::basic --lib` passed with 3 tests; `cargo test --manifest-path src/compiler_rust/Cargo.toml -p simple-driver cli::test_runner --lib` passed with 84 tests; `cargo test --manifest-path src/compiler_rust/Cargo.toml -p simple-driver cli::spipe_docgen --lib` passed with 19 tests; `cargo test --manifest-path src/compiler_rust/Cargo.toml -p simple-driver cli:: --lib` passed with 200 tests; `cargo build --manifest-path src/compiler_rust/Cargo.toml --release -p simple-driver --bin simple` passed; installed the rebuilt release binary to `bin/release/x86_64-unknown-linux-gnu/simple`; `bin/release/x86_64-unknown-linux-gnu/simple check src/compiler` exited 0 with warnings only and import warnings reduced to 18; `bin/release/x86_64-unknown-linux-gnu/simple check src/lib` exited 0 with warnings only after correcting the `parse_key_usage` return type, with import warnings reduced to 94; `bin/release/x86_64-unknown-linux-gnu/simple check src/app/mcp src/app/simple_lsp_mcp` passed with no warnings; `bin/release/x86_64-unknown-linux-gnu/simple check test/integration/app/diagnostics/check_diagnostics_contract_spec.spl test/integration/app/diagnostics/run_diagnostics_contract_spec.spl` passed; `bin/release/x86_64-unknown-linux-gnu/simple test test/integration/app/diagnostics/check_diagnostics_contract_spec.spl --mode=interpreter --clean` passed with 10 examples and 0 failures; `bin/release/x86_64-unknown-linux-gnu/simple test test/integration/app/diagnostics/run_diagnostics_contract_spec.spl --mode=interpreter --clean` passed with 3 examples and 0 failures; `bin/release/x86_64-unknown-linux-gnu/simple test test/unit/compiler/diagnostic_formatter_contract_spec.spl --mode=interpreter --clean` passed with 3 examples and 0 failures; `bin/release/x86_64-unknown-linux-gnu/simple test test/unit/compiler/lint/public_doc_spec.spl --mode=interpreter --clean` passed with 9 examples and 0 failures; `SIMPLE_LIB=src bin/release/x86_64-unknown-linux-gnu/simple test test/integration/app/mcp_stdio_integration_spec.spl --mode=interpreter --clean` passed with 3 examples and 0 failures. | Complete for this slice |

## Remaining Production Gaps

| Gap | Required evidence before closing |
| --- | --- |
| Broad compiler/API conformance | The API consistency gate guards hard-rejected names and now freezes predicate-prefix debt by baseline. Remaining work is reducing the baseline through compatibility aliases and caller migrations. |
| Empty/legacy source directory regression | Keep `scripts/audit/repo_hygiene_policy.json` and `scripts/audit/repo_hygiene_audit.py` in the verification path so new generated clutter, policy drift, or empty source directories fail fast. |
| Error catalog completeness | Stable assigned diagnostic codes now have useful catalog entries, async/VHDL code overloading was removed, formatter behavior tests cover rendered code/span/label/note/help fields, `simple check` has end-to-end parse, basic annotation/return type-mismatch, missing-file, and unresolved-import warning fixtures, and `simple run` has undefined-function, division-by-zero, and missing-file fixtures. Remaining work is broadening deeper semantic type errors beyond direct literal checks, resolving the remaining 18 compiler and 94 lib import warnings, and reducing uncataloged internal-only error strings that are not yet public CLI diagnostics. |
| Full production readiness | Run the repository's verify workflow after implementation lanes converge; current worktree is too broad to claim global PASS from this docs slice alone. The broad driver CLI probe now passes (`cargo test --manifest-path src/compiler_rust/Cargo.toml -p simple-driver cli:: --lib`: 200 passed), compiler/lib/app checks now exit 0, and MCP stdio smoke passes, but repository-wide verify is still open. |

## Stop Condition

Do not mark the overall objective complete until every checklist row is complete
or explicitly delegated with current evidence, and the final verification covers
docs, structure hygiene, API consistency, diagnostics, and non-library
production gates.
