Kernel closure Phase 0 baseline — 2026-09-01

Command:
  sh scripts/check/check-kernel-closure.shs

Verdict:
  FAIL — 1855 file(s) classified, 0 unclassified, 17 K0->P imports

Grouped findings:
  3  semantics K0 -> tools P-static (api_surface, layer_call_scan)
  5  driver K0 -> tools P-static (header generation, async integration, AOP)
  9  driver K0 -> non-bootstrap VHDL P-static

This RED baseline is intentional evidence, not an allowlist. The checker keeps
failing until ownership is corrected; removing or weakening the rules is not a
valid migration. Run `sh scripts/check/check-kernel-closure.shs --selftest` for
the clean, injected-K0->P, and empty-tree fixture contract.

--------------------------------------------------------------------------
Update — 2026-09-07 (macOS, after PR #471 repaired the GNU-only sed/awk that
had made this guard match ZERO rows on BSD userland):

Verdict:
  FAIL — 1908 file(s) classified, 0 unclassified, 10 K0->P imports

7 of the original 17 were misclassifications of files under 90.tools that are
kernel components by kernel_pluggable_partition.md §3 (api_surface, aop,
async_integration -> K0; header_gen/shared_lib_flags -> K1). They are now
file-level rows in kernel_closure.sdn, each cited to §3.

Remaining, all REAL and none baselined:
  9  driver K0 -> VHDL backend P-static  (fix = migration plan Phase 5)
  1  semantics K0 -> tools.verify.layer_call_scan (fix = HIR-derived call edges)

Also strengthened in the same change: the import scan now matches `pub use`,
not only bare `use`. That surfaced 0 new violations today but closes a real
under-report (driver.spl:22 re-exports the VHDL product module via `pub use`).

Record: doc/08_tracking/bug/kernel_closure_k0_to_p_static_residual_2026-09-07.md

This is still a RED baseline held as evidence, not an allowlist.
