# Frontend offload switch observed through `simple run`

> Runs a hello world through the binary named by `SIMPLE_BIN` (default

<!-- sdn-diagram:id=frontend_offload_driver_spec.arch -->
<details class="sdn-source">
<summary>SDN source</summary>

```sdn id=frontend_offload_driver_spec.arch hash=sha256:auto render=ascii
@layout dag
@direction LR

frontend_offload_driver_spec -> std
```

</details>

<details class="sdn-ascii" open>
<summary>Diagram</summary>

```ascii generated-from=frontend_offload_driver_spec.arch hash=sha256:auto
# run: simple md-diagram-update
```

</details>
<!-- sdn-diagram:end -->

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 3 | 3 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Frontend offload switch observed through `simple run`

Runs a hello world through the binary named by `SIMPLE_BIN` (default

## At a Glance

| Field | Value |
|-------|-------|
| Category | Compiler / Driver |
| Status | Implemented (receipt cases need a pure-Simple binary) |
| Source | `/home/yoon/dev/simple-gpu-frontend/test/02_integration/compiler/frontend_offload_driver_spec.spl` |
| Updated | 2026-06-01 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and Audience

Runs a hello world through the binary named by `SIMPLE_BIN` (default
`/home/yoon/dev/simple/bin/simple`). The default path must stay unchanged. The
two switch cases are only observable through the pure-Simple driver; the
deployed `bin/simple` is the Rust seed, which ignores the switch, so they are
skipped unless `SIMPLE_BIN` names a self-hosted binary.

## Scenarios

### frontend offload switch through simple run

#### keeps the default path unchanged

- Default to CPU reference when nothing is set
   - Expected: code equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Default to CPU reference when nothing is set")
val (out, _err, code) = run_probe("", "", "")
expect(code).to_equal(0)
expect(out).to_contain("hello offload")
```

</details>

#### prints the demotion receipt on a real compile

- Record the offload decision receipt
   - Expected: code equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Record the offload decision receipt")
val (out, err, code) = run_probe("on", "allow-cpu", "1")
expect(code).to_equal(0)
expect(out).to_contain("hello offload")
expect(err).to_contain(
    "[frontend-offload] requested=hybrid_vector_gpu selected=cpu_reference reason=parse_mode_unimplemented source=env")
```

</details>

#### fails the compile instead of demoting under require-requested

- Refuse to demote under require-requested


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Refuse to demote under require-requested")
val (_out, err, code) = run_probe("on", "require-requested", "")
expect(code).to_not_equal(0)
expect(err).to_contain("frontend_offload_required_mode_unavailable")
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 3 |
| Active scenarios | 3 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

## Generation history

Generated by `simple spipe-docgen` (Simple).
Source SHA-256: `0b663972316461ee76a772ed8bbc5f56f7672ac4e867795ed2d997ef30586424`
