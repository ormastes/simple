# .spipe Wrapped Entry Editor Gui Specification

> Tests covering editor platform — detection, editor config — SDN settings, editor TUI backend — formalized, editor GUI backend — Surface integration, editor headless backend — testing, editor SimpleOS adapter, editor GUI shell, editor GUI backend — rendering, editor main — GUI flag, editor MCP tools.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 80 | 80 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# .spipe Wrapped Entry Editor Gui Specification

## Scenarios

### editor platform — detection

#### defines EditorPlatform enum

- defines EditorPlatform enum


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines EditorPlatform enum")
val src = read_text("src/lib/editor/00.common/platform.spl")
expect src.contains("enum EditorPlatform:") == true
expect src.contains("Desktop") == true
expect src.contains("SimpleOS") == true
expect src.contains("Web") == true
```

</details>

#### defines PlatformConfig with resource limits

- defines PlatformConfig with resource limits


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines PlatformConfig with resource limits")
val src = read_text("src/lib/editor/00.common/platform.spl")
expect src.contains("struct PlatformConfig:") == true
expect src.contains("max_open_files: i64") == true
expect src.contains("max_undo_depth: i64") == true
expect src.contains("file_watcher_enabled: bool") == true
```

</details>

#### has platform_default_config per platform

- has platform_default_config per platform


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has platform_default_config per platform")
val src = read_text("src/lib/editor/00.common/platform.spl")
expect src.contains("fn platform_default_config(platform: EditorPlatform) -> PlatformConfig") == true
```

</details>

### editor config — SDN settings

#### defines EditorConfig with theme, tab_size, etc

- defines EditorConfig with theme, tab_size, etc


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines EditorConfig with theme, tab_size, etc")
val src = read_text("src/lib/editor/00.common/config.spl")
expect src.contains("struct EditorConfig:") == true
expect src.contains("theme: text") == true
expect src.contains("tab_size: i64") == true
expect src.contains("line_numbers: bool") == true
expect src.contains("auto_save: bool") == true
expect src.contains("hover_delay_ms: i64") == true
```

</details>

#### has editor_config_default with sensible defaults

- has editor_config_default with sensible defaults


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has editor_config_default with sensible defaults")
val src = read_text("src/lib/editor/00.common/config.spl")
expect src.contains("fn editor_config_default() -> EditorConfig") == true
expect editor_config_default().hover_delay_ms == 300
expect editor_config_get_by_key(editor_config_set_by_key(editor_config_default(), "hover_delay_ms", "125"), "hover_delay_ms") == "125"
```

</details>

#### has editor_config_load with layered merge

- has editor_config_load with layered merge


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has editor_config_load with layered merge")
val src = read_text("src/lib/editor/00.common/config.spl")
expect src.contains("fn editor_config_load(user_path: text, workspace_path: text) -> EditorConfig") == true
```

</details>

### editor TUI backend — formalized

#### defines TuiBackendConfig

- defines TuiBackendConfig


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines TuiBackendConfig")
val src = read_text("src/lib/editor/70.backend/tui_backend.spl")
expect src.contains("struct TuiBackendConfig:") == true
expect src.contains("color_mode: text") == true
expect src.contains("line_numbers: bool") == true
```

</details>

#### has tui_backend_render_line and tui_backend_render_status

- has tui_backend_render_line and tui_backend_render_status


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has tui_backend_render_line and tui_backend_render_status")
val src = read_text("src/lib/editor/70.backend/tui_backend.spl")
expect src.contains("fn tui_backend_render_line") == true
expect src.contains("fn tui_backend_render_status") == true
```

</details>

### editor GUI backend — Surface integration

#### defines GuiBackendConfig

- defines GuiBackendConfig


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines GuiBackendConfig")
val src = read_text("src/lib/editor/70.backend/gui_backend.spl")
expect src.contains("struct GuiBackendConfig:") == true
expect src.contains("window_title: text") == true
expect src.contains("initial_width: i64") == true
```

</details>

#### has gui_render_markdown_block for HTML output

- has gui_render_markdown_block for HTML output


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has gui_render_markdown_block for HTML output")
val src = read_text("src/lib/editor/70.backend/gui_backend.spl")
expect src.contains("fn gui_render_markdown_block(block: RenderBlock) -> GuiRenderBlock") == true
```

</details>

#### renders Obsidian callout blocks with GUI metadata

- renders Obsidian callout blocks with GUI metadata


<details>
<summary>Executable SSpec</summary>

Runnable source: 21 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders Obsidian callout blocks with GUI metadata")
val block = RenderBlock(id: 9, kind: "callout", from_line: 0, to_line: 1, content: "> [!TIP]+ Useful\n> Use [[links]]", rendered_lines: ["> [!TIP]+ Useful", "> Use [[links]]"], status: "ok")
val rendered = gui_render_markdown_block(block)
expect rendered.html.contains("class=\"md-callout md-callout-tip\"") == true
expect rendered.html.contains("data-callout=\"tip\"") == true
expect rendered.html.contains("data-folded=\"false\"") == true
expect rendered.html.contains("data-source-from=\"0\"") == true
expect rendered.html.contains("data-source-to=\"1\"") == true
expect rendered.html.contains("class=\"md-callout-title-text\"") == true
expect rendered.html.contains("<p data-line=\"1\">Use [[links]]</p>") == true
expect rendered.html.contains("data-event=\"callout-fold-toggle\"") == true
expect rendered.html.contains("TIP Useful") == true
expect rendered.html.contains("Use [[links]]") == true
expect rendered.html.contains("[!TIP]") == false

val folded_block = RenderBlock(id: 10, kind: "callout", from_line: 0, to_line: 1, content: "> [!TIP]- Useful\n> Hidden", rendered_lines: ["> [!TIP]- Useful", "> Hidden"], status: "ok")
val folded = gui_render_markdown_block(folded_block)
expect folded.html.contains("data-folded=\"true\"") == true
expect folded.html.contains("class=\"md-callout md-callout-tip folded\"") == true
expect folded.html.contains("Hidden") == false
```

</details>

#### renders Obsidian embed blocks with GUI metadata

- renders Obsidian embed blocks with GUI metadata


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders Obsidian embed blocks with GUI metadata")
val block = RenderBlock(id: 10, kind: "embed", from_line: 0, to_line: 0, content: "![[Project Alpha|Alpha embed]]", rendered_lines: ["![[Project Alpha|Alpha embed]]"], status: "ok")
val rendered = gui_render_markdown_block(block)
expect rendered.html.contains("class=\"md-embed md-embed-note\"") == true
expect rendered.html.contains("data-embed-kind=\"note\"") == true
expect rendered.html.contains("data-target=\"Project Alpha\"") == true
expect rendered.html.contains("Alpha embed") == true
expect rendered.html.contains("![[") == false
```

</details>

#### renders resolved Obsidian note embeds as GUI transclusions

- renders resolved Obsidian note embeds as GUI transclusions


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders resolved Obsidian note embeds as GUI transclusions")
val block = RenderBlock(id: 10, kind: "embed", from_line: 0, to_line: 0, content: "![[Project Alpha|Alpha embed]]", rendered_lines: ["![[Project Alpha|Alpha embed]]"], status: "ok")
val index = md_wiki_index_documents([
    md_wiki_document("/vault/Project Alpha.md", "# Project Alpha\nBody <safe>")
])
val rendered = gui_render_markdown_block_with_wiki(block, index)
expect rendered.html.contains("md-transclusion") == true
expect rendered.html.contains("data-target=\"Project Alpha\"") == true
expect rendered.html.contains("Project Alpha") == true
expect rendered.html.contains("Body &lt;safe&gt;") == true
```

</details>

#### renders nested resolved Obsidian note embeds as GUI transclusions

- renders nested resolved Obsidian note embeds as GUI transclusions


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders nested resolved Obsidian note embeds as GUI transclusions")
val block = RenderBlock(id: 11, kind: "embed", from_line: 0, to_line: 0, content: "![[Project Alpha|Alpha embed]]", rendered_lines: ["![[Project Alpha|Alpha embed]]"], status: "ok")
val index = md_wiki_index_documents([
    md_wiki_document("/vault/Project Alpha.md", "# Project Alpha\n![[Project Beta|Beta embed]]"),
    md_wiki_document("/vault/Project Beta.md", "# Project Beta\nNested body")
])
val rendered = gui_render_markdown_block_with_wiki(block, index)
expect rendered.html.contains("data-target=\"Project Alpha\"") == true
expect rendered.html.contains("data-target=\"Project Beta\"") == true
expect rendered.html.contains("Beta embed") == true
expect rendered.html.contains("Nested body") == true
```

</details>

#### renders wiki property panels as editable GUI forms

- renders wiki property panels as editable GUI forms


<details>
<summary>Executable SSpec</summary>

Runnable source: 20 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders wiki property panels as editable GUI forms")
val panel = wiki_panel_with_rows("Properties", [
    WikiPanelRow(icon: "property", label: "status", detail: "active", path: "/tmp/note.md", line: 0)
])
val html = gui_render_wiki_property_form_html(panel)
expect html.contains("class=\"wiki-property-form\"") == true
expect html.contains("data-command=\"wiki-property-set\"") == true
expect html.contains("class=\"wiki-property-value\"") == true
expect html.contains("data-event=\"wiki-property-submit\"") == true
expect html.contains("data-payload=\"status|active\"") == true
val vault_html = gui_render_wiki_property_form_html_with_root(panel, "/tmp/vault")
expect vault_html.contains("data-event=\"wiki-vault-property-submit\"") == true
expect vault_html.contains("data-payload=\"/tmp/vault|status|active\"") == true
expect vault_html.contains("data-event=\"wiki-vault-property-review\"") == true
val schema_html = gui_render_wiki_property_form_html_with_root_and_schema(panel, "/tmp/vault", "status|required|active,draft")
expect schema_html.contains("<select class=\"wiki-property-value schema-aware\"") == true
expect schema_html.contains("data-required=\"required\"") == true
expect schema_html.contains("<option value=\"active\" selected>active</option>") == true
expect schema_html.contains("<option value=\"draft\">draft</option>") == true
```

</details>

#### renders markdown image attachment blocks with GUI metadata

- renders markdown image attachment blocks with GUI metadata


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders markdown image attachment blocks with GUI metadata")
val block = RenderBlock(id: 11, kind: "embed", from_line: 0, to_line: 0, content: "![Diagram](assets/diagram.png)", rendered_lines: ["![Diagram](assets/diagram.png)"], status: "ok")
val rendered = gui_render_markdown_block(block)
expect rendered.html.contains("class=\"md-embed md-embed-image\"") == true
expect rendered.html.contains("data-embed-kind=\"image\"") == true
expect rendered.html.contains("data-target=\"assets/diagram.png\"") == true
expect rendered.html.contains("<img class=\"md-embed-image-preview\" src=\"assets/diagram.png\" alt=\"Diagram\" loading=\"lazy\">") == true
expect rendered.html.contains("Diagram") == true
expect rendered.html.contains("![Diagram]") == false
```

</details>

#### renders inline markdown images inside mixed paragraph content

- renders inline markdown images inside mixed paragraph content


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders inline markdown images inside mixed paragraph content")
val block = RenderBlock(id: 111, kind: "paragraph", from_line: 0, to_line: 0, content: "Before ![Diagram](assets/diagram.png) after <safe>", rendered_lines: ["Before ![Diagram](assets/diagram.png) after <safe>"], status: "ok")
val rendered = gui_render_markdown_block(block)
expect rendered.html.contains("Before ") == true
expect rendered.html.contains("class=\"md-inline-image\"") == true
expect rendered.html.contains("class=\"md-inline-image-preview\" src=\"assets/diagram.png\" alt=\"Diagram\" loading=\"lazy\"") == true
expect rendered.html.contains(" after &lt;safe&gt;") == true
expect rendered.html.contains("![Diagram]") == false
```

</details>

#### renders markdown tables as structured GUI table cells

- renders markdown tables as structured GUI table cells


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders markdown tables as structured GUI table cells")
val block = RenderBlock(id: 12, kind: "table", from_line: 0, to_line: 2, content: "| A | B |\n|---|---|\n| 1 | 2 |", rendered_lines: ["| A | B |", "|---|---|", "| 1 | 2 |"], status: "ok")
val rendered = gui_render_markdown_block(block)
expect rendered.html.contains("class=\"md-table-editor\"") == true
expect rendered.html.contains("data-event=\"table-row-insert\"") == true
expect rendered.html.contains("data-event=\"table-column-insert\"") == true
expect rendered.html.contains("class=\"md-table\"") == true
expect rendered.html.contains(">A</th>") == true
expect rendered.html.contains(">B</th>") == true
expect rendered.html.contains("data-line=\"2\" data-col=\"0\"") == true
expect rendered.html.contains(">1</td>") == true
expect rendered.html.contains(">2</td>") == true
expect rendered.html.contains("---") == false
```

</details>

#### renders markdown task list items as GUI checkboxes

- renders markdown task list items as GUI checkboxes


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders markdown task list items as GUI checkboxes")
val block = RenderBlock(id: 13, kind: "list", from_line: 4, to_line: 5, content: "- [ ] Todo\n- [x] Done", rendered_lines: ["- [ ] Todo", "- [x] Done"], status: "ok")
val rendered = gui_render_markdown_block(block)
expect rendered.html.contains("class=\"md-task\"") == true
expect rendered.html.contains("class=\"md-task checked\"") == true
expect rendered.html.contains("class=\"md-task-checkbox\"") == true
expect rendered.html.contains("data-line=\"4\"") == true
expect rendered.html.contains("data-line=\"5\"") == true
expect rendered.html.contains("Todo") == true
expect rendered.html.contains("Done") == true
expect rendered.html.contains("[ ]") == false
```

</details>

#### renders markdown links with GUI hit-test metadata

- renders markdown links with GUI hit-test metadata


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders markdown links with GUI hit-test metadata")
var buffer = EditorBuffer.from_text(EditorBufferId(value: 1), "Title\nSee [Target](target.md) and [[Wiki Note]]\n")
buffer.path = "/tmp/gui_link_source.md"
buffer.move_cursor(0, 0)
val html = gui_render_editor_area(buffer, "markdown", buffer.viewport)
expect html.contains("class=\"md-link\"") == true
expect html.contains("data-line=\"1\"") == true
expect html.contains("data-col=\"4\"") == true
expect html.contains("data-target=\"target.md\"") == true
expect html.contains("data-target=\"Wiki Note\"") == true
```

</details>

#### opens markdown links from GUI click events

- opens markdown links from GUI click events


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("opens markdown links from GUI click events")
val source_path = "/tmp/simple_gui_click_source.md"
val target_path = "/tmp/target.md"
expect rt_file_write_text(source_path, "See [Target](target.md)\n") == true
expect rt_file_write_text(target_path, "# Target\n") == true
var session = EditSession.new()
session.open_file(source_path)
var state = gui_shell_new(session)

state = gui_shell_handle_event(state, "link-click", "target.md")

expect state.ctrl.session.active_document().path() == target_path
```

</details>

#### opens markdown links from GUI ctrl-click cursor hit testing

- opens markdown links from GUI ctrl-click cursor hit testing


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("opens markdown links from GUI ctrl-click cursor hit testing")
val source_path = "/tmp/simple_gui_ctrl_click_source.md"
val target_path = "/tmp/simple_gui_ctrl_click_target.md"
expect rt_file_write_text(source_path, "See [Target](simple_gui_ctrl_click_target.md)\n") == true
expect rt_file_write_text(target_path, "# Target\n") == true
var session = EditSession.new()
session.open_file(source_path)
var state = gui_shell_new(session)

state = gui_shell_handle_event(state, "editor-ctrl-click", "0,6")

expect state.ctrl.session.active_document().path() == target_path
```

</details>

#### toggles markdown task checkboxes from GUI task events

- toggles markdown task checkboxes from GUI task events


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("toggles markdown task checkboxes from GUI task events")
var session = EditSession.new()
val path = "/tmp/simple_gui_task_toggle.md"
expect rt_file_write_text(path, "- [ ] Todo\n- [x] Done\n") == true
session.open_file(path)
var state = gui_shell_new(session)

state = gui_shell_handle_event(state, "task-toggle", "0")
expect state.ctrl.session.active_buffer().line_at(0) == "- [x] Todo"

state = gui_shell_handle_event(state, "task-toggle", "1")
expect state.ctrl.session.active_buffer().line_at(1) == "- [ ] Done"
```

</details>

#### filters and batches markdown tasks from GUI task controls

- filters and batches markdown tasks from GUI task controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 24 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("filters and batches markdown tasks from GUI task controls")
var session = EditSession.new()
val path = "/tmp/simple_gui_task_batch.md"
expect rt_file_write_text(path, "- [ ] Todo\n- [ ] Review notes\n- [x] Done\n") == true
session.open_file(path)
var state = gui_shell_new(session)
state = gui_shell_handle_event(state, "task-filter", "done")
expect state.ctrl.status_msg == "wiki tasks: done"
expect state.ctrl.wiki_panel.rows.len() == 1
val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("class=\"task-query\"") == true
expect frame.editor_html.contains("data-event=\"task-query\"") == true
expect frame.editor_html.contains("class=\"task-count\"") == true
state = gui_shell_handle_event(state, "task-filter", "all")
expect state.ctrl.status_msg == "wiki tasks: all"
state = gui_shell_handle_event(state, "task-query", "review")
expect state.ctrl.status_msg == "wiki tasks: all"
expect state.ctrl.wiki_task_query == "review"
expect state.ctrl.wiki_panel.rows.len() == 1
expect state.ctrl.wiki_panel.rows[0].label == "Review notes"
state = gui_shell_handle_event(state, "task-batch", "done")
expect state.ctrl.status_msg == "tasks marked done"
expect state.ctrl.session.active_buffer().line_at(0) == "- [x] Todo"
```

</details>

#### filters vault search results from GUI search controls

- filters vault search results from GUI search controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 38 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("filters vault search results from GUI search controls")
var suffix = 0
var root = "/tmp/simple_gui_vault_search"
while rt_file_exists(root + "/Alpha.md") or rt_file_exists(root + "/Projects/Project.md"):
    suffix = suffix + 1
    root = "/tmp/simple_gui_vault_search_" + suffix.to_text()
expect rt_dir_create_all(root + "/Projects") == true
val path = root + "/Alpha.md"
val project_path = root + "/Projects/Project.md"
expect rt_file_write_text(path, "# Cache\n- cache task\nbody cache\n") == true
expect rt_file_write_text(project_path, "# Project Cache\nproject body\n") == true
var session = EditSession.new()
var state = gui_shell_new(session)
var ctrl = state.ctrl
val searched = ctrl._execute_palette_command("wiki-vault-search", root + "|cache")
ctrl.status_msg = searched.status_msg
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("vault-search-tools") == true
expect frame.editor_html.contains("data-event=\"vault-search-filter\"") == true
expect frame.editor_html.contains("data-event=\"vault-search-path-filter\"") == true

state = gui_shell_handle_event(state, "vault-search-filter", "heading")
expect state.ctrl.wiki_vault_search_query == "kind:heading cache"
expect state.ctrl.wiki_panel.rows.len() == 2
expect state.ctrl.wiki_panel.rows[0].label == "# Cache"

state = gui_shell_handle_event(state, "vault-search-path-filter", "Projects")
expect state.ctrl.wiki_vault_search_query == "path:Projects kind:heading cache"
expect state.ctrl.wiki_panel.rows.len() == 1
expect state.ctrl.wiki_panel.rows[0].path == project_path

state = gui_shell_handle_event(state, "vault-search-filter", "all")
expect state.ctrl.wiki_vault_search_query == "path:Projects cache"
expect state.ctrl.wiki_panel.rows.len() == 1
expect state.ctrl.wiki_panel.rows[0].path == project_path
```

</details>

#### renders and filters the quick switch picker from GUI controls

- renders and filters the quick switch picker from GUI controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 35 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders and filters the quick switch picker from GUI controls")
var session = EditSession.new()
val alpha_path = "/tmp/simple_gui_quick_switch_alpha.md"
val beta_path = "/tmp/simple_gui_quick_switch_beta.md"
expect rt_file_write_text(alpha_path, "# Alpha\n") == true
expect rt_file_write_text(beta_path, "# Beta\n\n") == true
session.open_file(alpha_path)
session.open_file(beta_path)
var beta_buffer = session.active_buffer()
beta_buffer.move_cursor(1, 0)
session.update_active_buffer(beta_buffer)
var state = gui_shell_new(session)
var ctrl = state.ctrl
val opened = ctrl._execute_palette_command("wiki-quick-switch", "")
ctrl.status_msg = opened.status_msg
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("quick-switch-tools") == true
expect frame.editor_html.contains("data-event=\"quick-switch-filter\"") == true
expect frame.editor_html.contains("data-event=\"quick-switch-insert\"") == true
expect frame.editor_html.contains("2 notes") == true

state = gui_shell_handle_event(state, "quick-switch-filter", "beta")
expect state.ctrl.wiki_quick_switch_query == "beta"
expect state.ctrl.wiki_panel.rows.len() == 1
expect state.ctrl.wiki_panel.rows[0].path == beta_path
state = gui_shell_handle_event(state, "quick-switch-insert", state.ctrl.wiki_panel.rows[0].detail)
expect state.ctrl.status_msg == "inserted embed: simple_gui_quick_switch_beta"
expect state.ctrl.session.active_buffer().to_text() == "# Beta\n![[simple_gui_quick_switch_beta]]\n\n"

state = gui_shell_handle_event(state, "quick-switch-filter", "")
expect state.ctrl.wiki_quick_switch_query == ""
expect state.ctrl.wiki_panel.rows.len() == 2
```

</details>

#### renders and submits the daily note date picker from GUI controls

- renders and submits the daily note date picker from GUI controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 23 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders and submits the daily note date picker from GUI controls")
val daily_root = "/tmp/simple_gui_daily_note_picker"
expect rt_dir_create_all(daily_root) == true
var session = EditSession.new()
var state = gui_shell_new(session)
var ctrl = state.ctrl
val opened = ctrl._execute_palette_command("daily-note-picker", "2026-05-20|" + daily_root + "|# {{title}}\nDate: {{date}}\n|{{date}}-daily|md")
ctrl.status_msg = opened.status_msg
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("daily-note-tools") == true
expect frame.editor_html.contains("type=\"date\"") == true
expect frame.editor_html.contains("data-event=\"daily-note-create\"") == true
expect frame.editor_html.contains("value=\"2026-05-20\"") == true
expect frame.editor_html.contains("data-folder=\"" + daily_root + "\"") == true

state = gui_shell_handle_event(state, "daily-note-create", "2026-05-21|" + daily_root + "|# {{title}}\nDate: {{date}}\n|{{date}}-daily|md")

expect state.ctrl.status_msg == "created daily note: " + daily_root + "/2026-05-21-daily.md"
expect state.ctrl.session.active_document().path() == daily_root + "/2026-05-21-daily.md"
expect rt_file_read_text(daily_root + "/2026-05-21-daily.md") == "# 2026-05-21-daily\nDate: 2026-05-21\n"
```

</details>

#### renders filters and submits the template picker variable form from GUI controls

- renders filters and submits the template picker variable form from GUI controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 32 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders filters and submits the template picker variable form from GUI controls")
val tpl_root = "/tmp/simple_gui_template_picker"
val target_path = "/tmp/simple_gui_template_picker_target.md"
expect rt_dir_create_all(tpl_root + "/nested") == true
expect rt_file_write_text(tpl_root + "/Meeting.md", "## {{title}}\nDate: {{date}}\n") == true
expect rt_file_write_text(tpl_root + "/nested/Review.md", "# {{title}}\n") == true
expect rt_file_write_text(target_path, "# Target\n") == true
var session = EditSession.new()
var state = gui_shell_new(session)
var ctrl = state.ctrl
val opened = ctrl._execute_palette_command("template-picker", tpl_root + "|" + target_path + "|Planning|2026-05-22|")
ctrl.status_msg = opened.status_msg
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("template-tools") == true
expect frame.editor_html.contains("class=\"template-query\"") == true
expect frame.editor_html.contains("class=\"template-title\"") == true
expect frame.editor_html.contains("class=\"template-date\"") == true
expect frame.editor_html.contains("data-event=\"template-picker-filter\"") == true
expect frame.editor_html.contains("data-event=\"template-insert\"") == true
expect frame.editor_html.contains("2 templates") == true

state = gui_shell_handle_event(state, "template-picker-filter", tpl_root + "|" + target_path + "|Review|2026-05-23|review")
expect state.ctrl.status_msg == "templates: 1"
expect state.ctrl.wiki_template_query == "review"
expect state.ctrl.wiki_panel.rows[0].label == "Review.md"

state = gui_shell_handle_event(state, "template-insert", state.ctrl.wiki_panel.rows[0].detail)
expect state.ctrl.status_msg.contains("inserted template: ") == true
expect rt_file_read_text(target_path).contains("# Review") == true
```

</details>

#### renders filters and submits the attachment picker target controls from GUI events

- renders filters and submits the attachment picker target controls from GUI events
   - Expected: rt_file_read_text(target_path) contains `![screenshot.png](attachments/screenshot`


<details>
<summary>Executable SSpec</summary>

Runnable source: 32 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders filters and submits the attachment picker target controls from GUI events")
val attachment_root = "/tmp/simple_gui_attachment_picker"
val target_path = "/tmp/simple_gui_attachment_picker_target.md"
expect rt_dir_create_all(attachment_root + "/assets") == true
expect rt_file_write_text(attachment_root + "/assets/screenshot.png", "png") == true
expect rt_file_write_text(attachment_root + "/assets/diagram.svg", "svg") == true
expect rt_file_write_text(target_path, "# Target\n") == true
var session = EditSession.new()
var state = gui_shell_new(session)
var ctrl = state.ctrl
val opened = ctrl._execute_palette_command("attachment-picker", attachment_root + "||" + target_path + "|attachments")
ctrl.status_msg = opened.status_msg
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("attachment-tools") == true
expect frame.editor_html.contains("class=\"attachment-query\"") == true
expect frame.editor_html.contains("class=\"attachment-target\"") == true
expect frame.editor_html.contains("class=\"attachment-storage\"") == true
expect frame.editor_html.contains("data-event=\"attachment-picker-filter\"") == true
expect frame.editor_html.contains("data-event=\"attachment-insert\"") == true
expect frame.editor_html.contains("2 attachments") == true

state = gui_shell_handle_event(state, "attachment-picker-filter", attachment_root + "|screen|" + target_path + "|attachments")
expect state.ctrl.status_msg == "attachments: 1"
expect state.ctrl.wiki_attachment_query == "screen"
expect state.ctrl.wiki_panel.rows[0].label == "screenshot.png"

state = gui_shell_handle_event(state, "attachment-insert", state.ctrl.wiki_panel.rows[0].detail)
expect state.ctrl.status_msg.contains("inserted attachment: attachments/screenshot") == true
expect(rt_file_read_text(target_path).contains("![screenshot.png](attachments/screenshot")).to_equal(true)
```

</details>

#### filters and groups Problems panel rows from GUI controls

- filters and groups Problems panel rows from GUI controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 47 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("filters and groups Problems panel rows from GUI controls")
val first_path = "/tmp/simple_gui_problem_filter_first.spl"
val second_path = "/tmp/simple_gui_problem_filter_second.spl"
expect rt_file_write_text(first_path, "fn first():\n    return\n") == true
expect rt_file_write_text(second_path, "fn second():\n    return\n") == true
var session = EditSession.new()
session.open_file(first_path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
ctrl.diagnostic_store.set_diagnostics(first_path, [
    EditorDiagnostic(path: first_path, line: 0, col: 3, end_line: 0, end_col: 8, severity: "warning", message: "lint warning", source: "lint"),
    EditorDiagnostic(path: first_path, line: 1, col: 4, end_line: 1, end_col: 8, severity: "error", message: "simple error", source: "simple")
])
ctrl.diagnostic_store.set_diagnostics(second_path, [
    EditorDiagnostic(path: second_path, line: 1, col: 2, end_line: 1, end_col: 5, severity: "error", message: "lint error", source: "lint")
])
val shown = ctrl._execute_palette_command("problems", "")
ctrl.status_msg = shown.status_msg
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("diagnostics-tools") == true
expect frame.editor_html.contains("diagnostics-table") == true
expect frame.editor_html.contains("<th>Severity</th>") == true
expect frame.editor_html.contains("class=\"diagnostics-message\"") == true
expect frame.editor_html.contains("class=\"diagnostics-source\"") == true
expect frame.editor_html.contains("simple_gui_problem_filter_first.spl") == true
expect frame.editor_html.contains("data-event=\"diagnostics-filter\"") == true
expect frame.editor_html.contains("data-event=\"diagnostics-group\"") == true
expect frame.editor_html.contains("data-event=\"diagnostics-sort\"") == true

state = gui_shell_handle_event(state, "diagnostics-filter", "error|")
expect state.ctrl.status_msg == "problems: 2"
expect state.ctrl.diagnostics_panel.title == "Problems (error)"
expect state.ctrl.diagnostics_panel.rows[0].label == "simple error"

state = gui_shell_handle_event(state, "diagnostics-group", "source")
expect state.ctrl.status_msg == "problems: 3"
expect state.ctrl.diagnostics_panel.title == "Problems grouped by source"
expect state.ctrl.diagnostics_panel.rows[0].label == "lint"
expect state.ctrl.diagnostics_panel.rows[3].label == "simple"

state = gui_shell_handle_event(state, "diagnostics-sort", "severity")
expect state.ctrl.status_msg == "problems: 3"
expect state.ctrl.diagnostics_panel.title == "Problems sorted by severity"
expect state.ctrl.diagnostics_panel.rows[0].label == "simple error"
```

</details>

#### renders WorkspaceEdit preview controls in the LSP panel

- renders WorkspaceEdit preview controls in the LSP panel


<details>
<summary>Executable SSpec</summary>

Runnable source: 22 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders WorkspaceEdit preview controls in the LSP panel")
val path = "/tmp/simple_gui_workspace_edit_tools.spl"
expect rt_file_write_text(path, "fn main():\n  return\n") == true
var session = EditSession.new()
session.open_file(path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
val edit_json = "[{\"range\":{\"start\":{\"line\":1,\"character\":0},\"end\":{\"line\":1,\"character\":8}},\"newText\":\"    return\"}]"
ctrl._show_workspace_edit_preview(edit_json, "formatting")
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)

expect frame.editor_html.contains("workspace-edit-tools") == true
expect frame.editor_html.contains("data-event=\"workspace-edit-confirm\"") == true
expect frame.editor_html.contains("data-event=\"workspace-edit-cancel\"") == true
expect frame.editor_html.contains("workspace-edit-diff") == true
expect frame.editor_html.contains("diff-before") == true
expect frame.editor_html.contains("line 2:1-2:9") == true
expect frame.editor_html.contains("diff-after") == true
expect frame.editor_html.contains("    return") == true
```

</details>

#### renders rename preview conflicts in the LSP panel

- renders rename preview conflicts in the LSP panel


<details>
<summary>Executable SSpec</summary>

Runnable source: 20 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders rename preview conflicts in the LSP panel")
val path = "/tmp/simple_gui_rename_conflict_ui.spl"
val missing_path = "/tmp/simple_gui_rename_conflict_missing/target.spl"
expect rt_file_write_text(path, "fn main():\n    val answer = 1\n") == true
var session = EditSession.new()
session.open_file(path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
val edit_json = "{\"changes\":{\"file://" + missing_path + "\":[{\"range\":{\"start\":{\"line\":1,\"character\":8},\"end\":{\"line\":1,\"character\":14}},\"newText\":\"conflict_answer\"}]}}"
ctrl._show_workspace_edit_preview(edit_json, "rename")
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)

expect frame.editor_html.contains("workspace-edit-diff") == true
expect frame.editor_html.contains("workspace-edit-conflict") == true
expect frame.editor_html.contains("Conflict") == true
expect frame.editor_html.contains("conflict: missing file") == true
expect frame.editor_html.contains("conflict_answer") == true
```

</details>

#### applies and cancels WorkspaceEdit previews from GUI controls

- applies and cancels WorkspaceEdit previews from GUI controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 33 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("applies and cancels WorkspaceEdit previews from GUI controls")
val apply_path = "/tmp/simple_gui_workspace_edit_apply.spl"
expect rt_file_write_text(apply_path, "fn main():\n  return\n") == true
var apply_session = EditSession.new()
apply_session.open_file(apply_path)
var apply_state = gui_shell_new(apply_session)
var apply_ctrl = apply_state.ctrl
val apply_json = "[{\"range\":{\"start\":{\"line\":1,\"character\":0},\"end\":{\"line\":1,\"character\":8}},\"newText\":\"    return\"}]"
apply_ctrl._show_workspace_edit_preview(apply_json, "formatting")
apply_state.ctrl = apply_ctrl

apply_state = gui_shell_handle_event(apply_state, "workspace-edit-confirm", "")

expect apply_state.ctrl.status_msg == "applied: formatting"
expect apply_state.ctrl.pending_workspace_edit_json == ""
expect apply_state.ctrl.session.active_buffer().line_at(1) == "    return"

val cancel_path = "/tmp/simple_gui_workspace_edit_cancel.spl"
expect rt_file_write_text(cancel_path, "fn main():\n  return\n") == true
var cancel_session = EditSession.new()
cancel_session.open_file(cancel_path)
var cancel_state = gui_shell_new(cancel_session)
var cancel_ctrl = cancel_state.ctrl
val cancel_json = "[{\"range\":{\"start\":{\"line\":1,\"character\":0},\"end\":{\"line\":1,\"character\":8}},\"newText\":\"    return\"}]"
cancel_ctrl._show_workspace_edit_preview(cancel_json, "formatting")
cancel_state.ctrl = cancel_ctrl

cancel_state = gui_shell_handle_event(cancel_state, "workspace-edit-cancel", "")

expect cancel_state.ctrl.status_msg == "workspace edit cancelled"
expect cancel_state.ctrl.pending_workspace_edit_json == ""
expect cancel_state.ctrl.session.active_buffer().line_at(1) == "  return"
```

</details>

#### renders and uses LSP navigation history controls

- renders and uses LSP navigation history controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 28 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders and uses LSP navigation history controls")
val first_path = "/tmp/simple_gui_lsp_history_first.spl"
val second_path = "/tmp/simple_gui_lsp_history_second.spl"
expect rt_file_write_text(first_path, "fn first():\n    return\n") == true
expect rt_file_write_text(second_path, "fn second():\n    return\n") == true
var session = EditSession.new()
session.open_file(second_path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
ctrl.lsp_location_stack = [EditorLocation(path: first_path, line: 1, col: 4)]
var dock = ctrl.session.dock_layout
dock.right_visible = true
dock.active_right = "lsp-panel"
ctrl.session.dock_layout = dock
state.ctrl = ctrl

expect state.ctrl.lsp_location_stack.len() == 1
val tools_html = gui_render_lsp_navigation_tools_html(state.ctrl)
expect tools_html.contains("lsp-navigation-tools") == true
expect tools_html.contains("data-event=\"lsp-back\"") == true

state = gui_shell_handle_event(state, "lsp-back", "")
expect state.ctrl.status_msg == "lsp back: " + first_path + ":2"
expect state.ctrl.session.active_document().path() == first_path
expect state.ctrl.session.active_buffer().cursor_row == 1
expect state.ctrl.session.active_buffer().cursor_col == 4
expect state.ctrl.lsp_location_stack.len() == 0
```

</details>

#### renders and selects LSP result rows from GUI clicks

- renders and selects LSP result rows from GUI clicks


<details>
<summary>Executable SSpec</summary>

Runnable source: 26 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders and selects LSP result rows from GUI clicks")
val first_path = "/tmp/simple_gui_lsp_result_first.spl"
val second_path = "/tmp/simple_gui_lsp_result_second.spl"
expect rt_file_write_text(first_path, "fn first():\n    return\n") == true
expect rt_file_write_text(second_path, "fn second():\n    return\n") == true
var session = EditSession.new()
session.open_file(first_path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
ctrl.lsp_result_panel = lsp_result_panel_from_response("references", LspResponse(id: 3, ok: true, result_json: "[{\"uri\":\"file://" + first_path + "\",\"range\":{\"start\":{\"line\":0,\"character\":0},\"end\":{\"line\":0,\"character\":2}}},{\"uri\":\"file://" + second_path + "\",\"range\":{\"start\":{\"line\":1,\"character\":4},\"end\":{\"line\":1,\"character\":8}}}]", error_text: ""))
var dock = ctrl.session.dock_layout
dock.right_visible = true
dock.active_right = "lsp-panel"
ctrl.session.dock_layout = dock
state.ctrl = ctrl

val rows_html = gui_render_lsp_result_rows_html(state.ctrl.lsp_result_panel)
expect rows_html.contains("class=\"lsp-result-row selected\"") == true
expect rows_html.contains("data-event=\"lsp-result-click\"") == true
expect rows_html.contains("data-row=\"1\"") == true

state = gui_shell_handle_event(state, "lsp-result-click", "1")

expect state.ctrl.status_msg == "lsp result selected"
expect state.ctrl.lsp_result_panel.selected == 1
```

</details>

#### renders non-Markdown LSP document symbols in the outline pane

- renders non-Markdown LSP document symbols in the outline pane


<details>
<summary>Executable SSpec</summary>

Runnable source: 24 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders non-Markdown LSP document symbols in the outline pane")
val path = "/tmp/simple_gui_simple_outline.spl"
expect rt_file_write_text(path, "fn main():\n    return\n") == true
var session = EditSession.new()
session.open_file(path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
ctrl.lsp_result_panel = LspResultPanel(
    title: "Document Symbols",
    rows: [LspResultRow(icon: "symbol", label: "main", detail: path + ":1", path: path, line: 0, col: 3, command_name: "", command_payload: "", depth: 0, expanded: false)],
    selected: 0,
    scroll_offset: 0,
    pending: false,
    request_id: 46
)
ctrl._apply_lsp_document_symbols_outline()
state.ctrl = ctrl

val frame = gui_shell_render_frame(state)

expect frame.editor_html.contains("outline-pane") == true
expect frame.editor_html.contains("main") == true
expect state.ctrl.session.dock_layout.active_right == "md-outline"
```

</details>

#### renders LSP command argument forms for code lens rows

- renders LSP command argument forms for code lens rows


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders LSP command argument forms for code lens rows")
val panel = lsp_result_panel_from_response("code-lens", LspResponse(id: 4, ok: true, result_json: "[{\"range\":{\"start\":{\"line\":0,\"character\":0},\"end\":{\"line\":0,\"character\":9}},\"command\":{\"title\":\"Run main\",\"command\":\"debug-start\",\"arguments\":[\"simple\"]}}]", error_text: ""))
val html = gui_render_lsp_result_rows_html(panel)
expect html.contains("lsp-command-form") == true
expect html.contains("class=\"lsp-command-name\"") == true
expect html.contains("value=\"debug-start\"") == true
expect html.contains("class=\"lsp-command-args\"") == true
expect html.contains("value=\"simple\"") == true
expect html.contains("data-event=\"code-lens-run\"") == true
expect html.contains("data-value=\"debug-start|simple\"") == true
```

</details>

#### renders and dispatches Code Actions filter and grouping controls

- renders and dispatches Code Actions filter and grouping controls


<details>
<summary>Executable SSpec</summary>

Runnable source: 31 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders and dispatches Code Actions filter and grouping controls")
var session = EditSession.new()
var state = gui_shell_new(session)
var code_ctrl = state.ctrl
code_ctrl.code_actions = [
    EditorCodeAction(title: "Quick one", kind: "quickfix", edit_json: "{\"line\":0,\"newText\":\"quick\"}", command: ""),
    EditorCodeAction(title: "Organize imports", kind: "source.organizeImports", edit_json: "{\"line\":0,\"newText\":\"source\"}", command: "")
]
code_ctrl.lsp_result_panel = code_ctrl._code_action_result_panel(12)
state.ctrl = code_ctrl
val html = gui_render_lsp_result_rows_html(state.ctrl.lsp_result_panel)
expect html.contains("class=\"code-action-tools\"") == true
expect html.contains("data-event=\"code-actions-filter\"") == true
expect html.contains("data-event=\"code-actions-group\"") == true

state = gui_shell_handle_event(state, "code-actions-group", "kind")
expect state.ctrl.status_msg == "code actions grouped: kind"
expect state.ctrl.lsp_result_panel.rows[0].icon == "group"

var filter_state = gui_shell_new(session)
var filter_ctrl = filter_state.ctrl
filter_ctrl.code_actions = [
    EditorCodeAction(title: "Quick one", kind: "quickfix", edit_json: "{\"line\":0,\"newText\":\"quick\"}", command: ""),
    EditorCodeAction(title: "Organize imports", kind: "source.organizeImports", edit_json: "{\"line\":0,\"newText\":\"source\"}", command: "")
]
filter_ctrl.lsp_result_panel = filter_ctrl._code_action_result_panel(13)
filter_state.ctrl = filter_ctrl
filter_state = gui_shell_handle_event(filter_state, "code-actions-filter", "source")
expect filter_state.ctrl.status_msg == "code actions filtered: source"
expect filter_state.ctrl.lsp_result_panel.rows[0].label == "Organize imports"
```

</details>

#### escapes HTML in rendered output

- escapes HTML in rendered output


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("escapes HTML in rendered output")
val src = read_text("src/lib/editor/70.backend/gui_backend.spl")
expect src.contains("fn _gui_escape_html(s: text) -> text") == true
expect src.contains("&lt;") == true
expect src.contains("&gt;") == true
```

</details>

### editor headless backend — testing

#### defines HeadlessBackend class

- defines HeadlessBackend class


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines HeadlessBackend class")
val src = read_text("src/lib/editor/70.backend/headless_backend.spl")
expect src.contains("class HeadlessBackend:") == true
expect src.contains("frames: [HeadlessFrame]") == true
```

</details>

#### has capture_frame, frame_count, last_frame

- has capture_frame, frame_count, last_frame


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has capture_frame, frame_count, last_frame")
val src = read_text("src/lib/editor/70.backend/headless_backend.spl")
expect src.contains("me capture_frame") == true
expect src.contains("fn frame_count() -> i64") == true
expect src.contains("fn last_frame() -> HeadlessFrame") == true
```

</details>

### editor SimpleOS adapter

#### provides SimpleOS platform config

- provides SimpleOS platform config


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("provides SimpleOS platform config")
val src = read_text("src/app/editor/simpleos_adapter.spl")
expect src.contains("fn simpleos_editor_config() -> PlatformConfig") == true
expect src.contains("EditorPlatform.SimpleOS") == true
```

</details>

#### has reduced resource limits

- has reduced resource limits


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has reduced resource limits")
val src = read_text("src/app/editor/simpleos_adapter.spl")
expect src.contains("fn simpleos_max_open_files() -> i64") == true
expect src.contains("32") == true
```

</details>

### editor GUI shell

#### defines GuiShellState struct with controller and GUI config

- defines GuiShellState struct with controller and GUI config


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines GuiShellState struct with controller and GUI config")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("struct GuiShellState:") == true
expect src.contains("ctrl: EditorController") == true
expect src.contains("extension_host: ExtensionHost") == true
expect src.contains("config: GuiBackendConfig") == true
expect src.contains("platform_config: PlatformConfig") == true
```

</details>

#### has gui_shell_init that activates built-in extensions

- has gui_shell_init that activates built-in extensions


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has gui_shell_init that activates built-in extensions")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("fn gui_shell_init(state: GuiShellState)") == true
expect src.contains("extension_host: ctrl.extension_host") == true
expect src.contains("activate_language(\"simple\")") == true
expect src.contains("activate_language(\"markdown\")") == true
```

</details>

#### activates extension language events when GUI opens files

- activates extension language events when GUI opens files


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("activates extension language events when GUI opens files")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("fn _gui_activate_active_language") == true
expect src.contains("state.ctrl.extension_host.activate_language(doc.language_id)") == true
expect src.contains("state.ctrl.extension_host.emit_event(\"onDidOpenTextDocument\", doc.path())") == true
expect src.contains("_gui_activate_active_language(state)") == true
```

</details>

#### has gui_shell_run entry point

- has gui_shell_run entry point


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has gui_shell_run entry point")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("fn gui_shell_run(session: EditSession)") == true
expect src.contains("gui_shell_render_frame(state)") == true
```

</details>

#### has GuiFrame for rendered output

- has GuiFrame for rendered output


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has GuiFrame for rendered output")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("struct GuiFrame:") == true
expect src.contains("tab_bar_html: text") == true
expect src.contains("editor_html: text") == true
```

</details>

#### handle_resize parses width and height from data

- handle_resize parses width and height from data


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("handle_resize parses width and height from data")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("fn _gui_handle_resize") == true
expect src.contains("index_of(\",\")") == true
expect src.contains("initial_width") == true
expect src.contains("initial_height") == true
```

</details>

#### handle_tab_select selects tab by index

- handle_tab_select selects tab by index


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("handle_tab_select selects tab by index")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("fn _gui_handle_tab_select") == true
expect src.contains("select_tab(tab_index)") == true
```

</details>

#### gui_shell_present_frame calls rt_gui_present_html

- gui_shell_present_frame calls rt_gui_present_html


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("gui_shell_present_frame calls rt_gui_present_html")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("extern fn rt_gui_present_html") == true
expect src.contains("rt_gui_present_html(html)") == true
```

</details>

#### renders and opens wiki panel rows from GUI click events

- renders and opens wiki panel rows from GUI click events


<details>
<summary>Executable SSpec</summary>

Runnable source: 28 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders and opens wiki panel rows from GUI click events")
val source_path = "/tmp/simple_gui_wiki_source.md"
val target_path = "/tmp/simple_gui_wiki_target.md"
expect rt_file_write_text(source_path, "[[simple_gui_wiki_target]]\n") == true
expect rt_file_write_text(target_path, "# Target\n") == true
var session = EditSession.new()
session.open_file(source_path)
session.open_file(target_path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
ctrl.wiki_panel = wiki_panel_with_rows("Backlinks", [
    WikiPanelRow(icon: "backlink", label: "source", detail: source_path + ":1", path: source_path, line: 0)
])
var dock = ctrl.session.dock_layout
dock.right_visible = true
dock.active_right = "wiki-panel"
ctrl.session.dock_layout = dock
state.ctrl = ctrl

val gui_src = read_text("src/app/editor/gui_shell.spl")
expect gui_src.contains("class=\\\"wiki-row") == true
expect gui_src.contains("data-row=\\\"") == true
expect gui_src.contains("data-path=\\\"") == true

state = gui_shell_handle_event(state, "wiki-panel-click", "0")
expect state.ctrl.session.active_document().path() == source_path
expect state.ctrl.status_msg == "wiki jump: " + source_path + ":1"
```

</details>

#### renders LSP hover popup HTML near the source position

- renders LSP hover popup HTML near the source position


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders LSP hover popup HTML near the source position")
val html = gui_render_lsp_hover_popup_html(LspHoverPopup(visible: true, content: "fn main() <safe>", detail: "line 1:4", line: 0, col: 3))
expect html.contains("class=\"lsp-hover-popup\"") == true
expect html.contains("data-line=\"0\"") == true
expect html.contains("data-col=\"3\"") == true
expect html.contains("data-event=\"lsp-hover-dismiss\"") == true
expect html.contains("fn main() &lt;safe&gt;") == true
expect gui_render_lsp_hover_popup_html(lsp_hover_popup_hidden()) == ""
```

</details>

#### requests and debounces LSP hover from GUI editor hover events

- requests and debounces LSP hover from GUI editor hover events


<details>
<summary>Executable SSpec</summary>

Runnable source: 22 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("requests and debounces LSP hover from GUI editor hover events")
val path = "/tmp/simple_gui_hover_event.spl"
expect rt_file_write_text(path, "fn main():\n    return\n") == true
var session = EditSession.new()
session.open_file(path)
var state = gui_shell_new(session)
val frame = gui_shell_render_frame(state)
expect frame.editor_html.contains("data-event=\"editor-hover\"") == true
expect frame.editor_html.contains("data-value=\"0,0\"") == true

state = gui_shell_handle_event(state, "editor-hover", "0,3")
expect state.ctrl.status_msg == "hover results"
expect state.ctrl.session.active_buffer().cursor_row == 0
expect state.ctrl.session.active_buffer().cursor_col == 3
val next_id = state.ctrl.simple_lsp_state.client.next_id
var ctrl_hover = state.ctrl
ctrl_hover.lsp_hover_popup = LspHoverPopup(visible: true, content: "fn main()", detail: "line 1:4", line: 0, col: 3)
state.ctrl = ctrl_hover
state = gui_shell_handle_event(state, "editor-hover", "0,3")
expect state.ctrl.status_msg == "hover unchanged"
expect state.ctrl.simple_lsp_state.client.next_id == next_id
```

</details>

#### delays configurable GUI LSP hover until the hover threshold elapses

- delays configurable GUI LSP hover until the hover threshold elapses


<details>
<summary>Executable SSpec</summary>

Runnable source: 20 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("delays configurable GUI LSP hover until the hover threshold elapses")
val path = "/tmp/simple_gui_hover_delay_event.spl"
expect rt_file_write_text(path, "fn main():\n    return\n") == true
var session = EditSession.new()
session.open_file(path)
var state = gui_shell_new(session)
state.ctrl.config = editor_config_set_by_key(state.ctrl.config, "hover_delay_ms", "500")
val backend_src = read_text("src/lib/editor/70.backend/gui_backend.spl")
val shell_src = read_text("src/app/editor/gui_shell.spl")
expect backend_src.contains("data-hover-delay-ms") == true
expect shell_src.contains("state.ctrl.config.hover_delay_ms") == true
val next_id = state.ctrl.simple_lsp_state.client.next_id

state = gui_shell_handle_event(state, "editor-hover", "0,3,100")
expect state.ctrl.status_msg == "hover delayed"
expect state.ctrl.simple_lsp_state.client.next_id == next_id

state = gui_shell_handle_event(state, "editor-hover", "0,3,500")
expect state.ctrl.status_msg == "hover results"
```

</details>

#### dismisses LSP hover popup from GUI events

- dismisses LSP hover popup from GUI events


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("dismisses LSP hover popup from GUI events")
var session = EditSession.new()
var state = gui_shell_new(session)
state.ctrl.lsp_hover_popup = LspHoverPopup(visible: true, content: "fn main()", detail: "line 1:4", line: 0, col: 3)

state = gui_shell_handle_event(state, "lsp-hover-dismiss", "")

expect state.ctrl.lsp_hover_popup.visible == false
expect state.ctrl.status_msg == "hover dismissed"
```

</details>

#### renders and dismisses LSP signature popup from GUI events

- renders and dismisses LSP signature popup from GUI events


<details>
<summary>Executable SSpec</summary>

Runnable source: 41 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders and dismisses LSP signature popup from GUI events")
val html = gui_render_lsp_signature_popup_html(LspSignaturePopup(visible: true, label: "add(value: i64) <safe>", detail: "Returns value", active_parameter: "value: i64", line: 1, col: 8))
expect html.contains("class=\"lsp-signature-popup\"") == true
expect html.contains("data-event=\"lsp-signature-dismiss\"") == true
expect html.contains("data-event=\"lsp-signature-prev\"") == true
expect html.contains("data-event=\"lsp-signature-next\"") == true
expect html.contains("data-active-parameter=\"value: i64\"") == true
expect html.contains("class=\"signature-active-parameter\"") == true
expect html.contains(">value: i64</span>") == true
expect html.contains("&lt;safe&gt;") == true
expect gui_render_lsp_signature_popup_html(lsp_signature_popup_hidden()) == ""

var session = EditSession.new()
var state = gui_shell_new(session)
var ctrl = state.ctrl
ctrl.lsp_result_panel = LspResultPanel(
    title: "Signature Help",
    rows: [
        LspResultRow(icon: "signature", label: "add(value: i64)", detail: "Returns value", path: "", line: 0, col: 0, command_name: "", command_payload: "value: i64", depth: 0, expanded: false),
        LspResultRow(icon: "signature", label: "add(value: text)", detail: "Returns text", path: "", line: 0, col: 0, command_name: "", command_payload: "value: text", depth: 0, expanded: false)
    ],
    selected: 0,
    scroll_offset: 0,
    pending: false,
    request_id: 1
)
ctrl.lsp_signature_popup = LspSignaturePopup(visible: true, label: "add(value: i64)", detail: "Returns value", active_parameter: "value: i64", line: 1, col: 8)
state.ctrl = ctrl

state = gui_shell_handle_event(state, "lsp-signature-next", "")

expect state.ctrl.status_msg == "signature overload 2/2"
expect state.ctrl.lsp_result_panel.selected == 1
expect state.ctrl.lsp_signature_popup.label == "add(value: text)"
expect state.ctrl.lsp_signature_popup.active_parameter == "value: text"

state = gui_shell_handle_event(state, "lsp-signature-dismiss", "")

expect state.ctrl.lsp_signature_popup.visible == false
expect state.ctrl.status_msg == "signature dismissed"
```

</details>

#### executes inline code lens commands from GUI events

- executes inline code lens commands from GUI events


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("executes inline code lens commands from GUI events")
var session = EditSession.new()
var state = gui_shell_new(session)

state = gui_shell_handle_event(state, "code-lens-run", "debug-start|simple")

expect state.ctrl.status_msg == "debug adapter: Launch Simple"
expect state.ctrl.debug_status == "starting"
```

</details>

#### moves the cursor from GUI inlay hint clicks

- moves the cursor from GUI inlay hint clicks


<details>
<summary>Executable SSpec</summary>

Runnable source: 16 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("moves the cursor from GUI inlay hint clicks")
val path = "/tmp/simple_gui_inlay_hint_click.spl"
expect rt_file_write_text(path, "fn main():\n    val answer = 1\n") == true
var session = EditSession.new()
session.open_file(path)
var state = gui_shell_new(session)
var buffer = state.ctrl.session.active_buffer()
buffer.add_inlay_hint(1, 14, ": i64")
state.ctrl.session.update_active_buffer(buffer)

state = gui_shell_handle_event(state, "inlay-hint-click", "1,14")

expect state.ctrl.session.active_buffer().cursor_row == 1
expect state.ctrl.session.active_buffer().cursor_col == 14
expect state.ctrl.status_msg == "inlay hint selected"
```

</details>

#### renders wiki graph panels as clickable GUI graph nodes

- renders wiki graph panels as clickable GUI graph nodes


<details>
<summary>Executable SSpec</summary>

Runnable source: 24 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders wiki graph panels as clickable GUI graph nodes")
var session = EditSession.new()
var state = gui_shell_new(session)
var ctrl = state.ctrl
ctrl.wiki_panel = wiki_panel_with_rows("Graph", [
    WikiPanelRow(icon: "link", label: "Target", detail: "source -> Target link resolved", path: "/tmp/simple_gui_graph_target.md", line: 0)
])
var dock = ctrl.session.dock_layout
dock.right_visible = true
dock.active_right = "wiki-panel"
ctrl.session.dock_layout = dock
state.ctrl = ctrl

val graph_html = gui_render_wiki_graph_html(state.ctrl.wiki_panel)
expect graph_html.contains("class=\"wiki-graph\"") == true
expect graph_html.contains("data-layout=\"deterministic-grid\"") == true
expect graph_html.contains("class=\"wiki-graph-node selected\"") == true
expect graph_html.contains("data-event=\"wiki-panel-click\"") == true
expect graph_html.contains("data-x=\"12\"") == true
expect graph_html.contains("data-y=\"12\"") == true
expect graph_html.contains("data-from-x=\"12\"") == true
expect graph_html.contains("data-to-x=\"12\"") == true
expect graph_html.contains("source -&gt; Target link resolved") == true
```

</details>

#### submits wiki property form edits through the existing property command

- submits wiki property form edits through the existing property command


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("submits wiki property form edits through the existing property command")
val note_path = "/tmp/simple_gui_wiki_property.md"
expect rt_file_write_text(note_path, "---\nstatus: active\n---\n# Note\n") == true
var session = EditSession.new()
session.open_file(note_path)
var state = gui_shell_new(session)
state = gui_shell_handle_event(state, "wiki-property-submit", "status|done")
expect state.ctrl.status_msg == "property set: status"
expect state.ctrl.session.active_buffer().to_text().contains("status: done") == true
```

</details>

#### submits wiki property vault edits through the bulk property command

- submits wiki property vault edits through the bulk property command


<details>
<summary>Executable SSpec</summary>

Runnable source: 18 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("submits wiki property vault edits through the bulk property command")
val root = "/tmp/simple_gui_wiki_property_vault"
expect rt_dir_create_all(root + "/Projects") == true
val note_path = root + "/Home.md"
val other_path = root + "/Projects/Other.md"
expect rt_file_write_text(note_path, "---\nstatus: active\n---\n# Note\n") == true
expect rt_file_write_text(other_path, "# Other\n") == true
var session = EditSession.new()
session.open_file(note_path)
var state = gui_shell_new(session)
state = gui_shell_handle_event(state, "wiki-vault-property-review", root + "|status|done")
expect state.ctrl.status_msg == "vault property review: 2"
expect state.ctrl.wiki_panel.title == "Property Review"
state = gui_shell_handle_event(state, "wiki-vault-property-submit", root + "|status|done")
expect state.ctrl.status_msg == "vault property set: 2"
expect state.ctrl.session.active_buffer().to_text().contains("status: done") == true
expect rt_file_read_text(other_path).contains("status: done") == true
```

</details>

#### routes GUI table edit controls through markdown table commands

- routes GUI table edit controls through markdown table commands


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("routes GUI table edit controls through markdown table commands")
val note_path = "/tmp/simple_gui_table_edit.md"
expect rt_file_write_text(note_path, "| A | B |\n| --- | --- |\n| 1 | 2 |\n") == true
var session = EditSession.new()
session.open_file(note_path)
var state = gui_shell_new(session)
var buffer = state.ctrl.session.active_buffer()
buffer.move_cursor(2, 3)
state.ctrl.session.update_active_buffer(buffer)
state = gui_shell_handle_event(state, "table-row-insert", "")
expect state.ctrl.status_msg == "table row inserted"
expect state.ctrl.session.active_buffer().dirty == true
buffer = state.ctrl.session.active_buffer()
buffer.move_cursor(0, 3)
state.ctrl.session.update_active_buffer(buffer)
state = gui_shell_handle_event(state, "table-column-insert", "")
expect state.ctrl.status_msg == "table column inserted"
expect state.ctrl.session.active_buffer().dirty == true
```

</details>

#### commits GUI table cell edits back to markdown source

- commits GUI table cell edits back to markdown source


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("commits GUI table cell edits back to markdown source")
val note_path = "/tmp/simple_gui_table_cell_edit.md"
expect rt_file_write_text(note_path, "| A | B |\n| --- | --- |\n| 1 | 2 |\n") == true
var session = EditSession.new()
session.open_file(note_path)
var state = gui_shell_new(session)
state = gui_shell_handle_event(state, "table-cell-commit", "2|1|updated")
expect state.ctrl.status_msg == "table cell updated"
expect state.ctrl.session.active_buffer().to_text().contains("| 1 | updated |") == true
expect state.ctrl.session.active_buffer().dirty == true
```

</details>

### editor GUI backend — rendering

#### has gui_render_editor_area for HTML output

- has gui_render_editor_area for HTML output


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has gui_render_editor_area for HTML output")
val src = read_text("src/lib/editor/70.backend/gui_backend.spl")
expect src.contains("fn gui_render_editor_area(buffer: EditorBuffer, language_id: text, viewport: EditorViewport) -> text") == true
expect src.contains("editor-area") == true
```

</details>

#### renders source area as an editable GUI surface

- renders source area as an editable GUI surface


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders source area as an editable GUI surface")
val src = read_text("src/lib/editor/70.backend/gui_backend.spl")
expect src.contains("contenteditable=\\\"true\\\"") == true
expect src.contains("data-event=\\\"table-cell-commit\\\"") == true
expect src.contains("role=\\\"textbox\\\"") == true
expect src.contains("spellcheck=\\\"false\\\"") == true
expect src.contains("data-language=\\\"") == true
expect src.contains("data-line=\\\"") == true
expect src.contains("data-cursor=\\\"true\\\"") == true
```

</details>

#### renders active source characters with GUI hover column targets

- renders active source characters with GUI hover column targets


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders active source characters with GUI hover column targets")
var buffer = EditorBuffer.from_text(EditorBufferId(value: 1), "main\n")
buffer.viewport.height = 1
buffer.move_cursor(0, 2)
val html = gui_render_editor_area(buffer, "simple", buffer.viewport)
expect html.contains("class=\"hover-target\"") == true
expect html.contains("data-event=\"editor-hover\"") == true
expect html.contains("data-value=\"0,2\"") == true
expect html.contains("data-col=\"2\"") == true
```

</details>

#### renders hover targets inside decorated GUI source lines

- renders hover targets inside decorated GUI source lines


<details>
<summary>Executable SSpec</summary>

Runnable source: 29 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders hover targets inside decorated GUI source lines")
var selected_buffer = EditorBuffer.from_text(EditorBufferId(value: 1), "alpha\n")
selected_buffer.viewport.height = 1
selected_buffer.set_selection_range(0, 1, 0, 4)
val selected_html = gui_render_editor_area(selected_buffer, "simple", selected_buffer.viewport)
expect selected_html.contains("class=\"selection-range\"") == true
expect selected_html.contains("data-value=\"0,2\"") == true

var semantic_buffer = EditorBuffer.from_text(EditorBufferId(value: 2), "main\n")
semantic_buffer.viewport.height = 1
semantic_buffer.add_semantic_token(0, 0, 4, 1)
val semantic_html = gui_render_editor_area(semantic_buffer, "simple", semantic_buffer.viewport)
expect semantic_html.contains("semantic-token-1") == true
expect semantic_html.contains("data-value=\"0,2\"") == true

var hinted_buffer = EditorBuffer.from_text(EditorBufferId(value: 3), "answer\n")
hinted_buffer.viewport.height = 1
hinted_buffer.add_inlay_hint(0, 6, ": i64")
val hinted_html = gui_render_editor_area(hinted_buffer, "simple", hinted_buffer.viewport)
expect hinted_html.contains("class=\"inlay-hint\"") == true
expect hinted_html.contains("data-value=\"0,2\"") == true

var markdown_buffer = EditorBuffer.from_text(EditorBufferId(value: 4), "Title\n[[Target]]\n")
markdown_buffer.viewport.height = 2
markdown_buffer.move_cursor(0, 0)
val markdown_html = gui_render_editor_area(markdown_buffer, "markdown", markdown_buffer.viewport)
expect markdown_html.contains("class=\"md-link\"") == true
expect markdown_html.contains("data-event=\"editor-hover\"") == true
```

</details>

#### renders diagnostic gutter markers in GUI source lines

- renders diagnostic gutter markers in GUI source lines


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders diagnostic gutter markers in GUI source lines")
var buffer = EditorBuffer.from_text(EditorBufferId(value: 1), "first\nsecond\n")
buffer.path = "/tmp/simple_editor_gui_gutter.spl"
buffer.viewport.height = 3
val html = gui_render_editor_area_with_diagnostics(buffer, "simple", buffer.viewport, buffer.path, [
    EditorDiagnostic(path: buffer.path, line: 1, col: 0, end_line: 1, end_col: 1, severity: "error", message: "bad", source: "test")
])

expect html.contains("diagnostic-gutter error") == true
expect html.contains("data-diagnostic=\"error\"") == true
expect html.contains(">E</span>") == true
```

</details>

#### renders folded headers in GUI source lines

- renders folded headers in GUI source lines


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("renders folded headers in GUI source lines")
var buffer = EditorBuffer.from_text(EditorBufferId(value: 1), "# One\nbody\n# Two")
buffer.viewport.height = 3
val open_html = gui_render_editor_area(buffer, "markdown", buffer.viewport)
expect open_html.contains("foldable-header") == true
expect open_html.contains("fold-badge-open") == true
expect open_html.contains("data-event=\"fold-toggle\"") == true
expect open_html.contains("data-fold-end=\"1\"") == true
expect open_html.contains(">fold</button>") == true
buffer.fold_range(0, 1)
val html = gui_render_editor_area(buffer, "markdown", buffer.viewport)
expect html.contains("folded-header") == true
expect html.contains("fold-badge") == true
expect html.contains("fold-badge-open") == false
expect html.contains("data-event=\"fold-toggle\"") == true
expect html.contains("data-value=\"0\"") == true
expect html.contains("+1 folded") == true
expect html.contains("body") == false
```

</details>

#### unfolds folded source ranges from GUI fold badges

- unfolds folded source ranges from GUI fold badges


<details>
<summary>Executable SSpec</summary>

Runnable source: 17 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("unfolds folded source ranges from GUI fold badges")
val note_path = "/tmp/simple_gui_fold_badge.md"
expect rt_file_write_text(note_path, "# One\nbody\n# Two\n") == true
var session = EditSession.new()
session.open_file(note_path)
var state = gui_shell_new(session)
var ctrl = state.ctrl
var buffer = ctrl.session.active_buffer()
buffer.fold_range(0, 1)
ctrl.session.update_active_buffer(buffer)
state.ctrl = ctrl
state = gui_shell_handle_event(state, "fold-toggle", "0")
buffer = state.ctrl.session.active_buffer()
expect buffer.folded_ranges.len() == 0
expect buffer.cursor_row == 0
expect state.ctrl.status_msg == "unfolded section"
```

</details>

#### captures folded header markers in headless frames

- captures folded header markers in headless frames


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("captures folded header markers in headless frames")
var buffer = EditorBuffer.from_text(EditorBufferId(value: 1), "head\nbody\nnext\n")
buffer.fold_range(0, 1)
var backend = HeadlessBackend.new(40, 5)
backend.capture_frame(buffer, "NORMAL", "")
val frame = backend.last_frame()
expect frame.lines[0].contains("[+1 folded | za]") == true
expect frame.lines[1] == "next"
```

</details>

#### has gui_render_tab_bar_html for tab rendering

- has gui_render_tab_bar_html for tab rendering


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has gui_render_tab_bar_html for tab rendering")
val src = read_text("src/lib/editor/70.backend/gui_backend.spl")
expect src.contains("fn gui_render_tab_bar_html(session: EditSession) -> text") == true
expect src.contains("tab-bar") == true
```

</details>

#### has gui_render_file_tree_html for tree rendering

- has gui_render_file_tree_html for tree rendering


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has gui_render_file_tree_html for tree rendering")
val src = read_text("src/lib/editor/70.backend/gui_backend.spl")
expect src.contains("fn gui_render_file_tree_html(file_tree: FileTreeState) -> text") == true
expect src.contains("file-tree") == true
```

</details>

### editor main — GUI flag

#### supports --gui flag for GUI mode

- supports --gui flag for GUI mode


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("supports --gui flag for GUI mode")
val src = read_text("src/app/editor/main.spl")
expect src.contains("\"--gui\"") == true
expect src.contains("gui_shell_run(session)") == true
expect src.contains("editor_tui_run(session)") == true
```

</details>

### editor MCP tools

#### defines editor_mcp_tools registry

- defines editor_mcp_tools registry


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("defines editor_mcp_tools registry")
val src = read_text("src/app/editor/mcp_tools.spl")
expect src.contains("fn editor_mcp_tools() -> [McpToolDef]") == true
```

</details>

#### registers 8 MCP tools

- registers 8 MCP tools


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("registers 8 MCP tools")
val src = read_text("src/app/editor/mcp_tools.spl")
expect src.contains("editor.open_file") == true
expect src.contains("editor.read_buffer") == true
expect src.contains("editor.edit") == true
expect src.contains("editor.search") == true
expect src.contains("editor.diagnostics") == true
expect src.contains("editor.goto_definition") == true
expect src.contains("editor.list_open_files") == true
expect src.contains("editor.run_command") == true
```

</details>

#### has editor_mcp_dispatch for tool execution

- has editor_mcp_dispatch for tool execution


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("has editor_mcp_dispatch for tool execution")
val src = read_text("src/app/editor/mcp_tools.spl")
expect src.contains("fn editor_mcp_dispatch(session: EditSession, tool_name: text, args: [text]) -> McpToolResult") == true
```

</details>

#### dispatches registered navigation MCP tools

- dispatches registered navigation MCP tools


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("dispatches registered navigation MCP tools")
val src = read_text("src/app/editor/mcp_tools.spl")
expect src.contains("if tool_name == \"editor.goto_definition\"") == true
expect src.contains("ctrl._request_active_definition()") == true
expect src.contains("ctrl_cmd._execute_palette_command(args[0], \"\")") == true
```

</details>

#### maps VSCode-style GUI shortcuts to editor IDE actions

- maps VSCode-style GUI shortcuts to editor IDE actions


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("maps VSCode-style GUI shortcuts to editor IDE actions")
val src = read_text("src/app/editor/gui_shell.spl")
expect src.contains("gui_key == \"Ctrl+Space\"") == true
expect src.contains("gui_key == \"F12\"") == true
expect src.contains("gui_key == \"Ctrl+.\"") == true
expect src.contains("gui_key == \"Shift+Alt+Right\"") == true
expect src.contains("gui_key == \"Shift+Alt+Left\"") == true
expect src.contains("gui_key == \"F5\"") == true
expect src.contains("_request_active_lsp_action(\"completion\")") == true
expect src.contains("_request_active_lsp_action(\"definition\")") == true
expect src.contains("_expand_lsp_selection_range()") == true
expect src.contains("_shrink_lsp_selection_range()") == true
expect src.contains("_start_debug_adapter(\"simple\")") == true
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Other |
| Status | Active |
| Source | `test/03_system/.spipe_wrapped_entry_editor_gui_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering editor platform — detection, editor config — SDN settings, editor TUI backend — formalized, editor GUI backend — Surface integration, editor headless backend — testing, editor SimpleOS adapter, editor GUI shell, editor GUI backend — rendering, editor main — GUI flag, editor MCP tools.
- editor platform — detection
- editor config — SDN settings
- editor TUI backend — formalized
- editor GUI backend — Surface integration
- editor headless backend — testing
- editor SimpleOS adapter
- editor GUI shell
- editor GUI backend — rendering
- editor main — GUI flag
- editor MCP tools

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 80 |
| Active scenarios | 80 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-SYSTEM`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `f4cf17549ad2f5c673534ff222579ce019904cbfb9838674e0e3ced66d6e66f7`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `f4cf17549ad2f5c673534ff222579ce019904cbfb9838674e0e3ced66d6e66f7`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `f4cf17549ad2f5c673534ff222579ce019904cbfb9838674e0e3ced66d6e66f7`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **91/100**; effective score: **91/100**; blockers: **0**.

SSpec documentization score: 91/100
source: test/03_system/.spipe_wrapped_entry_editor_gui_spec.spl
mirror: doc/06_spec/03_system/.spipe_wrapped_entry_editor_gui_spec.md (current)
findings: 6 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=55
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/.spipe_wrapped_entry_editor_gui_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/.spipe_wrapped_entry_editor_gui_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, scope, assumptions/preconditions, primary workflow, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/.spipe_wrapped_entry_editor_gui_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/03_system/.spipe_wrapped_entry_editor_gui_spec.spl:65:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'defines EditorPlatform enum' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/.spipe_wrapped_entry_editor_gui_spec.spl:74:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'defines PlatformConfig with resource limits' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/.spipe_wrapped_entry_editor_gui_spec.spl:83:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'has platform_default_config per platform' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
