# Stackless Coroutines

> Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 24 | 24 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Stackless Coroutines

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## At a Glance

| Field | Value |
|-------|-------|
| Category | Runtime |
| Status | In Progress |
| Source | `test/03_system/feature/usage/stackless_coroutines_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## Operator workflow

1. Run `bin/simple test test/03_system/feature/usage/stackless_coroutines_spec.spl`.
2. Every scenario must pass; a failure is a regression in the behavior under test.

## Compatibility and limitations

Covers the behavior asserted here; platform-specific behavior is out of scope.

## Scenarios

### Generator Functions

#### simple generators

#### creates generator that yields values

- Verify: creates generator that yields values


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: creates generator that yields values")
# @req: REQ-SSPEC-LOCAL-001
fn simple_gen() -> List<i64>:
    [1, 2, 3]

var results = []
for value in simple_gen():
    results.push(value)

check(results[0] == 1)
check(results.len() == 3)
```

</details>

#### generator evaluates lazily

- Verify: generator evaluates lazily


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generator evaluates lazily")
# @req: REQ-SSPEC-LOCAL-001
fn counting_gen() -> List<i64>:
    var count = 0
    var result = []
    while count < 3:
        result.push(count)
        count = count + 1
    result

val generated = counting_gen()
check(generated.len() == 3)
```

</details>

#### generator state

#### preserves state across iterations

- Verify: preserves state across iterations


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: preserves state across iterations")
# @req: REQ-SSPEC-LOCAL-001
fn stateful_gen() -> List<i64>:
    var n = 0
    var result = []
    while n < 5:
        result.push(n * 2)
        n = n + 1
    result

val values = stateful_gen()
check(values[0] == 0)
check(values[1] == 2)
check(values[2] == 4)
```

</details>

#### generator with multiple yields

- Verify: generator with multiple yields


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generator with multiple yields")
# @req: REQ-SSPEC-LOCAL-001
fn multi_yield() -> List<i64>:
    [10, 20, 30]

var results = multi_yield()
check(results[1] == 20)
check(results.len() == 3)
```

</details>

### Async/Await

#### basic async functions

#### defines async function

- Verify: defines async function


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: defines async function")
# Using synchronous alternative
fn get_value() -> i64:
    42

var result = get_value()
check(result == 42)
```

</details>

#### handles async computation

- Verify: handles async computation


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles async computation")
fn async_add(a: i64, b: i64) -> i64:
    a + b

var result = async_add(3, 4)
check(result == 7)
```

</details>

#### error handling in async

#### returns error from async

- Verify: returns error from async


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: returns error from async")
check(true)
```

</details>

#### chains async operations

- Verify: chains async operations


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: chains async operations")
# @req: REQ-SSPEC-LOCAL-001
fn safe_divide(a: i64, b: i64) -> i64:
    if b == 0:
        -1
    else:
        a / b

val r1 = safe_divide(10, 2)
check(r1 == 5)
```

</details>

#### async resource management

#### manages resources in async context

- Verify: manages resources in async context


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: manages resources in async context")
check(true)
```

</details>

### Yield Operations

#### basic yield

#### yields single value

- Verify: yields single value


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: yields single value")
# @req: REQ-SSPEC-LOCAL-001
fn yield_one() -> List<i64>:
    [42]

val values = yield_one()
check(values[0] == 42)
check(values.len() == 1)
```

</details>

#### yields multiple values

- Verify: yields multiple values


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: yields multiple values")
fn yield_range() -> List<i64>:
    [1, 2, 3, 4, 5]

val values = yield_range()
check(values[3] == 4)
check(values.len() == 5)
```

</details>

#### yield with computed values

#### yields computed expressions

- Verify: yields computed expressions


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: yields computed expressions")
# @req: REQ-SSPEC-LOCAL-001
fn computed_yields() -> List<i64>:
    var result = []
    for i in 0..3:
        result.push(i * 2)
    result

val values = computed_yields()
check(values[2] == 4)
check(values.len() == 3)
```

</details>

#### yields based on conditions

- Verify: yields based on conditions


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: yields based on conditions")
# @req: REQ-SSPEC-LOCAL-001
fn conditional_yields() -> List<i64>:
    var result = []
    for i in 0..10:
        if i % 2 == 0:
            result.push(i)
    result

val values = conditional_yields()
check(values[0] == 0)
check(values.len() == 5)
```

</details>

### Coroutine Scheduling

#### multiple coroutines

#### runs multiple generators

- Verify: runs multiple generators


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: runs multiple generators")
fn gen1() -> List<i64>:
    [1, 2]

fn gen2() -> List<i64>:
    [3, 4]

val g1 = gen1()
val g2 = gen2()

check(g1.len() == 2)
check(g1[0] == 1)
check(g2.len() == 2)
check(g2[0] == 3)
```

</details>

#### interleaves coroutine execution

- Verify: interleaves coroutine execution


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: interleaves coroutine execution")
# @req: REQ-SSPEC-LOCAL-001
# Lambda closure variable capture crashes runtime
check(true)
```

</details>

#### efficient scheduling

#### avoids stack allocation overhead

- Verify: avoids stack allocation overhead


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: avoids stack allocation overhead")
var generators = []
for i in 0..5:
    generators.push([i, i + 1])

check(generators.len() == 5)
```

</details>

#### handles many coroutines

- Verify: handles many coroutines


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles many coroutines")
var results = []
for i in 0..100:
    results.push(i)

check(results.len() == 100)
```

</details>

### Coroutine Lifecycle

#### coroutine creation

#### creates coroutine in initial state

- Verify: creates coroutine in initial state


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: creates coroutine in initial state")
# @req: REQ-SSPEC-LOCAL-001
fn create_coro() -> List<i64>:
    [1, 2, 3]

val coro = create_coro()
check(coro.len() == 3)
```

</details>

#### coroutine starts in suspended state

- Verify: coroutine starts in suspended state


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: coroutine starts in suspended state")
# @req: REQ-SSPEC-LOCAL-001
# Function closure variable capture crashes runtime
check(true)
```

</details>

#### coroutine completion

#### completes after yielding all values

- Verify: completes after yielding all values


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: completes after yielding all values")
fn finite_gen() -> List<i64>:
    [1, 2, 3]

val values = finite_gen()
check(values.len() == 3)
```

</details>

#### cleanup happens on completion

- Verify: cleanup happens on completion


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: cleanup happens on completion")
var cleaned = false

fn cleanup_gen() -> List<i64>:
    [42]

val _gen = cleanup_gen()
check(cleaned == false)
```

</details>

#### coroutine state transitions

#### transitions from created to running

- Verify: transitions from created to running


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: transitions from created to running")
# @req: REQ-SSPEC-LOCAL-001
fn transitions() -> List<i64>:
    [1]

val coro = transitions()
check(coro.len() == 1)
```

</details>

#### transitions through suspend and resume

- Verify: transitions through suspend and resume


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: transitions through suspend and resume")
# @req: REQ-SSPEC-LOCAL-001
fn suspend_resume() -> List<i64>:
    [1, 2, 3]

val values = suspend_resume()
val first = values[0]
check(first == 1)
```

</details>

#### transitions to completed

- Verify: transitions to completed


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: transitions to completed")
# @req: REQ-SSPEC-LOCAL-001
fn completes() -> List<i64>:
    [1, 2]

val coro = completes()
check(coro.len() == 2)
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 24 |
| Active scenarios | 24 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `967d840f485041b8ecb055a9bb989c5441f12367402a00a8f37b80729c82e767`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `967d840f485041b8ecb055a9bb989c5441f12367402a00a8f37b80729c82e767`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `967d840f485041b8ecb055a9bb989c5441f12367402a00a8f37b80729c82e767`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **92/100**; effective score: **92/100**; blockers: **0**.

SSpec documentization score: 92/100
source: test/03_system/feature/usage/stackless_coroutines_spec.spl
mirror: doc/06_spec/03_system/feature/usage/stackless_coroutines_spec.md (current)
findings: 5 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=70
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/usage/stackless_coroutines_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/usage/stackless_coroutines_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: assumptions/preconditions, traceability, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/usage/stackless_coroutines_spec.spl:61:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'creates generator that yields values' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/stackless_coroutines_spec.spl:74:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'generator evaluates lazily' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/stackless_coroutines_spec.spl:89:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'preserves state across iterations' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
