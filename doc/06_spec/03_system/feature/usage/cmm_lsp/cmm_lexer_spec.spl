# CMM Lexer Specification

> Purpose: lexes semicolon comment

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 95 | 95 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# CMM Lexer Specification

Purpose: lexes semicolon comment

## At a Glance

| Field | Value |
|-------|-------|
| Feature IDs | #CMM-LEX |
| Category | Tooling |
| Status | Implemented |
| Source | `test/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience
Purpose: lexes semicolon comment
Audience: compiler and tooling engineers who maintain this spec

# CMM Lexer Specification

**Feature ID:** #CMM-LEX
**Category:** Tooling
**Status:** Implemented

## Overview

Tests for the CMM (PRACTICE Script) lexer. Validates tokenization of all
CMM lexical elements: comments, labels, macros, numbers, strings, dot
commands, operators, options, file channels, and full lines.

Interpreter mode: it block bodies don't execute. Tests verify that
all CMM lexer concepts are structurally valid Simple code that loads
without errors.

## Scenarios

### CMM Lexer - Comments

#### lexes semicolon comment

- Verify: lexes semicolon comment
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes semicolon comment")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("; this is a comment", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes double-slash comment

- Verify: lexes double-slash comment
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes double-slash comment")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("// another comment", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes semicolon comment with no trailing text

- Verify: lexes semicolon comment with no trailing text
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes semicolon comment with no trailing text")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line(";", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes double-slash comment with no trailing text

- Verify: lexes double-slash comment with no trailing text
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes double-slash comment with no trailing text")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("//", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes inline comment after whitespace

- Verify: lexes inline comment after whitespace
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes inline comment after whitespace")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ; inline comment", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Labels

#### lexes label at column 1

- Verify: lexes label at column 1
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes label at column 1")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("start:", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes label with underscore

- Verify: lexes label with underscore
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes label with underscore")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("_my_label:", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes label with alphanumeric chars

- Verify: lexes label with alphanumeric chars
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes label with alphanumeric chars")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("FlashSetup3:", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### distinguishes label from device selector

- Verify: distinguishes label from device selector
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: distinguishes label from device selector")
# @req: REQ-FEATURE-CmmLexe-001
# B:: is a device selector, not a label
val tokens = lex_cmm_line("B::", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### returns empty for empty line

- Verify: returns empty for empty line
   - Expected: tokens.len() equals `0)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns empty for empty line")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("", 1)
expect(tokens.len()).to_equal(0)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Macro References

#### lexes simple macro ref

- Verify: lexes simple macro ref
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes simple macro ref")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  &myvar", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes recursive macro ref

- Verify: lexes recursive macro ref
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes recursive macro ref")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  &&myvar", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes bare ampersand as operator when no identifier follows

- Verify: lexes bare ampersand as operator when no identifier follows
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes bare ampersand as operator when no identifier follows")
# @req: REQ-FEATURE-CmmLexe-001
# & without an identifier after it is the binary AND operator
val tokens = lex_cmm_line("  &", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes bare double-ampersand as logical AND when no identifier follows

- Verify: lexes bare double-ampersand as logical AND when no identifier follows
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes bare double-ampersand as logical AND when no identifier follows")
# @req: REQ-FEATURE-CmmLexe-001
# && without an identifier is the logical AND operator
val tokens = lex_cmm_line("  &&", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes macro ref with underscore in name

- Verify: lexes macro ref with underscore in name
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes macro ref with underscore in name")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  &my_var_1", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Hex Numbers

#### lexes hex number with lowercase 0x

- Verify: lexes hex number with lowercase 0x
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes hex number with lowercase 0x")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  0x1000", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes hex number with uppercase 0X

- Verify: lexes hex number with uppercase 0X
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes hex number with uppercase 0X")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  0XABCDEF", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes hex mask with dont-care bits

- Verify: lexes hex mask with dont-care bits
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes hex mask with dont-care bits")
# @req: REQ-FEATURE-CmmLexe-001
# 0xFX has dont-care nibble
val tokens = lex_cmm_line("  0xFX", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes hex mask with multiple dont-care bits

- Verify: lexes hex mask with multiple dont-care bits
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes hex mask with multiple dont-care bits")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  0xff1cxxxx", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Binary Numbers

#### lexes binary number with 0y prefix

- Verify: lexes binary number with 0y prefix
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes binary number with 0y prefix")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  0y101", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes binary mask with dont-care bits

- Verify: lexes binary mask with dont-care bits
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes binary mask with dont-care bits")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  0yX111XXX", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Decimal and Integer Numbers

#### lexes decimal number with trailing dot

- Verify: lexes decimal number with trailing dot
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes decimal number with trailing dot")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  100.", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes plain integer

- Verify: lexes plain integer
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes plain integer")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  1234", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes float number

- Verify: lexes float number
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes float number")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  1.3", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes float with exponent

- Verify: lexes float with exponent
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes float with exponent")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  1.3e+34", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Time Literals

#### lexes millisecond time literal

- Verify: lexes millisecond time literal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes millisecond time literal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  10ms", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes microsecond time literal

- Verify: lexes microsecond time literal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes microsecond time literal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  100us", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes nanosecond time literal

- Verify: lexes nanosecond time literal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes nanosecond time literal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  75ns", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes second time literal

- Verify: lexes second time literal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes second time literal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  10s", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes float time literal

- Verify: lexes float time literal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes float time literal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  23.24ms", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Strings

#### lexes simple string

- Verify: lexes simple string
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes simple string")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  \"hello world\"", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes empty string

- Verify: lexes empty string
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes empty string")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  \"\"", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes string with escaped double quote

- Verify: lexes string with escaped double quote
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes string with escaped double quote")
# @req: REQ-FEATURE-CmmLexe-001
# In CMM, "" inside a string is an escaped double-quote
val tokens = lex_cmm_line("  \"abc\"\"def\"", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Char Literals

#### lexes single character literal

- Verify: lexes single character literal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes single character literal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  'A'", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes lowercase char literal

- Verify: lexes lowercase char literal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes lowercase char literal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  'z'", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Dot Commands

#### lexes simple dot command

- Verify: lexes simple dot command
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes simple dot command")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  Data.dump", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes two-level dot command

- Verify: lexes two-level dot command
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes two-level dot command")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  SYStem.CPU", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes multi-level dot command

- Verify: lexes multi-level dot command
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes multi-level dot command")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  FLASH.ReProgram.ALL", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes dot command with reset

- Verify: lexes dot command with reset
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes dot command with reset")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  FLASH.RESet", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Comparison Operators

#### lexes equality operator

- Verify: lexes equality operator
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes equality operator")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ==", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes not-equal operator

- Verify: lexes not-equal operator
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes not-equal operator")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  !=", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes less-than-or-equal

- Verify: lexes less-than-or-equal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes less-than-or-equal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  <=", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes greater-than-or-equal

- Verify: lexes greater-than-or-equal
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes greater-than-or-equal")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  >=", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Range Operators

#### lexes range-to operator

- Verify: lexes range-to operator
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes range-to operator")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  --", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes range-offset operator

- Verify: lexes range-offset operator
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes range-offset operator")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ++", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes range-dots operator

- Verify: lexes range-dots operator
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes range-dots operator")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ..", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Logical Operators

#### lexes logical AND

- Verify: lexes logical AND
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes logical AND")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  &&", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes logical OR

- Verify: lexes logical OR
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes logical OR")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ||", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes logical XOR

- Verify: lexes logical XOR
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes logical XOR")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ^^", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Shift Operators

#### lexes shift left

- Verify: lexes shift left
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes shift left")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  <<", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes shift right

- Verify: lexes shift right
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes shift right")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  >>", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Classic Operators

#### lexes classic AND colon form

- Verify: lexes classic AND colon form
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes classic AND colon form")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  :A:", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes classic OR colon form

- Verify: lexes classic OR colon form
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes classic OR colon form")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  :O:", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes classic XOR colon form

- Verify: lexes classic XOR colon form
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes classic XOR colon form")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  :X:", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Single-Char Operators

#### lexes plus

- Verify: lexes plus
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes plus")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  +", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes minus

- Verify: lexes minus
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes minus")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  -", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes star

- Verify: lexes star
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes star")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  *", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes percent

- Verify: lexes percent
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes percent")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  %", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes tilde

- Verify: lexes tilde
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes tilde")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ~", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes bang

- Verify: lexes bang
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes bang")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  !", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes assign

- Verify: lexes assign
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes assign")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  =", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Delimiters

#### lexes left paren

- Verify: lexes left paren
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes left paren")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  (", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes right paren

- Verify: lexes right paren
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes right paren")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  )", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes left brace

- Verify: lexes left brace
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes left brace")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  {", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes right brace

- Verify: lexes right brace
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes right brace")
# @req: REQ-FEATURE-CmmLexe-001
# Using }}} to produce single } inside string interpolation
val tokens = lex_cmm_line("  }", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes comma

- Verify: lexes comma
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes comma")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ,", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Options

#### lexes option token

- Verify: lexes option token
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes option token")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  /Write", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes read option

- Verify: lexes read option
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes read option")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  /Read", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes create option

- Verify: lexes create option
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes create option")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  /Create", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - File Channels

#### lexes file channel 1

- Verify: lexes file channel 1
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes file channel 1")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  #1", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes file channel 2

- Verify: lexes file channel 2
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes file channel 2")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  #2", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Hash Classic Operators

#### lexes hash classic AND

- Verify: lexes hash classic AND
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes hash classic AND")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  #A#", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes hash classic OR

- Verify: lexes hash classic OR
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes hash classic OR")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  #O#", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes hash classic XOR

- Verify: lexes hash classic XOR
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes hash classic XOR")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  #X#", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Device Selectors

#### lexes device selector B::

- Verify: lexes device selector B::
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes device selector B::")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("B::", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes device selector E::

- Verify: lexes device selector E::
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes device selector E::")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("E::", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Identifiers

#### lexes simple identifier

- Verify: lexes simple identifier
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes simple identifier")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  Step", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes uppercase identifier

- Verify: lexes uppercase identifier
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes uppercase identifier")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  ENDDO", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### lexes identifier with underscore

- Verify: lexes identifier with underscore
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes identifier with underscore")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  my_var", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

### CMM Lexer - Full Lines

#### lexes command with parameter

- Verify: lexes command with parameter


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes command with parameter")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  SYStem.CPU 78K0R", 1)
expect(tokens.len()).to_be_greater_than(1)
```

</details>

#### lexes command with hex parameter

- Verify: lexes command with hex parameter


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes command with hex parameter")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  Data.dump 0x1000", 1)
expect(tokens.len()).to_be_greater_than(1)
```

</details>

#### lexes macro assignment

- Verify: lexes macro assignment


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes macro assignment")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  &cpu=CPU()", 1)
expect(tokens.len()).to_be_greater_than(1)
```

</details>

#### lexes command with option

- Verify: lexes command with option


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes command with option")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  FLASH.Create 1. 0x0--0xFFF /Write", 1)
expect(tokens.len()).to_be_greater_than(3)
```

</details>

#### lexes print with string

- Verify: lexes print with string


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes print with string")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  PRINT \"hello world\"", 1)
expect(tokens.len()).to_be_greater_than(1)
```

</details>

#### lexes write with file channel

- Verify: lexes write with file channel


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes write with file channel")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  WRITE #1 \"data\"", 1)
expect(tokens.len()).to_be_greater_than(1)
```

</details>

#### lexes range expression

- Verify: lexes range expression


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes range expression")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  0x1000--0x1FFF", 1)
expect(tokens.len()).to_be_greater_than(1)
```

</details>

### CMM Lexer - Full Source

#### lexes multi-line CMM source

- Verify: lexes multi-line CMM source


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes multi-line CMM source")
# @req: REQ-FEATURE-CmmLexe-001
val source = "; comment\nstart:\n  SYStem.CPU ARM\n  ENDDO\n"
val tokens = lex_cmm_source(source)
# Should have: Comment, Newline, Label, Newline, DotCommand, Identifier, Newline, Identifier, Newline, Eof
expect(tokens.len()).to_be_greater_than(4)
```

</details>

#### lexes empty source

- Verify: lexes empty source


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes empty source")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_source("")
# At minimum there should be an Eof token
expect(tokens.len()).to_be_greater_than(0)
```

</details>

#### lexes source with line continuation

- Verify: lexes source with line continuation


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes source with line continuation")
# @req: REQ-FEATURE-CmmLexe-001
val source = "  Data.dump \\\n  0x1000\n"
val tokens = lex_cmm_source(source)
expect(tokens.len()).to_be_greater_than(2)
```

</details>

#### lexes real-world flash script

- Verify: lexes real-world flash script


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: lexes real-world flash script")
# @req: REQ-FEATURE-CmmLexe-001
val source = "; Flash programming\nFlashSetup:\n  LOCAL &size\n  ENTRY &size\n  FLASH.RESet\n  FLASH.Create 1. 0x0--0xFFF\n  RETURN\n"
val tokens = lex_cmm_source(source)
expect(tokens.len()).to_be_greater_than(10)
```

</details>

### CMM Lexer - Edge Cases

#### handles whitespace-only line

- Verify: handles whitespace-only line
   - Expected: tokens.len() equals `0)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles whitespace-only line")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("   ", 1)
expect(tokens.len()).to_equal(0)  # oracle: value fixed by the spec contract
```

</details>

#### handles tab whitespace

- Verify: handles tab whitespace
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles tab whitespace")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("\tStep", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### preserves line numbers

- Verify: preserves line numbers
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: preserves line numbers")
# @req: REQ-FEATURE-CmmLexe-001
val tokens = lex_cmm_line("  Step", 42)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### handles classic NOT operator N:

- Verify: handles classic NOT operator N:
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles classic NOT operator N:")
# @req: REQ-FEATURE-CmmLexe-001
# N: is the classic NOT prefix operator
val tokens = lex_cmm_line("  N:", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### handles slash as division when not followed by alpha

- Verify: handles slash as division when not followed by alpha
   - Expected: tokens.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles slash as division when not followed by alpha")
# @req: REQ-FEATURE-CmmLexe-001
# / alone (not followed by alpha) should be Slash operator, not Option
val tokens = lex_cmm_line("  /", 1)
expect(tokens.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 95 |
| Active scenarios | 95 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `d864c5dda348fe49ba17c9e3f120510157da75c3688ca2687afcc850625b4503`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `d864c5dda348fe49ba17c9e3f120510157da75c3688ca2687afcc850625b4503`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `d864c5dda348fe49ba17c9e3f120510157da75c3688ca2687afcc850625b4503`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **87/100**; effective score: **87/100**; blockers: **0**.

SSpec documentization score: 87/100
source: test/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.spl
mirror: doc/06_spec/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.md (current)
findings: 8 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=55 coverage=100 maintainability=45
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.md:1:1: warning SSDOC-EVD-002 [evidence] (-15): source steps are not visible in the generated manual
  why: Source tokens alone do not prove reader-visible workflow structure.
  improve: Use supported literal step calls and regenerate the manual.
doc/06_spec/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: scope, assumptions/preconditions, primary workflow, traceability, evidence, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.spl:1:1: advice SSDOC-MNT-007 [maintainability] (-10): research, plan, architecture, or design metadata links are incomplete
  why: Reviewers need selected lifecycle evidence, not inferred project state.
  improve: Link the selected lifecycle artifacts or configure a reasoned scope suppression.
test/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.spl:79:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'lexes semicolon comment' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.spl:85:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'lexes double-slash comment' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/cmm_lsp/cmm_lexer_spec.spl:91:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'lexes semicolon comment with no trailing text' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
