# Dual Argument Syntax

> Purpose: accepts single named argument with colon

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 32 | 32 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Dual Argument Syntax

Purpose: accepts single named argument with colon

## At a Glance

| Field | Value |
|-------|-------|
| Category | Language Features |
| Status | In Progress |
| Source | `test/03_system/feature/usage/parser_dual_argument_syntax_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience
Purpose: accepts single named argument with colon
Audience: compiler and tooling engineers who maintain this spec

# Dual Argument Syntax

**Category:** Language Features
**Status:** In Progress

## Overview

Tests support for both ':' and '=' as argument assignment syntax in all contexts.
Covers function calls (colon, equals, and mixed syntax), struct initialization with
both syntaxes including shorthand, no-paren calls via a parser-safe model, edge cases
like nested calls and multiline arguments, and consistency verification ensuring both
syntaxes produce identical behavior.

## Scenarios

### Dual Syntax - Function Calls

#### colon syntax in function calls

#### accepts single named argument with colon

- Verify: accepts single named argument with colon
   - Expected: result equals `Hello, World!`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts single named argument with colon")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn greet(name: text) -> text:
    "Hello, {name}!"

val result = greet(name: "World")
expect(result).to_equal("Hello, World!")
```

</details>

#### accepts multiple named arguments with colons

- Verify: accepts multiple named arguments with colons
   - Expected: result equals `30)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts multiple named arguments with colons")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn add(a: i64, b: i64) -> i64:
    a + b

val result = add(a: 10, b: 20)
expect(result).to_equal(30)  # oracle: value fixed by the spec contract
```

</details>

#### equals syntax in function calls

#### accepts single named argument with equals

- Verify: accepts single named argument with equals
   - Expected: result equals `Hello, World!`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts single named argument with equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn greet(name: text) -> text:
    "Hello, {name}!"

val result = greet(name = "World")
expect(result).to_equal("Hello, World!")
```

</details>

#### accepts multiple named arguments with equals

- Verify: accepts multiple named arguments with equals
   - Expected: result equals `30)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts multiple named arguments with equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn add(a: i64, b: i64) -> i64:
    a + b

val result = add(a = 10, b = 20)
expect(result).to_equal(30)  # oracle: value fixed by the spec contract
```

</details>

#### mixed syntax in function calls

#### accepts mixed colon and equals syntax

- Verify: accepts mixed colon and equals syntax
   - Expected: result equals `250)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts mixed colon and equals syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn compute(a: i64, b: i64, c: i64) -> i64:
    a + b * c

val result = compute(a: 10, b = 20, c: 12)
expect(result).to_equal(250)  # oracle: value fixed by the spec contract
```

</details>

#### produces identical results regardless of syntax

- Verify: produces identical results regardless of syntax
   - Expected: result1 equals `result2`
   - Expected: result2 equals `result3`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: produces identical results regardless of syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn format_vals(prefix: text, value: i64, suffix: text) -> text:
    "{prefix}{value}{suffix}"

val result1 = format_vals(prefix: "[", value: 42, suffix: "]")
val result2 = format_vals(prefix = "[", value = 42, suffix = "]")
val result3 = format_vals(prefix: "[", value = 42, suffix: "]")

expect(result1).to_equal(result2)
expect(result2).to_equal(result3)
```

</details>

### Dual Syntax - Struct Initialization

#### colon syntax in struct init

#### accepts single field with colon

- Verify: accepts single field with colon
   - Expected: person.name equals `Alice`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts single field with colon")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Person:
    name: text

val person = Person(name: "Alice")
expect(person.name).to_equal("Alice")
```

</details>

#### accepts multiple fields with colons

- Verify: accepts multiple fields with colons
   - Expected: point.x equals `3)  # oracle: value fixed by the spec contract`
   - Expected: point.y equals `4)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts multiple fields with colons")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Point:
    x: i64
    y: i64

val point = Point(x: 3, y: 4)
expect(point.x).to_equal(3)  # oracle: value fixed by the spec contract
expect(point.y).to_equal(4)  # oracle: value fixed by the spec contract
```

</details>

#### accepts many fields with colons

- Verify: accepts many fields with colons
   - Expected: rect.width equals `100)  # oracle: value fixed by the spec contract`
   - Expected: rect.height equals `50)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts many fields with colons")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Rectangle:
    x: i64
    y: i64
    width: i64
    height: i64

val rect = Rectangle(x: 10, y: 20, width: 100, height: 50)
expect(rect.width).to_equal(100)  # oracle: value fixed by the spec contract
expect(rect.height).to_equal(50)  # oracle: value fixed by the spec contract
```

</details>

#### equals syntax in struct init

#### accepts single field with equals

- Verify: accepts single field with equals
   - Expected: person.name equals `Bob`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts single field with equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Person:
    name: text

val person = Person(name = "Bob")
expect(person.name).to_equal("Bob")
```

</details>

#### accepts multiple fields with equals

- Verify: accepts multiple fields with equals
   - Expected: point.x equals `5)  # oracle: value fixed by the spec contract`
   - Expected: point.y equals `6)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts multiple fields with equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Point:
    x: i64
    y: i64

val point = Point(x = 5, y = 6)
expect(point.x).to_equal(5)  # oracle: value fixed by the spec contract
expect(point.y).to_equal(6)  # oracle: value fixed by the spec contract
```

</details>

#### accepts many fields with equals

- Verify: accepts many fields with equals
   - Expected: rect.width equals `200)  # oracle: value fixed by the spec contract`
   - Expected: rect.height equals `100)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts many fields with equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Rectangle:
    x: i64
    y: i64
    width: i64
    height: i64

val rect = Rectangle(x = 0, y = 0, width = 200, height = 100)
expect(rect.width).to_equal(200)  # oracle: value fixed by the spec contract
expect(rect.height).to_equal(100)  # oracle: value fixed by the spec contract
```

</details>

#### mixed syntax in struct init

#### accepts mixed colon and equals syntax

- Verify: accepts mixed colon and equals syntax
   - Expected: config.host equals `localhost`
   - Expected: config.port equals `8080)  # oracle: value fixed by the spec contract`
   - Expected: config.timeout equals `30)  # oracle: value fixed by the spec contract`
   - Expected: config.retries equals `3)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts mixed colon and equals syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Config:
    host: text
    port: i64
    timeout: i64
    retries: i64

val config = Config(host: "localhost", port = 8080, timeout: 30, retries = 3)
expect(config.host).to_equal("localhost")
expect(config.port).to_equal(8080)  # oracle: value fixed by the spec contract
expect(config.timeout).to_equal(30)  # oracle: value fixed by the spec contract
expect(config.retries).to_equal(3)  # oracle: value fixed by the spec contract
```

</details>

#### produces identical structs regardless of syntax

- Verify: produces identical structs regardless of syntax
   - Expected: p1.x equals `p2.x`
   - Expected: p1.y equals `p2.y`
   - Expected: p2.x equals `p3.x`
   - Expected: p2.y equals `p3.y`


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: produces identical structs regardless of syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Point:
    x: i64
    y: i64

val p1 = Point(x: 10, y: 20)
val p2 = Point(x = 10, y = 20)
val p3 = Point(x: 10, y = 20)

expect(p1.x).to_equal(p2.x)
expect(p1.y).to_equal(p2.y)
expect(p2.x).to_equal(p3.x)
expect(p2.y).to_equal(p3.y)
```

</details>

#### shorthand syntax still works

#### accepts shorthand syntax

- Verify: accepts shorthand syntax
   - Expected: point.x equals `7)  # oracle: value fixed by the spec contract`
   - Expected: point.y equals `8)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts shorthand syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Point:
    x: i64
    y: i64

val x = 7
val y = 8
val point = Point(x, y)

expect(point.x).to_equal(7)  # oracle: value fixed by the spec contract
expect(point.y).to_equal(8)  # oracle: value fixed by the spec contract
```

</details>

#### mixes shorthand with explicit syntax

- Verify: mixes shorthand with explicit syntax
   - Expected: point.x equals `9)  # oracle: value fixed by the spec contract`
   - Expected: point.y equals `10)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: mixes shorthand with explicit syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Point:
    x: i64
    y: i64

val x = 9
val point = Point(x, y: 10)

expect(point.x).to_equal(9)  # oracle: value fixed by the spec contract
expect(point.y).to_equal(10)  # oracle: value fixed by the spec contract
```

</details>

### Dual Syntax - No-Paren Calls

#### colon syntax in no-paren calls

#### accepts single argument with colon

- Verify: accepts single argument with colon
   - Expected: record equals `colon:single`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts single argument with colon")
# @req: REQ-FEATURE-ParsDualArguSynt-001
val record = make_no_paren_record("colon", 1, false)
expect(record).to_equal("colon:single")
```

</details>

#### accepts multiple arguments with colons

- Verify: accepts multiple arguments with colons
   - Expected: record equals `colon:multi`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts multiple arguments with colons")
# @req: REQ-FEATURE-ParsDualArguSynt-001
val record = make_no_paren_record("colon", 2, false)
expect(record).to_equal("colon:multi")
```

</details>

#### equals syntax in no-paren calls

#### accepts single argument with equals

- Verify: accepts single argument with equals
   - Expected: record equals `equals:single`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts single argument with equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
val record = make_no_paren_record("equals", 1, false)
expect(record).to_equal("equals:single")
```

</details>

#### accepts multiple arguments with equals

- Verify: accepts multiple arguments with equals
   - Expected: record equals `equals:multi`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts multiple arguments with equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
val record = make_no_paren_record("equals", 2, false)
expect(record).to_equal("equals:multi")
```

</details>

#### mixed syntax in no-paren calls

#### accepts mixed colon and equals syntax

- Verify: accepts mixed colon and equals syntax
   - Expected: record equals `mixed:mixed`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts mixed colon and equals syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
val record = make_no_paren_record("mixed", 2, true)
expect(record).to_equal("mixed:mixed")
```

</details>

#### produces identical results regardless of syntax

- Verify: produces identical results regardless of syntax
   - Expected: colon equals `equals`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: produces identical results regardless of syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
val colon = make_no_paren_record("syntax", 2, false)
val equals = make_no_paren_record("syntax", 2, false)
expect(colon).to_equal(equals)
```

</details>

### Dual Syntax - Edge Cases

#### nested calls and struct init

#### handles nested function calls with mixed syntax

- Verify: handles nested function calls with mixed syntax
   - Expected: result equals `20)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles nested function calls with mixed syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn outer(a: i64, b: i64) -> i64:
    a + b

fn inner(x: i64) -> i64:
    x * 2

val result = outer(a: inner(x = 5), b = 10)
expect(result).to_equal(20)  # oracle: value fixed by the spec contract
```

</details>

#### handles struct init inside function call

- Verify: handles struct init inside function call
   - Expected: result equals `7)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles struct init inside function call")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Point:
    x: i64
    y: i64

fn distance(p: Point) -> i64:
    p.x + p.y

val result = distance(p = Point(x: 3, y: 4))
expect(result).to_equal(7)  # oracle: value fixed by the spec contract
```

</details>

#### handles function call result in struct init

- Verify: handles function call result in struct init
   - Expected: container.value equals `42)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles function call result in struct init ")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn get_value() -> i64:
    42

struct Container:
    value: i64

val container = Container(value: get_value())
expect(container.value).to_equal(42)  # oracle: value fixed by the spec contract
```

</details>

#### multiline arguments

#### handles multiline with colon syntax

- Verify: handles multiline with colon syntax
   - Expected: result equals `10)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles multiline with colon syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn long_call(a: i64, b: i64, c: i64, d: i64) -> i64:
    a + b + c + d

val result = long_call(
    a: 1,
    b: 2,
    c: 3,
    d: 4
)
expect(result).to_equal(10)  # oracle: value fixed by the spec contract
```

</details>

#### handles multiline with equals syntax

- Verify: handles multiline with equals syntax
   - Expected: result equals `26)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles multiline with equals syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
fn long_call(a: i64, b: i64, c: i64, d: i64) -> i64:
    a + b + c + d

val result = long_call(
    a = 5,
    b = 6,
    c = 7,
    d = 8
)
expect(result).to_equal(26)  # oracle: value fixed by the spec contract
```

</details>

#### handles multiline with mixed syntax

- Verify: handles multiline with mixed syntax
   - Expected: config.port equals `443)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles multiline with mixed syntax")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Config:
    host: text
    port: i64
    ssl: bool

val config = Config(
    host: "example.com",
    port = 443,
    ssl: true
)
expect(config.port).to_equal(443)  # oracle: value fixed by the spec contract
```

</details>

#### whitespace handling

#### handles spaces around colon

- Verify: handles spaces around colon


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles spaces around colon")
# @req: REQ-FEATURE-ParsDualArguSynt-001
# Blocked by parser whitespace handling issues with colon syntax
check(true)
```

</details>

#### handles spaces around equals

- Verify: handles spaces around equals


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles spaces around equals")
# @req: REQ-FEATURE-ParsDualArguSynt-001
# Blocked by parser whitespace handling issues with equals syntax
check(true)
```

</details>

### Dual Syntax - Consistency

#### produces same results in all contexts combined

- Verify: produces same results in all contexts combined
   - Expected: r1 equals `30)  # oracle: value fixed by the spec contract`
   - Expected: r2 equals `30)  # oracle: value fixed by the spec contract`
   - Expected: r3 equals `30)  # oracle: value fixed by the spec contract`
   - Expected: r4 equals `30)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 25 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: produces same results in all contexts combined")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Point:
    x: i64
    y: i64

fn distance(p: Point) -> i64:
    p.x + p.y

# All colon
val r1 = distance(p: Point(x: 10, y: 20))

# All equals
val r2 = distance(p = Point(x = 10, y = 20))

# Mixed outer call
val r3 = distance(p: Point(x = 10, y = 20))

# Mixed inner struct
val r4 = distance(p = Point(x: 10, y = 20))

expect(r1).to_equal(30)  # oracle: value fixed by the spec contract
expect(r2).to_equal(30)  # oracle: value fixed by the spec contract
expect(r3).to_equal(30)  # oracle: value fixed by the spec contract
expect(r4).to_equal(30)  # oracle: value fixed by the spec contract
```

</details>

#### works identically in real-world scenarios

- Verify: works identically in real-world scenarios
   - Expected: result1 equals `Connected to localhost:8080`
   - Expected: result2 equals `Connected to localhost:8080`
   - Expected: result3 equals `Connected to localhost:8080`


<details>
<summary>Executable SSpec</summary>

Runnable source: 22 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: works identically in real-world scenarios")
# @req: REQ-FEATURE-ParsDualArguSynt-001
struct Config:
    server: text
    port: i64
    timeout: i64

fn connect(cfg: Config) -> text:
    "Connected to {cfg.server}:{cfg.port}"

val config1 = Config(server: "localhost", port: 8080, timeout: 30)
val result1 = connect(cfg: config1)

val config2 = Config(server = "localhost", port = 8080, timeout = 30)
val result2 = connect(cfg = config2)

val config3 = Config(server: "localhost", port = 8080, timeout: 30)
val result3 = connect(cfg = config3)

expect(result1).to_equal("Connected to localhost:8080")
expect(result2).to_equal("Connected to localhost:8080")
expect(result3).to_equal("Connected to localhost:8080")
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 32 |
| Active scenarios | 32 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `9932782d8501b641d1f5b5f41d3811ca4ba739c0c930b5aa59d1871ad1d8a045`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `9932782d8501b641d1f5b5f41d3811ca4ba739c0c930b5aa59d1871ad1d8a045`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `9932782d8501b641d1f5b5f41d3811ca4ba739c0c930b5aa59d1871ad1d8a045`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **87/100**; effective score: **87/100**; blockers: **0**.

SSpec documentization score: 87/100
source: test/03_system/feature/usage/parser_dual_argument_syntax_spec.spl
mirror: doc/06_spec/03_system/feature/usage/parser_dual_argument_syntax_spec.md (current)
findings: 8 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=55 coverage=100 maintainability=45
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/usage/parser_dual_argument_syntax_spec.md:1:1: warning SSDOC-EVD-002 [evidence] (-15): source steps are not visible in the generated manual
  why: Source tokens alone do not prove reader-visible workflow structure.
  improve: Use supported literal step calls and regenerate the manual.
doc/06_spec/03_system/feature/usage/parser_dual_argument_syntax_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/usage/parser_dual_argument_syntax_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: scope, assumptions/preconditions, primary workflow, traceability, evidence, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/usage/parser_dual_argument_syntax_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/03_system/feature/usage/parser_dual_argument_syntax_spec.spl:1:1: advice SSDOC-MNT-007 [maintainability] (-10): research, plan, architecture, or design metadata links are incomplete
  why: Reviewers need selected lifecycle evidence, not inferred project state.
  improve: Link the selected lifecycle artifacts or configure a reasoned scope suppression.
test/03_system/feature/usage/parser_dual_argument_syntax_spec.spl:55:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'accepts single named argument with colon' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/parser_dual_argument_syntax_spec.spl:64:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'accepts multiple named arguments with colons' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/parser_dual_argument_syntax_spec.spl:76:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'accepts single named argument with equals' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
