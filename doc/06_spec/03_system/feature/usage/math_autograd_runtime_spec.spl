# Math Blocks Autograd Runtime

> Purpose: evaluates scalar math expression

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 9 | 9 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Math Blocks Autograd Runtime

Purpose: evaluates scalar math expression

## At a Glance

| Field | Value |
|-------|-------|
| Category | Runtime |
| Status | In Progress |
| Source | `test/03_system/feature/usage/math_autograd_runtime_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience
Purpose: evaluates scalar math expression
Audience: compiler and tooling engineers who maintain this spec

# Math Blocks Autograd Runtime

**Category:** Runtime
**Status:** In Progress

## Overview

Compiled-mode proof tests for m{}, loss{}, and nograd{} runtime semantics. Verifies
that MIR lowering emits real runtime calls and the torch autograd backend produces
correct gradient behavior, including scalar math evaluation, loss backward with
automatic gradient computation, nograd gradient suppression, and loss/nograd interaction.

## Scenarios

### Math Blocks Autograd Runtime

### m{} evaluation

#### evaluates scalar math expression

- Verify: evaluates scalar math expression
   - Expected: result equals `10.0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: evaluates scalar math expression")
# @req: REQ-FEATURE-MathAutoRunt-001
val x = 3.0
val result = m{ x^2 + 1 }
expect(result).to_equal(10.0)
```

<details>
<summary>Rendered scenario source</summary>

> step("Verify: evaluates scalar math expression")<br>
> # @req: REQ-FEATURE-MathAutoRunt-001<br>
> val x = 3.0<br>
> val result = $x^{2} + 1$<br>
> expect(result).to_equal(10.0)

</details>

</details>

#### evaluates with implicit multiplication

- Verify: evaluates with implicit multiplication
   - Expected: result equals `13.0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: evaluates with implicit multiplication")
# @req: REQ-FEATURE-MathAutoRunt-001
val x = 2.0
val y = 3.0
val result = m{ 2x + 3y }
expect(result).to_equal(13.0)
```

<details>
<summary>Rendered scenario source</summary>

> step("Verify: evaluates with implicit multiplication")<br>
> # @req: REQ-FEATURE-MathAutoRunt-001<br>
> val x = 2.0<br>
> val y = 3.0<br>
> val result = $2 x + 3 y$<br>
> expect(result).to_equal(13.0)

</details>

</details>

### loss{} backward semantics

#### simple quadratic loss

#### computes gradients on requires_grad parameter

- Verify: computes gradients on requires_grad parameter
   - Expected: grad.item() equals `-6.0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: computes gradients on requires_grad parameter")
# @req: REQ-FEATURE-MathAutoRunt-001
val w = Tensor.from_data([2.0], requires_grad: true)
val target = Tensor.from_data([5.0])

val loss_val = loss{ (w - target)^2 }

# After loss{} exits, backward() has been called automatically.
# The gradient of (w - target)^2 w.r.t. w is 2*(w - target) = 2*(2-5) = -6
val grad = w.grad()
assert_true(grad != nil)
expect(grad.item()).to_equal(-6.0)
```

</details>

#### repeated loss evaluation

#### accumulates gradients across loss blocks

- Verify: accumulates gradients across loss blocks
   - Expected: grad1 equals `2.0`
   - Expected: grad2 equals `4.0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accumulates gradients across loss blocks")
# @req: REQ-FEATURE-MathAutoRunt-001
val w = Tensor.from_data([1.0], requires_grad: true)
val target = Tensor.from_data([0.0])

# First loss: grad = 2*(1-0) = 2.0
loss{ (w - target)^2 }
val grad1 = w.grad().item()
expect(grad1).to_equal(2.0)

# Second loss without zero_grad: grad accumulates
loss{ (w - target)^2 }
val grad2 = w.grad().item()
expect(grad2).to_equal(4.0)
```

</details>

#### detached input and escape-path restore

#### keeps detached values out of autograd and restores gradient tracking after escaped nograd

- Verify: keeps detached values out of autograd and restores gradient tracking after escaped nograd
   - Expected: detached.requires_grad() is false
   - Expected: msg equals `nograd escape`
   - Expected: restored.requires_grad() is true


<details>
<summary>Executable SSpec</summary>

Runnable source: 17 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: keeps detached values out of autograd and restores gradient tracking after escaped nograd")
# @req: REQ-FEATURE-MathAutoRunt-001
val source = Tensor.from_data([2.0], requires_grad: true)
val detached = source.detach()
expect(detached.requires_grad()).to_equal(false)

val restore_w = Tensor.from_data([1.0], requires_grad: true)
val restore_x = Tensor.from_data([3.0], requires_grad: true)
val outcome = escaped_nograd(restore_w, restore_x)
match outcome:
    case Err(msg):
        expect(msg).to_equal("nograd escape")
    case Ok(_):
        assert_true(false)

val restored = restore_w * restore_x
expect(restored.requires_grad()).to_equal(true)
```

</details>

### nograd{} suppression semantics

#### basic suppression

#### prevents gradient recording

- Verify: prevents gradient recording
   - Expected: result.item() equals `6.0`
   - Expected: result.requires_grad() is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: prevents gradient recording")
# @req: REQ-FEATURE-MathAutoRunt-001
val w = Tensor.from_data([3.0], requires_grad: true)
val x = Tensor.from_data([2.0])

val result = nograd{ w * x }

# Result should be computed (3*2 = 6)
expect(result.item()).to_equal(6.0)
# But no gradient graph was built
expect(result.requires_grad()).to_equal(false)
```

</details>

#### nesting

#### nested nograd does not leak state

- Verify: nested nograd does not leak state
   - Expected: outer.requires_grad() is false
   - Expected: tracked.requires_grad() is true


<details>
<summary>Executable SSpec</summary>

Runnable source: 17 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: nested nograd does not leak state")
# @req: REQ-FEATURE-MathAutoRunt-001
val w = Tensor.from_data([1.0], requires_grad: true)
val x = Tensor.from_data([2.0])

# Outer nograd
val outer = nograd{
    val inner = nograd{ w * x }
    inner + w
}
# After both nograd blocks exit, grad tracking is restored
# The result inside nograd should not require grad
expect(outer.requires_grad()).to_equal(false)

# But operations after nograd{} resume tracking
val tracked = w * x
expect(tracked.requires_grad()).to_equal(true)
```

</details>

### loss{} and nograd{} interaction

#### nograd inside loss

#### only tracks gradient-enabled parts

- Verify: only tracks gradient-enabled parts
   - Expected: w.grad().item() equals `-2.0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 18 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: only tracks gradient-enabled parts")
# @req: REQ-FEATURE-MathAutoRunt-001
val w = Tensor.from_data([2.0], requires_grad: true)
val bias = Tensor.from_data([1.0], requires_grad: true)
val target = Tensor.from_data([4.0])

# The nograd part does not contribute to gradient computation
# loss = (nograd{bias} + w - target)^2
# = (1 + 2 - 4)^2 = 1.0
# d(loss)/dw = 2*(1+2-4) = -2
# d(loss)/dbias = 0 (bias inside nograd, detached)
val loss_val = loss{
    val const_part = nograd{ bias }
    (const_part + w - target)^2
}

expect(w.grad().item()).to_equal(-2.0)
# bias gradient should be nil or zero (not tracked through nograd)
```

</details>

#### loss after nograd

#### resumes normal gradient behavior

- Verify: resumes normal gradient behavior
   - Expected: grad.item() equals `4.0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: resumes normal gradient behavior")
# @req: REQ-FEATURE-MathAutoRunt-001
val w = Tensor.from_data([3.0], requires_grad: true)
val target = Tensor.from_data([1.0])

# nograd block — no gradient ops
val _ = nograd{ w * w }

# loss block after nograd — should work normally
loss{ (w - target)^2 }

# grad = 2*(3-1) = 4.0
val grad = w.grad()
assert_true(grad != nil)
expect(grad.item()).to_equal(4.0)
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 9 |
| Active scenarios | 9 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `1c9e23962d395967987e16351cce2013246fc72a3f576d734b38b17bf2a64f4f`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `1c9e23962d395967987e16351cce2013246fc72a3f576d734b38b17bf2a64f4f`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `1c9e23962d395967987e16351cce2013246fc72a3f576d734b38b17bf2a64f4f`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **81/100**; effective score: **81/100**; blockers: **0**.

SSpec documentization score: 81/100
source: test/03_system/feature/usage/math_autograd_runtime_spec.spl
mirror: doc/06_spec/03_system/feature/usage/math_autograd_runtime_spec.md (current)
findings: 9 blockers: 0
  narrative=100 structure=100 oracle=70
  traceability=100 evidence=55 coverage=100 maintainability=45
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/usage/math_autograd_runtime_spec.md:1:1: warning SSDOC-EVD-002 [evidence] (-15): source steps are not visible in the generated manual
  why: Source tokens alone do not prove reader-visible workflow structure.
  improve: Use supported literal step calls and regenerate the manual.
doc/06_spec/03_system/feature/usage/math_autograd_runtime_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/usage/math_autograd_runtime_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: scope, assumptions/preconditions, primary workflow, traceability, evidence, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/usage/math_autograd_runtime_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/03_system/feature/usage/math_autograd_runtime_spec.spl:1:1: advice SSDOC-MNT-007 [maintainability] (-10): research, plan, architecture, or design metadata links are incomplete
  why: Reviewers need selected lifecycle evidence, not inferred project state.
  improve: Link the selected lifecycle artifacts or configure a reasoned scope suppression.
test/03_system/feature/usage/math_autograd_runtime_spec.spl:1:1: advice SSDOC-ORA-003 [oracle] (-30): 8 unexplained numeric expected value(s)
  why: Reviewers need to know why a magic expected value is authoritative.
  improve: Name the authoritative expected value or add a '# oracle:' explanation.
test/03_system/feature/usage/math_autograd_runtime_spec.spl:46:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'evaluates scalar math expression' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/math_autograd_runtime_spec.spl:53:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'evaluates with implicit multiplication' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/usage/math_autograd_runtime_spec.spl:63:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'computes gradients on requires_grad parameter' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
