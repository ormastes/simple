# SFM Codec & Manifest

> Purpose: should preserve the manifest name and version

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 8 | 8 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# SFM Codec & Manifest

Purpose: should preserve the manifest name and version

## At a Glance

| Field | Value |
|-------|-------|
| Feature IDs | #SFM |
| Category | Infrastructure |
| Status | Draft |
| Requirements | doc/04_architecture/language/simple_feature_module.md |
| Design | doc/05_design/simple_feature_module.md |
| Source | `test/03_system/feature/sfm/sfm_codec_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience
Purpose: should preserve the manifest name and version
Audience: compiler and tooling engineers who maintain this spec

# SFM Codec & Manifest

**Feature IDs:** #SFM
**Category:** Infrastructure
**Status:** Draft
**Requirements:** doc/04_architecture/language/simple_feature_module.md
**Design:** doc/05_design/simple_feature_module.md

## Overview

`.sfm` is the primary Simple Feature Module container. It is a pure-Simple outer
container that embeds an opaque SMF code payload plus a feature manifest declaring
the exposed front-end / back-end layers, their security level, and the build version.
This spec covers AC-1 (codec round-trip preserves manifest fields and the embedded
SMF bytes) and AC-2 (the manifest declares exposed layers with name/kind/entry).

The byte-exact round-trip is *also* asserted by a standalone runnable probe
(`sfm_roundtrip_probe.spl`), because the test runner does not execute `it` blocks —
see `## Related Specifications`.

## Key Concepts

| Concept | Description |
|---------|-------------|
| FeatureManifest | Name, version, security level, and the list of exposed layers |
| LayerDescriptor | One exposed layer: role, kind, entry symbol, privileged flag |
| LayerKind | FrontGui / FrontTui / FrontArgParser / BackDb / BackHw |
| encode_sfm | Pure byte encoder: `(manifest, smf_bytes) -> [u8]` |
| decode_sfm | Pure byte decoder: `[u8] -> Result<(manifest, smf_bytes), text>` |

## Related Specifications

- [sfm_roundtrip_probe.spl](sfm_roundtrip_probe.spl) — ground-truth AC-1 byte gate (run via `bin/simple run`)
- [sfm_di_authz_spec.spl](sfm_di_authz_spec.spl) — DI + AOP authorization (AC-3/4/5)
- [sfm_loader_profiles_spec.spl](sfm_loader_profiles_spec.spl) — profile selection (AC-6)

## Scenarios

### SFM codec

### AC-1: write -> read round-trip

<details>
<summary>Advanced: should preserve the manifest name and version</summary>

#### should preserve the manifest name and version

- Verify: should preserve the manifest name and version
   - Binary capture: after_step
   - Evidence: binary artifact verified by 3 expected checks
   - Expected: decoded.name equals `sample-sfm`
   - Expected: decoded.version equals `1.4.2`
   - Expected: "decode-failed: " + e equals `ok`


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should preserve the manifest name and version")
# @req: REQ-FEATURE-SfmCode-001
val m = build_manifest()
val smf = build_fake_smf_bytes()
match round_trip(m, smf):
    Ok((decoded, _)):
        expect(decoded.name).to_equal("sample-sfm")
        expect(decoded.version).to_equal("1.4.2")
    Err(e):
        expect("decode-failed: " + e).to_equal("ok")
```

</details>


</details>

<details>
<summary>Advanced: should preserve the embedded SMF bytes exactly</summary>

#### should preserve the embedded SMF bytes exactly

- Verify: should preserve the embedded SMF bytes exactly
   - Binary capture: after_step
   - Evidence: binary artifact verified by 2 expected checks
   - Expected: out.len() equals `smf.len()`
   - Expected: "decode-failed: " + e equals `ok`


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should preserve the embedded SMF bytes exactly")
# @req: REQ-FEATURE-SfmCode-001
val m = build_manifest()
val smf = build_fake_smf_bytes()
match round_trip(m, smf):
    Ok((_, out)):
        expect(out.len()).to_equal(smf.len())
        assert_true(bytes_equal(out, smf))
    Err(e):
        expect("decode-failed: " + e).to_equal("ok")
```

</details>


</details>

<details>
<summary>Advanced: should be re-encode idempotent (catches symmetric codec bugs)</summary>

#### should be re-encode idempotent (catches symmetric codec bugs)

- Verify: should be re-encode idempotent (catches symmetric codec bugs)
   - Binary capture: after_step
   - Evidence: binary artifact verified by 1 expected check
   - Expected: "decode-failed: " + e equals `ok`


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should be re-encode idempotent (catches symmetric codec bugs)")
# @req: REQ-FEATURE-SfmCode-001
val m = build_manifest()
val smf = build_fake_smf_bytes()
val first: [u8] = encode_sfm(m, smf)
match decode_sfm(first):
    Ok((m2, smf2)):
        assert_true(reencode_matches(m2, smf2, first))
    Err(e):
        expect("decode-failed: " + e).to_equal("ok")
```

</details>


</details>

<details>
<summary>Advanced: should preserve the security level marker</summary>

#### should preserve the security level marker

- Verify: should preserve the security level marker
   - Binary capture: after_step
   - Evidence: binary artifact verified by 1 expected check
   - Expected: "decode-failed: " + e equals `ok`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should preserve the security level marker")
# @req: REQ-FEATURE-SfmCode-001
val m = build_manifest()
val smf = build_fake_smf_bytes()
match round_trip(m, smf):
    Ok((decoded, _)):
        match decoded.security_level:
            Ordinary: assert_true(true)
            Trusted:  expect("expected Ordinary").to_equal("ok")
    Err(e):
        expect("decode-failed: " + e).to_equal("ok")
```

</details>


</details>

### AC-2: exposed layer manifest fields

<details>
<summary>Advanced: should declare each layer with a role, kind and entry symbol</summary>

#### should declare each layer with a role, kind and entry symbol

- Verify: should declare each layer with a role, kind and entry symbol
   - Expected: m.layers.len() equals `2)  # oracle: value fixed by the spec contract`
   - Expected: front.role equals `cli`
   - Expected: front.entry_symbol equals `arg_parse_main`


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should declare each layer with a role, kind and entry symbol")
# @req: REQ-FEATURE-SfmCode-001
val m = build_manifest()
expect(m.layers.len()).to_equal(2)  # oracle: value fixed by the spec contract
val front = m.layers[0]
expect(front.role).to_equal("cli")
expect(front.entry_symbol).to_equal("arg_parse_main")
match front.kind:
    FrontArgParser: assert_true(true)
    _:              expect("expected FrontArgParser").to_equal("ok")
```

</details>


</details>

<details>
<summary>Advanced: should carry the layer kind through a codec round-trip</summary>

#### should carry the layer kind through a codec round-trip

- Verify: should carry the layer kind through a codec round-trip
   - Expected: decoded.layers.len() equals `2)  # oracle: value fixed by the spec contract`
   - Expected: back.role equals `device`
   - Expected: back.entry_symbol equals `hw_open`
   - Expected: "decode-failed: " + e equals `ok`


<details>
<summary>Executable SSpec</summary>

Runnable source: 16 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should carry the layer kind through a codec round-trip")
# @req: REQ-FEATURE-SfmCode-001
val m = build_manifest()
val smf = build_fake_smf_bytes()
match round_trip(m, smf):
    Ok((decoded, _)):
        expect(decoded.layers.len()).to_equal(2)  # oracle: value fixed by the spec contract
        val back = decoded.layers[1]
        expect(back.role).to_equal("device")
        expect(back.entry_symbol).to_equal("hw_open")
        assert_true(back.privileged)
        match back.kind:
            BackHw: assert_true(true)
            _:      expect("expected BackHw").to_equal("ok")
    Err(e):
        expect("decode-failed: " + e).to_equal("ok")
```

</details>


</details>

### SFM codec edge cases

<details>
<summary>Advanced: should reject bytes too short to hold a header</summary>

#### should reject bytes too short to hold a header

- Verify: should reject bytes too short to hold a header


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should reject bytes too short to hold a header")
# @req: REQ-FEATURE-SfmCode-001
var tiny: [u8] = []
tiny.append(83 as u8)
match decode_sfm(tiny):
    Ok(_):  expect("should have failed").to_equal("ok")
    Err(_): assert_true(true)
```

</details>


</details>

<details>
<summary>Advanced: should preserve an empty embedded SMF payload</summary>

#### should preserve an empty embedded SMF payload

- Verify: should preserve an empty embedded SMF payload
   - Expected: out.len() equals `0)  # oracle: value fixed by the spec contract`
   - Expected: "decode-failed: " + e equals `ok`


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: should preserve an empty embedded SMF payload")
# @req: REQ-FEATURE-SfmCode-001
val m = build_manifest()
var empty: [u8] = []
match round_trip(m, empty):
    Ok((_, out)):
        expect(out.len()).to_equal(0)  # oracle: value fixed by the spec contract
    Err(e):
        expect("decode-failed: " + e).to_equal("ok")
```

</details>


</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 8 |
| Active scenarios | 8 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


## Related Documentation

- **Requirements:** `doc/04_architecture/language/simple_feature_module.md`
- **Design:** `doc/05_design/simple_feature_module.md`


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `c72cbf397502ee3e5a92d4100657654116f17e5ad289b54b449cc500da205a61`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `c72cbf397502ee3e5a92d4100657654116f17e5ad289b54b449cc500da205a61`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `c72cbf397502ee3e5a92d4100657654116f17e5ad289b54b449cc500da205a61`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **84/100**; effective score: **84/100**; blockers: **0**.

SSpec documentization score: 84/100
source: test/03_system/feature/sfm/sfm_codec_spec.spl
mirror: doc/06_spec/03_system/feature/sfm/sfm_codec_spec.md (current)
findings: 13 blockers: 0
  narrative=100 structure=70 oracle=100
  traceability=100 evidence=55 coverage=100 maintainability=60
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/sfm/sfm_codec_spec.md:1:1: warning SSDOC-EVD-002 [evidence] (-15): source steps are not visible in the generated manual
  why: Source tokens alone do not prove reader-visible workflow structure.
  improve: Use supported literal step calls and regenerate the manual.
doc/06_spec/03_system/feature/sfm/sfm_codec_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/sfm/sfm_codec_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: scope, assumptions/preconditions, primary workflow, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/sfm/sfm_codec_spec.spl:1:1: advice SSDOC-MNT-007 [maintainability] (-10): research, plan, architecture, or design metadata links are incomplete
  why: Reviewers need selected lifecycle evidence, not inferred project state.
  improve: Link the selected lifecycle artifacts or configure a reasoned scope suppression.
test/03_system/feature/sfm/sfm_codec_spec.spl:137:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should preserve the manifest name and version' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/sfm/sfm_codec_spec.spl:137:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'should preserve the manifest name and version' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/sfm/sfm_codec_spec.spl:150:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should preserve the embedded SMF bytes exactly' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/sfm/sfm_codec_spec.spl:150:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'should preserve the embedded SMF bytes exactly' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/sfm/sfm_codec_spec.spl:163:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should be re-encode idempotent (catches symmetric codec bugs)' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/sfm/sfm_codec_spec.spl:163:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'should be re-encode idempotent (catches symmetric codec bugs)' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/sfm/sfm_codec_spec.spl:176:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should preserve the security level marker' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/sfm/sfm_codec_spec.spl:193:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should declare each layer with a role, kind and entry symbol' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/sfm/sfm_codec_spec.spl:205:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should carry the layer kind through a codec round-trip' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
<!-- sspec-maintain:scorecard:end -->
