# Code Quality Tools

> Purpose: prints lint help and exits 0 when invoked with no files

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 8 | 8 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Code Quality Tools

Purpose: prints lint help and exits 0 when invoked with no files

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | In Progress |
| Source | `test/03_system/feature/app/code_quality_tools_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience
Purpose: prints lint help and exits 0 when invoked with no files
Audience: compiler and tooling engineers who maintain this spec

# Code Quality Tools

**Category:** Application
**Status:** In Progress

## Overview

Tests the code quality tooling pipeline including linting, formatting, and static
analysis checks. Verifies that quality tools correctly detect issues, report
diagnostics, and apply automated fixes to Simple source files.

## Scenarios

### Code Quality Tools

#### prints lint help and exits 0 when invoked with no files

- Verify: prints lint help and exits 0 when invoked with no files
   - Expected: code equals `0)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: prints lint help and exits 0 when invoked with no files")
# @req: REQ-FEATURE-CodeQualTool-001
val (out, err, code) = run_quality_tool(["lint"])
expect(code).to_equal(0)  # oracle: value fixed by the spec contract
expect(out).to_contain("Usage: simple lint")
expect(out).to_contain("Runs lint passes")
```

</details>

#### rejects lint help combined with an invalid option

- Verify: rejects lint help combined with an invalid option
   - Expected: code equals `2)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects lint help combined with an invalid option")
# @req: REQ-FEATURE-CodeQualTool-001
val (out, err, code) = run_quality_tool(["lint", "--help", "--bogus"])
expect(code).to_equal(2)  # oracle: value fixed by the spec contract
expect(err + out).to_contain("unknown option: --bogus")
```

</details>

#### rejects lint help combined with a file

- Verify: rejects lint help combined with a file
   - Expected: code equals `2)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects lint help combined with a file")
# @req: REQ-FEATURE-CodeQualTool-001
val (out, err, code) = run_quality_tool(["lint", "--help", CLEAN_FIXTURE])
expect(code).to_equal(2)  # oracle: value fixed by the spec contract
expect(err + out).to_contain("help cannot be combined")
```

</details>

#### surfaces the interpreter-mode limitation when a file lint is requested

- Verify: surfaces the interpreter-mode limitation when a file lint is requested
   - Expected: code equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: surfaces the interpreter-mode limitation when a file lint is requested")
# @req: REQ-FEATURE-CodeQualTool-001
# Real file linting delegates to the native rt_cli_run_lint, which is not
# available under `bin/simple run` (interpreter). Driving lint through this
# entrypoint therefore fails closed (exit 1) with an explicit message rather
# than silently. Real linting runs through the compiled `bin/simple lint`
# subcommand. See bug lint_cli_single_file_gated_by_workspace_root_guard_2026-07-08.
val target = QUALITY_FIXTURE_DIR + "/does_not_exist.spl"
val (out, err, code) = run_quality_tool(["lint", target])
expect(code).to_equal(1)  # oracle: value fixed by the spec contract
expect(err + out).to_contain("not supported in interpreter mode")
```

</details>

#### prints formatter help and exits 0 when invoked with no files

- Verify: prints formatter help and exits 0 when invoked with no files
   - Expected: code equals `0)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: prints formatter help and exits 0 when invoked with no files")
# @req: REQ-FEATURE-CodeQualTool-001
val (out, err, code) = run_quality_tool(["fmt"])
expect(code).to_equal(0)  # oracle: value fixed by the spec contract
expect(out).to_contain("Usage: simple fmt")
expect(out).to_contain("--check")
```

</details>

#### rejects formatter help combined with another argument

- Verify: rejects formatter help combined with another argument
   - Expected: code equals `2)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects formatter help combined with another argument")
# @req: REQ-FEATURE-CodeQualTool-001
val (out, err, code) = run_quality_tool(["fmt", "--help", "--bogus"])
expect(code).to_equal(2)  # oracle: value fixed by the spec contract
expect(err + out).to_contain("unknown option: --bogus")
```

</details>

#### rejects fix help combined with an invalid option

- Verify: rejects fix help combined with an invalid option
   - Expected: code equals `2)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects fix help combined with an invalid option")
# @req: REQ-FEATURE-CodeQualTool-001
val (out, err, code) = run_quality_tool(["fix", "--help", "--bogus"])
expect(code).to_equal(2)  # oracle: value fixed by the spec contract
expect(err + out).to_contain("unknown option: --bogus")
```

</details>

#### rejects an unknown quality command before help handling

- Verify: rejects an unknown quality command before help handling
   - Expected: bare_code equals `1)  # oracle: value fixed by the spec contract`
   - Expected: help_code equals `1)  # oracle: value fixed by the spec contract`
   - Expected: code equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects an unknown quality command before help handling")
# @req: REQ-FEATURE-CodeQualTool-001
val dummy = QUALITY_FIXTURE_DIR + "/x.spl"
val (bare_out, _, bare_code) = run_quality_tool(["unknown"])
val (help_out, _, help_code) = run_quality_tool(["unknown", "--help"])
val (out, err, code) = run_quality_tool(["unknown", dummy])
expect(bare_code).to_equal(1)  # oracle: value fixed by the spec contract
expect(bare_out).to_contain("Unknown command: unknown")
expect(help_code).to_equal(1)  # oracle: value fixed by the spec contract
expect(help_out).to_contain("Unknown command: unknown")
expect(code).to_equal(1)  # oracle: value fixed by the spec contract
expect(out).to_contain("Unknown command: unknown")
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 8 |
| Active scenarios | 8 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `a5e0f414697fcb808f6fa7467f37b600066fc14449f7a78fbeb638342c4815a4`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `a5e0f414697fcb808f6fa7467f37b600066fc14449f7a78fbeb638342c4815a4`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `a5e0f414697fcb808f6fa7467f37b600066fc14449f7a78fbeb638342c4815a4`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **87/100**; effective score: **87/100**; blockers: **0**.

SSpec documentization score: 87/100
source: test/03_system/feature/app/code_quality_tools_spec.spl
mirror: doc/06_spec/03_system/feature/app/code_quality_tools_spec.md (current)
findings: 8 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=55 coverage=100 maintainability=45
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/app/code_quality_tools_spec.md:1:1: warning SSDOC-EVD-002 [evidence] (-15): source steps are not visible in the generated manual
  why: Source tokens alone do not prove reader-visible workflow structure.
  improve: Use supported literal step calls and regenerate the manual.
doc/06_spec/03_system/feature/app/code_quality_tools_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/app/code_quality_tools_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: scope, assumptions/preconditions, primary workflow, traceability, evidence, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/app/code_quality_tools_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/03_system/feature/app/code_quality_tools_spec.spl:1:1: advice SSDOC-MNT-007 [maintainability] (-10): research, plan, architecture, or design metadata links are incomplete
  why: Reviewers need selected lifecycle evidence, not inferred project state.
  improve: Link the selected lifecycle artifacts or configure a reasoned scope suppression.
test/03_system/feature/app/code_quality_tools_spec.spl:57:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'prints lint help and exits 0 when invoked with no files' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/app/code_quality_tools_spec.spl:65:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'rejects lint help combined with an invalid option' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/app/code_quality_tools_spec.spl:72:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'rejects lint help combined with a file' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
