# EasyFix Rules

> Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 100 | 100 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# EasyFix Rules

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | In Progress |
| Source | `test/03_system/feature/app/easy_fix_rules_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## Operator workflow

1. Run `bin/simple test test/03_system/feature/app/easy_fix_rules_spec.spl`.
2. Every scenario must pass; a failure is a regression in the behavior under test.

## Compatibility and limitations

Covers the behavior asserted here; platform-specific behavior is out of scope.

## Scenarios

### Rule: print_in_test_spec

#### basic detection

#### detects print() in spec file

- Verify: detects print() in spec file


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects print() in spec file")
# @req: REQ-SSPEC-LOCAL-001
val source = "it \"test\":\n    print(42)\n"
val fixes = check_print_in_test_spec(source, "my_spec.spl")
expect fixes[0].id.contains("print_in_test_spec")
expect fixes.len() == 1
```

</details>

#### detects print with string arg

- Verify: detects print with string arg


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects print with string arg")
val source = "it \"test\":\n    print(\"hello\")\n"
val fixes = check_print_in_test_spec(source, "my_spec.spl")
expect fixes.len() == 1
```

</details>

#### detects print with expression

- Verify: detects print with expression


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects print with expression")
val source = "it \"test\":\n    print(x + y)\n"
val fixes = check_print_in_test_spec(source, "test_spec.spl")
expect fixes.len() == 1
```

</details>

#### detects multiple prints

- Verify: detects multiple prints


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects multiple prints")
# @req: REQ-SSPEC-LOCAL-001
val source = "it \"a\":\n    print(1)\n    print(2)\n    print(3)\n"
val fixes = check_print_in_test_spec(source, "test_spec.spl")
expect fixes.len() == 3
```

</details>

#### generates correct replacement

- Verify: generates correct replacement


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: generates correct replacement")
val source = "    print(42)"
val fixes = check_print_in_test_spec(source, "test_spec.spl")
expect fixes[0].replacements[0].new_text.contains("expect")
expect fixes.len() == 1
```

</details>

#### preserves indentation

- Verify: preserves indentation


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: preserves indentation")
# @req: REQ-SSPEC-LOCAL-001
val source = "        print(x)"
val fixes = check_print_in_test_spec(source, "test_spec.spl")
val rep = fixes[0].replacements[0]
expect fixes.len() == 1
expect rep.new_text.starts_with("        ")
```

</details>

#### non-spec files

#### ignores print in regular .spl files

- Verify: ignores print in regular .spl files


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores print in regular .spl files")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn main():\n    print(42)\n"
val fixes = check_print_in_test_spec(source, "main.spl")
expect fixes.len() == 0
```

</details>

#### ignores print in non-spl files

- Verify: ignores print in non-spl files


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores print in non-spl files")
val source = "print(42)"
val fixes = check_print_in_test_spec(source, "test.txt")
expect fixes.len() == 0
```

</details>

#### edge cases

#### handles empty file

- Verify: handles empty file


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles empty file")
# @req: REQ-SSPEC-LOCAL-001
val fixes = check_print_in_test_spec("", "test_spec.spl")
expect fixes.len() == 0
```

</details>

#### handles file with only comments

- Verify: handles file with only comments


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles file with only comments")
# @req: REQ-SSPEC-LOCAL-001
val source = "# comment\n# another\n"
val fixes = check_print_in_test_spec(source, "test_spec.spl")
expect fixes.len() == 0
```

</details>

#### has Likely confidence

- Verify: has Likely confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: has Likely confidence")
val source = "    print(42)"
val fixes = check_print_in_test_spec(source, "test_spec.spl")
expect fixes.len() > 0
match fixes[0].confidence:
    FixConfidence.Likely: expect true
    _: fail("Expected Likely confidence")
```

</details>

### Rule: unnamed_duplicate_typed_args

#### basic detection

#### detects fn with two same types

- Verify: detects fn with two same types


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects fn with two same types")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn add(Int, Int) -> Int:\n    0\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### generates named params

- Verify: generates named params


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates named params")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn add(Int, Int) -> Int:\n    0\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text.contains("int1: Int")
expect rep.new_text.contains("int2: Int")
```

</details>

#### detects three same types

- Verify: detects three same types


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects three same types")
val source = "fn triple(String, String, String):\n    pass\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### rewrites declaration and same-file positional call

- Verify: rewrites declaration and same-file positional call


<details>
<summary>Executable SSpec</summary>

Runnable source: 18 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rewrites declaration and same-file positional call")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn add(Int, Int) -> Int:\n    0\nval total = add(1, 2)\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 1
expect fixes[0].replacements.len() == 2

var sources: Dict<String, String> = {}
sources["test.spl"] = source

val result = FixApplicator.apply(fixes, sources)
match result:
    case Ok(new_sources):
        val fixed = new_sources["test.spl"]
        expect fixed.contains("fn add(int1: Int, int2: Int)")
        expect fixed.contains("add(int1: 1, int2: 2)")
    case Err(_):
        expect false
```

</details>

#### rewrites same-file positional call when duplicate typed params already have names

- Verify: rewrites same-file positional call when duplicate typed params already have names


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rewrites same-file positional call when duplicate typed params already have names")
val source = "fn clamp(min: Int, max: Int, value: Int) -> Int:\n    value\nval result = clamp(1, 5, current)\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 1
expect fixes[0].replacements.len() == 1
expect fixes[0].replacements[0].new_text == "min: 1, max: 5, value: current"
```

</details>

#### rewrites same-file method calls

- Verify: rewrites same-file method calls


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rewrites same-file method calls")
val source = "me move(dx: Int, dy: Int):\n    pass\nval _ = point.move(3, 4)\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 1
expect fixes[0].replacements[0].new_text == "dx: 3, dy: 4"
```

</details>

#### no false positives

#### ignores named params

- Verify: ignores named params


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores named params")
val source = "fn add(a: Int, b: Int) -> Int:\n    a + b\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### ignores single type-only param

- Verify: ignores single type-only param


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores single type-only param")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn identity(Int) -> Int:\n    0\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### ignores different types

- Verify: ignores different types


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores different types")
val source = "fn convert(Int, String) -> Bool:\n    true\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### does not rewrite already named calls

- Verify: does not rewrite already named calls


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: does not rewrite already named calls")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn clamp(min: Int, max: Int, value: Int) -> Int:\n    value\nval result = clamp(min: 1, max: 5, value: current)\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### does not rewrite ambiguous same-name call targets

- Verify: does not rewrite ambiguous same-name call targets


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: does not rewrite ambiguous same-name call targets")
val source = "fn move(dx: Int, dy: Int):\n    pass\nfn move(x: Int, y: Int):\n    pass\nval _ = move(1, 2)\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### does not rewrite mismatched arity calls

- Verify: does not rewrite mismatched arity calls


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: does not rewrite mismatched arity calls")
val source = "fn clamp(min: Int, max: Int, value: Int) -> Int:\n    value\nval result = clamp(1, 5)\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### edge cases

#### handles empty file

- Verify: handles empty file


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles empty file")
val fixes = check_unnamed_duplicate_typed_args("", "test.spl")
expect fixes.len() == 0
```

</details>

#### handles me methods

- Verify: handles me methods


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles me methods")
val source = "me move(Int, Int):\n    pass\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### handles static fn

- Verify: handles static fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles static fn")
val source = "static fn create(String, String) -> Self:\n    pass\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### has Uncertain confidence

- Verify: has Uncertain confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: has Uncertain confidence")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn f(Int, Int):\n    pass\n"
val fixes = check_unnamed_duplicate_typed_args(source, "test.spl")
expect fixes.len() > 0
match fixes[0].confidence:
    FixConfidence.Uncertain: expect true
    _: fail("Expected Uncertain confidence")
```

</details>

### Rule: resource_leak

#### basic detection

#### detects val x = open(...)

- Verify: detects val x = open(...)


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects val x = open(...)")
# @req: REQ-SSPEC-LOCAL-001
val source = "val f = open(\"file.txt\")\n"
val fixes = check_resource_leak(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### detects File.open

- Verify: detects File.open


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects File.open")
val source = "val f = File.open(\"path\")\n"
val fixes = check_resource_leak(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### detects connect

- Verify: detects connect


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects connect")
# @req: REQ-SSPEC-LOCAL-001
val source = "val conn = connect(\"localhost:8080\")\n"
val fixes = check_resource_leak(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### generates with block

- Verify: generates with block


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates with block")
# @req: REQ-SSPEC-LOCAL-001
val source = "val f = open(\"file.txt\")\n"
val fixes = check_resource_leak(source, "test.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text.contains("with f = open")
```

</details>

#### no false positives

#### ignores non-resource assignments

- Verify: ignores non-resource assignments


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores non-resource assignments")
val source = "val x = compute(42)\n"
val fixes = check_resource_leak(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### ignores var assignments

- Verify: ignores var assignments


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores var assignments")
# @req: REQ-SSPEC-LOCAL-001
val source = "var f = open(\"file\")\n"
val fixes = check_resource_leak(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### edge cases

#### handles empty file

- Verify: handles empty file


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles empty file")
# @req: REQ-SSPEC-LOCAL-001
val fixes = check_resource_leak("", "test.spl")
expect fixes.len() == 0
```

</details>

#### has Uncertain confidence

- Verify: has Uncertain confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: has Uncertain confidence")
val source = "val f = open(\"x\")\n"
val fixes = check_resource_leak(source, "test.spl")
expect fixes.len() > 0
match fixes[0].confidence:
    FixConfidence.Uncertain: expect true
    _: fail("Expected Uncertain confidence")
```

</details>

### Rule: spipe_missing_docstrings

#### basic detection

#### detects describe without docstring

- Verify: detects describe without docstring


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects describe without docstring")
val source = "describe \"My Feature\":\n    it \"works\":\n        expect true\n"
val fixes = check_spipe_missing_docstrings(source, "test_spec.spl")
expect fixes[0].id.contains("spipe_missing_docstrings")
expect fixes.len() >= 1
```

</details>

#### detects context without docstring

- Verify: detects context without docstring


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects context without docstring")
val source = "describe \"X\":\n    \"\"\"\n    ## X\n    \"\"\"\n    context \"when Y\":\n        it \"works\":\n            expect true\n"
val fixes = check_spipe_missing_docstrings(source, "test_spec.spl")
# context block should be detected (describe has docstring)
expect fixes.len() >= 1
```

</details>

#### generates docstring template

- Verify: generates docstring template


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates docstring template")
# @req: REQ-SSPEC-LOCAL-001
val source = "describe \"My Feature\":\n    it \"works\":\n        expect true\n"
val fixes = check_spipe_missing_docstrings(source, "test_spec.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text.contains("\"\"\"")
expect rep.new_text.contains("My Feature")
```

</details>

#### no false positives

#### ignores describe with docstring

- Verify: ignores describe with docstring


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores describe with docstring")
val source = "describe \"X\":\n    \"\"\"\n    ## X\n    \"\"\"\n    it \"works\":\n        expect true\n"
val fixes = check_spipe_missing_docstrings(source, "test_spec.spl")
expect fixes.len() == 0
```

</details>

#### ignores non-spec files

- Verify: ignores non-spec files


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores non-spec files")
val source = "describe \"X\":\n    pass\n"
val fixes = check_spipe_missing_docstrings(source, "main.spl")
expect fixes.len() == 0
```

</details>

#### edge cases

#### handles empty file

- Verify: handles empty file


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles empty file")
val fixes = check_spipe_missing_docstrings("", "test_spec.spl")
expect fixes.len() == 0
```

</details>

#### has Safe confidence

- Verify: has Safe confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: has Safe confidence")
val source = "describe \"X\":\n    it \"y\":\n        expect true\n"
val fixes = check_spipe_missing_docstrings(source, "test_spec.spl")
expect fixes.len() > 0
match fixes[0].confidence:
    FixConfidence.Safe: expect true
    _: fail("Expected Safe confidence")
```

</details>

### Rule: spipe_manual_assertions

#### basic detection

#### detects if x: fail()

- Verify: detects if x: fail()


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects if x: fail()")
# @req: REQ-SSPEC-LOCAL-001
val source = "    if x > 5: fail(\"too big\")\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
expect fixes.len() == 1
```

</details>

#### detects if not x: fail()

- Verify: detects if not x: fail()


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects if not x: fail()")
val source = "    if not valid: fail(\"invalid\")\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
expect fixes.len() == 1
```

</details>

#### generates expect for positive condition

- Verify: generates expect for positive condition


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates expect for positive condition")
# @req: REQ-SSPEC-LOCAL-001
val source = "    if x > 5: fail(\"too big\")\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text.contains("expect")
expect rep.new_text.contains("to_be_falsy")
```

</details>

#### generates expect for negated condition

- Verify: generates expect for negated condition


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates expect for negated condition")
# @req: REQ-SSPEC-LOCAL-001
val source = "    if not valid: fail(\"invalid\")\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text.contains("expect(valid)")
expect rep.new_text.contains("to_be_truthy")
```

</details>

#### detects multiple manual assertions

- Verify: detects multiple manual assertions


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects multiple manual assertions")
# @req: REQ-SSPEC-LOCAL-001
val source = "    if a: fail(\"a\")\n    if b: fail(\"b\")\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
expect fixes.len() == 2
```

</details>

#### no false positives

#### ignores if without fail

- Verify: ignores if without fail


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores if without fail")
# @req: REQ-SSPEC-LOCAL-001
val source = "    if x > 5: print(x)\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
expect fixes.len() == 0
```

</details>

#### ignores non-spec files

- Verify: ignores non-spec files


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores non-spec files")
val source = "    if x: fail(\"err\")\n"
val fixes = check_spipe_manual_assertions(source, "main.spl")
expect fixes.len() == 0
```

</details>

#### edge cases

#### handles empty file

- Verify: handles empty file


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles empty file")
# @req: REQ-SSPEC-LOCAL-001
val fixes = check_spipe_manual_assertions("", "test_spec.spl")
expect fixes.len() == 0
```

</details>

#### has Likely confidence

- Verify: has Likely confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: has Likely confidence")
# @req: REQ-SSPEC-LOCAL-001
val source = "    if x: fail(\"y\")\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
expect fixes.len() > 0
match fixes[0].confidence:
    FixConfidence.Likely: expect true
    _: fail("Expected Likely confidence")
```

</details>

#### preserves indentation

- Verify: preserves indentation


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: preserves indentation")
val source = "        if x: fail(\"y\")\n"
val fixes = check_spipe_manual_assertions(source, "test_spec.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text.starts_with("        ")
```

</details>

### Rule: non_exhaustive_match

#### basic detection

#### detects match without case _

- Verify: detects match without case _


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects match without case _")
# @req: REQ-SSPEC-LOCAL-001
val source = "match x:\n    case 1: \"one\"\n    case 2: \"two\"\nval y = 0\n"
val fixes = check_non_exhaustive_match(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### generates catch-all arm with todo

- Verify: generates catch-all arm with todo


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates catch-all arm with todo")
# @req: REQ-SSPEC-LOCAL-001
val source = "match x:\n    case 1: \"one\"\nval y = 0\n"
val fixes = check_non_exhaustive_match(source, "test.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text.contains("case _:")
expect rep.new_text.contains("todo(")
```

</details>

#### no false positives

#### ignores match with case _

- Verify: ignores match with case _


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores match with case _")
val source = "match x:\n    case 1: \"one\"\n    case _: \"other\"\nval y = 0\n"
val fixes = check_non_exhaustive_match(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### edge cases

#### handles empty file

- Verify: handles empty file


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles empty file")
# @req: REQ-SSPEC-LOCAL-001
val fixes = check_non_exhaustive_match("", "test.spl")
expect fixes.len() == 0
```

</details>

#### has Safe confidence

- Verify: has Safe confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: has Safe confidence")
val source = "match x:\n    case 1: \"one\"\nval y = 0\n"
val fixes = check_non_exhaustive_match(source, "test.spl")
expect fixes.len() > 0
match fixes[0].confidence:
    FixConfidence.Safe: expect true
    _: fail("Expected Safe confidence")
```

</details>

### Rule: typo_suggestion

#### Levenshtein distance

#### returns 0 for identical strings

- Verify: returns 0 for identical strings


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns 0 for identical strings")
# @req: REQ-SSPEC-LOCAL-001
expect levenshtein("hello", "hello") == 0
```

</details>

#### returns length for empty string

- Verify: returns length for empty string


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: returns length for empty string")
expect levenshtein("", "abc") == 3
expect levenshtein("abc", "") == 3
```

</details>

#### returns 1 for single insertion

- Verify: returns 1 for single insertion


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns 1 for single insertion")
# @req: REQ-SSPEC-LOCAL-001
expect levenshtein("helo", "hello") == 1
```

</details>

#### returns 1 for single deletion

- Verify: returns 1 for single deletion


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: returns 1 for single deletion")
expect levenshtein("hello", "helo") == 1
```

</details>

#### returns 1 for single substitution

- Verify: returns 1 for single substitution


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: returns 1 for single substitution")
expect levenshtein("hello", "hallo") == 1
```

</details>

#### returns 2 for two edits

- Verify: returns 2 for two edits


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns 2 for two edits")
# @req: REQ-SSPEC-LOCAL-001
expect levenshtein("kitten", "sitten") == 1
expect levenshtein("kitten", "sittin") == 2
```

</details>

#### handles completely different strings

- Verify: handles completely different strings


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles completely different strings")
expect levenshtein("abc", "xyz") == 3
```

</details>

#### handles single character strings

- Verify: handles single character strings


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles single character strings")
# @req: REQ-SSPEC-LOCAL-001
expect levenshtein("a", "b") == 1
expect levenshtein("a", "a") == 0
```

</details>

#### handles both empty

- Verify: handles both empty


<details>
<summary>Executable SSpec</summary>

Runnable source: 3 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles both empty")
# @req: REQ-SSPEC-LOCAL-001
expect levenshtein("", "") == 0
```

</details>

#### typo fix suggestion

#### suggests close match

- Verify: suggests close match


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: suggests close match")
val known = ["println", "print", "format"]
val result = suggest_typo_fix("test.spl", 1, 1, 0, 5, "prnt", known)
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == "print"
        expect fix.id.contains("typo_suggestion")
    case None:
        expect false
```

</details>

#### returns None for no close match

- Verify: returns None for no close match


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns None for no close match")
# @req: REQ-SSPEC-LOCAL-001
val known = ["println", "print", "format"]
val result = suggest_typo_fix("test.spl", 1, 1, 0, 10, "xyzxyzxyz", known)
match result:
    case Some(_):
        expect false
    case None:
        expect true
```

</details>

#### has Likely confidence

- Verify: has Likely confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: has Likely confidence")
val known = ["hello"]
val result = suggest_typo_fix("test.spl", 1, 1, 0, 4, "helo", known)
match result:
    case Some(fix):
        match fix.confidence:
            FixConfidence.Likely: expect true
            _: fail("Expected Likely confidence")
    case None:
        expect false
```

</details>

#### picks closest match

- Verify: picks closest match


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: picks closest match")
val known = ["print", "println", "printf"]
val result = suggest_typo_fix("test.spl", 1, 1, 0, 5, "prnt", known)
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == "print"
    case None:
        expect false
```

</details>

### Rule: parser_contextual_keyword

#### async static reorder

#### detects async static fn

- Verify: detects async static fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects async static fn")
val source = "async static fn serve():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### generates correct reorder

- Verify: generates correct reorder


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates correct reorder")
# @req: REQ-SSPEC-LOCAL-001
val source = "async static fn serve():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text == "static async fn "
```

</details>

#### has Safe confidence

- Verify: has Safe confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: has Safe confidence")
# @req: REQ-SSPEC-LOCAL-001
val source = "async static fn serve():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() > 0
match fixes[0].confidence:
    FixConfidence.Safe: expect true
    _: fail("Expected Safe confidence")
```

</details>

#### static pub reorder

#### detects static pub fn

- Verify: detects static pub fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects static pub fn")
val source = "static pub fn factory():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### generates pub static fn

- Verify: generates pub static fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: generates pub static fn")
# @req: REQ-SSPEC-LOCAL-001
val source = "static pub fn factory():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text == "pub static fn "
```

</details>

#### pub async static reorder

#### detects pub async static fn

- Verify: detects pub async static fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects pub async static fn")
# @req: REQ-SSPEC-LOCAL-001
val source = "pub async static fn handler():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 1
```

</details>

#### generates pub static async fn

- Verify: generates pub static async fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: generates pub static async fn")
val source = "pub async static fn handler():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
val rep = fixes[0].replacements[0]
expect rep.new_text == "pub static async fn "
```

</details>

#### no false positives

#### ignores correct static async fn

- Verify: ignores correct static async fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores correct static async fn")
val source = "static async fn serve():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### ignores plain fn

- Verify: ignores plain fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores plain fn")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn simple():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### ignores pub static fn

- Verify: ignores pub static fn


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: ignores pub static fn")
val source = "pub static fn factory():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 0
```

</details>

#### edge cases

#### handles empty file

- Verify: handles empty file


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles empty file")
val fixes = check_parser_contextual_keyword("", "test.spl")
expect fixes.len() == 0
```

</details>

#### handles indented keywords

- Verify: handles indented keywords


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles indented keywords")
# @req: REQ-SSPEC-LOCAL-001
val source = "    async static fn serve():\n        pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 1
```

</details>

### Rule: type_mismatch_coercion

#### coercion suggestions

#### suggests .to_string() for Int to String

- Verify: suggests .to_string() for Int to String


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: suggests .to_string() for Int to String")
# @req: REQ-SSPEC-LOCAL-001
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "String", "Int")
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == ".to_string()"
    case None:
        expect false
```

</details>

#### suggests .to_string() for Float to String

- Verify: suggests .to_string() for Float to String


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: suggests .to_string() for Float to String")
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "String", "Float")
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == ".to_string()"
    case None:
        expect false
```

</details>

#### suggests .to_string() for Bool to String

- Verify: suggests .to_string() for Bool to String


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: suggests .to_string() for Bool to String")
# @req: REQ-SSPEC-LOCAL-001
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "String", "Bool")
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == ".to_string()"
    case None:
        expect false
```

</details>

#### suggests .to_int() for Float to Int

- Verify: suggests .to_int() for Float to Int


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: suggests .to_int() for Float to Int")
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "Int", "Float")
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == ".to_int()"
    case None:
        expect false
```

</details>

#### suggests .to_float() for Int to Float

- Verify: suggests .to_float() for Int to Float


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: suggests .to_float() for Int to Float")
# @req: REQ-SSPEC-LOCAL-001
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "Float", "Int")
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == ".to_float()"
    case None:
        expect false
```

</details>

#### suggests != 0 for Int to Bool

- Verify: suggests != 0 for Int to Bool


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: suggests != 0 for Int to Bool")
# @req: REQ-SSPEC-LOCAL-001
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "Bool", "Int")
match result:
    case Some(fix):
        expect fix.replacements[0].new_text == " != 0"
    case None:
        expect false
```

</details>

#### no coercion available

#### returns None for unknown type pair

- Verify: returns None for unknown type pair


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: returns None for unknown type pair")
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "MyType", "OtherType")
match result:
    case Some(_):
        expect false
    case None:
        expect true
```

</details>

#### returns None for same types

- Verify: returns None for same types


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns None for same types")
# @req: REQ-SSPEC-LOCAL-001
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "Int", "Int")
match result:
    case Some(_):
        expect false
    case None:
        expect true
```

</details>

#### fix metadata

#### has Likely confidence

- Verify: has Likely confidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: has Likely confidence")
# @req: REQ-SSPEC-LOCAL-001
val result = suggest_type_coercion_fix("test.spl", 1, 10, 15, "String", "Int")
match result:
    case Some(fix):
        match fix.confidence:
            FixConfidence.Likely: expect true
            _: fail("Expected Likely confidence")
    case None:
        expect false
```

</details>

#### inserts at correct position

- Verify: inserts at correct position


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: inserts at correct position")
# @req: REQ-SSPEC-LOCAL-001
val result = suggest_type_coercion_fix("test.spl", 5, 20, 42, "String", "Int")
match result:
    case Some(fix):
        val rep = fix.replacements[0]
        expect rep.start == 42
        expect rep.end == 42
        expect rep.line == 5
    case None:
        expect false
```

</details>

### check_all_rules integration

#### aggregation

#### returns empty for clean file

- Verify: returns empty for clean file


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns empty for clean file")
# @req: REQ-SSPEC-LOCAL-001
val source = "fn main():\n    val x = 42\n"
val fixes = check_all_rules(source, "main.spl")
expect fixes.len() == 0
```

</details>

#### collects fixes from multiple rules

- Verify: collects fixes from multiple rules


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: collects fixes from multiple rules")
val source = "async static fn serve():\n    pass\n"
val fixes = check_all_rules(source, "test.spl")
# At minimum: parser_contextual_keyword should fire
expect fixes.len() >= 1
```

</details>

#### collects spec-related fixes for spec files

- Verify: collects spec-related fixes for spec files


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: collects spec-related fixes for spec files")
val source = "describe \"X\":\n    it \"y\":\n        print(42)\n        if z: fail(\"err\")\n"
val fixes = check_all_rules(source, "test_spec.spl")
# Should get: missing docstring + print_in_test + manual assertion
expect fixes.len() >= 2
```

</details>

#### no duplicates

#### each fix has unique ID

- Verify: each fix has unique ID


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: each fix has unique ID")
val source = "describe \"X\":\n    it \"y\":\n        print(1)\n        print(2)\n"
val fixes = check_all_rules(source, "test_spec.spl")
var ids: List<String> = []
for fix in fixes:
    expect not ids.contains(fix.id)
    ids.push(fix.id)
```

</details>

### Round-trip fix verification

#### parser_contextual_keyword round-trip

#### fixing async static fn clears the warning

- Verify: fixing async static fn clears the warning


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: fixing async static fn clears the warning")
# @req: REQ-SSPEC-LOCAL-001
val source = "async static fn serve():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 1

# Apply fix manually
val rep = fixes[0].replacements[0]
val fixed = source.slice(0, rep.start) + rep.new_text + source.slice(rep.end)

# Re-check: should be clean
val recheck = check_parser_contextual_keyword(fixed, "test.spl")
expect recheck.len() == 0
```

</details>

#### print_in_test_spec round-trip

#### fixing print clears the warning

- Verify: fixing print clears the warning


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: fixing print clears the warning")
# @req: REQ-SSPEC-LOCAL-001
val source = "    print(42)"
val fixes = check_print_in_test_spec(source, "test_spec.spl")
expect fixes.len() == 1

val rep = fixes[0].replacements[0]
val fixed = source.slice(0, rep.start) + rep.new_text + source.slice(rep.end)

val recheck = check_print_in_test_spec(fixed, "test_spec.spl")
expect recheck.len() == 0
```

</details>

### FixApplicator with shared rules

#### apply rule fixes

#### applies parser keyword fix

- Verify: applies parser keyword fix


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: applies parser keyword fix")
val source = "async static fn serve():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")

var sources: Dict<String, String> = {}
sources["test.spl"] = source

val result = FixApplicator.apply(fixes, sources)
match result:
    case Ok(new_sources):
        expect new_sources["test.spl"].contains("static async fn")
    case Err(e):
        expect false
```

</details>

#### applies multiple rule fixes without conflict

- Verify: applies multiple rule fixes without conflict


<details>
<summary>Executable SSpec</summary>

Runnable source: 17 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: applies multiple rule fixes without conflict")
# @req: REQ-SSPEC-LOCAL-001
val source = "async static fn serve():\n    pass\nstatic pub fn make():\n    pass\n"
val fixes = check_parser_contextual_keyword(source, "test.spl")
expect fixes.len() == 2

var sources: Dict<String, String> = {}
sources["test.spl"] = source

val result = FixApplicator.apply(fixes, sources)
match result:
    case Ok(new_sources):
        val fixed = new_sources["test.spl"]
        expect fixed.contains("static async fn")
        expect fixed.contains("pub static fn")
    case Err(e):
        expect false
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 100 |
| Active scenarios | 100 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `9d902baff24371cbc3acd0e587f3988b844ea32ede6c28dc81ab71104a4ba6aa`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `9d902baff24371cbc3acd0e587f3988b844ea32ede6c28dc81ab71104a4ba6aa`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `9d902baff24371cbc3acd0e587f3988b844ea32ede6c28dc81ab71104a4ba6aa`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **92/100**; effective score: **92/100**; blockers: **0**.

SSpec documentization score: 92/100
source: test/03_system/feature/app/easy_fix_rules_spec.spl
mirror: doc/06_spec/03_system/feature/app/easy_fix_rules_spec.md (current)
findings: 5 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=70
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/app/easy_fix_rules_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/app/easy_fix_rules_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: assumptions/preconditions, traceability, evidence, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/app/easy_fix_rules_spec.spl:81:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'detects print() in spec file' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/app/easy_fix_rules_spec.spl:89:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'detects print with string arg' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/app/easy_fix_rules_spec.spl:96:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'detects print with expression' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
