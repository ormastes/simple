# T32 LSP MCP Server -- CMM Intelligence Tests

> Purpose: splits multiline string

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 23 | 23 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# T32 LSP MCP Server -- CMM Intelligence Tests

Purpose: splits multiline string

## At a Glance

| Field | Value |
|-------|-------|
| Feature IDs | #T32-LSP-001 |
| Category | Tooling |
| Difficulty | 2/5 |
| Status | Implemented |
| Source | `test/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience
Purpose: splits multiline string
Audience: compiler and tooling engineers who maintain this spec

# T32 LSP MCP Server -- CMM Intelligence Tests

**Feature IDs:** #T32-LSP-001
**Category:** Tooling
**Difficulty:** 2/5
**Status:** Implemented

## Overview

Tests for the T32 LSP MCP server: text utility functions (split, extract),
CLI validation helpers (GUI keyword detection, command check),
and JSON encoding/extraction from the LSP JSON helpers module.

## Source

`examples/10_tooling/trace32_tools/t32_lsp_mcp/tools.spl`
`examples/10_tooling/trace32_tools/t32_lsp_mcp/json_helpers.spl`

## Scenarios

### T32 LSP MCP Text Utilities

#### split_to_lines

#### splits multiline string

- Verify: splits multiline string
   - Expected: lines[0] equals `line1`
   - Expected: lines.len() equals `3)  # oracle: value fixed by the spec contract`
   - Expected: lines[1] equals `line2`
   - Expected: lines[2] equals `line3`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: splits multiline string")
# @req: REQ-FEATURE-T32LspMcp-001
val lines = split_to_lines("line1\nline2\nline3")
expect(lines[0]).to_equal("line1")
expect(lines.len()).to_equal(3)  # oracle: value fixed by the spec contract
expect(lines[1]).to_equal("line2")
expect(lines[2]).to_equal("line3")
```

</details>

#### handles single line

- Verify: handles single line
   - Expected: lines[0] equals `hello`
   - Expected: lines.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles single line")
# @req: REQ-FEATURE-T32LspMcp-001
val lines = split_to_lines("hello")
expect(lines[0]).to_equal("hello")
expect(lines.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### handles empty string

- Verify: handles empty string
   - Expected: lines[0] equals ``
   - Expected: lines.len() equals `1)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles empty string")
# @req: REQ-FEATURE-T32LspMcp-001
val lines = split_to_lines("")
expect(lines[0]).to_equal("")
expect(lines.len()).to_equal(1)  # oracle: value fixed by the spec contract
```

</details>

#### extract_word_at

#### extracts word at start

- Verify: extracts word at start
   - Expected: word equals `BREAK.Set`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: extracts word at start")
# @req: REQ-FEATURE-T32LspMcp-001
val word = extract_word_at("BREAK.Set 0x1000", 3)
expect(word).to_equal("BREAK.Set")
```

</details>

#### extracts word in middle

- Verify: extracts word in middle
   - Expected: word equals `Data.dump`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: extracts word in middle")
# @req: REQ-FEATURE-T32LspMcp-001
val word = extract_word_at("Data.dump 0x1000", 5)
expect(word).to_equal("Data.dump")
```

</details>

#### returns empty for out-of-bounds

- Verify: returns empty for out-of-bounds
   - Expected: word equals ``


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns empty for out-of-bounds")
# @req: REQ-FEATURE-T32LspMcp-001
val word = extract_word_at("hello", -1)
expect(word).to_equal("")
```

</details>

#### stops at delimiters

- Verify: stops at delimiters
   - Expected: word equals `arg1`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: stops at delimiters")
# @req: REQ-FEATURE-T32LspMcp-001
val word = extract_word_at("fn(arg1, arg2)", 3)
expect(word).to_equal("arg1")
```

</details>

### T32 LSP MCP CLI Validation

#### GUI keyword detection

#### detects dialog keyword

- Verify: detects dialog keyword
   - Expected: violations[0] equals `dialog`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects dialog keyword")
# @req: REQ-FEATURE-T32LspMcp-001
val violations = check_gui_keywords("DIALOG.view test.dlg")
expect(violations[0]).to_equal("dialog")
expect(violations.len()).to_be_greater_than(0)
```

</details>

#### detects winpos keyword

- Verify: detects winpos keyword
   - Expected: violations[0] equals `winpos`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: detects winpos keyword")
# @req: REQ-FEATURE-T32LspMcp-001
val violations = check_gui_keywords("WinPOS 0 0 80 40")
expect(violations[0]).to_equal("winpos")
expect(violations.len()).to_be_greater_than(0)
```

</details>

#### returns empty for clean CLI script

- Verify: returns empty for clean CLI script
   - Expected: violations.len() equals `0)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns empty for clean CLI script")
# @req: REQ-FEATURE-T32LspMcp-001
val script = "SCREEN.OFF\nAREA.Create A000\nAREA.Select A000\nBREAK.Set 0x1000"
val violations = check_gui_keywords(script)
expect(violations.len()).to_equal(0)  # oracle: value fixed by the spec contract
```

</details>

#### ignores comments

- Verify: ignores comments
   - Expected: violations.len() equals `0)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores comments")
# @req: REQ-FEATURE-T32LspMcp-001
val violations = check_gui_keywords("; dialog is fine in comments")
expect(violations.len()).to_equal(0)  # oracle: value fixed by the spec contract
```

</details>

#### ignores labels ending with colon

- Verify: ignores labels ending with colon
   - Expected: violations.len() equals `0)  # oracle: value fixed by the spec contract`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores labels ending with colon")
# @req: REQ-FEATURE-T32LspMcp-001
val violations = check_gui_keywords("dialog_closed:")
expect(violations.len()).to_equal(0)  # oracle: value fixed by the spec contract
```

</details>

#### command detection

#### finds SCREEN.OFF

- Verify: finds SCREEN.OFF
   - Expected: found is true


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: finds SCREEN.OFF")
# @req: REQ-FEATURE-T32LspMcp-001
val script = "SCREEN.OFF\nBREAK.Set main\nGo"
val found = check_has_command(script, "screen.off")
expect(found).to_equal(true)
```

</details>

#### returns false when absent

- Verify: returns false when absent
   - Expected: found is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns false when absent")
# @req: REQ-FEATURE-T32LspMcp-001
val script = "BREAK.Set main\nGo"
val found = check_has_command(script, "screen.off")
expect(found).to_equal(false)
```

</details>

#### ignores commented-out commands

- Verify: ignores commented-out commands
   - Expected: found is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores commented-out commands")
# @req: REQ-FEATURE-T32LspMcp-001
val script = "; SCREEN.OFF\nBREAK.Set main"
val found = check_has_command(script, "screen.off")
expect(found).to_equal(false)
```

</details>

### T32 LSP MCP JSON Helpers

#### encoding

#### escapes special characters

- Verify: escapes special characters


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: escapes special characters")
# @req: REQ-FEATURE-T32LspMcp-001
val result = lsp_escape_json("a\"b\\c\nd")
expect(result).to_contain("\\\"")
expect(result).to_contain("\\\\")
expect(result).to_contain("\\n")
```

</details>

#### builds quoted string

- Verify: builds quoted string
   - Expected: result equals `"test"`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: builds quoted string")
# @req: REQ-FEATURE-T32LspMcp-001
val result = lsp_js("test")
expect(result).to_equal("\"test\"")
```

</details>

#### builds key-value pair

- Verify: builds key-value pair
   - Expected: result equals `"key":"val"`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: builds key-value pair")
# @req: REQ-FEATURE-T32LspMcp-001
val result = lsp_jp("key", lsp_js("val"))
expect(result).to_equal("\"key\":\"val\"")
```

</details>

#### builds single-pair object

- Verify: builds single-pair object
   - Expected: result equals `{"x":"1"}`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: builds single-pair object")
# @req: REQ-FEATURE-T32LspMcp-001
val pair = lsp_jp("x", lsp_js("1"))
val result = lsp_jo1(pair)
expect(result).to_equal("{\"x\":\"1\"}")
```

</details>

#### extraction

#### extracts field from JSON

- Verify: extracts field from JSON
   - Expected: result equals `initialize`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: extracts field from JSON")
# @req: REQ-FEATURE-T32LspMcp-001
val json = "{\"method\":\"initialize\",\"id\":1}"
val result = lsp_extract_field(json, "method")
expect(result).to_equal("initialize")
```

</details>

#### handles missing fields

- Verify: handles missing fields
   - Expected: result equals ``


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles missing fields")
# @req: REQ-FEATURE-T32LspMcp-001
val json = "{\"a\":1}"
val result = lsp_extract_field(json, "missing")
expect(result).to_equal("")
```

</details>

### T32 LSP MCP Wrapper

#### recovers from stale ready marker before tools/call

- Verify: recovers from stale ready marker before tools/call
   - Expected: input_path != "" is true
   - Expected: rt_file_write_text(input_path, messages) is true
   - Expected: setup.exit_code equals `0)  # oracle: value fixed by the spec contract`
   - Expected: result.exit_code equals `0)  # oracle: value fixed by the spec contract`
   - Expected: stdout contains `"id":"1"`
   - Expected: stdout contains `"id":"2"`
   - Expected: stdout contains `"result"`
   - Expected: stdout does not contain `Tool daemon timed out`
   - Expected: stdout contains `failed to become ready`
   - Expected: stderr does not contain `Transport closed`


<details>
<summary>Executable SSpec</summary>

Runnable source: 28 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: recovers from stale ready marker before tools/call")
# @req: REQ-FEATURE-T32LspMcp-001
val mk_input = shell("mktemp /tmp/t32_lsp_mcp_input.XXXXXX")
val input_path = (mk_input.stdout ?? "").trim()
val daemon_dir = "/tmp/t32_lsp_mcp_spec_stale_ready"
val stale_req = daemon_dir + "/stale.req"
val messages = init_request("1") + tools_call_request("2", "cmm_complete", "{\"source\":\"BR\",\"line\":0,\"character\":2}") + shutdown_request("3")
expect(input_path != "").to_equal(true)
expect(rt_file_write_text(input_path, messages)).to_equal(true)

val setup = shell("rm -rf " + shell_escape_text(daemon_dir) + " && mkdir -p " + shell_escape_text(daemon_dir) + " && printf ready > " + shell_escape_text(daemon_dir + "/ready") + " && printf stale > " + shell_escape_text(stale_req))
expect(setup.exit_code).to_equal(0)  # oracle: value fixed by the spec contract

val cmd = "timeout 15s env T32_LSP_MCP_TOOL_DAEMON_DIR=" + shell_escape_text(daemon_dir) + " /home/ormastes/dev/pub/simple/bin/t32_lsp_mcp_server < " + shell_escape_text(input_path)
val result = shell(cmd)
val stdout = result.stdout ?? ""
val stderr = result.stderr ?? ""

rt_file_delete(input_path)
shell("rm -rf " + shell_escape_text(daemon_dir))

expect(result.exit_code).to_equal(0)  # oracle: value fixed by the spec contract
expect(stdout.contains("\"id\":\"1\"")).to_equal(true)
expect(stdout.contains("\"id\":\"2\"")).to_equal(true)
expect(stdout.contains("\"result\"")).to_equal(true)
expect(stdout.contains("Tool daemon timed out")).to_equal(false)
expect(stdout.contains("failed to become ready")).to_equal(true)
expect(stderr.contains("Transport closed")).to_equal(false)
```

</details>

#### passes escaped source text through tools/call arguments

- Verify: passes escaped source text through tools/call arguments
   - Expected: input_path != "" is true
   - Expected: rt_file_write_text(input_path, messages) is true
   - Expected: setup.exit_code equals `0)  # oracle: value fixed by the spec contract`
   - Expected: result.exit_code equals `0)  # oracle: value fixed by the spec contract`
   - Expected: stdout contains `"id":"2"`
   - Expected: stdout does not contain `Missing required parameter: source`
   - Expected: stdout contains `"node_count"`
   - Expected: stderr does not contain `Transport closed`


<details>
<summary>Executable SSpec</summary>

Runnable source: 26 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: passes escaped source text through tools/call arguments")
# @req: REQ-FEATURE-T32LspMcp-001
val mk_input = shell("mktemp /tmp/t32_lsp_mcp_input.XXXXXX")
val input_path = (mk_input.stdout ?? "").trim()
val daemon_dir = "/tmp/t32_lsp_mcp_spec_escaped_source"
val args_json = "{\"source\":\"PRINT \\\"arguments\\\": 1\",\"line\":0,\"character\":0}"
val messages = init_request("1") + tools_call_request("2", "cmm_parse", args_json) + shutdown_request("3")
expect(input_path != "").to_equal(true)
expect(rt_file_write_text(input_path, messages)).to_equal(true)

val setup = shell("rm -rf " + shell_escape_text(daemon_dir))
expect(setup.exit_code).to_equal(0)  # oracle: value fixed by the spec contract

val cmd = "timeout 20s env SIMPLE_RUNTIME=" + shell_escape_text("/home/ormastes/dev/pub/simple/src/compiler_rust/target/debug/simple") + " T32_LSP_MCP_TOOL_DAEMON_DIR=" + shell_escape_text(daemon_dir) + " /home/ormastes/dev/pub/simple/bin/t32_lsp_mcp_server < " + shell_escape_text(input_path)
val result = shell(cmd)
val stdout = result.stdout ?? ""
val stderr = result.stderr ?? ""

rt_file_delete(input_path)
shell("rm -rf " + shell_escape_text(daemon_dir))

expect(result.exit_code).to_equal(0)  # oracle: value fixed by the spec contract
expect(stdout.contains("\"id\":\"2\"")).to_equal(true)
expect(stdout.contains("Missing required parameter: source")).to_equal(false)
expect(stdout.contains("\"node_count\"")).to_equal(true)
expect(stderr.contains("Transport closed")).to_equal(false)
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 23 |
| Active scenarios | 23 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `f38f5bc0224771dc72540b5216695cb35edb11970efad85587524edce62e2182`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `f38f5bc0224771dc72540b5216695cb35edb11970efad85587524edce62e2182`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `f38f5bc0224771dc72540b5216695cb35edb11970efad85587524edce62e2182`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **87/100**; effective score: **87/100**; blockers: **0**.

SSpec documentization score: 87/100
source: test/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.spl
mirror: doc/06_spec/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.md (current)
findings: 8 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=55 coverage=100 maintainability=45
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.md:1:1: warning SSDOC-EVD-002 [evidence] (-15): source steps are not visible in the generated manual
  why: Source tokens alone do not prove reader-visible workflow structure.
  improve: Use supported literal step calls and regenerate the manual.
doc/06_spec/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: scope, assumptions/preconditions, primary workflow, traceability, evidence, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.spl:1:1: advice SSDOC-MNT-007 [maintainability] (-10): research, plan, architecture, or design metadata links are incomplete
  why: Reviewers need selected lifecycle evidence, not inferred project state.
  improve: Link the selected lifecycle artifacts or configure a reasoned scope suppression.
test/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.spl:195:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'splits multiline string' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.spl:204:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'handles single line' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/app/t32_tools/t32_lsp_mcp_spec.spl:211:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'handles empty string' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
