# Host/GPU Event Path Spec

> These scenarios prove the Engine2D evidence adapter can move from a host event

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 5 | 5 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Host/GPU Event Path Spec

These scenarios prove the Engine2D evidence adapter can move from a host event

## At a Glance

| Field | Value |
|-------|-------|
| Category | Other |
| Status | Active |
| Source | `test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

These scenarios prove the Engine2D evidence adapter can move from a host event
    decision to queue submit and receipt evidence without requiring real GPU
    hardware. They cover the production routing rule: unresolved, stale-cache, and
    host-semantic events remain on host; coarse Draw IR batches can be queued to the
    GPU lane.

## Scenarios

### host/GPU event path evidence

#### should submit a coarse GPU Draw IR batch as a queue packet

- should submit a coarse GPU Draw IR batch as a queue packet
- Create a host event with a GPU batch decision


<details>
<summary>Executable SSpec</summary>

Runnable source: 25 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("should submit a coarse GPU Draw IR batch as a queue packet")
step("Create a host event with a GPU batch decision")
val lane_result = engine2d_host_gpu_lane_schedule(
    ENGINE2D_HOST_GPU_LANE_HOST,
    ENGINE2D_HOST_GPU_LANE_GPU,
    "draw_ir_delta",
    256,
    4096,
    false,
    false,
    true,
    12
)
val event = engine2d_host_gpu_event("evt-gpu", ENGINE2D_HOST_GPU_LANE_GPU, "draw_ir_delta", 256, 4096, false, false, false, true)
val decision = engine2d_host_gpu_event_handler_decision(event, lane_result)
val submit = engine2d_host_gpu_event_submit(decision, lane_result)
val receipt = engine2d_host_gpu_event_receipt(submit, lane_result)

expect decision.target_lane == ENGINE2D_HOST_GPU_LANE_GPU
expect decision.queue_required == true
expect submit.queued == true
expect receipt.gpu_batched == true
expect (engine2d_host_gpu_submit_summary(submit)).contains("kind=queue_packet")
expect (engine2d_host_gpu_receipt_summary(receipt)).contains("gpu_batched=true")
```

</details>

#### should keep unresolved or stale event targets on the host

- should keep unresolved or stale event targets on the host
- Reject unsafe GPU routing before queue submission


<details>
<summary>Executable SSpec</summary>

Runnable source: 25 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("should keep unresolved or stale event targets on the host")
step("Reject unsafe GPU routing before queue submission")
val lane_result = engine2d_host_gpu_lane_schedule(
    ENGINE2D_HOST_GPU_LANE_HOST,
    ENGINE2D_HOST_GPU_LANE_GPU,
    "draw_ir_delta",
    128,
    4096,
    false,
    false,
    true,
    12
)
val unresolved = engine2d_host_gpu_event("evt-unresolved", ENGINE2D_HOST_GPU_LANE_GPU, "draw_ir_delta", 128, 4096, false, false, true, true)
val stale = engine2d_host_gpu_event("evt-stale", ENGINE2D_HOST_GPU_LANE_GPU, "draw_ir_delta", 128, 4096, false, true, false, true)
val unresolved_decision = engine2d_host_gpu_event_handler_decision(unresolved, lane_result)
val stale_decision = engine2d_host_gpu_event_handler_decision(stale, lane_result)

expect unresolved_decision.target_lane == ENGINE2D_HOST_GPU_LANE_HOST
expect unresolved_decision.queue_required == false
expect (unresolved_decision.reason).contains("unresolved")
expect stale_decision.target_lane == ENGINE2D_HOST_GPU_LANE_HOST
expect stale_decision.queue_required == false
expect (stale_decision.reason).contains("stale")
```

</details>

#### should route Draw IR event flow to GPU only for coarse non-semantic batches

- should route Draw IR event flow to GPU only for coarse non-semantic batches
- Compare coarse GPU batch and host-semantic event routing


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("should route Draw IR event flow to GPU only for coarse non-semantic batches")
step("Compare coarse GPU batch and host-semantic event routing")
val gpu_flow = engine2d_host_gpu_draw_ir_event_flow("evt-flow-gpu", "draw_ir_delta", 512, 4096, true, false, false, false, true, 16)
val host_flow = engine2d_host_gpu_draw_ir_event_flow("evt-flow-host", "draw_ir_delta", 512, 4096, true, true, false, false, true, 16)

expect gpu_flow.decision.target_lane == ENGINE2D_HOST_GPU_LANE_GPU
expect gpu_flow.submit.queued == true
expect gpu_flow.receipt.gpu_batched == true
expect (engine2d_host_gpu_draw_ir_event_flow_summary(gpu_flow)).contains("queued=true")
expect host_flow.decision.target_lane == ENGINE2D_HOST_GPU_LANE_HOST
expect host_flow.submit.queued == false
expect host_flow.receipt.committed_on_host == true
```

</details>

#### should emit and drain a runtime GPU queue packet with typed unavailable status

- should emit and drain a runtime GPU queue packet with typed unavailable status
- Emit a runtime queue packet for a GPU Draw IR batch
- Drain without a backend and keep the unavailable status typed


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("should emit and drain a runtime GPU queue packet with typed unavailable status")
step("Emit a runtime queue packet for a GPU Draw IR batch")
rt_host_gpu_queue_reset()
rt_host_gpu_queue_emit(2, 1, 512, 0)

step("Drain without a backend and keep the unavailable status typed")
val queue = engine2d_host_gpu_runtime_queue("typed-unavailable", 1, true, 4096)
val drain = engine2d_host_gpu_runtime_drain(queue, 4)
expect drain.drained == 1
expect drain.submitted_count == 1
expect drain.completed_count == 1
expect drain.last_status_code == 4
expect drain.status == "typed_unavailable"
expect (engine2d_host_gpu_runtime_drain_summary(drain)).contains("status=typed_unavailable")
```

</details>

#### should distinguish runtime submission from evidence-only queueing

- should distinguish runtime submission from evidence-only queueing
- Submit only accepted GPU packet decisions to the runtime queue
- Refuse runtime submission when the queue is unavailable or packet is too large


<details>
<summary>Executable SSpec</summary>

Runnable source: 37 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-SYSTEM
step("should distinguish runtime submission from evidence-only queueing")
step("Submit only accepted GPU packet decisions to the runtime queue")
rt_host_gpu_queue_reset()
val lane_result = engine2d_host_gpu_lane_schedule(
    ENGINE2D_HOST_GPU_LANE_HOST,
    ENGINE2D_HOST_GPU_LANE_GPU,
    "draw_ir_delta",
    512,
    4096,
    false,
    false,
    true,
    16
)
val event = engine2d_host_gpu_event("evt-runtime-gpu", ENGINE2D_HOST_GPU_LANE_GPU, "draw_ir_delta", 512, 4096, false, false, false, true)
val decision = engine2d_host_gpu_event_handler_decision(event, lane_result)
val queue = engine2d_host_gpu_runtime_queue("vulkan", 7, true, 4096)
val runtime_submit = engine2d_host_gpu_event_submit_to_runtime(queue, decision, lane_result)

expect runtime_submit.submitted == true
expect runtime_submit.packet_id == 1
expect (engine2d_host_gpu_runtime_submit_summary(runtime_submit)).contains("status=submitted")

step("Refuse runtime submission when the queue is unavailable or packet is too large")
val unavailable_queue = engine2d_host_gpu_runtime_queue("vulkan", 0, false, 4096)
val unavailable_submit = engine2d_host_gpu_event_submit_to_runtime(unavailable_queue, decision, lane_result)
val oversized_queue = engine2d_host_gpu_runtime_queue("vulkan", 7, true, 128)
val oversized_submit = engine2d_host_gpu_event_submit_to_runtime(oversized_queue, decision, lane_result)
val runtime_drain = engine2d_host_gpu_runtime_drain(queue, 4)

expect unavailable_submit.submitted == false
expect unavailable_submit.status == "runtime_unavailable"
expect oversized_submit.submitted == false
expect oversized_submit.status == "packet_too_large"
expect runtime_drain.drained == 1
expect runtime_drain.status == "completed"
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 5 |
| Active scenarios | 5 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-SYSTEM`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `b7c6aec98879ba854e266a6666f7d9e1bbc16b61ebf99054180f7d1f8d4bbbeb`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `b7c6aec98879ba854e266a6666f7d9e1bbc16b61ebf99054180f7d1f8d4bbbeb`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `b7c6aec98879ba854e266a6666f7d9e1bbc16b61ebf99054180f7d1f8d4bbbeb`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **87/100**; effective score: **87/100**; blockers: **0**.

SSpec documentization score: 87/100
source: test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl
mirror: doc/06_spec/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.md (current)
findings: 11 blockers: 0
  narrative=100 structure=75 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=55
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, scope, assumptions/preconditions, primary workflow, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:106:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should submit a coarse GPU Draw IR batch as a queue packet' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:106:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'should submit a coarse GPU Draw IR batch as a queue packet' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:133:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should keep unresolved or stale event targets on the host' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:133:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'should keep unresolved or stale event targets on the host' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:160:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should route Draw IR event flow to GPU only for coarse non-semantic batches' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:160:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'should route Draw IR event flow to GPU only for coarse non-semantic batches' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:175:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should emit and drain a runtime GPU queue packet with typed unavailable status' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
test/03_system/feature/language/.spipe_wrapped_entry_host_gpu_event_path_spec.spl:192:1: advice SSDOC-BEH-002 [structure] (-5): scenario name 'should distinguish runtime submission from evidence-only queueing' describes the test rather than its outcome
  why: Outcome names describe product behavior rather than test mechanics.
  improve: Rename it to the observable product outcome.
<!-- sspec-maintain:scorecard:end -->
