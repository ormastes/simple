# Cached Render Entry Closure Contract

Status: **executable specification present; admitted-runtime execution blocked**.

This operator manual mirrors
`test/03_system/check/cached_render_entry_closure_contract_spec.spl`. It is
checked in so the blocked lane remains discoverable; TODO688 requires canonical
`spipe-docgen` regeneration before this manual is admitted as generated evidence.

## Operator discovery and honest status

1. Open the cached render entry-closure guide.
2. Confirm it exposes `CachedRenderEntryClosureV1` and says the workflow is blocked.
3. Confirm the guide links the canonical plan and tracked CLI blocker.
4. Confirm it calls this a planned production-evidence workflow.

## Fail-closed CLI ownership

1. Inspect the tracked blocker ownership.
2. Confirm `_CliMain`, `_CliCompile`, and `native_build_main` are named.
3. Confirm exit zero without a fresh output artifact is failure.
4. Confirm neither a Rust seed nor a stale artifact may substitute for admission.

## Sparse executor evidence boundary

1. Inspect the 7680x4320, 20-frame, 256x128 damage contract.
2. Require two considered and 512 culled commands, nonzero readback, zero
   mismatches, and a stable checksum.
3. Require p50/p95 at most 12.5 ms, max RSS, backend/fallback, execution mode,
   and binary/source hashes.
4. Keep the result executor-only; it does not prove presentation or scanout.

## Blocked verification

Run the three commands in
`doc/03_plan/sys_test/cached_render_entry_closure.md` with the exact admitted
self-hosted runtime. Accept only nine examples, zero failures, all seven
maintenance scores, zero stubs, and regenerated manual parity.
