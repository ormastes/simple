# Office Cli Tui Ui Access Specification

> **Verification status — stale, not acceptance evidence (2026-08-08).**
> This previously generated manual described a receipt-reading scenario. The
> executable SSpec now creates a unique run ID and invokes a deployed-process
> gate, so this document must be regenerated by a current self-hosted Simple
> runtime before it can be used as the operator manual. Do not reuse the
> captures or PASS summary below.

> <details>

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 1 | 1 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Office Cli Tui Ui Access Specification

## Scenarios

### Office CLI, Calc TUI, formulas, and semantic UI access

#### should pass the complete deployed F1/N1 Office workflow

- Launch the deployed IDE and Calc commands
   - Artifact capture: after_step
- Discover the Calc main surface and stable semantic controls
   - Artifact capture: after_step
- Enter source values, multiplication, and AVG through UI actions
   - Artifact capture: after_step
- Inspect independent state, correlated history, and 124x37 TUI captures
   - Artifact capture: after_step
- Verify rejection, performance, isolation, and compatibility gates
   - Artifact capture: after_step


<details>
<summary>Executable SSpec</summary>

Runnable source: 18 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Launch the deployed IDE and Calc commands")
step("Discover the Calc main surface and stable semantic controls")
step("Enter source values, multiplication, and AVG through UI actions")
step("Inspect independent state, correlated history, and 124x37 TUI captures")
step("Verify rejection, performance, isolation, and compatibility gates")
expect(file_exists(OFFICE_UI_EVIDENCE + "/suite.txt")).to_be(true)
expect(file_exists(OFFICE_UI_EVIDENCE + "/tui/calc-after.txt")).to_be(true)
expect(file_exists(OFFICE_UI_EVIDENCE + "/protocol/snapshot-after.json")).to_be(true)
expect(file_exists(OFFICE_UI_EVIDENCE + "/protocol/history.json")).to_be(true)
val receipt = read_file(OFFICE_UI_EVIDENCE + "/suite.txt")
val screen = read_file(OFFICE_UI_EVIDENCE + "/tui/calc-after.txt")
expect(receipt).to_contain("B1_display=48")
expect(receipt).to_contain("C1_display=7")
expect(receipt).to_contain("capture_size=124x37")
expect(receipt).to_contain("runtime=pure-simple-self-hosted")
expect(receipt).to_contain("suite_status=pass")
expect(screen.split("\n").len()).to_be_greater_than(36)
expect(screen).to_contain("Simple Calc")
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | Active |
| Source | `test/03_system/app/office/feature/office_cli_tui_ui_access_spec.spl` |
| Updated | 2026-07-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering:
- Office CLI, Calc TUI, formulas, and semantic UI access

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 1 |
| Active scenarios | 1 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>
