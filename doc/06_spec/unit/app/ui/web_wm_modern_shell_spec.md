# web_wm_modern_shell_spec

> Verifies the first modern Simple Web WM slice at the contract level.

<!-- sdn-diagram:id=web_wm_modern_shell_spec.arch -->
<details class="sdn-source">
<summary>SDN source</summary>

```sdn id=web_wm_modern_shell_spec.arch hash=sha256:auto render=ascii
@layout dag
@direction LR

web_wm_modern_shell_spec -> std
web_wm_modern_shell_spec -> app
```

</details>

<details class="sdn-ascii" open>
<summary>Diagram</summary>

```ascii generated-from=web_wm_modern_shell_spec.arch hash=sha256:auto
# run: simple md-diagram-update
```

</details>
<!-- sdn-diagram:end -->

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 5 | 5 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# web_wm_modern_shell_spec

Verifies the first modern Simple Web WM slice at the contract level.

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | Active |
| Source | `test/unit/app/ui/web_wm_modern_shell_spec.spl` |
| Updated | 2026-06-01 |
| Generator | `simple spipe-docgen` (Simple) |

Verifies the first modern Simple Web WM slice at the contract level.
    The shell remains server-authoritative, so this spec checks generated CSS
    and browser-side lifecycle hooks rather than issuing local state mutations.

## Scenarios

### Simple Web WM modern shell contract

#### generates rounded translucent WM chrome and motion CSS

<details>
<summary>Executable SPipe</summary>

Runnable source: 492 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
val html = generate_wm_html_page("glass_dark", "Simple WM", 8080)
expect(html).to_contain(".wm-window.opening")
expect(html).to_contain("--ui-motion-open-ms: 260")
expect(html).to_contain("--ui-motion-close-ms: 180")
expect(html).to_contain("--ui-motion-minimize-ms: 210")
expect(html).to_contain("--ui-motion-spring: cubic-bezier(0.18, 0.89, 0.28, 1.12)")
expect(html).to_contain("calc(var(--ui-motion-open-ms) * 1ms)")
expect(html).to_contain("calc(var(--ui-motion-close-ms) * 1ms)")
expect(html).to_contain("calc(var(--ui-motion-minimize-ms) * 1ms)")
expect(html).to_contain("scale(1.012)")
expect(html).to_contain("translate3d(0,42px,0) scale(0.72)")
expect(html).to_contain("--ui-backdrop-pointer-x: 50%")
expect(html).to_contain("--ui-backdrop-pointer-y: 50%")
expect(html).to_contain("#wm-desktop::before")
expect(html).to_contain("#wm-desktop::after")
expect(html).to_contain("z-index: 0")
expect(html).to_contain("radial-gradient(ellipse 420px 320px at var(--ui-backdrop-pointer-x")
expect(html).to_contain(":root[data-wm-motion=off] #wm-desktop::after")
expect(html).to_contain(":root[data-wm-backdrop-motion=static] #wm-desktop::after")
expect(html).to_contain(":root[data-wm-quiet-mode=on] #wm-desktop::after")
expect(html).to_contain(":root[data-wm-energy=critical] #wm-desktop::after")
expect(html).to_contain(".wm-window { position: absolute; z-index: 20")
expect(html).to_contain(".wm-window::after")
expect(html).to_contain(".wm-window.focused::after")
expect(html).to_contain(".wm-window:not(.focused):not(.opening)")
expect(html).to_contain(":root[data-wm-surface-depth=flat] .wm-window:not(.focused)")
expect(html).to_contain(":root[data-wm-focus-mode=work] .wm-window:not(.focused)")
expect(html).to_contain(":root[data-wm-motion=off] .wm-window.focused::after")
expect(html).to_contain("@keyframes wm-focus-halo")
expect(html).to_contain("@keyframes wm-window-open")
expect(html).to_contain("@keyframes wm-window-close")
expect(html).to_contain("@keyframes wm-window-minimize")
expect(html).to_contain("@keyframes widget-pop-in")
expect(html).to_contain("@keyframes wm-widget-float")
expect(html).to_contain("@keyframes wm-bg-drift")
expect(html).to_contain("data-wm-dock-visibility=\"shown\"")
expect(html).to_contain("data-wm-backdrop-intensity=\"balanced\"")
expect(html).to_contain("data-wm-quiet-mode=\"off\"")
expect(html).to_contain("data-wm-contrast=\"comfortable\"")
expect(html).to_contain("data-wm-feedback=\"standard\"")
expect(html).to_contain("data-wm-feedback=off")
expect(html).to_contain("--ui-feedback-policy: off")
expect(html).to_contain("data-wm-energy=\"standard\"")
expect(html).to_contain("data-wm-energy=low")
expect(html).to_contain("data-wm-energy=critical")
expect(html).to_contain("--ui-energy-policy: critical-saver")
expect(html).to_contain("--ui-energy-motion-scale-x100: 0")
expect(html).to_contain("--ui-energy-backdrop-opacity-x100")
expect(html).to_contain(".wm-quality-performance-metric[data-performance-metric='energy']")
expect(html).to_contain(".wm-quality-performance-metric[data-performance-metric='backdrop']")
expect(html).to_contain(":root[data-wm-energy=low] .wm-quality-performance-meter")
expect(html).to_contain(":root[data-wm-energy=critical] .wm-quality-performance-meter")
expect(html).to_contain(":root[data-wm-energy=critical] #wm-desktop::before")
expect(html).to_contain(".wm-quality-computed-backdrop-preview")
expect(html).to_contain(".wm-quality-computed-backdrop-metric.warn")
expect(html).to_contain(".wm-quality-computed-backdrop-orb")
expect(html).to_contain(".wm-quality-computed-backdrop-value")
expect(html).to_contain(":root[data-wm-backdrop-motion=static] .wm-quality-computed-backdrop-orb")
expect(html).to_contain(".wm-quality-computed-wallpaper-preview")
expect(html).to_contain(".wm-quality-computed-wallpaper-metric.warn")
expect(html).to_contain(".wm-quality-computed-wallpaper-orb")
expect(html).to_contain(".wm-quality-computed-wallpaper-value")
expect(html).to_contain(":root[data-wm-wallpaper=solid] .wm-quality-computed-wallpaper-orb")
expect(html).to_contain(".wm-quality-computed-motion-preview")
expect(html).to_contain(".wm-quality-computed-motion-metric.warn")
expect(html).to_contain(".wm-quality-computed-motion-rail")
expect(html).to_contain(".wm-quality-computed-motion-value")
expect(html).to_contain(":root[data-wm-motion=off] .wm-quality-computed-motion-rail")
expect(html).to_contain(".wm-quality-computed-motion-budget-preview")
expect(html).to_contain(".wm-quality-computed-motion-budget-metric.warn")
expect(html).to_contain(".wm-quality-computed-motion-budget-rail")
expect(html).to_contain(".wm-quality-computed-motion-budget-value")
expect(html).to_contain(":root[data-wm-motion=off] .wm-quality-computed-motion-budget-rail")
expect(html).to_contain(".wm-quality-computed-lifecycle-preview")
expect(html).to_contain(".wm-quality-computed-lifecycle-metric.warn")
expect(html).to_contain(".wm-quality-computed-lifecycle-sample")
expect(html).to_contain(".wm-quality-computed-lifecycle-value")
expect(html).to_contain(":root[data-wm-window-transition=none] .wm-quality-computed-lifecycle-sample")
expect(html).to_contain(".wm-quality-computed-color-preview")
expect(html).to_contain(".wm-quality-computed-color-metric.warn")
expect(html).to_contain(".wm-quality-computed-color-chip")
expect(html).to_contain(".wm-quality-computed-color-value")
expect(html).to_contain(".wm-quality-computed-layout-preview")
expect(html).to_contain(".wm-quality-computed-layout-metric.warn")
expect(html).to_contain(".wm-quality-computed-layout-frame")
expect(html).to_contain(".wm-quality-computed-layout-value")
expect(html).to_contain(".wm-quality-computed-density-preview")
expect(html).to_contain(".wm-quality-computed-density-metric.warn")
expect(html).to_contain(".wm-quality-computed-density-frame")
expect(html).to_contain(".wm-quality-computed-density-value")
expect(html).to_contain(".wm-quality-computed-shape-preview")
expect(html).to_contain(".wm-quality-computed-shape-metric.warn")
expect(html).to_contain(".wm-quality-computed-shape-sample")
expect(html).to_contain(".wm-quality-computed-shape-value")
expect(html).to_contain("data-computed-shape-metric=taskbar")
expect(html).to_contain("data-computed-shape-metric=scrollbar")
expect(html).to_contain(".wm-quality-computed-icon-preview")
expect(html).to_contain(".wm-quality-computed-icon-metric.warn")
expect(html).to_contain(".wm-quality-computed-icon-glyph")
expect(html).to_contain(".wm-quality-computed-icon-value")
expect(html).to_contain(".wm-quality-computed-scrollbar-preview")
expect(html).to_contain(".wm-quality-computed-scrollbar-metric.warn")
expect(html).to_contain(".wm-quality-computed-scrollbar-rail")
expect(html).to_contain(".wm-quality-computed-scrollbar-value")
expect(html).to_contain(".wm-quality-computed-material-preview")
expect(html).to_contain(".wm-quality-computed-material-metric.warn")
expect(html).to_contain(".wm-quality-computed-material-sample")
expect(html).to_contain(".wm-quality-computed-material-value")
expect(html).to_contain(":root[data-wm-transparency=off] .wm-quality-computed-material-sample")
expect(html).to_contain(".wm-quality-computed-surface-preview")
expect(html).to_contain(".wm-quality-computed-surface-metric.warn")
expect(html).to_contain(".wm-quality-computed-surface-sample")
expect(html).to_contain(".wm-quality-computed-surface-value")
expect(html).to_contain(":root[data-wm-transparency=off] .wm-quality-computed-surface-sample")
expect(html).to_contain(".wm-resize-handle::after")
expect(html).to_contain(".wm-window:hover .wm-resize-handle::after")
expect(html).to_contain(".wm-window[data-resize-active='true'] .wm-resize-handle::after")
expect(html).to_contain(".wm-resize-se::after")
expect(html).to_contain(".wm-resize-e::after, .wm-resize-w::after")
expect(html).to_contain(".wm-resize-s::after, .wm-resize-n::after")
expect(html).to_contain(".wm-quality-report")
expect(html).to_contain(".wm-quality-report-title")
expect(html).to_contain(".wm-quality-report-summary")
expect(html).to_contain(".wm-quality-report-copy")
expect(html).to_contain(".wm-quality-report-copy:hover")
expect(html).to_contain(".wm-quality-recommendations")
expect(html).to_contain(".wm-quality-recommendations-list")
expect(html).to_contain(".wm-quality-recommendation")
expect(html).to_contain(".wm-quality-recommendation-change")
expect(html).to_contain(".wm-quality-recommendation-evidence")
expect(html).to_contain(".wm-quality-computed-titlebar-preview")
expect(html).to_contain(".wm-quality-computed-titlebar-metric.warn")
expect(html).to_contain(".wm-quality-computed-titlebar-glyph")
expect(html).to_contain(".wm-quality-computed-titlebar-value")
expect(html).to_contain(".wm-quality-computed-traffic-preview")
expect(html).to_contain(".wm-quality-computed-traffic-metric.warn")
expect(html).to_contain(".wm-quality-computed-traffic-dots")
expect(html).to_contain(".wm-quality-computed-traffic-value")
expect(html).to_contain(":root[data-wm-motion=off] .wm-quality-computed-traffic-dots::before")
expect(html).to_contain(".wm-traffic-lights button::after")
expect(html).to_contain(".wm-traffic-lights:hover button::after")
expect(html).to_contain(".wm-btn-close::after { content: 'x'")
expect(html).to_contain(".wm-btn-minimize::after { content: '-'")
expect(html).to_contain(".wm-btn-maximize::after { content: '+'")
expect(html).to_contain(":root[data-wm-motion=off] .wm-traffic-lights button::after")
expect(html).to_contain(".wm-quality-computed-dock-preview")
expect(html).to_contain(".wm-quality-computed-dock-metric.warn")
expect(html).to_contain(".wm-quality-computed-dock-icon")
expect(html).to_contain(".wm-quality-computed-dock-value")
expect(html).to_contain(":root[data-wm-motion=off] .wm-quality-computed-dock-icon")
expect(html).to_contain(".wm-quality-computed-widget-preview")
expect(html).to_contain(".wm-quality-computed-widget-metric.warn")
expect(html).to_contain(".wm-quality-computed-widget-glyph")
expect(html).to_contain(".wm-quality-computed-widget-value")
expect(html).to_contain(".wm-quality-viewport-preview")
expect(html).to_contain(".wm-quality-viewport-metric.warn")
expect(html).to_contain(".wm-quality-viewport-frame")
expect(html).to_contain(".wm-quality-viewport-value")
expect(html).to_contain(".wm-quality-computed-spatial-preview")
expect(html).to_contain(".wm-quality-computed-spatial-metric.warn")
expect(html).to_contain(".wm-quality-computed-spatial-path")
expect(html).to_contain(".wm-quality-computed-spatial-value")
expect(html).to_contain(":root[data-wm-window-transition=none] .wm-quality-computed-spatial-path::after")
expect(html).to_contain("data-wm-contrast=high")
expect(html).to_contain("--ui-contrast-policy: high")
expect(html).to_contain("data-wm-quiet-mode=on")
expect(html).to_contain("--ui-quiet-mode: on")
expect(html).to_contain("--ui-backdrop-duration-ms: 0")
expect(html).to_contain("data-wm-backdrop-intensity=vivid")
expect(html).to_contain("data-wm-backdrop-intensity=quiet")
expect(html).to_contain("--ui-backdrop-intensity: quiet")
expect(html).to_contain("--ui-backdrop-saturation-x100")
expect(html).to_contain("--ui-backdrop-drift-scale-x100")
expect(html).to_contain("scale(calc(var(--ui-backdrop-drift-scale-x100")
expect(html).to_contain(":root[data-wm-backdrop-motion=static] #wm-desktop::before")
expect(html).to_contain(".wm-quality-computed-personalization-preview")
expect(html).to_contain(".wm-quality-computed-personalization-metric.warn")
expect(html).to_contain(".wm-quality-computed-personalization-indicator")
expect(html).to_contain(".wm-quality-computed-personalization-value")
expect(html).to_contain(".wm-quality-computed-control-center-preview")
expect(html).to_contain(".wm-quality-computed-control-center-metric.warn")
expect(html).to_contain(".wm-quality-computed-control-center-glyph")
expect(html).to_contain(".wm-quality-computed-control-center-value")
expect(html).to_contain(".wm-quality-computed-shortcut-preview")
expect(html).to_contain(".wm-quality-computed-shortcut-metric.warn")
expect(html).to_contain(".wm-quality-computed-shortcut-glyph")
expect(html).to_contain(".wm-quality-computed-shortcut-value")
expect(html).to_contain(".wm-quality-computed-multitasking-preview")
expect(html).to_contain(".wm-quality-computed-multitasking-metric.warn")
expect(html).to_contain(".wm-quality-computed-multitasking-glyph")
expect(html).to_contain(".wm-quality-computed-multitasking-value")
expect(html).to_contain(".wm-quality-computed-workspace-controls-preview")
expect(html).to_contain(".wm-quality-computed-workspace-controls-metric.warn")
expect(html).to_contain(".wm-quality-computed-workspace-controls-glyph")
expect(html).to_contain(".wm-quality-computed-workspace-controls-value")
expect(html).to_contain(".wm-quality-computed-window-actions-preview")
expect(html).to_contain(".wm-quality-computed-window-actions-metric.warn")
expect(html).to_contain(".wm-quality-computed-window-actions-glyph")
expect(html).to_contain(".wm-quality-computed-window-actions-value")
expect(html).to_contain(".wm-quality-computed-os-tools-preview")
expect(html).to_contain(".wm-quality-computed-os-tools-metric.warn")
expect(html).to_contain(".wm-quality-computed-os-tools-glyph")
expect(html).to_contain(".wm-quality-computed-os-tools-value")
expect(html).to_contain(":root[data-wm-backdrop-intensity=quiet] #wm-desktop::before")
expect(html).to_contain("animation: wm-bg-drift calc(var(--ui-backdrop-duration-ms")
expect(html).to_contain("--ui-titlebar-context-opacity-x100: 0")
expect(html).to_contain(":root[data-wm-quiet-mode=on] #wm-desktop::before")
expect(html).to_contain(":root[data-wm-quiet-mode=on] #wm-taskbar")
expect(html).to_contain("data-wm-dock-visibility=shown")
expect(html).to_contain("data-wm-dock-visibility=auto")
expect(html).to_contain("data-wm-dock-visibility=hidden")
expect(html).to_contain("--ui-dock-visibility: shown")
expect(html).to_contain("--ui-dock-visibility: auto")
expect(html).to_contain("--ui-dock-visibility: hidden")
expect(html).to_contain(":root[data-wm-dock-visibility=auto] #wm-taskbar")
expect(html).to_contain(":root[data-wm-dock-visibility=hidden] #wm-taskbar")
expect(html).to_contain(".wm-top-menu-bar")
expect(html).to_contain(".wm-top-menu-left")
expect(html).to_contain(".wm-top-menu-right")
expect(html).to_contain(".wm-top-menu-item")
expect(html).to_contain("@keyframes wm-top-menu-in")
expect(html).to_contain("@keyframes wm-snap-pulse")
expect(html).to_contain(".wm-snap-preview")
expect(html).to_contain(".wm-snap-preview.active")
expect(html).to_contain(".wm-snap-preview.active::after")
expect(html).to_contain(".wm-window.dragging")
expect(html).to_contain(".wm-window.snap-committed")
expect(html).to_contain("data-snap-commit-zone")
expect(html).to_contain(".wm-ghost")
expect(html).to_contain(".wm-ghost.drag-lifted")
expect(html).to_contain("data-snap-zone=left")
expect(html).to_contain("data-snap-zone=right")
expect(html).to_contain("data-snap-zone=full")
expect(html).to_contain("data-wm-drag-state='active'")
expect(html).to_contain("data-wm-snap-target='right'")
expect(html).to_contain("@keyframes wm-drag-lift")
expect(html).to_contain("@keyframes wm-snap-magnet")
expect(html).to_contain("@keyframes wm-snap-commit")
expect(html).to_contain(":root[data-wm-motion=off] .wm-window.snap-committed")
expect(html).to_contain(".wm-snap-layout-palette")
expect(html).to_contain(".wm-snap-layout-grid")
expect(html).to_contain(".wm-snap-layout-choice")
expect(html).to_contain(".wm-snap-layout-preview")
expect(html).to_contain("@keyframes wm-snap-layout-palette-in")
expect(html).to_contain(".wm-window-arrange-palette")
expect(html).to_contain(".wm-arrange-grid")
expect(html).to_contain(".wm-arrange-mode")
expect(html).to_contain(".wm-arrange-preview")
expect(html).to_contain("@keyframes wm-window-arrange-in")
expect(html).to_contain("data-desktop-peek='true'")
expect(html).to_contain("translate3d(0,18px,0) scale(0.94)")
expect(html).to_contain("filter: blur(2px) saturate(0.82)")
expect(html).to_contain(".wm-desktop-widgets")
expect(html).to_contain(".wm-desktop-widgets.collapsed")
expect(html).to_contain(".wm-desktop-widget")
expect(html).to_contain(".wm-desktop-widget-label")
expect(html).to_contain(".wm-desktop-widget-value")
expect(html).to_contain(".wm-desktop-widget-meta")
expect(html).to_contain(".wm-widget-controls")
expect(html).to_contain(".wm-widget-control")
expect(html).to_contain(".wm-desktop-widget.pinned")
expect(html).to_contain(".wm-desktop-widget.removing")
expect(html).to_contain("@keyframes wm-widget-remove")
expect(html).to_contain(".wm-desktop-items")
expect(html).to_contain(".wm-desktop-items.collapsed")
expect(html).to_contain(".wm-desktop-item")
expect(html).to_contain(".wm-desktop-drop-target")
expect(html).to_contain(".wm-desktop-item-icon")
expect(html).to_contain(".wm-desktop-item-label")
expect(html).to_contain("@keyframes wm-desktop-item-in")
expect(html).to_contain(".wm-window-overview")
expect(html).to_contain(".wm-overview-grid")
expect(html).to_contain(".wm-overview-card")
expect(html).to_contain(".wm-overview-card.active")
expect(html).to_contain(".wm-overview-icon")
expect(html).to_contain(".wm-overview-search")
expect(html).to_contain(".wm-overview-search:focus")
expect(html).to_contain("@keyframes wm-overview-in")
expect(html).to_contain(".wm-stage-rail")
expect(html).to_contain(".wm-stage-rail[hidden]")
expect(html).to_contain(".wm-stage-rail-item")
expect(html).to_contain(".wm-stage-rail-icon")
expect(html).to_contain(".wm-stage-rail-label")
expect(html).to_contain(".wm-stage-rail-focus")
expect(html).to_contain(".wm-stage-rail-focus:focus-visible")
expect(html).to_contain(".wm-stage-rail-actions")
expect(html).to_contain(".wm-stage-rail-action")
expect(html).to_contain(".wm-stage-rail-action:focus-visible")
expect(html).to_contain("@keyframes wm-stage-rail-in")
expect(html).to_contain(".wm-window-switcher")
expect(html).to_contain(".wm-window-switcher[hidden]")
expect(html).to_contain(".wm-window-switcher-list")
expect(html).to_contain(".wm-window-switcher-card")
expect(html).to_contain(".wm-window-switcher-card:hover")
expect(html).to_contain(".wm-window-switcher-card:focus-visible")
expect(html).to_contain(".wm-switcher-icon")
expect(html).to_contain("@keyframes wm-window-switcher-in")
expect(html).to_contain(".wm-workspace-switcher")
expect(html).to_contain(".wm-workspace-switcher[hidden]")
expect(html).to_contain(".wm-workspace-grid")
expect(html).to_contain(".wm-workspace-card")
expect(html).to_contain(".wm-workspace-card:hover")
expect(html).to_contain(".wm-workspace-switch")
expect(html).to_contain(".wm-workspace-switch:focus-visible")
expect(html).to_contain(".wm-workspace-icon")
expect(html).to_contain(".wm-workspace-actions")
expect(html).to_contain(".wm-workspace-action")
expect(html).to_contain(".wm-workspace-action:focus-visible")
expect(html).to_contain("@keyframes wm-workspace-switcher-in")
expect(html).to_contain(".wm-dock-stack")
expect(html).to_contain(".wm-dock-stack[hidden]")
expect(html).to_contain(".wm-dock-stack-list")
expect(html).to_contain(".wm-dock-stack-list.grid")
expect(html).to_contain(".wm-dock-stack-item")
expect(html).to_contain(".wm-dock-stack-item.active")
expect(html).to_contain(".wm-dock-stack-item:focus-visible")
expect(html).to_contain(".wm-dock-stack-icon")
expect(html).to_contain("@keyframes wm-dock-stack-in")
expect(html).to_contain("@keyframes wm-dock-stack-fan")
expect(html).to_contain(".wm-quality-inspector")
expect(html).to_contain(".wm-quality-inspector[hidden]")
expect(html).to_contain(".wm-quality-grid")
expect(html).to_contain(".wm-quality-row.good")
expect(html).to_contain(".wm-quality-row.warn")
expect(html).to_contain(".wm-quality-details")
expect(html).to_contain(".wm-quality-swatches")
expect(html).to_contain(".wm-quality-swatch")
expect(html).to_contain(".wm-quality-swatch-chip")
expect(html).to_contain(".wm-quality-metrics")
expect(html).to_contain(".wm-quality-metric")
expect(html).to_contain(".wm-quality-metric-value")
expect(html).to_contain(".wm-quality-audit-policy")
expect(html).to_contain(".wm-quality-policy-item")
expect(html).to_contain(".wm-quality-policy-label")
expect(html).to_contain(".wm-quality-policy-value")
expect(html).to_contain("@keyframes wm-quality-inspector-in")
expect(html).to_contain(".wm-accent-palette")
expect(html).to_contain(".wm-accent-palette[hidden]")
expect(html).to_contain(".wm-accent-grid")
expect(html).to_contain(".wm-accent-choice")
expect(html).to_contain(".wm-accent-swatch")
expect(html).to_contain(".wm-accent-blue")
expect(html).to_contain("@keyframes wm-accent-palette-in")
expect(html).to_contain(".wm-wallpaper-picker")
expect(html).to_contain(".wm-wallpaper-picker[hidden]")
expect(html).to_contain(".wm-wallpaper-grid")
expect(html).to_contain(".wm-wallpaper-choice")
expect(html).to_contain(".wm-wallpaper-swatch")
expect(html).to_contain(".wm-wallpaper-swatch-aurora")
expect(html).to_contain("@keyframes wm-wallpaper-picker-in")
expect(html).to_contain("@keyframes wm-wallpaper-orbit")
expect(html).to_contain(".wm-clipboard-history")
expect(html).to_contain(".wm-clipboard-history[hidden]")
expect(html).to_contain(".wm-clipboard-list")
expect(html).to_contain(".wm-clipboard-item")
expect(html).to_contain(".wm-clipboard-item.pinned")
expect(html).to_contain(".wm-clipboard-mini-action")
expect(html).to_contain("@keyframes wm-clipboard-in")
expect(html).to_contain(".wm-screen-capture-overlay")
expect(html).to_contain(".wm-screen-capture-overlay[hidden]")
expect(html).to_contain(".wm-capture-selection")
expect(html).to_contain(".wm-capture-toolbar")
expect(html).to_contain(".wm-capture-mode")
expect(html).to_contain(".wm-capture-action")
expect(html).to_contain("@keyframes wm-capture-in")
expect(html).to_contain(".wm-quick-settings")
expect(html).to_contain(".wm-quick-settings[hidden]")
expect(html).to_contain(".wm-quick-grid")
expect(html).to_contain(".wm-quick-setting")
expect(html).to_contain(".wm-quick-setting:hover")
expect(html).to_contain(".wm-quick-icon")
expect(html).to_contain(".wm-quick-sliders")
expect(html).to_contain(".wm-quick-slider-row")
expect(html).to_contain(".wm-quick-slider")
expect(html).to_contain(".wm-quick-slider:focus-visible")
expect(html).to_contain(".wm-quick-slider-value[aria-live='polite']")
expect(html).to_contain("@keyframes wm-quick-settings-in")
expect(html).to_contain(".wm-live-activity")
expect(html).to_contain(".wm-live-activity[hidden]")
expect(html).to_contain(".wm-live-activity[data-live-state='paused']")
expect(html).to_contain(".wm-live-activity-progress")
expect(html).to_contain(".wm-live-activity-bar")
expect(html).to_contain(".wm-live-activity-actions")
expect(html).to_contain(".wm-live-activity-action")
expect(html).to_contain(".wm-live-activity-action:focus-visible")
expect(html).to_contain("@keyframes wm-live-activity-in")
expect(html).to_contain(".wm-system-hud")
expect(html).to_contain(".wm-system-hud[hidden]")
expect(html).to_contain(".wm-system-hud-icon")
expect(html).to_contain(".wm-system-hud-meter")
expect(html).to_contain("@keyframes wm-system-hud-in")
expect(html).to_contain(".wm-privacy-indicator")
expect(html).to_contain(".wm-privacy-indicator[hidden]")
expect(html).to_contain(".wm-privacy-dot")
expect(html).to_contain(".wm-privacy-row")
expect(html).to_contain("@keyframes wm-privacy-in")
expect(html).to_contain(".wm-control-center")
expect(html).to_contain(".wm-control-center[hidden]")
expect(html).to_contain(".wm-control-group")
expect(html).to_contain(".wm-control-button")
expect(html).to_contain("@keyframes wm-control-in")
expect(html).to_contain("prefers-reduced-motion")
expect(html).to_contain("data-wm-motion=reduced")
expect(html).to_contain("data-wm-motion=off")
expect(html).to_contain(":root[data-wm-motion=reduced] { --ui-motion-open-ms: 120")
expect(html).to_contain(":root[data-wm-motion=off] { --ui-motion-open-ms: 0")
expect(html).to_contain("transition-duration: 80ms")
expect(html).to_contain(".wm-titlebar-icon")
expect(html).to_contain(".wm-round-icon")
expect(html).to_contain(".wm-icon-normalized-square")
expect(html).to_contain(".wm-icon-image")
expect(html).to_contain(".wm-icon-glyph")
expect(html).to_contain(".wm-quality-computed-icon-kit-preview")
expect(html).to_contain(".wm-quality-computed-icon-kit-item")
expect(html).to_contain(".wm-quality-computed-icon-kit-icon")
expect(html).to_contain(".wm-quality-computed-icon-kit-label")
expect(html).to_contain("grid-template-columns: repeat(5, minmax(0, 1fr))")
expect(html).to_contain(".wm-title-context")
expect(html).to_contain(".wm-title-input")
expect(html).to_contain(".wm-command-bar")
expect(html).to_contain(".wm-title-command-modes")
expect(html).to_contain(".wm-title-command-mode")
expect(html).to_contain(".wm-title-command-profiles")
expect(html).to_contain(".wm-title-command-profile")
expect(html).to_contain(".wm-quality-computed-command-preview")
expect(html).to_contain(".wm-quality-computed-command-metric.warn")
expect(html).to_contain(".wm-quality-computed-command-glyph")
expect(html).to_contain(".wm-quality-computed-command-value")
expect(html).to_contain(".wm-command-palette")
expect(html).to_contain(".wm-command-palette-input")
expect(html).to_contain(".wm-command-item")
expect(html).to_contain(".wm-app-launcher")
expect(html).to_contain(".wm-app-launcher[hidden]")
expect(html).to_contain(".wm-app-launcher-tile")
expect(html).to_contain(".wm-app-launcher-filters")
expect(html).to_contain(".wm-app-launcher-filter.active")
expect(html).to_contain(".wm-app-launcher-category")
expect(html).to_contain(".wm-app-launcher-empty")
expect(html).to_contain("@keyframes wm-launcher-in")
expect(html).to_contain(".wm-widget-gallery")
expect(html).to_contain(".wm-widget-gallery-card.active")
expect(html).to_contain(".wm-widget-gallery-card:focus-visible")
expect(html).to_contain(".wm-widget-gallery-status")
expect(html).to_contain(".wm-shortcut-overlay")
expect(html).to_contain(".wm-shortcut-overlay[hidden]")
expect(html).to_contain(".wm-shortcut-search")
expect(html).to_contain(".wm-shortcut-search:focus")
expect(html).to_contain(".wm-shortcut-section")
expect(html).to_contain(".wm-shortcut-row")
expect(html).to_contain(".wm-shortcut-empty")
expect(html).to_contain("@keyframes wm-shortcuts-in")
expect(html).to_contain(".wm-tooltip")
expect(html).to_contain(".wm-tooltip[hidden]")
expect(html).to_contain("@keyframes wm-tooltip-in")
expect(html).to_contain(":root[data-wm-motion=reduced] .wm-tooltip")
expect(html).to_contain(":root[data-wm-motion=off] .wm-tooltip")
expect(html).to_contain(".wm-window-context-menu")
expect(html).to_contain(".wm-window-context-menu[hidden]")
expect(html).to_contain(".wm-window-context-item")
expect(html).to_contain("@keyframes wm-context-menu-in")

expect(html).to_contain(":focus-visible")
expect(html).to_contain("outline-offset: 3px")
expect(html).to_contain(".wm-traffic-lights button::before")
expect(html).to_contain("inset: -8px")
expect(html).to_contain(".wm-quality-computed-accessibility-preview")
expect(html).to_contain(".wm-quality-computed-accessibility-metric.warn")
expect(html).to_contain(".wm-quality-computed-accessibility-indicator")
expect(html).to_contain(".wm-quality-computed-accessibility-value")
expect(html).to_contain(".wm-quality-computed-status-preview")
expect(html).to_contain(".wm-quality-computed-status-metric.warn")
expect(html).to_contain(".wm-quality-computed-status-indicator")
expect(html).to_contain(".wm-quality-computed-status-value")
expect(html).to_contain(".wm-quality-computed-productivity-preview")
expect(html).to_contain(".wm-quality-computed-productivity-metric.warn")
expect(html).to_contain(".wm-quality-computed-productivity-indicator")
expect(html).to_contain(".wm-quality-computed-productivity-value")
expect(html).to_contain(".wm-command-palette-input { width: 100%; height: 44px")
expect(html).to_contain("min-height: 44px")
expect(html).to_contain(".wm-taskbar-icon")
expect(html).to_contain(".wm-taskbar-badge")
expect(html).to_contain(".wm-taskbar-item.attention")
expect(html).to_contain("@keyframes wm-taskbar-attention")
expect(html).to_contain("@keyframes wm-taskbar-badge-pulse")
expect(html).to_contain(".wm-notification-actions")
expect(html).to_contain(".wm-notification-action:hover")
expect(html).to_contain(".wm-notification-filters")
expect(html).to_contain(".wm-notification-filter.active")
expect(html).to_contain(".wm-notification-empty")
expect(html).to_contain("grid-column: 1 / -1")
expect(html).to_contain("translateY(-3px) scale(1.04)")
expect(html).to_contain("border-radius: 999px")
```

</details>

#### client applies lifecycle and icon classes without protocol changes

<details>
<summary>Executable SPipe</summary>

Runnable source: 865 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
val js = rt_file_read_text("src/app/ui.web/wm.js")
val retained = rt_file_read_text("src/app/ui.web/retained_renderer.js")
expect(js).to_contain("_makeTaskbarIcon")
expect(js).to_contain("WM_MOTION_STORAGE_KEY")
expect(js).to_contain("simple.wm.motion")
expect(js).to_contain("simpleWmSetMotion")
expect(js).to_contain("_applyMotionPreference")
expect(js).to_contain("_normalizeMotionPreference")
expect(js).to_contain("_ensureTopMenuBar")
expect(js).to_contain("_renderTopMenuBar")
expect(js).to_contain("_makeTopMenuButton")
expect(js).to_contain("_activeWindowTitle")
expect(js).to_contain("_topMenuTimeLabel")
expect(js).to_contain("_activateTopMenuAction")
expect(js).to_contain("top_menu_action")
expect(js).to_contain("_ensureCommandPalette")
expect(js).to_contain("_commandPaletteCommands")
expect(js).to_contain("_toggleCommandPalette")
expect(js).to_contain("_moveCommandPaletteSelection")
expect(js).to_contain("_executeCommandPaletteAction")
expect(js).to_contain("_renderCommandPaletteRecents")
expect(js).to_contain("category: 'Apps'")
expect(js).to_contain("category: 'System'")
expect(js).to_contain("category: 'Appearance'")
expect(js).to_contain("category: 'Quality'")
expect(js).to_contain("wm-command-section")
expect(js).to_contain("wm-command-recents")
expect(js).to_contain("wm-command-recent")
expect(js).to_contain("_activateTaskbarItem")
expect(js).to_contain("Open Simple IDE")
expect(js).to_contain("Set motion: off")
expect(js).to_contain("Set transparency: standard")
expect(js).to_contain("Set transparency: reduced")
expect(js).to_contain("Set transparency: off")
expect(js).to_contain("Set wallpaper: aurora")
expect(js).to_contain("Set wallpaper: mesh")
expect(js).to_contain("Set wallpaper: solid")
expect(js).to_contain("WM_DOCK_VISIBILITY_STORAGE_KEY")
expect(js).to_contain("simple.wm.dock_visibility")
expect(js).to_contain("WM_QUIET_MODE_STORAGE_KEY")
expect(js).to_contain("WM_CONTRAST_STORAGE_KEY")
expect(js).to_contain("WM_FEEDBACK_STORAGE_KEY")
expect(js).to_contain("simple.wm.feedback")
expect(js).to_contain("simpleWmSetFeedback")
expect(js).to_contain("setFeedbackPreference")
expect(js).to_contain("_applyFeedbackPreference")
expect(js).to_contain("_readFeedbackPreference")
expect(js).to_contain("_normalizeFeedbackPreference")
expect(js).to_contain("_feedbackLabel")
expect(js).to_contain("_feedbackAllows")
expect(js).to_contain("Set feedback: standard")
expect(js).to_contain("Set feedback: subtle")
expect(js).to_contain("Set feedback: off")
expect(js).to_contain("WM_ENERGY_STORAGE_KEY")
expect(js).to_contain("simple.wm.energy")
expect(js).to_contain("simpleWmSetEnergy")
expect(js).to_contain("setEnergyPreference")
expect(js).to_contain("_applyEnergyPreference")
expect(js).to_contain("_readEnergyPreference")
expect(js).to_contain("_normalizeEnergyPreference")
expect(js).to_contain("_energyLabel")
expect(js).to_contain("Set energy: standard")
expect(js).to_contain("Set energy: low power")
expect(js).to_contain("Set energy: critical saver")
expect(js).to_contain("setEnergyPreference('low')")
expect(js).to_contain("setEnergyPreference('critical')")
expect(js).to_contain("this._energyMode || 'standard'")
expect(js).to_contain("--ui-backdrop-opacity-x100")
expect(js).to_contain("simple.wm.contrast")
expect(js).to_contain("simpleWmSetContrast")
expect(js).to_contain("setContrastPreference")
expect(js).to_contain("_applyContrastPreference")
expect(js).to_contain("_readContrastPreference")
expect(js).to_contain("_normalizeContrastPreference")
expect(js).to_contain("_contrastLabel")
expect(js).to_contain("Set contrast: comfortable")
expect(js).to_contain("Set contrast: high")
expect(js).to_contain("simple.wm.quiet_mode")
expect(js).to_contain("simpleWmSetQuietMode")
expect(js).to_contain("setQuietModePreference")
expect(js).to_contain("_applyQuietModePreference")
expect(js).to_contain("_readQuietModePreference")
expect(js).to_contain("Set quiet mode: off")
expect(js).to_contain("Set quiet mode: on")
expect(js).to_contain("Quiet mode")
expect(js).to_contain("Quiet mode off")
expect(js).to_contain("Quiet mode on")
expect(js).to_contain("simpleWmSetDockVisibility")
expect(js).to_contain("setDockVisibilityPreference")
expect(js).to_contain("_applyDockVisibilityPreference")
expect(js).to_contain("_readDockVisibilityPreference")
expect(js).to_contain("_normalizeDockVisibilityPreference")
expect(js).to_contain("_dockVisibilityLabel")
expect(js).to_contain("dataset.wmDockVisibility")
expect(js).to_contain("Set dock visibility: shown")
expect(js).to_contain("Set dock visibility: auto-hide")
expect(js).to_contain("Set dock visibility: hidden")
expect(js).to_contain("Dock visibility")
expect(js).to_contain("Dock shown")
expect(js).to_contain("Dock auto-hide")
expect(js).to_contain("Dock hidden")
expect(js).to_contain("_ensureDockStack")
expect(js).to_contain("_toggleDockStack")
expect(js).to_contain("_renderDockStack")
expect(js).to_contain("_dockStackItems")
expect(js).to_contain("_makeDockStackItem")
expect(js).to_contain("_setDockStackMode")
expect(js).to_contain("_moveDockStackSelection")
expect(js).to_contain("_syncDockStackSelection")
expect(js).to_contain("_activateDockStackSelection")
expect(js).to_contain("_openDockStackItem")
expect(js).to_contain("_dockStackActiveIndex")
expect(js).to_contain("aria-activedescendant")
expect(js).to_contain("aria-selected")
expect(js).to_contain("event.key === 'ArrowRight'")
expect(js).to_contain("event.key === 'ArrowDown'")
expect(js).to_contain("event.key === 'Enter'")
expect(js).to_contain("event.key === ' '")
expect(js).to_contain("event.key === 'Escape'")
expect(js).to_contain("_makeQualityComputedDockPreview")
expect(js).to_contain("_makeQualityComputedDockMetric")
expect(js).to_contain("dataset.qualityComputedDock")
expect(js).to_contain("dataset.computedDockMetric")
expect(js).to_contain("--ui-dock-target-scale-x100")
expect(js).to_contain("_qualityElementMinHeight(item)")
expect(js).to_contain("Open Dock stack")
expect(js).to_contain("Cmd Shift D")
expect(js).to_contain("wantsDockStack")
expect(js).to_contain("dock_stack_open")
expect(js).to_contain("_ensureQualityInspector")
expect(js).to_contain("_toggleQualityInspector")
expect(js).to_contain("_renderQualityInspector")
expect(js).to_contain("_qualityInspectorItems")
expect(js).to_contain("_makeQualityInspectorRow")
expect(js).to_contain("_makeQualityDetailPanel")
expect(js).to_contain("_makeQualitySwatches")
expect(js).to_contain("_makeQualityMetric")
expect(js).to_contain("_makeQualityAuditPolicy")
expect(js).to_contain("_makeQualityReportPreview")
expect(js).to_contain("_qualityReportText")
expect(js).to_contain("_copyQualityReport")
expect(js).to_contain("_qualityReportScore")
expect(js).to_contain("_makeQualityRecommendations")
expect(js).to_contain("_qualityRecommendationItems")
expect(js).to_contain("dataset.qualityRecommendations")
expect(js).to_contain("wm-quality-recommendation")
expect(js).to_contain("item.fix || 'Review theme tokens'")
expect(js).to_contain("preferred = ['color', 'motion', 'material'")
expect(js).to_contain("quality_report_copy")
expect(js).to_contain("navigator.clipboard.writeText")
expect(js).to_contain("Simple WM quality report")
expect(js).to_contain("_makeQualityComputedColorPreview")
expect(js).to_contain("_makeQualityComputedColorMetric")
expect(js).to_contain("_qualityComputedSurfaceColors")
expect(js).to_contain("_qualityVisibleColor")
expect(js).to_contain("dataset.qualityComputedColor")
expect(js).to_contain("dataset.computedColorMetric")
expect(js).to_contain("getComputedStyle(el)")
expect(js).to_contain(".wm-window.focused, .wm-window")
expect(js).to_contain("_makeQualityComputedLayoutPreview")
expect(js).to_contain("_makeQualityComputedLayoutMetric")
expect(js).to_contain("_qualityComputedGeometry")
expect(js).to_contain("dataset.qualityComputedLayout")
expect(js).to_contain("dataset.computedLayoutMetric")
expect(js).to_contain("_makeQualityComputedDensityPreview")
expect(js).to_contain("_makeQualityComputedDensityMetric")
expect(js).to_contain("dataset.qualityComputedDensity")
expect(js).to_contain("dataset.computedDensityMetric")
expect(js).to_contain("document.documentElement.dataset.wmDensity")
expect(js).to_contain("--ui-layout-panel-gap-px")
expect(js).to_contain("--ui-layout-min-touch-target-px")
expect(js).to_contain("#wm-taskbar")
expect(js).to_contain("_makeQualityComputedShapePreview")
expect(js).to_contain("_makeQualityComputedShapeMetric")
expect(js).to_contain("_qualityRadiusEvidence")
expect(js).to_contain("dataset.qualityComputedShape")
expect(js).to_contain("dataset.computedShapeMetric")
expect(js).to_contain("style.borderRadius")
expect(js).to_contain("_makeQualityComputedIconPreview")
expect(js).to_contain("_makeQualityComputedIconMetric")
expect(js).to_contain("_qualityComputedIconEvidence")
expect(js).to_contain("_simpleOsIconSeeds")
expect(js).to_contain("_makeQualityComputedIconKitPreview")
expect(js).to_contain("dataset.qualityComputedIcon")
expect(js).to_contain("dataset.qualityComputedIconKit")
expect(js).to_contain("dataset.iconKitId")
expect(js).to_contain("dataset.computedIconMetric")
expect(js).to_contain("simple.os")
expect(js).to_contain("simple.wm")
expect(js).to_contain("simple.square")
expect(js).to_contain("style.objectFit")
expect(js).to_contain("style.clipPath")
expect(js).to_contain("style.borderRadius")
expect(js).to_contain("_makeQualityComputedScrollbarPreview")
expect(js).to_contain("_makeQualityComputedScrollbarMetric")
expect(js).to_contain("_qualityComputedScrollbarEvidence")
expect(js).to_contain("dataset.qualityComputedScrollbar")
expect(js).to_contain("dataset.computedScrollbarMetric")
expect(js).to_contain("style.overflowY")
expect(js).to_contain("style.scrollbarWidth")
expect(js).to_contain("_makeQualityComputedMaterialPreview")
expect(js).to_contain("_makeQualityComputedMaterialMetric")
expect(js).to_contain("_qualityComputedMaterialEvidence")
expect(js).to_contain("_qualityCssAlpha")
expect(js).to_contain("dataset.qualityComputedMaterial")
expect(js).to_contain("dataset.computedMaterialMetric")
expect(js).to_contain("style.backdropFilter")
expect(js).to_contain("style.webkitBackdropFilter")
expect(js).to_contain("style.backgroundColor")
expect(js).to_contain("_makeQualityComputedSurfacePreview")
expect(js).to_contain("_makeQualityComputedSurfaceMetric")
expect(js).to_contain("_qualityComputedSurfaceEvidence")
expect(js).to_contain("dataset.qualityComputedSurface")
expect(js).to_contain("dataset.computedSurfaceMetric")
expect(js).to_contain("_makeQualityComputedBackdropPreview")
expect(js).to_contain("_makeQualityComputedBackdropMetric")
expect(js).to_contain("dataset.qualityComputedBackdrop")
expect(js).to_contain("dataset.computedBackdropMetric")
expect(js).to_contain("_makeQualityComputedWallpaperPreview")
expect(js).to_contain("_makeQualityComputedWallpaperMetric")
expect(js).to_contain("dataset.qualityComputedWallpaper")
expect(js).to_contain("dataset.computedWallpaperMetric")
expect(js).to_contain("_readBackdropMotionPreference")
expect(js).to_contain("_wallpaperChoices()")
expect(js).to_contain("--ui-backdrop-duration-ms")
expect(js).to_contain("_makeQualityComputedLifecyclePreview")
expect(js).to_contain("_makeQualityComputedLifecycleMetric")
expect(js).to_contain("dataset.qualityComputedLifecycle")
expect(js).to_contain("dataset.computedLifecycleMetric")
expect(js).to_contain("_makeQualityComputedWidgetPreview")
expect(js).to_contain("_makeQualityComputedWidgetMetric")
expect(js).to_contain("dataset.qualityComputedWidget")
expect(js).to_contain("dataset.computedWidgetMetric")
expect(js).to_contain("_makeQualityComputedTitlebarPreview")
expect(js).to_contain("_makeQualityComputedTitlebarMetric")
expect(js).to_contain("_qualityVisibleText")
expect(js).to_contain("dataset.qualityComputedTitlebar")
expect(js).to_contain("dataset.computedTitlebarMetric")
expect(js).to_contain("_trafficSideMode === 'left'")
expect(js).to_contain("_makeQualityComputedTrafficPreview")
expect(js).to_contain("_makeQualityComputedTrafficMetric")
expect(js).to_contain("_qualityTrafficHitEvidence")
expect(js).to_contain("_qualityTrafficMotionEvidence")
expect(js).to_contain("dataset.qualityComputedTraffic")
expect(js).to_contain("dataset.computedTrafficMetric")
expect(js).to_contain("button.dataset.action")
expect(js).to_contain("getComputedStyle(button, '::before')")
expect(js).to_contain("_makeQualityComputedMotionPreview")
expect(js).to_contain("_makeQualityComputedMotionMetric")
expect(js).to_contain("_qualityComputedMotionTiming")
expect(js).to_contain("_qualityCssDurationListMs")
expect(js).to_contain("_makeQualityComputedMotionBudgetPreview")
expect(js).to_contain("_makeQualityComputedMotionBudgetMetric")
expect(js).to_contain("_qualityMotionBudgetSnapshot")
expect(js).to_contain("dataset.qualityComputedMotion")
expect(js).to_contain("dataset.computedMotionMetric")
expect(js).to_contain("dataset.qualityComputedMotionBudget")
expect(js).to_contain("dataset.computedMotionBudgetMetric")
expect(js).to_contain("document.getAnimations")
expect(js).to_contain("animation.effect?.getTiming")
expect(js).to_contain("getComputedStyle(el, pseudo || null)")
expect(js).to_contain("style.animationDuration")
expect(js).to_contain("style.transitionDuration")
expect(js).to_contain("#wm-desktop")
expect(js).to_contain("::before")
expect(js).to_contain("_desktopBackdropPointer")
expect(js).to_contain("_desktopBackdropPointerRaf")
expect(js).to_contain("_desktopBackdropPointerEnabled")
expect(js).to_contain("_updateDesktopBackdropPointer")
expect(js).to_contain("_resetDesktopBackdropPointer")
expect(js).to_contain("requestAnimationFrame")
expect(js).to_contain("--ui-backdrop-pointer-x")
expect(js).to_contain("--ui-backdrop-pointer-y")
expect(js).to_contain("_makeQualityComputedPersonalizationPreview")
expect(js).to_contain("_makeQualityComputedPersonalizationMetric")
expect(js).to_contain("_qualityPersonalizationSnapshot")
expect(js).to_contain("dataset.qualityComputedPersonalization")
expect(js).to_contain("dataset.computedPersonalizationMetric")
expect(js).to_contain("document.documentElement")
expect(js).to_contain("dataset.wmMotion")
expect(js).to_contain("dataset.wmTransparency")
expect(js).to_contain("dataset.wmWallpaper")
expect(js).to_contain("dataset.wmAccent")
expect(js).to_contain("_accentChoices()")
expect(js).to_contain("_makeQualityComputedControlCenterPreview")
expect(js).to_contain("_makeQualityComputedControlCenterMetric")
expect(js).to_contain("_qualityControlCenterSnapshot")
expect(js).to_contain("dataset.qualityComputedControlCenter")
expect(js).to_contain("dataset.computedControlCenterMetric")
expect(js).to_contain("_ensureControlCenter()")
expect(js).to_contain("_renderControlCenter()")
expect(js).to_contain("querySelectorAll('.wm-control-group')")
expect(js).to_contain("querySelectorAll('.wm-control-button')")
expect(js).to_contain("querySelectorAll('.wm-control-button.active[aria-pressed=\"true\"]')")
expect(js).to_contain("Workspace tools")
expect(js).to_contain("_makeQualityComputedOsToolsPreview")
expect(js).to_contain("_makeQualityComputedOsToolsMetric")
expect(js).to_contain("_qualityOsToolsSnapshot")
expect(js).to_contain("dataset.qualityComputedOsTools")
expect(js).to_contain("dataset.computedOsToolsMetric")
expect(js).to_contain("_appLauncherGrid.querySelectorAll('.wm-app-launcher-tile')")
expect(js).to_contain("_appLauncherFilters.querySelectorAll('[data-app-category]')")
expect(js).to_contain("_ensureWidgetGallery()")
expect(js).to_contain("_renderWidgetGallery()")
expect(js).to_contain("querySelectorAll('.wm-widget-gallery-card')")
expect(js).to_contain("_widgetGalleryActiveIndex")
expect(js).to_contain("_moveWidgetGallerySelection")
expect(js).to_contain("_syncWidgetGallerySelection")
expect(js).to_contain("_activateWidgetGallerySelection")
expect(js).to_contain("aria-activedescendant")
expect(js).to_contain("aria-selected")
expect(js).to_contain("event.key === 'ArrowRight'")
expect(js).to_contain("event.key === 'ArrowDown'")
expect(js).to_contain("event.key === 'Enter'")
expect(js).to_contain("event.key === ' '")
expect(js).to_contain("event.key === 'Escape'")
expect(js).to_contain("widget_add")
expect(js).to_contain("_ensureWallpaperPicker()")
expect(js).to_contain("_renderWallpaperPicker()")
expect(js).to_contain("querySelectorAll('[data-wallpaper-choice]')")
expect(js).to_contain("_ensureAccentPalette()")
expect(js).to_contain("_renderAccentPalette()")
expect(js).to_contain("querySelectorAll('[data-accent-choice]')")
expect(js).to_contain("_ensureDockStack()")
expect(js).to_contain("_renderDockStack()")
expect(js).to_contain("querySelectorAll('[data-stack-item]')")
expect(js).to_contain("querySelectorAll('[data-stack-mode]')")
expect(js).to_contain("_makeQualityComputedShortcutPreview")
expect(js).to_contain("_makeQualityComputedShortcutMetric")
expect(js).to_contain("_qualityShortcutSnapshot")
expect(js).to_contain("dataset.qualityComputedShortcut")
expect(js).to_contain("dataset.computedShortcutMetric")
expect(js).to_contain("_commandPaletteCommands().length")
expect(js).to_contain("_shortcutOverlayItems().length")
expect(js).to_contain("_ensureShortcutOverlay()")
expect(js).to_contain("querySelector('.wm-shortcut-search')")
expect(js).to_contain("_titleCommandModes().length")
expect(js).to_contain("_windowContextMenuGroups().length")
expect(js).to_contain("_makeQualityComputedMultitaskingPreview")
expect(js).to_contain("_makeQualityComputedMultitaskingMetric")
expect(js).to_contain("_qualityMultitaskingSnapshot")
expect(js).to_contain("dataset.qualityComputedMultitasking")
expect(js).to_contain("dataset.computedMultitaskingMetric")
expect(js).to_contain("_windowSwitcherWindows()")
expect(js).to_contain("_ensureWindowOverview()")
expect(js).to_contain("_renderWindowOverview()")
expect(js).to_contain("querySelectorAll('.wm-overview-card')")
expect(js).to_contain("querySelector('.wm-overview-search')")
expect(js).to_contain("_ensureWindowSwitcher()")
expect(js).to_contain("_renderWindowSwitcher()")
expect(js).to_contain("querySelectorAll('.wm-window-switcher-card')")
expect(js).to_contain("switcher.dataset.switcherShortcut === 'Meta Tab'")
expect(js).to_contain("_ensureStageRail()")
expect(js).to_contain("_renderStageRail()")
expect(js).to_contain("querySelectorAll('.wm-stage-rail-item')")
expect(js).to_contain("querySelectorAll('[data-stage-action]')")
expect(js).to_contain("desktop.dataset.desktopPeek")
expect(js).to_contain("_makeQualityComputedWorkspaceControlsPreview")
expect(js).to_contain("_makeQualityComputedWorkspaceControlsMetric")
expect(js).to_contain("_qualityWorkspaceControlsSnapshot")
expect(js).to_contain("dataset.qualityComputedWorkspaceControls")
expect(js).to_contain("dataset.computedWorkspaceControlsMetric")
expect(js).to_contain("_ensureWorkspaceSwitcher()")
expect(js).to_contain("_renderWorkspaceSwitcher()")
expect(js).to_contain("querySelectorAll('.wm-workspace-card')")
expect(js).to_contain("querySelectorAll('[data-workspace-action]')")
expect(js).to_contain("_ensureHotCorners()")
expect(js).to_contain("querySelectorAll('[data-hot-corner-action]')")
expect(js).to_contain("_ensureGestureHints()")
expect(js).to_contain("_renderGestureHints()")
expect(js).to_contain("querySelectorAll('[data-gesture-hint]')")
expect(js).to_contain("_ensureResizeHud()")
expect(js).to_contain("_normalizeFocusMode(document.documentElement.dataset.wmFocusMode || this._focusMode)")
expect(js).to_contain("_makeQualityViewportPreview")
expect(js).to_contain("_makeQualityViewportMetric")
expect(js).to_contain("_qualityViewportSnapshot")
expect(js).to_contain("dataset.qualityViewport")
expect(js).to_contain("dataset.viewportMetric")
expect(js).to_contain("_makeQualityComputedSpatialPreview")
expect(js).to_contain("_makeQualityComputedSpatialMetric")
expect(js).to_contain("_qualitySpatialSnapshot")
expect(js).to_contain("dataset.qualityComputedSpatial")
expect(js).to_contain("dataset.computedSpatialMetric")
expect(js).to_contain("_normalizeThreeMode")
expect(js).to_contain("_readWindowTransitionPreference")
expect(js).to_contain("Math.hypot")
expect(js).to_contain("dockRect.top")
expect(js).to_contain("_makeQualityComputedAccessibilityPreview")
expect(js).to_contain("_makeQualityComputedAccessibilityMetric")
expect(js).to_contain("_qualityAccessibilitySnapshot")
expect(js).to_contain("dataset.qualityComputedAccessibility")
expect(js).to_contain("dataset.computedAccessibilityMetric")
expect(js).to_contain("document.styleSheets")
expect(js).to_contain("cssRules")
expect(js).to_contain("querySelectorAll('button, input, [role=\"button\"], [role=\"menuitem\"], [role=\"option\"]')")
expect(js).to_contain("aria-labelledby")
expect(js).to_contain("window.matchMedia")
expect(js).to_contain("_makeQualityComputedStatusPreview")
expect(js).to_contain("_makeQualityComputedStatusMetric")
expect(js).to_contain("_qualityStatusSnapshot")
expect(js).to_contain("dataset.qualityComputedStatus")
expect(js).to_contain("dataset.computedStatusMetric")
expect(js).to_contain(".wm-system-hud")
expect(js).to_contain(".wm-privacy-indicator")
expect(js).to_contain(".wm-notification-card.unread")
expect(js).to_contain(".wm-live-activity")
expect(js).to_contain("data-privacy-service")
expect(js).to_contain("data-live-progress")
expect(js).to_contain("_makeQualityComputedProductivityPreview")
expect(js).to_contain("_makeQualityComputedProductivityMetric")
expect(js).to_contain("_qualityProductivitySnapshot")
expect(js).to_contain("dataset.qualityComputedProductivity")
expect(js).to_contain("dataset.computedProductivityMetric")
expect(js).to_contain("_ensureClipboardHistory()")
expect(js).to_contain("_renderClipboardHistory()")
expect(js).to_contain("querySelectorAll('.wm-clipboard-item')")
expect(js).to_contain("querySelectorAll('[data-clipboard-filter]')")
expect(js).to_contain("_ensureQuickSettings()")
expect(js).to_contain("_renderQuickSettings()")
expect(js).to_contain("querySelectorAll('.wm-quick-setting')")
expect(js).to_contain("querySelectorAll('.wm-quick-slider')")
expect(js).to_contain("_ensureNotificationCenter()")
expect(js).to_contain("_renderNotificationCenter()")
expect(js).to_contain("querySelectorAll('.wm-notification-card')")
expect(js).to_contain("querySelectorAll('[data-notification-action]')")
expect(js).to_contain("_ensureScreenCapture()")
expect(js).to_contain("_renderScreenCapture()")
expect(js).to_contain("querySelectorAll('[data-capture-mode]')")
expect(js).to_contain("querySelectorAll('[data-capture-action]')")
expect(js).to_contain("window.innerWidth")
expect(js).to_contain("root.scrollWidth")
expect(js).to_contain("getBoundingClientRect()")
expect(js).to_contain("overflowX")
expect(js).to_contain("overflowY")
expect(js).to_contain("_qualityCssPx")
expect(js).to_contain("_qualityContrastRatio")
expect(js).to_contain("_qualityLuminance")
expect(js).to_contain("--ui-layout-safe-area-px")
expect(js).to_contain("--ui-layout-panel-gap-px")
expect(js).to_contain("--ui-layout-min-touch-target-px")
expect(js).to_contain("Open quality inspector")
expect(js).to_contain("Cmd Shift I")
expect(js).to_contain("wantsQualityInspector")
expect(js).to_contain("WM_ACCENT_STORAGE_KEY")
expect(js).to_contain("simple.wm.accent")
expect(js).to_contain("simpleWmSetAccent")
expect(js).to_contain("setAccentPreference")
expect(js).to_contain("_applyAccentPreference")
expect(js).to_contain("_normalizeAccentPreference")
expect(js).to_contain("_ensureAccentPalette")
expect(js).to_contain("_toggleAccentPalette")
expect(js).to_contain("_renderAccentPalette")
expect(js).to_contain("_accentChoices")
expect(js).to_contain("_makeAccentChoice")
expect(js).to_contain("_selectAccentChoice")
expect(js).to_contain("_setAccentFromControlCenter")
expect(js).to_contain("Set accent: blue")
expect(js).to_contain("Set accent: green")
expect(js).to_contain("Set accent: rose")
expect(js).to_contain("Set accent: amber")
expect(js).to_contain("Set accent: violet")
expect(js).to_contain("Open accent palette")
expect(js).to_contain("Cmd Shift C")
expect(js).to_contain("wantsAccentPalette")
expect(js).to_contain("accent_pick")
expect(js).to_contain("_ensureWallpaperPicker")
expect(js).to_contain("_toggleWallpaperPicker")
expect(js).to_contain("_renderWallpaperPicker")
expect(js).to_contain("_wallpaperChoices")
expect(js).to_contain("_makeWallpaperChoice")
expect(js).to_contain("_selectWallpaperChoice")
expect(js).to_contain("Open wallpaper picker")
expect(js).to_contain("Cmd Shift B")
expect(js).to_contain("wantsWallpaperPicker")
expect(js).to_contain("wallpaper_pick")
expect(js).to_contain("_ensureSystemHud")
expect(js).to_contain("_showSystemHud")
expect(js).to_contain("_systemHudTimer")
expect(js).to_contain("Show system HUD")
expect(js).to_contain("Cmd Shift H")
expect(js).to_contain("wantsSystemHud")
expect(js).to_contain("_ensurePrivacyIndicator")
expect(js).to_contain("_togglePrivacyIndicator")
expect(js).to_contain("_renderPrivacyIndicator")
expect(js).to_contain("_makePrivacyRow")
expect(js).to_contain("_browserPrivacyState")
expect(js).to_contain("Show privacy status")
expect(js).to_contain("Cmd Shift P")
expect(js).to_contain("wantsPrivacyIndicator")
expect(js).to_contain("Open widget gallery")
expect(js).to_contain("_ensureAppLauncher")
expect(js).to_contain("_toggleAppLauncher")
expect(js).to_contain("_renderAppLauncher")
expect(js).to_contain("_executeAppLauncherSelection")
expect(js).to_contain("_appLauncherCategory")
expect(js).to_contain("_appLauncherCategories")
expect(js).to_contain("_renderAppLauncherFilters")
expect(js).to_contain("_makeAppLauncherCategoryButton")
expect(js).to_contain("_setAppLauncherCategory")
expect(js).to_contain("_appLauncherCategoryForApp")
expect(js).to_contain("dataset.appCategory")
expect(js).to_contain("Cmd Space")
expect(js).to_contain("e.key === ' '")
expect(js).to_contain("_ensureShortcutOverlay")
expect(js).to_contain("_toggleShortcutOverlay")
expect(js).to_contain("_shortcutOverlayItems")
expect(js).to_contain("_filteredShortcutOverlayItems")
expect(js).to_contain("_renderShortcutOverlay")
expect(js).to_contain("_makeShortcutOverlayRow")
expect(js).to_contain("_shortcutSearchQuery")
expect(js).to_contain("Show keyboard shortcuts")
expect(js).to_contain("Search shortcuts")
expect(js).to_contain("No matching shortcuts")
expect(js).to_contain("dataset.shortcutCategory")
expect(js).to_contain("_ensureWmTooltip")
expect(js).to_contain("_tooltipAnchorFromEvent")
expect(js).to_contain("_tooltipTextForAnchor")
expect(js).to_contain("_showWmTooltipForEvent")
expect(js).to_contain("_positionWmTooltipForEvent")
expect(js).to_contain("_hideWmTooltipForEvent")
expect(js).to_contain("_hideWmTooltip")
expect(js).to_contain("_wmTooltipAnchor")
expect(js).to_contain("data-wm-tooltip")
expect(js).to_contain("Cmd /")
expect(js).to_contain("e.key === '/'")
expect(js).to_contain("e.key.toLowerCase() === 'k'")
expect(js).to_contain("role', 'listbox'")
expect(js).to_contain("role', 'option'")
expect(js).to_contain("aria-selected")
expect(js).to_contain("Window taskbar")
expect(js).to_contain("_taskbarBadgeCount")
expect(js).to_contain("_taskbarNeedsAttention")
expect(js).to_contain("_decorateTaskbarAttention")
expect(js).to_contain("_appendTaskbarBadge")
expect(js).to_contain("badge_count")
expect(js).to_contain("unread_count")
expect(js).to_contain("needs_attention")
expect(js).to_contain("notification_count")
expect(js).to_contain("_makeNotificationAction")
expect(js).to_contain("_openNotification")
expect(js).to_contain("_snoozeNotification")
expect(js).to_contain("notification_open")
expect(js).to_contain("notification_snooze")
expect(js).to_contain("minutes: 60")
expect(js).to_contain("_notificationFilter")
expect(js).to_contain("_notificationFilters")
expect(js).to_contain("_makeNotificationFilterButton")
expect(js).to_contain("_setNotificationFilter")
expect(js).to_contain("_filteredNotificationItems")
expect(js).to_contain("dataset.notificationFilter")
expect(js).to_contain("a.display_name || a.app_id")
expect(js).to_contain("w.minimized ? 'Restore' : 'Focus'")
expect(js).to_contain("w.title || w.window_id")
expect(js).to_contain("ev.key !== 'Enter' && ev.key !== ' '")
expect(js).to_contain("Close window")
expect(js).to_contain("Minimize window")
expect(js).to_contain("Maximize window")
expect(js).to_contain("_titleCommandModes")
expect(js).to_contain("_makeTitleCommandModeChips")
expect(js).to_contain("_titleCommandProfiles")
expect(js).to_contain("_makeTitleCommandProfileChips")
expect(js).to_contain("_applyTitleCommandProfile")
expect(js).to_contain("_titleCommandSuggestions")
expect(js).to_contain("_makeQualityComputedCommandPreview")
expect(js).to_contain("_makeQualityComputedCommandMetric")
expect(js).to_contain("dataset.qualityComputedCommand")
expect(js).to_contain("dataset.computedCommandMetric")
expect(js).to_contain("_titleCommandKind(value)")
expect(js).to_contain("_moveTitleCommandSuggestionSelection")
expect(js).to_contain("_executeTitleCommandSuggestion")
expect(js).to_contain("dataset.commandProfile")
expect(js).to_contain("VS Code-like file command")
expect(js).to_contain("browser URL command")
expect(js).to_contain("ArrowDown")
expect(js).to_contain("ArrowUp")
expect(js).to_contain("_makeRoundIcon")
expect(js).to_contain("_isImageIcon")
expect(js).to_contain("square-to-round")
expect(js).to_contain("glyph-to-round")
expect(js).to_contain("_makeTitleInput")
expect(js).to_contain("_markWindowLifecycle")
expect(js).to_contain("_ensureSnapPreview")
expect(js).to_contain("_detectSnapZone")
expect(js).to_contain("_snapRectForZone")
expect(js).to_contain("_applySnapPreview")
expect(js).to_contain("_clearSnapPreview")
expect(js).to_contain("_setDragFeedback")
expect(js).to_contain("_clearDragFeedback")
expect(js).to_contain("WM_SNAP_COMMIT_MS")
expect(js).to_contain("_windowElementById")
expect(js).to_contain("_markSnapCommitted")
expect(js).to_contain("dataset.wmDragState")
expect(js).to_contain("dataset.wmSnapTarget")
expect(js).to_contain("drag-lifted")
expect(js).to_contain("dataset.snapCommitZone")
expect(js).to_contain("snap-committed")
expect(js).to_contain("_wmSnapCommitTimer")
expect(js).to_contain("dataset.snapZone = snapZone || 'free'")
expect(js).to_contain("_ensureSnapLayoutPalette")
expect(js).to_contain("_toggleSnapLayoutPalette")
expect(js).to_contain("_renderSnapLayoutPalette")
expect(js).to_contain("_snapLayoutItems")
expect(js).to_contain("_applySnapLayoutChoice")
expect(js).to_contain("wm-snap-layout-palette")
expect(js).to_contain("snap_zone")
expect(js).to_contain("_ensureWindowArrangePalette")
expect(js).to_contain("_toggleWindowArrangePalette")
expect(js).to_contain("_renderWindowArrangePalette")
expect(js).to_contain("_arrangeModes")
expect(js).to_contain("_makeArrangeModeButton")
expect(js).to_contain("_arrangeRectForIndex")
expect(js).to_contain("_applyWindowArrangement")
expect(js).to_contain("Arrange windows")
expect(js).to_contain("Cmd Shift L")
expect(js).to_contain("wantsWindowArrange")
expect(js).to_contain("arrange_windows")
expect(js).to_contain("arrange_mode")
expect(js).to_contain("_toggleDesktopPeek")
expect(js).to_contain("_desktopPeekActive")
expect(js).to_contain("_desktopPeekWindowIds")
expect(js).to_contain("Peek desktop")
expect(js).to_contain("Cmd Shift M")
expect(js).to_contain("wantsDesktopPeek")
expect(js).to_contain("desktop_peek")
expect(js).to_contain("_ensureDesktopWidgets")
expect(js).to_contain("_makeDesktopWidget")
expect(js).to_contain("_makeDesktopWidgetControls")
expect(js).to_contain("_makeWidgetControlButton")
expect(js).to_contain("_pinDesktopWidget")
expect(js).to_contain("_removeDesktopWidget")
expect(js).to_contain("widget_pin")
expect(js).to_contain("widget_remove")
expect(js).to_contain("_toggleDesktopWidgets")
expect(js).to_contain("Toggle desktop widgets")
expect(js).to_contain("wm-desktop-widget")
expect(js).to_contain("_ensureDesktopItems")
expect(js).to_contain("_makeDesktopItem")
expect(js).to_contain("_makeDesktopDropTarget")
expect(js).to_contain("_toggleDesktopItems")
expect(js).to_contain("Toggle desktop items")
expect(js).to_contain("Cmd Shift E")
expect(js).to_contain("wm-desktop-item")
expect(js).to_contain("_ensureWindowOverview")
expect(js).to_contain("_toggleWindowOverview")
expect(js).to_contain("_renderWindowOverview")
expect(js).to_contain("_overviewWindows")
expect(js).to_contain("_filteredOverviewWindows")
expect(js).to_contain("_overviewSearchQuery")
expect(js).to_contain("_makeOverviewCard")
expect(js).to_contain("_focusWindowById")
expect(js).to_contain("Show window overview")
expect(js).to_contain("_ensureStageRail")
expect(js).to_contain("_toggleStageRail")
expect(js).to_contain("_renderStageRail")
expect(js).to_contain("_makeStageRailItem")
expect(js).to_contain("_makeStageRailAction")
expect(js).to_contain("_activateStageRailAction")
expect(js).to_contain("_stageRailActiveIndex")
expect(js).to_contain("_moveStageRailSelection")
expect(js).to_contain("_syncStageRailSelection")
expect(js).to_contain("_activateStageRailSelection")
expect(js).to_contain("aria-activedescendant")
expect(js).to_contain("aria-selected")
expect(js).to_contain("event.key === 'ArrowDown'")
expect(js).to_contain("event.key === 'ArrowRight'")
expect(js).to_contain("event.key === 'Enter'")
expect(js).to_contain("event.key === ' '")
expect(js).to_contain("event.key === 'Escape'")
expect(js).to_contain("dataset.stageAction")
expect(js).to_contain("Show stage rail")
expect(js).to_contain("Cmd Shift O")
expect(js).to_contain("wantsStageRail")
expect(js).to_contain("_ensureWindowSwitcher")
expect(js).to_contain("_toggleWindowSwitcher")
expect(js).to_contain("_renderWindowSwitcher")
expect(js).to_contain("_windowSwitcherWindows")
expect(js).to_contain("_makeWindowSwitcherCard")
expect(js).to_contain("_handleWindowSwitcherKeydown")
expect(js).to_contain("_moveWindowSwitcherSelection")
expect(js).to_contain("_focusWindowSwitcherActiveCard")
expect(js).to_contain("_activateWindowSwitcherSelection")
expect(js).to_contain("aria-activedescendant")
expect(js).to_contain("ArrowRight")
expect(js).to_contain("ArrowLeft")
expect(js).to_contain("ArrowDown")
expect(js).to_contain("ArrowUp")
expect(js).to_contain("event.key === 'Tab'")
expect(js).to_contain("event.shiftKey ? -1 : 1")
expect(js).to_contain("event.key === 'Escape'")
expect(js).to_contain("Show window switcher")
expect(js).to_contain("Cmd Tab")
expect(js).to_contain("wantsWindowSwitcher")
expect(js).to_contain("_ensureWorkspaceSwitcher")
expect(js).to_contain("_toggleWorkspaceSwitcher")
expect(js).to_contain("_renderWorkspaceSwitcher")
expect(js).to_contain("_workspaceItems")
expect(js).to_contain("_makeWorkspaceCard")
expect(js).to_contain("_workspaceSwitcherActiveIndex")
expect(js).to_contain("_moveWorkspaceSwitcherSelection")
expect(js).to_contain("_syncWorkspaceSwitcherSelection")
expect(js).to_contain("_activateWorkspaceSwitcherSelection")
expect(js).to_contain("aria-activedescendant")
expect(js).to_contain("aria-selected")
expect(js).to_contain("event.key === 'ArrowRight'")
expect(js).to_contain("event.key === 'ArrowDown'")
expect(js).to_contain("event.key === 'Enter'")
expect(js).to_contain("event.key === ' '")
expect(js).to_contain("event.key === 'Escape'")
expect(js).to_contain("_makeWorkspaceAction")
expect(js).to_contain("_activateWorkspaceAction")
expect(js).to_contain("_renameWorkspace")
expect(js).to_contain("_duplicateWorkspace")
expect(js).to_contain("_closeWorkspace")
expect(js).to_contain("workspace_rename")
expect(js).to_contain("workspace_duplicate")
expect(js).to_contain("workspace_close")
expect(js).to_contain("_switchWorkspace")
expect(js).to_contain("Switch workspace")
expect(js).to_contain("Cmd Shift W")
expect(js).to_contain("wantsWorkspaceSwitcher")
expect(js).to_contain("switch_workspace")
expect(js).to_contain("workspace_id")
expect(js).to_contain("_ensureClipboardHistory")
expect(js).to_contain("_toggleClipboardHistory")
expect(js).to_contain("_renderClipboardHistory")
expect(js).to_contain("_clipboardItems")
expect(js).to_contain("_clipboardKindFilters")
expect(js).to_contain("_filteredClipboardItems")
expect(js).to_contain("_makeClipboardFilterButton")
expect(js).to_contain("_setClipboardKindFilter")
expect(js).to_contain("_clipboardSearchQuery")
expect(js).to_contain("_clipboardKindFilter")
expect(js).to_contain("_makeClipboardItem")
expect(js).to_contain("_pasteClipboardItem")
expect(js).to_contain("_pinClipboardItem")
expect(js).to_contain("_clearClipboardHistory")
expect(js).to_contain("Show clipboard history")
expect(js).to_contain("Cmd Shift V")
expect(js).to_contain("wantsClipboardHistory")
expect(js).to_contain("clipboard_paste")
expect(js).to_contain("clipboard_pin")
expect(js).to_contain("clipboard_clear")
expect(js).to_contain("_ensureScreenCapture")
expect(js).to_contain("_toggleScreenCapture")
expect(js).to_contain("_renderScreenCapture")
expect(js).to_contain("_screenCaptureModes")
expect(js).to_contain("_makeCaptureModeButton")
expect(js).to_contain("_makeCaptureActionButton")
expect(js).to_contain("_setScreenCaptureMode")
expect(js).to_contain("_captureScreenSelection")
expect(js).to_contain("Open screen capture")
expect(js).to_contain("Cmd Shift S")
expect(js).to_contain("wantsScreenCapture")
expect(js).to_contain("screen_capture")
expect(js).to_contain("capture_mode")
expect(js).to_contain("_ensureQuickSettings")
expect(js).to_contain("_toggleQuickSettings")
expect(js).to_contain("_renderQuickSettings")
expect(js).to_contain("_quickSettingItems")
expect(js).to_contain("_makeQuickSetting")
expect(js).to_contain("_quickSettingLevels")
expect(js).to_contain("_quickSliderItems")
expect(js).to_contain("_makeQuickSlider")
expect(js).to_contain("_setQuickSettingLevel")
expect(js).to_contain("_updateQuickSettingLevelDom")
expect(js).to_contain("aria-valuetext")
expect(js).to_contain("aria-live")
expect(js).to_contain("querySelectorAll(`.wm-quick-slider[data-setting=")
expect(js).to_contain("_activateQuickSetting")
expect(js).to_contain("Open quick settings")
expect(js).to_contain("Cmd Shift Q")
expect(js).to_contain("wantsQuickSettings")
expect(js).to_contain("quick_setting")
expect(js).to_contain("quick_setting_level")
expect(js).to_contain("_ensureTaskbarPreview")
expect(js).to_contain("_showTaskbarPreview")
expect(js).to_contain("_makeTaskbarPreviewAction")
expect(js).to_contain("_activateTaskbarPreviewAction")
expect(js).to_contain("_hideTaskbarPreviewSoon")
expect(js).to_contain("_makeQualityComputedWindowActionsPreview")
expect(js).to_contain("_makeQualityComputedWindowActionsMetric")
expect(js).to_contain("_qualityWindowActionsSnapshot")
expect(js).to_contain("dataset.qualityComputedWindowActions")
expect(js).to_contain("dataset.computedWindowActionsMetric")
expect(js).to_contain("_renderSnapLayoutPalette(active.el")
expect(js).to_contain("querySelectorAll('[data-snap-layout]')")
expect(js).to_contain("querySelectorAll('[data-snap-preview]')")
expect(js).to_contain("_renderWindowArrangePalette()")
expect(js).to_contain("querySelectorAll('[data-arrange-mode]')")
expect(js).to_contain("querySelectorAll('[data-arrange-preview]')")
expect(js).to_contain("querySelectorAll('[data-preview-action]')")
expect(js).to_contain("querySelectorAll('.wm-traffic-lights button')")
expect(js).to_contain("querySelectorAll('.wm-title-input')")
expect(js).to_contain("taskbar_preview_action")
expect(js).to_contain("window_id")
expect(js).to_contain("_ensureLiveActivity")
expect(js).to_contain("_toggleLiveActivity")
expect(js).to_contain("_renderLiveActivity")
expect(js).to_contain("dataset.liveState")
expect(js).to_contain("role', 'progressbar'")
expect(js).to_contain("aria-valuenow")
expect(js).to_contain("aria-valuetext")
expect(js).to_contain("Show live activity")
expect(js).to_contain("Cmd Shift A")
expect(js).to_contain("wantsLiveActivity")
expect(js).to_contain("live_activity_open")
expect(js).to_contain("_makeLiveActivityAction")
expect(js).to_contain("_activateLiveActivityAction")
expect(js).to_contain("aria-pressed")
expect(js).to_contain("live_activity_pause")
expect(js).to_contain("live_activity_resume")
expect(js).to_contain("live_activity_cancel")
expect(js).to_contain("live_activity_dismiss")
expect(js).to_contain("wm-window-overview")
expect(js).to_contain("wm-overview-card")
expect(js).to_contain("_ensureControlCenter")
expect(js).to_contain("_toggleControlCenter")
expect(js).to_contain("_renderControlCenter")
expect(js).to_contain("_makeControlCenterButton")
expect(js).to_contain("_setMotionFromControlCenter")
expect(js).to_contain("_setTransparencyFromControlCenter")
expect(js).to_contain("_setWallpaperFromControlCenter")
expect(js).to_contain("Open control center")
expect(js).to_contain("Material transparency")
expect(js).to_contain("Standard glass")
expect(js).to_contain("Reduced glass")
expect(js).to_contain("Solid surfaces")
expect(js).to_contain("Animated wallpaper")
expect(js).to_contain("WM_BACKDROP_INTENSITY_STORAGE_KEY")
expect(js).to_contain("simple.wm.backdrop_intensity")
expect(js).to_contain("simpleWmSetBackdropIntensity")
expect(js).to_contain("setBackdropIntensityPreference")
expect(js).to_contain("_applyBackdropIntensityPreference")
expect(js).to_contain("_readBackdropIntensityPreference")
expect(js).to_contain("_backdropIntensityLabel")
expect(js).to_contain("Set backdrop intensity: vivid")
expect(js).to_contain("Set backdrop intensity: balanced")
expect(js).to_contain("Set backdrop intensity: quiet")
expect(js).to_contain("Backdrop intensity")
expect(js).to_contain("Vivid backdrop")
expect(js).to_contain("Balanced backdrop")
expect(js).to_contain("Quiet backdrop")
expect(js).to_contain("transparency_pick")
expect(js).to_contain("transparency_id")
expect(js).to_contain("wallpaper_pick")
expect(js).to_contain("wallpaper_id")
expect(js).to_contain("wm-control-center")
expect(js).to_contain("wm-control-button")
expect(js).to_contain("wm-titlebar-icon")
expect(js).to_contain("wm-command-bar")
expect(js).to_contain("wm-btn-")
expect(retained).to_contain("_makeSurfaceIcon")
expect(retained).to_contain("_makeRoundIcon")
expect(retained).to_contain("_isImageIcon")
expect(retained).to_contain("square-to-round")
expect(retained).to_contain("_makeTitleInput")
expect(retained).to_contain("_markLifecycle")
expect(retained).to_contain("wm-command-bar")
expect(retained).to_contain("'wm-resize-handle wm-resize-'")
expect(retained).to_contain("handle.dataset.direction = direction")
expect(retained).to_contain("closing")
expect(retained).to_contain("minimizing")
expect(retained).to_contain("Close window")
expect(retained).to_contain("Minimize window")
expect(retained).to_contain("Maximize window")
```

</details>

#### generates a standalone modern WM visual preview fixture

<details>
<summary>Executable SPipe</summary>

Runnable source: 854 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
val preview = generate_wm_modern_preview_html("glass_dark")
expect(preview).to_contain("data-preview=\"modern-wm\"")
expect(preview).to_contain("data-wm-motion=\"standard\"")
expect(preview).to_contain("wm-window focused opening")
expect(preview).to_contain("wm-window restoring dragging")
expect(preview).to_contain("wm-window restoring dragging snap-committed")
expect(preview).to_contain("data-snap-commit-zone=\"right\"")
expect(preview).to_contain("data-wm-drag-state=\"active\"")
expect(preview).to_contain("data-wm-snap-target=\"right\"")
expect(preview).to_contain("wm-snap-preview active")
expect(preview).to_contain("data-snap-zone=\"right\"")
expect(preview).to_contain("aria-hidden=\"true\"")
expect(preview).to_contain("wm-ghost drag-lifted")
expect(preview).to_contain("data-drag-feedback=\"lifted\"")
expect(preview).to_contain("wm-resize-handle wm-resize-se")
expect(preview).to_contain("data-direction=\"se\"")
expect(preview).to_contain("wm-resize-handle wm-resize-n")
expect(preview).to_contain("role=\"dialog\" aria-label=\"Snap layout chooser\"")
expect(preview).to_contain("data-window-id-hint=\"simple.ide\"")
expect(preview).to_contain("data-snap-layout=\"left\"")
expect(preview).to_contain("data-snap-layout=\"right\"")
expect(preview).to_contain("data-snap-layout=\"full\"")
expect(preview).to_contain("Left half")
expect(preview).to_contain("Right half")
expect(preview).to_contain("Fullscreen")
expect(preview).to_contain("role=\"dialog\" aria-label=\"Window arrangement\"")
expect(preview).to_contain("data-arrange-source=\"workspace\"")
expect(preview).to_contain("data-arrange-mode=\"tile_grid\"")
expect(preview).to_contain("data-arrange-mode=\"cascade\"")
expect(preview).to_contain("data-arrange-mode=\"focus_left\"")
expect(preview).to_contain("data-arrange-mode=\"center_stage\"")
expect(preview).to_contain("data-arrange-preview=\"tile_grid\"")
expect(preview).to_contain("Arrange windows")
expect(preview).to_contain("Tile grid")
expect(preview).to_contain("Cascade")
expect(preview).to_contain("Focus left")
expect(preview).to_contain("Center stage")
expect(preview).to_contain("data-desktop-peek=\"false\"")
expect(preview).to_contain("Peek desktop")
expect(preview).to_contain("Cmd Shift M")
expect(preview).to_contain("data-hot-corner-action=\"desktop_peek\"")
expect(preview).to_contain("aria-label=\"Peek desktop\"")
expect(preview).to_contain("wm-desktop-widgets")
expect(preview).to_contain("role=\"menubar\" aria-label=\"Simple desktop menu bar\"")
expect(preview).to_contain("aria-label=\"Application menu\"")
expect(preview).to_contain("aria-label=\"System menu\"")
expect(preview).to_contain("data-menu-action=\"launcher\"")
expect(preview).to_contain("data-menu-action=\"command_palette\"")
expect(preview).to_contain("data-menu-action=\"quick_settings\"")
expect(preview).to_contain("Battery 96%")
expect(preview).to_contain("wm-widget-clock")
expect(preview).to_contain("wm-widget-system")
expect(preview).to_contain("wm-widget-workspace")
expect(preview).to_contain("aria-label=\"Desktop widgets\"")
expect(preview).to_contain("role=\"group\" aria-label=\"Widget controls\"")
expect(preview).to_contain("data-widget-control=\"pin\"")
expect(preview).to_contain("data-widget-control=\"remove\"")
expect(preview).to_contain("aria-label=\"Pin widget\"")
expect(preview).to_contain("aria-label=\"Remove widget\"")
expect(preview).to_contain("role=\"dialog\" aria-label=\"Dock stack Downloads\"")
expect(preview).to_contain("data-stack-layout=\"fan-grid\"")
expect(preview).to_contain("data-stack-mode-active=\"fan\"")
expect(preview).to_contain("role=\"listbox\" aria-label=\"Downloads stack items\"")
expect(preview).to_contain("aria-activedescendant=\"wm-dock-stack-item-0\"")
expect(preview).to_contain("wm-dock-stack-item active")
expect(preview).to_contain("aria-selected=\"true\"")
expect(preview).to_contain("data-stack-index=\"0\"")
expect(preview).to_contain("aria-label=\"Widget gallery\"")
expect(preview).to_contain("role=\"listbox\" aria-label=\"Available widgets\"")
expect(preview).to_contain("aria-activedescendant=\"wm-widget-gallery-card-0\"")
expect(preview).to_contain("wm-widget-gallery-card active")
expect(preview).to_contain("data-widget-kind=\"clock\"")
expect(preview).to_contain("data-widget-index=\"0\"")
expect(preview).to_contain("role=\"option\" aria-selected=\"true\"")
expect(preview).to_contain("tabindex=\"0\"")
expect(preview).to_contain("role=\"status\" aria-live=\"polite\"")
expect(preview).to_contain("Choose a widget to add")
expect(preview).to_contain("aria-label=\"Desktop items\"")
expect(preview).to_contain("data-desktop-item=\"report\"")
expect(preview).to_contain("data-desktop-item=\"screenshot\"")
expect(preview).to_contain("data-drop-target=\"desktop\"")
expect(preview).to_contain("aria-label=\"Drop files on desktop\"")
expect(preview).to_contain("Quality report")
expect(preview).to_contain("Screenshot")
expect(preview).to_contain("aria-label=\"Window overview\"")
expect(preview).to_contain("wm-window-overview")
expect(preview).to_contain("wm-overview-card active")
expect(preview).to_contain("wm-overview-card-title")
expect(preview).to_contain("wm-overview-card-meta")
expect(preview).to_contain("class=\"wm-overview-search\"")
expect(preview).to_contain("aria-label=\"Search window overview\"")
expect(preview).to_contain("No matching windows")
expect(preview).to_contain("Show window overview")
expect(preview).to_contain("aria-label=\"Window stage rail\"")
expect(preview).to_contain("aria-activedescendant=\"wm-stage-rail-item-0\"")
expect(preview).to_contain("data-stage-active-index=\"0\"")
expect(preview).to_contain("wm-stage-rail-item active")
expect(preview).to_contain("data-window-id-hint=\"simple.ide\"")
expect(preview).to_contain("data-stage-index=\"0\"")
expect(preview).to_contain("role=\"option\" aria-selected=\"true\"")
expect(preview).to_contain("tabindex=\"0\"")
expect(preview).to_contain("class=\"wm-stage-rail-focus\"")
expect(preview).to_contain("role=\"group\" aria-label=\"Simple IDE stage actions\"")
expect(preview).to_contain("data-stage-action=\"minimize\"")
expect(preview).to_contain("data-stage-action=\"close\"")
expect(preview).to_contain("Show stage rail")
expect(preview).to_contain("Cmd Shift O")
expect(preview).to_contain("aria-label=\"Window switcher\"")
expect(preview).to_contain("data-switcher-shortcut=\"Meta Tab\"")
expect(preview).to_contain("aria-activedescendant=\"wm-window-switcher-card-0\"")
expect(preview).to_contain("id=\"wm-window-switcher-card-0\"")
expect(preview).to_contain("wm-window-switcher-card active")
expect(preview).to_contain("data-switch-window=\"simple.ide\"")
expect(preview).to_contain("data-switch-window=\"simple.browser\"")
expect(preview).to_contain("data-switch-window=\"simple.terminal\"")
expect(preview).to_contain("aria-label=\"Workspace switcher\"")
expect(preview).to_contain("aria-activedescendant=\"wm-workspace-card-0\"")
expect(preview).to_contain("data-workspace-active-index=\"0\"")
expect(preview).to_contain("role=\"listbox\" aria-label=\"Available workspaces\"")
expect(preview).to_contain("wm-workspace-card active")
expect(preview).to_contain("data-workspace-id=\"main\"")
expect(preview).to_contain("data-workspace-index=\"0\"")
expect(preview).to_contain("role=\"option\" aria-selected=\"true\"")
expect(preview).to_contain("tabindex=\"0\"")
expect(preview).to_contain("data-workspace-id=\"focus\"")
expect(preview).to_contain("data-workspace-id=\"review\"")
expect(preview).to_contain("class=\"wm-workspace-switch\"")
expect(preview).to_contain("role=\"group\" aria-label=\"Main workspace actions\"")
expect(preview).to_contain("data-workspace-action=\"rename\"")
expect(preview).to_contain("data-workspace-action=\"duplicate\"")
expect(preview).to_contain("data-workspace-action=\"close\"")
expect(preview).to_contain("Switch workspace")
expect(preview).to_contain("Cmd Shift W")
expect(preview).to_contain("Workspace switcher")
expect(preview).to_contain("aria-label=\"Clipboard history\"")
expect(preview).to_contain("aria-label=\"Search clipboard history\"")
expect(preview).to_contain("aria-label=\"Clipboard filters\"")
expect(preview).to_contain("data-clipboard-filter=\"all\"")
expect(preview).to_contain("data-clipboard-filter=\"command\"")
expect(preview).to_contain("data-clipboard-filter=\"code\"")
expect(preview).to_contain("data-clipboard-filter=\"link\"")
expect(preview).to_contain("No matching clips")
expect(preview).to_contain("wm-clipboard-item active pinned")
expect(preview).to_contain("data-clipboard-kind=\"command\"")
expect(preview).to_contain("data-clipboard-kind=\"code\"")
expect(preview).to_contain("data-clipboard-kind=\"link\"")
expect(preview).to_contain("data-clipboard-action=\"paste\"")
expect(preview).to_contain("data-clipboard-action=\"pin\"")
expect(preview).to_contain("data-clipboard-action=\"clear\"")
expect(preview).to_contain("Show clipboard history")
expect(preview).to_contain("Cmd Shift V")
expect(preview).to_contain("Clipboard history")
expect(preview).to_contain("aria-label=\"Screen capture\"")
expect(preview).to_contain("aria-label=\"Capture selection preview\"")
expect(preview).to_contain("role=\"toolbar\" aria-label=\"Capture controls\"")
expect(preview).to_contain("data-capture-mode=\"selection\"")
expect(preview).to_contain("data-capture-mode=\"window\"")
expect(preview).to_contain("data-capture-mode=\"screen\"")
expect(preview).to_contain("data-capture-action=\"capture\"")
expect(preview).to_contain("Open screen capture")
expect(preview).to_contain("Cmd Shift S")
expect(preview).to_contain("Screen capture")
expect(preview).to_contain("aria-label=\"Quick settings\"")
expect(preview).to_contain("data-command-lane-source=\"right-icons\"")
expect(preview).to_contain("data-setting=\"wifi\"")
expect(preview).to_contain("data-setting=\"audio\"")
expect(preview).to_contain("data-setting=\"battery\"")
expect(preview).to_contain("data-setting=\"brightness\"")
expect(preview).to_contain("aria-label=\"Quick setting levels\"")
expect(preview).to_contain("class=\"wm-quick-slider\"")
expect(preview).to_contain("type=\"range\"")
expect(preview).to_contain("aria-label=\"Audio level\"")
expect(preview).to_contain("aria-label=\"Brightness level\"")
expect(preview).to_contain("aria-live=\"polite\"")
expect(preview).to_contain("aria-valuetext=\"42%\"")
expect(preview).to_contain("aria-valuetext=\"68%\"")
expect(preview).to_contain("data-wm-focus-mode=\"off\"")
expect(preview).to_contain("data-menu-action=\"focus_mode\"")
expect(preview).to_contain("aria-label=\"Focus mode\"")
expect(preview).to_contain("Work focus")
expect(preview).to_contain("Deep focus")
expect(preview).to_contain("data-setting=\"focus\"")
expect(preview).to_contain("data-focus-mode=\"work\"")
expect(preview).to_contain("wm-notification-card unread quiet")
expect(preview).to_contain("data-focus-filtered=\"true\"")
expect(preview).to_contain("class=\"wm-notification-actions\"")
expect(preview).to_contain("role=\"group\" aria-label=\"Build finished actions\"")
expect(preview).to_contain("data-notification-action=\"open\"")
expect(preview).to_contain("data-notification-action=\"snooze\"")
expect(preview).to_contain("aria-label=\"Notification filters\"")
expect(preview).to_contain("data-notification-filter=\"all\"")
expect(preview).to_contain("data-notification-filter=\"unread\"")
expect(preview).to_contain("data-notification-filter=\"quiet\"")
expect(preview).to_contain("No matching notifications")
expect(preview).to_contain("Open quick settings")
expect(preview).to_contain("Cmd Shift Q")
expect(preview).to_contain("Quick settings")
expect(preview).to_contain("aria-label=\"Live activity\"")
expect(preview).to_contain("data-live-state=\"running\"")
expect(preview).to_contain("role=\"progressbar\"")
expect(preview).to_contain("aria-label=\"Build progress\"")
expect(preview).to_contain("aria-valuenow=\"64\"")
expect(preview).to_contain("aria-valuetext=\"64%\"")
expect(preview).to_contain("role=\"group\" aria-label=\"Live activity controls\"")
expect(preview).to_contain("data-live-action=\"pause\"")
expect(preview).to_contain("data-live-action=\"cancel\"")
expect(preview).to_contain("aria-label=\"Live activity pause\"")
expect(preview).to_contain("aria-pressed=\"false\"")
expect(preview).to_contain("Pause")
expect(preview).to_contain("Cancel")
expect(preview).to_contain("aria-label=\"System status HUD\"")
expect(preview).to_contain("aria-live=\"polite\"")
expect(preview).to_contain("wm-system-hud-title")
expect(preview).to_contain("wm-system-hud-value")
expect(preview).to_contain("Show system HUD")
expect(preview).to_contain("Cmd Shift H")
expect(preview).to_contain("aria-label=\"Privacy status\"")
expect(preview).to_contain("data-privacy-service=\"camera\"")
expect(preview).to_contain("data-privacy-service=\"microphone\"")
expect(preview).to_contain("data-privacy-service=\"screen-capture\"")
expect(preview).to_contain("aria-label=\"Privacy controls\"")
expect(preview).to_contain("Show privacy status")
expect(preview).to_contain("Cmd Shift P")
expect(preview).to_contain("aria-label=\"WM control center\"")
expect(preview).to_contain("wm-control-center")
expect(preview).to_contain("wm-control-button active")
expect(preview).to_contain("wm-quality-details")
expect(preview).to_contain("data-quality-swatch=\"background\"")
expect(preview).to_contain("data-quality-swatch=\"text\"")
expect(preview).to_contain("data-quality-swatch=\"accent\"")
expect(preview).to_contain("data-quality-metric=\"safe_area\"")
expect(preview).to_contain("data-quality-metric=\"panel_gap\"")
expect(preview).to_contain("data-quality-metric=\"min_touch\"")
expect(preview).to_contain("Safe area")
expect(preview).to_contain("Panel gap")
expect(preview).to_contain("Min touch")
expect(preview).to_contain("wm-quality-audit-policy")
expect(preview).to_contain("data-quality-policy=\"contrast\"")
expect(preview).to_contain("data-quality-policy=\"layout\"")
expect(preview).to_contain("data-quality-policy=\"touch\"")
expect(preview).to_contain("data-quality-policy=\"bounds\"")
expect(preview).to_contain("WCAG contrast")
expect(preview).to_contain("&gt;= 4.5:1")
expect(preview).to_contain("Layout tokens")
expect(preview).to_contain("safe 16px / gap 12px")
expect(preview).to_contain("Panel bounds")
expect(preview).to_contain("&lt;= 680px")
expect(preview).to_contain("wm-quality-summary")
expect(preview).to_contain("data-quality-score=\"100\"")
expect(preview).to_contain("wm-quality-summary-metric")
expect(preview).to_contain("wm-quality-summary-value")
expect(preview).to_contain("Score")
expect(preview).to_contain("Checks")
expect(preview).to_contain("Contrast")
expect(preview).to_contain("role=\"group\" aria-label=\"Quality audit mode\"")
expect(preview).to_contain("wm-quality-mode active")
expect(preview).to_contain("data-quality-mode=\"full\"")
expect(preview).to_contain("data-quality-mode=\"compact\"")
expect(preview).to_contain("role=\"group\" aria-label=\"Quality filters\"")
expect(preview).to_contain("wm-quality-filter active")
expect(preview).to_contain("data-quality-filter=\"all\"")
expect(preview).to_contain("data-quality-filter=\"layout\"")
expect(preview).to_contain("data-quality-filter=\"visual\"")
expect(preview).to_contain("data-quality-filter=\"motion\"")
expect(preview).to_contain("data-quality-filter=\"material\"")
expect(preview).to_contain("wm-quality-row good active")
expect(preview).to_contain("data-quality-detail=\"color\"")
expect(preview).to_contain("wm-quality-check-title")
expect(preview).to_contain("wm-quality-check-category")
expect(preview).to_contain("wm-quality-check-metric")
expect(preview).to_contain("Measured")
expect(preview).to_contain("Threshold")
expect(preview).to_contain("Source")
expect(preview).to_contain("Fix")
expect(preview).to_contain("--ui-bg / --ui-text")
expect(preview).to_contain("Adjust theme contrast tokens")
expect(preview).to_contain("data-quality-fix=\"color\"")
expect(preview).to_contain("Apply fix")
expect(preview).to_contain("data-quality-motion=\"standard\"")
expect(preview).to_contain("data-motion-step=\"open\"")
expect(preview).to_contain("data-motion-step=\"close\"")
expect(preview).to_contain("data-motion-step=\"minimize\"")
expect(preview).to_contain("data-motion-step=\"easing\"")
expect(preview).to_contain("wm-quality-motion-value")
expect(preview).to_contain("wm-quality-motion-sample")
expect(preview).to_contain("aria-hidden=\"true\"")
expect(preview).to_contain("data-motion-policy=\"standard\"")
expect(preview).to_contain("data-motion-policy=\"reduced\"")
expect(preview).to_contain("data-motion-policy=\"off\"")
expect(preview).to_contain("Full animation")
expect(preview).to_contain("Short, no blur")
expect(preview).to_contain("No animation")
expect(preview).to_contain("data-quality-material=\"standard\"")
expect(preview).to_contain("data-material-metric=\"blur\"")
expect(preview).to_contain("data-material-metric=\"floor\"")
expect(preview).to_contain("data-material-metric=\"solid\"")
expect(preview).to_contain("data-material-metric=\"mode\"")
expect(preview).to_contain("wm-quality-material-value")
expect(preview).to_contain("data-quality-computed-material=\"standard\"")
expect(preview).to_contain("data-computed-material-metric=\"window\"")
expect(preview).to_contain("data-computed-material-metric=\"command\"")
expect(preview).to_contain("data-computed-material-metric=\"taskbar\"")
expect(preview).to_contain("data-computed-material-metric=\"widget\"")
expect(preview).to_contain("wm-quality-computed-material-sample")
expect(preview).to_contain("wm-quality-computed-material-value")
expect(preview).to_contain("blur(24px)")
expect(preview).to_contain("blur(32px)")
expect(preview).to_contain("blur(28px)")
expect(preview).to_contain("data-quality-contrast=\"4.5\"")
expect(preview).to_contain("data-color-metric=\"background\"")
expect(preview).to_contain("data-color-metric=\"text\"")
expect(preview).to_contain("data-color-metric=\"accent\"")
expect(preview).to_contain("data-color-metric=\"contrast\"")
expect(preview).to_contain("wm-quality-color-chip")
expect(preview).to_contain("data-quality-computed-color=\"live\"")
expect(preview).to_contain("data-computed-color-metric=\"window\"")
expect(preview).to_contain("data-computed-color-metric=\"command\"")
expect(preview).to_contain("data-computed-color-metric=\"taskbar\"")
expect(preview).to_contain("data-computed-color-metric=\"widget\"")
expect(preview).to_contain("wm-quality-computed-color-chip")
expect(preview).to_contain("wm-quality-computed-color-value")
expect(preview).to_contain("7.2:1")
expect(preview).to_contain("data-quality-layout=\"tokens\"")
expect(preview).to_contain("data-layout-metric=\"titlebar\"")
expect(preview).to_contain("data-layout-metric=\"title_input\"")
expect(preview).to_contain("data-layout-metric=\"safe_area\"")
expect(preview).to_contain("data-layout-metric=\"min_touch\"")
expect(preview).to_contain("wm-quality-layout-value")
expect(preview).to_contain("data-quality-computed-layout=\"live\"")
expect(preview).to_contain("data-computed-layout-metric=\"window\"")
expect(preview).to_contain("data-computed-layout-metric=\"titlebar\"")
expect(preview).to_contain("data-computed-layout-metric=\"taskbar\"")
expect(preview).to_contain("data-computed-layout-metric=\"widget\"")
expect(preview).to_contain("wm-quality-computed-layout-frame")
expect(preview).to_contain("wm-quality-computed-layout-value")
expect(preview).to_contain("640x420")
expect(preview).to_contain("320x56")
expect(preview).to_contain("data-quality-computed-density=\"comfortable\"")
expect(preview).to_contain("data-computed-density-metric=\"mode\"")
expect(preview).to_contain("data-computed-density-metric=\"safe\"")
expect(preview).to_contain("data-computed-density-metric=\"gap\"")
expect(preview).to_contain("data-computed-density-metric=\"touch\"")
expect(preview).to_contain("wm-quality-computed-density-frame")
expect(preview).to_contain("comfortable")
expect(preview).to_contain("12px")
expect(preview).to_contain("data-quality-computed-shape=\"round\"")
expect(preview).to_contain("data-computed-shape-metric=\"window\"")
expect(preview).to_contain("data-computed-shape-metric=\"taskbar\"")
expect(preview).to_contain("data-computed-shape-metric=\"widget\"")
expect(preview).to_contain("data-computed-shape-metric=\"scrollbar\"")
expect(preview).to_contain("wm-quality-computed-shape-sample")
expect(preview).to_contain("wm-quality-computed-shape-value")
expect(preview).to_contain("999px")
expect(preview).to_contain("thin round")
expect(preview).to_contain("data-quality-titlebar=\"command\"")
expect(preview).to_contain("data-titlebar-metric=\"traffic\"")
expect(preview).to_contain("data-titlebar-metric=\"icon\"")
expect(preview).to_contain("data-titlebar-metric=\"command\"")
expect(preview).to_contain("data-titlebar-metric=\"context\"")
expect(preview).to_contain("wm-quality-titlebar-glyph")
expect(preview).to_contain("data-quality-computed-titlebar=\"live\"")
expect(preview).to_contain("data-computed-titlebar-metric=\"traffic\"")
expect(preview).to_contain("data-computed-titlebar-metric=\"icon\"")
expect(preview).to_contain("data-computed-titlebar-metric=\"input\"")
expect(preview).to_contain("data-computed-titlebar-metric=\"context\"")
expect(preview).to_contain("wm-quality-computed-titlebar-glyph")
expect(preview).to_contain("wm-quality-computed-titlebar-value")
expect(preview).to_contain("3 left")
expect(preview).to_contain("browser mode")
expect(preview).to_contain("data-quality-computed-traffic=\"mac\"")
expect(preview).to_contain("data-computed-traffic-metric=\"side\"")
expect(preview).to_contain("data-computed-traffic-metric=\"order\"")
expect(preview).to_contain("data-computed-traffic-metric=\"color\"")
expect(preview).to_contain("data-computed-traffic-metric=\"hit\"")
expect(preview).to_contain("data-computed-traffic-metric=\"motion\"")
expect(preview).to_contain("wm-quality-computed-traffic-dots")
expect(preview).to_contain("close-minimize-maximize")
expect(preview).to_contain("red yellow green")
expect(preview).to_contain("120ms")
expect(preview).to_contain("data-quality-icon=\"round\"")
expect(preview).to_contain("data-icon-metric=\"mask\"")
expect(preview).to_contain("data-icon-metric=\"padding\"")
expect(preview).to_contain("data-icon-metric=\"fit\"")
expect(preview).to_contain("data-icon-metric=\"square\"")
expect(preview).to_contain("wm-quality-icon-glyph wm-round-icon")
expect(preview).to_contain("data-quality-computed-icon=\"live\"")
expect(preview).to_contain("data-computed-icon-metric=\"round\"")
expect(preview).to_contain("data-computed-icon-metric=\"image\"")
expect(preview).to_contain("data-computed-icon-metric=\"square\"")
expect(preview).to_contain("data-computed-icon-metric=\"glyph\"")
expect(preview).to_contain("wm-quality-computed-icon-glyph wm-round-icon")
expect(preview).to_contain("wm-quality-computed-icon-value")
expect(preview).to_contain("999px")
expect(preview).to_contain("clipped")
expect(preview).to_contain("glyph I")
expect(preview).to_contain("data-quality-computed-icon-kit=\"simple-os\"")
expect(preview).to_contain("data-icon-kit-id=\"simple.os\"")
expect(preview).to_contain("data-icon-kit-id=\"simple.wm\"")
expect(preview).to_contain("data-icon-kit-id=\"simple.ide\"")
expect(preview).to_contain("data-icon-kit-id=\"simple.browser\"")
expect(preview).to_contain("data-icon-kit-id=\"simple.square\"")
expect(preview).to_contain("wm-quality-computed-icon-kit-icon wm-round-icon")
expect(preview).to_contain("Simple OS")
expect(preview).to_contain("Square app")
expect(preview).to_contain("data-quality-computed-scrollbar=\"live\"")
expect(preview).to_contain("data-computed-scrollbar-metric=\"app\"")
expect(preview).to_contain("data-computed-scrollbar-metric=\"command\"")
expect(preview).to_contain("data-computed-scrollbar-metric=\"clipboard\"")
expect(preview).to_contain("data-computed-scrollbar-metric=\"notification\"")
expect(preview).to_contain("wm-quality-computed-scrollbar-rail")
expect(preview).to_contain("wm-quality-computed-scrollbar-value")
expect(preview).to_contain("Notify")
expect(preview).to_contain("data-quality-typography=\"tokens\"")
expect(preview).to_contain("data-typography-metric=\"title\"")
expect(preview).to_contain("data-typography-metric=\"input\"")
expect(preview).to_contain("data-typography-metric=\"label\"")
expect(preview).to_contain("data-typography-metric=\"overflow\"")
expect(preview).to_contain("wm-quality-typography-sample")
expect(preview).to_contain("data-quality-depth=\"elevation\"")
expect(preview).to_contain("data-depth-metric=\"window\"")
expect(preview).to_contain("data-depth-metric=\"taskbar\"")
expect(preview).to_contain("data-depth-metric=\"blur\"")
expect(preview).to_contain("data-depth-metric=\"shadow\"")
expect(preview).to_contain("wm-quality-depth-plane")
expect(preview).to_contain("data-quality-interaction=\"affordance\"")
expect(preview).to_contain("data-interaction-metric=\"focus\"")
expect(preview).to_contain("data-interaction-metric=\"hover\"")
expect(preview).to_contain("data-interaction-metric=\"pointer\"")
expect(preview).to_contain("data-interaction-metric=\"keys\"")
expect(preview).to_contain("wm-quality-interaction-affordance")
expect(preview).to_contain("data-quality-computed-shortcut=\"live\"")
expect(preview).to_contain("data-computed-shortcut-metric=\"commands\"")
expect(preview).to_contain("data-computed-shortcut-metric=\"search\"")
expect(preview).to_contain("data-computed-shortcut-metric=\"modes\"")
expect(preview).to_contain("data-computed-shortcut-metric=\"context\"")
expect(preview).to_contain("wm-quality-computed-shortcut-glyph")
expect(preview).to_contain("16 commands")
expect(preview).to_contain("search ready")
expect(preview).to_contain("4 modes")
expect(preview).to_contain("4 groups")
expect(preview).to_contain("data-quality-computed-multitasking=\"live\"")
expect(preview).to_contain("data-computed-multitasking-metric=\"overview\"")
expect(preview).to_contain("data-computed-multitasking-metric=\"switcher\"")
expect(preview).to_contain("data-computed-multitasking-metric=\"stage\"")
expect(preview).to_contain("data-computed-multitasking-metric=\"peek\"")
expect(preview).to_contain("wm-quality-computed-multitasking-glyph")
expect(preview).to_contain("2 cards")
expect(preview).to_contain("3 cards")
expect(preview).to_contain("2 items")
expect(preview).to_contain("peek ready")
expect(preview).to_contain("data-quality-computed-workspace-controls=\"live\"")
expect(preview).to_contain("data-computed-workspace-controls-metric=\"workspace\"")
expect(preview).to_contain("data-computed-workspace-controls-metric=\"corners\"")
expect(preview).to_contain("data-computed-workspace-controls-metric=\"gestures\"")
expect(preview).to_contain("data-computed-workspace-controls-metric=\"focus\"")
expect(preview).to_contain("wm-quality-computed-workspace-controls-glyph")
expect(preview).to_contain("3 spaces")
expect(preview).to_contain("4 zones")
expect(preview).to_contain("4 hints")
expect(preview).to_contain("data-quality-computed-window-actions=\"live\"")
expect(preview).to_contain("data-computed-window-actions-metric=\"snap\"")
expect(preview).to_contain("data-computed-window-actions-metric=\"arrange\"")
expect(preview).to_contain("data-computed-window-actions-metric=\"taskbar\"")
expect(preview).to_contain("data-computed-window-actions-metric=\"chrome\"")
expect(preview).to_contain("wm-quality-computed-window-actions-glyph")
expect(preview).to_contain("3 layouts")
expect(preview).to_contain("4 modes")
expect(preview).to_contain("3 actions")
expect(preview).to_contain("3 traffic")
expect(preview).to_contain("data-quality-state=\"transitions\"")
expect(preview).to_contain("data-state-metric=\"hover\"")
expect(preview).to_contain("data-state-metric=\"focus\"")
expect(preview).to_contain("data-state-metric=\"active\"")
expect(preview).to_contain("data-state-metric=\"window\"")
expect(preview).to_contain("wm-quality-state-sample")
expect(preview).to_contain("data-quality-computed-lifecycle=\"mac\"")
expect(preview).to_contain("data-computed-lifecycle-metric=\"open\"")
expect(preview).to_contain("data-computed-lifecycle-metric=\"close\"")
expect(preview).to_contain("data-computed-lifecycle-metric=\"minimize\"")
expect(preview).to_contain("data-computed-lifecycle-metric=\"mode\"")
expect(preview).to_contain("wm-quality-computed-lifecycle-sample")
expect(preview).to_contain("wm-quality-computed-lifecycle-value")
expect(preview).to_contain("210ms")
expect(preview).to_contain("data-quality-verbosity=\"calm\"")
expect(preview).to_contain("data-verbosity-metric=\"audit\"")
expect(preview).to_contain("data-verbosity-metric=\"motion\"")
expect(preview).to_contain("data-verbosity-metric=\"material\"")
expect(preview).to_contain("data-verbosity-metric=\"focus\"")
expect(preview).to_contain("wm-quality-verbosity-indicator")
expect(preview).to_contain("data-quality-computed-personalization=\"live\"")
expect(preview).to_contain("data-computed-personalization-metric=\"motion\"")
expect(preview).to_contain("data-computed-personalization-metric=\"transparency\"")
expect(preview).to_contain("data-computed-personalization-metric=\"wallpaper\"")
expect(preview).to_contain("data-computed-personalization-metric=\"accent\"")
expect(preview).to_contain("wm-quality-computed-personalization-indicator")
expect(preview).to_contain("standard")
expect(preview).to_contain("aurora")
expect(preview).to_contain("blue")
expect(preview).to_contain("data-quality-computed-os-tools=\"live\"")
expect(preview).to_contain("data-computed-os-tools-metric=\"launcher\"")
expect(preview).to_contain("data-quality-computed-control-center=\"live\"")
expect(preview).to_contain("data-computed-control-center-metric=\"groups\"")
expect(preview).to_contain("data-computed-control-center-metric=\"policy\"")
expect(preview).to_contain("data-computed-control-center-metric=\"active\"")
expect(preview).to_contain("data-computed-control-center-metric=\"tools\"")
expect(preview).to_contain("wm-quality-computed-control-center-glyph")
expect(preview).to_contain("20 groups")
expect(preview).to_contain("12/12 policy")
expect(preview).to_contain("18 active")
expect(preview).to_contain("12 tools")
expect(preview).to_contain("data-computed-os-tools-metric=\"widgets\"")
expect(preview).to_contain("data-computed-os-tools-metric=\"style\"")
expect(preview).to_contain("data-computed-os-tools-metric=\"dock\"")
expect(preview).to_contain("wm-quality-computed-os-tools-glyph")
expect(preview).to_contain("4 apps")
expect(preview).to_contain("3 cards")
expect(preview).to_contain("3/5 choices")
expect(preview).to_contain("3 items")
expect(preview).to_contain("data-quality-performance=\"standard\"")
expect(preview).to_contain("data-performance-metric=\"frame\"")
expect(preview).to_contain("data-performance-metric=\"motion\"")
expect(preview).to_contain("data-performance-metric=\"opacity\"")
expect(preview).to_contain("data-performance-metric=\"energy\"")
expect(preview).to_contain("data-performance-metric=\"backdrop\"")
expect(preview).to_contain("data-performance-metric=\"fallback\"")
expect(preview).to_contain("Standard energy")
expect(preview).to_contain("Backdrop")
expect(preview).to_contain("wm-quality-performance-meter")
expect(preview).to_contain("data-quality-computed-backdrop=\"standard\"")
expect(preview).to_contain("data-computed-backdrop-metric=\"motion\"")
expect(preview).to_contain("data-computed-backdrop-metric=\"opacity\"")
expect(preview).to_contain("data-computed-backdrop-metric=\"drift\"")
expect(preview).to_contain("data-computed-backdrop-metric=\"energy\"")
expect(preview).to_contain("wm-quality-computed-backdrop-orb")
expect(preview).to_contain("wm-quality-computed-backdrop-value")
expect(preview).to_contain("1.03x")
expect(preview).to_contain("data-quality-computed-wallpaper=\"aurora\"")
expect(preview).to_contain("data-computed-wallpaper-metric=\"mode\"")
expect(preview).to_contain("data-computed-wallpaper-metric=\"choices\"")
expect(preview).to_contain("data-computed-wallpaper-metric=\"swatches\"")
expect(preview).to_contain("data-computed-wallpaper-metric=\"motion\"")
expect(preview).to_contain("data-computed-wallpaper-metric=\"duration\"")
expect(preview).to_contain("wm-quality-computed-wallpaper-orb")
expect(preview).to_contain("3 choices")
expect(preview).to_contain("3 live")
expect(preview).to_contain("ambient")
expect(preview).to_contain("24000ms")
expect(preview).to_contain("data-quality-computed-motion=\"standard\"")
expect(preview).to_contain("data-computed-motion-metric=\"window\"")
expect(preview).to_contain("data-computed-motion-metric=\"dock\"")
expect(preview).to_contain("data-computed-motion-metric=\"command\"")
expect(preview).to_contain("data-computed-motion-metric=\"backdrop\"")
expect(preview).to_contain("wm-quality-computed-motion-rail")
expect(preview).to_contain("260ms wm-window-open")
expect(preview).to_contain("24000ms wm-bg-drift")
expect(preview).to_contain("data-quality-computed-motion-budget=\"standard\"")
expect(preview).to_contain("data-computed-motion-budget-metric=\"active\"")
expect(preview).to_contain("data-computed-motion-budget-metric=\"longest\"")
expect(preview).to_contain("data-computed-motion-budget-metric=\"policy\"")
expect(preview).to_contain("data-computed-motion-budget-metric=\"fallback\"")
expect(preview).to_contain("wm-quality-computed-motion-budget-rail")
expect(preview).to_contain("standard motion")
expect(preview).to_contain("css tokens")
expect(preview).to_contain("data-quality-spatial=\"origin\"")
expect(preview).to_contain("data-spatial-metric=\"open\"")
expect(preview).to_contain("data-spatial-metric=\"close\"")
expect(preview).to_contain("data-spatial-metric=\"minimize\"")
expect(preview).to_contain("data-spatial-metric=\"restore\"")
expect(preview).to_contain("wm-quality-spatial-path")
expect(preview).to_contain("data-quality-computed-spatial=\"mac\"")
expect(preview).to_contain("data-computed-spatial-metric=\"origin\"")
expect(preview).to_contain("data-computed-spatial-metric=\"target\"")
expect(preview).to_contain("data-computed-spatial-metric=\"bounds\"")
expect(preview).to_contain("data-computed-spatial-metric=\"mode\"")
expect(preview).to_contain("wm-quality-computed-spatial-path")
expect(preview).to_contain("centered")
expect(preview).to_contain("dock return")
expect(preview).to_contain("in viewport")
expect(preview).to_contain("data-quality-responsive=\"adaptive\"")
expect(preview).to_contain("data-responsive-metric=\"desktop\"")
expect(preview).to_contain("data-responsive-metric=\"tablet\"")
expect(preview).to_contain("data-responsive-metric=\"mobile\"")
expect(preview).to_contain("data-responsive-metric=\"safe\"")
expect(preview).to_contain("wm-quality-responsive-frame")
expect(preview).to_contain("data-quality-viewport=\"live\"")
expect(preview).to_contain("data-viewport-metric=\"viewport\"")
expect(preview).to_contain("data-viewport-metric=\"desktop\"")
expect(preview).to_contain("data-viewport-metric=\"window\"")
expect(preview).to_contain("data-viewport-metric=\"overflow\"")
expect(preview).to_contain("wm-quality-viewport-frame")
expect(preview).to_contain("wm-quality-viewport-value")
expect(preview).to_contain("Viewport")
expect(preview).to_contain("Overflow")
expect(preview).to_contain("1440x900")
expect(preview).to_contain("data-quality-accessibility=\"operable\"")
expect(preview).to_contain("data-accessibility-metric=\"focus\"")
expect(preview).to_contain("data-accessibility-metric=\"labels\"")
expect(preview).to_contain("data-accessibility-metric=\"touch\"")
expect(preview).to_contain("data-accessibility-metric=\"reduce\"")
expect(preview).to_contain("wm-quality-accessibility-indicator")
expect(preview).to_contain("data-quality-computed-accessibility=\"live\"")
expect(preview).to_contain("data-computed-accessibility-metric=\"focus\"")
expect(preview).to_contain("data-computed-accessibility-metric=\"labels\"")
expect(preview).to_contain("data-computed-accessibility-metric=\"touch\"")
expect(preview).to_contain("data-computed-accessibility-metric=\"reduce\"")
expect(preview).to_contain("wm-quality-computed-accessibility-indicator")
expect(preview).to_contain("focus rule")
expect(preview).to_contain("42/44")
expect(preview).to_contain("media")
expect(preview).to_contain("data-quality-computed-status=\"live\"")
expect(preview).to_contain("data-computed-status-metric=\"hud\"")
expect(preview).to_contain("data-computed-status-metric=\"privacy\"")
expect(preview).to_contain("data-computed-status-metric=\"notify\"")
expect(preview).to_contain("data-computed-status-metric=\"activity\"")
expect(preview).to_contain("wm-quality-computed-status-indicator")
expect(preview).to_contain("3 services")
expect(preview).to_contain("1 unread")
expect(preview).to_contain("64%")
expect(preview).to_contain("data-quality-computed-productivity=\"live\"")
expect(preview).to_contain("data-computed-productivity-metric=\"clipboard\"")
expect(preview).to_contain("data-computed-productivity-metric=\"quick\"")
expect(preview).to_contain("data-computed-productivity-metric=\"notify\"")
expect(preview).to_contain("data-computed-productivity-metric=\"capture\"")
expect(preview).to_contain("wm-quality-computed-productivity-indicator")
expect(preview).to_contain("3 clips")
expect(preview).to_contain("5/2 controls")
expect(preview).to_contain("3 cards")
expect(preview).to_contain("3 modes")
expect(preview).to_contain("data-quality-animation=\"timeline\"")
expect(preview).to_contain("data-animation-track=\"open\"")
expect(preview).to_contain("data-animation-track=\"dock\"")
expect(preview).to_contain("data-animation-track=\"command\"")
expect(preview).to_contain("data-animation-track=\"background\"")
expect(preview).to_contain("wm-quality-animation-rail")
expect(preview).to_contain("data-quality-widget=\"desktop\"")
expect(preview).to_contain("data-widget-metric=\"stack\"")
expect(preview).to_contain("data-widget-metric=\"controls\"")
expect(preview).to_contain("data-widget-metric=\"shape\"")
expect(preview).to_contain("data-widget-metric=\"gallery\"")
expect(preview).to_contain("wm-quality-widget-glyph")
expect(preview).to_contain("data-quality-computed-widget=\"live\"")
expect(preview).to_contain("data-computed-widget-metric=\"count\"")
expect(preview).to_contain("data-computed-widget-metric=\"shape\"")
expect(preview).to_contain("data-computed-widget-metric=\"controls\"")
expect(preview).to_contain("data-computed-widget-metric=\"gallery\"")
expect(preview).to_contain("wm-quality-computed-widget-glyph")
expect(preview).to_contain("wm-quality-computed-widget-value")
expect(preview).to_contain("3 live")
expect(preview).to_contain("2 actions")
expect(preview).to_contain("data-quality-surface=\"transparency\"")
expect(preview).to_contain("data-surface-metric=\"window\"")
expect(preview).to_contain("data-surface-metric=\"command\"")
expect(preview).to_contain("data-surface-metric=\"taskbar\"")
expect(preview).to_contain("data-surface-metric=\"widget\"")
expect(preview).to_contain("wm-quality-surface-sample")
expect(preview).to_contain("data-quality-computed-surface=\"standard\"")
expect(preview).to_contain("data-computed-surface-metric=\"window\"")
expect(preview).to_contain("data-computed-surface-metric=\"command\"")
expect(preview).to_contain("data-computed-surface-metric=\"taskbar\"")
expect(preview).to_contain("data-computed-surface-metric=\"widget\"")
expect(preview).to_contain("wm-quality-computed-surface-sample")
expect(preview).to_contain("wm-quality-computed-surface-value")
expect(preview).to_contain("86% alpha")
expect(preview).to_contain("data-quality-recommendations=\"actionable\"")
expect(preview).to_contain("Recommended changes")
expect(preview).to_contain("wm-quality-recommendation wm-quality-fix")
expect(preview).to_contain("data-quality-fix=\"color\"")
expect(preview).to_contain("data-quality-fix=\"motion\"")
expect(preview).to_contain("data-quality-fix=\"material\"")
expect(preview).to_contain("Adjust theme contrast tokens")
expect(preview).to_contain("Use motion preference toggle")
expect(preview).to_contain("Use transparency preference toggle")
expect(preview).to_contain("role=\"group\" aria-label=\"Quality inspector actions\"")
expect(preview).to_contain("data-quality-action=\"motion\"")
expect(preview).to_contain("data-quality-action=\"material\"")
expect(preview).to_contain("data-quality-action=\"energy_low\"")
expect(preview).to_contain("data-quality-action=\"energy_critical\"")
expect(preview).to_contain("data-quality-action=\"layout\"")
expect(preview).to_contain("data-quality-action=\"contrast\"")
expect(preview).to_contain("data-quality-action=\"copy_report\"")
expect(preview).to_contain("data-quality-report=\"shareable\"")
expect(preview).to_contain("Reduce motion")
expect(preview).to_contain("Reduce glass")
expect(preview).to_contain("Low power")
expect(preview).to_contain("Critical saver")
expect(preview).to_contain("Open layout tools")
expect(preview).to_contain("Accent palette")
expect(preview).to_contain("Quality report")
expect(preview).to_contain("Simple WM quality report")
expect(preview).to_contain("Score: 100%")
expect(preview).to_contain("Checks: 7/7")
expect(preview).to_contain("Copy report")
expect(preview).to_contain("role=\"menu\" aria-label=\"Window context menu\"")
expect(preview).to_contain("wm-window-context-header")
expect(preview).to_contain("wm-window-context-icon wm-round-icon")
expect(preview).to_contain("wm-window-context-status")
expect(preview).to_contain("Active window")
expect(preview).to_contain("wm-window-context-glyph wm-round-icon")
expect(preview).to_contain("wm-window-context-label")
expect(preview).to_contain("wm-window-context-item active")
expect(preview).to_contain("data-context-action=\"focus\"")
expect(preview).to_contain("data-context-action=\"minimize\"")
expect(preview).to_contain("data-context-action=\"pin_stage\"")
expect(preview).to_contain("data-context-action=\"snap_left\"")
expect(preview).to_contain("data-context-action=\"snap_right\"")
expect(preview).to_contain("data-context-action=\"snap_full\"")
expect(preview).to_contain("data-context-action=\"move_workspace\"")
expect(preview).to_contain("data-context-workspace-id=\"main\"")
expect(preview).to_contain("data-context-workspace-id=\"focus\"")
expect(preview).to_contain("data-context-workspace-id=\"review\"")
expect(preview).to_contain("data-context-action=\"copy_title\"")
expect(preview).to_contain("data-context-action=\"close\"")
expect(preview).to_contain("Pin to Stage")
expect(preview).to_contain("Move to Main")
expect(preview).to_contain("Move to Focus")
expect(preview).to_contain("Move to Review")
expect(preview).to_contain("Copy title")
expect(preview).to_contain("role=\"listbox\" aria-label=\"Window command suggestions\"")
expect(preview).to_contain("wm-title-suggestion-item active")
expect(preview).to_contain("wm-title-suggestion-label")
expect(preview).to_contain("wm-title-suggestion-meta")
expect(preview).to_contain("role=\"group\" aria-label=\"Command modes\"")
expect(preview).to_contain("wm-title-command-mode active")
expect(preview).to_contain("data-command-kind=\"url\"")
expect(preview).to_contain("data-command-kind=\"search\"")
expect(preview).to_contain("data-command-kind=\"command\"")
expect(preview).to_contain("role=\"group\" aria-label=\"Command profiles\"")
expect(preview).to_contain("wm-title-command-profile active")
expect(preview).to_contain("data-command-profile=\"ide\"")
expect(preview).to_contain("data-command-profile=\"browser\"")
expect(preview).to_contain("VS Code-like file command")
expect(preview).to_contain("browser URL command")
expect(preview).to_contain("Search workspace for")
expect(preview).to_contain("command action")
expect(preview).to_contain("workspace search")
expect(preview).to_contain("data-quality-computed-command=\"titlebar\"")
expect(preview).to_contain("data-computed-command-metric=\"input\"")
expect(preview).to_contain("data-computed-command-metric=\"kind\"")
expect(preview).to_contain("data-computed-command-metric=\"modes\"")
expect(preview).to_contain("data-computed-command-metric=\"suggestions\"")
expect(preview).to_contain("data-computed-command-metric=\"payload\"")
expect(preview).to_contain("wm-quality-computed-command-glyph")
expect(preview).to_contain("4 modes")
expect(preview).to_contain("4 options")
expect(preview).to_contain("role=\"dialog\" aria-label=\"App launcher\"")
expect(preview).to_contain("wm-app-launcher-tile active")
expect(preview).to_contain("data-app-id=\"simple.ide\"")
expect(preview).to_contain("aria-label=\"App categories\"")
expect(preview).to_contain("data-app-category=\"all\"")
expect(preview).to_contain("data-app-category=\"work\"")
expect(preview).to_contain("data-app-category=\"system\"")
expect(preview).to_contain("data-app-category=\"tools\"")
expect(preview).to_contain("wm-app-launcher-name")
expect(preview).to_contain("wm-app-launcher-category")
expect(preview).to_contain("No matching apps")
expect(preview).to_contain("role=\"dialog\" aria-label=\"Keyboard shortcuts\"")
expect(preview).to_contain("aria-label=\"Search keyboard shortcuts\"")
expect(preview).to_contain("wm-shortcut-title")
expect(preview).to_contain("wm-shortcut-section")
expect(preview).to_contain("wm-shortcut-row")
expect(preview).to_contain("data-shortcut-category=\"Apps\"")
expect(preview).to_contain("data-shortcut-category=\"Windows\"")
expect(preview).to_contain("data-shortcut-category=\"System\"")
expect(preview).to_contain("Cmd L")
expect(preview).to_contain("No matching shortcuts")
expect(preview).to_contain("Standard motion")
expect(preview).to_contain("Reduced motion")
expect(preview).to_contain("Motion off")
expect(preview).to_contain("aria-label=\"Quiet mode\"")
expect(preview).to_contain("Quiet mode off")
expect(preview).to_contain("Quiet mode on")
expect(preview).to_contain("aria-label=\"Material transparency\"")
expect(preview).to_contain("aria-label=\"Readability contrast\"")
expect(preview).to_contain("Comfortable contrast")
expect(preview).to_contain("High contrast")
expect(preview).to_contain("aria-label=\"Feedback policy\"")
expect(preview).to_contain("Standard feedback")
expect(preview).to_contain("Subtle feedback")
expect(preview).to_contain("Feedback off")
expect(preview).to_contain("aria-label=\"Energy policy\"")
expect(preview).to_contain("Standard energy")
expect(preview).to_contain("Low power")
expect(preview).to_contain("Critical saver")
expect(preview).to_contain("Standard glass")
expect(preview).to_contain("Reduced glass")
expect(preview).to_contain("Solid surfaces")
expect(preview).to_contain("aria-label=\"Accent color\"")
expect(preview).to_contain("Blue accent")
expect(preview).to_contain("Green accent")
expect(preview).to_contain("Rose accent")
expect(preview).to_contain("Amber accent")
expect(preview).to_contain("Violet accent")
expect(preview).to_contain("aria-label=\"Animated wallpaper\"")
expect(preview).to_contain("Aurora")
expect(preview).to_contain("Mesh")
expect(preview).to_contain("aria-label=\"Backdrop intensity\"")
expect(preview).to_contain("Vivid backdrop")
expect(preview).to_contain("Balanced backdrop")
expect(preview).to_contain("Quiet backdrop")
expect(preview).to_contain("Open control center")
expect(preview).to_contain("wm-traffic-lights")
expect(preview).to_contain("aria-label=\"Taskbar preview actions\"")
expect(preview).to_contain("data-preview-action=\"focus\"")
expect(preview).to_contain("data-preview-action=\"minimize\"")
expect(preview).to_contain("data-preview-action=\"close\"")
expect(preview).to_contain("aria-label=\"Focus window\"")
expect(preview).to_contain("aria-label=\"Minimize window\"")
expect(preview).to_contain("role=\"tooltip\"")
expect(preview).to_contain("data-tooltip-source=\"taskbar\"")
expect(preview).to_contain("Focus Simple IDE")
expect(preview).to_contain("wm-title-input wm-command-bar")
expect(preview).to_contain("wm-command-palette")
expect(preview).to_contain("wm-command-palette-input")
expect(preview).to_contain("aria-label=\"Recent commands\"")
expect(preview).to_contain("wm-command-recent")
expect(preview).to_contain("wm-command-section")
expect(preview).to_contain("data-command-category=\"Apps\"")
expect(preview).to_contain("data-command-category=\"System\"")
expect(preview).to_contain("wm-command-item active")
expect(preview).to_contain("role=\"listbox\"")
expect(preview).to_contain("role=\"option\"")
expect(preview).to_contain("aria-selected=\"true\"")
expect(preview).to_contain("aria-label=\"Close window\"")
expect(preview).to_contain("aria-label=\"Minimize window\"")
expect(preview).to_contain("aria-label=\"Maximize window\"")
expect(preview).to_contain("role=\"navigation\" aria-label=\"Window taskbar\"")
expect(preview).to_contain("aria-label=\"Dock visibility\"")
expect(preview).to_contain("Dock shown")
expect(preview).to_contain("Dock auto-hide")
expect(preview).to_contain("Dock hidden")
expect(preview).to_contain("data-wm-dock-visibility=\"shown\"")
expect(preview).to_contain("data-dock-metric=\"visibility\"")
expect(preview).to_contain("Visibility")
expect(preview).to_contain("data-quality-computed-dock=\"live\"")
expect(preview).to_contain("data-computed-dock-metric=\"scale\"")
expect(preview).to_contain("data-computed-dock-metric=\"hit\"")
expect(preview).to_contain("data-computed-dock-metric=\"visibility\"")
expect(preview).to_contain("data-computed-dock-metric=\"stack\"")
expect(preview).to_contain("wm-quality-computed-dock-icon")
expect(preview).to_contain("wm-quality-computed-dock-value")
expect(preview).to_contain("44px")
expect(preview).to_contain("aria-label=\"Focus Simple IDE\"")
expect(preview).to_contain("Set motion: reduced")
expect(preview).to_contain("widget-menubar")
expect(preview).to_contain("widget-panel focused")
expect(preview).to_contain("widget-statusbar")
expect(preview).to_contain("wm-taskbar-item active")
expect(preview).to_contain("wm-taskbar-item attention")
expect(preview).to_contain("wm-taskbar-badge")
expect(preview).to_contain("data-taskbar-badge=\"3\"")
expect(preview).to_contain("data-taskbar-attention=\"true\"")
expect(preview).to_contain("aria-label=\"Focus Browser, 3 notifications\"")
expect(preview).to_contain("wm-icon-normalized-square")
expect(preview).to_contain("contrast_ratio_x100=450")
```

</details>

#### writes a browser-loadable modern WM preview evidence artifact

<details>
<summary>Executable SPipe</summary>

Runnable source: 16 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
val path = "build/simple_wm_modern_preview.html"
expect(write_wm_modern_preview_html(path, "glass_dark"))
val written = rt_file_read_text(path)
expect(written).to_contain("<!DOCTYPE html>")
expect(written).to_contain("Simple WM Modern Preview")
expect(written).to_contain("wm-command-palette")
expect(written).to_contain("wm-window focused opening")
expect(written).to_contain("wm-taskbar-item active")
val report = wm_modern_preview_evidence_report(path, "glass_dark")
expect(report).to_contain("wm_modern_preview")
expect(report).to_contain("path=build/simple_wm_modern_preview.html")
expect(report).to_contain("command_palette=true")
expect(report).to_contain("control_center=true")
expect(report).to_contain("window_overview=true")
expect(report).to_contain("normalized_icons=true")
expect(report).to_contain("desktop_widgets=true")
```

</details>

#### reports theme quality for color size layout motion and shell affordances

<details>
<summary>Executable SPipe</summary>

Runnable source: 251 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
val report = wm_theme_quality_report("glass_dark")
expect(report.passed)
expect(report.color_checked)
expect(report.contrast_ratio_x100).to_be_greater_than(449)
expect(report.contrast_ratio_x100).to_be_greater_than(1700)
expect(report.contrast_ratio_x100).to_be_less_than(1900)
expect(report.contrast_delta).to_be_greater_than(96)
expect(report.semantic_tokens)
expect(report.stable_layout)
expect(report.size_layout_configured)
expect(report.titlebar_height_px).to_equal(38)
expect(report.window_min_width_px).to_equal(200)
expect(report.window_min_height_px).to_equal(120)
expect(report.title_input_min_width_px).to_equal(140)
expect(report.taskbar_icon_size_px).to_equal(26)
expect(report.taskbar_attention_ready)
expect(report.system_hud_max_width_px).to_equal(280)
expect(report.privacy_indicator_max_width_px).to_equal(300)
expect(report.workspace_switcher_max_width_px).to_equal(420)
expect(report.clipboard_history_max_width_px).to_equal(360)
expect(report.screen_capture_max_width_px).to_equal(560)
expect(report.quick_settings_max_width_px).to_equal(300)
expect(report.notification_center_max_width_px).to_equal(340)
expect(report.live_activity_max_width_px).to_equal(420)
expect(report.hot_corner_max_size_px).to_equal(44)
expect(report.resize_hud_max_width_px).to_equal(240)
expect(report.gesture_hints_max_width_px).to_equal(360)
expect(report.command_palette_max_width_px).to_equal(680)
expect(report.quality_inspector_max_width_px).to_equal(420)
expect(report.control_center_max_width_px).to_equal(320)
expect(report.desktop_widget_max_width_px).to_equal(260)
expect(report.app_launcher_max_width_px).to_equal(520)
expect(report.wallpaper_picker_max_width_px).to_equal(380)
expect(report.accent_palette_max_width_px).to_equal(360)
expect(report.dock_stack_max_width_px).to_equal(340)
expect(report.stage_rail_max_width_px).to_equal(88)
expect(report.window_switcher_max_width_px).to_equal(460)
expect(report.top_menu_bar_height_px).to_equal(38)
expect(report.overview_card_min_width_px).to_equal(180)
expect(report.touch_target_min_height_px).to_equal(44)
expect(report.rounded_shapes)
expect(report.round_icon_masking)
expect(report.round_icon_converter)
expect(report.round_scrollbars)
expect(report.translucent_shell)
expect(report.lifecycle_motion)
expect(report.lifecycle_motion_profile_ready)
expect(report.widget_motion)
expect(report.reduced_motion)
expect(report.motion_verbosity_control)
expect(report.preview_fixture_ready)
expect(report.command_context_ready)
expect(report.command_palette_ready)
expect(report.command_palette_grouping_ready)
expect(report.context_menu_actions_ready)
expect(report.context_menu_keyboard_ready)
expect(report.context_menu_workspace_targets_ready)
expect(report.context_menu_visuals_ready)
expect(report.quality_inspector_ready)
expect(report.quality_inspector_summary_ready)
expect(report.quality_inspector_details_ready)
expect(report.quality_inspector_policy_ready)
expect(report.quality_inspector_actions_ready)
expect(report.quality_inspector_filters_ready)
expect(report.quality_inspector_check_detail_ready)
expect(report.quality_inspector_audit_mode_ready)
expect(report.quality_inspector_guidance_ready)
expect(report.quality_inspector_fix_action_ready)
expect(report.quality_inspector_motion_preview_ready)
expect(report.quality_inspector_motion_samples_ready)
expect(report.quality_inspector_motion_policy_ready)
expect(report.quality_inspector_animation_preview_ready)
expect(report.quality_inspector_widget_preview_ready)
expect(report.quality_inspector_material_preview_ready)
expect(report.quality_inspector_surface_preview_ready)
expect(report.quality_inspector_color_preview_ready)
expect(report.quality_inspector_layout_preview_ready)
expect(report.quality_inspector_titlebar_preview_ready)
expect(report.quality_inspector_icon_preview_ready)
expect(report.quality_inspector_typography_preview_ready)
expect(report.quality_inspector_depth_preview_ready)
expect(report.quality_inspector_interaction_preview_ready)
expect(report.quality_inspector_state_preview_ready)
expect(report.quality_inspector_verbosity_preview_ready)
expect(report.quality_inspector_performance_preview_ready)
expect(report.quality_inspector_spatial_preview_ready)
expect(report.quality_inspector_dock_preview_ready)
expect(report.quality_inspector_responsive_preview_ready)
expect(report.quality_inspector_accessibility_preview_ready)
expect(report.title_input_ready)
expect(report.title_command_modes_ready)
expect(report.visual_layering_ready)
expect(report.accessible_controls_ready)
expect(report.snap_assist_ready)
expect(report.drag_snap_feedback_ready)
expect(report.snap_layout_palette_ready)
expect(report.window_arrange_ready)
expect(report.desktop_peek_ready)
expect(report.system_hud_ready)
expect(report.taskbar_preview_actions_ready)
expect(report.privacy_indicator_ready)
expect(report.workspace_switcher_ready)
expect(report.workspace_switcher_actions_ready)
expect(report.clipboard_history_ready)
expect(report.clipboard_history_search_ready)
expect(report.screen_capture_ready)
expect(report.quick_settings_ready)
expect(report.quick_settings_levels_ready)
expect(report.notification_center_ready)
expect(report.notification_center_actions_ready)
expect(report.notification_center_filters_ready)
expect(report.focus_mode_ready)
expect(report.live_activity_ready)
expect(report.live_activity_controls_ready)
expect(report.hot_corners_ready)
expect(report.resize_hud_ready)
expect(report.gesture_hints_ready)
expect(report.desktop_widgets_ready)
expect(report.desktop_widget_controls_ready)
expect(report.desktop_items_ready)
expect(report.top_menu_bar_ready)
expect(report.wallpaper_picker_ready)
expect(report.accent_palette_ready)
expect(report.dock_stack_ready)
expect(report.app_launcher_ready)
expect(report.app_launcher_categories_ready)
expect(report.shortcut_overlay_ready)
expect(report.shortcut_overlay_search_ready)
expect(report.wm_tooltips_ready)
expect(report.widget_gallery_ready)
expect(report.stage_rail_ready)
expect(report.stage_rail_actions_ready)
expect(report.window_switcher_ready)
expect(report.window_overview_ready)
expect(report.window_overview_search_ready)
expect(report.control_center_ready)
expect(report.control_center_personalization_ready)
expect(report.responsive_layout_ready)
expect(wm_theme_quality_summary("glass_dark")).to_contain("status=pass")
expect(wm_theme_quality_summary("glass_dark")).to_contain("lifecycle_motion=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("lifecycle_motion_profile_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("contrast_ratio_x100=")
expect(wm_theme_quality_summary("glass_dark")).to_contain("titlebar_height_px=38")
expect(wm_theme_quality_summary("glass_dark")).to_contain("system_hud_max_width_px=280")
expect(wm_theme_quality_summary("glass_dark")).to_contain("privacy_indicator_max_width_px=300")
expect(wm_theme_quality_summary("glass_dark")).to_contain("workspace_switcher_max_width_px=420")
expect(wm_theme_quality_summary("glass_dark")).to_contain("clipboard_history_max_width_px=360")
expect(wm_theme_quality_summary("glass_dark")).to_contain("screen_capture_max_width_px=560")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quick_settings_max_width_px=300")
expect(wm_theme_quality_summary("glass_dark")).to_contain("notification_center_max_width_px=340")
expect(wm_theme_quality_summary("glass_dark")).to_contain("live_activity_max_width_px=420")
expect(wm_theme_quality_summary("glass_dark")).to_contain("hot_corner_max_size_px=44")
expect(wm_theme_quality_summary("glass_dark")).to_contain("resize_hud_max_width_px=240")
expect(wm_theme_quality_summary("glass_dark")).to_contain("gesture_hints_max_width_px=360")
expect(wm_theme_quality_summary("glass_dark")).to_contain("command_palette_max_width_px=680")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_max_width_px=420")
expect(wm_theme_quality_summary("glass_dark")).to_contain("control_center_max_width_px=320")
expect(wm_theme_quality_summary("glass_dark")).to_contain("desktop_widget_max_width_px=260")
expect(wm_theme_quality_summary("glass_dark")).to_contain("app_launcher_max_width_px=520")
expect(wm_theme_quality_summary("glass_dark")).to_contain("wallpaper_picker_max_width_px=380")
expect(wm_theme_quality_summary("glass_dark")).to_contain("accent_palette_max_width_px=360")
expect(wm_theme_quality_summary("glass_dark")).to_contain("dock_stack_max_width_px=340")
expect(wm_theme_quality_summary("glass_dark")).to_contain("stage_rail_max_width_px=88")
expect(wm_theme_quality_summary("glass_dark")).to_contain("window_switcher_max_width_px=460")
expect(wm_theme_quality_summary("glass_dark")).to_contain("top_menu_bar_height_px=38")
expect(wm_theme_quality_summary("glass_dark")).to_contain("overview_card_min_width_px=180")
expect(wm_theme_quality_summary("glass_dark")).to_contain("touch_target_min_height_px=44")
expect(wm_theme_quality_summary("glass_dark")).to_contain("responsive_layout_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("visual_layering_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("accessible_controls_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("snap_assist_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("drag_snap_feedback_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("snap_layout_palette_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("window_arrange_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("desktop_peek_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("taskbar_attention_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("taskbar_preview_actions_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("top_menu_bar_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("focus_mode_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("system_hud_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("privacy_indicator_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("workspace_switcher_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("workspace_switcher_actions_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("clipboard_history_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("clipboard_history_search_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("screen_capture_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quick_settings_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quick_settings_levels_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("notification_center_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("notification_center_actions_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("notification_center_filters_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("live_activity_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("live_activity_controls_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("window_switcher_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("hot_corners_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("resize_hud_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("gesture_hints_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("desktop_widgets_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("desktop_widget_controls_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("desktop_items_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("wallpaper_picker_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("accent_palette_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("dock_stack_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("app_launcher_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("app_launcher_categories_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("shortcut_overlay_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("shortcut_overlay_search_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("wm_tooltips_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("widget_gallery_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("stage_rail_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("stage_rail_actions_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("window_overview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("window_overview_search_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("control_center_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("control_center_personalization_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("command_palette_grouping_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("context_menu_actions_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("context_menu_keyboard_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("context_menu_workspace_targets_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("context_menu_visuals_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_summary_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_details_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_policy_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_actions_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_filters_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_check_detail_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_audit_mode_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_guidance_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_fix_action_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_motion_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_motion_samples_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_motion_policy_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_animation_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_widget_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_material_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_surface_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_color_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_layout_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_titlebar_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_icon_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_typography_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_depth_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_interaction_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_state_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_verbosity_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_performance_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_spatial_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_responsive_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("quality_inspector_accessibility_preview_ready=true")
expect(wm_theme_quality_summary("glass_dark")).to_contain("title_command_modes_ready=true")
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 5 |
| Active scenarios | 5 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>
