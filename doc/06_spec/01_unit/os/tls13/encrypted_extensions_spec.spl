# encrypted_extensions_spec

> Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 11 | 11 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# encrypted_extensions_spec

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## At a Glance

| Field | Value |
|-------|-------|
| Category | Hardware & OS |
| Status | Active |
| Source | `test/01_unit/os/tls13/encrypted_extensions_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## Operator workflow

1. Run `bin/simple test test/01_unit/os/tls13/encrypted_extensions_spec.spl`.
2. Every scenario must pass; a failure is a regression in the behavior under test.

## Compatibility and limitations

Covers the behavior asserted here; platform-specific behavior is out of scope.

## Scenarios

### parse_encrypted_extensions happy paths

#### accepts an empty extensions list

- Verify: accepts an empty extensions list
   - Expected: value.server_name_acknowledged is false
   - Expected: value.selected_alpn equals ``
   - Expected: value.max_fragment_length equals `0u8`


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts an empty extensions list")
# @req: REQ-SSPEC-LOCAL-001
val res = parse_encrypted_extensions(_ee_empty())
if val EncryptedExtensionsResult.Ok(value) = res:
    expect(value.server_name_acknowledged).to_equal(false)
    expect(value.selected_alpn).to_equal("")
    expect(value.max_fragment_length).to_equal(0u8)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### records server_name acknowledgement

- Verify: records server_name acknowledgement
   - Expected: value.server_name_acknowledged is true


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: records server_name acknowledgement")
val res = parse_encrypted_extensions(_ee_server_name_ack())
if val EncryptedExtensionsResult.Ok(value) = res:
    expect(value.server_name_acknowledged).to_equal(true)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### extracts the selected ALPN protocol

- Verify: extracts the selected ALPN protocol
   - Expected: value.selected_alpn equals `h2`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: extracts the selected ALPN protocol")
val res = parse_encrypted_extensions(_ee_alpn_h2())
if val EncryptedExtensionsResult.Ok(value) = res:
    expect(value.selected_alpn).to_equal("h2")
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### ignores unknown extension types and continues parsing

- Verify: ignores unknown extension types and continues parsing
   - Expected: value.selected_alpn equals `h2`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: ignores unknown extension types and continues parsing")
# @req: REQ-SSPEC-LOCAL-001
val res = parse_encrypted_extensions(_ee_unknown_then_alpn())
if val EncryptedExtensionsResult.Ok(value) = res:
    expect(value.selected_alpn).to_equal("h2")
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### echoes max_fragment_length byte

- Verify: echoes max_fragment_length byte
   - Expected: value.max_fragment_length equals `0x04u8`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: echoes max_fragment_length byte")
# @req: REQ-SSPEC-LOCAL-001
val res = parse_encrypted_extensions(_ee_mfl())
if val EncryptedExtensionsResult.Ok(value) = res:
    expect(value.max_fragment_length).to_equal(0x04u8)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

### parse_encrypted_extensions rejections

#### rejects duplicate extension_type as illegal_parameter

- Verify: rejects duplicate extension_type as illegal_parameter
   - Expected: reason contains `illegal_parameter`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects duplicate extension_type as illegal_parameter")
val res = parse_encrypted_extensions(_ee_dup_sni())
if val EncryptedExtensionsResult.Err(reason) = res:
    expect(reason.contains("illegal_parameter")).to_equal(true)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### rejects truncated body as decode_error

- Verify: rejects truncated body as decode_error
   - Expected: reason contains `decode_error`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects truncated body as decode_error")
val res = parse_encrypted_extensions(_ee_truncated_outer())
if val EncryptedExtensionsResult.Err(reason) = res:
    expect(reason.contains("decode_error")).to_equal(true)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### rejects non-empty server_name in EE

- Verify: rejects non-empty server_name in EE
   - Expected: reason contains `illegal_parameter`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects non-empty server_name in EE")
# @req: REQ-SSPEC-LOCAL-001
val res = parse_encrypted_extensions(_ee_server_name_nonempty())
if val EncryptedExtensionsResult.Err(reason) = res:
    expect(reason.contains("illegal_parameter")).to_equal(true)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### rejects early_data extension (0-RTT not implemented)

- Verify: rejects early_data extension (0-RTT not implemented)
   - Expected: reason contains `unsupported_extension`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects early_data extension (0-RTT not implemented)")
# @req: REQ-SSPEC-LOCAL-001
val res = parse_encrypted_extensions(_ee_early_data())
if val EncryptedExtensionsResult.Err(reason) = res:
    expect(reason.contains("unsupported_extension")).to_equal(true)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### rejects ALPN with multiple ProtocolNames

- Verify: rejects ALPN with multiple ProtocolNames
   - Expected: reason contains `illegal_parameter`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects ALPN with multiple ProtocolNames")
# @req: REQ-SSPEC-LOCAL-001
val res = parse_encrypted_extensions(_ee_alpn_two_names())
if val EncryptedExtensionsResult.Err(reason) = res:
    expect(reason.contains("illegal_parameter")).to_equal(true)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

#### rejects malformed max_fragment_length

- Verify: rejects malformed max_fragment_length
   - Expected: reason contains `illegal_parameter`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects malformed max_fragment_length")
# @req: REQ-SSPEC-LOCAL-001
val res = parse_encrypted_extensions(_ee_mfl_bad_len())
if val EncryptedExtensionsResult.Err(reason) = res:
    expect(reason.contains("illegal_parameter")).to_equal(true)
else:
    fail("unexpected TLS13 EncryptedExtensions parse branch")
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 11 |
| Active scenarios | 11 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `03d03bd4dcba0ea4c9f245d69a0d1d2bd6a89c60314eccbacbec2eb9e05fcc90`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `03d03bd4dcba0ea4c9f245d69a0d1d2bd6a89c60314eccbacbec2eb9e05fcc90`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `03d03bd4dcba0ea4c9f245d69a0d1d2bd6a89c60314eccbacbec2eb9e05fcc90`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **92/100**; effective score: **92/100**; blockers: **0**.

SSpec documentization score: 92/100
source: test/01_unit/os/tls13/encrypted_extensions_spec.spl
mirror: doc/06_spec/01_unit/os/tls13/encrypted_extensions_spec.md (current)
findings: 5 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=70
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/os/tls13/encrypted_extensions_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/os/tls13/encrypted_extensions_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: assumptions/preconditions, traceability, evidence, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/os/tls13/encrypted_extensions_spec.spl:149:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'accepts an empty extensions list' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/os/tls13/encrypted_extensions_spec.spl:160:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'records server_name acknowledgement' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/os/tls13/encrypted_extensions_spec.spl:169:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'extracts the selected ALPN protocol' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
