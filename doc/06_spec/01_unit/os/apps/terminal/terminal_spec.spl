# Terminal Emulator Unit Tests

> Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 40 | 40 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Terminal Emulator Unit Tests

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## At a Glance

| Field | Value |
|-------|-------|
| Category | Hardware & OS |
| Status | Active |
| Source | `test/01_unit/os/apps/terminal/terminal_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## Operator workflow

1. Run `bin/simple test test/01_unit/os/apps/terminal/terminal_spec.spl`.
2. Every scenario must pass; a failure is a regression in the behavior under test.

## Compatibility and limitations

Covers the behavior asserted here; platform-specific behavior is out of scope.

## Scenarios

### TerminalChar

#### constructs with default values

- Verify: constructs with default values
   - Expected: ch.ch equals ` `
   - Expected: ch.fg equals `7`
   - Expected: ch.bg equals `0`
   - Expected: ch.bold is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: constructs with default values")
# @req: REQ-SSPEC-LOCAL-001
"""TerminalChar(ch, fg, bg, bold) round-trips its fields unchanged."""
val ch = TerminalChar(ch: " ", fg: 7, bg: 0, bold: false)
expect(ch.ch).to_equal(" ")
expect(ch.fg).to_equal(7)
expect(ch.bg).to_equal(0)
expect(ch.bold).to_equal(false)
```

</details>

#### constructs with custom color

- Verify: constructs with custom color
   - Expected: ch.ch equals `A`
   - Expected: ch.fg equals `1`
   - Expected: ch.bg equals `4`
   - Expected: ch.bold is true


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: constructs with custom color")
# @req: REQ-SSPEC-LOCAL-001
val ch = TerminalChar(ch: "A", fg: 1, bg: 4, bold: true)
expect(ch.ch).to_equal("A")
expect(ch.fg).to_equal(1)
expect(ch.bg).to_equal(4)
expect(ch.bold).to_equal(true)
```

</details>

#### default_char returns space with white-on-black

- Verify: default_char returns space with white-on-black
   - Expected: ch.ch equals ` `
   - Expected: ch.fg equals `7`
   - Expected: ch.bg equals `0`
   - Expected: ch.bold is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: default_char returns space with white-on-black")
# @req: REQ-SSPEC-LOCAL-001
val ch = default_char()
expect(ch.ch).to_equal(" ")
expect(ch.fg).to_equal(7)
expect(ch.bg).to_equal(0)
expect(ch.bold).to_equal(false)
```

</details>

### TerminalLine

#### new_line creates line with correct width

- Verify: new_line creates line with correct width
   - Expected: line.chars.len() equals `80`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: new_line creates line with correct width")
# @req: REQ-SSPEC-LOCAL-001
val line = new_line(80)
expect(line.chars.len()).to_equal(80)
```

</details>

#### new_line fills with blank chars

- Verify: new_line fills with blank chars
   - Expected: line.chars[0].ch equals ` `
   - Expected: line.chars[9].ch equals ` `


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: new_line fills with blank chars")
val line = new_line(10)
expect(line.chars[0].ch).to_equal(" ")
expect(line.chars[9].ch).to_equal(" ")
```

</details>

#### line_to_text renders plain text

- Verify: line_to_text renders plain text


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: line_to_text renders plain text")
# @req: REQ-SSPEC-LOCAL-001
var line = new_line(5)
line.chars[0] = TerminalChar(ch: "H", fg: 7, bg: 0, bold: false)
line.chars[1] = TerminalChar(ch: "i", fg: 7, bg: 0, bold: false)
val text_out = line_to_text(line)
expect(text_out).to_start_with("Hi")
```

</details>

### Terminal

#### when newly created

#### has correct cols

- Verify: has correct cols
   - Expected: term.cols equals `80`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: has correct cols")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.cols).to_equal(80)
```

</details>

#### has correct rows

- Verify: has correct rows
   - Expected: term.rows equals `25`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: has correct rows")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.rows).to_equal(25)
```

</details>

#### starts with cursor at row 0

- Verify: starts with cursor at row 0
   - Expected: term.cursor_row equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: starts with cursor at row 0")
val term = Terminal.new(80, 25)
expect(term.cursor_row).to_equal(0)
```

</details>

#### starts with cursor at col 0

- Verify: starts with cursor at col 0
   - Expected: term.cursor_col equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: starts with cursor at col 0")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.cursor_col).to_equal(0)
```

</details>

#### has correct number of lines

- Verify: has correct number of lines
   - Expected: term.lines.len() equals `25`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: has correct number of lines")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.lines.len()).to_equal(25)
```

</details>

#### starts with default fg color (white)

- Verify: starts with default fg color (white)
   - Expected: term.fg_color equals `7`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: starts with default fg color (white)")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.fg_color).to_equal(7)
```

</details>

#### starts with default bg color (black)

- Verify: starts with default bg color (black)
   - Expected: term.bg_color equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: starts with default bg color (black)")
val term = Terminal.new(80, 25)
expect(term.bg_color).to_equal(0)
```

</details>

#### starts not bold

- Verify: starts not bold
   - Expected: term.bold is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: starts not bold")
val term = Terminal.new(80, 25)
expect(term.bold).to_equal(false)
```

</details>

#### starts with empty input buffer

- Verify: starts with empty input buffer
   - Expected: term.input_buffer equals ``


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: starts with empty input buffer")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.input_buffer).to_equal("")
```

</details>

#### starts with empty output buffer

- Verify: starts with empty output buffer
   - Expected: term.output_buffer equals ``


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: starts with empty output buffer")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.output_buffer).to_equal("")
```

</details>

#### starts with ANSI state Normal

- Verify: starts with ANSI state Normal
   - Expected: term.ansi_state equals `AnsiState.Normal`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: starts with ANSI state Normal")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.ansi_state).to_equal(AnsiState.Normal)
```

</details>

#### starts with max_scrollback of 1000

- Verify: starts with max_scrollback of 1000
   - Expected: term.max_scrollback equals `1000`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: starts with max_scrollback of 1000")
val term = Terminal.new(80, 25)
expect(term.max_scrollback).to_equal(1000)
```

</details>

#### with small dimensions

#### creates 1x1 terminal

- Verify: creates 1x1 terminal
   - Expected: term.cols equals `1`
   - Expected: term.rows equals `1`
   - Expected: term.lines.len() equals `1`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: creates 1x1 terminal")
val term = Terminal.new(1, 1)
expect(term.cols).to_equal(1)
expect(term.rows).to_equal(1)
expect(term.lines.len()).to_equal(1)
```

</details>

### Terminal write_char

#### places character at cursor position

- Verify: places character at cursor position
   - Expected: ch.ch equals `A`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: places character at cursor position")
# @req: REQ-SSPEC-LOCAL-001
var term = Terminal.new(80, 25)
term.write_char("A")
val ch = term.lines[0].chars[0]
expect(ch.ch).to_equal("A")
```

</details>

#### advances cursor column after write

- Verify: advances cursor column after write
   - Expected: term.cursor_col equals `1`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: advances cursor column after write")
var term = Terminal.new(80, 25)
term.write_char("A")
expect(term.cursor_col).to_equal(1)
```

</details>

#### writes multiple characters sequentially

- Verify: writes multiple characters sequentially
   - Expected: term.cursor_col equals `2`
   - Expected: term.lines[0].chars[0].ch equals `H`
   - Expected: term.lines[0].chars[1].ch equals `i`


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: writes multiple characters sequentially")
var term = Terminal.new(80, 25)
term.write_char("H")
term.write_char("i")
expect(term.cursor_col).to_equal(2)
expect(term.lines[0].chars[0].ch).to_equal("H")
expect(term.lines[0].chars[1].ch).to_equal("i")
```

</details>

#### wraps at end of line

- Verify: wraps at end of line


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: wraps at end of line")
# @req: REQ-SSPEC-LOCAL-001
var term = Terminal.new(3, 2)
term.write_char("A")
term.write_char("B")
term.write_char("C")
# After writing 3 chars in a 3-col terminal, cursor should wrap
expect(term.cursor_row).to_be_greater_than(0)
```

</details>

#### handles newline character

- Verify: handles newline character
   - Expected: term.cursor_row equals `1`
   - Expected: term.cursor_col equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: handles newline character")
# @req: REQ-SSPEC-LOCAL-001
var term = Terminal.new(80, 25)
term.write_char("A")
term.write_char("\n")
expect(term.cursor_row).to_equal(1)
expect(term.cursor_col).to_equal(0)
```

</details>

### Terminal newline

#### moves cursor to next row

- Verify: moves cursor to next row
   - Expected: term.cursor_row equals `1`
   - Expected: term.cursor_col equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: moves cursor to next row")
var term = Terminal.new(80, 25)
term.newline()
expect(term.cursor_row).to_equal(1)
expect(term.cursor_col).to_equal(0)
```

</details>

#### resets cursor_col to 0

- Verify: resets cursor_col to 0
   - Expected: term.cursor_col equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: resets cursor_col to 0")
# @req: REQ-SSPEC-LOCAL-001
var term = Terminal.new(80, 25)
term.write_char("X")
term.write_char("Y")
term.newline()
expect(term.cursor_col).to_equal(0)
```

</details>

#### scrolls when at last row

- Verify: scrolls when at last row
   - Expected: term.cursor_row equals `2`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: scrolls when at last row")
var term = Terminal.new(80, 3)
term.cursor_row = 2
term.newline()
# After newline at last row, cursor stays at last row
expect(term.cursor_row).to_equal(2)
```

</details>

### Terminal scroll_up

#### shifts buffer up by one line

- Verify: shifts buffer up by one line
   - Expected: term.lines[0].chars[0].ch equals `B`
   - Expected: term.lines[1].chars[0].ch equals `C`


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: shifts buffer up by one line")
var term = Terminal.new(10, 3)
term.lines[0].chars[0] = TerminalChar(ch: "A", fg: 7, bg: 0, bold: false)
term.lines[1].chars[0] = TerminalChar(ch: "B", fg: 7, bg: 0, bold: false)
term.lines[2].chars[0] = TerminalChar(ch: "C", fg: 7, bg: 0, bold: false)
term.scroll_up()
# Line 0 should now be what was line 1
expect(term.lines[0].chars[0].ch).to_equal("B")
expect(term.lines[1].chars[0].ch).to_equal("C")
```

</details>

#### adds blank line at bottom after scroll

- Verify: adds blank line at bottom after scroll
   - Expected: term.lines[2].chars[0].ch equals ` `


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: adds blank line at bottom after scroll")
# @req: REQ-SSPEC-LOCAL-001
var term = Terminal.new(10, 3)
term.lines[2].chars[0] = TerminalChar(ch: "X", fg: 7, bg: 0, bold: false)
term.scroll_up()
# Last line should be blank
expect(term.lines[2].chars[0].ch).to_equal(" ")
```

</details>

#### preserves line count after scroll

- Verify: preserves line count after scroll
   - Expected: term.lines.len() equals `5`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: preserves line count after scroll")
# @req: REQ-SSPEC-LOCAL-001
var term = Terminal.new(10, 5)
term.scroll_up()
expect(term.lines.len()).to_equal(5)
```

</details>

### Terminal clear

#### resets all lines to blank

- Verify: resets all lines to blank
   - Expected: term.lines[0].chars[0].ch equals ` `
   - Expected: term.lines[0].chars[1].ch equals ` `


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: resets all lines to blank")
var term = Terminal.new(10, 3)
term.write_char("A")
term.write_char("B")
term.clear()
expect(term.lines[0].chars[0].ch).to_equal(" ")
expect(term.lines[0].chars[1].ch).to_equal(" ")
```

</details>

#### resets cursor to origin

- Verify: resets cursor to origin
   - Expected: term.cursor_row equals `0`
   - Expected: term.cursor_col equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: resets cursor to origin")
var term = Terminal.new(80, 25)
term.cursor_row = 10
term.cursor_col = 20
term.clear()
expect(term.cursor_row).to_equal(0)
expect(term.cursor_col).to_equal(0)
```

</details>

#### preserves number of lines

- Verify: preserves number of lines
   - Expected: term.lines.len() equals `25`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: preserves number of lines")
var term = Terminal.new(80, 25)
term.clear()
expect(term.lines.len()).to_equal(25)
```

</details>

### Terminal ANSI parsing

#### starts in Normal state

- Verify: starts in Normal state
   - Expected: term.ansi_state equals `AnsiState.Normal`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: starts in Normal state")
# @req: REQ-SSPEC-LOCAL-001
val term = Terminal.new(80, 25)
expect(term.ansi_state).to_equal(AnsiState.Normal)
```

</details>

#### AnsiState enum has Normal variant

- Verify: AnsiState enum has Normal variant
   - Expected: state equals `AnsiState.Normal`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: AnsiState enum has Normal variant")
val state = AnsiState.Normal
expect(state).to_equal(AnsiState.Normal)
```

</details>

#### AnsiState enum has Escape variant

- Verify: AnsiState enum has Escape variant
   - Expected: state equals `AnsiState.Escape`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: AnsiState enum has Escape variant")
# @req: REQ-SSPEC-LOCAL-001
val state = AnsiState.Escape
expect(state).to_equal(AnsiState.Escape)
```

</details>

#### AnsiState enum has Bracket variant

- Verify: AnsiState enum has Bracket variant
   - Expected: state equals `AnsiState.Bracket`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: AnsiState enum has Bracket variant")
# @req: REQ-SSPEC-LOCAL-001
val state = AnsiState.Bracket
expect(state).to_equal(AnsiState.Bracket)
```

</details>

#### AnsiState enum has Param variant

- Verify: AnsiState enum has Param variant
   - Expected: state equals `AnsiState.Param`


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: AnsiState enum has Param variant")
# @req: REQ-SSPEC-LOCAL-001
val state = AnsiState.Param
expect(state).to_equal(AnsiState.Param)
```

</details>

### Terminal write_string

#### writes multiple characters

- Verify: writes multiple characters
   - Expected: term.cursor_col equals `5`
   - Expected: term.lines[0].chars[0].ch equals `H`
   - Expected: term.lines[0].chars[4].ch equals `o`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: writes multiple characters")
var term = Terminal.new(80, 25)
term.write_string("Hello")
expect(term.cursor_col).to_equal(5)
expect(term.lines[0].chars[0].ch).to_equal("H")
expect(term.lines[0].chars[4].ch).to_equal("o")
```

</details>

#### handles embedded newlines

- Verify: handles embedded newlines
   - Expected: term.lines[0].chars[0].ch equals `A`
   - Expected: term.lines[0].chars[1].ch equals `B`
   - Expected: term.lines[1].chars[0].ch equals `C`
   - Expected: term.lines[1].chars[1].ch equals `D`


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: handles embedded newlines")
var term = Terminal.new(80, 25)
term.write_string("AB\nCD")
expect(term.lines[0].chars[0].ch).to_equal("A")
expect(term.lines[0].chars[1].ch).to_equal("B")
expect(term.lines[1].chars[0].ch).to_equal("C")
expect(term.lines[1].chars[1].ch).to_equal("D")
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 40 |
| Active scenarios | 40 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `7c5aa3a8b717415bf2f3421ff17fb1e52bf03109d30cfd93da20d15c4ceda67e`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `7c5aa3a8b717415bf2f3421ff17fb1e52bf03109d30cfd93da20d15c4ceda67e`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `7c5aa3a8b717415bf2f3421ff17fb1e52bf03109d30cfd93da20d15c4ceda67e`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **86/100**; effective score: **86/100**; blockers: **0**.

SSpec documentization score: 86/100
source: test/01_unit/os/apps/terminal/terminal_spec.spl
mirror: doc/06_spec/01_unit/os/apps/terminal/terminal_spec.md (current)
findings: 6 blockers: 0
  narrative=100 structure=100 oracle=70
  traceability=100 evidence=70 coverage=100 maintainability=70
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/os/apps/terminal/terminal_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/os/apps/terminal/terminal_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: assumptions/preconditions, traceability, evidence, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/os/apps/terminal/terminal_spec.spl:1:1: advice SSDOC-ORA-003 [oracle] (-30): 31 unexplained numeric expected value(s)
  why: Reviewers need to know why a magic expected value is authoritative.
  improve: Name the authoritative expected value or add a '# oracle:' explanation.
test/01_unit/os/apps/terminal/terminal_spec.spl:40:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'constructs with default values' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/os/apps/terminal/terminal_spec.spl:50:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'constructs with custom color' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/os/apps/terminal/terminal_spec.spl:59:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'default_char returns space with white-on-black' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
