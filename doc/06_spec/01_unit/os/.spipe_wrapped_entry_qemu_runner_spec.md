# .spipe Wrapped Entry Qemu Runner Specification

> Tests covering Qemu runner serial routing, Qemu runner native-build prerequisites, Qemu runner release artifacts, Qemu runner board bundle smoke, Qemu runner board execution smoke, Qemu runner desktop UEFI tool app validator.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 65 | 65 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# .spipe Wrapped Entry Qemu Runner Specification

## Scenarios

### Qemu runner serial routing

#### does not accept non-x86 QEMU exit code 1 as success

- does not accept non-x86 QEMU exit code 1 as success


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not accept non-x86 QEMU exit code 1 as success")
val arm64 = get_target(Architecture.Arm64)
val arm32 = get_target(Architecture.Arm32)
val rv64 = get_target(Architecture.Riscv64)
expect is_qemu_success(arm64, 1) == false
expect is_qemu_success(arm32, 1) == false
expect is_qemu_success(rv64, 1) == false
expect is_qemu_success(arm64, 0) == true
```

</details>

#### keeps isa-debug-exit success limited to x86 scenarios

- keeps isa-debug-exit success limited to x86 scenarios


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps isa-debug-exit success limited to x86 scenarios")
val x64 = scenario_x64_desktop_uefi()
val arm64 = scenario_arm64_virtio_fat32_smf()
val rv64 = scenario_riscv64_virtio_fat32_smf()
expect scenario_qemu_exit_success(x64, 1) == true
expect scenario_qemu_exit_success(x64, 0) == false
expect scenario_qemu_exit_success(arm64, 1) == false
expect scenario_qemu_exit_success(rv64, 1) == false
expect scenario_qemu_exit_success(arm64, 0) == true
```

</details>

#### exposes a runner-facing protection serial acceptance gate

- exposes a runner-facing protection serial acceptance gate


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("exposes a runner-facing protection serial acceptance gate")
val an505_serial = "[BOOT] Platform: MPS2-AN505 (QEMU)\n[MPU] Enabled, 8 regions available, 4 configured\nSimpleOS Lite v0.5"
expect qemu_protection_serial_accepts_hardening("mps2-an505", "enforce", "qemu", an505_serial) == true
expect qemu_protection_serial_reason("mps2-an505", "enforce", "none", an505_serial) == "missing-runtime-check"

val detect_serial = "protection_probe=pass\n"
expect qemu_protection_serial_accepts_hardening("mps2-an505", "detect", "qemu", detect_serial) == false
expect qemu_protection_serial_reason("mps2-an505", "detect", "qemu", detect_serial) == "diagnostic-protection-mode:detect"

val fault_missing = "protection_probe=pass\nprotection_enabled=pass\nregion_contract=pass\n"
expect qemu_protection_serial_reason("stm32u585-uno-q", "fault-test", "real-board", fault_missing) == "missing-fault-recovery"
```

</details>

#### maps known QEMU scenarios to board protection gates

- maps known QEMU scenarios to board protection gates


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("maps known QEMU scenarios to board protection gates")
val x64 = scenario_x64_net_user()
val x64_serial = "[BOOT64] call _start\n[harden] text_write_trap=pass\nTEST PASSED"
expect qemu_scenario_protection_board_id(x64) == "x86_64-q35"
expect qemu_scenario_protection_serial_accepts_hardening(x64, "enforce", x64_serial) == true

val rv64 = scenario_riscv64_hosted()
val rv64_serial = "[PAGING] MMU enabled (SCTLR_EL1.M=1, C=1, I=1)\npage_contract=pass\nTEST PASSED"
expect qemu_scenario_protection_board_id(rv64) == "riscv64-virt"
expect qemu_scenario_protection_serial_accepts_hardening(rv64, "enforce", rv64_serial) == true

val arm64 = scenario_arm64_virtio_fat32_smf()
expect qemu_scenario_protection_board_id(arm64) == ""
expect qemu_scenario_protection_serial_reason(arm64, "enforce", "protection_probe=pass") == "unsupported-qemu-board:arm64-virtio-fat32-smf"
```

</details>

#### enumerates 32-bit x86 as a first-class target

- enumerates 32-bit x86 as a first-class target


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("enumerates 32-bit x86 as a first-class target")
var found_x86_32 = false
for target in get_all_targets():
    if target.arch == Architecture.X86:
        found_x86_32 = true
        expect target.target_triple == "i686-unknown-none"
        expect target.qemu_system == "qemu-system-i386"
expect found_x86_32 == true
```

</details>

#### launches the x86_32 kernel lane through the compatible QEMU frontend

- launches the x86_32 kernel lane through the compatible QEMU frontend


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("launches the x86_32 kernel lane through the compatible QEMU frontend")
val target = get_target(Architecture.X86)
val cmd = build_qemu_command(target)
expect target.qemu_system == "qemu-system-i386"
expect cmd[0] == "qemu-system-x86_64"
expect (cmd).contains("-machine")
expect (cmd).contains("pc")
expect (cmd).contains("-cpu")
expect (cmd).contains("qemu32")
expect (cmd).contains("-kernel")
expect (cmd).contains("build/os/simpleos_x86_32.elf")
```

</details>

#### builds normal targets from the shared platform catalog

- builds normal targets from the shared platform catalog


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("builds normal targets from the shared platform catalog")
val x86_64 = get_target(Architecture.X86_64)
expect x86_64.entry == "examples/09_embedded/simple_os/arch/x86_64/boot_stage1_entry.spl"
expect x86_64.linker_script == "examples/09_embedded/simple_os/arch/x86_64/linker.ld"
expect x86_64.target_triple == "x86_64-unknown-none"
expect x86_64.qemu_system == "qemu-system-x86_64"

val arm32 = get_target(Architecture.Arm32)
expect arm32.entry == "src/os/kernel/arch/arm32/boot.spl"
expect arm32.target_triple == "armv7-unknown-none-eabihf"
expect arm32.qemu_system == "qemu-system-arm"
```

</details>

#### routes default ARM QEMU targets to the fs-exec acceptance lane

- routes default ARM QEMU targets to the fs-exec acceptance lane


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("routes default ARM QEMU targets to the fs-exec acceptance lane")
val arm64 = get_qemu_target(Architecture.Arm64)
expect arm64.entry == "examples/09_embedded/simple_os/arch/arm64/fs_exec_entry.spl"
expect arm64.output == "build/os/simpleos_arm64_fs_exec.elf"
val arm32 = get_qemu_target(Architecture.Arm32)
expect arm32.entry == "examples/09_embedded/simple_os/arch/arm32/fs_exec_entry.spl"
expect arm32.output == "build/os/simpleos_arm32_fs_exec.elf"
```

</details>

#### requires four-app fs launch markers for ARM acceptance lanes

- requires four-app fs launch markers for ARM acceptance lanes


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("requires four-app fs launch markers for ARM acceptance lanes")
val markers = arm_fs_exec_required_marker_fragments(scenario_arm64_virtio_fat32_smf())
expect (markers).contains("[desktop-e2e] process-backed:ok app=simple_compiler pid=")
expect (markers).contains("[desktop-e2e] process-backed:ok app=simple_loader pid=")
expect (markers).contains("[desktop-e2e] process-backed:ok app=clang pid=")
expect (markers).contains("[desktop-e2e] process-backed:ok app=rust pid=")
expect (markers).contains("[desktop-e2e] vfs-app-read:ok source=generic-vfs path=/sys/apps/simple_compiler bytes=")
expect (markers).contains("[desktop-e2e] vfs-app-read:ok source=generic-vfs path=/sys/apps/simple_loader bytes=")
expect (markers).contains("[desktop-e2e] vfs-app-read:ok source=generic-vfs path=/sys/apps/clang bytes=")
expect (markers).contains("[desktop-e2e] vfs-app-read:ok source=generic-vfs path=/sys/apps/rust bytes=")
expect markers.contains("[desktop-e2e] process-backed:ok app=hello_world pid=") == false
expect markers.contains("[desktop-e2e] process-backed:ok app=simple_interpreter pid=") == false
expect markers.contains("[desktop-e2e] process-backed:ok app=llvm pid=") == false
val lane = simpleos_platform_qemu_lane("arm64", "arm64-virtio-fat32-smf")
if val resolved_lane = lane:
    expect markers == resolved_lane.required_serial_markers
else:
    fail("missing arm64 acceptance lane")
```

</details>

#### keeps ARM and RV64 marker helper surfaces aligned with catalog lanes

- keeps ARM and RV64 marker helper surfaces aligned with catalog lanes


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps ARM and RV64 marker helper surfaces aligned with catalog lanes")
val arm64_lane = simpleos_platform_qemu_lane("arm64", "arm64-virtio-fat32-smf")
if val lane = arm64_lane:
    expect arm_fs_exec_required_marker_fragments(scenario_arm64_virtio_fat32_smf()) == lane.required_serial_markers
else:
    fail("missing arm64 virtio lane")

val arm32_lane = simpleos_platform_qemu_lane("arm32", "arm32-virtio-fat32-smf")
if val lane = arm32_lane:
    expect arm_fs_exec_required_marker_fragments(scenario_arm32_virtio_fat32_smf()) == lane.required_serial_markers
else:
    fail("missing arm32 virtio lane")

val rv64_hosted_lane = simpleos_platform_qemu_lane("riscv64", "riscv64-hosted")
if val lane = rv64_hosted_lane:
    expect riscv64_hosted_required_marker_fragments() == lane.required_serial_markers
else:
    fail("missing riscv64 hosted lane")
```

</details>

#### routes default RISC-V QEMU targets to the canonical boot lanes

- routes default RISC-V QEMU targets to the canonical boot lanes


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("routes default RISC-V QEMU targets to the canonical boot lanes")
val rv64 = get_qemu_target(Architecture.Riscv64)
expect rv64.entry == "src/os/kernel/arch/riscv64/boot.spl"
expect rv64.output == "build/os/simpleos_riscv64.elf"
val rv32 = get_qemu_target(Architecture.Riscv32)
expect rv32.entry == "examples/09_embedded/simple_os/arch/riscv32/smoke_entry.spl"
expect rv32.output == "build/os/simpleos_riscv32_smf_fs.elf"
```

</details>

#### defines ARM VirtIO FAT32 SMF execution scenarios

- defines ARM VirtIO FAT32 SMF execution scenarios


<details>
<summary>Executable SSpec</summary>

Runnable source: 33 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("defines ARM VirtIO FAT32 SMF execution scenarios")
val arm64 = scenario_arm64_virtio_fat32_smf()
expect arm64.name == "arm64-virtio-fat32-smf"
expect arm64.arch == Architecture.Arm64
expect (arm64.qemu_extra).contains("virtio-blk-device,drive=armdisk")
expect (arm64.qemu_extra).contains("file=build/os/fat32-arm64.img,if=none,id=armdisk,format=raw")
expect (arm64.qemu_extra).contains("-semihosting-config")
expect scenario_test_timeout_ms(arm64) == 60000
expect scenario_lane_kind(arm64) == SimpleOsLaneKind.FsExec

val arm64_lane = simpleos_platform_qemu_lane("arm64", "arm64-virtio-fat32-smf")
if val lane = arm64_lane:
    expect arm64.memory == lane.qemu_memory
    expect arm64.qemu_extra == lane.qemu_extra
else:
    fail("missing arm64 virtio lane")

val arm32 = scenario_arm32_virtio_fat32_smf()
expect arm32.name == "arm32-virtio-fat32-smf"
expect arm32.arch == Architecture.Arm32
expect (arm32.qemu_extra).contains("virtio-blk-device,drive=armdisk")
expect (arm32.qemu_extra).contains("file=build/os/fat32-arm32.img,if=none,id=armdisk,format=raw")
expect (arm32.qemu_extra).contains("-semihosting-config")
expect scenario_test_timeout_ms(arm32) == 60000
expect scenario_lane_kind(arm32) == SimpleOsLaneKind.FsExec

val arm32_lane = simpleos_platform_qemu_lane("arm32", "arm32-virtio-fat32-smf")
if val lane = arm32_lane:
    expect arm32.memory == lane.qemu_memory
    expect arm32.qemu_extra == lane.qemu_extra
else:
    fail("missing arm32 virtio lane")
```

</details>

#### builds QEMU commands with ARM loader and VirtIO block disks

- builds QEMU commands with ARM loader and VirtIO block disks


<details>
<summary>Executable SSpec</summary>

Runnable source: 33 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("builds QEMU commands with ARM loader and VirtIO block disks")
val arm64 = scenario_arm64_virtio_fat32_smf()
val cmd = build_scenario_command(arm64, "build/os/simpleos_arm64_fs_exec.bin")
expect cmd[0] == "qemu-system-aarch64"
expect (cmd).contains("loader,file=build/os/simpleos_arm64_fs_exec.bin,addr=0x40200000,force-raw=on")
expect (cmd).contains("loader,addr=0x40200000,cpu-num=0")
expect cmd.contains("-kernel") == false
expect (cmd).contains("virtio-blk-device,drive=armdisk")

val target = scenario_target(arm64)
expect target.entry == "examples/09_embedded/simple_os/arch/arm64/fs_exec_entry.spl"
expect target.linker_script == "examples/09_embedded/simple_os/arch/arm64/fs_exec_linker.ld"
expect target.output == "build/os/simpleos_arm64_fs_exec.elf"

val arm64_lane = simpleos_platform_qemu_lane("arm64", "arm64-virtio-fat32-smf")
if val lane = arm64_lane:
    expect target.entry == lane.entry
    expect target.linker_script == lane.linker_script
    expect target.output == lane.output
    expect arm_fs_exec_disk_image_path(Architecture.Arm64) == lane.media_path_hint
else:
    fail("missing arm64 virtio lane")

val arm32_target = scenario_target(scenario_arm32_virtio_fat32_smf())
val arm32_lane = simpleos_platform_qemu_lane("arm32", "arm32-virtio-fat32-smf")
if val lane = arm32_lane:
    expect arm32_target.entry == lane.entry
    expect arm32_target.linker_script == lane.linker_script
    expect arm32_target.output == lane.output
    expect arm_fs_exec_disk_image_path(Architecture.Arm32) == lane.media_path_hint
else:
    fail("missing arm32 virtio lane")
```

</details>

#### looks up ARM SMF scenarios by name

- looks up ARM SMF scenarios by name


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("looks up ARM SMF scenarios by name")
expect scenario_name_or_missing("arm64-virtio-fat32-smf") == "arm64-virtio-fat32-smf"
expect scenario_name_or_missing("arm32-virtio-fat32-smf") == "arm32-virtio-fat32-smf"
expect arm_fs_exec_disk_image_path(Architecture.Arm64) == "build/os/fat32-arm64.img"
expect arm_fs_exec_disk_image_path(Architecture.Arm32) == "build/os/fat32-arm32.img"
```

</details>

#### dispatches named ARM scenarios through catalog-backed lane kind, serial markers, and media

- dispatches named ARM scenarios through catalog-backed lane kind, serial markers, and media


<details>
<summary>Executable SSpec</summary>

Runnable source: 33 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("dispatches named ARM scenarios through catalog-backed lane kind, serial markers, and media")
val arm64 = get_scenario("arm64-virtio-fat32-smf")
if val resolved_arm64 = arm64:
    val target = scenario_target(resolved_arm64)
    expect scenario_lane_kind(resolved_arm64) == SimpleOsLaneKind.FsExec
    expect scenario_test_timeout_ms(resolved_arm64) == 60000
    val lane = simpleos_platform_qemu_lane("arm64", resolved_arm64.name)
    if val resolved_lane = lane:
        expect arm_fs_exec_required_marker_fragments(resolved_arm64) == resolved_lane.required_serial_markers
        expect target.entry == resolved_lane.entry
        expect target.output == resolved_lane.output
        expect arm_fs_exec_disk_image_path(resolved_arm64.arch) == resolved_lane.media_path_hint
    else:
        fail("missing arm64 virtio lane")
else:
    fail("missing arm64 scenario")

val arm32 = get_scenario("arm32-virtio-fat32-smf")
if val resolved_arm32 = arm32:
    val target = scenario_target(resolved_arm32)
    expect scenario_lane_kind(resolved_arm32) == SimpleOsLaneKind.FsExec
    expect scenario_test_timeout_ms(resolved_arm32) == 60000
    val lane = simpleos_platform_qemu_lane("arm32", resolved_arm32.name)
    if val resolved_lane = lane:
        expect arm_fs_exec_required_marker_fragments(resolved_arm32) == resolved_lane.required_serial_markers
        expect target.entry == resolved_lane.entry
        expect target.output == resolved_lane.output
        expect arm_fs_exec_disk_image_path(resolved_arm32.arch) == resolved_lane.media_path_hint
    else:
        fail("missing arm32 virtio lane")
else:
    fail("missing arm32 scenario")
```

</details>

#### defines RISC-V VirtIO FAT32 SMF execution scenarios

- defines RISC-V VirtIO FAT32 SMF execution scenarios


<details>
<summary>Executable SSpec</summary>

Runnable source: 23 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("defines RISC-V VirtIO FAT32 SMF execution scenarios")
val rv64 = scenario_riscv64_virtio_fat32_smf()
expect rv64.name == "riscv64-virtio-fat32-smf"
expect rv64.arch == Architecture.Riscv64
expect (rv64.qemu_extra).contains("virtio-blk-device,drive=rvdisk")
expect (rv64.qemu_extra).contains("file=build/os/fat32-riscv64.img,if=none,id=rvdisk,format=raw")
expect scenario_test_timeout_ms(rv64) == 60000
expect scenario_lane_kind(rv64) == SimpleOsLaneKind.FsExec

val rv64_lane = simpleos_platform_qemu_lane("riscv64", "riscv64-virtio-fat32-smf")
if val lane = rv64_lane:
    expect rv64.memory == lane.qemu_memory
    expect rv64.qemu_extra == lane.qemu_extra[2:]
else:
    fail("missing riscv64 virtio lane")

val rv32 = scenario_riscv32_virtio_fat32_smf()
expect rv32.name == "riscv32-virtio-fat32-smf"
expect rv32.arch == Architecture.Riscv32
expect (rv32.qemu_extra).contains("virtio-blk-device,drive=rvdisk")
expect (rv32.qemu_extra).contains("file=build/os/fat32-riscv32.img,if=none,id=rvdisk,format=raw")
expect scenario_test_timeout_ms(rv32) == 60000
```

</details>

#### builds QEMU commands with RISC-V kernels and VirtIO block disks

- builds QEMU commands with RISC-V kernels and VirtIO block disks


<details>
<summary>Executable SSpec</summary>

Runnable source: 20 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("builds QEMU commands with RISC-V kernels and VirtIO block disks")
val rv64 = scenario_riscv64_virtio_fat32_smf()
val cmd64 = build_scenario_command(rv64, "build/os/simpleos_riscv64_smf_fs.elf")
expect cmd64[0] == "qemu-system-riscv64"
expect (cmd64).contains("-kernel")
expect (cmd64).contains("build/os/simpleos_riscv64_smf_fs.elf")
expect (cmd64).contains("virtio-blk-device,drive=rvdisk")
val target64 = scenario_target(rv64)
expect target64.entry == "examples/09_embedded/simple_os/arch/riscv64/smoke_entry.spl"

val rv32 = scenario_riscv32_virtio_fat32_smf()
val cmd32 = build_scenario_command(rv32, "build/os/simpleos_riscv32_smf_fs.elf")
expect cmd32[0] == "qemu-system-riscv32"
expect (cmd32).contains("-kernel")
expect (cmd32).contains("build/os/simpleos_riscv32_smf_fs.elf")
expect (cmd32).contains("virtio-blk-device,drive=rvdisk")
val target32 = scenario_target(rv32)
expect target32.entry == "examples/09_embedded/simple_os/arch/riscv32/smoke_entry.spl"
expect target32.output == "build/os/simpleos_riscv32_smf_fs.elf"
```

</details>

#### keeps RV64 media-backed scenario targets aligned with catalog lanes

- keeps RV64 media-backed scenario targets aligned with catalog lanes


<details>
<summary>Executable SSpec</summary>

Runnable source: 23 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps RV64 media-backed scenario targets aligned with catalog lanes")
val rv64 = scenario_riscv64_virtio_fat32_smf()
val rv64_target = scenario_target(rv64)
val rv64_lane = simpleos_platform_qemu_lane("riscv64", "riscv64-virtio-fat32-smf")
if val lane = rv64_lane:
    expect rv64_target.entry == lane.entry
    expect rv64_target.linker_script == lane.linker_script
    expect rv64_target.output == lane.output
    expect riscv_fs_exec_disk_image_path(Architecture.Riscv64) == lane.media_path_hint
else:
    fail("missing riscv64 virtio lane")

val hosted = scenario_riscv64_hosted()
val hosted_target = scenario_target(hosted)
val hosted_lane = simpleos_platform_qemu_lane("riscv64", "riscv64-hosted")
if val lane = hosted_lane:
    expect hosted_target.entry == lane.entry
    expect hosted_target.linker_script == lane.linker_script
    expect hosted_target.output == lane.output
    expect riscv_fs_exec_disk_image_path(Architecture.Riscv64) == lane.media_path_hint
else:
    fail("missing riscv64 hosted lane")
```

</details>

#### dispatches named RISC-V scenarios to resolved catalog lanes

- dispatches named RISC-V scenarios to resolved catalog lanes


<details>
<summary>Executable SSpec</summary>

Runnable source: 50 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("dispatches named RISC-V scenarios to resolved catalog lanes")
val media = get_scenario("riscv64-virtio-fat32-smf")
if val resolved_media = media:
    val target = scenario_target(resolved_media)
    expect scenario_lane_kind(resolved_media) == SimpleOsLaneKind.FsExec
    expect scenario_test_timeout_ms(resolved_media) == 60000
    val lane = simpleos_platform_qemu_lane("riscv64", resolved_media.name)
    if val resolved_lane = lane:
        expect _scenario_required_marker_fragments(resolved_media) == resolved_lane.required_serial_markers
        expect target.entry == resolved_lane.entry
        expect target.output == resolved_lane.output
        expect riscv_fs_exec_disk_image_path(resolved_media.arch) == resolved_lane.media_path_hint
    else:
        fail("missing riscv64 media lane")
else:
    fail("missing riscv64 media scenario")

val hosted = get_scenario("riscv64-hosted")
if val resolved_hosted = hosted:
    val target = scenario_target(resolved_hosted)
    expect scenario_lane_kind(resolved_hosted) == SimpleOsLaneKind.HostedCompileSmoke
    expect scenario_test_timeout_ms(resolved_hosted) == 120000
    val lane = simpleos_platform_qemu_lane("riscv64", resolved_hosted.name)
    if val resolved_lane = lane:
        expect riscv64_hosted_required_marker_fragments() == resolved_lane.required_serial_markers
        expect target.entry == resolved_lane.entry
        expect target.output == resolved_lane.output
        expect riscv_fs_exec_disk_image_path(resolved_hosted.arch) == resolved_lane.media_path_hint
    else:
        fail("missing riscv64 hosted lane")
else:
    fail("missing riscv64 hosted scenario")

val rv32 = get_scenario("riscv32-virtio-fat32-smf")
if val resolved_rv32 = rv32:
    val target = scenario_target(resolved_rv32)
    expect scenario_lane_kind(resolved_rv32) == SimpleOsLaneKind.FsExec
    expect scenario_test_timeout_ms(resolved_rv32) == 60000
    val lane = simpleos_platform_qemu_lane("riscv32", resolved_rv32.name)
    if val resolved_lane = lane:
        expect _scenario_required_marker_fragments(resolved_rv32) == resolved_lane.required_serial_markers
        expect target.entry == resolved_lane.entry
        expect target.linker_script == resolved_lane.linker_script
        expect target.output == resolved_lane.output
        expect riscv_fs_exec_disk_image_path(resolved_rv32.arch) == resolved_lane.media_path_hint
    else:
        fail("missing riscv32 media lane")
else:
    fail("missing riscv32 media scenario")
```

</details>

#### scopes RISC-V SMF native builds to arch-local sources

- scopes RISC-V SMF native builds to arch-local sources


<details>
<summary>Executable SSpec</summary>

Runnable source: 17 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("scopes RISC-V SMF native builds to arch-local sources")
val rv64_target = scenario_target(scenario_riscv64_virtio_fat32_smf())
val rv64_args = os_native_build_args(rv64_target, "llvm")
expect (rv64_args).contains("--log")
expect (rv64_args).contains("on")
expect (rv64_args).contains("examples/09_embedded/simple_os/arch/riscv64")
expect rv64_args.contains("src") == false
expect os_native_build_sources(rv64_target)[0] == "build/os/generated"

val rv32_target = scenario_target(scenario_riscv32_virtio_fat32_smf())
val rv32_args = os_native_build_args(rv32_target, "llvm")
expect (rv32_args).contains("--log")
expect (rv32_args).contains("on")
expect (rv32_args).contains("examples/09_embedded/simple_os/arch/riscv32")
expect rv32_args.contains("src") == false
expect os_native_build_sources(rv32_target)[0] == "build/os/generated"
```

</details>

#### lets callers disable compiled OS logging through native-build args

- lets callers disable compiled OS logging through native-build args


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("lets callers disable compiled OS logging through native-build args")
expect rt_env_set("SIMPLE_OS_LOG_MODE", "off") == true
val x64_target = get_target(Architecture.X86_64)
val x64_args = os_native_build_args(x64_target, "llvm")
expect (x64_args).contains("--log")
expect (x64_args).contains("off")
expect os_native_build_sources(x64_target)[0] == "build/os/generated"
expect (os_native_build_sources(x64_target)).contains("src/os")
expect rt_env_set("SIMPLE_OS_LOG_MODE", "on") == true
```

</details>

#### looks up RISC-V SMF scenarios by name

- looks up RISC-V SMF scenarios by name


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("looks up RISC-V SMF scenarios by name")
expect scenario_name_or_missing("riscv64-virtio-fat32-smf") == "riscv64-virtio-fat32-smf"
expect scenario_name_or_missing("riscv32-virtio-fat32-smf") == "riscv32-virtio-fat32-smf"
expect riscv_fs_exec_disk_image_path(Architecture.Riscv64) == "build/os/fat32-riscv64.img"
expect riscv_fs_exec_disk_image_path(Architecture.Riscv32) == "build/os/fat32-riscv32.img"
```

</details>

#### defines a truthful RV64 hosted preflight scenario

- defines a truthful RV64 hosted preflight scenario


<details>
<summary>Executable SSpec</summary>

Runnable source: 27 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("defines a truthful RV64 hosted preflight scenario")
val hosted = scenario_riscv64_hosted()
expect hosted.name == "riscv64-hosted"
expect hosted.arch == Architecture.Riscv64
expect (hosted.qemu_extra).contains("virtio-blk-device,drive=rvdisk")
expect (hosted.qemu_extra).contains("user,id=n0,hostfwd=tcp::2222-:22,hostfwd=tcp::8080-:80")
expect (hosted.qemu_extra).contains("virtio-net-pci,netdev=n0,disable-legacy=on")
expect scenario_name_or_missing("riscv64-hosted") == "riscv64-hosted"
expect scenario_test_timeout_ms(hosted) == 120000
expect scenario_lane_kind(hosted) == SimpleOsLaneKind.HostedCompileSmoke

val hosted_lane = simpleos_platform_qemu_lane("riscv64", "riscv64-hosted")
if val lane = hosted_lane:
    expect hosted.memory == lane.qemu_memory
    expect hosted.qemu_extra == lane.qemu_extra
else:
    fail("missing riscv64 hosted lane")

val hosted_cmd = build_scenario_command(hosted, "build/os/simpleos_riscv64_hosted.elf")
expect hosted_cmd[0] == "qemu-system-riscv64"
expect (hosted_cmd).contains("build/os/simpleos_riscv64_hosted.elf")
expect (hosted_cmd).contains("user,id=n0,hostfwd=tcp::2222-:22,hostfwd=tcp::8080-:80")

val hosted_target = scenario_target(hosted)
expect hosted_target.entry == "examples/09_embedded/simple_os/arch/riscv64/hosted_entry.spl"
expect hosted_target.output == "build/os/simpleos_riscv64_hosted.elf"
```

</details>

#### uses per-platform x86_64 FAT32 media for filesystem scenarios

- uses per-platform x86_64 FAT32 media for filesystem scenarios


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("uses per-platform x86_64 FAT32 media for filesystem scenarios")
val scenario = get_scenario("x64-nvme-fat32")
expect scenario_name_or_missing("x64-nvme-fat32") == "x64-nvme-fat32"
if val resolved = scenario:
    val cmd = build_scenario_command(resolved, "build/os/simpleos_fs_test_32.elf")
    expect (cmd).contains("file=build/os/fat32-x86_64.img,if=none,id=nvm,format=raw")
```

</details>

#### keeps x64 desktop smoke diskless and leaves storage to the disk lane

- keeps x64 desktop smoke diskless and leaves storage to the disk lane


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps x64 desktop smoke diskless and leaves storage to the disk lane")
val smoke = get_scenario("x64-desktop-test")
expect scenario_name_or_missing("x64-desktop-test") == "x64-desktop-test"
if val resolved_smoke = smoke:
    val smoke_cmd = build_scenario_command(resolved_smoke, "build/os/simpleos_desktop_e2e_32.elf")
    expect command_contains_label(smoke_cmd, "drive=nvm") == "absent"
    expect command_contains_label(smoke_cmd, "-vga") == "present"

val disk = get_scenario("x64-desktop-disk")
expect scenario_name_or_missing("x64-desktop-disk") == "x64-desktop-disk"
if val resolved_disk = disk:
    val disk_cmd = build_scenario_command(resolved_disk, "build/os/simpleos_desktop_e2e_32.elf")
    expect (disk_cmd).contains("drive=nvm")
    expect (disk_cmd).contains("file=build/os/fat32-x86_64.img,if=none,id=nvm,format=raw")
```

</details>

#### defines a UEFI-native x64 desktop disk boot scenario

- defines a UEFI-native x64 desktop disk boot scenario


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("defines a UEFI-native x64 desktop disk boot scenario")
expect rt_env_set("SIMPLEOS_OVMF_CODE", "build/test-ovmf/OVMF_CODE.fd") == true
expect ovmf_code_path() == "build/test-ovmf/OVMF_CODE.fd"
val scenario = scenario_x64_desktop_uefi()
expect scenario.name == "x64-desktop-uefi"
expect scenario.arch == Architecture.X86_64
expect scenario_test_timeout_ms(scenario) == 180000

val resolved = get_scenario("x64-desktop-uefi")
if val s = resolved:
    val cmd = build_scenario_command(s, "build/os/simpleos_desktop_e2e_32.elf")
    expect cmd[0] == "qemu-system-x86_64"
    expect cmd.contains("-kernel") == false
    expect (cmd).contains("if=pflash,format=raw,readonly=on,file=build/test-ovmf/OVMF_CODE.fd")
    expect (cmd).contains("file=build/os/fat32-x86_64.img,if=none,id=uefidisk,format=raw")
    expect (cmd).contains("nvme,serial=uefi-desktop,drive=uefidisk")
else:
    expect "missing" == "x64-desktop-uefi"
```

</details>

#### builds a desktop UEFI validator command requiring structured FAT checks for migrated tool apps

- builds a desktop UEFI validator command requiring structured FAT checks for migrated tool apps


<details>
<summary>Executable SSpec</summary>

Runnable source: 35 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("builds a desktop UEFI validator command requiring structured FAT checks for migrated tool apps")
val cmd = desktop_uefi_disk_image_tool_app_validation_command("build/os/fat32-x86_64.img")
expect (cmd).contains("command -v mdir")
expect (cmd).contains("::/SYS/APPS/simple_browser")
expect (cmd).contains("::/SYS/APPS/simple_browser.smf")
expect (cmd).contains("::/SYS/APPS/simple_compiler")
expect (cmd).contains("::/SYS/APPS/simple_compiler.smf")
expect (cmd).contains("::/SYS/APPS/simple_interpreter")
expect (cmd).contains("::/SYS/APPS/simple_interpreter.smf")
expect (cmd).contains("::/SYS/APPS/simple_loader")
expect (cmd).contains("::/SYS/APPS/simple_loader.smf")
expect (cmd).contains("::/SYS/APPS/simple")
expect (cmd).contains("::/SYS/APPS/simple.smf")
expect (cmd).contains("::/SYS/APPS/llvm")
expect (cmd).contains("::/SYS/APPS/llvm.smf")
expect (cmd).contains("::/SYS/APPS/rust")
expect (cmd).contains("::/SYS/APPS/rust.smf")
expect (cmd).contains("::/SYS/LLVMVER.TXT")
expect (cmd).contains("::/SYS/RUSTVER.TXT")
expect (cmd).contains("mdir required; raw-image scan is diagnostic-only")
expect (cmd).contains("command -v mtype")
expect (cmd).contains("/sys/apps/simple_browser")
expect (cmd).contains("/sys/apps/simple_compiler")
expect (cmd).contains("/sys/apps/simple_interpreter")
expect (cmd).contains("/sys/apps/simple_loader")
expect (cmd).contains("/sys/apps/simple")
expect (cmd).contains("/sys/apps/llvm")
expect (cmd).contains("/sys/apps/rust")
expect (cmd).contains("grep -q '^SimpleOS LLVM standalone app v1'")
expect (cmd).contains("grep -q '^SimpleOS Rust standalone app v1'")
expect (cmd).contains("grep -q 'mode=native-filesystem-app'")
expect (cmd).contains("grep -q 'status=standalone-required'")
expect (cmd).contains("/usr/share/simpleos/toolchain/llvm/hello.ll")
expect (cmd).contains("/usr/share/simpleos/toolchain/rust/hello.rs")
```

</details>

#### defines a BGA WM Simple Web Engine2D scenario

- defines a BGA WM Simple Web Engine2D scenario


<details>
<summary>Executable SSpec</summary>

Runnable source: 27 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("defines a BGA WM Simple Web Engine2D scenario")
val scenario = scenario_x64_wm_simple_web_check()
expect scenario.name == "x64-wm-simple-web-check"
expect scenario.arch == Architecture.X86_64
expect scenario.memory == "2G"
expect (scenario.qemu_extra).contains("-vga")
expect (scenario.qemu_extra).contains("std")
expect scenario.qemu_extra.contains("virtio-gpu,disable-modern=on,disable-legacy=off") == false
expect scenario_test_timeout_ms(scenario) == 120000

expect scenario_name_or_missing("x64-wm-simple-web-check") == "x64-wm-simple-web-check"
val target = scenario_target(scenario)
expect target.entry == "examples/09_embedded/simple_os/arch/x86_64/gui_entry_engine2d.spl"
expect target.output == "build/os/simpleos_wm_simple_web_check_32.elf"
expect target.qemu_memory == "2G"

val direct_target = get_wm_simple_web_check_target()
expect direct_target.entry == target.entry

val cmd = build_scenario_command(scenario, target.output)
expect cmd[0] == "qemu-system-x86_64"
expect (cmd).contains("-kernel")
expect (cmd).contains("build/os/simpleos_wm_simple_web_check_32.elf")
expect (cmd).contains("-vga")
expect (cmd).contains("std")
expect cmd.contains("virtio-gpu,disable-modern=on,disable-legacy=off") == false
```

</details>

#### keeps headless targets on stdio and disables the display

- keeps headless targets on stdio and disables the display


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps headless targets on stdio and disables the display")
val target = get_target(Architecture.X86_64)
val cmd = build_qemu_command(target)
expect (cmd).contains("-no-user-config")
expect (cmd).contains("-monitor")
expect (cmd).contains("none")
expect (cmd).contains("-net")
expect (cmd).contains("-serial")
expect (cmd).contains("stdio")
expect (cmd).contains("-display")
expect (cmd).contains("none")
```

</details>

#### hardens x86_64 scenario launches without disabling explicit network scenarios

- hardens x86_64 scenario launches without disabling explicit network scenarios


<details>
<summary>Executable SSpec</summary>

Runnable source: 32 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("hardens x86_64 scenario launches without disabling explicit network scenarios")
val desktop = get_scenario("x64-desktop-test")
if val resolved_desktop = desktop:
    val desktop_cmd = build_scenario_command(resolved_desktop, "build/os/simpleos_desktop_e2e_32.elf")
    expect (desktop_cmd).contains("-no-user-config")
    expect (desktop_cmd).contains("-monitor")
    expect (desktop_cmd).contains("-net")
    expect (desktop_cmd).contains("none")
else:
    expect "missing" == "x64-desktop-test"

val net_user = scenario_x64_net_user()
val net_cmd = build_scenario_command(net_user, "build/os/simpleos_x86_64.elf")
expect (net_cmd).contains("-no-user-config")
expect (net_cmd).contains("-monitor")
expect (net_cmd).contains("-netdev")
expect net_cmd.contains("-net") == false

val ssh = scenario_x64_ssh()
expect scenario_name_or_missing("x64-ssh") == "x64-ssh"
val ssh_cmd = build_scenario_command(ssh, "build/os/simpleos_x86_64.elf")
expect (ssh_cmd).contains("user,id=n0,hostfwd=tcp::2222-:22")
expect ssh_cmd.contains("-net") == false
expect scenario_test_timeout_ms(ssh) == 120000
val ssh_target = scenario_target(ssh)
expect ssh_target.entry == get_ssh_live_target().entry
expect ssh_target.output == "build/os/simpleos_ssh_live_32.elf"

val ssh_probe_target = get_ssh_x25519_probe_target()
expect ssh_probe_target.entry == "examples/09_embedded/simple_os/arch/x86_64/ssh_x25519_probe_entry.spl"
expect ssh_probe_target.output == "build/os/simpleos_ssh_x25519_probe_32.elf"
```

</details>

#### keeps gui targets quiet by default

- keeps gui targets quiet by default


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps gui targets quiet by default")
val target = get_gui_target()
val cmd = build_qemu_command(target)
expect (cmd).contains("-no-user-config")
expect (cmd).contains("-monitor")
expect (cmd).contains("-net")
expect (cmd).contains("-serial")
expect (cmd).contains("none")
expect cmd.contains("stdio") == false
expect cmd.contains("-display") == false
```

</details>

#### enables gui serial output only in the explicit debug lane

- enables gui serial output only in the explicit debug lane


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("enables gui serial output only in the explicit debug lane")
val target = get_gui_target()
val cmd = build_qemu_command_with_options(target, qemu_run_options_debug_gui())
expect (cmd).contains("-serial")
expect (cmd).contains("stdio")
expect cmd.contains("-display") == false
```

</details>

### Qemu runner native-build prerequisites

#### passes entry closure for the x86_64 OS entry build

- passes entry closure for the x86_64 OS entry build


<details>
<summary>Executable SSpec</summary>

Runnable source: 21 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("passes entry closure for the x86_64 OS entry build")
val target = OsTarget(
    arch: Architecture.X86_64,
    entry: "examples/09_embedded/simple_os/arch/x86_64/os_entry.spl",
    linker_script: "examples/09_embedded/simple_os/arch/x86_64/linker.ld",
    target_triple: "x86_64-unknown-none",
    output: "build/os/simpleos_x86_64.elf",
    qemu_system: "qemu-system-x86_64",
    qemu_machine: "q35",
    qemu_cpu: "qemu64",
    qemu_memory: "128M",
    qemu_bios: "",
    qemu_extra: [],
    gui_mode: false
)
val args = os_native_build_args(target, "llvm")
expect (args).contains("--entry-closure")
expect (args).contains("--entry")
expect (args).contains("examples/09_embedded/simple_os/arch/x86_64/os_entry.spl")
expect args.contains("examples/09_embedded/simple_os/arch/x86_64/wm_entry.spl") == false
```

</details>

#### defaults RISC-V OS targets to LLVM native-build backend

- defaults RISC-V OS targets to LLVM native-build backend


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("defaults RISC-V OS targets to LLVM native-build backend")
expect default_os_build_backend_for_target(get_target(Architecture.Riscv32)) == "llvm"
expect default_os_build_backend_for_target(get_target(Architecture.Riscv64)) == "llvm"
expect default_os_build_backend_for_target(get_target(Architecture.Arm64)) == "llvm"
expect default_os_build_backend_for_target(get_target(Architecture.Arm32)) == "llvm"
```

</details>

#### explains when the selected compiler lacks the LLVM backend

- explains when the selected compiler lacks the LLVM backend


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("explains when the selected compiler lacks the LLVM backend")
val stderr = "LLVM backend requested but 'llvm' feature not enabled"
val hint = native_build_prerequisite_hint("llvm", stderr)
expect (hint).contains("Rust `llvm` feature")
expect (hint).contains("LLVM 18")
```

</details>

#### explains when cranelift cannot initialize a freestanding target

- explains when cranelift cannot initialize a freestanding target


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("explains when cranelift cannot initialize a freestanding target")
val stderr = "Compilation error: Support for this target has not been implemented yet"
val hint = native_build_prerequisite_hint("cranelift", stderr)
expect (hint).contains("Cranelift cannot build this freestanding target")
expect (hint).contains("LLVM-enabled")
```

</details>

#### adds the generated SSH live marker source to the live build source set

- adds the generated SSH live marker source to the live build source set


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("adds the generated SSH live marker source to the live build source set")
val sources = os_native_build_sources(get_ssh_live_target())
expect (sources).contains("src/os")
expect (sources).contains("src/lib")
expect (sources).contains("examples/09_embedded/simple_os")
expect (sources).contains("build/os/generated")
```

</details>

#### uses the baremetal bootstrap env for the SSH live target

- uses the baremetal bootstrap env for the SSH live target


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("uses the baremetal bootstrap env for the SSH live target")
val env_prefix = os_native_build_env_prefix(get_ssh_live_target(), "")
expect (env_prefix).contains("SIMPLE_BOOTSTRAP=1")
expect (env_prefix).contains("SIMPLE_LIB=\"$(pwd)/src\"")
```

</details>

#### uses the baremetal bootstrap env for the SSH X25519 probe target

- uses the baremetal bootstrap env for the SSH X25519 probe target


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("uses the baremetal bootstrap env for the SSH X25519 probe target")
val env_prefix = os_native_build_env_prefix(get_ssh_x25519_probe_target(), "")
expect (env_prefix).contains("SIMPLE_BOOTSTRAP=1")
expect (env_prefix).contains("SIMPLE_LIB=\"$(pwd)/src\"")
```

</details>

#### keeps RV64 hosted builds on minimal boot sources without freestanding unresolved-symbol stubs

- keeps RV64 hosted builds on minimal boot sources without freestanding unresolved-symbol stubs


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps RV64 hosted builds on minimal boot sources without freestanding unresolved-symbol stubs")
val hosted_target = scenario_target(scenario_riscv64_hosted())
val env_prefix = os_native_build_env_prefix(hosted_target, "")
expect (env_prefix).contains("SIMPLE_BOOT_MINIMAL=1")
expect env_prefix.contains("SIMPLE_ALLOW_FREESTANDING_STUBS=1") == false
```

</details>

#### keeps default RV64 boot builds on minimal boot sources without freestanding unresolved-symbol stubs

- keeps default RV64 boot builds on minimal boot sources without freestanding unresolved-symbol stubs


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps default RV64 boot builds on minimal boot sources without freestanding unresolved-symbol stubs")
val rv64_target = get_qemu_target(Architecture.Riscv64)
val rv64_args = os_native_build_args(rv64_target, "llvm")
val rv64_sources = os_native_build_sources(rv64_target)
val env_prefix = os_native_build_env_prefix(rv64_target, "")
expect (rv64_args).contains("--timeout")
expect (rv64_args).contains("180")
expect (rv64_args).contains("--clean")
expect rv64_sources[0] == "build/os/generated"
expect (rv64_sources).contains("src")
expect (rv64_sources).contains("examples")
expect (env_prefix).contains("SIMPLE_BOOT_MINIMAL=1")
expect (env_prefix).contains("SIMPLE_ALLOW_FREESTANDING_STUBS=1")
```

</details>

#### keeps ARM64 smoke builds on minimal boot sources without freestanding unresolved-symbol stubs

- keeps ARM64 smoke builds on minimal boot sources without freestanding unresolved-symbol stubs


<details>
<summary>Executable SSpec</summary>

Runnable source: 20 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps ARM64 smoke builds on minimal boot sources without freestanding unresolved-symbol stubs")
val smoke = OsTarget(
    arch: Architecture.Arm64,
    entry: "examples/09_embedded/simple_os/arch/arm64/smoke_entry.spl",
    linker_script: "examples/09_embedded/simple_os/arch/arm64/fs_exec_linker.ld",
    target_triple: "aarch64-unknown-none",
    output: "build/os/simpleos_arm64_smoke.elf",
    qemu_system: "qemu-system-aarch64",
    qemu_machine: "virt",
    qemu_cpu: "cortex-a72",
    qemu_memory: "128M",
    qemu_bios: "",
    qemu_extra: [],
    gui_mode: false
)
val env_prefix = os_native_build_env_prefix(smoke, "")
expect (env_prefix).contains("SIMPLE_BOOT_MINIMAL=1")
expect (env_prefix).contains("SIMPLE_LIB=\"$(pwd)/src\"")
expect env_prefix.contains("SIMPLE_ALLOW_FREESTANDING_STUBS=1") == false
```

</details>

#### keeps ARM32 smoke builds on minimal boot sources without freestanding unresolved-symbol stubs

- keeps ARM32 smoke builds on minimal boot sources without freestanding unresolved-symbol stubs


<details>
<summary>Executable SSpec</summary>

Runnable source: 20 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps ARM32 smoke builds on minimal boot sources without freestanding unresolved-symbol stubs")
val smoke = OsTarget(
    arch: Architecture.Arm32,
    entry: "examples/09_embedded/simple_os/arch/arm32/smoke_entry.spl",
    linker_script: "examples/09_embedded/simple_os/arch/arm32/fs_exec_linker.ld",
    target_triple: "armv7-none-eabi",
    output: "build/os/simpleos_arm32_smoke.elf",
    qemu_system: "qemu-system-arm",
    qemu_machine: "virt",
    qemu_cpu: "cortex-a15",
    qemu_memory: "128M",
    qemu_bios: "",
    qemu_extra: [],
    gui_mode: false
)
val env_prefix = os_native_build_env_prefix(smoke, "")
expect (env_prefix).contains("SIMPLE_BOOT_MINIMAL=1")
expect (env_prefix).contains("SIMPLE_LIB=\"$(pwd)/src\"")
expect env_prefix.contains("SIMPLE_ALLOW_FREESTANDING_STUBS=1") == false
```

</details>

#### keeps ARM64 fs-exec on minimal boot sources without freestanding unresolved-symbol stubs

- keeps ARM64 fs-exec on minimal boot sources without freestanding unresolved-symbol stubs


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps ARM64 fs-exec on minimal boot sources without freestanding unresolved-symbol stubs")
val fs_exec = scenario_target(scenario_arm64_virtio_fat32_smf())
val env_prefix = os_native_build_env_prefix(fs_exec, "")
expect (env_prefix).contains("SIMPLE_BOOT_MINIMAL=1")
expect (env_prefix).contains("SIMPLE_LIB=\"$(pwd)/src\"")
expect env_prefix.contains("SIMPLE_ALLOW_FREESTANDING_STUBS=1") == false
```

</details>

#### keeps ARM32 fs-exec on minimal boot sources without freestanding unresolved-symbol stubs

- keeps ARM32 fs-exec on minimal boot sources without freestanding unresolved-symbol stubs


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps ARM32 fs-exec on minimal boot sources without freestanding unresolved-symbol stubs")
val fs_exec = scenario_target(scenario_arm32_virtio_fat32_smf())
val env_prefix = os_native_build_env_prefix(fs_exec, "")
expect (env_prefix).contains("SIMPLE_BOOT_MINIMAL=1")
expect (env_prefix).contains("SIMPLE_LIB=\"$(pwd)/src\"")
expect env_prefix.contains("SIMPLE_ALLOW_FREESTANDING_STUBS=1") == false
```

</details>

#### keeps the checked-in generated fallback aligned with the enabled override template

- keeps the checked-in generated fallback aligned with the enabled override template


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps the checked-in generated fallback aligned with the enabled override template")
val fallback = rt_file_read_text("src/generated/simpleos_log_config.spl")
expect fallback == _simpleos_log_config_source(true)
```

</details>

#### does not reuse a stale SSH live artifact after a failed build

- does not reuse a stale SSH live artifact after a failed build


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not reuse a stale SSH live artifact after a failed build")
val target = get_ssh_live_target()
expect rt_dir_create_all("build/os") == true
expect rt_file_write_text(target.output, "stale-kernel") == true
expect rt_file_exists(target.output) == true
expect rt_env_set("SIMPLE_BINARY", install_failing_simple_shim("build/test-qemu-failing-simple/stale", 42)) == true

val built = build_os_with_backend(target, "cranelift")

expect built == false
expect rt_file_exists(target.output) == false
expect rt_env_set("SIMPLE_BINARY", "bin/simple") == true
```

</details>

#### cleans up the generated log config override after a failed build

- cleans up the generated log config override after a failed build


<details>
<summary>Executable SSpec</summary>

Runnable source: 21 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("cleans up the generated log config override after a failed build")
val target = get_ssh_live_target()
val config_path = "build/os/generated/generated/simpleos_log_config.spl"
val original_path = rt_env_get("PATH") ?? ""
val original_marker = rt_env_get("SIMPLE_SSH_LIVE_BUILD_MARKER") ?? ""
expect rt_env_set("PATH", "/tmp/simple-path-before-build") == true
expect rt_env_set("SIMPLE_BINARY", install_failing_simple_shim("build/test-qemu-failing-simple/log-config", 43)) == true
expect rt_env_set("SIMPLE_OS_LOG_MODE", "off") == true
expect rt_env_set("SIMPLE_SSH_LIVE_BUILD_MARKER", "prior-marker") == true

val built = build_os_with_backend(target, "cranelift")

expect built == false
expect rt_file_exists(config_path) == false
expect rt_env_get("PATH") ?? "" == "/tmp/simple-path-before-build"
expect rt_env_get("SIMPLE_SSH_LIVE_BUILD_MARKER") ?? "" == "prior-marker"
expect rt_env_set("SIMPLE_OS_LOG_MODE", "on") == true
expect rt_env_set("SIMPLE_BINARY", "bin/simple") == true
expect rt_env_set("SIMPLE_SSH_LIVE_BUILD_MARKER", original_marker) == true
expect rt_env_set("PATH", original_path) == true
```

</details>

#### cleans up the generated log config override after a prepare-stage failure

- cleans up the generated log config override after a prepare-stage failure


<details>
<summary>Executable SSpec</summary>

Runnable source: 20 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("cleans up the generated log config override after a prepare-stage failure")
val target = get_ssh_live_target()
val config_path = "build/os/generated/generated/simpleos_log_config.spl"
val marker_path = "build/os/generated/ssh_live_build_marker.spl"
val original_marker = rt_env_get("SIMPLE_SSH_LIVE_BUILD_MARKER") ?? ""
expect rt_env_set("SIMPLE_BINARY", "bin/simple") == true
expect rt_env_set("SIMPLE_OS_LOG_MODE", "off") == true
expect rt_env_set("SIMPLE_SSH_LIVE_BUILD_MARKER", "prepare-prior-marker") == true
expect shell_exit_code("rm -rf \"" + marker_path + "\"") == 0
expect rt_dir_create_all(marker_path) == true

val built = build_os_with_backend(target, "cranelift")

expect built == false
expect rt_file_exists(config_path) == false
expect shell_exit_code("rm -rf \"" + marker_path + "\"") == 0
expect rt_env_get("SIMPLE_SSH_LIVE_BUILD_MARKER") ?? "" == "prepare-prior-marker"
expect rt_env_set("SIMPLE_SSH_LIVE_BUILD_MARKER", original_marker) == true
expect rt_env_set("SIMPLE_OS_LOG_MODE", "on") == true
```

</details>

#### restores the prior SSH live marker and PATH on native-build failure

- restores the prior SSH live marker and PATH on native-build failure


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("restores the prior SSH live marker and PATH on native-build failure")
val target = get_ssh_live_target()
val original_marker = rt_env_get("SIMPLE_SSH_LIVE_BUILD_MARKER") ?? ""
val original_path = rt_env_get("PATH") ?? ""
val original_simple_binary = rt_env_get("SIMPLE_BINARY") ?? ""
expect rt_env_set("SIMPLE_SSH_LIVE_BUILD_MARKER", "tooling-prior-marker") == true
expect rt_env_set("SIMPLE_BINARY", install_failing_simple_shim("build/test-qemu-failing-simple/tooling", 44)) == true
expect rt_env_set("SIMPLE_OS_LOG_MODE", "off") == true
expect rt_env_set("PATH", "/tmp/simple-path-tooling-failure") == true

val built = build_os_with_backend(target, "cranelift")

expect built == false
expect rt_env_get("SIMPLE_SSH_LIVE_BUILD_MARKER") ?? "" == "tooling-prior-marker"
expect rt_env_set("SIMPLE_SSH_LIVE_BUILD_MARKER", original_marker) == true
expect rt_env_set("SIMPLE_OS_LOG_MODE", "on") == true
expect rt_env_set("SIMPLE_BINARY", original_simple_binary) == true
expect rt_env_set("PATH", original_path) == true
```

</details>

### Qemu runner release artifacts

#### prefers release disk images before platform and legacy build fixtures

- prefers release disk images before platform and legacy build fixtures


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("prefers release disk images before platform and legacy build fixtures")
val candidates = desktop_disk_image_candidates()
expect (candidates[0]).starts_with("release/")
expect candidates[1] == "build/os/fat32-x86_64.img"
expect candidates[2] == "build/os/fat32.img"
```

</details>

#### materializes the desktop disk lane at the platform image path

- materializes the desktop disk lane at the platform image path


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("materializes the desktop disk lane at the platform image path")
val args = desktop_disk_make_script_args("build/os/fat32-x86_64.img")
expect args[0] == "scripts/os/make_os_disk.shs"
expect args[1] == "64"
expect args[2] == "build/os/fat32-x86_64.img"
```

</details>

#### resolves the release disk path when the release artifact exists

- resolves the release disk path when the release artifact exists


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("resolves the release disk path when the release artifact exists")
val release_root = "build/test-qemu-release-artifacts"
val release_path = desktop_release_disk_image_path_in(release_root)
rt_dir_create_all("{release_root}/x86_64/images")
expect rt_file_write_text(release_path, "disk-image") == true
expect desktop_disk_image_path_in(release_root) == release_path
```

</details>

#### exposes the future installer ISO path hook

- exposes the future installer ISO path hook


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("exposes the future installer ISO path hook")
val iso_path = desktop_release_installer_iso_path()
expect (iso_path).ends_with(".iso")
val candidates = desktop_installer_iso_candidates()
expect candidates[0] == iso_path
```

</details>

### Qemu runner board bundle smoke

#### materializes representative board bundle outputs through deterministic host-side shims

- materializes representative board bundle outputs through deterministic host-side shims
   - Expected: riscv64_cmd equals `[`


<details>
<summary>Executable SSpec</summary>

Runnable source: 46 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("materializes representative board bundle outputs through deterministic host-side shims")
val root = "build/test-qemu-board-bundle-smoke"
val kernel_path = root + "/kernel.elf"
val x64_output = root + "/x64-board.img"
val arm64_output = root + "/arm64-board.img"
val riscv64_output = root + "/riscv64-board"
expect rt_dir_create_all(root) == true
expect rt_file_write_text(kernel_path, "kernel") == true

val x64_cmd = board_bundle_command("x86_64", kernel_path, x64_output)
expect x64_cmd[0] == "/bin/sh"
expect x64_cmd[1] == "-c"
expect (x64_cmd[2]).contains("scripts/os/make_os_disk.shs")
expect (x64_cmd[2]).contains("x86_64")
expect x64_cmd[3] == "simpleos-uefi-disk"
expect x64_cmd[4] == kernel_path
expect x64_cmd[5] == x64_output

val arm64_cmd = board_bundle_command("arm64", kernel_path, arm64_output)
expect arm64_cmd == ["/bin/sh", "scripts/os/make_os_disk.shs", "64", arm64_output, "", "arm64"]

val riscv64_cmd = board_bundle_command("riscv64", kernel_path, riscv64_output)
expect(riscv64_cmd).to_equal([
    "bin/simple", "run", "src/hardware/fpga_linux/generate_riscv_fpga_bundle.spl",
    "--", "--board=mlk_s02_100t", riscv64_output
])

val setup_ok = install_board_bundle_tooling_shims(root)
var x64_ok = false
var arm64_ok = false
var riscv64_ok = false
if setup_ok:
    x64_ok = ensure_board_bundle("x86_64", kernel_path, x64_output)
    arm64_ok = ensure_board_bundle("arm64", kernel_path, arm64_output)
    riscv64_ok = ensure_board_bundle("riscv64", kernel_path, riscv64_output)
val restore_ok = restore_board_bundle_tooling(root)

expect setup_ok == true
expect restore_ok == true
expect x64_ok == true
expect arm64_ok == true
expect riscv64_ok == true
expect rt_file_exists(x64_output) == true
expect rt_file_exists(arm64_output) == true
expect rt_file_exists(riscv64_output + "/board_linux_boot_products.sdn") == true
```

</details>

### Qemu runner board execution smoke

#### runs the representative riscv64 board lane through the generated-linux wrapper

- runs the representative riscv64 board lane through the generated-linux wrapper
   - Expected: cmd equals `[`


<details>
<summary>Executable SSpec</summary>

Runnable source: 30 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("runs the representative riscv64 board lane through the generated-linux wrapper")
val root = "build/test-qemu-board-lane-smoke"
val kernel_path = root + "/kernel.elf"
val riscv64_output = root + "/riscv64-board"
expect rt_dir_create_all(root) == true
expect rt_file_write_text(kernel_path, "kernel") == true

val cmd = board_lane_test_command("riscv64", riscv64_output)
expect(cmd).to_equal([
    "/bin/sh",
    "scripts/mlk_s02_100t_generated_linux.shs",
    "--arch=rv64",
    "--bundle-root=" + riscv64_output,
    "--product-root=" + riscv64_output + "/products"
])
expect board_lane_test_command("x86_64", root).len() == 4
expect board_lane_test_command("arm64", root).len() == 4

val setup_ok = install_board_bundle_tooling_shims(root)
var riscv64_ok = false
if setup_ok:
    riscv64_ok = test_board_lane("riscv64", kernel_path, riscv64_output)
val restore_ok = restore_board_bundle_tooling(root)

expect setup_ok == true
expect restore_ok == true
expect riscv64_ok == true
expect rt_file_exists(riscv64_output + "/board_linux_boot_products.sdn") == true
expect rt_file_exists(riscv64_output + "/products/run.log") == true
```

</details>

### Qemu runner desktop UEFI tool app validator

#### requires mdir and mtype checks for the migrated tool apps and CLI catalog

- requires mdir and mtype checks for the migrated tool apps and CLI catalog


<details>
<summary>Executable SSpec</summary>

Runnable source: 33 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("requires mdir and mtype checks for the migrated tool apps and CLI catalog")
val cmd = desktop_uefi_disk_image_tool_app_validation_command("build/os/fat32-x86_64.img")
expect (cmd).contains("if command -v mdir >/dev/null 2>&1; then")
expect (cmd).contains("if command -v mtype >/dev/null 2>&1; then")
expect (cmd).contains("::/SYS/APPS/info")
expect (cmd).contains("::/SYS/APPS/info.smf")
expect (cmd).contains("::/SYS/APPS/SIMPLECLI.CAT")
expect (cmd).contains("grep -q '^info|src/app/info/main.spl|smoke|staged$'")
expect_tool_app_validation_reference(cmd, "simple_browser", "SBROWSMF", "/sys/apps/simple_browser")
expect_tool_app_validation_reference(cmd, "simple_compiler", "SCOMPSMF", "/sys/apps/simple_compiler")
expect_tool_app_validation_reference(cmd, "simple_interpreter", "SINTSMF", "/sys/apps/simple_interpreter")
expect_tool_app_validation_reference(cmd, "simple_loader", "SLOADSMF", "/sys/apps/simple_loader")
expect (cmd).contains("::/SYS/APPS/simple")
expect (cmd).contains("::/SYS/APPS/simple.smf")
expect (cmd).contains("mdir required; raw-image scan is diagnostic-only")
expect (cmd).contains("mtype required; raw-image scan is diagnostic-only")
expect_tool_app_validation_reference(cmd, "llvm", "LLVMSMF", "/sys/apps/llvm")
expect_tool_app_validation_reference(cmd, "clang", "CLANGSMF", "/sys/apps/clang")
expect_tool_app_validation_reference(cmd, "rust", "RUSTSMF", "/sys/apps/rust")
expect (cmd).contains("::/SYS/LLVMMAN.TXT")
expect (cmd).contains("::/SYS/CLANGMAN.TXT")
expect (cmd).contains("::/SYS/RUSTMAN.TXT")
expect (cmd).contains("::/SYS/TOOLSET.SDN")
expect (cmd).contains("grep -q 'mode=native-filesystem-app'")
expect (cmd).contains("grep -q 'lane=x86_64-uefi-hardware'")
expect (cmd).contains("grep -q 'status=standalone-required'")
expect (cmd).contains("grep -q 'lane = \"x86_64-uefi-hardware\"'")
expect (cmd).contains("grep -q '/sys/apps/clang status=standalone-required'")
expect (cmd).contains("/usr/share/simpleos/toolchain/clang/pipeline.step")
expect (cmd).contains("/usr/share/simpleos/toolchain/llvm/pipeline.step")
expect (cmd).contains("/usr/share/simpleos/toolchain/rust/pipeline.step")
expect (cmd).contains("grep -a -q 'SMF'")
```

</details>

#### requires clang in the x64 desktop live acceptance marker set

- requires clang in the x64 desktop live acceptance marker set


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("requires clang in the x64 desktop live acceptance marker set")
val markers = desktop_uefi_required_marker_fragments()
expect (markers).contains("[desktop-e2e] process-backed:ok app=clang pid=")
expect (markers).contains("[desktop-e2e] native-toolchain-launch:ok app=clang lane=x86_64-uefi-hardware mode=native-filesystem-app status=standalone-required tool=/sys/apps/clang manifest=/SYS/CLANGMAN.TXT")
expect (markers).contains("[desktop-e2e] native-capability:ok app=clang capability=local-c-source-inspection proof=/usr/share/simpleos/toolchain/clang/hello.c")
expect (markers).contains("[desktop-e2e] native-capability:ok app=clang capability=driver-flag-inspection proof=/usr/share/simpleos/toolchain/clang/flags.rsp")
expect (markers).contains("[desktop-e2e] native-capability:ok app=clang capability=compile-pipeline-step proof=/usr/share/simpleos/toolchain/clang/pipeline.step")
```

</details>

#### requires validated container namespace and rootfs markers in x64 desktop acceptance

- requires validated container namespace and rootfs markers in x64 desktop acceptance


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("requires validated container namespace and rootfs markers in x64 desktop acceptance")
val container_markers = desktop_container_marker_fragments()
val uefi_markers = desktop_uefi_required_marker_fragments()
expect container_markers == ["[desktop-e2e] container-namespace:ok", "[desktop-e2e] container-rootfs:ok"]
expect (uefi_markers).contains("[desktop-e2e] container-namespace:ok")
expect (uefi_markers).contains("[desktop-e2e] container-rootfs:ok")
```

</details>

#### requires Wine hello to be VFS-backed and spawned in x64 desktop acceptance

- requires Wine hello to be VFS-backed and spawned in x64 desktop acceptance


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("requires Wine hello to be VFS-backed and spawned in x64 desktop acceptance")
val markers = desktop_uefi_required_marker_fragments()
expect (markers).contains("[desktop-e2e] vfs-app-read:ok source=generic-vfs path=/sys/apps/wine_hello bytes=")
expect (markers).contains("[fs-exec] spawn:image path=/sys/apps/wine_hello")
expect (markers).contains("[desktop-e2e] process-backed:ok app=wine_hello pid=")
```

</details>

#### defines the extra Wine executable-environment markers needed before readiness claims

- defines the extra Wine executable-environment markers needed before readiness claims


<details>
<summary>Executable SSpec</summary>

Runnable source: 26 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("defines the extra Wine executable-environment markers needed before readiness claims")
val markers = desktop_wine_exec_env_required_marker_fragments()
expect (markers).contains("[fs-exec] spawn:image path=/sys/apps/wine_hello")
expect (markers).contains("[desktop-e2e] process-backed:ok app=wine_hello pid=")
expect (markers).contains("[desktop-e2e] container-namespace:ok")
expect (markers).contains("[desktop-e2e] container-rootfs:ok")

val serial = "QEMU x86_64 desktop lane\n" +
    "[phase-3-mount] fat32 ok\n" +
    "[kernel] syscall-abi:ok arch=x86_64\n" +
    "[kernel] scheduler:ok runqueue=user\n" +
    "[kernel] vfs-service:ok root=/\n" +
    "[vfs] mounted fat32 device=nvme0 volume=simpleos\n" +
    "[fs-exec] spawn:image path=/sys/apps/wine_hello entry=0 segments=1 stub_bytes=128 argv_count=1 env_count=0\n" +
    "[desktop-e2e] process-backed:ok app=wine_hello pid=42\n" +
    "[desktop-e2e] wm:ok pid=42 wid=1001\n" +
    "[desktop-e2e] network-smoke:bounded ok packets=1\n" +
    "[desktop-e2e] container-namespace:ok pid fs ipc net capability\n" +
    "[desktop-e2e] container-rootfs:ok rootfs=/containers/wine rootfs_backend=nvfs\n" +
    "[desktop-e2e] mdsoc-capsule:ok owner=process-container-wm\n" +
    "[desktop-e2e] mdsoc-public-port:ok facade=exec-env\n" +
    "[desktop-e2e] mdsoc-resident-state-owner:ok ecs=wm,process,container\n" +
    "TEST PASSED"
expect desktop_wine_exec_env_marker_contract_accepts(serial) == true
expect desktop_wine_exec_env_marker_contract_accepts(serial.replace("[desktop-e2e] container-rootfs:ok", "[desktop-e2e] container-rootfs:missing")) == false
```

</details>

#### captures the x64 desktop disk probe terminal serial pass marker

- captures the x64 desktop disk probe terminal serial pass marker


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("captures the x64 desktop disk probe terminal serial pass marker")
val root = "build/test-x64-desktop-disk-probe"
val serial = root + "/serial.log"
expect rt_dir_create_all(root) == true
val (stdout, _stderr, exit_code) = run_x64_desktop_disk_probe(
    ["/bin/sh", "-c", "'printf \"boot\\\\nTEST PASSED\\\\n\"'"],
    serial,
    1500)
expect exit_code == 0
expect (stdout).contains("TEST PASSED")
```

</details>

#### requires clang in the riscv64 hosted acceptance marker set

- requires clang in the riscv64 hosted acceptance marker set


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("requires clang in the riscv64 hosted acceptance marker set")
val markers = riscv64_hosted_required_marker_fragments()
expect (markers).contains("[desktop-e2e] process-backed:ok app=clang pid=")
expect (markers).contains("[desktop-e2e] native-toolchain-launch:ok app=clang lane=riscv64-hosted mode=native-filesystem-app status=standalone-required tool=/sys/apps/clang manifest=/SYS/CLANGMAN.TXT")
expect (markers).contains("HOSTED_FS_TOOLCHAIN_READY arch=riscv64 apps=simple_compiler,simple_loader,llvm,clang,rust")
val lane = simpleos_platform_qemu_lane("riscv64", "riscv64-hosted")
if val resolved_lane = lane:
    expect markers == resolved_lane.required_serial_markers
else:
    fail("missing riscv64 hosted lane")
```

</details>

#### rejects raw-image validation when mdir and mtype are unavailable even when catalog markers are present

- rejects raw-image validation when mdir and mtype are unavailable even when catalog markers are present


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rejects raw-image validation when mdir and mtype are unavailable even when catalog markers are present")
val root = "build/test-qemu-tool-app-validator"
val bin_dir = root + "/bin"
val img_path = root + "/desktop-uefi-tools.raw"
expect rt_dir_create_all(bin_dir) == true
expect shell_exit_code("ln -sf /usr/bin/grep \"" + bin_dir + "/grep\"") == 0
expect shell_exit_code("ln -sf /usr/bin/strings \"" + bin_dir + "/strings\"") == 0
expect rt_file_write_text(img_path, desktop_tool_app_fallback_fixture()) == true

val cmd = "PATH=\"" + bin_dir + "\"; export PATH; " + desktop_uefi_disk_image_tool_app_validation_command(img_path)
expect (shell_status_output(cmd)).contains("__exit=1")
```

</details>

#### rejects the raw-image fallback when the CLI catalog marker is missing

- rejects the raw-image fallback when the CLI catalog marker is missing


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rejects the raw-image fallback when the CLI catalog marker is missing")
val root = "build/test-qemu-tool-app-validator-missing"
val bin_dir = root + "/bin"
val img_path = root + "/desktop-uefi-tools.raw"
expect rt_dir_create_all(bin_dir) == true
expect shell_exit_code("ln -sf /usr/bin/grep \"" + bin_dir + "/grep\"") == 0
expect shell_exit_code("ln -sf /usr/bin/strings \"" + bin_dir + "/strings\"") == 0
expect rt_file_write_text(img_path, desktop_tool_app_fallback_fixture().replace("info|src/app/info/main.spl|smoke|staged\n", "")) == true

val cmd = "PATH=\"" + bin_dir + "\"; export PATH; " + desktop_uefi_disk_image_tool_app_validation_command(img_path)
expect (shell_status_output(cmd)).contains("__exit=1")
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Hardware & OS |
| Status | Active |
| Source | `test/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering Qemu runner serial routing, Qemu runner native-build prerequisites, Qemu runner release artifacts, Qemu runner board bundle smoke, Qemu runner board execution smoke, Qemu runner desktop UEFI tool app validator.
- Qemu runner serial routing
- Qemu runner native-build prerequisites
- Qemu runner release artifacts
- Qemu runner board bundle smoke
- Qemu runner board execution smoke
- Qemu runner desktop UEFI tool app validator

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 65 |
| Active scenarios | 65 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-UNIT`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `fd7fd7b2643875d8899b5f96c8911956279fe9ae2e9bd5f564696317892c5949`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `fd7fd7b2643875d8899b5f96c8911956279fe9ae2e9bd5f564696317892c5949`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `fd7fd7b2643875d8899b5f96c8911956279fe9ae2e9bd5f564696317892c5949`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **91/100**; effective score: **91/100**; blockers: **0**.

SSpec documentization score: 91/100
source: test/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.spl
mirror: doc/06_spec/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.md (current)
findings: 6 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=55
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, assumptions/preconditions, primary workflow
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.spl:253:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'does not accept non-x86 QEMU exit code 1 as success' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.spl:264:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'keeps isa-debug-exit success limited to x86 scenarios' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/os/.spipe_wrapped_entry_qemu_runner_spec.spl:276:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'exposes a runner-facing protection serial acceptance gate' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
