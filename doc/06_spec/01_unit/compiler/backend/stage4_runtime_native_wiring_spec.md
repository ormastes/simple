# Stage4 runtime-native wiring

Mirror of `test/01_unit/compiler/backend/stage4_runtime_native_wiring_spec.spl`.

The executable SSpec verifies weak Vulkan availability fallbacks, localization of legacy dynamic-loader owners before inventory, and resolution of current owners before exact selected-archive projection.

It checks wiring and ordering in source rather than exercising a live Vulkan loader.
