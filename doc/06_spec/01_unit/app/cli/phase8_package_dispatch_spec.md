# Phase 8 Package Command Dispatch

- Executable: `test/01_unit/app/cli/phase8_package_dispatch_spec.spl`
- Requirements: `KPM-REQ-009`, `KPM-REQ-014`
- Evidence class: executable SPipe definition; no execution result is embedded.

## Scenarios

- routes update through both canonical and runtime command tables
- routes lock through both canonical and runtime command tables
- passes only command-tail arguments from the production root CLI

## Freshness

The requirement IDs and scenario titles mirror the executable source. No
runtime PASS is claimed.
