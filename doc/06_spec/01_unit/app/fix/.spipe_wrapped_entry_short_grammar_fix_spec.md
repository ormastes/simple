# .spipe Wrapped Entry Short Grammar Fix Specification

> Tests covering short grammar easy fix.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 35 | 35 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# .spipe Wrapped Entry Short Grammar Fix Specification

## Scenarios

### short grammar easy fix

#### rewrites explicit method-call lambda to method reference

- rewrites explicit method-call lambda to method reference


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites explicit method-call lambda to method reference")
val source = "fn sample():\n    val lengths = words.map(\\w: w.len())\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "&:len"
```

</details>

#### applies method reference replacement in memory

- applies method reference replacement in memory


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("applies method reference replacement in memory")
val source = "fn sample():\n    val lengths = words.map(\\w: w.len())\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
var sources: Dict<text, text> = {}
sources["sample.spl"] = source
val result = FixApplicator.apply(fixes, sources)
match result:
    case Ok(updated):
        expect (updated["sample.spl"]).contains("words.map(&:len)")
    case Err(e):
        expect e == ""
```

</details>

#### rewrites no-arg receiver method expressions to placeholder grammar

- rewrites no-arg receiver method expressions to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites no-arg receiver method expressions to placeholder grammar")
val source = "fn sample():\n    val adjusted = words.map(\\w: w.len() + 1)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1.len() + 1"
```

</details>

#### rewrites arithmetic lambdas to placeholder grammar

- rewrites arithmetic lambdas to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites arithmetic lambdas to placeholder grammar")
val source = "fn sample():\n    val doubled = nums.map(\\x: x * 2)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1 * 2"
```

</details>

#### rewrites repeated single parameter uses to numbered placeholder grammar

- rewrites repeated single parameter uses to numbered placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites repeated single parameter uses to numbered placeholder grammar")
val source = "fn sample():\n    val squares = nums.map(\\x: x * x)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1 * _1"
```

</details>

#### does not rewrite bare identity lambdas to a standalone placeholder

- does not rewrite bare identity lambdas to a standalone placeholder


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not rewrite bare identity lambdas to a standalone placeholder")
val source = "fn sample():\n    val same = atom.swap(\\x: x)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 0
```

</details>

#### rewrites predicate lambdas to placeholder grammar

- rewrites predicate lambdas to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites predicate lambdas to placeholder grammar")
val source = "fn sample():\n    val evens = nums.filter(\\x: x % 2 == 0)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1 % 2 == 0"
```

</details>

#### rewrites compact predicate lambdas to placeholder grammar

- rewrites compact predicate lambdas to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites compact predicate lambdas to placeholder grammar")
val source = "fn sample():\n    val twos = nums.filter(\\x: x==2)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1==2"
```

</details>

#### rewrites compact arithmetic lambdas to placeholder grammar

- rewrites compact arithmetic lambdas to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites compact arithmetic lambdas to placeholder grammar")
val source = "fn sample():\n    val next = nums.map(\\x: x+1)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1+1"
```

</details>

#### rewrites direct callback argument lambdas to placeholder grammar

- rewrites direct callback argument lambdas to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites direct callback argument lambdas to placeholder grammar")
val source = "fn sample():\n    val missing = set_filter(s, \\x: x == \"Charlie\")\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1 == \"Charlie\""
```

</details>

#### is registered in all fix rules

- is registered in all fix rules


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("is registered in all fix rules")
val source = "fn sample():\n    val trimmed = parts.map(\\p: p.trim())\n"
val fixes = collect_fixes_from_source("sample.spl", source)
var found = false
for fix in fixes:
    val reps = easyfix_replacements(fix)
    if reps.len() > 0 and reps[0].new_text == "&:trim":
        found = true
expect found == true
```

</details>

#### registers placeholder rewrites in all fix rules

- registers placeholder rewrites in all fix rules


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("registers placeholder rewrites in all fix rules")
val source = "fn sample():\n    val doubled = nums.map(\\x: x * 2)\n"
val fixes = collect_fixes_from_source("sample.spl", source)
var found = false
for fix in fixes:
    val reps = easyfix_replacements(fix)
    if reps.len() > 0 and reps[0].new_text == "_1 * 2":
        found = true
expect found == true
```

</details>

#### rewrites receiver method predicates to placeholder grammar

- rewrites receiver method predicates to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites receiver method predicates to placeholder grammar")
val source = "fn sample():\n    val evens = values.filter(\\x: x.parse_int() % 2 == 0)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1.parse_int() % 2 == 0"
```

</details>

#### rewrites free function predicates to placeholder grammar

- rewrites free function predicates to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites free function predicates to placeholder grammar")
val source = "fn sample():\n    val evens = values.filter(\\x: int(x) % 2 == 0)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "int(_1) % 2 == 0"
```

</details>

#### rewrites single argument free function transforms to placeholder grammar

- rewrites single argument free function transforms to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites single argument free function transforms to placeholder grammar")
val source = "fn sample():\n    val doubled = values.map(\\x: double(x))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "double(_1)"
```

</details>

#### rewrites simple field access transforms to placeholder grammar

- rewrites simple field access transforms to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites simple field access transforms to placeholder grammar")
val source = "fn sample():\n    val names = items.map(\\item: item.name)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1.name"
```

</details>

#### does not rewrite direct field-access predicates

- does not rewrite direct field-access predicates


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not rewrite direct field-access predicates")
val source = "fn sample():\n    val stale = rows.filter(\\row: row.violation_type == \"StaleRunning\")\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 0
```

</details>

#### does not rewrite functional update arrow callbacks

- does not rewrite functional update arrow callbacks


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not rewrite functional update arrow callbacks")
val source = "fn sample():\n    arr->map(\\x: x * 2)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 0
```

</details>

#### rewrites quoted literal comparisons to placeholder grammar

- rewrites quoted literal comparisons to placeholder grammar


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites quoted literal comparisons to placeholder grammar")
val source = "fn sample():\n    val has_json = args.any(\\arg: arg == \"--json\")\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1 == \"--json\""
```

</details>

#### rewrites indexed predicates with param-free index expressions

- rewrites indexed predicates with param-free index expressions


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites indexed predicates with param-free index expressions")
val source = "fn sample():\n    val open = rows.filter(\\row: row[self.table.column_index(\"status\")] == status)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1[self.table.column_index(\"status\")] == status"
```

</details>

#### rewrites indexed receiver method predicates

- rewrites indexed receiver method predicates


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites indexed receiver method predicates")
val source = "fn sample():\n    val names = rows.filter(\\row: row[0].starts_with(\"A\"))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1[0].starts_with(\"A\")"
```

</details>

#### rewrites repeated list argument predicates

- rewrites repeated list argument predicates


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites repeated list argument predicates")
val source = "fn sample():\n    cond.add_condition(\\args: args.len() > 0 and args[0] == \"GET\", \"retrieve\")\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1.len() > 0 and _1[0] == \"GET\""
```

</details>

#### does not rewrite string interpolation transforms until parser support exists

- does not rewrite string interpolation transforms until parser support exists


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not rewrite string interpolation transforms until parser support exists")
val open = "{"
val close = "}"
val source = "fn sample():\n    val text = bytes.map(\\b: \"" + open + "b" + close + "\")\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 0
```

</details>

#### rewrites receiver calls with param-free explicit arguments

- rewrites receiver calls with param-free explicit arguments


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites receiver calls with param-free explicit arguments")
val source = "fn sample():\n    val values = items.map(\\x: x.adjust(1) + 1)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_1.adjust(1) + 1"
```

</details>

#### rewrites free function calls with param-free additional arguments

- rewrites free function calls with param-free additional arguments


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites free function calls with param-free additional arguments")
val source = "fn sample():\n    val values = items.map(\\x: add(x, 1))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "add(_1, 1)"
```

</details>

#### rewrites method calls where placeholder is not the first argument

- rewrites method calls where placeholder is not the first argument


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites method calls where placeholder is not the first argument")
val source = "fn sample():\n    val found = params.any(\\p: self.occurs_check(id, p))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "self.occurs_check(id, _1)"
```

</details>

#### rewrites captured forwarding calls

- rewrites captured forwarding calls


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites captured forwarding calls")
val source = "fn sample(source: text, file: text):\n    for_each_line(source, \\ctx: _check_short_grammar_line(ctx, file))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "_check_short_grammar_line(_1, file)"
```

</details>

#### rewrites callback field receiver forwarding calls

- rewrites callback field receiver forwarding calls


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites callback field receiver forwarding calls")
val source = "fn sample():\n    Ok(RemoteExecutionManager.new(read_register_fn: \\reg: self.read_register(reg)))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "self.read_register(_1)"
```

</details>

#### rewrites nested call arguments that use the callback parameter

- rewrites nested call arguments that use the callback parameter


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites nested call arguments that use the callback parameter")
val source = "fn sample():\n    val result = Promise.resolved(3).then(\\x: Promise.resolved(x + 4))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "Promise.resolved(_1 + 4)"
```

</details>

#### rewrites nested host future callbacks

- rewrites nested host future callbacks


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites nested host future callbacks")
val source = "fn sample():\n    val result = HostFuture.ready(5).then(\\x: HostFuture.ready(x + 8))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "HostFuture.ready(_1 + 8)"
```

</details>

#### rewrites nested receiver no-arg method call arguments

- rewrites nested receiver no-arg method call arguments


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites nested receiver no-arg method call arguments")
val source = "fn sample():\n    val block = define_block(\"csv\", LexerMode.Raw, \\p: Ok(p.trim()))\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "Ok(_1.trim())"
```

</details>

#### rewrites unary not field predicates

- rewrites unary not field predicates


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("rewrites unary not field predicates")
val source = "fn sample():\n    val local = symbols.filter(\\sym: not sym.is_imported)\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 1
val reps = easyfix_replacements(fixes[0])
expect reps[0].new_text == "not _1.is_imported"
```

</details>

#### does not rewrite tuple parameter interpolation transforms until parser support exists

- does not rewrite tuple parameter interpolation transforms until parser support exists


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not rewrite tuple parameter interpolation transforms until parser support exists")
val open = "{"
val close = "}"
val source = "fn sample():\n    val parts = fields.map(\\(name, value): \"" + open + "name" + close + ": " + open + "value" + close + "\")\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 0
```

</details>

#### does not rewrite tuple parameter interpolation with calls until parser support exists

- does not rewrite tuple parameter interpolation with calls until parser support exists


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("does not rewrite tuple parameter interpolation with calls until parser support exists")
val open = "{"
val close = "}"
val source = "fn sample():\n    val parts = fields.map(\\(name, pattern): \"" + open + "name" + close + ": " + open + "pattern_to_text(pattern)" + close + "\")\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
expect fixes.len() == 0
```

</details>

#### applies replacements after non-ascii text

- applies replacements after non-ascii text


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("applies replacements after non-ascii text")
val source = "# unicode before fix — keeps text offsets honest\nfn sample():\n    val values = items.map(\\x: x.trim())\n"
val fixes = check_short_grammar_refactor(source, "sample.spl")
var sources: Dict<text, text> = {}
sources["sample.spl"] = source
val result = FixApplicator.apply(fixes, sources)
match result:
    case Ok(updated):
        expect (updated["sample.spl"]).contains("items.map(&:trim)")
        expect (updated["sample.spl"]).contains("# unicode before fix — keeps text offsets honest")
    case Err(e):
        expect e == ""
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | Active |
| Source | `test/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering short grammar easy fix.
- short grammar easy fix

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 35 |
| Active scenarios | 35 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-UNIT`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `8656af4521956f73782cbede6ed067c8da9845f3ada77b8c0baf166172208989`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `8656af4521956f73782cbede6ed067c8da9845f3ada77b8c0baf166172208989`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `8656af4521956f73782cbede6ed067c8da9845f3ada77b8c0baf166172208989`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **91/100**; effective score: **91/100**; blockers: **0**.

SSpec documentization score: 91/100
source: test/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.spl
mirror: doc/06_spec/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.md (current)
findings: 6 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=55
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, scope, assumptions/preconditions, primary workflow, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.spl:67:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'rewrites explicit method-call lambda to method reference' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.spl:76:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'applies method reference replacement in memory' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/fix/.spipe_wrapped_entry_short_grammar_fix_spec.spl:90:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'rewrites no-arg receiver method expressions to placeholder grammar' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
