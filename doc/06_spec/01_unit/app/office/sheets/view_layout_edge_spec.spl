# view_layout_edge_spec

> Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 22 | 22 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# view_layout_edge_spec

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | Active |
| Source | `test/01_unit/app/office/sheets/view_layout_edge_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## Operator workflow

1. Run `bin/simple test test/01_unit/app/office/sheets/view_layout_edge_spec.spl`.
2. Every scenario must pass; a failure is a regression in the behavior under test.

## Compatibility and limitations

Covers the behavior asserted here; platform-specific behavior is out of scope.

## Scenarios

### freeze panes: rejected input
_Invalid freeze counts are rejected and leave the layout unchanged._

#### rejects a negative row count

- Verify: rejects a negative row count
   - Expected: result.ok is false
   - Expected: result.reason equals `negative freeze count`
   - Expected: frozen_extent(result.layout) equals `R0C0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects a negative row count")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val result = freeze_panes(sheet, default_view_layout(), 0 - 1, 0)
expect(result.ok).to_equal(false)
expect(result.reason).to_equal("negative freeze count")
expect(frozen_extent(result.layout)).to_equal("R0C0")
```

</details>

#### rejects a negative column count

- Verify: rejects a negative column count
   - Expected: result.ok is false
   - Expected: has_frozen_panes(result.layout) is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects a negative column count")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val result = freeze_panes(sheet, default_view_layout(), 0, 0 - 5)
expect(result.ok).to_equal(false)
expect(has_frozen_panes(result.layout)).to_equal(false)
```

</details>

#### rejects freezing every row, leaving nothing to scroll

- Verify: rejects freezing every row, leaving nothing to scroll
   - Expected: result.ok is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects freezing every row, leaving nothing to scroll")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val result = freeze_panes(sheet, default_view_layout(), sheet.row_count, 0)
expect(result.ok).to_equal(false)
```

</details>

#### rejects freezing every column, leaving nothing to scroll

- Verify: rejects freezing every column, leaving nothing to scroll
   - Expected: result.ok is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects freezing every column, leaving nothing to scroll")
var sheet = Sheet.new("S1")
val result = freeze_panes(sheet, default_view_layout(), 0, sheet.col_count)
expect(result.ok).to_equal(false)
```

</details>

#### rejects a row count beyond the sheet bounds

- Verify: rejects a row count beyond the sheet bounds
   - Expected: result.ok is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: rejects a row count beyond the sheet bounds")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val result = freeze_panes(sheet, default_view_layout(), sheet.row_count + 10, 0)
expect(result.ok).to_equal(false)
```

</details>

#### preserves an existing freeze when a new one is rejected

- Verify: preserves an existing freeze when a new one is rejected
   - Expected: bad.ok is false
   - Expected: frozen_extent(bad.layout) equals `R2C2`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: preserves an existing freeze when a new one is rejected")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val good = freeze_panes(sheet, default_view_layout(), 2, 2).layout
val bad = freeze_panes(sheet, good, 0 - 1, 0)
expect(bad.ok).to_equal(false)
expect(frozen_extent(bad.layout)).to_equal("R2C2")
```

</details>

### freeze panes: boundary and no-op cases
_Zero counts, the last legal count, and repeated unfreezing._

#### accepts a zero freeze as an explicit unfreeze

- Verify: accepts a zero freeze as an explicit unfreeze
   - Expected: result.ok is true
   - Expected: has_frozen_panes(result.layout) is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts a zero freeze as an explicit unfreeze")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val frozen = freeze_panes(sheet, default_view_layout(), 3, 3).layout
val result = freeze_panes(sheet, frozen, 0, 0)
expect(result.ok).to_equal(true)
expect(has_frozen_panes(result.layout)).to_equal(false)
```

</details>

#### accepts the largest count that still leaves one scrollable line

- Verify: accepts the largest count that still leaves one scrollable line
   - Expected: result.ok is true


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: accepts the largest count that still leaves one scrollable line")
var sheet = Sheet.new("S1")
val result = freeze_panes(sheet, default_view_layout(), sheet.row_count - 1, sheet.col_count - 1)
expect(result.ok).to_equal(true)
```

</details>

#### unfreezing when nothing is frozen is a no-op

- Verify: unfreezing when nothing is frozen is a no-op
   - Expected: has_frozen_panes(cleared) is false
   - Expected: frozen_extent(cleared) equals `R0C0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: unfreezing when nothing is frozen is a no-op")
val cleared = unfreeze_panes(default_view_layout())
expect(has_frozen_panes(cleared)).to_equal(false)
expect(frozen_extent(cleared)).to_equal("R0C0")
```

</details>

#### treats an unparseable reference as not frozen

- Verify: treats an unparseable reference as not frozen
   - Expected: is_frozen_ref(layout, "not-a-ref") is false
   - Expected: is_frozen_ref(layout, "") is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: treats an unparseable reference as not frozen")
var sheet = Sheet.new("S1")
val layout = freeze_panes(sheet, default_view_layout(), 5, 5).layout
expect(is_frozen_ref(layout, "not-a-ref")).to_equal(false)
expect(is_frozen_ref(layout, "")).to_equal(false)
```

</details>

### print area: rejected input
_Malformed and out-of-bounds ranges are rejected._

#### rejects a single cell reference with no colon

- Verify: rejects a single cell reference with no colon
   - Expected: result.ok is false
   - Expected: has_print_area(result.layout) is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects a single cell reference with no colon")
var sheet = Sheet.new("S1")
val result = set_print_area(sheet, default_view_layout(), "A1")
expect(result.ok).to_equal(false)
expect(has_print_area(result.layout)).to_equal(false)
```

</details>

#### rejects an empty range string

- Verify: rejects an empty range string
   - Expected: set_print_area(sheet, default_view_layout(), "").ok is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects an empty range string")
var sheet = Sheet.new("S1")
expect(set_print_area(sheet, default_view_layout(), "").ok).to_equal(false)
```

</details>

#### rejects a garbage range

- Verify: rejects a garbage range
   - Expected: set_print_area(sheet, default_view_layout(), "1A:!!").ok is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects a garbage range")
var sheet = Sheet.new("S1")
expect(set_print_area(sheet, default_view_layout(), "1A:!!").ok).to_equal(false)
```

</details>

#### rejects a range past the sheet column bound

- Verify: rejects a range past the sheet column bound
   - Expected: result.ok is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects a range past the sheet column bound")
var sheet = Sheet.new("S1")
val result = set_print_area(sheet, default_view_layout(), "A1:ZZ5")
expect(result.ok).to_equal(false)
```

</details>

#### rejects a range past the sheet row bound

- Verify: rejects a range past the sheet row bound
   - Expected: result.ok is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: rejects a range past the sheet row bound")
var sheet = Sheet.new("S1")
val result = set_print_area(sheet, default_view_layout(), "A1:B5000")
expect(result.ok).to_equal(false)
```

</details>

#### preserves an existing print area when a new one is rejected

- Verify: preserves an existing print area when a new one is rejected
   - Expected: bad.ok is false
   - Expected: bad.layout.print_area equals `A1:C3`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: preserves an existing print area when a new one is rejected")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val good = set_print_area(sheet, default_view_layout(), "A1:C3").layout
val bad = set_print_area(sheet, good, "A1:B5000")
expect(bad.ok).to_equal(false)
expect(bad.layout.print_area).to_equal("A1:C3")
```

</details>

### print area: normalization and no-ops
_Inverted ranges normalize; clearing an unset area is safe._

#### normalizes a fully inverted range

- Verify: normalizes a fully inverted range
   - Expected: result.ok is true
   - Expected: result.layout.print_area equals `A1:C3`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: normalizes a fully inverted range")
var sheet = Sheet.new("S1")
val result = set_print_area(sheet, default_view_layout(), "C3:A1")
expect(result.ok).to_equal(true)
expect(result.layout.print_area).to_equal("A1:C3")
```

</details>

#### normalizes a corner-swapped range per axis

- Verify: normalizes a corner-swapped range per axis
   - Expected: result.ok is true
   - Expected: result.layout.print_area equals `A1:C3`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: normalizes a corner-swapped range per axis")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val result = set_print_area(sheet, default_view_layout(), "C1:A3")
expect(result.ok).to_equal(true)
expect(result.layout.print_area).to_equal("A1:C3")
```

</details>

#### accepts a degenerate single-cell range

- Verify: accepts a degenerate single-cell range
   - Expected: result.ok is true
   - Expected: result.layout.print_area equals `B2:B2`
   - Expected: print_area_refs(result.layout).len() equals `1`


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts a degenerate single-cell range")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val result = set_print_area(sheet, default_view_layout(), "B2:B2")
expect(result.ok).to_equal(true)
expect(result.layout.print_area).to_equal("B2:B2")
expect(print_area_refs(result.layout).len()).to_equal(1)
```

</details>

#### accepts a lowercase range and stores it uppercased

- Verify: accepts a lowercase range and stores it uppercased
   - Expected: result.ok is true
   - Expected: result.layout.print_area equals `A1:B2`


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: accepts a lowercase range and stores it uppercased")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val result = set_print_area(sheet, default_view_layout(), "a1:b2")
expect(result.ok).to_equal(true)
expect(result.layout.print_area).to_equal("A1:B2")
```

</details>

#### clearing when nothing is set is a no-op

- Verify: clearing when nothing is set is a no-op
   - Expected: has_print_area(cleared) is false
   - Expected: print_area_refs(cleared).len() equals `0`


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: clearing when nothing is set is a no-op")
val cleared = clear_print_area(default_view_layout())
expect(has_print_area(cleared)).to_equal(false)
expect(print_area_refs(cleared).len()).to_equal(0)
```

</details>

#### clearing twice stays cleared

- Verify: clearing twice stays cleared
   - Expected: has_print_area(clear_print_area(clear_print_area(layout))) is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: clearing twice stays cleared")
# @req: REQ-SSPEC-LOCAL-001
var sheet = Sheet.new("S1")
val layout = set_print_area(sheet, default_view_layout(), "A1:C3").layout
expect(has_print_area(clear_print_area(clear_print_area(layout)))).to_equal(false)
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 22 |
| Active scenarios | 22 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `e020a9ae2d483a2d17830ae9a044bfa0c803ce3f3d5a48517c6eef8955c68613`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `e020a9ae2d483a2d17830ae9a044bfa0c803ce3f3d5a48517c6eef8955c68613`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `e020a9ae2d483a2d17830ae9a044bfa0c803ce3f3d5a48517c6eef8955c68613`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **88/100**; effective score: **88/100**; blockers: **0**.

SSpec documentization score: 88/100
source: test/01_unit/app/office/sheets/view_layout_edge_spec.spl
mirror: doc/06_spec/01_unit/app/office/sheets/view_layout_edge_spec.md (current)
findings: 6 blockers: 0
  narrative=100 structure=100 oracle=80
  traceability=100 evidence=70 coverage=100 maintainability=70
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/app/office/sheets/view_layout_edge_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/app/office/sheets/view_layout_edge_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: assumptions/preconditions, traceability, evidence, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/app/office/sheets/view_layout_edge_spec.spl:1:1: advice SSDOC-ORA-003 [oracle] (-20): 2 unexplained numeric expected value(s)
  why: Reviewers need to know why a magic expected value is authoritative.
  improve: Name the authoritative expected value or add a '# oracle:' explanation.
test/01_unit/app/office/sheets/view_layout_edge_spec.spl:42:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'rejects a negative row count' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/office/sheets/view_layout_edge_spec.spl:51:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'rejects a negative column count' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/office/sheets/view_layout_edge_spec.spl:59:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'rejects freezing every row, leaving nothing to scroll' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
