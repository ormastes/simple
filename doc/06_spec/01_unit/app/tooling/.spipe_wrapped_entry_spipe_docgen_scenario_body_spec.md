# .spipe Wrapped Entry Spipe Docgen Scenario Body Specification

> Tests covering spipe docgen scenario body extraction.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 24 | 24 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# .spipe Wrapped Entry Spipe Docgen Scenario Body Specification

## Scenarios

### spipe docgen scenario body extraction

#### renders scenario bodies as dedented Simple code fences

- renders scenario bodies as dedented Simple code fences


<details>
<summary>Executable SSpec</summary>

Runnable source: 17 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("renders scenario bodies as dedented Simple code fences")
val source = "describe \"Math Block Arithmetic\":\n" +
    "    it \"evaluates addition\":\n" +
    "        val result = m{ 2 + 3 }\n" +
    "        expect(result).to_equal(5)\n"
val structure = extract_test_structure(source)
expect structure != "x"
expect (structure).contains("#### evaluates addition")
expect(structure).not_to_contain("### evaluates addition")
expect (structure).contains("```simple\nval result = m{ 2 + 3 }\nexpect(result).to_equal(5)\n```")
expect (structure).contains("<summary>Rendered scenario source</summary>")
expect (structure).contains("> val result = $2 + 3$<br>\n> expect(result).to_equal(5)")
expect(structure).not_to_contain("Rendered `m{...}`")
expect(structure).not_to_contain("**Plain text**")
expect(structure).not_to_contain("<summary>Raw scenario source</summary>")
expect(structure).not_to_contain("```latex")
```

<details>
<summary>Rendered scenario source</summary>

> # @req REQ-SSPEC-APP<br>
> step("renders scenario bodies as dedented Simple code fences")<br>
> val source = "describe \"Math Block Arithmetic\":\n" +<br>
>     "    it \"evaluates addition\":\n" +<br>
>     "        val result = $2 + 3$\n" +<br>
>     "        expect(result).to_equal(5)\n"<br>
> val structure = extract_test_structure(source)<br>
> expect structure != "x"<br>
> expect (structure).contains("#### evaluates addition")<br>
> expect(structure).not_to_contain("### evaluates addition")<br>
> expect (structure).contains("```simple\nval result = $2 + 3$\nexpect(result).to_equal(5)\n```")<br>
> expect (structure).contains("<summary>Rendered scenario source</summary>")<br>
> expect (structure).contains("> val result = $2 + 3$<br>\n> expect(result).to_equal(5)")<br>
> expect(structure).not_to_contain("Rendered `$?$`")<br>
> expect(structure).not_to_contain("**Plain text**")<br>
> expect(structure).not_to_contain("<summary>Raw scenario source</summary>")<br>
> expect(structure).not_to_contain("```latex")

</details>

</details>

#### strips leading scenario docstrings from rendered code

- strips leading scenario docstrings from rendered code


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("strips leading scenario docstrings from rendered code")
val source = "describe \"Docstring bodies\":\n" +
    "    it \"keeps runnable code only\":\n" +
    "        \"\"\"\n" +
    "        Explains the scenario.\n" +
    "        \"\"\"\n" +
    "        val result = 1\n" +
    "        expect(result).to_equal(1)\n"
val structure = extract_test_structure(source)
expect (structure).contains("#### keeps runnable code only")
expect (structure).contains("```simple\nval result = 1\nexpect(result).to_equal(1)\n```")
expect(structure).not_to_contain("Explains the scenario.")
```

</details>

#### does not emit a code fence for single-line pass scenarios

- does not emit a code fence for single-line pass scenarios


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("does not emit a code fence for single-line pass scenarios")
val source = "describe \"Empty bodies\":\n" +
    "    it \"has no multiline body\": pass\n"
val structure = extract_test_structure(source)
expect (structure).contains("#### has no multiline body")
expect(structure).not_to_contain("```simple")
expect(structure).not_to_contain("Raw scenario source")
```

</details>

#### renders skip_it scenarios with skipped marker and body

- renders skip_it scenarios with skipped marker and body


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("renders skip_it scenarios with skipped marker and body")
val source = "describe \"Skipped scenarios\":\n" +
    "    skip_it \"documents underscore skipped scenarios\":\n" +
    "        expect(\"reason\").to_equal(\"reason\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("#### documents underscore skipped scenarios _(skipped)_")
expect (structure).contains("```simple\nexpect(\"reason\").to_equal(\"reason\")\n```")
```

</details>

#### folds scenarios marked as advanced manual content

- folds scenarios marked as advanced manual content


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("folds scenarios marked as advanced manual content")
val source = "describe \"Manual visibility\":\n" +
    "    # @manual: folded\n" +
    "    it \"documents an edge case without dominating the manual\":\n" +
    "        expect(\"edge\").to_equal(\"edge\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("<details>")
expect (structure).contains("<summary>Advanced: documents an edge case without dominating the manual</summary>")
expect (structure).contains("#### documents an edge case without dominating the manual")
expect (structure).contains("```simple\nexpect(\"edge\").to_equal(\"edge\")\n```")
```

</details>

#### skips scenarios marked as hidden manual content

- skips scenarios marked as hidden manual content
   - Expected: structure).not_to_contain("expect(\"helper\" equals `"helper")"`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("skips scenarios marked as hidden manual content")
val source = "describe \"Manual visibility\":\n" +
    "    # @manual: skip\n" +
    "    it \"keeps helper-only checks out of the manual\":\n" +
    "        expect(\"helper\").to_equal(\"helper\")\n" +
    "    it \"keeps primary scenario visible\":\n" +
    "        expect(\"primary\").to_equal(\"primary\")\n"
val structure = extract_test_structure(source)
expect(structure).not_to_contain("keeps helper-only checks out of the manual")
expect(structure).not_to_contain("expect(\"helper\").to_equal(\"helper\")")
expect (structure).contains("#### keeps primary scenario visible")
```

</details>

#### folds all scenarios when file manual metadata is folded

- folds all scenarios when file manual metadata is folded


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("folds all scenarios when file manual metadata is folded")
val source = "# @manual-file: folded\n" +
    "describe \"Manual visibility\":\n" +
    "    it \"keeps detailed file collapsed\":\n" +
    "        expect(\"detail\").to_equal(\"detail\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("<summary>Advanced: keeps detailed file collapsed</summary>")
expect (structure).contains("#### keeps detailed file collapsed")
```

</details>

#### skips all scenarios when file manual metadata is skip

- skips all scenarios when file manual metadata is skip
   - Expected: structure).not_to_contain("expect(\"helper\" equals `"helper")"`


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("skips all scenarios when file manual metadata is skip")
val source = "# @manual-file: skip\n" +
    "describe \"Manual visibility\":\n" +
    "    it \"keeps generated helper file out of the manual\":\n" +
    "        expect(\"helper\").to_equal(\"helper\")\n"
val structure = extract_test_structure(source)
expect(structure).not_to_contain("keeps generated helper file out of the manual")
expect(structure).not_to_contain("expect(\"helper\").to_equal(\"helper\")")
```

</details>

#### lets scenario manual metadata override folded file default

- lets scenario manual metadata override folded file default


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("lets scenario manual metadata override folded file default")
val source = "# @manual-file: folded\n" +
    "describe \"Manual visibility\":\n" +
    "    # @manual: show\n" +
    "    it \"shows the primary flow in a detailed file\":\n" +
    "        expect(\"primary\").to_equal(\"primary\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("#### shows the primary flow in a detailed file")
expect(structure).not_to_contain("<summary>Advanced: shows the primary flow in a detailed file</summary>")
```

</details>

#### folds scenarios from folder manual config

- folds scenarios from folder manual config


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("folds scenarios from folder manual config")
file_write("build/sspec-manual.sdn", "manual: folded\n")
val source = "describe \"Folder visibility\":\n" +
    "    it \"keeps folder detail collapsed\":\n" +
    "        expect(\"folder\").to_equal(\"folder\")\n"
val structure = extract_test_structure_with_path(source, "build/folder_visibility_spec.spl")
expect (structure).contains("#### keeps folder detail collapsed")
expect (structure).contains("<summary>Advanced: keeps folder detail collapsed</summary>")
```

</details>

#### hides inline setup scenarios from top-level manual output

- hides inline setup scenarios from top-level manual output
   - Expected: structure).not_to_contain("expect(\"open\" equals `"open")"`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("hides inline setup scenarios from top-level manual output")
val source = "describe \"Inline setup\":\n" +
    "    # @inline\n" +
    "    it \"app is open\":\n" +
    "        expect(\"open\").to_equal(\"open\")\n" +
    "    it \"user continues from setup\":\n" +
    "        expect(\"continue\").to_equal(\"continue\")\n"
val structure = extract_test_structure(source)
expect(structure).not_to_contain("app is open")
expect(structure).not_to_contain("expect(\"open\").to_equal(\"open\")")
expect (structure).contains("#### user continues from setup")
```

</details>

#### expands prev inline setup without rendering Previous label

- expands prev inline setup without rendering Previous label


<details>
<summary>Executable SSpec</summary>

Runnable source: 14 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("expands prev inline setup without rendering Previous label")
val source = "describe \"Inline setup\":\n" +
    "    # @inline\n" +
    "    it \"app is open\":\n" +
    "        user.open_app()\n" +
    "    # @prev(\"app is open\")\n" +
    "    it \"user logs in\":\n" +
    "        user.enter_login(\"demo\")\n"
val structure = extract_test_structure(source)
expect(structure).not_to_contain("#### app is open")
expect (structure).contains("#### user logs in")
expect (structure).contains("```simple\nuser.open_app()\nuser.enter_login(\"demo\")\n```")
expect(structure).not_to_contain("Previous:")
```

</details>

#### expands bare prev from the nearest previous scenario

- expands bare prev from the nearest previous scenario


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("expands bare prev from the nearest previous scenario")
val source = "describe \"Previous setup\":\n" +
    "    # @inline\n" +
    "    it \"server is running\":\n" +
    "        admin.start_server()\n" +
    "    # @prev\n" +
    "    it \"operator sends request\":\n" +
    "        operator.send_request()\n"
val structure = extract_test_structure(source)
expect(structure).not_to_contain("#### server is running")
expect (structure).contains("#### operator sends request")
expect (structure).contains("```simple\nadmin.start_server()\noperator.send_request()\n```")
```

</details>

#### expands include scenario body at the call site

- expands include scenario body at the call site


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("expands include scenario body at the call site")
val source = "describe \"Inline include\":\n" +
    "    # @inline\n" +
    "    it \"user confirms dialog\":\n" +
    "        user.confirm_dialog()\n" +
    "    it \"user completes checkout\":\n" +
    "        user.add_item()\n" +
    "        # @include(\"user confirms dialog\")\n" +
    "        user.finish_checkout()\n"
val structure = extract_test_structure(source)
expect(structure).not_to_contain("#### user confirms dialog")
expect (structure).contains("#### user completes checkout")
expect (structure).contains("```simple\nuser.add_item()\nuser.confirm_dialog()\nuser.finish_checkout()\n```")
expect(structure).not_to_contain("# @include")
```

</details>

#### renders warning for missing prev scenario target

- renders warning for missing prev scenario target


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("renders warning for missing prev scenario target")
val source = "describe \"Missing previous\":\n" +
    "    # @prev(\"app is open\")\n" +
    "    it \"user logs in\":\n" +
    "        user.enter_login(\"demo\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("**Manual warnings:**\n- missing prev scenario: app is open")
expect(structure).not_to_contain("# @manual-warning")
```

</details>

#### renders warning for missing include target and removes metadata from source

- renders warning for missing include target and removes metadata from source


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("renders warning for missing include target and removes metadata from source")
val source = "describe \"Missing include\":\n" +
    "    it \"user finishes flow\":\n" +
    "        user.start_flow()\n" +
    "        # @include(\"dialog confirmed\")\n" +
    "        user.finish_flow()\n"
val structure = extract_test_structure(source)
expect (structure).contains("**Manual warnings:**\n- missing include scenario: dialog confirmed")
expect (structure).contains("```simple\nuser.start_flow()\nuser.finish_flow()\n```")
expect(structure).not_to_contain("# @include")
```

</details>

#### folds slow scenarios by default but allows manual show override

- folds slow scenarios by default but allows manual show override


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("folds slow scenarios by default but allows manual show override")
val source = "describe \"Slow manual visibility\":\n" +
    "    slow_it \"keeps stress detail folded\":\n" +
    "        expect(\"stress\").to_equal(\"stress\")\n" +
    "    # @manual: show\n" +
    "    slow_it \"shows important slow operator flow\":\n" +
    "        expect(\"operator\").to_equal(\"operator\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("<summary>Advanced: keeps stress detail folded</summary>")
expect (structure).contains("#### keeps stress detail folded _(slow)_")
expect (structure).contains("#### shows important slow operator flow _(slow)_")
expect(structure).not_to_contain("<summary>Advanced: shows important slow operator flow</summary>")
```

</details>

#### renders bare capture as after step tui scenario evidence

- renders bare capture as after step tui scenario evidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("renders bare capture as after step tui scenario evidence")
val source = "describe \"Capture policy\":\n" +
    "    # @capture\n" +
    "    it \"captures app after every step\":\n" +
    "        user.open_app()\n" +
    "        expect(\"open\").to_equal(\"open\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("#### captures app after every step")
expect (structure).contains("**Scenario capture:** tui after_step")
expect (structure).contains("```simple\nuser.open_app()\nexpect(\"open\").to_equal(\"open\")\n```")
```

</details>

#### renders explicit capture kind and mode from enum-like metadata

- renders explicit capture kind and mode from enum-like metadata


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("renders explicit capture kind and mode from enum-like metadata")
val source = "describe \"Capture policy\":\n" +
    "    # @capture(after_scenario, gui)\n" +
    "    it \"captures the final rendered window\":\n" +
    "        user.finish_flow()\n" +
    "        expect(\"done\").to_equal(\"done\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("**Scenario capture:** gui after_scenario")
```

</details>

#### renders step capture metadata without keeping metadata in source code

- renders step capture metadata without keeping metadata in source code


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("renders step capture metadata without keeping metadata in source code")
val source = "describe \"Capture policy\":\n" +
    "    it \"captures request evidence for submit\":\n" +
    "        user.open_app()\n" +
    "        # @capture(api)\n" +
    "        user.submit_login()\n" +
    "        expect(\"ok\").to_equal(\"ok\")\n"
val structure = extract_test_structure(source)
expect (structure).contains("**Step captures:**\n- api after_step before `user.submit_login()`")
expect (structure).contains("```simple\nuser.open_app()\nuser.submit_login()\nexpect(\"ok\").to_equal(\"ok\")\n```")
expect(structure).not_to_contain("# @capture(api)")
```

</details>

#### captures the math_blocks golden scenario body

- captures the math_blocks golden scenario body


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("captures the math_blocks golden scenario body")
val source = read_file("test/feature/usage/math_blocks_spec.spl")
val structure = extract_test_structure(source)
expect (structure).contains("#### evaluates addition")
expect (structure).contains("```simple\nval result = m{ 2 + 3 }\nexpect(result).to_equal(5)\n```")
expect (structure).contains("#### evaluates sqrt of 16")
expect (structure).contains("> val result = $\\sqrt{16}$<br>\n> expect(result).to_equal(4.0)")
```

<details>
<summary>Rendered scenario source</summary>

> # @req REQ-SSPEC-APP<br>
> step("captures the math_blocks golden scenario body")<br>
> val source = read_file("test/feature/usage/math_blocks_spec.spl")<br>
> val structure = extract_test_structure(source)<br>
> expect (structure).contains("#### evaluates addition")<br>
> expect (structure).contains("```simple\nval result = $2 + 3$\nexpect(result).to_equal(5)\n```")<br>
> expect (structure).contains("#### evaluates sqrt of 16")<br>
> expect (structure).contains("> val result = $\\sqrt{16}$<br>\n> expect(result).to_equal(4.0)")

</details>

</details>

#### generates through the pure Simple generator path

- generates through the pure Simple generator path


<details>
<summary>Executable SSpec</summary>

Runnable source: 22 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("generates through the pure Simple generator path")
val parsed_result = parse_spipe_file("test/feature/usage/math_blocks_spec.spl")
match parsed_result:
    case Err(e):
        expect e == ""
    case Ok(parsed):
        val metadata = extract_metadata(parsed)
        val doc = SspecDoc(
            file_path: parsed.file_path,
            raw_content: parsed.raw_content,
            doc_blocks: parsed.doc_blocks,
            feature_title: parsed.feature_title,
            feature_ids: parsed.feature_ids,
            metadata: metadata
        )
        val result = generate_feature_doc(doc, "/tmp")
        match result:
            case Err(e):
                expect e == ""
            case Ok(path):
                expect (path).contains("/feature/usage/math_blocks_spec.md")
```

</details>

#### mirrors the source spec folder under the output directory

- mirrors the source spec folder under the output directory


<details>
<summary>Executable SSpec</summary>

Runnable source: 22 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("mirrors the source spec folder under the output directory")
val parsed_result = parse_spipe_file("test/feature/usage/math_blocks_spec.spl")
match parsed_result:
    case Err(e):
        expect e == ""
    case Ok(parsed):
        val metadata = extract_metadata(parsed)
        val doc = SspecDoc(
            file_path: parsed.file_path,
            raw_content: parsed.raw_content,
            doc_blocks: parsed.doc_blocks,
            feature_title: parsed.feature_title,
            feature_ids: parsed.feature_ids,
            metadata: metadata
        )
        val result = generate_feature_doc(doc, "/tmp")
        match result:
            case Err(e):
                expect e == ""
            case Ok(path):
                expect path == "/tmp/feature/usage/math_blocks_spec.md"
```

</details>

#### embeds screenshot and TUI capture evidence in generated docs

- embeds screenshot and TUI capture evidence in generated docs


<details>
<summary>Executable SSpec</summary>

Runnable source: 41 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-APP
step("embeds screenshot and TUI capture evidence in generated docs")
val tui_path = "/tmp/spipe_docgen_ui_capture.txt"
file_write(tui_path, "Simple UI\n[OK] Ready\n")
val metadata = FeatureMetadata(
    id: "ui.capture",
    category: "Application",
    difficulty: "Medium",
    status: "Active",
    doc_type: "SSPEC",
    source_doc: "",
    requirement: "N/A",
    plan: "N/A",
    design: "N/A",
    research: "N/A",
    related: [],
    dependencies: [],
    artifacts: [],
    screenshots: ["doc/06_spec/image/feature/app/ui_capture_spec/main.png", "N/A"],
    tui_captures: [tui_path, "doc/06_spec/image/feature/app/ui_capture_spec/tui.png"],
    logs: []
)
val doc = SspecDoc(
    file_path: "test/feature/app/ui_capture_spec.spl",
    raw_content: "describe \"UI Capture\":\n    it \"captures the visible state\":\n        expect captured_state == \"visible\"\n",
    doc_blocks: [],
    feature_title: "UI Capture",
    feature_ids: ["ui.capture"],
    metadata: metadata
)
val result = generate_feature_doc(doc, "/tmp")
match result:
    case Err(e):
        expect e == ""
    case Ok(path):
        val generated = read_file(path)
        expect (generated).contains("![Screenshots: main.png](doc/06_spec/image/feature/app/ui_capture_spec/main.png)")
        expect (generated).contains("![TUI Captures: tui.png](doc/06_spec/image/feature/app/ui_capture_spec/tui.png)")
        expect (generated).contains("<summary>spipe_docgen_ui_capture.txt</summary>")
        expect (generated).contains("Simple UI")
        expect(generated).not_to_contain("`N/A`")
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | Active |
| Source | `test/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering spipe docgen scenario body extraction.
- spipe docgen scenario body extraction

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 24 |
| Active scenarios | 24 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-APP`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `ff39e5091fb3feeeb018291137c2c42b251c4cc8ac23db8719aa1365e370e0bd`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `ff39e5091fb3feeeb018291137c2c42b251c4cc8ac23db8719aa1365e370e0bd`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `ff39e5091fb3feeeb018291137c2c42b251c4cc8ac23db8719aa1365e370e0bd`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **92/100**; effective score: **92/100**; blockers: **0**.

SSpec documentization score: 92/100
source: test/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.spl
mirror: doc/06_spec/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.md (current)
findings: 5 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=70
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, scope, assumptions/preconditions, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.spl:23:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'renders scenario bodies as dedented Simple code fences' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.spl:42:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'strips leading scenario docstrings from rendered code' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/tooling/.spipe_wrapped_entry_spipe_docgen_scenario_body_spec.spl:57:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'does not emit a code fence for single-line pass scenarios' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
