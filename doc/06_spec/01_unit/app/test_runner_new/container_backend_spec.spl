# container_backend_spec

> Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 10 | 10 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# container_backend_spec

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## At a Glance

| Field | Value |
|-------|-------|
| Category | Application |
| Status | Active |
| Source | `test/01_unit/app/test_runner_new/container_backend_spec.spl` |
| Updated | 2026-08-22 |
| Generator | `simple spipe-docgen` (Simple) |

## Purpose and audience

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## Operator workflow

1. Run `bin/simple test test/01_unit/app/test_runner_new/container_backend_spec.spl`.
2. Every scenario must pass; a failure is a regression in the behavior under test.

## Compatibility and limitations

Covers the behavior asserted here; platform-specific behavior is out of scope.

## Scenarios

### container_detect_runtime

#### detects docker when available

- Verify: detects docker when available
   - Expected: runtime equals `docker`
   - Expected: runtime equals `podman`
   - Expected: runtime equals `none`


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: detects docker when available")
val runtime = container_detect_runtime()
val has_docker = shell_bool("docker --version")
val has_podman = shell_bool("podman --version")

if has_docker:
    expect(runtime).to_equal("docker")
elif has_podman:
    expect(runtime).to_equal("podman")
else:
    expect(runtime).to_equal("none")
```

</details>

#### returns none when no container runtime installed

- Verify: returns none when no container runtime installed
   - Expected: runtime equals `none`
   - Expected: has_runtime is true


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns none when no container runtime installed")
# @req: REQ-SSPEC-LOCAL-001
val runtime = container_detect_runtime()
if runtime == "none":
    expect(runtime).to_equal("none")
else:
    # Either docker or podman is installed
    val has_runtime = runtime == "docker" or runtime == "podman"
    expect(has_runtime).to_equal(true)
```

</details>

### container_get_version

#### gets docker version when available

- Verify: gets docker version when available
   - Expected: has_docker is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: gets docker version when available")
# @req: REQ-SSPEC-LOCAL-001
val has_docker = shell_bool("docker --version")
if has_docker:
    val version = container_get_version("docker")
    expect(version).to_contain("Docker")
else:
    expect(has_docker).to_equal(false)
```

</details>

#### gets podman version when available

- Verify: gets podman version when available
   - Expected: has_podman is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: gets podman version when available")
# @req: REQ-SSPEC-LOCAL-001
val has_podman = shell_bool("podman --version")
if has_podman:
    val version = container_get_version("podman")
    expect(version).to_contain("podman")
else:
    expect(has_podman).to_equal(false)
```

</details>

#### returns empty string for unknown runtime

- Verify: returns empty string for unknown runtime
   - Expected: version equals ``


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: returns empty string for unknown runtime")
val version = container_get_version("unknown")
expect(version).to_equal("")
```

</details>

### container_check_image

#### returns false for non-existent image with docker

- Verify: returns false for non-existent image with docker
   - Expected: exists is false
   - Expected: has_docker is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req: REQ-SSPEC-LOCAL-001
step("Verify: returns false for non-existent image with docker")
val has_docker = shell_bool("docker --version")
if has_docker:
    val exists = container_check_image("nonexistent-image:999", "docker")
    expect(exists).to_equal(false)
else:
    expect(has_docker).to_equal(false)
```

</details>

#### returns false for non-existent image with podman

- Verify: returns false for non-existent image with podman
   - Expected: exists is false
   - Expected: has_podman is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns false for non-existent image with podman")
# @req: REQ-SSPEC-LOCAL-001
val has_podman = shell_bool("podman --version")
if has_podman:
    val exists = container_check_image("nonexistent-image:999", "podman")
    expect(exists).to_equal(false)
else:
    expect(has_podman).to_equal(false)
```

</details>

#### returns false for unknown runtime

- Verify: returns false for unknown runtime
   - Expected: exists is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns false for unknown runtime")
# @req: REQ-SSPEC-LOCAL-001
val exists = container_check_image("any-image", "unknown")
expect(exists).to_equal(false)
```

</details>

### container_cleanup_volumes

#### returns false for unknown runtime

- Verify: returns false for unknown runtime
   - Expected: result is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: returns false for unknown runtime")
# @req: REQ-SSPEC-LOCAL-001
val result = container_cleanup_volumes("unknown")
expect(result).to_equal(false)
```

</details>

#### attempts cleanup with docker when available

- Verify: attempts cleanup with docker when available
   - Expected: is_bool is true
   - Expected: has_docker is false


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
step("Verify: attempts cleanup with docker when available")
# @req: REQ-SSPEC-LOCAL-001
val has_docker = shell_bool("docker --version")
if has_docker:
    val result = container_cleanup_volumes("docker")
    # Should return true or false, not crash
    val is_bool = result == true or result == false
    expect(is_bool).to_equal(true)
else:
    expect(has_docker).to_equal(false)
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 10 |
| Active scenarios | 10 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `7dc39791e04a284ad84b83cb22e5d6ed9492020bb1ffa02c5d415113c6c68a60`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `7dc39791e04a284ad84b83cb22e5d6ed9492020bb1ffa02c5d415113c6c68a60`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `7dc39791e04a284ad84b83cb22e5d6ed9492020bb1ffa02c5d415113c6c68a60`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **92/100**; effective score: **92/100**; blockers: **0**.

SSpec documentization score: 92/100
source: test/01_unit/app/test_runner_new/container_backend_spec.spl
mirror: doc/06_spec/01_unit/app/test_runner_new/container_backend_spec.md (current)
findings: 5 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=70
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/app/test_runner_new/container_backend_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/app/test_runner_new/container_backend_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: assumptions/preconditions, traceability, evidence, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/app/test_runner_new/container_backend_spec.spl:37:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'detects docker when available' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/test_runner_new/container_backend_spec.spl:51:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'returns none when no container runtime installed' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/app/test_runner_new/container_backend_spec.spl:63:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'gets docker version when available' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
