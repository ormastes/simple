# .spipe Wrapped Entry Db Hardening Optimizer Specification

> Tests covering DbTable index maintenance on insert, DbTable index maintenance on delete, DbTable index maintenance on update, cache invalidation after mutations, planner-driven indexed queries, legacy raw-row query backward compat, empty table queries.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 25 | 25 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# .spipe Wrapped Entry Db Hardening Optimizer Specification

## Scenarios

### DbTable index maintenance on insert

#### builds indexes after first insert

- builds indexes after first insert


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("builds indexes after first insert")
var t = db_table_new("users")
t.insert("user:alice", "payload_a")
val ki = t.key_index()
val ti = t.text_idx()
expect prefix_index_size(ki) == 1
expect text_index_size(ti) == 1
```

</details>

#### indexes grow with each insert

- indexes grow with each insert


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("indexes grow with each insert")
var t = db_table_new("items")
t.insert("item:one", "v1")
t.insert("item:two", "v2")
t.insert("item:three", "v3")
val ki = t.key_index()
expect prefix_index_size(ki) == 3
```

</details>

#### marks indexes dirty after insert

- marks indexes dirty after insert


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("marks indexes dirty after insert")
var t = db_table_new("test")
expect t.indexes_dirty() == false
t.insert("k1", "v1")
expect t.indexes_dirty() == true
```

</details>

### DbTable index maintenance on delete

#### removes entry from indexes after delete

- removes entry from indexes after delete


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("removes entry from indexes after delete")
var t = db_table_new("users")
t.insert("user:alice", "a")
t.insert("user:bob", "b")
t.delete_by_key("user:alice")
val ki = t.key_index()
expect prefix_index_size(ki) == 1
val hits = prefix_index_lookup_exact(ki, "user:alice")
expect hits.len() == 0
```

</details>

#### still finds remaining entries after delete

- still finds remaining entries after delete


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("still finds remaining entries after delete")
var t = db_table_new("users")
t.insert("user:alice", "a")
t.insert("user:bob", "b")
t.delete_by_key("user:alice")
val ki = t.key_index()
val hits = prefix_index_lookup_exact(ki, "user:bob")
expect hits.len() == 1
```

</details>

### DbTable index maintenance on update

#### updates payload without changing index count

- updates payload without changing index count


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("updates payload without changing index count")
var t = db_table_new("users")
t.insert("user:alice", "old_payload")
t.update("user:alice", "new_payload")
val ki = t.key_index()
expect prefix_index_size(ki) == 1
```

</details>

#### returns updated payload via get_by_id

- returns updated payload via get_by_id


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("returns updated payload via get_by_id")
var t = db_table_new("users")
val id = t.insert("user:alice", "old")
t.update("user:alice", "new")
val payload = t.get_by_id(id)
expect payload == "new"
```

</details>

#### returns false for non-existent key update

- returns false for non-existent key update


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("returns false for non-existent key update")
var t = db_table_new("users")
val result = t.update("missing", "val")
expect result == false
```

</details>

### cache invalidation after mutations

#### find_by_key returns not_found after delete

- find_by_key returns not_found after delete


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("find_by_key returns not_found after delete")
var t = db_table_new("test")
t.insert("k1", "v1")
val id_before = t.find_by_key("k1")
expect id_before >= 0 == true
t.delete_by_key("k1")
val id_after = t.find_by_key("k1")
expect id_after == row_not_found()
```

</details>

#### prefix scan reflects inserts immediately

- prefix scan reflects inserts immediately


<details>
<summary>Executable SSpec</summary>

Runnable source: 9 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("prefix scan reflects inserts immediately")
var t = db_table_new("log")
t.insert("log:a", "1")
val qr1 = db_table_prefix_scan(t, "log:")
expect qr1.count == 1
t.insert("log:b", "2")
val qr2 = db_table_prefix_scan(t, "log:")
expect qr2.count == 2
```

</details>

#### exact match reflects delete

- exact match reflects delete


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("exact match reflects delete")
var t = db_table_new("test")
t.insert("key1", "v1")
t.insert("key2", "v2")
val qr1 = db_table_exact_match(t, "key1")
expect qr1.count == 1
t.delete_by_key("key1")
val qr2 = db_table_exact_match(t, "key1")
expect qr2.count == 0
```

</details>

#### contains scan reflects update

- contains scan reflects update


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("contains scan reflects update")
var t = db_table_new("test")
t.insert("user:alice", "admin")
val qr1 = db_table_contains_scan(t, "alice")
expect qr1.count == 1
t.delete_by_key("user:alice")
t.insert("user:bob", "viewer")
val qr2 = db_table_contains_scan(t, "alice")
expect qr2.count == 0
```

</details>

### planner-driven indexed queries

#### db_table_prefix_scan uses index for prefix

- db_table_prefix_scan uses index for prefix


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_table_prefix_scan uses index for prefix")
var t = db_table_new("data")
t.insert("ns:a", "1")
t.insert("ns:b", "2")
t.insert("other:c", "3")
val qr = db_table_prefix_scan(t, "ns:")
expect qr.count == 2
```

</details>

#### db_table_exact_match uses index for exact

- db_table_exact_match uses index for exact


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_table_exact_match uses index for exact")
var t = db_table_new("data")
t.insert("key:one", "1")
t.insert("key:two", "2")
val qr = db_table_exact_match(t, "key:one")
expect qr.count == 1
```

</details>

#### db_table_contains_scan uses text index

- db_table_contains_scan uses text index


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_table_contains_scan uses text index")
var t = db_table_new("data")
t.insert("hello_world", "1")
t.insert("hello_there", "2")
t.insert("goodbye", "3")
val qr = db_table_contains_scan(t, "hello")
expect qr.count == 2
```

</details>

#### db_table_range_scan works on ids

- db_table_range_scan works on ids


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_table_range_scan works on ids")
var t = db_table_new("data")
t.insert("a", "1")
t.insert("b", "2")
t.insert("c", "3")
val qr = db_table_range_scan(t, 0, 1)
expect qr.count == 2
```

</details>

#### db_table_all_ids returns all

- db_table_all_ids returns all


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_table_all_ids returns all")
var t = db_table_new("data")
t.insert("a", "1")
t.insert("b", "2")
val qr = db_table_all_ids(t)
expect qr.count == 2
```

</details>

### legacy raw-row query backward compat

#### db_prefix_scan still works on raw rows

- db_prefix_scan still works on raw rows


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_prefix_scan still works on raw rows")
var t = db_table_new("log")
t.insert("log:x", "1")
t.insert("log:y", "2")
t.insert("other:z", "3")
val qr = db_prefix_scan(t.rows, "log:")
expect qr.count == 2
```

</details>

#### db_keys_matching still works on raw rows

- db_keys_matching still works on raw rows


<details>
<summary>Executable SSpec</summary>

Runnable source: 7 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_keys_matching still works on raw rows")
var t = db_table_new("test")
t.insert("k1", "v1")
t.insert("k2", "v2")
val qr = db_keys_matching(t.rows, "k1")
expect qr.count == 1
```

</details>

#### db_range_scan still works on raw rows

- db_range_scan still works on raw rows


<details>
<summary>Executable SSpec</summary>

Runnable source: 8 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_range_scan still works on raw rows")
var t = db_table_new("test")
t.insert("a", "1")
t.insert("b", "2")
t.insert("c", "3")
val qr = db_range_scan(t.rows, 0, 1)
expect qr.count == 2
```

</details>

#### db_all_ids still works on raw rows

- db_all_ids still works on raw rows


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("db_all_ids still works on raw rows")
var t = db_table_new("test")
t.insert("a", "1")
val qr = db_all_ids(t.rows)
expect qr.count == 1
```

</details>

### empty table queries

#### prefix scan on empty table returns 0

- prefix scan on empty table returns 0


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("prefix scan on empty table returns 0")
var t = db_table_new("empty")
val qr = db_table_prefix_scan(t, "any:")
expect qr.count == 0
```

</details>

#### exact match on empty table returns 0

- exact match on empty table returns 0


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("exact match on empty table returns 0")
var t = db_table_new("empty")
val qr = db_table_exact_match(t, "any")
expect qr.count == 0
```

</details>

#### contains scan on empty table returns 0

- contains scan on empty table returns 0


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("contains scan on empty table returns 0")
var t = db_table_new("empty")
val qr = db_table_contains_scan(t, "x")
expect qr.count == 0
```

</details>

#### has_indexes returns false on empty table

- has_indexes returns false on empty table


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("has_indexes returns false on empty table")
var t = db_table_new("empty")
expect t.has_indexes() == false
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Standard Library |
| Status | Active |
| Source | `test/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering DbTable index maintenance on insert, DbTable index maintenance on delete, DbTable index maintenance on update, cache invalidation after mutations, planner-driven indexed queries, legacy raw-row query backward compat, empty table queries.
- DbTable index maintenance on insert
- DbTable index maintenance on delete
- DbTable index maintenance on update
- cache invalidation after mutations
- planner-driven indexed queries
- legacy raw-row query backward compat
- empty table queries

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 25 |
| Active scenarios | 25 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-UNIT`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `e7104b97c30025eb6e29e3699ccf1b2c933383b947c623c83b087c9f3bf23680`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `e7104b97c30025eb6e29e3699ccf1b2c933383b947c623c83b087c9f3bf23680`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `e7104b97c30025eb6e29e3699ccf1b2c933383b947c623c83b087c9f3bf23680`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **91/100**; effective score: **91/100**; blockers: **0**.

SSpec documentization score: 91/100
source: test/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.spl
mirror: doc/06_spec/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.md (current)
findings: 6 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=55
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, scope, assumptions/preconditions, primary workflow, evidence, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.spl:31:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'builds indexes after first insert' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.spl:41:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'indexes grow with each insert' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/lib/db/.spipe_wrapped_entry_db_hardening_optimizer_spec.spl:51:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'marks indexes dirty after insert' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
