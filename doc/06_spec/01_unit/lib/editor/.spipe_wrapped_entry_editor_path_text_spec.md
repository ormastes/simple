# .spipe Wrapped Entry Editor Path Text Specification

> Tests covering editor shared path/text helpers.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 5 | 5 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# .spipe Wrapped Entry Editor Path Text Specification

## Scenarios

### editor shared path/text helpers

#### parses paths and filenames without app dependencies

- parses paths and filenames without app dependencies


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("parses paths and filenames without app dependencies")
expect editor_dirname("/vault/note.md") == "/vault"
expect editor_path_basename("/vault/note.md") == "note.md"
expect editor_join_path("/vault", "note.md") == "/vault/note.md"
expect editor_join_path("/vault/", "note.md") == "/vault/note.md"
```

</details>

#### parses payload and numeric values

- parses payload and numeric values


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("parses payload and numeric values")
expect editor_payload_part("root|key|value", 1) == "key"
expect editor_payload_csv("a, b,, c").len() == 3
expect editor_parse_i64("-42x", 7) == -42
expect editor_parse_i64("x", 7) == 7
```

</details>

#### normalizes relative dirs and markdown paths

- normalizes relative dirs and markdown paths


<details>
<summary>Executable SSpec</summary>

Runnable source: 6 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("normalizes relative dirs and markdown paths")
expect editor_clean_relative_dir("./attachments/") == "attachments"
expect editor_clean_relative_dir("/attachments/") == "attachments"
expect editor_is_markdown_path("NOTE.MARKDOWN") == true
expect editor_is_markdown_path("note.txt") == false
```

</details>

#### keeps GUI shell path and payload helpers in shared lib

- keeps GUI shell path and payload helpers in shared lib
   - Expected: shell does not contain `fn _gui_shell_basename(`
   - Expected: shell does not contain `fn _gui_payload_part(`
   - Expected: shell does not contain `fn _gui_text_list_contains(`
   - Expected: tui does not contain `fn _tui_is_markdown_path(`
   - Expected: tui_panels does not contain `fn _tui_basename(`


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps GUI shell path and payload helpers in shared lib")
val shell = read_text("src/app/editor/gui_shell_render.spl")
val tui = read_text("src/app/editor/tui_shell.spl")
val tui_panels = read_text("src/app/editor/tui_shell_panels.spl")
expect shell.contains("std.editor.core.path_text") == true
expect(shell.contains("fn _gui_shell_basename(")).to_equal(false)
expect(shell.contains("fn _gui_payload_part(")).to_equal(false)
expect(shell.contains("fn _gui_text_list_contains(")).to_equal(false)
expect tui.contains("std.editor.core.path_text") == true
expect(tui.contains("fn _tui_is_markdown_path(")).to_equal(false)
expect tui.contains("editor_is_markdown_path(path)") == true
expect tui_panels.contains("std.editor.core.path_text") == true
expect(tui_panels.contains("fn _tui_basename(")).to_equal(false)
expect tui_panels.contains("editor_path_basename(tab_path)") == true
```

</details>

#### keeps reusable editor modules on shared path helpers

- keeps reusable editor modules on shared path helpers
   - Expected: workspace does not contain `fn _workspace_basename(`
   - Expected: ext_workspace does not contain `fn _workspace_basename(`
   - Expected: ext_host does not contain `fn _extension_host_dirname(`
   - Expected: debug_registry does not contain `fn _ed_dirname(`
   - Expected: debug_registry does not contain `fn _ed_basename(`
   - Expected: navigator does not contain `fn _basename(`
   - Expected: lsp_panel does not contain `fn _lsp_result_basename(`
   - Expected: diagnostics_panel does not contain `fn _diagnostics_panel_text_list_contains(`
   - Expected: md_search does not contain `fn _md_vault_search_is_markdown_path(`
   - Expected: file_watcher does not contain `fn _fw_path_list_contains(`
   - Expected: mcp_helpers does not contain `fn editor_mcp_dirname(`
   - Expected: mcp_helpers does not contain `fn editor_mcp_path_basename(`
   - Expected: mcp_helpers does not contain `fn editor_mcp_last_dot(`
   - Expected: mcp_helpers does not contain `fn editor_mcp_clean_relative_dir(`
   - Expected: workspace_symbols contains `editor_find_text(trimmed, ")")`


<details>
<summary>Executable SSpec</summary>

Runnable source: 50 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-UNIT
step("keeps reusable editor modules on shared path helpers")
val workspace = read_text("src/lib/editor/core/workspace.spl")
val ext_workspace = read_text("src/lib/editor/extensions/workspace.spl")
val ext_host = read_text("src/lib/editor/extensions/host.spl")
val debug_registry = read_text("src/lib/editor/services/debug_session_registry.spl")
val navigator = read_text("src/lib/editor/unified/project_navigator.spl")
val lsp_panel = read_text("src/lib/editor/view/lsp_result_panel.spl")
val diagnostics_panel = read_text("src/lib/editor/view/diagnostics_panel.spl")
val md_search = read_text("src/lib/editor/services/md_search.spl")
val file_watcher = read_text("src/lib/editor/services/file_watcher.spl")
val mcp_helpers = read_text("src/app/editor/mcp_tools_helpers.spl")
val attachment_template = read_text("src/app/editor/editor_attachment_template.spl")
val workspace_symbols = read_text("src/app/editor/editor_workspace_symbols.spl")
expect workspace.contains("use std.editor.core.path_text") == true
expect(workspace.contains("fn _workspace_basename(")).to_equal(false)
expect(ext_workspace.contains("fn _workspace_basename(")).to_equal(false)
expect(ext_host.contains("fn _extension_host_dirname(")).to_equal(false)
expect ext_host.contains("editor_dirname(file)") == true
expect(debug_registry.contains("fn _ed_dirname(")).to_equal(false)
expect(debug_registry.contains("fn _ed_basename(")).to_equal(false)
expect(navigator.contains("fn _basename(")).to_equal(false)
expect(lsp_panel.contains("fn _lsp_result_basename(")).to_equal(false)
expect lsp_panel.contains("editor_path_basename(path) +") == true
expect diagnostics_panel.contains("use std.editor.core.path_text") == true
expect(diagnostics_panel.contains("fn _diagnostics_panel_text_list_contains(")).to_equal(false)
expect diagnostics_panel.contains("editor_text_list_contains(groups, group_key)") == true
expect md_search.contains("use std.editor.core.path_text") == true
expect(md_search.contains("fn _md_vault_search_is_markdown_path(")).to_equal(false)
expect md_search.contains("editor_is_markdown_path(path)") == true
expect file_watcher.contains("use std.editor.core.path_text") == true
expect(file_watcher.contains("fn _fw_path_list_contains(")).to_equal(false)
expect file_watcher.contains("editor_text_list_contains(dir.known_paths, path)") == true
expect file_watcher.contains("editor_text_list_contains(current, path)") == true
expect(mcp_helpers.contains("fn editor_mcp_dirname(")).to_equal(false)
expect(mcp_helpers.contains("fn editor_mcp_path_basename(")).to_equal(false)
expect(mcp_helpers.contains("fn editor_mcp_last_dot(")).to_equal(false)
expect(mcp_helpers.contains("fn editor_mcp_clean_relative_dir(")).to_equal(false)
expect mcp_helpers.contains("editor_dirname(target_path)") == true
expect mcp_helpers.contains("editor_path_basename(attachment_path)") == true
expect mcp_helpers.contains("editor_clean_relative_dir(storage_dir)") == true
expect mcp_helpers.contains("editor_last_dot(name)") == true
expect attachment_template.contains("app.editor.editor_path_text_helpers") == false
expect attachment_template.contains("use std.editor.core.path_text") == true
expect attachment_template.contains("editor_path_basename(file)") == true
expect attachment_template.contains("editor_is_markdown_path(file)") == true
expect workspace_symbols.contains("app.editor.editor_path_text_helpers") == false
expect workspace_symbols.contains("use std.editor.core.path_text") == true
expect(workspace_symbols.contains("editor_find_text(trimmed, \")\")")).to_equal(true)
expect workspace_symbols.contains("editor_find_text(line, name)") == true
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Standard Library |
| Status | Active |
| Source | `test/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering editor shared path/text helpers.
- editor shared path/text helpers

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 5 |
| Active scenarios | 5 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-UNIT`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `72beeef145199b0a513c7ac2450f125bef961c3d2b18201d719965ab8d6e36dd`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `72beeef145199b0a513c7ac2450f125bef961c3d2b18201d719965ab8d6e36dd`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `72beeef145199b0a513c7ac2450f125bef961c3d2b18201d719965ab8d6e36dd`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **91/100**; effective score: **91/100**; blockers: **0**.

SSpec documentization score: 91/100
source: test/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.spl
mirror: doc/06_spec/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.md (current)
findings: 6 blockers: 0
  narrative=100 structure=100 oracle=100
  traceability=100 evidence=70 coverage=100 maintainability=55
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
doc/06_spec/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, scope, assumptions/preconditions, primary workflow, evidence, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.spl:20:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'parses paths and filenames without app dependencies' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.spl:28:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'parses payload and numeric values' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
test/01_unit/lib/editor/.spipe_wrapped_entry_editor_path_text_spec.spl:36:1: warning SSDOC-EVD-001 [evidence] (-10): visible scenario 'normalizes relative dirs and markdown paths' has no retained capture or evidence
  why: Professional manuals need retained observable evidence.
  improve: Capture typed user/operator-facing evidence or explain why the oracle is complete.
<!-- sspec-maintain:scorecard:end -->
