# SimpleOS Hardening Evidence Matrix

- status: pass
- reason: pass
- passed: 13/13
- executable_launch_from_fs: pass
- ssh_shell_smf_and_exec: pass
- shared_wm_logic: pass
- cpu_simd_engine2d_diagram: pass
- cpu_simd_report: doc/09_report/cpu_simd_engine2d_evidence_current_2026-07-02.md
- llvm_port: pass
- llvm_port_report: doc/09_report/simpleos_llvm_port_evidence_current_2026-07-02.md
- gui_renderdoc_vulkan: pass
- gui_renderdoc_report: doc/09_report/gui_renderdoc_feature_coverage_status_2026-07-03.md
- web_renderer_engine2d_bitmap: pass
- simple_gui_webrenderer_bitmap: pass
- production_gui_web_renderer_parity: pass
- qemu_host_counterpart_bitmap: pass
- qemu_gui_smf_artifact_contract: pass
- qemu_gui_smf_artifact_contract_status: pass
- qemu_gui_smf_artifact_contract_row: GUI_SMF_ARTIFACT_CONTRACT status=pass artifact=build/gui/pure_gui_hot.smf sha256=d2978950c6ac8046b84e2d017b39434a47f77cf28476c8d4b744af423e698c48 size=69400 smf_role=2 arch=3 embedded_dynlib=true symbol=gui_dynlib_hot_probe_tick qemu_status=pass qemu_reason=arm64-smf-parity-verified macos_status=not-run macos_reason=requires-macos-arm64
- qemu_guest_perf_release_gate: guest-simple-frame-plus-host-gtk-baseline
- qemu_guest_perf_release_gate_status: pass
- qemu_guest_perf_release_blocker: none
- qemu_guest_perf_harness_status: pass
- qemu_guest_perf_harness_reason: pass
- qemu_guest_perf_harness_sample_origin: qemu-guest
- qemu_guest_perf_harness_required_sample_origin: qemu-guest
- qemu_guest_perf_harness_pending_marker: [desktop-e2e] qemu-perf sample_origin=qemu-guest simple_frame_cycles=<positive> iterations=<positive> timing_unit=tsc
- qemu_guest_perf_harness_marker_line: [desktop-e2e] qemu-perf sample_origin=qemu-guest simple_frame_cycles=251664583 iterations=16 timing_unit=tsc scope=simple-vga-paint
- qemu_host_perf_promotes_qemu_perf: false
- qemu_wm_simple_gui_mdi: pass
- qemu_wm_simple_gui_mdi_ppm_anchors: pass
- qemu_wm_simple_gui_mdi_ppm_nonblack: 779590
- qemu_wm_simple_gui_mdi_input_status: pass
- qemu_wm_simple_gui_mdi_input_before_ppm_bytes: 2359312
- qemu_wm_simple_gui_mdi_input_after_ppm_bytes: 2359312
- qemu_wm_simple_gui_mdi_input_before_raw_bytes: 3145728
- qemu_wm_simple_gui_mdi_input_after_raw_bytes: 3145728
- qemu_wm_simple_gui_mdi_input_before_sha256: 5fcc2dc196224169bc9a044e57bd2faa88f4ff3dddf415f6d4b44e805d2d1f8f
- qemu_wm_simple_gui_mdi_input_after_sha256: 1f3b7077cc368c8b8f072f80cc0975cd216734b24d918ba3eb8dd2f7867df760
- rv64_display_smoke_qmp: pass
- rv64_display_smoke_qmp_reason: rv64-display-smoke-qmp-wm-lifecycle-pass
- rv64_display_smoke_qmp_report: doc/09_report/rv64_display_smoke_qmp_evidence_current_2026-07-02.md
- rv64_display_smoke_qmp_width: 320
- rv64_display_smoke_qmp_height: 240
- rv64_display_smoke_qmp_nonblack: 76800
- rv64_display_smoke_qmp_anchor_matches: 5
- rv64_display_smoke_qmp_anchor_names: top,header,bodyA,bodyB,taskbar
- qemu_virtio_gpu_access: pass
- qemu_virtio_gpu_access_reason: rv64-qmp-virtio-gpu-display-smoke-pass
- qemu_virtio_gpu_access_contract: src/os/_QemuRunner/scenario_catalog.spl:scenario_riscv64_display_smoke
- kernel_scheduler_resource_formal: pass
- kernel_scheduler_resource_formal_scope: bounded ResourcePool acquire/release capacity, acquire grant iff capacity remains, acquire denial iff at capacity with exact denied result, release no-op iff empty and non-empty release is not a no-op, successful-acquire exact granted result and one-increment pool, strict increase, never-decrease monotonicity, capacity preservation, and well-formed result, failed-acquire live-count and capacity preservation, denied acquire followed by release equals releasing the original pool, zero-capacity denial/emptiness, non-empty release exact one-decrement pool, capacity preservation, decrement, and strict decrease, acquire/release capacity preservation, underflow, acquire-release no-leak roundtrip invariants, acquire-release-release over-release equivalence to releasing the original pool, CPU affinity allows exactly unrestricted mask or set CPU bit, work stealing fires exactly above the remote-load threshold and stays off at or below it, new task exact runnable record, park recording reason and exact parked task record, parked unpark assigning and reporting the waker CPU placement plus exact runnable task record and exact enqueue wake decision, non-parked unpark reporting no placement move and exact no-op wake decision, done tasks cannot be woken back onto the ready queue, park/unpark/complete preserving task identity, done-complete no-op, complete preserving CPU placement/accounting fields, non-done completion exact done-state record and result value, enqueue appending exactly one task to ready queue tail, ready-queue pop returning exactly the head/rest or no-oping when empty, and runOne consuming exactly one ready task, strictly decreasing ready length, or no-oping when empty
- kernel_scheduler_resource_formal_evidence: src/verification/kernel_scheduler/KernelScheduler/Theorems.lean T1c + T2c2-T2d + T3c2-T3c5 + T3d + T4c-T4e + T5b0 + T5c-T5e + T7a1-T7a2 + T7c-T7g + T8d + T9a0-T9b0 + T10a0-T10i plus new_eq_runnable_record + park_records_reason + park_eq_parked_record + park_preserves_task_id + unpark_parked_sets_assigned_cpu + unpark_parked_reports_waker_placement + unpark_parked_task_eq_runnable_record + unpark_parked_eq_enqueue_decision + unpark_parked_preserves_task_id + unpark_nonparked_reports_no_move + unpark_nonparked_eq_noop_decision + unpark_done_no_enqueue + complete_done_noop + complete_preserves_cpu_assignment + complete_records_result_when_not_done + complete_non_done_eq_done_record + complete_preserves_task_id + enqueue_increases_ready_length + enqueue_ready_eq_append + popHead_nonempty_returns_head + popHead_empty_noop + runOne_empty_noop + runOne_nonempty_drops_head + runOne_nonempty_decreases_length + cpuAllowed_true_iff + shouldSteal_true_iff + shouldSteal_false_iff + resource_acquire_granted_iff_below_capacity + resource_acquire_full_eq_denied_result + resource_acquire_denied_iff_at_capacity + resource_acquire_below_capacity_eq_granted_result + resource_acquire_below_capacity_pool_eq + resource_acquire_below_capacity_preserves_capacity + resource_acquire_below_capacity_wf + resource_acquire_below_capacity_increases + resource_acquire_never_decreases + resource_acquire_denied_preserves_in_use + resource_acquire_denied_preserves_capacity + resource_denied_acquire_then_release_eq_release + resource_acquire_release_release_eq_release + resource_zero_capacity_acquire_denied + resource_zero_capacity_wf_empty + resource_release_noop_iff_empty + resource_release_nonempty_not_noop + resource_release_nonempty_preserves_capacity + resource_release_nonempty_pool_eq + resource_release_nonempty_decrements + resource_release_nonempty_decreases + resource_release_preserves_capacity_bound + resource_acquire_preserves_capacity_bound; `cd src/verification/kernel_scheduler && lake build`
- actor_channel_backpressure_formal: pass
- actor_channel_backpressure_formal_scope: closed send returns the exact closed failure result without mutation and preserves closed state; closed empty recv returns the exact terminal no-park result; closed non-empty recv drains buffered values and preserves the closed channel state; buffered recv returns the exact received result, keeps the channel open, and does not alter waiters; close_drain has exact wake accounting, no spurious wakeups, clears waiters, closes the channel, preserves buffered values, keeps them receivable, and is idempotent after close; bounded producer/consumer channel cannot overflow when full and no receiver is waiting and preserves open state; open empty recv returns the exact parked result; receiver parked by recv keeps the channel open, is woken by close_drain, and close-after-park wakes exactly the prior waiters plus that receiver; sends to a waiting receiver return the exact wake result, sends to available buffer capacity keep the channel open with the exact appended-queue result, legacy sync-channel receive on a non-empty queue dequeues exactly the head, dead or empty actors produce no scheduler mutation, and a handler error on one scheduler tick does not starve the next queued successful actor message
- actor_channel_backpressure_formal_evidence: src/verification/actor_channel/ActorChannel/Theorems.lean recv_buffered_eq_received_result + recv_buffered_preserves_open + recv_buffered_preserves_waiters + T2a2 + T2a2a + T2a3 + T2b2 + T2b2a + T2b2b + T2b3 + T2b4 + T3a0 + T3a0b + T3a0c + T3a1 + T3a2 + T3a3 + T3b + T6c1 + T6c2 + T6d + T6d1 + T6e + T6e1 + T6f + T6g + T6g1 + T7 + T7b + closed_send_eq_closed_result + closed_empty_recv_eq_closed_result + recv_open_empty_eq_parked_result + recv_then_close_exact_woken_waiters + send_buffered_eq_sent_result + send_to_waiter_eq_wake_result + legacy_recv_nonempty_dequeues_head + scheduler_dead_actor_no_dispatch + scheduler_empty_mailbox_no_dispatch + scheduler_error_then_next_ok_dispatches; `cd src/verification/actor_channel && lake build`
- db_storage_wal_formal: pass
- db_storage_wal_formal_scope: transaction commit requires WAL flush, successful commit returns committed status, WAL append precedes commit on the D4 chain, pager writes are blocked when page LSN is ahead of flushed WAL LSN, and pager writes are allowed once WAL is flushed far enough
- db_storage_wal_formal_evidence: src/verification/db_storage/DbStorage/Theorems.lean T4_wal_before_data + T4_commit_sets_committed + T4_wal_appended_before_commit + T4_pager_wal_gate + T4_pager_wal_gate_blocked + T4_pager_wal_gate_allows_flushed; `cd src/verification/db_storage && lake build`
- db_storage_mvcc_formal: pass
- db_storage_mvcc_formal_scope: visible versions are committed before snapshot xmax, active inserts and future commits are excluded, and committed deletes before the snapshot are excluded
- db_storage_mvcc_formal_evidence: src/verification/db_storage/DbStorage/Theorems.lean T5_snapshot_committed + T5_snapshot_excludes_active_insert + T5_snapshot_excludes_future + T5_snapshot_excludes_deleted; `cd src/verification/db_storage && lake build`
- db_storage_btree_formal: pass
- db_storage_btree_formal_scope: B-tree ordered insertion preserves ordering and contains the inserted key, leaf split halves remain balanced, and split occupancy bounds hold
- db_storage_btree_formal_evidence: src/verification/db_storage/DbStorage/Theorems.lean T1_btree_ordered + T1_btree_insert_contains_key + T2_btree_balanced_leaf + T3_btree_bounds; src/verification/db_storage/DbStorage/BTree.lean orderedInsert_mem + T2_btree_balanced_internal_split + T3_btree_bounds_insert_nonfull; `cd src/verification/db_storage && lake build`
- nvfs_mount_formal: pass
- nvfs_mount_formal_scope: successful mount preserves all NVFS invariants and increments mountEpoch exactly once; unmount preserves all invariants
- nvfs_mount_formal_evidence: src/verification/formal/nvfs/Nvfs/Theorems.lean mount_preserves_all + mount_success_increments_epoch + unmount_preserves_all; `cd src/verification/formal/nvfs && lake build`
- fat32_chain_formal: pass
- fat32_chain_formal_scope: guarded FAT32 chain walks are fuel bounded, zero fuel returns the empty chain, cluster-to-LBA mapping is monotone and injective for well-formed geometry, cluster counts cover file size, EOC and FREE markers are exclusive, FREE and BAD entries are not valid links, and a newly allocated free singleton is disjoint from existing chains before predecessor linking
- fat32_chain_formal_evidence: src/verification/fat32/Fat32/Theorems.lean T1_chain_length_bound + T1_zero_fuel_empty + T2_lba_monotone + T3_lba_injective + T5_cluster_count_covers + T6_eoc_not_free + T6_free_not_valid_chain + T6_bad_not_valid_chain + T7a_valid_link_ge2 + T7b_free_not_in_chain + T7c_alloc_step2_new_chain_disjoint; `cd src/verification/fat32 && lake build`
- gc_boundary_formal: pass
- gc_boundary_formal_scope: alias-free GC/nogc checker acceptance prevents GC-tier values in nogc/noalloc contexts, noalloc-accepted bodies do not allocate, allocation inference joins are monotone, explicit Copy nodes are alias-free, Copy results are nogc rather than gc, and the alias-blind counterexample is machine-checked as GC-tier but structurally noalloc-ok
- gc_boundary_formal_evidence: src/verification/gc_boundary/GcBoundary/Theorems.lean check_sound_wrt_model + noalloc_closed + inference_monotone + inference_join_ge + alloc_join_alloc + copy_alias_free + copy_isolates + copy_result_not_gc; src/verification/gc_boundary/GcBoundary/AliasCounterexample.lean aliasExpr_not_alias_free + aliasExpr_has_tier_gc + aliasExpr_noalloc_body_ok; `cd src/verification/gc_boundary && lake build`
- nogc_compile_formal: pass
- nogc_compile_formal_scope: nogc instruction lists compose under append and explicitly reject GC allocation instructions
- nogc_compile_formal_evidence: src/verification/nogc_compile/src/NogcCompile.lean nogc_append + nogc_singleton + gc_alloc_singleton_not_nogc; `cd src/verification/nogc_compile && lake build`
- gc_manual_borrow_formal: pass
- gc_manual_borrow_formal_scope: manual borrows are modeled as GC pins; collecting a borrowed object is a no-op, and allocation, borrow, release, and safe collection preserve the invariant that every borrowed object remains live
- gc_manual_borrow_formal_evidence: src/verification/gc_manual_borrow/src/GcManualBorrow.lean collect_borrowed_noop + allocate_preserves + borrow_preserves + release_preserves + collect_preserves; `cd src/verification/gc_manual_borrow && lake build`
- memory_capabilities_formal: pass
- memory_capabilities_formal_scope: empty environments allow shared, exclusive, and isolated references, deny all access attempts, are well-formed, creating any reference from an empty environment yields a well-formed singleton environment, singleton environments are well-formed for any capability, two-shared-reference environments are well-formed, creating a second shared alias from a singleton shared environment yields a well-formed two-shared-reference environment, two-exclusive, two-isolated, or mixed exclusive+isolated-reference environments are not well-formed, capability conversion permits downgrades but denies shared-to-exclusive, shared-to-isolated, and exclusive-to-isolated upgrades, boolean capability conversion is equivalent to the Prop-level restrictive relation, any existing reference blocks creating unique exclusive or isolated references at the same location, existing one- or two-shared-reference environments allow shared alias creation, read access implies the target is the reference location, shared references can read but cannot write, write access implies an exclusive or isolated capability, singleton or two-shared-reference environments permit a read and reject a write at that location, exclusive or isolated references can write the referenced location, singleton exclusive or isolated-reference environments permit a write at that location, and reads or writes through a reference to a different location are denied at both reference and singleton-environment level
- memory_capabilities_formal_evidence: src/verification/memory_capabilities/src/MemoryCapabilities.lean shared_to_exclusive_denied + exclusive_to_isolated_denied + shared_to_isolated_denied + can_convert_implies_restrictive + restrictive_implies_can_convert + can_convert_iff_restrictive + empty_env_allows_shared + empty_env_allows_exclusive + empty_env_allows_isolated + empty_env_access_not_safe + empty_env_wellformed + singleton_env_wellformed + empty_create_ref_wellformed + two_shared_env_wellformed + shared_alias_create_wellformed + two_exclusive_env_not_wellformed + two_isolated_env_not_wellformed + mixed_exclusive_isolated_env_not_wellformed + existing_ref_denies_exclusive + existing_ref_denies_isolated + existing_ref_denies_unique_create + existing_shared_allows_shared + two_shared_allows_shared + shared_read_same_loc + singleton_shared_read_safe + two_shared_read_safe + read_access_implies_same_location + exclusive_write_same_loc + singleton_exclusive_write_safe + isolated_write_same_loc + singleton_isolated_write_safe + write_access_implies_unique_cap + shared_write_same_loc_denied + singleton_shared_write_not_safe + two_shared_write_not_safe + read_wrong_location_denied + singleton_read_wrong_location_not_safe + write_wrong_location_denied + singleton_write_wrong_location_not_safe; `cd src/verification/memory_capabilities && lake build`
- manual_pointer_borrow_formal: pass
- manual_pointer_borrow_formal_scope: valid borrow states exclude shared borrows while exclusive, typed valid states are always valid, roundtrip conversion preserves valid states, taking shared while exclusive is held is a no-op, and take/release operations preserve validity
- manual_pointer_borrow_formal_evidence: src/verification/manual_pointer_borrow/src/ManualPointerBorrow.lean valid_exclusive_has_no_shared + validState_always_valid + toValid_toState + exclusive_ok + shared_ok + take_shared_exclusive_noop + release_ok; `cd src/verification/manual_pointer_borrow && lake build`
- tensor_memory_formal: pass
- tensor_memory_formal_scope: per-component tensor memory min/max bounds compose into total min <= total max, and any actual usage below total max fits when total max fits available device memory
- tensor_memory_formal_evidence: src/verification/tensor_dimensions/src/TensorMemory.lean training_total_min_le_max + training_fits_if_max_fits; `cd src/verification/tensor_dimensions && lake build`
- tensor_dimensions_formal: pass
- tensor_dimensions_formal_scope: tensor shape compatibility is reflexive, mismatched ranks are rejected, matmul shape inference is deterministic, and computed min/max element bounds are comparable when both exist
- tensor_dimensions_formal_evidence: src/verification/tensor_dimensions/src/TensorDimensions.lean shapesCompatible_refl + shapesCompatible_rank_mismatch_left + matmulShape_deterministic + min_le_max_elements; `cd src/verification/tensor_dimensions && lake build`
- type_inference_core_formal: pass
- type_inference_core_formal_scope: core expression type inference is deterministic and rejects numeric addition with a boolean operand
- type_inference_core_formal_evidence: src/verification/type_inference_compile/src/TypeInferenceCompile.lean infer_deterministic + infer_add_rejects_bool_operand; `cd src/verification/type_inference_compile && lake build`
- contract_verification_formal: pass
- contract_verification_formal_scope: empty function contracts contain no preconditions, invariants, postconditions, or error postconditions
- contract_verification_formal_evidence: src/verification/type_inference_compile/src/Contracts.lean empty_contract_has_no_preconditions + empty_contract_has_no_postconditions + empty_contract_has_no_clauses; `cd src/verification/type_inference_compile && lake build`
- type_inference_generics_formal: pass
- type_inference_generics_formal_scope: empty substitution is identity, single substitution rewrites its target type variable, occurs checks reject primitive non-recursive types and accept the same variable, instantiation/generalization preserve type shape, and literal inference is deterministic
- type_inference_generics_formal_evidence: src/verification/type_inference_compile/src/Generics.lean applySubst_empty + apply_single_subst_var + occurs_nat_false + occurs_bool_false + occurs_str_false + occurs_var_true + instantiate_preserves_shape + generalize_preserves_type + infer_lit_nat + infer_lit_bool + infer_lit_str; `cd src/verification/type_inference_compile && lake build`
- async_effect_inference_formal: pass
- async_effect_inference_formal_scope: explicit sync declarations reject suspension bodies, async return types are promise-wrapped without double wrapping, await insertion is sound for matching promise inner types, non-promise i32 actual values do not insert await, and promise unwrap succeeds only for matching inner type
- async_effect_inference_formal_evidence: src/verification/type_inference_compile/src/AsyncEffectInference.lean sync_safety + async_returns_promise + sync_no_promise_wrap + no_double_wrap + await_inference_sound + await_not_inserted_for_i32 + promise_unwrap_correct; src/compiler_rust/lib/std/src/verification/regenerate/async_effect_inference.spl generated theorem source; `cd src/verification/type_inference_compile && lake build`
- async_compile_formal: pass
- async_compile_formal_scope: async compile pipelines compose only from async-safe effects and reject blocking wait effects
- async_compile_formal_evidence: src/verification/async_compile/src/AsyncCompile.lean append_safe + wait_detected + wait_pipeline_not_safe; `cd src/verification/async_compile && lake build`
- visibility_export_formal: pass
- visibility_export_formal_scope: visibility meet is commutative and associative, private visibility absorbs from either side, all-public ancestor paths remain public, any private ancestor makes the path private, and a private symbol remains private even under a public ancestor path
- visibility_export_formal_evidence: src/verification/visibility_export/src/VisibilityExport.lean meet_comm + meet_assoc + meet_private_left + meet_private_right + meet_public_left + meet_public_right + foldl_priv_absorbs + any_private_means_private + all_public_means_public + private_symbol_under_public_path_private; `cd src/verification/visibility_export && lake build`
- module_resolution_formal: pass
- module_resolution_formal_scope: module resolution is deterministic, well-formed roots cannot resolve as both file and directory, unique resolutions imply the resolved path exists, not-found means neither candidate exists, and simultaneous file/directory candidates resolve to the exact ambiguous result
- module_resolution_formal_evidence: src/verification/module_resolution/src/ModuleResolution.lean resolve_deterministic + wellformed_not_ambiguous + unique_implies_exists + notfound_means_neither + both_paths_existing_resolves_ambiguous; `cd src/verification/module_resolution && lake build`
- macro_auto_import_formal: pass
- macro_auto_import_formal_scope: glob imports include non-macros, include explicitly auto-imported macros, exclude non-auto-imported macros from well-formed exports, and empty auto-import manifests glob exactly non-macro exports
- macro_auto_import_formal_evidence: src/verification/macro_auto_import/src/MacroAutoImport.lean glob_doesnt_leak_macros_wf + nonmacros_always_globbed + auto_imported_in_glob + glob_subset + empty_auto_import_no_macros + empty_auto_import_glob_nonmacros; `cd src/verification/macro_auto_import && lake build`
- aop_weaver_formal: pass
- aop_weaver_formal_scope: aspect selection is sorted and stable in the model, no-match wrapping preserves target execution, denying advice skips the target and increments denial count, denial counts are monotone, and well-formed around chains execute the target at most once
- aop_weaver_formal_evidence: src/verification/aop_weaver/src/AopWeaver/Model.lean T1_selection_sorted + T2_selection_stable + T3_no_match_preserves + deny_increments + T4_deny_skips_target + T5_deny_monotone + T6_proceed_linear; `cd src/verification/aop_weaver && lake build`
- ui_compositor_formal: pass
- ui_compositor_formal_scope: damage additions append exactly one region, merged damage covers both inputs, clips stay inside both source rects, z-order sorts preserve all windows, stacking-context sorts preserve all contexts, renormalization preserves relative order, and recursive flattening preserves all tree surfaces
- ui_compositor_formal_evidence: src/verification/ui_compositor/UiCompositor/Theorems.lean damage_add_increases_length + merge_covers_left + merge_covers_right + clip_inside_left + clip_inside_right + insertSorted_is_perm + sortStackingContexts_perm + flattenTree_perm_treeSurfaces; `cd src/verification/ui_compositor && lake build`
- kernel_capability_depth_formal: pass
- kernel_capability_depth_formal_scope: capability delegation with exhausted depth is denied
- kernel_capability_depth_formal_evidence: src/verification/kernel_capabilities/KernelCapabilities/Theorems.lean zero_depth_grant_denied; `cd src/verification/kernel_capabilities && lake build`
- kernel_capability_empty_set_formal: pass
- kernel_capability_empty_set_formal_scope: no capability record and CapSet.empty deny every direct check and syscall authorization
- kernel_capability_empty_set_formal_evidence: src/verification/kernel_capabilities/KernelCapabilities/Theorems.lean default_deny + default_deny_syscall + empty_set_denies_all + empty_set_syscall_denies_all; `cd src/verification/kernel_capabilities && lake build`
- kernel_capability_full_set_hazard_formal: pass
- kernel_capability_full_set_hazard_scope: CapSet.full is formally verified to allow any capability kind through direct checks and syscall authorization; the full-set restriction invariant proves every full-set holder is the trusted init/kernel principal and excludes full-set records for non-trusted principals; runtime CapabilityManager.init_task lowers non-init ambient full-set input to deny-all, and the sanitized non-trusted record denies every capability-kind check and syscall authorization; direct/transitive revocation of the last explicit token pledges the empty set so revocation cannot create ambient full authority, direct owner-only revoke preserves non-owner checks, direct-child transitive revocation removes child authority at check and syscall gates, file path access requires matching FileRead/FileWrite/FileExec/FileCreate capability before unveil path checks, invalid permission strings fail closed, and file/unveil path prefixes are segment-aware so `/tmp` does not authorize `/tmp2`
- kernel_capability_full_set_hazard_evidence: src/verification/kernel_capabilities/KernelCapabilities/Basic.lean CapState.fullSetOnlyFor + sanitizeInitCaps + fileAccessAllowed + fileAccessAllowedWithPerm + segmentPrefixAllowed + rwFileAccessAllowed; src/verification/kernel_capabilities/KernelCapabilities/Theorems.lean full_set_allows_any_kind + full_set_syscall_authorizes_any_kind + full_set_owner_is_trusted + non_trusted_full_set_forbidden + sanitize_init_caps_non_trusted_full_is_empty + sanitize_init_caps_non_trusted_full_denies_all + sanitize_init_caps_non_trusted_full_syscall_denies_all + sanitize_init_caps_trusted_full_stays_full + revocation_complete_direct_child + revocation_complete_direct_child_syscall + revoke_non_owner_check_preserved + file_access_requires_capability + unveiled_file_access_requires_capability_and_path + file_access_before_unveil_depends_only_on_capability + invalid_permission_denies_file_access + valid_permission_uses_file_access_gate + path_prefix_without_segment_boundary_denied + root_path_prefix_allows + exact_path_prefix_allows + segment_boundary_prefix_allows + write_request_requires_write_capability + read_request_uses_read_capability + read_write_request_requires_both_capabilities; src/os/kernel/ipc/capability.spl init_task + revoke + revoke_transitive + check_file_access + _path_starts_with; src/os/kernel/types/capability_types.spl _path_prefix_allows; test/01_unit/os/kernel/ipc/capability_spec.spl lowers non-init ambient full sets to deny-all + revokes a capability token + before unveil file access still requires matching capability + before unveil explicit file capability allows matching path + read-only capability denies write/rw access + path prefix matching works correctly + invalid `rz` permission denial; test/01_unit/os/kernel/ipc/capability/capability_revoke_depth_spec.spl revoke_transitive of last unpledged token stays deny-all; `cd src/verification/kernel_capabilities && lake build`; `bin/simple test test/01_unit/os/kernel/ipc/capability_spec.spl --mode=interpreter`; `bin/simple test test/01_unit/os/kernel/ipc/capability/capability_revoke_depth_spec.spl --mode=interpreter`
- kernel_capability_full_set_check_note: `timeout 120s bin/simple check src/os/kernel/ipc/capability.spl` timed out after existing broad dependency warnings and no emitted capability.spl error
- memory_model_drf_formal: pass
- memory_model_drf_formal_scope: empty and single-operation executions, single-thread executions, multithread executions with no conflicts, any two non-conflicting operations, read-only same-location sharing, any two reads, any-location lock acquire/acquire pairs, fence/op pairs, or with all conflicting operations either same-thread or ordered by happens-before, program-order-only, synchronizes-with-only, program-order/reverse program-order, synchronizes-with/reverse synchronizes-with, or an explicit either-direction synchronizes-with edge for two operations, are data-race free; every race witness contains two distinct operations in the execution, two distinct threads, a real conflicting pair, and is unordered in both happens-before directions; synchronized same-location write/write, read/write, write/read, lock release/acquire, lock acquire/release, and lock release/release handoffs are data-race free; program-ordered same-location write/write, read/write, write/read, lock release/acquire, lock acquire/release, and lock release/release handoffs are data-race free; different-location write/write, read/write, and write/read two-operation executions are data-race free; conflicts and race witnesses are symmetric; fences are not conflicting memory operations; lock acquire is non-write-like and lock release is write-like; all read/read pairs and all lock acquire/acquire pairs are non-conflicting; same-location write/write, read/write, lock acquire/release, and lock release/release pairs are formally classified as conflicts; different-location write/write, read/write, write/read, and different-location lock acquire/release or release/release pairs are non-conflicting; unordered cross-thread same-location write/write, read/write, lock release/acquire, lock acquire/release, and lock release/release pairs are formally data races
- memory_model_drf_formal_evidence: src/verification/memory_model_drf/src/MemoryModelDRF.lean conflicts_symmetric + hasDataRace_symmetric + hasDataRace_has_conflict + hasDataRace_unordered + hasDataRace_has_distinct_ops + hasDataRace_has_distinct_threads + hasDataRace_witness_shape + drf_single_thread_execution + drf_empty_execution + drf_single_op_execution + drf_when_no_conflicts + drf_two_ops_no_conflict + drf_two_reads_same_location + drf_two_reads_any_location + drf_two_writes_different_location + drf_two_read_write_different_location + drf_two_write_read_different_location + drf_two_lock_acquires_same_location + drf_two_lock_acquires_any_location + drf_two_ops_fence_left + drf_two_ops_fence_right + drf_when_conflicts_ordered + drf_when_conflicts_same_thread_or_ordered + drf_when_conflicts_program_ordered + drf_when_conflicts_synchronized + drf_when_conflicts_program_or_sync_ordered + drf_two_ops_synchronized + drf_two_ops_synchronized_reverse + drf_two_ops_synchronized_either_direction + drf_two_ops_program_ordered + drf_two_ops_program_ordered_reverse + drf_two_synchronized_writes_same_location + drf_two_synchronized_read_write_same_location + drf_two_synchronized_write_read_same_location + drf_two_synchronized_lock_release_acquire_same_location + drf_two_synchronized_lock_acquire_release_same_location + drf_two_synchronized_lock_releases_same_location + drf_two_program_ordered_writes_same_location + drf_two_program_ordered_read_write_same_location + drf_two_program_ordered_write_read_same_location + drf_two_program_ordered_lock_release_acquire_same_location + drf_two_program_ordered_lock_acquire_release_same_location + drf_two_program_ordered_lock_releases_same_location + fence_left_no_conflict + fence_right_no_conflict + lock_acquire_is_not_write + lock_release_is_write + read_read_any_location_no_conflict + write_write_same_location_conflicts + read_write_same_location_conflicts + write_read_same_location_conflicts + write_write_different_location_no_conflict + read_write_different_location_no_conflict + write_read_different_location_no_conflict + lock_acquire_release_same_location_conflicts + lock_release_acquire_same_location_conflicts + lock_release_release_same_location_conflicts + lock_acquire_acquire_same_location_no_conflict + lock_acquire_acquire_any_location_no_conflict + lock_acquire_release_different_location_no_conflict + lock_release_acquire_different_location_no_conflict + lock_release_release_different_location_no_conflict + two_unordered_writes_same_location_race + two_unordered_read_write_same_location_race + two_unordered_write_read_same_location_race + two_unordered_lock_release_acquire_same_location_race + two_unordered_lock_acquire_release_same_location_race + two_unordered_lock_releases_same_location_race; `cd src/verification/memory_model_drf && lake build`
- riscv_product_formal: pass
- riscv_product_formal_scope: RV32/RV64 product profiles use RVFI/SBY formal gates, metadata and budget overrides preserve formal-gate constraints, round-robin alternates lanes, returns to the start lane after two steps, serves either lane within two turns, acquire succeeds from an empty resource, creates a single owner only from empty, never assigns a different owner, and only fails when the resource is already owned, held resources reject second owners, held resources reject reentrant acquire by the current owner, acquire-then-reentrant-acquire is denied, acquire-then-any-second-acquire is denied, failed second-lane acquire plus non-owner release preserves the original owner, owner release clears a resource, acquire/release on an empty resource roundtrips to empty, owner release after acquire clears the resource, wrong-lane release after acquire preserves the owner, empty release is a no-op, non-owner release cannot clear another lane's resource, a clear release result implies the requester was the owner, and an occupied release no-op implies the requester was not the owner
- riscv_product_formal_evidence: src/verification/riscv_product/src/RiscvProduct/Constraints.lean rv32_profile_expected + rv64_profile_expected + product_metadata_override_sets_fields + product_metadata_preserves_formal_gate + budget_override_sets_fields + next_lane_alternates + round_robin_no_starvation + round_robin_two_steps_returns_start + acquire_empty_succeeds + acquire_sets_single_owner + acquire_success_requires_empty + acquire_empty_never_sets_other_owner + acquire_none_requires_owner + held_resource_rejects_second_owner + held_resource_rejects_reentrant_acquire + acquire_then_reentrant_acquire_denied + acquire_then_any_second_acquire_denied + failed_second_acquire_and_release_preserves_owner + owner_release_clears_resource + acquire_release_roundtrip_empty + acquire_then_owner_release_clears + acquire_then_non_owner_release_preserves_owner + empty_release_noop + non_owner_release_preserves_resource + release_clears_only_for_owner + occupied_release_noop_implies_non_owner; `cd src/verification/riscv_product && lake build`
- simple_web_node_report: doc/09_report/simple_web_engine2d_js_bitmap_evidence_2026-06-02.md
- simple_web_bun_report: doc/09_report/bun_simple_web_engine2d_js_bitmap_evidence_2026-06-02.md
- production_gui_parity_report: doc/09_report/production_gui_web_renderer_parity_evidence_2026-06-29.md
- simple_web_node_env: build/simple_web_engine2d_js_bitmap_evidence_current/evidence.env
- simple_web_bun_env: build/bun_simple_web_engine2d_js_bitmap_evidence_current/evidence.env
- gui_entry_latest_dir: build/tmp/gui_entry_engine2d_wm_simple_web_spec_3406515_1782861032385213
- gui_entry_capture_ppm_bytes: 2359312
- gui_entry_capture_raw_bytes: 3145728
