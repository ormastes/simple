# SimpleOS Hardening Evidence Matrix

- status: pass
- reason: pass
- passed: 13/13
- lean_theorem_citation_audit: pass
- lean_theorem_citation_audit_scope: every Lean theorem declaration under `src/verification` has a matching theorem-name citation in this hardening evidence matrix; model definitions are excluded from this theorem-citation audit
- lean_theorem_citation_audit_evidence: `for f in $(find src/verification -path '*/.lake' -prune -o -name '*.lean' -print | sort); do miss=$(rg -o '^theorem [A-Za-z0-9_\.]+' "$f" | sed 's/^theorem //' | while read n; do rg -q "(^|[^A-Za-z0-9_.])$n([^A-Za-z0-9_]|$)" doc/09_report/simpleos_hardening_evidence_matrix_2026-07-03.md || echo "$n"; done | head -20); if [ -n "$miss" ]; then echo "$f"; echo "$miss" | sed 's/^/  /'; fi; done` produced no missing theorem names
- lean_package_build_audit: pass
- lean_package_build_audit_scope: all 21 Lean package roots under `src/verification` build through their stable `lake build` entry point
- lean_package_build_audit_evidence: `for d in $(find src/verification -name lakefile.toml -printf '%h\n' | sort); do (cd "$d" && lake build) || exit 1; done` passed for actor_channel, aop_weaver, async_compile, db_storage, fat32, formal/nvfs, gc_boundary, gc_manual_borrow, kernel_capabilities, kernel_scheduler, macro_auto_import, manual_pointer_borrow, memory_capabilities, memory_model_drf, module_resolution, nogc_compile, riscv_product, tensor_dimensions, type_inference_compile, ui_compositor, and visibility_export
- lean_trust_surface_audit: pass
- lean_trust_surface_audit_scope: Lean verification sources under `src/verification` contain no `sorry`, `admit`, `axiom`, `constant`, `opaque`, `unsafe`, `set_option autoImplicit true`, or `set_option maxHeartbeats 0` trust-surface bypasses
- lean_trust_surface_audit_evidence: `rg -n '^\s*(axiom|constant|opaque|unsafe|set_option\s+autoImplicit\s+true|set_option\s+maxHeartbeats\s+0)\b' src/verification --glob '*.lean' --glob '!.lake/**'` and `rg -n '^\s*(sorry|admit)\b' src/verification --glob '*.lean' --glob '!.lake/**'` both produced no hits
- byl_sby_artifact_audit: pass
- byl_sby_artifact_audit_scope: checked-in non-Lean formal artifacts currently consist of one BYL surface (`src/verification/riscv_product/riscv_product.byl`) and zero checked-in SBY files; generated SBY/RVFI artifacts are validated by the RISC-V sidecar contract and remain readiness evidence, not an RTL proof pass
- byl_sby_artifact_audit_evidence: `find . \( -path './.git' -o -path './.jj' -o -path './src/**/.lake' \) -prune -o \( -name '*.byl' -o -name '*.sby' -o -name '*formal*manifest*' -o -name '*rvfi*manifest*' \) -type f -print | sort` returned only `./src/verification/riscv_product/riscv_product.byl`; placeholder scan over `*.byl`, `*.sby`, `*formal*manifest*`, and `*rvfi*manifest*` produced no hits; with temporary `bin/simple` symlinked directly to the existing self-hosted runtime, `sh scripts/check/check-riscv-formal-dual-track.shs` reported `STATUS: PASS riscv-fpga-sidecar-contract` and `STATUS: PASS riscv-formal-dual-track`
- numbered_artifact_guard: pass
- numbered_artifact_guard_scope: no newly added or renamed numbered copy/version/part artifacts are present in the current working or staged evidence lane
- numbered_artifact_guard_evidence: `sh scripts/audit/numbered-artifact-guard.shs --self-test` reported `STATUS: PASS numbered-artifact-guard self-test`; `sh scripts/audit/numbered-artifact-guard.shs --working` and `--staged` reported `Numbered artifact guard: OK` in this jj checkout, with the expected Git no-repo diagnostic from the guard's Git-backed path listing
- runtime_bypass_guard: pass
- runtime_bypass_guard_scope: current working and staged changes introduce no app-leaf or `src/lib/gc_async_mut` direct env/process runtime bypasses outside owner facade modules
- runtime_bypass_guard_evidence: `sh scripts/audit/direct-env-runtime-guard.shs --working` and `sh scripts/audit/direct-env-runtime-guard.shs --staged` both reported `STATUS: PASS direct-env-runtime-guard`
- executable_launch_from_fs: pass
- ssh_shell_smf_and_exec: pass
- shared_wm_logic: pass
- cpu_simd_engine2d_diagram: pass
- cpu_simd_report: doc/09_report/cpu_simd_engine2d_evidence_current_2026-07-02.md
- llvm_port: pass
- llvm_port_report: doc/09_report/simpleos_llvm_port_evidence_current_2026-07-02.md
- gui_renderdoc_vulkan: pass
- gui_renderdoc_report: doc/09_report/gui_renderdoc_feature_coverage_status_2026-07-03.md
- web_renderer_engine2d_bitmap: pass
- simple_gui_webrenderer_bitmap: pass
- production_gui_web_renderer_parity: pass
- qemu_host_counterpart_bitmap: pass
- qemu_gui_smf_artifact_contract: pass
- qemu_gui_smf_artifact_contract_status: pass
- qemu_gui_smf_artifact_contract_row: GUI_SMF_ARTIFACT_CONTRACT status=pass artifact=build/gui/pure_gui_hot.smf sha256=d2978950c6ac8046b84e2d017b39434a47f77cf28476c8d4b744af423e698c48 size=69400 smf_role=2 arch=3 embedded_dynlib=true symbol=gui_dynlib_hot_probe_tick qemu_status=pass qemu_reason=arm64-smf-parity-verified macos_status=not-run macos_reason=requires-macos-arm64
- qemu_guest_perf_release_gate: guest-simple-frame-plus-host-gtk-baseline
- qemu_guest_perf_release_gate_status: pass
- qemu_guest_perf_release_blocker: none
- qemu_guest_perf_harness_status: pass
- qemu_guest_perf_harness_reason: pass
- qemu_guest_perf_harness_sample_origin: qemu-guest
- qemu_guest_perf_harness_required_sample_origin: qemu-guest
- qemu_guest_perf_harness_pending_marker: [desktop-e2e] qemu-perf sample_origin=qemu-guest simple_frame_cycles=<positive> iterations=<positive> timing_unit=tsc
- qemu_guest_perf_harness_marker_line: [desktop-e2e] qemu-perf sample_origin=qemu-guest simple_frame_cycles=251664583 iterations=16 timing_unit=tsc scope=simple-vga-paint
- qemu_host_perf_promotes_qemu_perf: false
- qemu_wm_simple_gui_mdi: pass
- qemu_wm_simple_gui_mdi_ppm_anchors: pass
- qemu_wm_simple_gui_mdi_ppm_nonblack: 779590
- qemu_wm_simple_gui_mdi_input_status: pass
- qemu_wm_simple_gui_mdi_input_before_ppm_bytes: 2359312
- qemu_wm_simple_gui_mdi_input_after_ppm_bytes: 2359312
- qemu_wm_simple_gui_mdi_input_before_raw_bytes: 3145728
- qemu_wm_simple_gui_mdi_input_after_raw_bytes: 3145728
- qemu_wm_simple_gui_mdi_input_before_sha256: 5fcc2dc196224169bc9a044e57bd2faa88f4ff3dddf415f6d4b44e805d2d1f8f
- qemu_wm_simple_gui_mdi_input_after_sha256: 1f3b7077cc368c8b8f072f80cc0975cd216734b24d918ba3eb8dd2f7867df760
- rv64_display_smoke_qmp: pass
- rv64_display_smoke_qmp_reason: rv64-display-smoke-qmp-wm-lifecycle-pass
- rv64_display_smoke_qmp_report: doc/09_report/rv64_display_smoke_qmp_evidence_current_2026-07-02.md
- rv64_display_smoke_qmp_width: 320
- rv64_display_smoke_qmp_height: 240
- rv64_display_smoke_qmp_nonblack: 76800
- rv64_display_smoke_qmp_anchor_matches: 5
- rv64_display_smoke_qmp_anchor_names: top,header,bodyA,bodyB,taskbar
- qemu_virtio_gpu_access: pass
- qemu_virtio_gpu_access_reason: rv64-qmp-virtio-gpu-display-smoke-pass
- qemu_virtio_gpu_access_contract: src/os/_QemuRunner/scenario_catalog.spl:scenario_riscv64_display_smoke
- kernel_scheduler_resource_formal: pass
- kernel_scheduler_resource_formal_scope: bounded ResourcePool acquire/release capacity, acquire grant iff capacity remains, acquire denial iff at capacity with exact denied result, release no-op iff empty and non-empty release is not a no-op, successful-acquire exact granted result and one-increment pool, strict increase, never-decrease monotonicity, capacity preservation, and well-formed result, failed-acquire live-count and capacity preservation, denied acquire followed by release equals releasing the original pool, zero-capacity denial/emptiness, non-empty release exact one-decrement pool, capacity preservation, decrement, and strict decrease, acquire/release capacity preservation and well-formedness preservation, release never underflows, release of a live resource from a well-formed pool reopens exactly one acquire slot and restores the original pool on the next acquire, acquire-release no-leak roundtrip invariants, acquire-release-release over-release equivalence to releasing the original pool, CPU affinity allows exactly unrestricted mask or set CPU bit with direct zero-mask/set-bit/not-set-bit lemmas, work stealing fires exactly above the remote-load threshold and stays off at or below it with direct above-threshold and at-threshold lemmas, new task exact runnable record with home CPU assigned, park recording reason and exact parked task record while preserving home/assigned CPU, parked unpark enqueues a runnable task, clears reason, assigns and reports the waker CPU placement plus exact runnable task record and exact enqueue wake decision, non-parked unpark reporting no placement move, preserving task identity, and exact no-op wake decision, done tasks cannot be woken back onto the ready queue, park/unpark/complete preserving task identity, completion marks done, done-complete no-op and double-complete preserving the first result, complete preserving CPU placement/accounting fields, non-done completion exact done-state record and result value, enqueue appending exactly one task to ready queue tail, ready-queue pop returning exactly the head/rest or no-oping when empty, runOne consuming exactly one ready task, strictly decreasing ready length, or no-oping when empty, empty runBatch no-op/empty proof, and enqueue-then-runBatch draining an empty-queue task in one scheduler batch
- kernel_scheduler_resource_formal_evidence: src/verification/kernel_scheduler/KernelScheduler/Theorems.lean new_is_runnable + new_home_eq_assigned + new_eq_runnable_record + park_state + park_preserves_home_cpu + park_preserves_assigned_cpu + park_records_reason + park_eq_parked_record + park_preserves_task_id + unpark_parked_enqueues + unpark_parked_runnable + unpark_clears_reason + unpark_parked_sets_assigned_cpu + unpark_parked_reports_waker_placement + unpark_parked_task_eq_runnable_record + unpark_parked_eq_enqueue_decision + unpark_parked_preserves_task_id + unpark_nonparked_noop + unpark_nonparked_reports_no_move + unpark_nonparked_eq_noop_decision + unpark_nonparked_task_id + unpark_done_no_enqueue + complete_marks_done + complete_done_noop + complete_double_safe + complete_double_preserves_first_result + complete_preserves_cpu_assignment + complete_records_result_when_not_done + complete_non_done_eq_done_record + complete_preserves_task_id + enqueue_increases_ready_length + enqueue_ready_eq_append + popHead_nonempty_returns_head + popHead_empty_noop + runOne_empty_noop + runOne_nonempty_drops_head + runOne_nonempty_decreases_length + enqueue_empty_then_runOne_empty + runBatch_empty + enqueue_then_runBatch_empty + runBatch_empty_is_empty + cpuAllowed_zero_mask + cpuAllowed_bit_not_set + cpuAllowed_bit_set + cpuAllowed_true_iff + steal_fires_above_threshold + steal_does_not_fire_at_threshold + shouldSteal_true_iff + shouldSteal_false_iff + resource_acquire_granted_iff_below_capacity + resource_acquire_full_eq_denied_result + resource_acquire_denied_iff_at_capacity + resource_acquire_below_capacity_grants + resource_acquire_below_capacity_eq_granted_result + resource_acquire_below_capacity_pool_eq + resource_acquire_below_capacity_preserves_capacity + resource_acquire_below_capacity_wf + resource_acquire_below_capacity_increases + resource_acquire_never_decreases + resource_acquire_full_noop + resource_acquire_denied_preserves_in_use + resource_acquire_denied_preserves_capacity + resource_denied_acquire_then_release_eq_release + resource_acquire_release_roundtrip + resource_acquire_release_release_eq_release + resource_zero_capacity_acquire_denied + resource_zero_capacity_wf_empty + resource_release_empty_noop + resource_release_noop_iff_empty + resource_release_nonempty_not_noop + resource_release_nonempty_preserves_capacity + resource_release_nonempty_pool_eq + resource_release_nonempty_decrements + resource_release_nonempty_decreases + resource_release_never_underflows + resource_acquire_preserves_wf + resource_release_preserves_wf + resource_release_preserves_capacity_bound + resource_acquire_preserves_capacity_bound + resource_release_nonempty_then_acquire_eq_granted_original; `cd src/verification/kernel_scheduler && lake build`
- actor_channel_backpressure_formal: pass
- actor_channel_backpressure_formal_scope: closed send returns the exact closed failure result without mutation and preserves closed state; closed empty recv returns the exact terminal no-park result; closed non-empty recv drains buffered values and preserves the closed channel state; buffered recv returns the exact received result, returns the queue head, advances the queue tail, keeps the channel open, and does not alter waiters; close_drain has exact wake accounting, no spurious wakeups, clears waiters, drains parked waiters, wakes all parked receivers, closes the channel, preserves buffered values, keeps them receivable, and is idempotent after close; bounded producer/consumer channel cannot overflow when full and no receiver is waiting and preserves open state; open empty recv returns the exact parked result; receiver parked by recv keeps the channel open, is woken by close_drain, and close-after-park wakes exactly the prior waiters plus that receiver; sends to a waiting receiver return the exact wake result and beat full-buffer backpressure without altering the buffer, no-lost-task send/close/recv-park obligations preserve task wakeability, per-sender FIFO and one-message processing preserve actor ordering, legacy sync-channel receive on a non-empty queue dequeues exactly the head, legacy closed send/try-send do not enqueue, dead or empty actors produce no scheduler mutation, handler errors do not halt actor or scheduler progress, a handler error on one scheduler tick does not starve the next queued successful actor message, and two queued successful actor messages dispatch in two scheduler ticks
- actor_channel_backpressure_formal_evidence: src/verification/actor_channel/ActorChannel/Theorems.lean recv_returns_head + recv_advances_tail + recv_buffered_eq_received_result + recv_buffered_preserves_open + recv_buffered_preserves_waiters + fifo_per_sender + process_one_at_most_one + process_one_preserves_fifo + closed_send_reports_failure + closed_send_no_mutation + closed_send_eq_closed_result + closed_send_preserves_closed + closed_empty_recv_no_park + closed_empty_recv_no_mutation + closed_empty_recv_eq_closed_result + closed_empty_recv_preserves_closed + closed_buffered_recv_drains_value + closed_buffered_recv_preserves_closed + close_drain_no_waiters_no_wake + close_drain_clears_waiters + close_drain_woken_eq_waiters + close_drains_parked + close_wakes_all_parked + close_drain_closes_channel + close_drain_preserves_buffer + close_then_recv_buffered_value + close_drain_idempotent_after_close + recv_open_empty_eq_parked_result + recv_parks_preserves_open + recv_then_close_wakes_parked + recv_then_close_exact_woken_waiters + send_to_waiter_wakes_without_buffer_growth + send_to_waiter_eq_wake_result + send_to_waiter_preserves_open + send_to_waiter_beats_full_buffer + send_buffered_preserves_open + send_buffered_eq_sent_result + bounded_channel_backpressure_no_overflow + bounded_channel_backpressure_preserves_open + no_lost_task_send + no_lost_task_close + no_lost_task_recv_parks + legacy_send_closed_noop + legacy_try_send_closed_fails + legacy_try_send_closed_no_enqueue + legacy_recv_nonempty_dequeues_head + scheduler_dead_actor_no_dispatch + scheduler_empty_mailbox_no_dispatch + dispatch_error_no_halt + scheduler_error_no_halt + scheduler_error_then_next_ok_dispatches + scheduler_two_ok_messages_dispatch_in_two_ticks; `cd src/verification/actor_channel && lake build`
- db_storage_wal_formal: pass
- db_storage_wal_formal_scope: transaction commit requires WAL flush, successful commit returns committed status, WAL append precedes commit on the D4 chain, WAL append uses the current next LSN and increments next_lsn monotonically, publish-root requires WAL flush and successful publish marks the root as published, pager writes are blocked when page LSN is ahead of flushed WAL LSN, and pager writes are allowed once WAL is flushed far enough
- db_storage_wal_formal_evidence: src/verification/db_storage/DbStorage/Theorems.lean T4_wal_before_data + T4_commit_sets_committed + T4_wal_appended_before_commit + T4_publish_root_requires_wal_flush + T4_publish_root_sets_published + T4_pager_wal_gate + T4_pager_wal_gate_blocked + T4_pager_wal_gate_allows_flushed; src/verification/db_storage/DbStorage/Wal.lean append_lsn_eq_next + append_next_lsn_succ + append_lsn_greatest; `cd src/verification/db_storage && lake build`
- db_storage_mvcc_formal: pass
- db_storage_mvcc_formal_scope: visible versions are committed before snapshot xmax, active inserts and future commits are excluded, committed deletes before the snapshot are excluded, and deletes by still-active transactions keep the version visible
- db_storage_mvcc_formal_evidence: src/verification/db_storage/DbStorage/Theorems.lean T5_snapshot_committed + T5_snapshot_excludes_active_insert + T5_snapshot_excludes_future + T5_snapshot_excludes_deleted + T5_snapshot_keeps_active_delete; `cd src/verification/db_storage && lake build`
- db_storage_btree_formal: pass
- db_storage_btree_formal_scope: B-tree ordered insertion preserves ordering and contains the inserted key, leaf split halves remain balanced, split children have pairwise uniform height, split membership stays within source ranges, and split occupancy bounds hold
- db_storage_btree_formal_evidence: src/verification/db_storage/DbStorage/Theorems.lean T1_btree_ordered + T1_btree_insert_contains_key + T2_btree_balanced_leaf + T3_btree_bounds; src/verification/db_storage/DbStorage/BTree.lean uniformHeight_pairwise + mem_take_of_mem + mem_drop_of_mem + orderedInsert_ordered + orderedInsert_mem + T2_btree_balanced_leaf_split + T2_btree_balanced_internal_split + T3_btree_bounds_split + T3_btree_bounds_insert_nonfull; `cd src/verification/db_storage && lake build`
- db_storage_recovery_formal: pass
- db_storage_recovery_formal_scope: checkpoint replay includes only data records at or after the checkpoint whose transaction id has a commit record in the same checkpoint window, and every committed data record in that window appears in replay
- db_storage_recovery_formal_evidence: src/verification/db_storage/DbStorage/Theorems.lean T6_recovery_equation + T6_replay_requires_committed_txn + T6_recovery_complete; `cd src/verification/db_storage && lake build`
- nvfs_mount_formal: pass
- nvfs_mount_formal_scope: arena liveness cons and invariant frame lemmas preserve individual NVFS invariants across unchanged state components; arena create preserves I1/I2/I4/I5/I6/I7/I8/I10 and all NVFS invariants; arena seal/append/discard/clone and pmap publish preserve all NVFS invariants, WAL append preserves all invariants and keeps WAL LSNs strictly increasing, successful mount preserves all NVFS invariants and increments mountEpoch exactly once; unmount preserves all invariants, checkpoint commit preserves all invariants, and checkpoint commit crash-refines replay state
- nvfs_mount_formal_evidence: src/verification/formal/nvfs/Nvfs/Theorems.lean arenaLive_cons_preserves + I1_frame + I2_frame + I3_frame + I4_frame + I5_frame + I6_frame + I7_frame + I8_frame + I9_frame + I10_frame + arena_create_preserves_I1 + arena_create_preserves_I2 + arena_create_preserves_I4 + arena_create_preserves_I5 + arena_create_preserves_I6 + arena_create_preserves_I7 + arena_create_preserves_I8 + arena_create_preserves_I10 + arena_create_preserves_all + arena_seal_preserves_all + arena_append_preserves_all + arena_discard_preserves_all + arena_clone_range_preserves_all + pmap_publish_preserves_all + walStrictlyIncreasing_append + wal_append_preserves_I4 + wal_append_preserves_all + mount_preserves_all + mount_success_increments_epoch + unmount_preserves_all + checkpoint_commit_preserves_all + checkpoint_commit_crash_refines; `cd src/verification/formal/nvfs && lake build`
- fat32_chain_formal: pass
- fat32_chain_formal_scope: guarded FAT32 chain walks are fuel bounded, zero fuel returns the empty chain, cluster-to-LBA mapping is monotone and injective for well-formed geometry, concrete 8.3 names roundtrip through encode/decode, cluster counts cover file size, EOC and FREE markers are exclusive, EOC, FREE, and BAD entries are not valid links, the concrete BAD marker is not a valid chain link, and a newly allocated free singleton is disjoint from existing chains before predecessor linking
- fat32_chain_formal_evidence: src/verification/fat32/Fat32/Theorems.lean T1_chain_length_bound + T1_zero_fuel_empty + T2_lba_monotone + T3_lba_injective + T4_encode_decode_README_MD + T4_encode_decode_KERNEL_ELF + T4_encode_decode_no_ext + T5_cluster_count_covers + T6_eoc_not_free + T6_eoc_not_valid_chain + T6_free_not_valid_chain + T6_bad_not_valid_chain + T6_bad_marker_not_valid_chain + T7a_valid_link_ge2 + T7b_free_not_in_chain + T7c_alloc_step2_new_chain_disjoint; `cd src/verification/fat32 && lake build`
- gc_boundary_formal: pass
- gc_boundary_formal_scope: alias-free GC/nogc checker acceptance prevents GC-tier values in nogc/noalloc contexts, noalloc-accepted bodies do not allocate and reject allocating callees, tier and allocation-order laws are reflexive/transitive with join upper bounds, allocation inference joins are monotone, explicit Copy nodes are alias-free, Copy results are nogc rather than gc, and the alias-blind counterexample is machine-checked as GC-tier but structurally noalloc-ok
- gc_boundary_formal_evidence: src/verification/gc_boundary/GcBoundary/Basic.lean Tier.le_refl + Tier.le_trans + AllocAnn.le_refl + AllocAnn.join_ge_left + AllocAnn.join_ge_right; src/verification/gc_boundary/GcBoundary/Theorems.lean check_sound_wrt_model + noalloc_closed + noalloc_rejects_alloc_call + inference_monotone + inference_join_ge + alloc_join_alloc + copy_alias_free + copy_isolates + copy_result_not_gc; src/verification/gc_boundary/GcBoundary/AliasCounterexample.lean aliasExpr_not_alias_free + aliasExpr_has_tier_gc + aliasExpr_noalloc_body_ok; `cd src/verification/gc_boundary && lake build`
- nogc_compile_formal: pass
- nogc_compile_formal_scope: nogc instruction lists compose under append and explicitly reject GC allocation instructions
- nogc_compile_formal_evidence: src/verification/nogc_compile/src/NogcCompile.lean nogc_append + nogc_singleton + gc_alloc_singleton_not_nogc; `cd src/verification/nogc_compile && lake build`
- gc_manual_borrow_formal: pass
- gc_manual_borrow_formal_scope: manual borrows are modeled as GC pins; allocation makes the allocated object live, allocation-then-borrow records a manual pin, borrowing any live object records a manual pin, borrowing a non-live object is a no-op, unpinned allocation-then-collect decrements the allocated object's live count, collecting after a live-object borrow is a no-op, collecting a borrowed object is a no-op, collection preserves the manual pin list and keeps every borrowed object live, collecting an unborrowed object decrements its live count, release decrements the target borrow count and keeps zero-count ids at zero pins, and allocation, borrow, release, safe collection, and release-then-collect preserve the invariant that every borrowed object remains live
- gc_manual_borrow_formal_evidence: src/verification/gc_manual_borrow/src/GcManualBorrow.lean collect_borrowed_noop + collect_unborrowed_decrements_live_count + collect_preserves_borrowed_list + collect_keeps_borrowed_live + allocate_preserves + allocate_makes_live + allocate_unborrowed_then_collect_decrements_live_count + allocate_then_borrow_records_pin + borrow_live_records_pin + borrow_nonlive_noop + borrow_live_then_collect_noop + borrow_preserves + release_preserves + release_decrements_borrow_count + release_zero_count_stays_zero + collect_preserves + release_then_collect_preserves; `cd src/verification/gc_manual_borrow && lake build`
- memory_capabilities_formal: pass
- memory_capabilities_formal_scope: empty environments allow shared, exclusive, and isolated references, deny all access attempts, are well-formed, empty environments have a complete policy proof allowing all first-reference capabilities while remaining well-formed and rejecting access, creating any reference from an empty environment yields a well-formed singleton environment for each capability, singleton environments are well-formed for any capability, two-shared-reference environments are well-formed, creating a second shared alias from a singleton shared environment yields a well-formed two-shared-reference environment and combined read-only policy proof, two-exclusive, two-isolated, or mixed exclusive+isolated-reference environments are not well-formed, capability conversion is reflexive, permits direct downgrades with a combined downgrade proof, but denies shared-to-exclusive, shared-to-isolated, and exclusive-to-isolated upgrades with combined upgrade-denial and conversion-table policy proofs, boolean capability conversion is equivalent to the Prop-level restrictive relation, any existing reference blocks creating unique exclusive or isolated references at the same location, existing one- or two-shared-reference environments allow shared alias creation, existing exclusive or isolated references block creating a shared alias at the same location, successful reference access implies the target is the reference location, read access is allowed exactly at the reference location for every capability, singleton references of any capability permit reading exactly their own location, shared references cannot write any location, singleton and two-shared-reference environments are read-only at that location, write access is allowed exactly at the reference location with exclusive or isolated capability at both reference and singleton-environment level, singleton or two-shared-reference environments permit a read and reject a write at that location, exclusive or isolated references can write the referenced location, singleton exclusive and isolated-reference environments permit a write at that location, and reads or writes through a reference to a different location are denied at both reference and singleton-environment level including combined singleton read/write rejection
- memory_capabilities_formal_evidence: src/verification/memory_capabilities/src/MemoryCapabilities.lean can_convert_refl + exclusive_to_shared + isolated_to_exclusive + isolated_to_shared + capability_downgrades_allowed + shared_to_exclusive_denied + exclusive_to_isolated_denied + shared_to_isolated_denied + capability_upgrades_denied + capability_conversion_table_policy + can_convert_implies_restrictive + restrictive_implies_can_convert + can_convert_iff_restrictive + empty_env_allows_shared + empty_env_allows_exclusive + empty_env_allows_isolated + empty_env_allows_all_caps + empty_env_access_not_safe + empty_env_wellformed + empty_env_wellformed_and_no_access + empty_env_complete_policy + singleton_env_wellformed + empty_create_ref_wellformed + empty_create_all_singletons_wellformed + two_shared_env_wellformed + shared_alias_create_wellformed + shared_alias_create_read_only_policy + two_exclusive_env_not_wellformed + two_isolated_env_not_wellformed + mixed_exclusive_isolated_env_not_wellformed + existing_ref_denies_exclusive + existing_ref_denies_isolated + existing_ref_denies_unique_create + existing_shared_allows_shared + two_shared_allows_shared + existing_exclusive_denies_shared + existing_isolated_denies_shared + shared_read_same_loc + read_same_loc_any_cap + singleton_shared_read_safe + singleton_read_safe_any_cap + singleton_read_safe_iff_same_location + two_shared_read_safe + read_access_implies_same_location + read_access_iff_same_location + exclusive_write_same_loc + singleton_exclusive_write_safe + isolated_write_same_loc + singleton_isolated_write_safe + singleton_unique_write_safe + singleton_write_safe_iff_same_location_and_unique_cap + write_access_implies_unique_cap + write_access_implies_same_location_and_unique_cap + write_access_iff_same_location_and_unique_cap + access_implies_same_location + shared_write_same_loc_denied + shared_write_any_loc_denied + singleton_shared_write_not_safe + singleton_shared_read_only_at_location + two_shared_write_not_safe + two_shared_read_only_at_location + read_wrong_location_denied + singleton_read_wrong_location_not_safe + write_wrong_location_denied + singleton_write_wrong_location_not_safe + singleton_access_wrong_location_not_safe; `cd src/verification/memory_capabilities && lake build`
- manual_pointer_borrow_formal: pass
- manual_pointer_borrow_formal_scope: valid borrow states exclude shared borrows while exclusive, typed valid states are always valid, roundtrip conversion preserves valid states, taking shared while exclusive is held is a no-op, taking shared preserves the exclusive flag and with no exclusive borrow increments the shared count, shared take then release when not exclusive returns the original state, taking shared then attempting exclusive is a no-op, active shared borrows block exclusive borrow, taking exclusive preserves shared count and with no shared borrows sets the flag, a valid exclusive-take result has no shared borrows, and exact canonical exclusive state, releasing that exclusive borrow returns the exact unborrowed state, releasing exclusive clears the exclusive flag while preserving shared count and is a no-op when no exclusive borrow is held, releasing shared preserves the exclusive flag, releasing shared at zero keeps the shared count at zero, releasing shared with an active shared borrow strictly decreases the shared count, and take/release operations preserve validity
- manual_pointer_borrow_formal_evidence: src/verification/manual_pointer_borrow/src/ManualPointerBorrow.lean valid_exclusive_has_no_shared + validState_always_valid + toValid_toState + exclusive_ok + shared_ok + take_shared_exclusive_noop + take_shared_increments_when_not_exclusive + take_shared_preserves_exclusive + take_shared_then_release_when_not_exclusive + take_shared_then_exclusive_noop_when_not_exclusive + take_exclusive_shared_noop + take_exclusive_sets_flag_when_no_shared + take_exclusive_valid_has_no_shared + take_exclusive_no_shared_eq_exclusive + take_exclusive_preserves_shared + release_exclusive_clears + release_exclusive_preserves_shared + release_exclusive_false_noop + take_exclusive_then_release_no_shared + release_shared_preserves_exclusive + release_shared_zero_noop + release_shared_decreases_when_present + release_ok; `cd src/verification/manual_pointer_borrow && lake build`
- tensor_memory_formal: pass
- tensor_memory_formal_scope: per-component tensor memory min/max bounds compose into total min <= total max, and any actual usage below total max fits when total max fits available device memory
- tensor_memory_formal_evidence: src/verification/tensor_dimensions/src/TensorMemory.lean training_total_min_le_max + training_fits_if_max_fits; `cd src/verification/tensor_dimensions && lake build`
- tensor_dimensions_formal: pass
- tensor_dimensions_formal_scope: tensor shape compatibility is reflexive, mismatched ranks are rejected, successful dimension unification returns the expected dimension, matmul shape inference is deterministic, and computed min/max element bounds are comparable when both exist
- tensor_dimensions_formal_evidence: src/verification/tensor_dimensions/src/TensorDimensions.lean shapesCompatible_refl + shapesCompatible_rank_mismatch_left + unifyDim_success_eq + matmulShape_deterministic + min_le_max_elements; `cd src/verification/tensor_dimensions && lake build`
- type_inference_core_formal: pass
- type_inference_core_formal_scope: core expression type inference is deterministic and rejects numeric addition with a boolean operand
- type_inference_core_formal_evidence: src/verification/type_inference_compile/src/TypeInferenceCompile.lean infer_deterministic + infer_add_rejects_bool_operand; `cd src/verification/type_inference_compile && lake build`
- contract_verification_formal: pass
- contract_verification_formal_scope: empty function contracts contain no preconditions, invariants, postconditions, or error postconditions
- contract_verification_formal_evidence: src/verification/type_inference_compile/src/Contracts.lean empty_contract_has_no_preconditions + empty_contract_has_no_postconditions + empty_contract_has_no_clauses; `cd src/verification/type_inference_compile && lake build`
- type_inference_generics_formal: pass
- type_inference_generics_formal_scope: empty substitution is identity, single substitution rewrites its target type variable, occurs checks reject primitive non-recursive types and accept the same variable, instantiation/generalization preserve type shape, and literal inference is deterministic
- type_inference_generics_formal_evidence: src/verification/type_inference_compile/src/Generics.lean applySubst_empty + apply_single_subst_var + occurs_nat_false + occurs_bool_false + occurs_str_false + occurs_var_true + instantiate_preserves_shape + generalize_preserves_type + infer_lit_nat + infer_lit_bool + infer_lit_str; `cd src/verification/type_inference_compile && lake build`
- async_effect_inference_formal: pass
- async_effect_inference_formal_scope: effect inference is deterministic, suspension operations imply async classification, async calls propagate async effects, literals stay sync, explicit sync declarations reject suspension bodies, async return types are promise-wrapped without double wrapping, await insertion is sound for matching promise inner types, non-promise i32 actual values do not insert await, and promise unwrap succeeds only for matching inner type
- async_effect_inference_formal_evidence: src/verification/type_inference_compile/src/AsyncEffectInference.lean effect_deterministic + suspension_implies_async + async_propagation + lit_is_sync + sync_safety + async_returns_promise + sync_no_promise_wrap + no_double_wrap + await_inference_sound + await_not_inserted_for_i32 + promise_unwrap_correct; src/compiler_rust/lib/std/src/verification/regenerate/async_effect_inference.spl generated theorem source; `cd src/verification/type_inference_compile && lake build`
- async_compile_formal: pass
- async_compile_formal_scope: async compile pipelines compose only from async-safe effects and reject blocking wait effects
- async_compile_formal_evidence: src/verification/async_compile/src/AsyncCompile.lean append_safe + wait_detected + wait_pipeline_not_safe; `cd src/verification/async_compile && lake build`
- visibility_export_formal: pass
- visibility_export_formal_scope: visibility meet is commutative and associative, private visibility absorbs from either side, all-public ancestor paths remain public, any private ancestor makes the path private, and a private symbol remains private even under a public ancestor path
- visibility_export_formal_evidence: src/verification/visibility_export/src/VisibilityExport.lean meet_comm + meet_assoc + meet_private_left + meet_private_right + meet_public_left + meet_public_right + foldl_priv_absorbs + any_private_means_private + all_public_means_public + private_symbol_under_public_path_private; `cd src/verification/visibility_export && lake build`
- module_resolution_formal: pass
- module_resolution_formal_scope: module resolution is deterministic, well-formed roots cannot resolve as both file and directory, unique resolutions imply the resolved path exists, not-found means neither candidate exists, and simultaneous file/directory candidates resolve to the exact ambiguous result
- module_resolution_formal_evidence: src/verification/module_resolution/src/ModuleResolution.lean resolve_deterministic + wellformed_not_ambiguous + unique_implies_exists + notfound_means_neither + both_paths_existing_resolves_ambiguous; `cd src/verification/module_resolution && lake build`
- macro_auto_import_formal: pass
- macro_auto_import_formal_scope: glob imports include non-macros, include explicitly auto-imported macros, auto-import success implies the symbol is a macro, exclude non-auto-imported macros from well-formed exports, and empty auto-import manifests glob exactly non-macro exports
- macro_auto_import_formal_evidence: src/verification/macro_auto_import/src/MacroAutoImport.lean auto_import_true_implies_macro + glob_doesnt_leak_macros_wf + nonmacros_always_globbed + auto_imported_in_glob + glob_subset + empty_auto_import_no_macros + empty_auto_import_glob_nonmacros; `cd src/verification/macro_auto_import && lake build`
- aop_weaver_formal: pass
- aop_weaver_formal_scope: aspect insertion/sort preserves non-empty head, sortedness, and stability, aspect selection is sorted and stable in the model, no-match wrapping preserves target execution, denying advice skips the target and increments denial count, denial counts are monotone, and well-formed around chains execute the target at most once
- aop_weaver_formal_evidence: src/verification/aop_weaver/src/AopWeaver/Model.lean insAspect_ne + insAspect_cons_head + insAspect_nil_head + insAspect_sorted + insAspect_stable + iSort_sorted_stable + T1_selection_sorted + T2_selection_stable + T3_no_match_preserves + deny_increments + T4_deny_skips_target + T5_deny_monotone + T6_proceed_linear; `cd src/verification/aop_weaver && lake build`
- ui_compositor_formal: pass
- ui_compositor_formal_scope: damage additions append exactly one region and retain the newly damaged rect, merged damage covers both inputs, clips stay inside both source rects, z-order sorts preserve all windows, stacking-context sorts preserve all contexts, renormalization preserves relative order, and recursive flattening preserves all tree surfaces
- ui_compositor_formal_evidence: src/verification/ui_compositor/UiCompositor/Theorems.lean damage_add_increases_length + damage_add_contains_new_rect + merge_covers_left + merge_covers_right + clip_inside_left + clip_inside_right + insertSorted_is_perm + sortStackingContexts_perm + renorm_order_preserving + flattenTree_perm_treeSurfaces; `cd src/verification/ui_compositor && lake build`
- kernel_capability_depth_formal: pass
- kernel_capability_depth_formal_scope: capability delegation with exhausted depth is denied
- kernel_capability_depth_formal_evidence: src/verification/kernel_capabilities/KernelCapabilities/Theorems.lean zero_depth_grant_denied; `cd src/verification/kernel_capabilities && lake build`
- kernel_capability_empty_set_formal: pass
- kernel_capability_empty_set_formal_scope: no capability record and CapSet.empty deny every direct check and syscall authorization
- kernel_capability_empty_set_formal_evidence: src/verification/kernel_capabilities/KernelCapabilities/Theorems.lean default_deny + default_deny_syscall + empty_set_denies_all + empty_set_syscall_denies_all; `cd src/verification/kernel_capabilities && lake build`
- kernel_capability_full_set_hazard_formal: pass
- kernel_capability_full_set_hazard_scope: capability grants are formally non-escalating and attenuating over reflexive/transitive rights-subset ordering with the boolean rights gate equivalent to the Prop-level subset relation, zero-depth grants are denied, default and empty capability sets deny checks and syscall authorization, CapSet.full is formally verified to allow any capability kind through direct checks and syscall authorization; the full-set restriction invariant proves every full-set holder is the trusted init/kernel principal and excludes full-set records for non-trusted principals; runtime CapabilityManager.init_task lowers non-init ambient full-set input to deny-all, and the sanitized non-trusted record denies every capability-kind check and syscall authorization; direct and transitive revocation completion remove revoked authority and pledge the empty set for the last explicit token so revocation cannot create ambient full authority, direct owner-only revoke preserves non-owner checks, direct-child transitive revocation removes child authority at check and syscall gates, file path access requires matching FileRead/FileWrite/FileExec/FileCreate capability before unveil path checks, invalid permission strings fail closed, read and write requests are denied without their matching capability bits, and file/unveil path prefixes are segment-aware so `/tmp` does not authorize `/tmp2`
- kernel_capability_full_set_hazard_evidence: src/verification/kernel_capabilities/KernelCapabilities/Basic.lean Rights.subsetB_iff + CapState.fullSetOnlyFor + sanitizeInitCaps + fileAccessAllowed + fileAccessAllowedWithPerm + segmentPrefixAllowed + rwFileAccessAllowed; src/verification/kernel_capabilities/KernelCapabilities/Theorems.lean rights_subset_refl + rights_subset_trans + non_escalation + zero_depth_grant_denied + attenuation + gate_sound + gate_denies_without_capability + default_deny + default_deny_syscall + empty_set_denies_all + empty_set_syscall_denies_all + full_set_allows_any_kind + full_set_syscall_authorizes_any_kind + full_set_owner_is_trusted + non_trusted_full_set_forbidden + sanitize_init_caps_non_trusted_full_is_empty + sanitize_init_caps_non_trusted_full_denies_all + sanitize_init_caps_non_trusted_full_syscall_denies_all + sanitize_init_caps_trusted_full_stays_full + revocation_complete + revocation_complete_direct + revocation_complete_direct_child + revocation_complete_direct_child_syscall + revoke_non_owner_check_preserved + file_access_requires_capability + unveiled_file_access_requires_capability_and_path + file_access_before_unveil_depends_only_on_capability + invalid_permission_denies_file_access + valid_permission_uses_file_access_gate + path_prefix_without_segment_boundary_denied + root_path_prefix_allows + exact_path_prefix_allows + segment_boundary_prefix_allows + write_request_requires_write_capability + read_request_requires_read_capability + read_request_uses_read_capability + read_write_request_requires_both_capabilities; src/os/kernel/ipc/capability.spl init_task + revoke + revoke_transitive + check_file_access + _path_starts_with; src/os/kernel/types/capability_types.spl _path_prefix_allows; test/01_unit/os/kernel/ipc/capability_spec.spl lowers non-init ambient full sets to deny-all + revokes a capability token + before unveil file access still requires matching capability + before unveil explicit file capability allows matching path + read-only capability denies write/rw access + path prefix matching works correctly + invalid `rz` permission denial; test/01_unit/os/kernel/ipc/capability/capability_revoke_depth_spec.spl revoke_transitive of last unpledged token stays deny-all; `cd src/verification/kernel_capabilities && lake build`; `bin/simple test test/01_unit/os/kernel/ipc/capability_spec.spl --mode=interpreter`; `bin/simple test test/01_unit/os/kernel/ipc/capability/capability_revoke_depth_spec.spl --mode=interpreter`
- kernel_capability_full_set_check_note: `timeout 120s bin/simple check src/os/kernel/ipc/capability.spl` timed out after existing broad dependency warnings and no emitted capability.spl error
- memory_model_drf_formal: pass
- memory_model_drf_formal_scope: empty and single-operation executions, single-thread executions, multithread executions with no conflicts or no cross-thread conflicts, multithread executions whose conflicting pairs stay same-thread, any two non-conflicting operations, read-only same-location sharing, any two reads, any-location lock acquire/acquire pairs, fence/op pairs, or with all conflicting operations either same-thread, ordered by happens-before, cross-thread conflicts ordered by happens-before, or cross-thread conflicts ordered by primitive program-order/synchronizes-with edges, program-order-only, synchronizes-with-only, program-order/reverse program-order, synchronizes-with/reverse synchronizes-with, or an explicit either-direction synchronizes-with edge for two operations, are data-race free; primitive program-order and synchronizes-with edges imply happens-before; data-race-free executions satisfy the SC-for-DRF model theorem; data-race freedom is equivalent to absence of a data race; any race witness contradicts data-race freedom, and data-race freedom excludes every concrete or existential unordered conflicting cross-thread witness; every race witness contains two distinct operations in the execution, two distinct threads, a real conflicting pair, is unordered in both happens-before directions, lacks primitive program-order/synchronizes-with edges in both directions, and therefore exposes a cross-thread conflict without primitive ordering; synchronized same-location write/write, read/write, write/read, lock release/acquire, lock acquire/release, and lock release/release handoffs are data-race free; program-ordered same-location write/write, read/write, write/read, lock release/acquire, lock acquire/release, and lock release/release handoffs are data-race free; different-location write/write, read/write, and write/read two-operation executions are data-race free; conflicts and race witnesses are symmetric; fences are not conflicting memory operations; reads are not write-like, writes are write-like, lock acquire is non-write-like, and lock release is write-like; all read/read pairs and all lock acquire/acquire pairs are non-conflicting; same-location write/write, read/write, lock acquire/release, and lock release/release pairs are formally classified as conflicts; different-location write/write, read/write, write/read, and different-location lock acquire/release or release/release pairs are non-conflicting; unordered cross-thread same-location write/write, read/write, write/read, lock release/acquire, lock acquire/release, and lock release/release executions are formally not data-race free, and unordered cross-thread same-location write/write, read/write, lock release/acquire, lock acquire/release, and lock release/release pairs are formally data races
- memory_model_drf_formal_evidence: src/verification/memory_model_drf/src/MemoryModelDRF.lean conflicts_symmetric + hb_program_order + hb_sync + scDRF + dataRaceFree_iff_no_dataRace + hasDataRace_not_dataRaceFree + dataRaceFree_excludes_witness + dataRaceFree_no_witness + hasDataRace_symmetric + hasDataRace_has_conflict + hasDataRace_unordered + hasDataRace_unordered_by_primitives + hasDataRace_cross_thread_conflict_without_primitive_order + hasDataRace_has_distinct_ops + hasDataRace_has_distinct_threads + hasDataRace_witness_shape + drf_single_thread_execution + drf_empty_execution + drf_single_op_execution + drf_when_no_conflicts + drf_when_no_cross_thread_conflicts + drf_two_ops_no_conflict + drf_two_reads_same_location + drf_two_reads_any_location + drf_two_writes_different_location + drf_two_read_write_different_location + drf_two_write_read_different_location + drf_two_lock_acquires_same_location + drf_two_lock_acquires_any_location + drf_two_ops_fence_left + drf_two_ops_fence_right + drf_when_conflicts_ordered + drf_when_conflicts_same_thread_or_ordered + drf_when_conflicts_same_thread + drf_when_cross_thread_conflicts_ordered + drf_when_cross_thread_conflicts_program_or_sync_ordered + drf_when_conflicts_program_ordered + drf_when_conflicts_synchronized + drf_when_conflicts_program_or_sync_ordered + drf_two_ops_synchronized + drf_two_ops_synchronized_reverse + drf_two_ops_synchronized_either_direction + drf_two_ops_program_ordered + drf_two_ops_program_ordered_reverse + drf_two_synchronized_writes_same_location + drf_two_synchronized_read_write_same_location + drf_two_synchronized_write_read_same_location + drf_two_synchronized_lock_release_acquire_same_location + drf_two_synchronized_lock_acquire_release_same_location + drf_two_synchronized_lock_releases_same_location + drf_two_program_ordered_writes_same_location + drf_two_program_ordered_read_write_same_location + drf_two_program_ordered_write_read_same_location + drf_two_program_ordered_lock_release_acquire_same_location + drf_two_program_ordered_lock_acquire_release_same_location + drf_two_program_ordered_lock_releases_same_location + read_is_not_write + write_is_write + read_read_no_conflict + fence_left_no_conflict + fence_right_no_conflict + lock_acquire_is_not_write + lock_release_is_write + read_read_any_location_no_conflict + write_write_same_location_conflicts + read_write_same_location_conflicts + write_read_same_location_conflicts + write_write_different_location_no_conflict + read_write_different_location_no_conflict + write_read_different_location_no_conflict + lock_acquire_release_same_location_conflicts + lock_release_acquire_same_location_conflicts + lock_release_release_same_location_conflicts + lock_acquire_acquire_same_location_no_conflict + lock_acquire_acquire_any_location_no_conflict + lock_acquire_release_different_location_no_conflict + lock_release_acquire_different_location_no_conflict + lock_release_release_different_location_no_conflict + two_unordered_writes_same_location_not_drf + two_unordered_read_write_same_location_not_drf + two_unordered_write_read_same_location_not_drf + two_unordered_lock_release_acquire_same_location_not_drf + two_unordered_lock_acquire_release_same_location_not_drf + two_unordered_lock_releases_same_location_not_drf + two_unordered_writes_same_location_race + two_unordered_read_write_same_location_race + two_unordered_write_read_same_location_race + two_unordered_lock_release_acquire_same_location_race + two_unordered_lock_acquire_release_same_location_race + two_unordered_lock_releases_same_location_race; `cd src/verification/memory_model_drf && lake build`
- riscv_product_formal: pass
- riscv_product_formal_scope: RV32/RV64 product profiles are distinct lanes, generated BYL pins the compact product/profile/budget/formal-gate/resource/scheduler surface consumed by tool gates, RV32/RV64 product profiles and every product lane use RVFI/SBY formal gates, every product profile has positive LUT and MHz budgets, metadata and budget overrides preserve formal-gate constraints, round-robin alternates lanes, returns to the start lane after two steps, serves either lane within two turns, acquire succeeds from an empty resource, creates the exact single-owner grant iff empty, creates a single owner only from empty, never assigns a different owner, has deterministic success results, and fails exactly when the resource is already owned, held resources reject second owners, held resources reject reentrant acquire by the current owner, acquire-then-reentrant-acquire is denied, acquire-then-any-second-acquire is denied, failed second-lane acquire plus non-owner release preserves the original owner, failed acquire followed by owner release reopens acquisition for the requester, release has deterministic results, empty release result occurs iff the resource was already empty or the requester was the owner, owner release clears a resource and reopens acquisition for any lane, any successful acquire is cleared by owner release, acquire/release on an empty resource roundtrips to empty, owner release after acquire clears the resource, wrong-lane release after acquire preserves the owner, empty release is a no-op, non-owner release cannot clear another lane's resource for literal and arbitrary occupied states, a clear release result implies the requester was the owner for literal and arbitrary occupied states, and an occupied release no-op implies the requester was not the owner
- riscv_product_formal_evidence: src/verification/riscv_product/riscv_product.byl generated_surface=byl + formal_gate=rvfi+sby + starvation_bound=2 + shared_bus single_owner_only/owner_clears; src/verification/riscv_product/GENERATED_CONTRACT.md stable BYL facts and regeneration rule; src/verification/riscv_product/src/RiscvProduct/Constraints.lean rv32_profile_expected + rv64_profile_expected + profiles_are_dual_arch + all_profiles_use_rvfi_sby + profile_budgets_positive + product_metadata_override_sets_fields + product_metadata_preserves_formal_gate + budget_override_sets_fields + next_lane_alternates + round_robin_no_starvation + round_robin_two_steps_returns_start + acquire_empty_succeeds + acquire_grants_iff_empty + acquire_sets_single_owner + acquire_success_requires_empty + acquire_success_deterministic + acquire_empty_never_sets_other_owner + acquire_none_requires_owner + acquire_none_iff_occupied + held_resource_rejects_second_owner + held_resource_rejects_reentrant_acquire + acquire_then_reentrant_acquire_denied + acquire_then_any_second_acquire_denied + failed_second_acquire_and_release_preserves_owner + failed_acquire_then_owner_release_allows_requester + release_deterministic + release_empty_iff_unowned_or_owner + owner_release_clears_resource + owner_release_then_any_lane_acquires + successful_acquire_then_owner_release_clears + acquire_release_roundtrip_empty + acquire_then_owner_release_clears + acquire_then_non_owner_release_preserves_owner + empty_release_noop + non_owner_release_preserves_resource + non_owner_release_preserves_occupied + release_clears_only_for_owner + release_clears_occupied_only_for_owner + occupied_release_noop_implies_non_owner; `cd src/verification/riscv_product && lake build`
- riscv_rtl_rvfi_readiness_note: `/tmp/simple_riscv_fpga_system_spec/rv32/rtl/simple_rv32gc_core.vhd` and `/tmp/simple_riscv_fpga_system_spec/rv64/rtl/simple_rv64gc_core.vhd` have RVFI port manifests and pass the generated formal harness/SBY/manifest artifact check, but `scripts/rtl/check-rvfi-formal-readiness.shs` currently reports `SKIP: sby not found`; this is RVFI/SymbiYosys readiness evidence, not an RTL proof pass
- simple_web_node_report: doc/09_report/simple_web_engine2d_js_bitmap_evidence_2026-06-02.md
- simple_web_bun_report: doc/09_report/bun_simple_web_engine2d_js_bitmap_evidence_2026-06-02.md
- production_gui_parity_report: doc/09_report/production_gui_web_renderer_parity_evidence_2026-06-29.md
- simple_web_node_env: build/simple_web_engine2d_js_bitmap_evidence_current/evidence.env
- simple_web_bun_env: build/bun_simple_web_engine2d_js_bitmap_evidence_current/evidence.env
- gui_entry_latest_dir: build/tmp/gui_entry_engine2d_wm_simple_web_spec_3406515_1782861032385213
- gui_entry_capture_ppm_bytes: 2359312
- gui_entry_capture_raw_bytes: 3145728
