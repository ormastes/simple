# DB WAL Compiled Timing

Date: 2026-06-16

This report is generated by `test/05_perf/db/db_wal_compiled_timing_spec.spl` under `bin/simple test --mode=native`.

## Results

| name | mode | metric | value | unit | arch | plane |
|------|------|--------|------:|------|------|-------|
| db_wal_insert100 | wal | compiled_insert+count_visible | 54060 | us | x86_64 | native |

The workload inserts 100 MVCC WAL-core rows, commits each transaction, then validates `count_visible == 100` before recording timing.
