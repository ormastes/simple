# Stage 3 seed-wrapper scope audit — 2026-09-22

Base: `e0dd873da1b7828389db4eb60e82972cc8245313` (`origin/main`). Isolated
Windows worktree: `D:/wk-p1-stage3-wrapper`. No production source, launcher,
codegen, runtime, cache, or deployed artifact is changed.

## Finding and correction

The historical fallback mechanism is removed; the convergence umbrella remains
**Open**. `bootstrap_main.spl` dispatches the exact Stage 3 entry to
`run_exact_stage3_focused_capsule`, which requires the Stage 3 environment flag,
the bootstrap entry, and dynload mode, and calls the real focused native build.
`driver_bootstrap.spl` returns a codegen error from `bootstrap_emit_seed_wrapper`.
This is source evidence, not proof that the complete compilation succeeds.

The integration helper rejected every occurrence of `compiler_rust`, including
the parser-path comment at `bootstrap_main.spl:239`. The corresponding unit
helper already excludes documentation-path mentions. This change aligns the
integration helper and adds source-citation, empty-input, `execv`, environment,
and ret-zero controls. The original broad llvm-lib routing scenario is retained.
No claim is made that substring guards prove absence of every possible seed
delegation: explicit `rt_native_build` routes remain outside the exact stage
path.

## Measured common launcher behavior

Host: Windows x86_64, PowerShell 7, Git `sh.exe`. The test extracts the exact
block from `# Decide which compiler to use for stage 4` through the next
separator in `scripts/bootstrap/bootstrap-from-scratch.sh`. It runs that block
in a fresh shell with fixture `stage3_ok`, `full_cli`, and executable/missing
stage paths. `/usr/bin/sh` is an executable-presence fixture, never a compiler.
The fixture hash function returns the same fixed string for both inputs, so
this does not exercise real hashing, admission verification, or codegen.

| Fixture | Exit | Selected for full CLI | Result | Elapsed ms |
|---|---:|---|---|---:|
| Stage 3 unadmitted, artifact present | 2 | nothing | PASS | 62.210 |
| Stage 3 admitted flag, artifact missing | 2 | nothing | PASS | 271.751 |
| Stage 3 admitted flag, artifact present | 0 | fixture Stage 3 path | PASS | 175.331 |
| Dynload only, Stage 3 failed | 2 | no full CLI attempted | PASS | 80.260 |
| Dynload only, Stage 3 admitted flag | 0 | no full CLI attempted | PASS | 384.806 |

Negative controls modify only extracted fixture text: replacing refusal
`exit 2` with `exit 0` makes three oracle checks fail; removing the
`stage3_ok == 1` admission check makes the unadmitted-present case fail. Both
mutants were detected. Fixtures do not start a bootstrap or run a compiler.

Local reproduction artifacts (ignored build output):
`build/stage3-wrapper-proof/check-selector.ps1`, its generated shell fixtures,
and `selector-results.json`. Run using `pwsh -NoProfile -File
build/stage3-wrapper-proof/check-selector.ps1`. This path is a session artifact,
not a checked-in regression runner. Shell child peak working set was
unavailable after exit and is recorded as null, never as zero.

## Marker source audit and performance scope

A separate host audit extracts the actual helper's literal contains/return
branches and checks six inputs against those branches. Baseline returns
`compiler_rust` for the real source and the provenance comment; the corrected
helper returns `ok`. Both versions reject each executable marker. This audit
does not parse or execute Simple and is not an SSpec pass.

| Source audit | Elapsed ms | Process peak working set bytes | Cases |
|---|---:|---:|---:|
| Baseline helper from origin/main | 459.4650 | 85,413,888 | 6 |
| Corrected helper | 118.6823 | 89,366,528 | 6 |

These are PowerShell audit costs. Baseline includes `git show`; current reads
the worktree, so this is not a comparable compiler benchmark. The larger
current audit working set does not measure a product regression. Product
elapsed/RSS comparison is inapplicable to this test/documentation-only change;
no runtime speed or memory improvement is claimed. No source modules were
compiled, objects emitted, libraries linked, or caches rebuilt.

## Verification status and remaining gates

**STATUS: WARN / TEST_BLOCKED for executable SSpec.** A full general
pure-Simple runner was not established. The offered Stage 2 executable
`D:/simple_build/bootstrap-msvc/stage2/x86_64-pc-windows-msvc/simple.exe`
has SHA-256 `4a8dd3eb3887b9cb61608dd6cc668dafa18bbd75bd0d98326328df48c6d54db5`,
matching its provenance/sanity receipt candidate hash. The remaining admission
chain was not validated and this candidate was not executed. Stage 2 evidence
cannot replace a general SSpec/docgen pass. The manual update is authored,
not generated by an unavailable runner.

The selector is common policy source across hosts, CPU targets, and LLVM,
Cranelift, and other codegen choices; it does not consult backend or CPU in the
tested block. Only its Windows Git-shell behavior was measured. Windows PE/COFF
codegen, Linux ELF, macOS Mach-O, FreeBSD, ARM, x86, different hash values, native
bootstrap convergence, and Stage 4 behavior remain unverified. No foreign-host
pass is inferred from this source commonality.

Open PR inspection found related Stage 3 work (#1255, #1256, #1258, #1207,
#1282) but no duplicate of this helper/documentation correction. Their compiler
and bootstrap fixes are separate lanes. The stale capability warning is
documented without changing launcher behavior.
