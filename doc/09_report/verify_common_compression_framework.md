=== Verification Report: common_compression_framework ===

[PASS] doc/04_architecture/common_compression_framework.md:1 — architecture now describes the currently landed compression subset instead of the original full-scope target.
[PASS] doc/05_design/common_compression_framework.md:1 — design now documents the actual facade restrictions, supported checksums, and known unsupported codec paths.
[PASS] test/unit/lib/common/compress_framework_spec.spl:1 — focused facade tests still align with the current checked/fail-fast API surface.
[PASS] test/unit/lib/common/lz4_spec.spl:1 — focused LZ4 tests cover block/frame round-trip behavior, corruption typing, and forced-tier parity.
[WARN] test/unit/lib/common/zstd_frame_variants_spec.spl:1 — the spec still documents the current framed-subset expectations, but repeated fresh runs in this session stalled after the first four passing cases instead of completing, so this lane cannot claim a clean current PASS for it.
[PASS] test/unit/lib/common/xz_lzma2_spec.spl:1 — focused XZ/LZMA2 tests cover emitted-stream interoperability, concatenated streams, and host-xz-generated range-coded chunk decode (LCLPPB=3/0/2).
[PASS] test/unit/lib/common/lzma2_range_coded_spec.spl:1 — pure-Simple LZMA range decoder round-trips host `xz -z --check=crc32` outputs across literal-only, slot-≥14 distance, and rep-heavy payloads.
[PASS] src/os/kernel/loader/zstd_decompress.spl:1 — the kernel loader remains a thin adapter over `std.common.compress`.

[FAIL] src/lib/common/compress/mod.spl:49 — the facade still rejects Zstd encode levels other than `3`, dictionaries for all codecs, and `checksum=false` for XZ/LZMA2 encode, so docs cannot claim wider option support.
[FAIL] src/lib/common/compress/zstd.spl:324 — compressed-block decode still rejects non-RLE sequence tables.
[NOTE 2026-05-01 evening, W6 audit] Empirical re-investigation: this verify-FAIL line number is stale. `_zstd_parse_single_sequence_table` (zstd.spl:367) already handles modes 0 (predefined), 1 (RLE), 2 (FSE-compressed), and 3 (Repeat_Mode); `_zstd_parse_sequence_tables` (zstd.spl:390) wires LL/OF/ML; the dispatcher (zstd.spl:1525-1531) calls them end-to-end with `sequence_table_history` flowing from `dictionary_state.sequence_history`. The leftover stub `_zstd_parse_rle_sequence_tables` at zstd.spl:352 (where the cited line was originally pointed) is **dead code** — nothing in the current dispatch path calls it. The honest current FAIL is downstream and unmeasurable today: empirical re-run of every existing zstd spec was blocked by a brand-new compiler regression (`Value::UInt` missing method dispatch) which broke EVERY zstd spec including W5-E's previously-PASSing `zstd_fse_weights_spec.spl` (now 1/6 PASS, was 6/6). Until that compiler bug is fixed, no zstd lane can claim PASS or FAIL on the live FSE-state path. Tracked under `doc/08_tracking/bug/interpreter_uint_method_dispatch_2026-05-01.md`. The "FAIL" status here is conservative; the line number citation should be migrated to the actual dispatcher line (`zstd.spl:1531`) once the compiler regression clears.
[PASS] src/lib/common/compress/zstd.spl:704 — FSE-compressed Huffman-weight decode is wired, dispatched (header_byte<128 -> `_zstd_parse_fse_compressed_weights`), and now byte-exact on real host-zstd output (FIXED 2026-05-01). Root cause was an LSB-vs-MSB bit-order mismatch in the shared `ZstdBackwardBits` reader (RFC 8478 Annex A mandates MSB-first within each byte for the FSE state stream); fixed via a dedicated `ZstdMsbBackwardBits` reader plus a Kraft-sum-derived `max_bits` in `zstd_huf_weights_to_symbols`. `test/unit/lib/common/zstd_fse_weights_spec.spl` (6 cases, PASS 2026-05-01) covers two real host-zstd Huffman-tree fixtures end-to-end (FX1 -> 123 weights, sum 128 = 2^7; FX_HOST_FRESH -> 123 weights, sum 256 = 2^8) plus the four typed-error rejections (truncated bitstream, out-of-range accuracy_log, zero compressed_size, empty input). See closed bug `doc/08_tracking/bug/bug_zstd_fse_huffman_weight_kraft_2026-05-01.md`.
[WARN] src/lib/common/compress/zstd.spl:1265 — Status: PARTIALLY LANDED 2026-05-01 (T24 Wave-4 advanced one gap). Pure-Simple decode parses RFC 8478 §6 dictionary blobs (magic `0xEC30A437`, Dictionary_ID match, HUF + FSE_Offsets/Match_Lengths/Literals_Lengths entropy tables, 3 recommended offsets, content as LZ77 prefix) and seeds per-frame decoder state from them. `test/unit/lib/common/zstd_dictionary_spec.spl` (10 cases, PASS 2026-05-01; was 8) covers parser positives, malformed-magic / id-mismatch / truncated-after-HUF / truncated-recommended-offsets / zero-rep-offset rejections, a Raw_Block frame round-trip referencing a dictionary by id, the missing-dictionary error, **and a new end-to-end consumption test for the seeded `huffman_table_state` slot**: a Compressed_Block whose literals section is a Treeless_Literals_Block (literals_type=3, size_format=0, 1-stream Huffman, `sequence_count=0`) that can only decode if `dictionary_state.huffman` flowed through `_zstd_frame_dictionary_state` into `_zstd_decompress_frame_for_tier_with_dictionary`'s `huffman_table_state` initialisation. The fixture hand-constructs a 1-byte backward Huffman bitstream `0x47` that decodes to literals `[0x00, 0x00, 0x00, 0x02]` against the dict's seeded tree (avoids `doc/08_tracking/bug/bug_zstd_fse_huffman_weight_kraft_2026-05-01.md` by using direct-weight Huffman, `header_byte ≥ 128`). The matching negative test asserts that the same Compressed_Block, framed with DID_flag=0, errors with `UnsupportedFeature("zstd treeless literals need a prior Huffman table")` — proving the failure mode is typed rather than silent. Three gaps still remain: (1) full host-`zstd --train --dict` round-trip is blocked by the FSE Kraft bug because real-world dict HUF sections use the buggy FSE-compressed weight path; (2) end-to-end consumption of seeded `sequence_table_history` via Repeat_Mode FSE sequence streams is not yet exercised — `sequence_count=0` in the new fixture trivially skips the FSE sequence-decode path, and constructing a hand-crafted Repeat_Mode FSE bitstream against the dict's tables is bit-fiddly enough to be tracked separately under `doc/08_tracking/bug/zstd_dict_repeat_mode_fse_endtoend_2026-05-01.md`; (3) end-to-end consumption of seeded `repeat_offsets` (offset_code 1/2/3 reaching back into the dict's `content_prefix` window) shares blocker (2). The `huffman_table_state` end-to-end seam is covered; the FSE-side seams are wired through the same dispatcher but not yet runtime-verified end-to-end in this lane.
[WARN] src/lib/common/compress/lzma2.spl:330 — Status: LANDED 2026-05-01 (LCLPPB=3/0/2 only). Pure-Simple LZMA range decoder + LZMA2 chunk decoder now interoperate with host `xz -z --check=crc32` output at the default LCLPPB tuple (props 0x5D); other LCLPPB values are rejected explicitly via `UnsupportedFeature("LZMA2 LCLPPB other than 3/0/2")`. Full-LCLPPB closure tracked in `doc/08_tracking/feature_request/lzma2_full_lclppb_2026-05-01.md`.
[FAIL] doc/02_requirements/feature/common_compression_framework.md:30 — REQ-030 still overstates forced-tier parity closure relative to the focused verified surface.
[FAIL] doc/02_requirements/nfr/common_compression_framework.md:11 — NFR-011 still overstates end-to-end verification closure; repository `verify` cannot honestly report PASS for the original full-scope claims.

[WARN] src/lib/common/compress/zstd.spl:1 — the Zstd implementation remains large and multi-responsibility, which raises maintenance risk even though this lane did not modify code.
[WARN] doc/01_research/local/common_compression_framework.md:1 — the research artifacts still describe the original full completion target; that may be fine as intent, but they should not be read as current-state support documentation.

STATUS: FAIL (5 failures, 5 warnings)
[AUDIT 2026-05-01 evening, T25 Wave-4 sweep] STATUS line re-verified post-Wave-3 pre-T24: counts match line-by-line tallies (5 FAIL, 5 WARN, 7 PASS). T24 dict-frame end-to-end test is in flight at audit time but has not yet landed; if it lands and flips zstd.spl:1265 from WARN to PASS, this status becomes `FAIL (5 failures, 4 warnings)`. See `doc/09_report/verify/wave_1to3_audit_2026-05-01.md`.
[UPDATE 2026-05-01 late evening, T24 partial closure] zstd.spl:1265 was advanced but **kept at WARN** on honest scope: `test/unit/lib/common/zstd_dictionary_spec.spl` now has 10 cases (was 8), adding an end-to-end Treeless_Literals_Block test that exercises `_zstd_frame_dictionary_state` -> per-frame `huffman_table_state` seeding plus a typed-error negative case. The Repeat_Mode FSE / repeat_offsets end-to-end consumption is NOT exercised (sequence_count=0 in the new fixture skips the FSE sequence path); residual gap is tracked under `doc/08_tracking/bug/zstd_dict_repeat_mode_fse_endtoend_2026-05-01.md`. WARN count therefore stays at 5; PASS-flip predicted by the audit will land once both Repeat_Mode FSE end-to-end coverage AND the underlying FSE Kraft bug have been resolved (or when one has been resolved and the other has the host-zstd round-trip path unblocked).
