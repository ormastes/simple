# GPU Binary Hardening Verification

## Passing evidence

- Portable CUDA/HIP/OpenCL/Metal/WebGPU emitter contract: 27/27 PASS.
- Shared generated Engine2D artifact contract: 2/2 PASS.
- CUDA intensive MIR control-flow, direct-call, global-memory, shared-memory, and fail-closed tests: 5/5 PASS.
- Vulkan intensive supported-SPIR-V and fail-closed unsupported-MIR tests: 10/10 PASS.
- Metal source generated by Simple compiled through Apple `metal` + `metallib`: PASS, 19,658-byte verified MTLB with all five entries.
- Metal live host upload -> GPU submit/wait -> host download: PASS for fill, copy, alpha, and scroll; all hashes match and mismatch count is zero.
- Metal wrapper self-test, shell syntax, numbered-artifact guard, direct-env runtime guards, isolated rendering-source-coupling gate, and `doc/06_spec` executable-layout guard: PASS.
- MCP stdio integration: 3/3 PASS.

## Blocking evidence

- `bin/simple check src/compiler`: exit 3 with no diagnostic.
- `bin/simple check src/lib`: exit 3 with no diagnostic.
- `bin/simple check src/app/mcp`: exit 3 with no diagnostic.
- `bin/simple check src/app/simple_lsp_mcp`: exit 3 with no diagnostic.
- Setting `SIMPLE_LIB=src` does not change the compiler check result.
- Setting `SIMPLE_FRONTEND_DELEGATED=1` confirms the deployed in-process checker
  also exits 3 silently. The sibling seed delegates back through `bin/simple run
  src/app/check/main.spl ...` and stalled past the session progress ceiling; the
  probe and its child processes were terminated.
- A second pure-Simple binary from the `simple-metal-119` workspace reproduces
  the same delegated stall and in-process exit 3. This matches
  `doc/08_tracking/bug/stage4_compiled_dict_no_growth_2026-07-11.md`, which says
  the runtime dictionary fix was necessary but the deployed native check path
  still has another compiled-semantics wall.
- A fresh bounded `bootstrap-from-scratch.sh --full-bootstrap --deploy
  --jobs=half` completed successfully after the GPU changes and deployed the
  full CLI plus both MCP servers. The newly deployed CLI still exits 3 silently
  for a single-file `check`, proving that stale deployment is not the cause.
- A scoped attempt to bypass the native `[text]` process ABI with scalar
  `rt_exec` was reverted: it did not change the exit-3 result, so the remaining
  defect is inside the compiled check path rather than argument transport.
- The concurrent `mcp-sync` full bootstrap/deploy completed unsuccessfully and
  produced no replacement verifier. Its stage-4 link failed with duplicate
  definitions of `rt_io_tcp_socket_create`, `rt_io_tcp_bind_fd`,
  `rt_io_tcp_listen`, `rt_io_tcp_set_nonblocking`, `rt_io_tcp_set_reuseaddr`,
  and `rt_io_tcp_set_reuseport` between the Rust runtime object and
  `runtime_smf_socket_shims.o`. Evidence:
  `/private/tmp/simple-mcp-sync/build/bootstrap/logs/aarch64-apple-darwin/stage4-native-build.log`.
- CUDA PTX and Vulkan SPIR-V binary execution cannot be claimed on this macOS host without CUDA/Vulkan runtime lanes; their current evidence is emission/error-contract coverage. Vulkan arithmetic, calls, branches/loops, and host/device loads/stores intentionally fail closed until implemented.

## Result

`STATUS: FAIL`

The GPU changes are not eligible for sync/push until the required self-hosted core check commands pass. The repository sync workflow forbids pushing compiler/core changes without verification PASS.
