# Pure-Simple tooling hardening status — 2026-07-19

No row is release-qualified until an admitted current Stage 4 runs its listed
evidence. Rust-seed or stale deployed output is bootstrap/debug evidence only.

| Tool | Current status | Known bug or risk | Root solution / proving evidence |
|---|---|---|---|
| Test runner | Structured evidence, indexed-u8 failure gate, opt-in-only SPipe docgen, canonical direct summaries, responsive daemon routing, and fail-closed atomic request publication fixed in source; request write/rename failures clean artifacts, name the request path, and return nonzero; seed/source MCP stdio spec exits 0 | self-hosted Stage 4 qualification pending; source launch emits excessive parser recovery diagnostics | admit an incremental Stage 4, then suppress recovery noise and prove stale-daemon direct fallback |
| Lint | Style strictness, query AST dispatch, invalid-profile false-greens, JSON/fix/dry-run atomic behavior, and parser-backed CLI parity for ARG001/ARG002, STUB001/STUB002, and W0404 are fixed in source; STUB003 retains generic `pass_todo` ownership; Stage 4 qualification pending | COLL, DTYP, and wildcard-import AST parity remain incomplete; source fixes still need admitted Stage 4 behavior proof | qualify the fixed JSON/mutation and ARG/STUB/W0404 paths through an admitted Stage 4 CLI, then add one remaining parsed semantic leaf at a time with CLI/query parity probes |
| Formatter/fix | `FixApplicator`, formatter `--write`, standalone accessor fix, and bare-import fix now fail closed through the canonical atomic provider; MCP stdio regression exits 0; Stage 4 integration pending | shipped CLI migration remains explicitly unavailable; standalone rewriters still need admitted Stage 4 qualification | qualify all live rewrite consumers on the admitted Stage 4 runtime; only harden migration persistence if that CLI is implemented |
| Duplicate checker | Missing-target, malformed-option, CLI/config invalid mode/format false-greens, valid CLI-over-config precedence, and same-file local semantic clone detection are fixed in source; focused repair probes pass | admitted Stage 4 runtime and authoritative scan qualification remain pending | keep explicit help at 0 and qualify the bounded clean/exact-clone/invalid/config fixtures on admitted Stage 4 |
| Native build/cache | Monitor classification, core-C `trim_start`/fail-closed fork-hook link gaps, focused self-hosted output-path transport, and BuildCache warm-path load/read hazards are fixed in source; a focused self-hosted native binary links and the warm path reaches a linked binary | full-CLI Stage 4 candidate, cache, Mach-O/COFF, and alias qualification remain | preserve the cache, produce and admit one full-CLI Stage 4 candidate, then run the bounded essential-tools smoke |
| Bootstrap admission | Focused self-hosted native-build cold and warm-cache paths reach linked binaries; no full-CLI Stage 4 artifact is admitted | full-CLI Stage 4 provenance, hash, and essential-tools qualification remain pending | use the existing cache for one bounded full-CLI replay; require provenance, exact hash, and bounded essential-tools smoke before deploy |
| Pure-Simple runtime ABI | Tuple row-length repair and locally-defined runtime-call resolution are source-fixed | post-fix exact Rust archive qualification has not run because the existing Rust test binary failed to link; a package-scoped one-job rebuild timed out before the test; runtime success remains unclaimed | finish the incremental Rust test build, then rerun the exact archive probe once |
| `check` | HM expression-bodied return false-positive fixed; CLI false-green remains | both workers stop at parse/policy; explicit `return` still lacks HM expected-return context, so fatal routing is not yet safe for the broader corpus | implement explicit-return inference, prove paired reject/positive CLI exits, then make HM fatal only in canonical Check mode and route both workers through its internal owner |
| MCP server | native-first source exists; qualification pending | wrapper/registry can select stale or source fallback; startup/request budgets unproven | exact native artifact admission, stdio integration, warm startup/latency/RSS evidence |
| LSP MCP server | diagnostics uses group-aware bounded execution; correlated `lsp_symbols` wrapper admission is fixed in source | installed candidate provenance is missing and its exact native smoke remains unqualified | regenerate the wrapper, invalidate stale stamps, then rerun the exact-artifact native smoke with hash, revision, time, and RSS evidence |
| SPipe/docgen | source/manual present; generated evidence blocked | argv/runtime crashes or stalls can prevent truthful manual generation | admitted runtime, bounded docgen, generated/manual parity and zero placeholders |
| Test daemon | absolute-expiry protocol and responsive production routing gates fixed and unit-tested; Stage 4 lifecycle/ownership evidence pending | old path-only requests could let a fixed 600s daemon child outlive its client and block later work; CLI ownership still needs qualification | tagged absolute expiry, dequeue-time spawn refusal, and remaining-time group-aware execution; then prove start/status/run/stop plus stale-state recovery |
| Essential-tools smoke | Aggregate behavior gate and fail-closed fixture contract are present | no freshly admitted Stage 4 CLI has passed the aggregate | first admit the exact binary and reject wrappers, source workers, seeds, and stale artifacts; then run the bounded external-CWD gate to reject help-only behavior, forged runner evidence, and vacuous duplicate results |
| Codex session guard | Duplicate guarded resumes use a rollout flock plus live-owner detection; default thresholds refuse resumes over 20M tokens and terminate live rollouts over 50M | direct Codex invocation outside `bin/codex` remains intentionally unguarded | launch through repo `bin/codex` or put repo `bin/` first on `PATH` |

## Next admission sequence

1. Produce one incremental Stage 4 candidate; do not run a full bootstrap.
2. Admit its provenance, then run the essential-tools aggregate once.
3. Run focused lint, duplication, atomic-provider, compiler/lib, MCP, and LSP MCP
   checks once each; generate the SPipe manual only after those pass.
4. Record command, candidate hash, exit, elapsed time, and max RSS, then review,
   sync, and push.
