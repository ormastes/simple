# Web Platform Feature Matrix

Date: 2026-05-10

Scope: Simple browser renderer compatibility evidence for HTML, CSS, DOM/rendering, and browser-facing host behavior. Status values are `supported`, `partial`, `missing`, and `not-applicable`.

## DOM And Rendering

| Feature | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Script execution | partial | `src/lib/gc_async_mut/gpu/browser_engine/script/script_runner.spl`; `src/lib/gc_async_mut/gpu/browser_engine/script/script_host.spl`; `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_script_spec.spl` | Inline `<script>` body collection via `html_tree_builder_build_with_scripts`, `<script src>` path collection, subprocess execution via `rt_process_run`, console stdout capture into `ConsoleBuffer`. DOM mutations from scripts do not cross the subprocess boundary. Full in-process execution requires a future `rt_interpret_file` runtime extern. |
| Browser DOM events | partial | `src/lib/gc_async_mut/gpu/browser_engine/dom.spl`; `src/lib/gc_async_mut/gpu/browser_engine/layout.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_dom_events_spec.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_hit_test_events_spec.spl` | Simplified `BeDomNode` listener registration, event-name normalization, inline `on...` handler discovery, lightweight Event state (`type`, `target`, `currentTarget`, `bubbles`, `cancelable`, `defaultPrevented`, propagation stop flags), deterministic capture/target/bubble ordering, first-match tree path discovery by target id, layout-box coordinate hit-test routing to root-to-target pointer/click dispatch, deterministic mousedown/up sequencing with click synthesis only when both phases hit the same target, focused default-action metadata, cancelable default suppression, simplified returned-node default effects for checkbox/radio/focus/button/form/navigation tokens, disabled control default suppression, focused Enter/Space keyboard activation mapping to click/default effects for common interactive elements, and deterministic `stop-propagation` / `stop-immediate-propagation` listener-token behavior are covered. Executable JavaScript listener callbacks, composed paths, shadow DOM retargeting, real PointerEvent/MouseEvent payload fields, pointer capture, full keyboard event dispatch, async event-loop delivery, and document-wide default-action side effects remain unsupported. |
| CSS basic selectors | partial | `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Covers fallback-renderer matching for universal `*`, type, class, id, selector-list, tag-class compound, multi-class compound, and tag-id compound selectors. Composed tree matching, non-`div` target rendering, full DOM style-block parity, and specificity parity are not claimed. |
| CSS selector-list pseudos | partial | `src/lib/gc_async_mut/gpu/browser_engine/style_block.spl`; `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_spec.spl`; `doc/09_report/chrome_modern_web_platform_compat_2026-05-06.md` | Covers fallback-renderer matching for `:is()`, `:where()`, tag-qualified functional pseudos, simple `:not()` selector-list exclusion, simple descendant `:has()`, and bounded direct-child `:has(> .class/tag/#id)` matching with comma-safe selector-list splitting. Complex relative selectors beyond direct child, sibling combinators, specificity parity, and WPT completeness are not claimed. |
| CSS descendant combinators | partial | `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Covers bounded fallback pixel matching for ancestor `tag/.class/#id` selectors targeting nested `div`, `.class`, `div.class`, `#id`, and `div#id` nodes, with sibling rejection. Arbitrary selector chains, non-`div` target rendering, composed tree matching, DOM style-block parity, and specificity parity are not claimed. |
| CSS child combinators | partial | `src/lib/gc_async_mut/gpu/browser_engine/style_block.spl`; `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Covers bounded fallback pixel matching for direct `body > div/.class/#id` rules and ancestor `tag/.class/#id > div/.class/#id` rules, with nested descendant rejection. Arbitrary selector chains, non-`div` target rendering, full DOM style-block parity, and specificity parity are not claimed. |
| CSS sibling combinators | partial | `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Covers bounded fallback pixel matching for adjacent `+` and general `~` direct body-child sibling relationships targeting `div`, including class/id-qualified previous and current selector forms, with non-adjacent rejection for `+` and preceding-source rejection for `~`. Arbitrary parent sibling matching, non-`div` target rendering, DOM style-block parity, and specificity parity are not claimed. |
| CSS structural pseudos | partial | `src/lib/gc_async_mut/gpu/browser_engine/style_block.spl`; `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_spec.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Covers partial `:empty` matching for elements with no child elements and no non-whitespace text, plus partial `:first-child`, `:last-child`, `:only-child`, and bounded `:nth-child(N/odd/even/common an+b forms)` matching from sibling index/count data and ordered fallback `div` rendering. `of <selector>` filters, generated content, comments, shadow DOM, and full Selectors Level 4 parity are not claimed. |
| CSS attribute selectors | partial | `src/lib/gc_async_mut/gpu/browser_engine/style_block.spl`; `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_spec.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Covers `[attr]` presence, `[attr=value]` exact matching including quoted values with spaces, bounded `[attr^=value]`, `[attr$=value]`, `[attr*=value]`, `[attr~=value]`, `[attr|=value]` operators, bounded ASCII case-insensitive `[attr=value i]`, and explicit case-sensitive `[attr=value s]` syntax, including tag-qualified exact/prefix/suffix/dash selectors in the fallback renderer. Namespaces, escaping, non-ASCII folding, and full selector specificity parity are not claimed. |
| CSS custom properties | partial | `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/custom_properties_wpt_spec.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Resolves bounded `var(--name)` color values from `:root` style rules, element inline styles, and all CSS rule blocks. Covers `var(--name, fallback)` color fallbacks for `background-color` and `background` shorthand, including inheritance scanning and recursive var() fallback resolution. Nested `var()`, cyclic variables, typed registration, and cascade parity are not claimed. |
| CSS layer blocks | partial | `src/lib/gc_async_mut/gpu/browser_engine/style_block.spl`; `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_spec.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Flattens simple `@layer name { ... }` wrappers so existing selector/declaration scans can apply nested rules. Cascade-layer ordering, layer precedence, anonymous layers, imports, media interactions, and full CSS Cascade Level 5 semantics are not claimed. |
| CSS nesting | partial | `src/lib/gc_async_mut/gpu/browser_engine/style_block.spl`; `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_spec.spl`; `test/03_system/feature/web_platform/css/selector_color_subset_spec.spl` | Flattens bounded simple `&` parent-selector nested rules so existing selector/declaration scans can apply self and descendant rules. Nested at-rules, selector-list specificity adjustment, relative selectors without `&`, media/container interactions, and full CSS Nesting Module parity are not claimed. |
| WPT CSS test suite | supported | `test/03_system/feature/web_platform/css/`; `doc/03_plan/sys_test/wpt_subset_migration.md` | 104/104 WPT-shaped test cases pass across 9 spec files: selectors and colors (57), scorecard (20), transforms (5), animations (5), custom properties (4), object-fit (4), @supports (3), scrollbar (3), sticky positioning (3). All render through `render_html_to_pixels_with_viewport` with deterministic pixel assertions. |
| CSS @supports | partial | `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/at_supports_wpt_spec.spl` | Covers `@supports (property: value)` feature queries including supported property application, unsupported property rejection, and `not` negation. Complex combinators (`and`, `or`) and selector() queries are not claimed. |
| CSS sticky positioning | partial | `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/sticky_wpt_spec.spl` | Covers `position: sticky` element rendering with `top` offset and sibling non-interference. Scroll-driven position clamping and nested containing block behavior are not claimed. |
| CSS transforms | partial | `src/lib/gc_async_mut/gpu/browser_engine/browser_renderer.spl`; `test/03_system/feature/web_platform/css/transforms_wpt_spec.spl` | Covers `translate`, `scale`, `rotate(0deg)` identity, `transform-origin`, and composed multi-transform rendering. 3D transforms, `matrix()`, and perspective are not claimed. |
| CSS animations/transitions | partial | `src/lib/gc_async_mut/gpu/browser_engine/style/animation_controller.spl`; `test/03_system/feature/web_platform/css/animations_wpt_spec.spl` | Covers color/length interpolation, timing function evaluation (ease, linear, cubic-bezier), keyframe parsing, and transition detection. Full Web Animations API and composite operations are not claimed. |

## Verification

Required focused commands after DOM event work:

- `bin/simple check src/lib/gc_async_mut/gpu/browser_engine/dom.spl test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_dom_events_spec.spl`
- `bin/simple test test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_dom_events_spec.spl --mode=interpreter --clean`
- `bin/simple test test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_hit_test_events_spec.spl --mode=interpreter --clean`
- `bin/simple test test/01_unit/lib/gc_async_mut/gpu/browser_engine/browser_renderer_spec.spl --mode=interpreter --clean`
- `bin/simple test test/03_system/feature/web_platform/css/ --mode=interpreter --clean`
- `bin/simple check src/lib`
