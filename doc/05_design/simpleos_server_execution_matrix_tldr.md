# SimpleOS server execution matrix design — TLDR

Each target emits a provenance-bound receipt. QEMU requires real VFS launch,
host-visible HTTP, and reboot-durable DB. UNO Q requires physical identity and
separate CPU/GPU evidence. The GPU returns a bounded validated result to the
parent owner. Linux comparisons retain fair controls and allow at most three
Pure-Simple optimization cycles.
