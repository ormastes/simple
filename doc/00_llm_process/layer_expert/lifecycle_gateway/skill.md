# Lifecycle gateway layer expert

Layer order is Spipe -> DevHub -> SCV lifecycle/SJ -> JJ/Git/provider backends.
Only adjacent-layer interfaces are permitted. SCV lifecycle modules are pure
provider-neutral values and rules. SJ owns typed protected mutation planning;
DevHub owns domain/provider projection; credentials remain in transports.

The base must stay observe/dry-run only until stage exit evidence promotes an
authority. Every rejection is structured and fail-closed; no hook, CLI string,
provider comment, or Git branch is canonical lifecycle evidence.

