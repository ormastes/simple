# WM/GUI/GPU false-success stub baseline (path:matrix-site-id:predicate-tag).
# Source of enumeration: doc/04_architecture/ui/wm_host_platform_matrix.md
# "False-success stubs, ranked" section (31 sites in 6 clusters, 0-5).
# Guard spec: test/03_system/gui/wm_host_platform/wm_false_success_family_spec.spl
#
# predicate-tag values:
#   1 = uses_native_[a-z0-9_]*_symbols() body is literal `true` (repo-wide .spl)
#   2 = readback() body returns "" success sentinel, in
#       src/lib/gc_async_mut/gpu/session/backend_*_adapter.spl
#   3 = engine2d_readback(.., "cpu_mirror") reachable unconditionally in
#       src/lib/gc_async_mut/gpu/engine2d/backend_webgpu.spl
#   4 = counter-handle (`_ctr + 1` / `.len() + 1`) used as a return value in
#       src/lib/nogc_async_mut/wm/*.spl, src/os/compositor/hosted_backend*.spl,
#       src/lib/nogc_async_mut/gpu/dxvk_d3d11.spl
#   5 = supports_*()/supports() body hardcodes `true`, same file set as tag 4
#   manual = real matrix site, but its false-success shape is not a generic
#       regex family (needs per-site logic) or is Rust (out of .spl grep
#       scope). Recorded for baseline count / shrink tracking only; NOT
#       cross-checked by an A0 predicate search. The owning fix lane's own
#       gate re-verifies it directly.
#
# Deviations from a literal 31-line copy of the matrix, both verified against
# live source at A0 dispatch time (not re-derived from the matrix by hand):
#   - Cluster 1 (matrix sites 1, 2, 3; src/lib/nogc_async_mut/wm/host.spl) is
#     ALREADY FIXED as of A0 dispatch -- WmHostLinux/WmHostSimpleOs no longer
#     exist; the file now ships only WmHost2dReference (in-memory reference,
#     honestly unnamed as a platform) and WmHost2dUnavailable (honest
#     refusal). Excluded from this baseline; nothing for A2 to shrink there.
#   - predicate-tag 4's search over dxvk_d3d11.spl found THREE more
#     counter-handle fabrications beyond the matrix's single named site 13
#     (device handle): a swapchain handle, a resource-id counter, and a
#     readback handle, all the same `.len() + 1` / `_ctr + 1` shape. Listed as
#     13b/13c/13d -- this is the guard finding real siblings the manual count
#     missed, which is the entire point of a machine-checked family sweep.
# Net count still lands on 31 (4 removed for cluster 1, 3 added for the dxvk
# siblings) -- coincidental, not fitted.
#
# This baseline may only SHRINK. A fix lane deletes its own lines in the same
# commit as the fix (single-writer-per-file rule: each Wave-1 lane owns
# exactly its own lines).
src/lib/gc_async_mut/gpu/engine2d/backend_webgpu.spl:12:3
src/lib/nogc_async_mut/gpu/dxvk_d3d11.spl:13:4
src/lib/nogc_async_mut/gpu/dxvk_d3d11.spl:13b:4
src/lib/nogc_async_mut/gpu/dxvk_d3d11.spl:13c:4
src/lib/nogc_async_mut/gpu/dxvk_d3d11.spl:13d:4
