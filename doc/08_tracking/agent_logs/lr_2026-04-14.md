# Agent LR Log — Launcher Exec Path Routing Diagnosis
Date: 2026-04-14

## Steps

[step] jj new main -m "(lr-diagnosis)" — created working copy rys fe

[step] batch-read: launcher.spl, browser_demo.spl, builtin_binary_registry.spl,
       executable_source.spl, desktop_e2e_entry.spl, hello_world.spl, app_manifest.spl,
       syscall.spl, shell.spl + greps for local-fallback, ENOSYS, exec_binary, remote_main

[step] search: confirmed `launcher_launch` calls `posix_spawn` directly (no
       `_builtin_remote_app_entry` check in live file at line 432).

[step] search: confirmed `_builtin_remote_app_entry` exists in launcher.spl
       (maps hello_world + browser_demo) but is NOT called by `launcher_launch`.

[step] search: confirmed `_dispatch_seeded_shortcut` also calls `_launch_seeded_app`
       which also calls raw `posix_spawn` → hits -38 stub.

[step] search: confirmed browser_demo succeeds via `_launch_browser_demo_local` in
       desktop_e2e_entry.spl — direct in-process call, not posix_spawn.

[step] search: confirmed `builtin_binary_registry.spl` has hello_world, file_manager,
       shell remote_main entries (HW's additions); browser_demo is NOT in
       builtin_binary_registry — it is in disk_launch_manifest.spl and in
       launcher's own `_builtin_remote_app_entry`.

[step] wrote resume doc:
       doc/08_tracking/todo/launcher_builtin_routing_resume_2026-04-14.md

[step] jj describe with diagnosis commit message

## Key Finding
The fix is a 4-line change in `launcher_launch` + same in `_launch_seeded_app`
to call `spawn(entry, APP_PRIORITY)` when `_builtin_remote_app_entry` returns
`Some(entry)`, falling back to `posix_spawn` otherwise. This is already the
pattern in a prior version of the file (visible in indexed batch diffs) but
was lost/reverted. SF territory reopen required.
