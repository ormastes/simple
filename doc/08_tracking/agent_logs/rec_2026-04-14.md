# Agent REC — Task #41 Restoration Log

Date: 2026-04-14
Agent: REC (recovery)
Task: Restore 4 spec files committed earlier but missing from current @ working copy.

## Source commits identified

| File | Source change_id | Commit title |
|------|------------------|--------------|
| `test/system/engine2d_primitives_spec.spl` | `ypvwnvmovvkq` | feat(sys-test): engine2d primitive draw verification (line/square/circle) |
| `test/system/sys_gui_001_boot_launcher_spec.spl` | `nsyrwywwpwwq` | feat(sys-test): record SYS-GUI-001 + SYS-GUI-002 framebuffer baselines |
| `test/system/sys_gui_002_compositor_cold_start_spec.spl` | `nsyrwywwpwwq` | feat(sys-test): record SYS-GUI-001 + SYS-GUI-002 framebuffer baselines |
| `test/system/sys_gui_005_cleanup_lifecycle_spec.spl` | `uukwlpszprms` | feat(sys-test): SYS-GUI-005 entry+spec+baseline (g5 follow-up) |

Note: G12's SYS-GUI-001/002 specs were both added in a single commit (`nsyrwywwpwwq`, 28/30 matches the "G12" keyword from the task description). G5's spec was present in both `uukwlpszprms` and `rpoqztqrvsyo` (both identical); used `uukwlpszprms` as canonical source (earlier G5 follow-up that actually introduced the file).

## Restore procedure

1. `jj file show -r <change_id> <path>` extracted each file to `/tmp/rec_restore/`.
2. Files copied into working copy at `test/system/...`.
3. No jj destructive ops used. No commits made. Log-only side effect.

## Recovered files (sizes)

- `test/system/engine2d_primitives_spec.spl` — 14241 bytes, 297 lines
- `test/system/sys_gui_001_boot_launcher_spec.spl` — 10881 bytes, 243 lines
- `test/system/sys_gui_002_compositor_cold_start_spec.spl` — 11220 bytes, 249 lines
- `test/system/sys_gui_005_cleanup_lifecycle_spec.spl` — 12310 bytes, 280 lines

All 4 recovered successfully. None failed / unrecoverable.

## Parse status

All 4 files FAIL to parse with the same error:

```
hint: Common mistake detected: Use ':' for function bodies, '=>' is for lambdas.
Ok(handle) =>
           ^
```

Lines affected:
- `engine2d_primitives_spec.spl:158`  `Ok(handle) =>`
- `sys_gui_001_boot_launcher_spec.spl:154`  `Ok(handle) =>`
- `sys_gui_002_compositor_cold_start_spec.spl:159`  `Ok(handle) =>`
- `sys_gui_005_cleanup_lifecycle_spec.spl:176`  `Ok(handle) =>`

Root cause: after these specs were committed, the parser's match-arm syntax was
canonicalized from `=>` to `:` (see commit `sltoqnyvvovx` "fix(test/system): use
canonical match arm colon form for Ok/Err arms in simpleos_desktop_framebuffer_spec").
The sibling specs restored here still use the older `=>` form for both
`Ok(handle) =>` and `Err(err) =>` arms and need the same `=>` → `:` mechanical
rewrite to parse under the current compiler.

Restore per task protocol: file content preserved verbatim from source commit.
Syntax fix is NOT applied here — flagged for main thread to decide (same fix
pattern as `sltoqnyvvovx`).

## Follow-up for main thread

Mechanical fix across all 4 files, per the `sltoqnyvvovx` pattern:

```
-            Ok(handle) =>
+            Ok(handle):
...
-            Err(err) =>
+            Err(err):
```

Apply, re-run `bin/simple test <path>` on each to confirm parse, then commit.
