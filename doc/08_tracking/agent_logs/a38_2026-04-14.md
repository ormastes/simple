# A38 — 2026-04-14

## Task
Fix "method X86_64 not found on type Architecture" in
`test/system/sys_gui_001_boot_launcher_spec.spl` and
`test/system/sys_gui_002_compositor_cold_start_spec.spl`.

## Finding
These two spec files DO NOT EXIST in the repo. Only build-artifact
summary.txt stubs exist under `build/test-artifacts/system/`. `git log`
shows no history for those paths. The SYS-GUI-001 / SYS-GUI-002
requirements are implemented as two `it` blocks inside
`test/system/os_desktop_integration_spec.spl`, which uses
`get_desktop_browser_target()` from `src/os/qemu_runner.spl`.

That function (line 303) and `get_target` (line 132) both use the
correct enum-variant form `Architecture.X86_64` (no parens), identical
to the working `test/system/os/boot_smoke_spec.spl` (line 43). No
`Architecture.X86_64()` (method-call form) exists anywhere in the repo.

The referenced "method X86_64 not found" trace in
`doc/08_tracking/todo/sys_gui_006_with_apps_resume_2026-04-14.md` is
an upstream semantic-analyzer issue, not a spec-level syntax bug.

## Action
No minimal spec-level fix is applicable — the failing spec files
listed in the task do not exist, and the shared call sites already
use the correct form. Deferring semantic-analyzer investigation as
out-of-scope per task guard "Do NOT touch interpreter code".

## Verify
`bin/simple test test/system/os_desktop_integration_spec.spl` still
reports `Failed: 2` (cached). No new regression introduced.
