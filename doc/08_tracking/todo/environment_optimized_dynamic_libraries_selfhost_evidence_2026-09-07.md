# TODO: Environment-Optimized Dynamic Libraries self-hosted evidence

**Status:** Open
**Owner:** compiler/runtime implementation lane
**Final reviewer:** independent highest-capability verifier
**Affected criteria:** AC-7, AC-8, NFR-001..010

## Missing prerequisite

`bin/simple` resolves to `bin/release/aarch64-unknown-linux-gnu/simple`, which
prints that it is a Rust-built bootstrap seed. Current focused checks therefore
provide development diagnostics only and cannot qualify the pure-Simple
self-hosted implementation, production SPipe/manual, performance, or release.

## Retained artifacts

- Stage-1 sources: `src/lib/nogc_sync_mut/composition/environment_variants/`
- Focused specs: `test/01_unit/lib/nogc_sync_mut/composition/environment_variants/`
- Fail-fast system matrix: `test/03_system/app/compiler/feature/environment_optimized_dynamic_libraries_spec.spl`

## Resume commands

After a provenance-admitted pure-Simple Stage-4 binary is deployed at
`bin/simple`, first record `readlink -f bin/simple`, its digest, version, stage,
and provenance. Then run each focused unit spec once, followed by:

```text
bin/simple check src/lib
bin/simple test test/03_system/app/compiler/feature/environment_optimized_dynamic_libraries_spec.spl --mode=interpreter --no-session-daemon
```

The system spec is expected to remain red until its production catalog,
binding, frontend/execution, and NFR helpers replace the explicit `fail(...)`
gates. Do not convert those gates to skip or placeholder passes.

## Unblock condition

A provenance-admitted self-hosted Stage-4 CLI exists and the implementation
behind every claimed scenario is present; focused results retain authenticated
execution counts and the system/manual evidence is regenerated by SPipe.
