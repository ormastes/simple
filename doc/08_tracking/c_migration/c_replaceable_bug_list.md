# C-replaceable inventory — bug list (generated from Wave-1 audit, 2026-08-18)

Projection of `c_migration_inventory.sdn` + the Wave-1 C audit
(`doc/03_plan/infra/binary_runtime_hardening/wave1_audit_results_2026-08-18.md`).
Scope: 107 non-vendored `.c` files, 51,156 lines under `src/runtime/`.
Classification per the frozen design §13: every entry below is project-owned C
that Simple should be able to replace; inability discovered during migration
becomes a SIMPLE-CAPABILITY bug, not a reason to keep the C.

## Status legend
`done` = migrated with differential + regression evidence · `planned` = next
tranches · `assess` = needs per-symbol caller analysis before commitment.

| Bug ID | C unit | Lines | Class | Replacement path | Status |
|---|---|---:|---|---|---|
| C-MIG-0001 | runtime_legacy_core.c:rt_crc32_text | ~15 | product algorithm | std.nogc_sync_mut.compression.gzip.crc32_text | **done** (5/5 differential, 35/35 regression, C retained as oracle) |
| C-MIG-0002 | runtime_simd_dispatch.c | 2,208 | product algorithm | pure-Simple scalar path + SIMPLE-CAPABILITY bug for vector intrinsics | planned |
| C-MIG-0003 | runtime_pool.c | 1,292 | runtime primitive | std.memory provider (sanctioned alias boundary) | planned |
| C-MIG-0004 | runtime_native.c (11,139 lines, monolith) | 11,139 | runtime primitive (mixed) | split by symbol family first; string/array/dict value ops are the Simple-replaceable half | assess |
| C-MIG-0005 | runtime.c | 3,456 | runtime primitive | core value/entry glue — provider boundary, not full rewrite | assess |
| C-MIG-0006 | runtime_process.c | 2,297 | platform shim | retain minimal syscall shim; command construction/decoding to Simple (HAL split per design §14) | planned |
| C-MIG-0007 | runtime_thread.c | 2,005 | platform shim | same HAL split: sync-primitive shim stays, orchestration to Simple | planned |
| C-MIG-0008 | platform/async_linux_epoll.c / async_windows.c / async_linux_uring.c | ~2,800 | platform shim | HAL contract (BlockDevice/EventLoop pattern, design §14 step 1) | planned |
| C-MIG-0009 | database/sqlite glue (2 files) | 1,111 | third-party wrapper | facade stays; owned glue logic (row/value marshalling) to Simple | assess |
| C-MIG-0010 | openssl/tls glue (3 files) | 968 | third-party wrapper | facade stays; owned handshake state tracking to Simple; KAT + interop corpus mandatory | assess |
| C-MIG-0011 | time/timestamp (2 files) | 265 | runtime primitive | provider alias (clock syscall is the boundary) | planned |
| C-MIG-0012 | bootstrap/startup (6 files) | 327 | bootstrap stage | staged self-hosting plan; not general-migration scope | planned |
| C-MIG-0013 | mcp shim | 0 (deleted 2026-08-18) | DELETED: forward-cache row mapped a symbol (mcp_stdio_read_message) that existed nowhere; pure-Simple read_stdin_message is the real implementation | runtime_mcp_core.c + decls + 4 link/roster refs + forward-cache row removed; smoke test retargeted | done |
| C-MIG-0014 | wasm shim (scv_wasm_shim.c) | 459 | platform ABI shim | retain; external SDK boundary (guard SKIPs it already) | assess |
| C-MIG-0015 | media wrappers (sdl2/sdl3/glfw/audio/font, 23 files) | 5,409 | third-party wrapper | facades retained; owned pixel/format conversion helpers to Simple | assess |
| C-MIG-0016 | src/runtime/test/** (27 files) | 3,870 | conformance oracle | test-only, VERIFIED never production-linked (capsule selfcheck binaries only) | **verified** |
| C-MIG-0017 | memory/memtrack/packed_span (excl. pool) | 1,146 | runtime primitive | provider boundary + Simple-side accounting | planned |
| C-MIG-0018 | hosted_win32.c + scilib/rocm/cuda shims | ~3,000 | platform shim | retain thin ABI shims; declared-retained entries in registry | assess |
| C-MIG-0020 | runtime_native.c:rt_hash_text + runtime_legacy_core.c:rt_str_hash | ~15 | product algorithm | std.hash text.hash() / rt_hash_text ABI bridge (already implemented, proven equivalent) | **done** (7/7 differential incl. KAT + UTF-8 + boundary lengths, C retained as oracle) |
| C-MIG-0021 | runtime_native.c:rt_string_to_int | ~10 | product algorithm | std.common.text.parse_i64 (text.spl, already implemented, proven equivalent on well-formed input) | **done** (7/7 differential incl. KAT + 100-vector shared branch-covering bulk loop with perf evidence, C retained as oracle; failure-sentinel divergence 0-vs--1 on malformed/empty input documented not asserted-equal; perf ratio ~1.59x, under 2x threshold) |
| C-MIG-0022 | runtime_simd_utf8.c:rt_text_validate_utf8 | ~5 | product algorithm | pure-Simple validated_utf8_bytes_to_text_linear (base_encoding/utilities.spl, already implemented, proven equivalent) | **done** (4/4 differential incl. KAT + malformed-byte discrimination + 88-vector bulk loop, C retained as oracle; PERF FINDING: Simple ~32.9x slower under interpreter, recorded not hidden) |
| C-MIG-0023 | runtime.c:rt_base64url_encode/rt_base64url_decode | ~60 | product algorithm | std.common.base_encoding.base64.{base64url_encode,base64url_decode} (base64.spl, already implemented, proven equivalent) | **done** (6/6 differential incl. RFC 4648 §10 KAT + 100-vector shared branch-covering bulk loop with UTF-8, C retained as oracle; PERF FINDING: Simple ~44.1x slower under interpreter, recorded not hidden) |
| C-MIG-0024 | runtime_native.c:rt_string_rfind | ~10 | product algorithm | std.common.string_core.str_last_index_of (string_core.spl, already implemented, proven equivalent) | **done** (5/5 differential incl. KAT + overlapping/full-match edge cases + 100-vector shared branch-covering bulk loop, C retained as oracle; PERF FINDING: Simple ~4.4x slower under interpreter, recorded not hidden) |
| C-MIG-0025 | runtime_native.c:rt_string_ends_with | ~6 | product algorithm | std.common.string_core.str_ends_with (string_core.spl, already implemented, proven equivalent) | **done** (6/6 differential incl. KAT + multibyte-UTF-8-suffix + full/near-full-match edge cases + 100-vector shared branch-covering bulk loop, C retained as oracle; ratio ~1.8x under interpreter, below the 2x threshold, no PERF finding) |
| C-MIG-0026 | runtime_native.c:rt_raw_i64_to_string | ~10 | product algorithm | std.common.convert.i64_to_text (convert.spl, already implemented, proven equivalent) | **done** (6/6 differential incl. KAT + i64::MIN/MAX boundaries + digit-count-transition edge cases + 100-vector shared branch-covering bulk loop, C retained as oracle; PERF FINDING: Simple ~2.3x slower under interpreter, recorded not hidden. Rejected candidates: rt_string_starts_with — unregistered in interpreter extern dispatch; rt_text_to_lower_ascii/str_to_lower — str_to_lower crashes on multibyte UTF-8 input, filed as bug str_to_lower_upper_crash_on_multibyte_utf8_2026-08-18; rt_string_rfind/str_last_index_of — already covered by C-MIG-0024) |
| C-MIG-0027 | runtime_native.c:rt_char_from_code | ~17 | product algorithm | std.common.string_core.char_from_code (string_core.spl, already implemented, proven equivalent) | **done** (6/6 differential incl. KAT + all 4 UTF-8 encoding-length classes + invalid-codepoint/surrogate-range edge cases + 100-vector shared branch-covering bulk loop, C retained as oracle; PERF FINDING: Simple ~3.5x slower under interpreter, recorded not hidden. Rejected candidates: rt_string_rfind/str_last_index_of — already covered by C-MIG-0024) |
| C-MIG-0028 | runtime_native.c:rt_byte_char | ~6 | product algorithm | std.common.encoding.byte_char.byte_char (new file byte_char.spl, written this round — pure-Simple UTF-8 encoder for the Latin-1-transparent byte-to-scalar mapping, deliberately independent of string_core.spl per this round's HARD AVOID) | **done** (6/6 differential incl. KAT + 1-byte/2-byte UTF-8 boundary + full Latin-1-supplement range + 100-vector shared branch-covering bulk loop, C retained as oracle; PERF FINDING: Simple ~2.5x slower under interpreter, just above the 2x threshold, recorded not hidden. Rejected candidates: rt_hash_text — already covered by C-MIG-0020 (std.hash text.hash())) |
| C-MIG-0029 | interpreter_extern/math.rs:rt_math_sqrt (no C runtime counterpart) | ~15 | product algorithm | std.math.special.sqrt_f64 (special.spl, already implemented — 40-iteration Newton's-method kernel, proven genuinely independent of the hardware `f64::sqrt` extern) | **done** (6/6 differential incl. perfect-square KAT + tolerance-based irrational-result comparison + explicit SEMANTIC-DIVERGENCE assertion (negative input: oracle NaN vs sqrt_f64 unguarded finite garbage) + 100-vector shared branch-covering bulk loop restricted to the documented x>=0 domain, C/Rust retained as oracle; PERF FINDING: Simple ~17.7x slower under interpreter — the largest ratio in this series, expected for a 40-iteration loop vs one hardware instruction — recorded not hidden) |

| C-MIG-0030 | interpreter_extern/math.rs:rt_math_cbrt (no C runtime counterpart) | ~15 | product algorithm | std.math.cbrt.cbrt_f64 (NEW file cbrt.spl, written this round -- 80-iteration Newton's-method kernel starting from g=1.0, deliberately independent of the rt_math_cbrt extern and NOT written into special.spl, this round's HARD AVOID) | **done** (7/7 differential incl. positive+negative perfect-cube KAT + tolerance-based irrational-result comparison + full domain-boundary coverage (+-0.0, +-infinity, NaN, no semantic divergence found) + 100-vector shared branch-covering bulk loop covering the full signed domain in one loop, C/Rust retained as oracle; PERF FINDING: Simple ~34.9x slower under interpreter — the largest ratio in this series so far — recorded not hidden. Bug found+fixed mid-spec: initial g=x Newton start converged too slowly for x<<1 (2% error at x=1e-8); fixed by starting at g=1.0) |

| C-MIG-0031 | interpreter_extern/math.rs:rt_math_floor (no C runtime counterpart) | ~10 | product algorithm | std.common.numeric_round.floor_f64 (NEW file numeric_round.spl, written this round -- truncate-through-i64 kernel with a 2^52 integrality guard, deliberately independent of the rt_math_floor extern and outside math/**, this round's HARD AVOID) | **done** (6/6 differential incl. integer+fraction KAT (pos/neg) + domain-boundary coverage (+-0.0, +-infinity, NaN, large-magnitude 1e20/-1e20 beyond i64 range) + 100-vector shared branch-covering bulk loop, C/Rust retained as oracle; PERF FINDING: Simple ~2.65x slower under interpreter, recorded not hidden. Bug found+fixed mid-spec: unguarded `x as i64` corrupted +-infinity/large-magnitude input; fixed with a 2^52 integrality-threshold guard) |
| C-MIG-0032 | interpreter_extern/math.rs:rt_math_ceil (no C runtime counterpart) | ~10 | product algorithm | std.common.numeric_round.ceil_f64 (same NEW file as C-MIG-0031's floor_f64, numeric_round.spl) | **done** (6/6 differential incl. integer+fraction KAT (pos/neg) + domain-boundary coverage (+-0.0, +-infinity, NaN, large-magnitude 1e20/-1e20 beyond i64 range) + 100-vector shared branch-covering bulk loop, C/Rust retained as oracle; PERF FINDING: Simple ~2.77x slower under interpreter, recorded not hidden. Shares C-MIG-0031's integrality-threshold guard/bug fix) |
| C-MIG-0033 | interpreter_extern/math.rs:rt_math_is_nan (no C runtime counterpart) | ~10 | product algorithm | std.common.numeric_round.is_nan_f64 (same file as C-MIG-0031/0032, numeric_round.spl -- IEEE 754 self-inequality `x != x`) | **done** (5/5 differential incl. finite KAT + domain-boundary coverage (+-infinity, two independent NaN constructions, large-magnitude 1e20/-1e20, denormal 5.0e-324) + 100-vector shared branch-covering bulk loop, C/Rust retained as oracle; ratio ~1.9x under interpreter, below the 2x threshold, no PERF finding. REAL divergence found (not in is_nan_f64 itself): interpreter float `/` raises `semantic: division by zero` for `0.0 / 0.0` instead of producing NaN, filed as `doc/08_tracking/bug/interpreter_float_division_by_zero_raises_instead_of_nan_2026-08-18.md`. Rejected candidates: rt_math_round/trunc/abs — unregistered in interpreter extern dispatch (grep confirms zero hits); transcendental externs (exp/log/sinh/etc.) deferred, not bit-exact-verifiable without a stated tolerance contract) |
| C-MIG-0034 | interpreter_extern/math.rs:rt_math_is_inf (no C runtime counterpart) | ~10 | product algorithm | std.common.numeric_round.is_inf_f64 (same file as C-MIG-0031/0032/0033, numeric_round.spl -- locally-built +infinity via overflow, compared against both signs) | **done** (5/5 differential incl. finite KAT + domain-boundary coverage (+-infinity both signs, NaN, large-magnitude 1e20/-1e20, largest normal finite 1.0e308, denormal 5.0e-324) + 100-vector shared branch-covering bulk loop, C/Rust retained as oracle; PERF FINDING: Simple ~2.56x slower under interpreter, recorded not hidden. No domain-edge defect found (both-signs comparison was written correctly from the start). Rejected candidates: same unregistered round/trunc/abs as C-MIG-0033; rt_math_is_finite — registered and a legitimate sibling but skipped to keep the pair non-redundant with is_nan_f64/is_inf_f64) |

| C-MIG-0035 | interpreter_extern/simd.rs:rt_text_to_upper_ascii (no C runtime counterpart) | ~10 | product algorithm | std.common.text_ascii.to_upper_ascii (NEW file text_ascii.spl, written this round -- character-scan + literal lowercase-alphabet `index_of` lookup, deliberately independent of the rt_text_to_upper_ascii extern's byte-map and outside string_core.spl, this round's HARD AVOID) | **done** (5/5 differential incl. mixed-case KAT + edge cases (empty, single char, all-whitespace, digits/punctuation passthrough, non-ASCII multibyte UTF-8 passthrough incl. adjacency to an ASCII run) + 100-vector shared branch-covering bulk loop, C/Rust retained as oracle; PERF FINDING: Simple ~25.4x slower under interpreter -- O(n^2) `text` concatenation in the char loop, the interpreter-lane caveat sharpened for a string-building kernel, recorded not hidden. Design-time defect caught before the spec ran: initial draft used a nonexistent `ch.code()` accessor, fixed with an `index_of` table lookup instead. Rejected candidates: rt_text_validate_utf8/rt_text_find_invalid_utf8 -- opaque result shape, deferred; rt_text_is_ascii -- registered legitimate sibling, skipped to diversify domains with C-MIG-0036) |
| C-MIG-0036 | interpreter_extern/file_io.rs:rt_path_ext (no C runtime counterpart) | ~10 | product algorithm | std.common.path_pure.path_ext (NEW file path_pure.spl, written this round -- explicit last-separator/last-dot character scan, deliberately independent of Rust's `Path::extension()` machinery) | **done** (5/5 differential incl. simple/nested/absolute-path KAT + edge cases (empty, no-dot name, dotfile with/without a further dot, '.'/'..', trailing separator with and without a preceding dot, path of only separators, repeated separators, trailing dot, bare filename, non-leading dotfile) + 100-vector shared branch-covering bulk loop over 9 base-name shapes, C/Rust retained as oracle; PERF FINDING: Simple ~10.7x slower under interpreter, recorded not hidden. REAL defect found+fixed via the bulk loop: `last_component` treated ANY trailing separator as "no final component", but Rust's `Path::file_name()`/`extension()` actually ignore trailing separators and use the component before them ("a.b/" -> "b", "dir/f.rs/" -> "rs" on the oracle, not ""); fixed by stripping trailing separators before scanning. Rejected candidates: rt_path_basename/rt_path_dirname -- registered legitimate siblings, extension() picked for its sharper edge-case surface; rt_path_absolute/rt_path_relative -- touch real filesystem/CWD, deferred as environment-dependent) |

Every `assess` entry must be resolved to a concrete class before Wave-4 work
on it starts (unclassified = critical failure per release gates §18). New
entries append here AND in `c_migration_inventory.sdn`; the SDN file is the
authority.

## Pointer: dispatch-dead audit (goal 2/6, 2026-08-18)

See `doc/08_tracking/c_migration/dispatch_dead_c_audit_2026-08-18.md` — full
sweep of 1458 owned `rt_*` C definitions vs. 1901 registered in
`interpreter_extern/*.rs`: 710 unregistered from the interpreter's dispatch,
of which 250 are native_lane_called (native/AOT codegen or `.spl extern fn`),
93 rust_seed_called, 344 c_internal, and **23 DEAD** (deletion candidates,
each needing one non-grep follow-up check, none deleted). All 8 symbols named
in the originating finding (`rt_string_find`, `rt_string_replace`,
`rt_wire_to_hex`, `rt_hex_to_wire`, string-case family) are live in the
native/AOT lane except `rt_string_replace_first` (registered in the codegen
`RuntimeFuncSpec` table but never emitted/called — linkable but unused).
