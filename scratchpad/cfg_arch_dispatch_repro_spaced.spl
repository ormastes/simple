@cfg(arch = "x86_64")
fn arch_name() -> text:
    "x86_64"

@cfg(arch = "arm64")
fn arch_name() -> text:
    "arm64"

fn main() -> i32:
    val got = arch_name()
    print("spaced form: arch_name() = {got}")
    0
