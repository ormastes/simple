# Test trait inheritance syntax

trait Clone:
    fn clone() -> Self

# Test basic inheritance
trait Copy: Clone:
    fn copy() -> Self

# Test multiple inheritance
trait Eq:
    fn eq(other: &Self) -> bool

trait Ord: Eq:
    fn cmp(other: &Self) -> Ordering

enum Ordering:
    Less
    Equal
    Greater
