# Test file for lean{} block feature

# Module-level lean block with inline code
lean{
    -- Lean 4 code embedded in Simple
    theorem add_comm : ∀ a b : Nat, a + b = b + a := by
        omega
}

# Lean import statement
lean import "proofs/base.lean"

# Lean import with extension
lean import "proofs/math.lean"

fn factorial(n: i64) -> i64:
    if n <= 1:
        1
    else:
        n * factorial(n - 1)
