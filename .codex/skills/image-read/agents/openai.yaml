interface:
  display_name: "Image Read"
  short_description: "Extract faithful Markdown and chart data from images"
  default_prompt: "Use $image-read to convert the supplied image into faithful Markdown and a provenance receipt."
