use std.gc_async_mut.gpu.engine2d.backend_metal.{probe_metal}
use std.gc_async_mut.gpu.engine2d.engine.{Engine2D}

val probe = probe_metal()
print probe.diagnostic_text()

val strict_probe = Engine2D.probe_backend(16, 16, "metal")
print strict_probe.diagnostic_text()
