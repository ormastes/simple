@allow(star_import) # reason: compatibility facade, re-exports backing family symbols
# Compatibility facade: std.gc_sync_mut.mqtt.connect -> std.gc_async_mut.mqtt.connect
export use std.gc_async_mut.mqtt.connect.*
