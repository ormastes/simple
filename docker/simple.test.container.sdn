# Example Test Configuration with Container Support
#
# Copy to simple.test.sdn to enable container-based test execution.
#
# Container execution provides:
# - Full isolation via Docker/Podman
# - Enforced resource limits (memory, CPU)
# - Network isolation
# - Read-only filesystem mounts

# Basic test settings
timeout_seconds: 60
memory_limit_mb: 512
cpu_limit_seconds: 30
max_fds: 256
max_procs: 64
parallel: false
fail_fast: false

# Performance thresholds
slow_threshold_ms: 5000
cpu_threshold: 80.0
memory_threshold: 80.0
throttle_enabled: false

# Container settings
container_enabled: false
container_runtime: auto
container_image: simple-test-runner:latest

# To enable container execution:
# 1. Build the container image:
#    docker build -f docker/test-runner.Dockerfile -t simple-test-runner:latest .
#
# 2. Set container_enabled to true:
#    container_enabled: true
#
# 3. Or use CLI flags:
#    bin/simple test --container
#    bin/simple test --container-runtime=docker
#    bin/simple test --container-image=custom-image:tag
#
# 4. Or use container execution mode:
#    bin/simple test --mode=container

# Notes:
# - container_runtime: "auto" (detect), "docker", or "podman"
# - container_enabled: true forces all tests to run in containers
# - --container flag overrides this setting
# - Container mode provides stronger isolation than safe mode
