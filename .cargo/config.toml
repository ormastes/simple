[build]
# Build configuration for Simple language

[target.x86_64-unknown-linux-gnu]
# Add rpath for PyTorch libraries so the binary can find them at runtime
rustflags = [
    "-C", "link-arg=-Wl,-rpath,/home/ormastes/.local/lib/python3.12/site-packages/torch/lib",
]
