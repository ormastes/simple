# 95_simd_vectors.spl - SIMD vector operations (IMPLEMENTED)
#
# SIMD operations are fully implemented via FFI:
# - Reductions: vec_sum, vec_product, vec_min, vec_max, vec_all, vec_any
# - Lane access: vec_extract, vec_with
# - Element-wise: vec_sqrt, vec_abs, vec_floor, vec_ceil, vec_round
# - Shuffle/blend: vec_shuffle, vec_blend, vec_select
#
# See: src/rust/runtime/src/value/simd.rs

# Demonstrate basic SIMD operations on arrays
val numbers = [1, 2, 3, 4, 5]
print "Vector: {numbers}"

# Use array operations (SIMD-optimized internally)
val total = numbers.sum()
print "Sum: {total}"

main = total
