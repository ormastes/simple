# 30_struct_class_semantics.spl - Value copy vs reference semantics

# Structs are value types (copy-on-assign)
struct Cursor:
    x: i32
    y: i32

# Classes are reference types
class Box:
    value: i32

var a = Cursor { x: 1, y: 2 }
var b = a       # copy
b.x = 9
print "struct copy done"   # expect a.x still 1

val p = Box { value: 10 }
val q = p       # reference alias
q.value = 99
print "class reference done"  # expect both 99

main = p.value
