# Simple Language Standard Library
# Default variant: async + no_gc + mutable
#
# Lint policy:
# - #[deny(primitive_api)] - No bare primitives in public APIs
# - #[deny(bare_string)] - No bare str/text in public APIs

#[deny(primitive_api)]
#[deny(bare_string)]
#[deny(bare_bool)]
mod std

# Prelude - auto-imported into every file
export prelude.*

# Core modules (variant-agnostic, pure)
common use core.*
common use core.option.*
common use core.result.*

# Unit type definitions
common use units.*

# Concurrency primitives (generators, futures, actors, channels)
common use concurrency.*

# Default async_nogc_mut profile
use host.async_nogc_mut.*

# System module (process control, stdio, args)
export sys.*

# UI Framework (shared TUI/GUI API)
export ui.*

# Infrastructure APIs (allocators, concurrency, atomics, etc.)
export infra.*

# Coverage analysis (condition/decision coverage, path coverage, MC/DC)
export coverage.*

# Multi-language tooling (build, test, deploy)
export tooling.*

# Compiler APIs (lexer, parser, etc.)
export compiler.*

# CLI argument parsing
export cli.*

# Configuration management (SDN-based, hierarchical)
export config.*

# File system operations (file I/O, directories, paths)
export fs.*

# Regular expressions (pattern matching, searching, replacement)
export regex.*

# Data serialization (JSON, etc.)
export data.*

# SDN (Simple Data Notation) parsing and serialization
export sdn.*

# Database operations (atomic writes, persistence, unified API)
export db.*

# Spec/BDD testing framework
export spec.*

# Mixins - reusable stateful compositions (compile-time field/method injection)
export mixins.*

# Traits - dynamic dispatch utilities (dyn Trait helpers)
export traits.*
