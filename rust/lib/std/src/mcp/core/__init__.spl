# MCP Core Library
# Reusable Model Context Protocol implementation for any language/tool
# Extended with MCP specification 2025-11-25 compliance

# Core protocol types
export protocol.*
export server.*
export provider.*

# Note: transport not re-exported here to avoid pulling in net/tcp.spl
# which uses async fn (not yet supported). Import directly:
#   use std.mcp.core.transport         # Base transport (stdio, tcp, mock)
#   use std.mcp.core.transport_http    # HTTP/SSE transport
#   use std.mcp.core.transport_websocket  # WebSocket transport

# Utility modules
export markdown.*
export logs.*
export diagnostics.*

# Phase 1: Core Protocol Compliance
export capabilities.*       # Capability negotiation
export pagination.*         # Cursor-based pagination

# Phase 2: MCP Primitives
export resources.*          # Resource management with subscriptions
export prompts.*            # Prompt management
export tool_schema.*        # Tool schema validation and annotations

# Phase 3: Security
export command_filter.*     # Command security filtering
export auth.*               # Authentication providers (OAuth2, API keys)
export secrets.*            # Secrets management and redaction

# Phase 4: Async & Performance
export tasks.*              # Task lifecycle management
export notifications.*      # Progress notifications
export streaming.*          # Enhanced streaming support
