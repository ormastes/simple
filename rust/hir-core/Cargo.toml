[package]
name = "simple-hir-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared HIR types for Simple compiler and interpreter"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
