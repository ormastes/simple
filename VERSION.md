# Simple toolchain / Simple Feature Module (SFM) version.
# Format: a single SemVer line (the first non-comment line is the version).
# Consumed at build time by std.sfm.version.read_version_md and embedded into
# built .sfm feature modules / apps (AC-12).
0.1.0
