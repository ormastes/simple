use compiler.tools.duplicate_check.config.{load_config}
use compiler.tools.duplicate_check.detector_files.{should_exclude_detector_path, collect_detector_files}

val config = load_config()
print "Exclude patterns: {config.exclude_patterns.len()}"
for pat in config.exclude_patterns:
    print "  '{pat}'"

val test_path = "test/fixtures/duplicate_check/exact_a.spl"
val excluded = should_exclude_detector_path(test_path, config)
print "should_exclude_detector_path('{test_path}'): {excluded}"
