# Build artifacts
build/target/
build/
build/test-artifacts/
*.smf

# Stray tool outputs dropped in CWD (Vivado runtime dir). The compiler
# default-binary entry (/a.out) was removed 2026-08-05: native-build now
# defaults output to build/native/<entry-stem>, never the repo root -- see
# doc/03_plan/compiler/driver/default_output_dir_containment_plan.md.
.Xil/

# Per-session agent scratch (recover dirs, harnesses, probe patches). Transient;
# must never be committed to origin (was leaking 26 files into syncs).
/scratchpad/

# mcp-publisher local auth tokens (NEVER commit) — written by `mcp-publisher login github`
.mcpregistry_*

*.o
*.a
*.so
*.dylib
*.dll
*.rlib
*.rmeta
*.bc
*.d
*.fingerprint
*.timestamp
*.bin
!test/09_baselines/famous_site_corpus/glyph_atlas/*.bin
!test/fixtures/slang/valid_pack/data-000.bin
!test/fixtures/slang/cross_chunk_pack/data-000.bin
!test/fixtures/slang/cross_chunk_pack/data-001.bin
*.app
*.pcm
*.pdb
*.ilk
*.lib
*.exp

# Swift/Xcode intermediate artifacts
*.swiftmodule
*.swiftdoc
*.swiftdeps
*.swiftsourceinfo
*.dia
*.modulemap

# Python caches
__pycache__/
*.pyc
*.pyo

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
log/
!src/compiler_rust/vendor/log/
!src/os/kernel/log/
!src/lib/log/
!src/lib/nogc_async_mut_noalloc/log/
!test/os/kernel/log/
*.log
*.log.*
.simple/
.playwright-mcp/
build/node-compile-cache/
build/verification/

# Seed bootstrap artifacts
src/compiler_rust/target/
src/compiler_rust/target_codex_*/
src/compiler_rust/bin/release/
bin/bootstrap/
build/bootstrap/
bootstrap_cl2/
/bootstrap/stage*/simple
/bootstrap/stage*/**/simple
/test/01_unit/lib/nogc_async_mut/multicore_green_native
bin/simple_mac
bin/simple_stage3
bin/simple
bin/simple_mcp_server
bin/simple_lsp_mcp_server
bin/t32_mcp_server
bin/t32_lsp_mcp_server
bin/codex_chrome_devtools_mcp.js
bin/codex_stitch_mcp.js
bin/mcp_stdio_proxy.spl
bin/obsidian_lsp_mcp_server
bin/simple-torch
bin/simple_lsp_server
bin/task
bin/simple-stub
tools/mcp-registry/native/
tools/mcp-registry/native/**/src/compiler_rust/bin/release/
examples/cuda_exercise/
examples/09_embedded/fpga_riscv/test/test_c_runtime.pkg.vhd
bin/release/
build/
!doc/02_requirements/app/build/
!doc/02_requirements/app/build/**
!src/app/build/
!src/app/build/**
!src/lib/*/engine/build/
!test/01_unit/app/build/
!test/01_unit/compiler/build/
!test/01_unit/lib/*/engine/build/
.DS_Store
**/.DS_Store

# Node / VSCode extension dependencies
node_modules/
**/node_modules/
*.vsix
**/.vscode-test/

# Tauri artifacts
tools/tauri-shell/src-tauri/target/

# Coverage
*.profraw
*.profdata

# Temporary
tmp/
temp/
.tmp*/
simple-mcp-native-smoke.*
simple_mcp_native_diag.*/
.spipe_wrapped_entry_*.spl
.spipe_matchers_*
!test/system/app/**/.spipe_matchers_*_spec.spl

# GHDL work library (generated by ghdl -a)
work-obj*.cf
e~*.o

# GCC linker/compiler temporaries
cc*.cdtor.*
cc*.res
cc*.s

# Rust compiler temporaries
/rustc*

# Node.js temporaries
cm-fs-preload-*.js

# JIT note files
build/map.smf.jit.note.sdn

# Root-level generated binaries/evidence from local smoke runs
cfat32_4k_baseline
simpleos_nvme_preflight_check_*.sdn
missing_input.spl/
/tmp_aot_smoke_*
/run_*.log
/.tmp_git_stash_test_file

# Test temp directories/files
daemon_lock_test_*
simple_llm_dashboard_*
simple_tls_hosted_interop_*
tmp.*

# Claude Code temp directories
claude-*/
# ...but tools/claude-plugin/ is real source (marketplace + plugins), not a temp dir
!tools/claude-plugin/

# Vivado / Xilinx
.Xil/
vivado*.log
vivado*.jou
vivado*.backup.*

# Windows NUL artifact
NUL

# Stray CLI flag dirs (FPGA bundle generator bug)
--/
--arch/
--help/
--output-dir/

# Build cache / dashboard
.build/

# Codex test artifacts
target_codex_*/

# Vendor board files (use src/hardware/vendor/)
third_party/

# Claude Code worktrees
.claude/worktrees/
doc/report/

# Generated artifacts
artifacts/
artifacts/*.png
artifacts/*.jpg
artifacts/*.jpeg
system/
!test/system/
!test/system/gui_entry_engine2d_wm_simple_web_spec.spl
src/verification/generated/
src/verification/**/.lake/
src/verification/**/*.olean
src/verification/**/*.ilean
src/verification/**/*.trace

# Test database volatile data
doc/08_tracking/test/test_db_runs.sdn
doc/08_tracking/test/test_db.sdn.lock
**/test_db.sdn.backup

# Scheduled task lock
.claude/scheduled_tasks.lock

# Local settings
.claude/settings.local.json

# Spipe skill state — tracked so jj preserves pipeline progress
# .spipe/

# Local jj workspace metadata
.jj/

# Plugin demo fixture build outputs (built by test/feature/plugin/fixtures/build_fixtures.shs)
test/feature/plugin/fixtures/demo.sdn

# NVMe preflight check stray outputs (run_simpleos_physical_nvme_perf.shs with TMPDIR=.)
simpleos_nvme_preflight_check_*.sdn
simpleos_nvme_preflight_scan_*.sdn

# FAT32 baseline binary (run-fat32-4k-cfat-baseline.shs with TMPDIR=.)
cfat32_4k_baseline

# Mobile Tauri shell UI source bundle (generated by tools/tauri-shell/scripts/build-ui-bundle.shs)
tools/tauri-shell/src-tauri/ui_bundle.tar.gz

# Mobile Tauri shell cross-compiled iOS-sim Simple runtime (embedded by build.rs)
tools/tauri-shell/src-tauri/ios_runtime_aarch64_sim.bin

# Vendored cargo sources must stay hermetic (src/compiler_rust/.cargo/config.toml
# replaces crates-io with this directory); earlier rules like log/, third_party/,
# system/, *.o, *.lib silently drop crate files and break offline CI builds.
# Keep last so it wins over every rule above.
!src/compiler_rust/vendor/**
examples/10_tooling/simple_ide/platform/vscode/node_modules/

# Test runner transient state (rewritten every run; was polluting every WC snapshot)
test-results/

# Deploy backups (dated .bak files next to deployed binaries)
bin/release/*/*.bak-*
bin/release/*/*.bak
.simple-big-storage-root

# recursion guard: simply showcases simple; never vendor it back as an example
examples/simply/
simply/

# Personal bootstrap launchers (machine-specific absolute paths, local toolchain
# locations). run_s2.sh was committed by accident in 2eb51991053; these are
# developer scratch wrappers around scripts/bootstrap/bootstrap-windows.sh and
# must not be shared repo content.
/run_s2*.sh

# Merge-conflict backup artifacts: <name>~<git-sha>
*~[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]*
target/test-artifacts/
# --- Build outputs and stray binaries that were tracked by accident (2026-09-03) ---
# Cargo build/coverage output under test fixtures and dev tools. These were
# committed (200MB, 151 files incl. a 44MB linux `simple`, two 28MB pinned
# stage2 binaries and a 30MB llvm-cov artifact) and are pure build product.
/release/
/scratch_fx/
/bin/simple_native
test/01_unit/compiler/i18n/extractor_isolated/target/
tools/tls_test_server/target/

# --- Repo-root pollution containment (rooted patterns only; see FILE.md) ---
# Every pattern below matched a file that was actually committed at the root and
# removed on 2026-09-03. Rooted (leading /) so nothing under src/ or test/ is hit.
/*.gcov
/*.jit.note.sdn
/*.rc
/*.out
/*.err
/test_threshold*.sdn
/fixture-event-proof*.js
/tmp_*.spl
/_tmp_*.spl
/probe*.spl
/repro_*.spl
/tiny_lint_probe.spl
/test_*.spl
/.w[0-9]*
/.shard_*
/.done.txt
/.skip.txt
/.todo.txt
/.targets.txt
/.sspec_targets.txt
/.reverted*.txt
/cpan_install_*.txt
/gh-cli-cache/
/logs/
/probes/
/repro/
/evidence/
/bench/
/valid/
/sweep/
/triage/
/scratch/
/tmp[0-9]*/
/tmpdrv/
/tmp_debug/
/NUL
/d.o
/README
/scratch_spec_files.txt
