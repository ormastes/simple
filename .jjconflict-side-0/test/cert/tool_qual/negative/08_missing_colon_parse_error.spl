# Tool-qual NEGATIVE case: malformed syntax (missing `:` after fn header).
# Distinct parse-error shape from 03_parse_error.spl (unclosed paren / dangling
# operator). MUST be rejected (non-zero exit, diagnostic on stderr) and MUST
# NOT silently produce stdout.
fn main()
    print(1)
