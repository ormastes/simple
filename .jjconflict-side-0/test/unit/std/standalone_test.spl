# Local stub for cached
# (import not resolvable in interpreter mode)
fn cached(f):
    f

fn main():
    fn square(x):
        return x * x
    val c = cached(square)
    print "Standalone success!"
