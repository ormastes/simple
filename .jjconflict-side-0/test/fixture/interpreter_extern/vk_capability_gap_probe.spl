# Fixture for test/01_unit/compiler/interpreter_extern/capability_gap_spec.spl.
# Calls a real rt_vk_* extern (declared in
# src/lib/gc_async_mut/gpu/session/backend_runtime_ops.spl) that has no entry
# in EXTERN_DISPATCH and no native implementation anywhere (M3 capability
# gap). Distinct from rt_vulkan_*, which is a real, separately-registered
# family (see interpreter_extern/vulkan.rs) — this fixture must never be
# confused with that one.

extern fn rt_vk_cleanup(handle: i64) -> i64

fn main() -> i64:
    val r = rt_vk_cleanup(0)
    print "unexpected success: rt_vk_cleanup={r}"
    0
