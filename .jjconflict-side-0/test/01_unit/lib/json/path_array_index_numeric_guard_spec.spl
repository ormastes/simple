extern fn rt_file_read_text(path: text) -> text

describe "json path array index numeric guard":
    it "returns nil for malformed array indexes":
        val source = rt_file_read_text("src/lib/common/json/path_ops.spl") ?? ""

        expect(source).to_contain("if idx == nil:")
        expect(source).to_contain("return nil")
