# Helper module for the cross-module any? receiver-type-erasure regression
# (see anyq_receiver_erasure_spec.spl). The whole point is that the RETURN
# TYPE is type-erased (`any?`), so the caller's explicit annotation is the
# only concrete type information available at the binding.

fn get_obj(x: any) -> any?:
    Some(x)
