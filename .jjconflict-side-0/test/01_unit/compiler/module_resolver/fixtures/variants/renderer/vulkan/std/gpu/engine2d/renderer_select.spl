# Fixture: variant impl for std.gpu.engine2d.renderer_select under renderer=vulkan.
# Content is intentionally minimal — the file's existence is what the spec probes.
fn renderer_select_variant_marker() -> text:
    "vulkan"
