# An external source program must never be able to invoke the compiler-only
# staging intrinsic.  The companion semantic spec executes this file with
# SIMPLE_TYPECHECK_PROFILE=advisory and requires E-RTHAL-STAGED-PRIVATE.

use app.io.env_access_host.{rt_hal_compiler_stage_isolated_exact_host_from_plan_path_or_fail}

fn main():
    rt_hal_compiler_stage_isolated_exact_host_from_plan_path_or_fail("", 0, "")
    print "RTHAL_STAGED_EXTERNAL_FIXTURE_RAN"
