# Fixture: direct raw runtime call with no local extern declaration. The lint
# must diagnose the call itself; link-time undefined-symbol checks are too late.

fn call_directly() -> i64:
    val (_out, _err, code) = rt_process_run("/bin/echo", ["hi"])
    code
