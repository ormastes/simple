# Sample feature module with public and internal functions

# This is a public function
fn public_function(x: i64) -> i64:
    x * 2

# This is an internal helper
fn _internal_helper(y: i64) -> i64:
    y + 1

# Public struct
struct PublicStruct:
    value: i64

# Internal struct
struct InternalStruct:
    data: text
