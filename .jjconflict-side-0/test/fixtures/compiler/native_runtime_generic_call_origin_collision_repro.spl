# OPEN BUG REPRO: compiler-generated array helpers and source calls currently
# have indistinguishable MIR targets. Do not add this to a green gate until MIR
# carries explicit call origin.
fn rt_array_new(capacity: i64) -> i64:
    99

fn rt_array_push(array: i64, value: i64) -> i64:
    98

fn first() -> i64:
    1

fn second() -> i64:
    2

fn main() -> i64:
    # Function-valued literals take the named rt_array_new/rt_array_push MIR
    # path; scalar literals use a dedicated ArrayLit instruction instead.
    val items = [first, second]
    print(items.len())
    print(rt_array_new(0))
    0
