# Fixture with real spacing violations that the pure-Simple formatter
# (src/compiler/90.tools/formatter/main.spl module compiler.tools.formatter)
# actually fixes. See test/03_system/app/fmt_doccov_contract_spec.spl for the
# runnable contract check and expected exit codes.

fn add(a:i64,b:i64)->i64:
    a+b

fn sub(a:i64,b:i64)->i64:
    a-b

fn main():
    print add(1,2)
    print sub(4,1)
