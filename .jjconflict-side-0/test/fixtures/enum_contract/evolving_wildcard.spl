@evolving(repr: u16, unknown: Unknown)
enum Opcode:
    Nop = 0
    Halt = 1
    Unknown(u16)

fn name_of(o: Opcode) -> text:
    match o:
        case Opcode.Nop: "nop"
        case Opcode.Halt: "halt"
        case _: "other"

fn main():
    print(name_of(Opcode.Nop))
