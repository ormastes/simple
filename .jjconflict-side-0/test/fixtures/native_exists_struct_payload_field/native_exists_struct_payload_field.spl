use test.fixtures.native_exists_struct_payload_field.provider.{EvidenceSource}

# `.?` must carry the imported Evidence payload, not its presence bool.
fn main() -> i64:
    val source = EvidenceSource()
    val evidence = source.evidence()
    print(evidence.?.marker)
    0
