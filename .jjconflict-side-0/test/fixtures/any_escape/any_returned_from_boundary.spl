# Fixture: `Any` created inside a granted type_erasure region and RETURNED
# out of it -> E-MC-ANY-002.
@unsafe(reason: "decode legacy plugin payload", capabilities: [type_erasure])
fn leak_raw() -> Any:
    unsafe:
        val raw: Any = 1
        return raw
    1
