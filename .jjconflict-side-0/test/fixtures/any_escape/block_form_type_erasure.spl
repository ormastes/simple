# Fixture: §8.1 PRIMARY block form — the grant is on the block, not on a
# declaration annotation and not in any profile list. Any inside is licensed.
fn decode_legacy() -> i64:
    unsafe(reason: "decode legacy plugin payload", capabilities: [type_erasure]):
        val raw: Any = 1
    4
