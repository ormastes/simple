#!/bin/sh
# Compatibility entrypoint: the migrated runtime requires admitted Stage 4.
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../../.." && pwd)
exec "$root/scripts/check/check-cosmos-runtime-core-policy.shs"
