class Counter:
    value: i64

fn sum_values(items: [Counter]) -> i64:
    var total = 0
    for c in items:
        total = total + c.value
    return total

fn main() -> i64:
    val items = [Counter(value: 10), Counter(value: 20), Counter(value: 12)]
    return sum_values(items)
