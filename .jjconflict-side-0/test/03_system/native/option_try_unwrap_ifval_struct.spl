struct OptUnwrapP:
    x: i64
    y: i64

fn main() -> i64:
    val x: OptUnwrapP? = OptUnwrapP(x: 3, y: 4)
    if val v = x.?:
        if v.x == 3 and v.y == 4:
            return 42
        return 1
    return 0
