# Native-codegen frontier map — TYPE / OOP / COLLECTION battery (2026-07-13)

Generated via isolated worktree `/tmp/wt_fmap_ty` (detached at origin/main
`f57dd0ae96f2`), copying `bin/release/x86_64-unknown-linux-gnu/simple` (Rust
seed) into the worktree. Interpreter oracle = `bin/simple run <src>`. Native =
`env -u SIMPLE_BOOTSTRAP bin/simple native-build --entry <src> -o <bin> --clean`
then execute under `timeout 8` (mirrors `scripts/check/native-smoke-matrix.shs`).
Does NOT repeat enum/match, single struct methods, trait defaults, Dict
insert/lookup, generics, or while/match/recursion/closures (covered by prior
sweeps).

| Feature | File | Oracle rc | Native rc/status | Verdict | Detail |
|---|---|---|---|---|---|
| (a) nested struct construct + read | `a_nested_struct.spl` | 17 | 16 | **MISCOMPILE** | Isolated via 3 sub-probes: `r.origin.x`→3 (correct), `r.w`→10 (correct), but `r.origin.y`→**3** (should be 4 — returns the *first* field of the nested inner struct instead of the requested second field). Root cause: field-offset computation for a sub-field access through a nested struct field picks the wrong offset when the outer struct's first field is itself a struct with >1 field. FIXABLE-BUG, high priority (silent wrong value, no error). |
| (b) struct in array, mutate element field, read back | `b_struct_in_array_mutate.spl` | 103 | 103 | **PASS** | `items[1].count = 99` then sum — both paths agree. |
| (c) enum value in Dict, retrieve + match | `c_enum_in_dict.spl` | 3 | build-fail | **BUILD-FAIL** | `llc-18: error: '%l3' defined with type 'i64' but expected 'ptr'` on `call i64 @rt_dict_set(ptr %l3, i64 %l10, i64 %l13)` — MIR→LLVM lowering emits an `i64` value where `rt_dict_set` needs a `ptr` when the Dict's value type is an enum. FIXABLE-BUG in Dict-of-enum codegen. |
| (d) array of enums, iterate + match each | `d_array_of_enums.spl` | 16 | 16 | **PASS** | `for s in shapes: match s: Shape.Circle(r): ... Shape.Square(side): ...` — both paths agree. |
| (e) Option Some/None match (manual, after `.map` proved a feature-gap) | `e_option_map.spl` | 5 | build-fail | **BUILD-FAIL** | Oracle note: `a.map(fn(x)...)` on `Option<i64>` is a FEATURE-GAP — interpreter raises `Runtime error: Function 'Option.map' not found` (not implemented at all), so the battery fell back to manual `Some`/`None` construct+match, which the interpreter handles correctly (rc=5). Native then fails at HIR lowering: `error: HIR lowering error in ...: unresolved name: Some` / `unresolved name: None` — bare `Some(...)`/`None` constructors used as plain expressions (not already known to be Option-typed at the call site) aren't resolved in native HIR lowering even though the interpreter resolves them fine. FIXABLE-BUG (native HIR gap), distinct from the `.map` feature-gap. |
| (f) tuple construct + destructure + `.0`/`.1` | `f_tuple_destructure.spl` | 26 | build-fail | **BUILD-FAIL** | `val (a, b) = p` then `p.0 + p.1 + a + b`. Interpreter: 7+6+7+6=26, correct. Native: `error: HIR lowering error in ...: unresolved name: a` / `unresolved name: b` — tuple-destructuring `val (a, b) = expr` binding pattern is not lowered to real bindings in native HIR (names never get declared). FIXABLE-BUG. |
| (g) mixin/composition via trait-alias forwarding (`class W with T: ... alias T = field`) | `g_mixin_forward.spl` | build-fail (interp) | build-fail (native) | **FEATURE-GAP (both paths broken, differently)** | Interpreter *parses* `class Wrapper with Sizeable: items: BucketImpl; alias Sizeable = items` but at runtime: `Runtime error: Function 'Wrapper.size' not found` — the desugar pass documented in `syntax_quick_reference.md` §"Forwarding a Trait to a Field" never actually generates the forwarding method for this construct. Native path is stricter and rejects the syntax outright at parse time: `[parser_error] ... expected :, got Ident 'Sizeable'` on the `alias Sizeable = items` line. So the documented mixin-forwarding syntax is non-functional end-to-end: interpreter accepts-but-silently-no-ops it, native doesn't even parse it. Worth filing as one gap (parser divergence + missing desugar codegen). |
| (h) operator overloading (`fn __add__`/`fn __eq__` on a struct, dispatched via `+`/`==`) | `h_operator_overload.spl`, `h_debug*.spl` | **CRASH** (SIGSEGV, core dump) | wrong-result / **CRASH** | **CRITICAL — MISCOMPILE+CRASH** | Renaming `__add__`→`add2` and calling it explicitly works fine in the interpreter (rc=4, correct) — so the struct/method machinery itself is fine. But using operator syntax `a + b` where `Vec2` defines `fn __add__(self, other: Vec2) -> Vec2` **crashes the interpreter** (`timeout: the monitored command dumped core`, confirmed reproducible on a minimal repro with just `__add__` and no `__eq__`/no print). Native build of the same minimal program (no print) *succeeds* and runs, but returns the wrong answer (rc=255 = -1, i.e. the `c == Vec2(x:4,y:6)` equality silently evaluates false — `__add__`/`__eq__` dispatch is not wired to the `+`/`==` operators in native codegen, no compile error, just silently-wrong behavior). Adding a `print "{c.x}"` to the same native program then makes it **crash with SIGBUS** instead. So: interpreter segfaults on custom `+` overload; native either silently miscomputes or crashes depending on surrounding code. FIXABLE-BUG, top priority — an oracle-crashing feature that also silently miscompiles/crashes natively. |
| (i) mutating a struct passed to a fn (value vs ref semantics) | `i_struct_mutate_fn_arg.spl` | 5 (unmutated) | 6 (mutated) | **MISCOMPILE (semantic divergence)** | `fn bump(c: Counter): c.n = c.n + 1` called on `var c = Counter(n: 5)`, then `return c.n`. Interpreter: JIT refuses with `Memory safety error [W1006]: mutation without mut capability`, falls back to its interpreter loop which honors **value semantics** — caller's `c.n` stays 5 (rc=5), matching the "arrays/structs are value types" repo rule. Native: builds cleanly with no warning and returns 6 — i.e. the native path silently gives the callee a mutable reference to the caller's struct, contradicting the documented value-passing semantics that the interpreter (and the W1006 diagnostic) enforce. This is a real semantic disagreement between the two execution paths, not just a missing feature — flag as high priority since it can silently change program behavior when switching interpreter→native. |
| (j) struct with bool field used in `if` | `j_struct_bool_if.spl` | 42 | 42 | **PASS** | `if f.active: return f.value` — both paths agree. |

## Highlights (silent-wrong-artifact risk, ranked)

1. **(h) operator overloading** — interpreter SIGSEGVs on `a + b` dispatching to a user `__add__`; native either silently returns a wrong answer or SIGBUSes depending on whether a `print` is present. Worst class of bug in this batch: both an oracle crash and a native miscompile/crash on the same feature.
2. **(i) struct-by-value mutation through a fn call** — native silently uses reference semantics where the interpreter (with an explicit W1006 diagnostic) uses value semantics. Program behavior changes across execution backends with no error.
3. **(a) nested struct field read** — native returns the nested struct's *first* field's value when the *second* field is requested; clean build, no diagnostic, silently wrong (17 vs 16).
4. **(c) enum-valued Dict**, **(e) Option Some/None construct**, **(f) tuple destructure** — all three are native `BUILD-FAIL`s (LLVM type mismatch / unresolved-name HIR errors), i.e. they fail loudly rather than silently, lower urgency than 1–3 but still block real programs using enums-in-Dict, Option construction, or tuple destructuring in native builds.
5. **(g) mixin/trait-alias forwarding** — documented syntax that is non-functional in both paths (interpreter accepts but no-ops; native rejects at parse time).

PASS: (b) struct-in-array mutation, (d) array-of-enums match, (j) bool-field-in-if — 3/10 clean.

Worktree `/tmp/wt_fmap_ty` removed after this run; no source pushed/committed.
