diff --git a/src/compiler/50.mir/_MirLoweringExpr/expr_dispatch.spl b/src/compiler/50.mir/_MirLoweringExpr/expr_dispatch.spl
index dfc54f2349a..fe5b31f3730 100644
--- a/src/compiler/50.mir/_MirLoweringExpr/expr_dispatch.spl
+++ b/src/compiler/50.mir/_MirLoweringExpr/expr_dispatch.spl
@@ -1091,11 +1091,24 @@ impl MirLowering:
         while idx < arms.len():
             val arm = arms[idx]
             if arm.has_guard:
-                self.error(
-                    "B5b Phase 2 not yet implemented: match arm guard",
-                    Some(arm.span)
-                )
+                # Guard clauses (`case x if cond:`) have no MIR lowering yet
+                # (B5b Phase 2). Fail loudly instead of silently: record a
+                # compile error AND emit an rt_panic call carrying the same
+                # message, so pipelines that drop MirLowering.errors (e.g.
+                # the native-build worker's lower_to_mir — see
+                # lower_for_iterator in mir_lowering_stmts.spl for the same
+                # pattern) still abort with a clear diagnostic instead of
+                # emitting an empty function body that crashes the LLVM
+                # backend on a dangling SSA reference.
+                val guard_unsupported_msg = "match guards (case x if cond:) are not supported by native codegen yet (B5b Phase 2)"
+                self.error(guard_unsupported_msg, Some(arm.span))
                 var b_err = self.builder
+                val panic_msg_local = b_err.emit_const_str(guard_unsupported_msg)
+                val panic_func_operand = MirOperand(kind: MirOperandKind.Const(
+                    MirConstValue.Str("rt_panic"),
+                    MirType(kind: MirTypeKind.FuncPtr(MirSignature(params: [], return_type: MirType.unit(), is_variadic: false)))
+                ))
+                b_err.emit_call(panic_func_operand, [mir_operand_copy(panic_msg_local)], MirType.unit())
                 val err_temp = b_err.new_temp(MirType.unit())
                 self.builder = b_err
                 return err_temp
diff --git a/src/compiler/80.driver/driver.spl b/src/compiler/80.driver/driver.spl
index d732878d229..516e39f04ce 100644
--- a/src/compiler/80.driver/driver.spl
+++ b/src/compiler/80.driver/driver.spl
@@ -91,6 +91,16 @@ fn dtrace(msg: text) -> ():
     if (rt_env_get("SIMPLE_INTERP_TRACE") ?? "") == "1":
         eprint(msg)
 
+# Allowlist of HirLowering error messages that must FAIL the build instead of
+# being silently dropped (see the promotion loop in lower_and_check_impl).
+# "unresolved name:" means an expression referenced a symbol the lowerer could
+# not resolve and decayed to HirExprKind.Error — MIR then emits a dangling SSA
+# reference and llc crashes with "use of undefined value", far from the source.
+# Keep this prefix-scoped: HirLowering also records other diagnostic kinds
+# that today's working builds trigger benignly; audit before widening.
+fn _hir_lowering_error_is_fatal(message: text) -> bool:
+    message.starts_with("unresolved name:")
+
 class CheckBackendImpl(Backend):
     fn name() -> text:
         "check"
@@ -676,6 +686,23 @@ pub class CompilerDriver:
                 )
             log_trace("HIR lowered")
 
+            # HirLowering.errors is a separate bucket from ctx.errors and was
+            # previously never consulted on this (native --entry) path, so a
+            # lowering failure like "unresolved name: x" (e.g. a match binding
+            # pattern in expression position that loses its symbol) decayed to
+            # HirExprKind.Error, sailed on into MIR/LLVM, and crashed llc with
+            # a cryptic "use of undefined value" instead of a source
+            # diagnostic. Promote only the reviewed, allowlisted prefixes (see
+            # _hir_lowering_error_is_fatal) into ctx.errors so they gate the
+            # build; other not-yet-audited lowering errors keep their
+            # pre-existing silent behavior until they get their own audit.
+            var hir_fatal_idx = 0
+            while hir_fatal_idx < lowering.errors.len():
+                val hir_low_err = lowering.errors[hir_fatal_idx]
+                if _hir_lowering_error_is_fatal(hir_low_err.message):
+                    self.ctx.errors = self.ctx.errors.push("HIR lowering error in {name}: {hir_low_err.message}")
+                hir_fatal_idx = hir_fatal_idx + 1
+
             # Fold this module's newly-lowered traits back into the shared
             # registry so later modules (which get their own fresh HirLowering
             # instance) can still resolve cross-module trait default methods.
diff --git a/src/compiler/80.driver/driver_pipeline.spl b/src/compiler/80.driver/driver_pipeline.spl
index e994209cd43..40b24b81f7a 100644
--- a/src/compiler/80.driver/driver_pipeline.spl
+++ b/src/compiler/80.driver/driver_pipeline.spl
@@ -38,6 +38,25 @@ fn _driver_env_bool(key: text) -> bool:
     val value = (rt_env_get(key) ?? "").to_lower()
     value == "1" or value == "true" or value == "yes" or value == "on"
 
+# MirLowering.errors is a separate bucket from CompileContext.errors (see
+# MirLowering.error() in 50.mir/_MirLowering/asm_and_targets.spl) and was
+# previously never read by the native-build worker's lower_to_mir, so any
+# error recorded there (e.g. an unsupported construct) was silently dropped:
+# the pipeline pressed on into codegen with broken/half-lowered MIR (dangling
+# SSA temps) and crashed later with a cryptic backend/llc verifier error
+# instead of a clear diagnostic.
+#
+# We do not promote every MirLowering error to fatal in one shot: the MIR
+# lowerer already accumulates other, unvetted diagnostics (e.g. "unsupported
+# MIR type kind: HirTypeKind::Infer" for some multi-function-call/local-
+# binding shapes) that today are silently dropped AND the surrounding code
+# still produces a working binary despite them; blanket-fatal would newly
+# break those builds. Only messages matching a reviewed prefix here are
+# turned into hard build failures; everything else keeps today's (already
+# broken, unrelated) silent-drop behavior until it gets its own audit.
+fn _mir_error_is_fatal(message: text) -> bool:
+    message.starts_with("match guards (case x if cond:) are not supported")
+
 impl CompilerDriver:
     # --------------------------------------------------------------------------
     # Mode: JIT Compile and Run
@@ -395,6 +414,22 @@ impl CompilerDriver:
                 if name_direct == "app.cli.bootstrap_main":
                     self.ctx.bootstrap_entry_mir = Some(mir_direct)
                 direct_idx = direct_idx + 1
+            # MirLowering.errors is a separate bucket from ctx.errors and was
+            # previously never consulted here, letting the AOT backend
+            # proceed on broken MIR (e.g. a match-guard arm that lowers to no
+            # instructions) and crash later with a cryptic llc verifier error
+            # instead of a clear diagnostic. Promote only the reviewed,
+            # allowlisted MIR errors (see _mir_error_is_fatal) into
+            # ctx.errors so they gate the build the same way HIR errors do;
+            # other not-yet-audited MirLowering errors keep today's
+            # pre-existing silent-drop behavior so this fix doesn't newly
+            # break builds that happened to succeed despite them.
+            var direct_mir_err_idx = 0
+            while direct_mir_err_idx < direct_lowering.errors.len():
+                val direct_mir_err = direct_lowering.errors[direct_mir_err_idx]
+                if _mir_error_is_fatal(direct_mir_err.message):
+                    self.ctx.errors = self.ctx.errors.push("MIR lowering error: {direct_mir_err.message}")
+                direct_mir_err_idx = direct_mir_err_idx + 1
             return self.ctx.errors.len() == 0
 
         var lowering = MirLowering(
