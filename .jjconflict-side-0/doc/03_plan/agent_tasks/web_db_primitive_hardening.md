# Agent Tasks - Web DB Primitive Hardening

1. Harden `BoundedChannel` close semantics and update backpressure tests.
2. Replace production HTTP/web placeholder no-op branches in touched control paths.
3. Replace placeholder DB system spec with production persistence/query workflows.
4. Fix production database bugs exposed by the system spec.
5. Run targeted tests and core/MCP smoke checks; record any harness-only native failures.
