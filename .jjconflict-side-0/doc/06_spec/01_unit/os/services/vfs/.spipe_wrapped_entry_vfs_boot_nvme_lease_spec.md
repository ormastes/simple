# .spipe Wrapped Entry Vfs Boot Nvme Lease Specification

> Tests covering VFS boot NVMe lease contract.

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 13 | 13 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# .spipe Wrapped Entry Vfs Boot Nvme Lease Specification

## Scenarios

### VFS boot NVMe lease contract

#### builds a filesystem-ready system FAT32 lease for pure Simple boot

- builds a filesystem-ready system FAT32 lease for pure Simple boot


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("builds a filesystem-ready system FAT32 lease for pure Simple boot")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
val lease = vfs_boot_nvme_system_fat32_lease(512u32, 65536u64, _tokenized_boot_grant())
expect lease.provider == "simple-driver"
expect lease.shared_block_interface == true
expect lease.namespace_identity.nsid == 1u32
expect lease.lba_count == 65536u64
expect lease.grant_kind.to_start_with("resource-grant-set:tok=") == true
expect nvme_filesystem_lease_reason(lease, NvmeFilesystemConsumer.Fat32) == "ready"
```

</details>

#### keeps invalid namespace geometry rejected before FAT32 can mount

- keeps invalid namespace geometry rejected before FAT32 can mount


<details>
<summary>Executable SSpec</summary>

Runnable source: 5 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("keeps invalid namespace geometry rejected before FAT32 can mount")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
val lease = vfs_boot_nvme_system_fat32_lease(0u32, 65536u64, _tokenized_boot_grant())
expect nvme_filesystem_lease_reason(lease, NvmeFilesystemConsumer.Fat32) == "nvme-fs-namespace-invalid-lba-size"
```

</details>

#### builds production boot FAT32 leases only from ready transfer evidence

- builds production boot FAT32 leases only from ready transfer evidence


<details>
<summary>Executable SSpec</summary>

Runnable source: 42 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("builds production boot FAT32 leases only from ready transfer evidence")
val ready = nvme_transfer_evidence(
    "simple-driver",
    true,
    true,
    true,
    true,
    true,
    true,
    true,
    true,
    true,
    "system-driver",
    "kernel-owned-resource",
    "system-kernel-namespace",
    true,
    true
)
val missing_write = nvme_transfer_evidence(
    "simple-driver",
    true,
    true,
    true,
    true,
    true,
    true,
    false,
    true,
    true,
    "system-driver",
    "kernel-owned-resource",
    "system-kernel-namespace",
    true,
    true
)

val lease = vfs_boot_nvme_system_fat32_lease_from_transfer_evidence(512u32, 65536u64, ready).unwrap()
val rejected = vfs_boot_nvme_system_fat32_lease_from_transfer_evidence(512u32, 65536u64, missing_write)
expect lease.provider == "simple-driver"
expect nvme_filesystem_lease_reason(lease, NvmeFilesystemConsumer.Fat32) == "ready"
expect rejected.unwrap_err() == "nvme-fs-transfer-not-ready:missing-nvme-sector-write"
```

</details>

#### uses hardware transfer evidence before mounting the pure Simple boot FAT32 lease

- uses hardware transfer evidence before mounting the pure Simple boot FAT32 lease
   - Expected: source contains `val transfer_evidence_result = g_nvme.transfer_evidence_from_reversible_probe(`
   - Expected: source contains `vfs_boot_nvme_system_fat32_lease_from_transfer_evidence(`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("uses hardware transfer evidence before mounting the pure Simple boot FAT32 lease")
val source = read_file("src/os/services/vfs/vfs_boot_init.spl")
expect(source.contains("val transfer_evidence_result = g_nvme.transfer_evidence_from_reversible_probe(")).to_equal(true)
expect source.contains("\"system-driver\"") == true
expect source.contains("\"kernel-owned-resource\"") == true
expect source.contains("\"system-kernel-namespace\"") == true
expect(source.contains("vfs_boot_nvme_system_fat32_lease_from_transfer_evidence(")).to_equal(true)
expect source.contains("g_vfs_nvme_active_leases = [lease]") == true
expect source.contains("g_vfs_nvme_direct_adapters = [g_adapter]") == true
expect source.contains("pure-Simple NVMe evidence lease failed") == true
```

</details>

#### records boot NVMe leases and rejects later user assignment of the same namespace

- records boot NVMe leases and rejects later user assignment of the same namespace


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("records boot NVMe leases and rejects later user assignment of the same namespace")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
vfs_boot_nvme_reset_active_leases_for_test()
val system_lease = vfs_boot_nvme_system_fat32_lease(512u32, 65536u64, _tokenized_boot_grant())
val conflicting_user = _user_lease_for_boot_nsid(1u32)
val other_user = _user_lease_for_boot_nsid(12u32)

expect vfs_boot_nvme_record_active_lease_for_test(system_lease) == "ready"
expect vfs_boot_nvme_active_lease_count_for_test() == 1u64
expect vfs_boot_nvme_active_lease_acceptance_reason(conflicting_user) == "nvme-fs-namespace-mode-conflict:system:user-assigned"
expect vfs_boot_nvme_active_lease_acceptance_reason(other_user) == "ready"
vfs_boot_nvme_reset_active_leases_for_test()
```

</details>

#### assigns user namespaces through the active VFS lease registry

- assigns user namespaces through the active VFS lease registry


<details>
<summary>Executable SSpec</summary>

Runnable source: 15 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("assigns user namespaces through the active VFS lease registry")
vfs_boot_nvme_reset_active_leases_for_test()
val boot_lease = vfs_boot_nvme_system_fat32_lease(512u32, 65536u64, _tokenized_boot_grant())
val grant = _tokenized_grant_for_owner(42u64)
val evidence = _user_evidence(grant)

expect vfs_boot_nvme_record_active_lease_for_test(boot_lease) == "ready"
val conflict = vfs_boot_nvme_assign_user_namespace_lease(evidence, grant, 0u32, 1u32, 512u32, 65536u64, 0u64, 1024u64, SIMPLEOS_NVME_FIRST_DATA_QUEUE_ID, 42u64, 64u16, NvmeFilesystemConsumer.Nvfs)
val assigned = vfs_boot_nvme_assign_user_namespace_lease(evidence, grant, 0u32, 12u32, 512u32, 65536u64, 0u64, 1024u64, SIMPLEOS_NVME_FIRST_DATA_QUEUE_ID, 42u64, 64u16, NvmeFilesystemConsumer.Dbfs)

expect conflict.unwrap_err() == "nvme-fs-namespace-mode-conflict:system:user-assigned"
expect assigned.is_ok() == true
expect vfs_boot_nvme_active_lease_count_for_test() == 2u64
vfs_boot_nvme_reset_active_leases_for_test()
```

</details>

#### creates user namespace driver instances only after active-lease admission

- creates user namespace driver instances only after active-lease admission


<details>
<summary>Executable SSpec</summary>

Runnable source: 16 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("creates user namespace driver instances only after active-lease admission")
vfs_boot_nvme_reset_active_leases_for_test()
val boot_lease = vfs_boot_nvme_system_fat32_lease(512u32, 65536u64, _tokenized_boot_grant())
val grant = _tokenized_grant_for_owner(42u64)
val evidence = _user_evidence(grant)

expect vfs_boot_nvme_record_active_lease_for_test(boot_lease) == "ready"
val conflict = vfs_boot_nvme_assign_user_namespace_driver_instance("user-fat", MemBlockDevice.new(1024u64, 512u32), evidence, grant, 0u32, 1u32, 512u32, 65536u64, 0u64, 1024u64, SIMPLEOS_NVME_FIRST_DATA_QUEUE_ID, 42u64, 64u16, NvmeFilesystemConsumer.Fat32)
expect conflict.unwrap_err() == "nvme-fs-namespace-mode-conflict:system:user-assigned"
expect vfs_boot_nvme_active_lease_count_for_test() == 1u64

val mounted = vfs_boot_nvme_assign_user_namespace_driver_instance("user-fat", MemBlockDevice.new(1024u64, 512u32), evidence, grant, 0u32, 13u32, 512u32, 65536u64, 0u64, 1024u64, SIMPLEOS_NVME_FIRST_DATA_QUEUE_ID, 42u64, 64u16, NvmeFilesystemConsumer.Fat32)
expect mounted.is_ok() == true
expect vfs_boot_nvme_active_lease_count_for_test() == 2u64
vfs_boot_nvme_reset_active_leases_for_test()
```

</details>

#### routes production user namespace mounts through the pure Simple NVMe block adapter

- routes production user namespace mounts through the pure Simple NVMe block adapter
   - Expected: source contains `fn vfs_boot_nvme_assign_user_namespace_hardware_driver_instance(`


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("routes production user namespace mounts through the pure Simple NVMe block adapter")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
val source = read_file("src/os/services/vfs/vfs_boot_init.spl")
expect(source.contains("fn vfs_boot_nvme_assign_user_namespace_hardware_driver_instance(")).to_equal(true)
expect source.contains("val driver_ready = vfs_boot_nvme_shared_driver_ready_for_user_assignment()") == true
expect source.contains("return Err(driver_ready)") == true
expect source.contains("val previous_nsid = g_nvme.get_namespace_id()") == true
expect source.contains("val identify = g_nvme.identify_namespace_id(lease.namespace_identity.nsid)") == true
expect source.contains("\"nvme-user-assign-namespace-identify-failed:\"") == true
expect source.contains("val queue_ready = g_nvme.ensure_user_data_queue(lease.queue.queue_id, lease.queue.max_depth)") == true
expect source.contains("\"nvme-user-assign-queue-create-failed:\"") == true
expect source.contains("val restore_identify = g_nvme.identify_namespace_id(previous_nsid)") == true
expect source.contains("\"nvme-user-assign-namespace-restore-failed:\"") == true
expect source.contains("NvmeBlockAdapter.for_filesystem_lease(g_nvme, lease, consumer)") == true
expect source.contains("val bounce = adapter.init_bounce_buffer()") == true
expect source.contains("vfs_nvme_driver_instance_for_lease(name, adapter, lease, consumer)") == true
expect source.contains("g_vfs_nvme_active_leases = g_vfs_nvme_active_leases.push(lease)") == true
expect source.contains("g_vfs_nvme_direct_adapters = g_vfs_nvme_direct_adapters.push(adapter)") == true
```

</details>

#### keeps concrete NVMe adapters addressable for admitted lease direct I/O

- keeps concrete NVMe adapters addressable for admitted lease direct I/O


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("keeps concrete NVMe adapters addressable for admitted lease direct I/O")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
vfs_boot_nvme_reset_active_leases_for_test()
val source = read_file("src/os/services/vfs/vfs_boot_init.spl")

expect source.contains("fn vfs_boot_nvme_submit_active_lease_direct_4k(lease: NvmeFilesystemLease, req: DirectIoRequest) -> Result<DirectIoResult, FsError>") == true
expect source.contains("return g_vfs_nvme_direct_adapters[idx].submit_direct_4k_shared_dma(req)") == true
expect source.contains("return Err(FsError.StaleHandle)") == true
vfs_boot_nvme_reset_active_leases_for_test()
```

</details>

#### checks shared driver readiness before constructing hardware user namespace adapters

- checks shared driver readiness before constructing hardware user namespace adapters


<details>
<summary>Executable SSpec</summary>

Runnable source: 12 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("checks shared driver readiness before constructing hardware user namespace adapters")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
vfs_boot_nvme_reset_active_leases_for_test()
val source = read_file("src/os/services/vfs/vfs_boot_init.spl")
val guard_pos = source.index_of("val driver_ready = vfs_boot_nvme_shared_driver_ready_for_user_assignment()")
val adapter_pos = source.index_of("NvmeBlockAdapter.for_filesystem_lease(g_nvme, lease, consumer)")

expect vfs_boot_nvme_shared_driver_ready_for_user_assignment() == "nvme-user-assign-boot-storage-not-ready:not-mounted"
expect guard_pos >= 0 == true
expect adapter_pos > guard_pos == true
expect vfs_boot_nvme_active_lease_count_for_test() == 0u64
```

</details>

#### releases active user namespace leases so reassignment can proceed

- releases active user namespace leases so reassignment can proceed


<details>
<summary>Executable SSpec</summary>

Runnable source: 17 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("releases active user namespace leases so reassignment can proceed")
vfs_boot_nvme_reset_active_leases_for_test()
val grant = _tokenized_grant_for_owner(42u64)
val evidence = _user_evidence(grant)
val assigned = vfs_boot_nvme_assign_user_namespace_lease(evidence, grant, 0u32, 14u32, 512u32, 65536u64, 0u64, 1024u64, SIMPLEOS_NVME_FIRST_DATA_QUEUE_ID, 42u64, 64u16, NvmeFilesystemConsumer.Nvfs).unwrap()
val missing = _user_lease_for_boot_nsid(15u32)

expect vfs_boot_nvme_active_lease_count_for_test() == 1u64
expect vfs_boot_nvme_release_active_lease(missing) == "nvme-fs-active-lease-not-found"
expect vfs_boot_nvme_active_lease_count_for_test() == 1u64
expect vfs_boot_nvme_release_active_lease(assigned) == "ready"
expect vfs_boot_nvme_active_lease_count_for_test() == 0u64
val reassigned = vfs_boot_nvme_assign_user_namespace_lease(evidence, grant, 0u32, 14u32, 512u32, 65536u64, 0u64, 1024u64, SIMPLEOS_NVME_FIRST_DATA_QUEUE_ID, 42u64, 64u16, NvmeFilesystemConsumer.Nvfs)
expect reassigned.is_ok() == true
expect vfs_boot_nvme_active_lease_count_for_test() == 1u64
vfs_boot_nvme_reset_active_leases_for_test()
```

</details>

#### accepts only pure Simple NVMe boot storage as production-ready

- accepts only pure Simple NVMe boot storage as production-ready


<details>
<summary>Executable SSpec</summary>

Runnable source: 10 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("accepts only pure Simple NVMe boot storage as production-ready")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
expect vfs_boot_storage_acceptance_reason(false, "not-mounted", false) == "not-mounted"
expect vfs_boot_storage_acceptance_reason(true, "c-boot-bridge", false) == "vfs-boot-storage-not-pure-simple:c-boot-bridge"
expect vfs_boot_storage_acceptance_reason(true, "virtio-blk", true) == "vfs-boot-storage-not-nvme-simple-driver:virtio-blk"
expect vfs_boot_storage_acceptance_reason(true, "simple-driver", true) == "ready"
expect vfs_boot_storage_production_ready(true, "c-boot-bridge", false) == false
expect vfs_boot_storage_production_ready(true, "virtio-blk", true) == false
expect vfs_boot_storage_production_ready(true, "simple-driver", true) == true
```

</details>

#### exports fail-closed production readiness through the VFS public surface

- exports fail-closed production readiness through the VFS public surface


<details>
<summary>Executable SSpec</summary>

Runnable source: 4 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# @req REQ-SSPEC-OS
step("exports fail-closed production readiness through the VFS public surface")
# evidence(protocol_json): asserted result fields below are the complete typed oracle
expect vfs_boot_storage_acceptance_ready() == false
```

</details>

## At a Glance

| Field | Value |
|-------|-------|
| Category | Hardware & OS |
| Status | Active |
| Source | `test/01_unit/os/services/vfs/.spipe_wrapped_entry_vfs_boot_nvme_lease_spec.spl` |
| Updated | 2026-08-26 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests covering VFS boot NVMe lease contract.
- VFS boot NVMe lease contract

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 13 |
| Active scenarios | 13 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>

<!-- sspec-maintain:traceability:start -->
## Traceability

Requirements covered by the scenarios in this manual:

- `REQ-SSPEC-OS`
<!-- sspec-maintain:traceability:end -->

<!-- sspec-maintain:provenance:start -->
## Generation history

- Canonical SPipe generation for source `5fef23766eb5a99d903694ff85d21c423dcef074bd5f49d96b42be2197fa48a7`; maintenance tool `1`, rules `ssdoc-rules/1`.

Source SHA-256: `5fef23766eb5a99d903694ff85d21c423dcef074bd5f49d96b42be2197fa48a7`.
<!-- sspec-maintain:provenance:end -->

<!-- sspec-maintain:scorecard:start -->
## SSpec documentization scorecard

Source SHA-256: `5fef23766eb5a99d903694ff85d21c423dcef074bd5f49d96b42be2197fa48a7`  
Analyzer: `1`; rules: `ssdoc-rules/1`  
Raw score: **85/100**; effective score: **49/100**; blockers: **1**.

SSpec documentization score: 49/100
source: test/01_unit/os/services/vfs/.spipe_wrapped_entry_vfs_boot_nvme_lease_spec.spl
mirror: doc/06_spec/01_unit/os/services/vfs/.spipe_wrapped_entry_vfs_boot_nvme_lease_spec.md (current)
findings: 4 blockers: 1
  narrative=100 structure=100 oracle=50
  traceability=100 evidence=100 coverage=100 maintainability=55
  cache=not-used suppressed=0
  lint-owned related rules=SPIPE001,SPIPE002,SPIPE003,SPIPE004,SPIPE005,SPIPE006,SPIPE007
  raw=85; blocker cap makes effective=49
doc/06_spec/01_unit/os/services/vfs/.spipe_wrapped_entry_vfs_boot_nvme_lease_spec.md:1:1: advice SSDOC-MNT-005 [maintainability] (-10): generated manual lacks verification or troubleshooting guidance
  why: Operators need recovery and evidence interpretation guidance.
  improve: Author verification and recovery facts in SSpec and regenerate.
doc/06_spec/01_unit/os/services/vfs/.spipe_wrapped_entry_vfs_boot_nvme_lease_spec.md:1:1: warning SSDOC-MNT-008 [maintainability] (-20): manual is missing: purpose, audience, scope, assumptions/preconditions, primary workflow, unsupported/limitations, recovery/troubleshooting
  why: A test dump is not a complete professional specification manual.
  improve: Author the missing facts in SSpec and regenerate through canonical SPipe docgen.
test/01_unit/os/services/vfs/.spipe_wrapped_entry_vfs_boot_nvme_lease_spec.spl:1:1: advice SSDOC-MNT-001 [maintainability] (-15): multiple scenarios form a flat, unfolded presentation
  why: Long flat dumps obscure the primary workflow.
  improve: Group secondary detail and keep the primary workflow visible.
test/01_unit/os/services/vfs/.spipe_wrapped_entry_vfs_boot_nvme_lease_spec.spl:1:1: blocker SSDOC-ORA-002 [oracle] (-50): scenario relies on source-text inspection as system evidence
  why: Source presence or self-created arithmetic does not demonstrate production behavior.
  improve: Observe runtime behavior or a stable generated artifact instead.
<!-- sspec-maintain:scorecard:end -->
