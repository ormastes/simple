# Trait Alias Forwarding

> Tests the delegation pattern generated by the desugar pass from `alias TraitName = field_name` inside class bodies. Validates immutable and mutable method forwarding to fields, forwarding to multiple different fields, argument forwarding, state preservation across mutable calls, correct return values through forwarding chains, coexistence with own methods, and mixed mutable/immutable forwarding.

<!-- sdn-diagram:id=trait_forwarding_spec.arch -->
<details class="sdn-source">
<summary>SDN source</summary>

```sdn id=trait_forwarding_spec.arch hash=sha256:auto render=ascii
@layout dag
@direction LR

trait_forwarding_spec
```

</details>

<details class="sdn-ascii" open>
<summary>Diagram</summary>

```ascii generated-from=trait_forwarding_spec.arch hash=sha256:auto
# run: simple md-diagram-update
```

</details>
<!-- sdn-diagram:end -->

| Tests | Active | Skipped | Pending |
|-------|--------|---------|--------:|
| 8 | 8 | 0 | 0 |

<details>
<summary>Full Scenario Manual</summary>

# Trait Alias Forwarding

Tests the delegation pattern generated by the desugar pass from `alias TraitName = field_name` inside class bodies. Validates immutable and mutable method forwarding to fields, forwarding to multiple different fields, argument forwarding, state preservation across mutable calls, correct return values through forwarding chains, coexistence with own methods, and mixed mutable/immutable forwarding.

## At a Glance

| Field | Value |
|-------|-------|
| Feature IDs | #TRAIT-001 |
| Category | Language |
| Status | Active |
| Source | `test/03_system/feature/usage/trait_forwarding_spec.spl` |
| Updated | 2026-06-01 |
| Generator | `simple spipe-docgen` (Simple) |

## Overview

Tests the delegation pattern generated by the desugar pass from `alias TraitName
= field_name` inside class bodies. Validates immutable and mutable method
forwarding to fields, forwarding to multiple different fields, argument
forwarding, state preservation across mutable calls, correct return values
through forwarding chains, coexistence with own methods, and mixed
mutable/immutable forwarding.

## Syntax

```simple
class Wrapper:
inner: Inner
alias Readable = inner
# desugar generates: fn get_data(): self.inner.get_data()
```
Trait Alias Forwarding Tests (Phase 3)

Tests verify the delegation pattern that desugar generates from
`alias TraitName = field_name` inside class bodies.

Since the desugar pass produces forwarding methods like:
  fn method(): self.field.method()
  me mutate(x): self.field.mutate(x)
these tests exercise that generated delegation pattern directly.

## Scenarios

### Trait Alias Forwarding (Phase 3)

#### delegates immutable method to field

1. fn get data
2. fn get data
3. self inner get data
   - Expected: w.get_data() equals `42`


<details>
<summary>Executable SSpec</summary>

Runnable source: 13 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
class Inner:
    data: i64
    fn get_data() -> i64:
        self.data

# This is what desugar generates from "alias Readable = inner"
class Wrapper:
    inner: Inner
    fn get_data() -> i64:
        self.inner.get_data()

val w = Wrapper(inner: Inner(data: 42))
expect(w.get_data()).to_equal(42)
```

</details>

#### delegates mutable method to field

1. me add
2. self items add
3. fn size
4. self items size
5. var c = Container
6. c add
   - Expected: c.size() equals `1`


<details>
<summary>Executable SSpec</summary>

Runnable source: 11 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# Desugar generates: me add(item): self.items.add(item)
class Container:
    items: MutableList
    me add(item: i64):
        self.items.add(item)
    fn size() -> i64:
        self.items.size()

var c = Container(items: MutableList(length: 0, last_added: 0))
c.add(99)
expect(c.size()).to_equal(1)
```

</details>

#### forwards to multiple different fields

1. me record
2. self log log
3. fn entry count
4. self log entry count
5. fn style text
6. self fmt format
7. var app = App
8. app record
   - Expected: app.entry_count() equals `1`
   - Expected: styled equals `bold: test`


<details>
<summary>Executable SSpec</summary>

Runnable source: 16 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# Desugar generates forwarding for two separate fields
class App:
    log: Logger
    fmt: Formatter
    me record(msg: text):
        self.log.log(msg)
    fn entry_count() -> i64:
        self.log.entry_count()
    fn style_text(value: text) -> text:
        self.fmt.format(value)

var app = App(log: Logger(entries: 0, prefix: "APP"), fmt: Formatter(style: "bold"))
app.record("hello")
expect(app.entry_count()).to_equal(1)
val styled = app.style_text("test")
expect(styled).to_equal("bold: test")
```

</details>

#### forwards methods that take arguments

1. fn add
2. fn multiply
3. fn add
4. self calc add
5. fn multiply
6. self calc multiply
   - Expected: w.add(3, 4) equals `17`
   - Expected: w.multiply(5, 6) equals `30`


<details>
<summary>Executable SSpec</summary>

Runnable source: 19 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
class Calculator:
    x: i64
    y: i64
    fn add(a: i64, b: i64) -> i64:
        a + b + self.x
    fn multiply(a: i64, b: i64) -> i64:
        a * b

# Desugar generates: fn add(a, b): self.calc.add(a, b)
class MathWrapper:
    calc: Calculator
    fn add(a: i64, b: i64) -> i64:
        self.calc.add(a, b)
    fn multiply(a: i64, b: i64) -> i64:
        self.calc.multiply(a, b)

val w = MathWrapper(calc: Calculator(x: 10, y: 0))
expect(w.add(3, 4)).to_equal(17)
expect(w.multiply(5, 6)).to_equal(30)
```

</details>

#### preserves state across mutable forwarding calls

1. me increment
2. me decrement
3. fn value
4. me increment
5. self counter increment
6. me decrement
7. self counter decrement
8. fn value
9. self counter value
10. var tc = TrackedCounter
11. tc increment
12. tc increment
13. tc increment
14. tc decrement
   - Expected: tc.value() equals `2`


<details>
<summary>Executable SSpec</summary>

Runnable source: 25 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
# Forwarded me methods must mutate the actual field
class Counter:
    n: i64
    me increment():
        self.n = self.n + 1
    me decrement():
        self.n = self.n - 1
    fn value() -> i64:
        self.n

class TrackedCounter:
    counter: Counter
    me increment():
        self.counter.increment()
    me decrement():
        self.counter.decrement()
    fn value() -> i64:
        self.counter.value()

var tc = TrackedCounter(counter: Counter(n: 0))
tc.increment()
tc.increment()
tc.increment()
tc.decrement()
expect(tc.value()).to_equal(2)
```

</details>

#### returns correct values through forwarding chain

1. fn get name
2. fn get version
3. fn describe
4. fn get name
5. self store get name
6. fn get version
7. self store get version
8. fn describe
9. self store describe
   - Expected: svc.get_name() equals `myapp`
   - Expected: svc.get_version() equals `3`
   - Expected: svc.describe() equals `myapp v3`


<details>
<summary>Executable SSpec</summary>

Runnable source: 24 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
class DataStore:
    name: text
    version: i64
    fn get_name() -> text:
        self.name
    fn get_version() -> i64:
        self.version
    fn describe() -> text:
        "{self.name} v{self.version}"

# Desugar generates three fn forwarding methods
class Service:
    store: DataStore
    fn get_name() -> text:
        self.store.get_name()
    fn get_version() -> i64:
        self.store.get_version()
    fn describe() -> text:
        self.store.describe()

val svc = Service(store: DataStore(name: "myapp", version: 3))
expect(svc.get_name()).to_equal("myapp")
expect(svc.get_version()).to_equal(3)
expect(svc.describe()).to_equal("myapp v3")
```

</details>

#### forwarding works alongside own methods

1. fn horsepower
2. fn horsepower
3. self engine horsepower
4. fn summary
5. "{self color} car with {self engine horsepower
   - Expected: car.horsepower() equals `200`
   - Expected: car.color equals `red`
   - Expected: car.summary() equals `red car with 200hp`


<details>
<summary>Executable SSpec</summary>

Runnable source: 18 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
class Engine:
    power: i64
    fn horsepower() -> i64:
        self.power

# Class has its own methods AND forwarded methods
class Car:
    engine: Engine
    color: text
    fn horsepower() -> i64:
        self.engine.horsepower()
    fn summary() -> text:
        "{self.color} car with {self.engine.horsepower()}hp"

val car = Car(engine: Engine(power: 200), color: "red")
expect(car.horsepower()).to_equal(200)
expect(car.color).to_equal("red")
expect(car.summary()).to_equal("red car with 200hp")
```

</details>

#### forwarding with mixed mutable and immutable methods

1. me write
2. fn read
3. fn count
4. me write
5. self buf write
6. fn read
7. self buf read
8. fn count
9. self buf count
10. var s = Stream
11. s write
12. s write
   - Expected: s.read() equals `hello world`
   - Expected: s.count() equals `2`


<details>
<summary>Executable SSpec</summary>

Runnable source: 26 lines folded for reproduction.
Reproduction: this block contains the complete executable scenario source.

```simple
class Buffer:
    content: text
    size: i64
    me write(data: text):
        self.content = self.content + data
        self.size = self.size + 1
    fn read() -> text:
        self.content
    fn count() -> i64:
        self.size

# Desugar generates both fn and me forwarding methods
class Stream:
    buf: Buffer
    me write(data: text):
        self.buf.write(data)
    fn read() -> text:
        self.buf.read()
    fn count() -> i64:
        self.buf.count()

var s = Stream(buf: Buffer(content: "", size: 0))
s.write("hello")
s.write(" world")
expect(s.read()).to_equal("hello world")
expect(s.count()).to_equal(2)
```

</details>

## Scenario Summary

| Metric | Count |
|--------|------:|
| Total scenarios | 8 |
| Active scenarios | 8 |
| Slow scenarios | 0 |
| Skipped scenarios | 0 |
| Pending scenarios | 0 |


</details>
