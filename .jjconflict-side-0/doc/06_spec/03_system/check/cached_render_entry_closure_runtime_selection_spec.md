# Cached Render Entry Closure Runtime Selection

Status: **source contract complete; `TEST_BLOCKED`**.

This operator manual mirrors
`test/03_system/check/cached_render_entry_closure_runtime_selection_spec.spl`.
It is manually synchronized because no admitted pure-Simple full CLI currently
supports `test`, `sspec-maintain`, and `spipe-docgen`. It is not generated PASS
evidence and must be regenerated by SPipe when a qualified runtime is ready.

## Purpose and audience

This manual is for operators resuming TODO686/TODO688. It checks only the
lightweight native-build worker-selection boundary for
`CachedRenderEntryClosureV1`. It does not admit Stage 4, build a carrier, run
the sparse 8K corpus, or prove performance.

## Preconditions

1. Use a provenance-qualified pure-Simple full CLI with `test`,
   `sspec-maintain`, and `spipe-docgen` support.
2. Record the runtime path and SHA-256.
3. Reject Rust seeds, the known-bad unadmitted `release/` artifact, stale
   outputs, and source-only review as runtime evidence.
4. Use a clean output/cache namespace and do not repeat a passing command.

## Operator workflow

### Admit the exact Stage 4 CLI

1. Select the first available configured pure-Simple candidate.
2. Confirm its preflight result is zero.
3. Supply the canonical Rust-seed path as the only candidate and confirm
   selection returns empty with preflight result one.
4. Leave only unavailable `bin/simple` and confirm the resolver fails closed
   rather than spawning an unchecked fallback.

## Scenario narratives

| REQ | Scenario | Required result |
|---|---|---|
| REQ-RENDER-CLI-002 | First available configured candidate | Exact first path; preflight `0` |
| REQ-RENDER-CLI-002 | Canonical Rust seed only | Empty selection; preflight `1` |
| REQ-RENDER-CLI-002 | Unavailable implicit path only | Empty selection; preflight `1` |

All three scenarios use real assertions against the production pure-Simple
selection helpers. No scenario shells out, substitutes a seed, or accepts a
placeholder pass.

## Scorecard

| Dimension | Current result |
|---|---|
| Scenario structure | 3 behavior scenarios with 3 visible `step(...)` calls |
| Assertions | 7 concrete `to_equal` assertions; no placeholder assertion |
| Traceability | All scenarios carry `REQ-RENDER-CLI-002` |
| Source contract | Fail-closed implementation and mirror are statically reviewable |
| Runtime execution | `TEST_BLOCKED`; no qualified full CLI is available |
| Maintenance/docgen | `TEST_BLOCKED`; must run with the same qualified CLI |

## Findings and remediation

- `SSDOC-RUNTIME-001`: runtime execution, deterministic maintenance scoring,
  and generator provenance remain blocked by the missing admitted full CLI.
- Remediation: qualify one source-matched pure-Simple full CLI, record its path,
  SHA-256, stage, and provenance, then run each command below exactly once.
- Do not suppress this blocker or replace it with seed, stale-release, or
  source-only evidence.

## Evidence and provenance

The executable source of record is the mirrored `test/03_system/check/` spec
named above. The setup helper is `setup_cached_render_entry_closure`; its
checker is `check_cached_render_entry_closure`. Current evidence is limited to
static structure, source-contract, traceability, environment-boundary, naming,
conflict, and layout guards. Runtime path/hash/provenance fields intentionally
remain unset until a qualified execution is possible.

## Qualified execution

```sh
<qualified-simple> test test/03_system/check/cached_render_entry_closure_runtime_selection_spec.spl --mode=interpreter
<qualified-simple> sspec-maintain scan test/03_system/check/cached_render_entry_closure_runtime_selection_spec.spl
<qualified-simple> spipe-docgen test/03_system/check/cached_render_entry_closure_runtime_selection_spec.spl --output doc/06_spec --no-index
```

Acceptance requires three examples, zero failures, zero stubs, all seven
maintenance scores reviewed, and byte-current manual regeneration. Until then,
runtime status remains `TEST_BLOCKED`, never PASS.

## Compatibility and limitations

- The policy recognizes canonical Rust-seed paths under
  `src/compiler_rust/target/`; it does not independently authenticate an
  arbitrarily renamed executable.
- Candidate admission and provenance remain the caller's responsibility.
- The remaining `CachedRenderEntryClosureV1` carrier and 8K gates are outside
  this focused criterion.
