"""
# RV64GC Hardware Emulation Module

Full RV64GC CPU emulation: RV64I base + M (multiply/divide) + A (atomics) +
F (single-precision float) + D (double-precision float) + C (compressed) +
Zicsr + Zifencei.

Supports M/S/U privilege modes, Sv39 virtual memory, and multicore SMP.
Compatible with QEMU virt machine memory map.
"""
