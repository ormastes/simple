# Type Checking Stage — Diagnostic
#
# A compiler diagnostic (error, warning, or info).

struct Diagnostic:
    """A compiler diagnostic (error, warning, or info)."""
    severity: text
    message: text
    line: i64
    col: i64
    span_end_line: i64
    span_end_col: i64

