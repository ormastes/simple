# MIR Lowering Stage — MirLowerInputPort
#
# HIR view from the hir_to_mir transform.

struct MirLowerInputPort:
    """HIR view from the hir_to_mir transform."""
    module_name: text
    function_count: i64

