# WS3: Explicit re-exports — no sibling preloading needed.
# Each export use points to the specific child module that defines the symbol.
# Refactored 2026-03-29 as part of Driver API Heavy Path Stabilization.

# Re-exported from bidirectional_checking.spl
export use compiler.types.bidirectional_checking.{TypeInferencer}

# Re-exported from dim_constraints.spl
export use compiler.types.dim_constraints.{
    DimConstraint, DimError, DimErrorKind, DimNote, DimNoteKind,
    DimSolver, DimCheckTiming, WhereClause, DimClause,
    DimCheckMode, RuntimeDimCheck, RuntimeDimCheckKind, DimCheckGenerator,
    format_shape
}

# Re-exported from dim_constraints_types.spl
export use compiler.types.dim_constraints_types.{DimConstraint, DimError, DimNote, DimNoteKind, DimErrorKind}

# Re-exported from type_infer_types.spl
export use compiler.types.type_infer_types.{TypeScheme, Substitution, TypeInferError, InferMode, empty_subst_map, empty_type_env}

# Re-exported from type_layout.spl
export use compiler.types.type_layout.{
    TypeLayout, FieldLayout, LayoutAttr, LayoutKind,
    PlatformAbi, PlatformBit, PlatformPredicate, PlatformVariant,
    PlatformParseResult, PlatformDiagnosticLevel, PlatformLayoutDiagnostic,
    platform_cpu_for_arch, platform_pointer_bit_for_arch,
    platform_predicate_matches_arch, platform_select_variant_for_arch,
    compute_layout, compute_struct_layout, compute_field_offset,
    size_of_type, align_of_type,
    size_of_type_for_arch, align_of_type_for_arch,
    primitive_size_for_arch, primitive_align_for_arch
}
