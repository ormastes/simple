# Host compositor public facade.
# Production winit entry imports host_compositor_core directly so optional
# filesystem/background and alternate-backend dependencies stay out of its
# native entry closure.
use os.compositor.host_compositor_core.*
use os.compositor.host_compositor_bootstrap.*
