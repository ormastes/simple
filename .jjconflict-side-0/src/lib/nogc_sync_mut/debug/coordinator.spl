# Debug Coordinator Facade — nogc_sync_mut family
#
# Re-exports shared debug types from nogc_async_mut.debug.coordinator
# so that sync-family consumers use a same-family import path.
# See doc/03_plan/import_warning_debt_2026-05-13.md for context.

use std.nogc_async_mut.debug.coordinator.{
    DebugBackend, FrameInfo, VarInfo, LocationInfo
}

export use std.nogc_async_mut.debug.coordinator.{
    DebugBackend, FrameInfo, VarInfo, LocationInfo
}
