# std.log.panic — thin re-export of the panic-mode API surface.
#
# Phase 5b consolidated all log code into `src/lib/log.spl`. Phase 4 specs
# import from `std.log.panic.{...}` (the originally drafted submodule path).
# This file re-exports the panic-side symbols from the consolidated facade
# so spec authorship is preserved without touching the implementation.
#
# Architecture spec: .sstack/log-lib-drivers/state.md §3-arch §H (panic path).

use lib.log.*

export log_panic_flush
export panic_mode_active
