# Async ML Integration Layer
#
# Combines async primitives with ML operations for
# training loops, data pipelines, and model evaluation.
#
# Usage:
#   use std.nogc_async_mut.ml.async_training.{train_epoch, evaluate_model, train_loop}
#   use std.nogc_async_mut.ml.data_pipeline.{prefetch_batches, shuffle_indices}

export *

# Re-exported from async_training.spl
export train_epoch, evaluate_model, train_loop

# Re-exported from data_pipeline.spl
export prefetch_batches, shuffle_indices

# Re-exported from mod.spl
export train_epoch, evaluate_model, train_loop
export prefetch_batches, shuffle_indices
