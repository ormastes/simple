"""
# No-GC async signal stubs

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.io.signal_stubs.{
    signal_handler_install,
    atexit_register, signal_dispatch_pending
}
