"""
# No-GC Async Time Operations

Compatibility facade over the no-GC sync time backend.
"""

export use std.nogc_sync_mut.io.time_ops.{
    time_now_unix_micros, current_time_unix, current_time_ms,
    timestamp_year, timestamp_month, timestamp_day,
    timestamp_hour, timestamp_minute, timestamp_second,
    time_now_nanos, time_now_micros, rt_timestamp_now, rt_sleep_ms
}
