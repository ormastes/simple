"""
# No-GC Async Directory Operations

Compatibility facade over the no-GC sync directory backend.
"""

export use std.nogc_sync_mut.io.dir_ops.{
    dir_create, dir_create_all, dir_walk, dir_walk_native,
    dir_remove_all, is_dir, dir_list, dir_remove
}
