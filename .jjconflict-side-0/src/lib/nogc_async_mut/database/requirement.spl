"""
# No-GC Async Requirement Database

Compatibility facade over the no-GC sync database backend.
"""

export use std.nogc_sync_mut.database.requirement.{
    Requirement, RequirementDatabase,
    _empty_requirement, _format_req_id, _serialize_requirements
}
