"""
# No-GC Async Hooks

Compatibility facade over the no-GC sync hooks backend.
"""

export use std.nogc_sync_mut.hooks.mod.{
    HookResult, HookCallback, Hook, HookRegistry,
    create_registry, get_global_registry, register_hook,
    run_hooks, run_hooks_by_priority, hooks_enabled,
    interactive_mode, get_max_priority, get_env
}
