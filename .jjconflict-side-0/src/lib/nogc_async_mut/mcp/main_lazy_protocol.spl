# MCP Lazy Server — Protocol hub
#
# Re-exports all public protocol functions from sub-modules.
# Split into: lazy_protocol_io.spl, lazy_protocol_schemas.spl, lazy_protocol_resources.spl

use nogc_async_mut.mcp.lazy_protocol_io.{
    mcp_read_line,
    mcp_read_chars,
    read_stdin_message,
    write_stdout_message,
    make_init_response
}
use nogc_async_mut.mcp.lazy_protocol_schemas.{
    make_tools_list,
    make_tool_schema
}
use nogc_async_mut.mcp.lazy_protocol_resources.{
    make_result_response,
    make_resource_json,
    build_resources_json,
    handle_resources_list,
    handle_resource_templates_list,
    get_cwd,
    make_resource_content_response,
    handle_resources_read,
    make_prompt_arg,
    make_prompt_json,
    handle_prompts_list,
    make_prompt_response,
    handle_prompts_get,
    handle_completion_req
}
