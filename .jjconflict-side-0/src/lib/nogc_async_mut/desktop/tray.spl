"""
# No-GC async desktop tray

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.desktop.tray.{
    TrayMenuItem, TrayConfig,
    create_tray, update_tray, destroy_tray
}
