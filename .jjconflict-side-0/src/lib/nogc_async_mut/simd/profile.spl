"""
# No-GC async SIMD profile

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.simd.profile.{
    SimdTier,
    detect_profile, profile_name,
    has_sse, has_avx, has_avx2, has_neon, has_rvv, simd_width
}
