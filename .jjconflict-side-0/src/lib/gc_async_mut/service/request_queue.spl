"""
# GC Async Service Request Queue

Compatibility facade over the no-GC sync service request-queue backend.
"""

export use std.nogc_sync_mut.service.request_queue.{
    QueueEntry,
    RequestQueue,
    request_queue_new,
    enqueue,
    dequeue,
    queue_len,
    queue_is_empty,
    queue_high_len,
    queue_low_len,
    queue_drain
}
