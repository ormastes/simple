# @gc — module uses GC runtime
#
# entrypoints/openai — mirrors vllm/entrypoints/openai/
#
# Phase A6 scope (doc/05_design/ml/slang/slang_master_plan.md):
#   - protocol.spl       : request parsing + OpenAI JSON envelopes
#   - serving_models.spl : on-disk model discovery via the A1 loader
#   - serving_chat.spl   : /v1/chat/completions — announced stub, never
#                          fabricates a completion (no engine exists yet)
#   - api_server.spl     : transport-free method/path dispatcher

export ApiResponse, ChatRequest
export json_string_field, json_int_field, json_bool_field_is_true, json_array_object_count
export parse_chat_request, openai_error_response, openai_models_response
export SlangModelEntry, read_model_entry, discover_models, model_ids, find_model
export slang_implemented_phase, slang_missing_inference_phases
export model_not_runnable_response, model_not_found_response, serve_chat_completions
export serve_models, slang_api_dispatch
export engine_refusal_response
