# HTTP compatibility facade
#
# HTTP transport is synchronous in this historical API. Re-export the one
# canonical typed owner at compile time instead of duplicating its SFFI surface.

export use std.nogc_sync_mut.net.http.{HttpMethod, HttpRequest, HttpResponse,
    HttpClient, Url, UrlBuilder, get, post, download}
