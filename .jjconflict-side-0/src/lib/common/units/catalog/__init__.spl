# Migration shim — src/lib/common/units/catalog/
#
# @deprecated("catalog SDN files remain here as the authoritative source for Track A registry; will relocate alongside unit.simple-lang in 0.11.0")
#
# world_units_v1.sdn and units_schema.sdn are SDN data files (not Simple modules),
# so there is nothing to re-export; readers parse the SDN directly.
