# Common Markdown type aliases and records.

struct MarkdownDocument:
    source: text
    blocks: i64

fn markdown_document(source: text, blocks: i64) -> MarkdownDocument:
    MarkdownDocument(source: source, blocks: blocks)
