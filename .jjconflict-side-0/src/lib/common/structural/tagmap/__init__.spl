# ============================================================================
# common/structural/tagmap — FROZEN TagSchema contract (ID-TAG lane)
#
# Architecture §5 (typed TagMap framework), §26 (contract freeze),
# §27 (ID-TAG owns structural/tagmap). Contract document:
# doc/05_design/platform/structural_compute/identity_tagmap_contract_v1.md
# ============================================================================

export use std.common.structural.tagmap.tag_schema.{
    TAGMAP_SCHEMA_VERSION,
    TAG_AUTHORITY_MAX,
    TAG_CARDINALITY_MAX,
    TAG_KEY_LEN,
    TAG_LIFETIME_MAX,
    TAG_MERGE_POLICY_MAX,
    TAG_VALUE_TYPE_MAX,
    TagAuthority,
    TagCardinality,
    TagKey,
    TagLifetime,
    TagMergePolicy,
    TagSchema,
    TagValue,
    TagValueType,
    tag_authority_from_u8,
    tag_authority_to_u8,
    tag_authority_valid,
    tag_cardinality_from_u8,
    tag_cardinality_to_u8,
    tag_cardinality_valid,
    tag_key,
    tag_key_equal,
    tag_lifetime_from_u8,
    tag_lifetime_to_u8,
    tag_lifetime_valid,
    tag_merge_policy_from_u8,
    tag_merge_policy_to_u8,
    tag_merge_policy_valid,
    tag_schema,
    tag_schema_equal,
    tag_small_set_count,
    tag_small_set_offset,
    tag_small_set_pack,
    tag_value_type_from_u8,
    tag_value_type_to_u8,
    tag_value_type_valid
}

export use std.common.structural.tagmap.tag_codec.{
    TagKeyResult,
    TagSchemaResult,
    TagValueResult,
    decode_tag_schema,
    decode_tag_value,
    encode_tag_schema,
    encode_tag_value,
    tag_key_put,
    tag_key_read,
    tag_value_payload_len,
    tag_value_put,
    tag_value_read,
    tagmap_empty_value,
    tagmap_magic_schema,
    tagmap_magic_value,
    tagmap_zero_anchor_bytes
}
