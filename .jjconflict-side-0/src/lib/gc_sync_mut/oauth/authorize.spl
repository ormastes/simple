@allow(star_import) # reason: compatibility facade, re-exports backing family symbols
# Compatibility facade: std.gc_sync_mut.oauth.authorize -> std.gc_async_mut.oauth.authorize
export use std.gc_async_mut.oauth.authorize.*
