@allow(star_import) # reason: compatibility facade, re-exports backing family symbols
# Compatibility facade: std.gc_sync_mut.tcp.connect -> std.gc_async_mut.tcp.connect
export use std.gc_async_mut.tcp.connect.*
