# Rich Editor Test File

fn main()
    let x = 42
    let result = m{ frac(1, 2) + pi }

    # Math block with Greek letters
    let formula = m{ alpha^2 + beta^2 = gamma^2 }

    # Loss function
    let cost = loss{ sum(i, 1..n) (y[i] - hat(y)[i])^2 }

    # Simple math
    let simple = m{ x + y }

    # Nograd block
    let det_val = nograd{ det(A) }

    # Image embedding test
    let fig = img{"test-image.png"}

    return result
