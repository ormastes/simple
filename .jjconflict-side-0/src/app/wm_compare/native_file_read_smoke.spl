use std.io_runtime.{file_read}

fn main() -> i64:
    val content = file_read("test/fixtures/html_compat/24_flex_wrap_reverse_basic.html")
    print "len=" + content.len().to_string()
    if content.len() <= 0:
        print "status=fail"
        return 1
    print "status=pass"
    0
