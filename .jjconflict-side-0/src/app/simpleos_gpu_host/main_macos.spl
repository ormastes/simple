"""Metal-only macOS entry for the shared SimpleOS GPU daemon runner."""

use app.simpleos_gpu_host.daemon_runner.{simpleos_gpu_host_run}
use app.simpleos_gpu_host.platform_macos.{SimpleOsGpuHostMacPlatform}

fn main() -> i64:
    simpleos_gpu_host_run(SimpleOsGpuHostMacPlatform.create())
