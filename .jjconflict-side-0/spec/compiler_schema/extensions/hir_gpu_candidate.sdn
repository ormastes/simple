# Admission fixture (hardening plan §13.1): a complete, statically linked
# candidate that arrives AFTER the universe was frozen. It provides the whole
# §13.2 operation contract and collides with nothing, so it is admitted.
extension:
  owner_enum: compiler.hir.HirExprKind
  constructor: gpu.Launch
  closure: complete
  payload_schema_hash: "7c4419ab0d5e2f83"
  module_abi_hash: "1e6d3b90fa7c4851"
  required_core_abi_hash: "core-abi-v1"
  provider_module: "hir_gpu"
  local_ordinal: 1
  schema_abi_version: 2

  provides:
    verify: gpu.verify_launch
    type_check: gpu.type_launch
    effects: gpu.effects_launch
    visit_children: gpu.visit_launch
    lower_mir: gpu.lower_launch
    print: gpu.print_launch
    hash: gpu.hash_launch
    serialize: gpu.serialize_launch

module:
  id: hir_gpu
  linkage: static_link
  closure: complete
  activation: startup

  extends:
    - enum: compiler.hir.HirExprKind
      constructor: gpu.Launch
      schema_abi: 2

  provides:
    - HirVerify
    - HirEffects
    - HirVisit
    - HirLowerMir
    - HirPrint
    - HirSerialize
