name: Windows Tests

on:
  push:
    branches: [ main, master, develop ]
    paths:
      - 'src/compiler_rust/**'
      - 'src/runtime/**'
      - 'bin/release/**'
      - '.github/workflows/windows-tests.yml'
  pull_request:
    branches: [ main, master, develop ]
    paths:
      - 'src/compiler_rust/**'
      - 'src/runtime/**'
      - 'bin/release/**'
  workflow_dispatch:
    inputs:
      run_slow_tests:
        description: 'Run slow tests'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================================================
  # Linux Validation (verify bootstrap works)
  # ============================================================================
  linux-validation:
    name: Linux Validation
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # `bin/simple` is NOT tracked in git -- it is a symlink created locally by
    # `scripts/setup/setup.shs`. A fresh CI checkout therefore never has it, so
    # the old `chmod +x bin/simple` step failed 100% of the time on every
    # branch. Build the bootstrap seed and deploy the symlink instead; the
    # steps below genuinely need a working binary, so guarding/removing the
    # chmod would only have made this job pass without testing anything.
    - name: Cache cargo + bootstrap target
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          src/compiler_rust/target
        key: windows-tests-bootstrap-${{ runner.os }}-${{ hashFiles('src/compiler_rust/Cargo.lock') }}
        restore-keys: |
          windows-tests-bootstrap-${{ runner.os }}-

    - name: Build and deploy bin/simple (bootstrap seed)
      run: |
        cd src/compiler_rust
        cargo build --profile bootstrap -p simple-driver
        test -x target/bootstrap/simple
        cd "$GITHUB_WORKSPACE"
        mkdir -p bin
        ln -sfn "$(pwd)/src/compiler_rust/target/bootstrap/simple" bin/simple
        bin/simple --version
        echo "✓ Bootstrap runtime ready"

    - name: Test Simple platform detection
      run: |
        export PATH="$PWD/bin:$PWD/bin/release:$PATH"
        DETECTED=$(simple test/ci/platform_linux.spl)
        echo "Detected is_linux: $DETECTED"
        if [[ "$DETECTED" == *"true"* ]]; then
          echo "✅ Platform detection correct"
        else
          echo "❌ Expected is_linux() == true"
          exit 1
        fi

    - name: Test shell execution
      run: |
        export PATH="$PWD/bin:$PWD/bin/release:$PATH"
        OUTPUT=$(simple test/ci/shell_exec.spl)
        echo "Shell output: '$OUTPUT'"
        if [[ "$OUTPUT" == *"test"* ]]; then
          echo "✅ Shell execution works"
        else
          echo "❌ Shell execution failed"
          exit 1
        fi

    - name: Run Simple tests (quick subset)
      run: |
        export PATH="$PWD/bin:$PWD/bin/release:$PATH"
        bin/simple test test/unit/std/
      timeout-minutes: 2
      continue-on-error: true

  # ============================================================================
  # Windows x64 Tests (Native)
  # NOTE: Currently skipped - no native Windows binary available.
  #       All tests use continue-on-error since bootstrap is Linux ELF.
  # ============================================================================
  windows-x64:
    name: Windows x64 Tests
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Verify bootstrap runtime exists
      shell: bash
      run: |
        if [ -f "bin/release/simple" ]; then
          echo "✓ Bootstrap runtime found (Linux ELF - cannot execute on Windows)"
          echo "⚠ Native Windows binary not yet available"
          echo "  Windows-specific tests will be skipped"
        else
          echo "✗ No bootstrap runtime at bin/release/simple"
        fi

    - name: Windows environment info
      run: |
        Write-Host "OS: $env:OS"
        Write-Host "Architecture: $env:PROCESSOR_ARCHITECTURE"
        Write-Host "Note: Windows tests require native binary (not yet available)"
      shell: pwsh

  # ============================================================================
  # Windows ARM64 Cross-Compilation
  # ============================================================================
  windows-arm64:
    name: Windows ARM64 Cross-Compile
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Verify bootstrap runtime
      shell: bash
      run: |
        if [ -f "bin/release/simple" ]; then
          echo "✓ Bootstrap runtime found (Linux ELF)"
          echo "⚠ Cross-compilation requires native Windows binary (not yet available)"
        else
          echo "✗ No bootstrap runtime"
        fi

  # ============================================================================
  # Linker Integration Tests (Linux only - has working binary)
  # ============================================================================
  linker-tests:
    name: Linker Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # See the note in `linux-validation`: `bin/simple` is untracked and cannot
    # exist on a fresh checkout, so the previous `chmod +x bin/simple` here
    # failed unconditionally.
    - name: Cache cargo + bootstrap target
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          src/compiler_rust/target
        key: windows-tests-bootstrap-${{ runner.os }}-${{ hashFiles('src/compiler_rust/Cargo.lock') }}
        restore-keys: |
          windows-tests-bootstrap-${{ runner.os }}-

    - name: Build and deploy bin/simple (bootstrap seed)
      run: |
        cd src/compiler_rust
        cargo build --profile bootstrap -p simple-driver
        test -x target/bootstrap/simple
        cd "$GITHUB_WORKSPACE"
        mkdir -p bin
        ln -sfn "$(pwd)/src/compiler_rust/target/bootstrap/simple" bin/simple
        bin/simple --version
        echo "✓ Bootstrap runtime ready"

    - name: Install mold
      run: |
        wget -q https://github.com/rui314/mold/releases/download/v2.4.0/mold-2.4.0-x86_64-linux.tar.gz
        tar xzf mold-2.4.0-x86_64-linux.tar.gz
        sudo cp mold-2.4.0-x86_64-linux/bin/mold /usr/local/bin/
        mold --version
      continue-on-error: true

    - name: Install lld
      run: |
        sudo apt-get update
        sudo apt-get install -y lld
        lld --version
      continue-on-error: true

    - name: Test linker auto-detection
      run: |
        which ld && echo "✅ ld found"
        which mold && echo "✅ mold found" || echo "⚠ mold not found"
        which lld && echo "✅ lld found" || echo "⚠ lld not found"
      continue-on-error: true

    - name: Build test binary
      run: |
        export PATH="$PWD/bin:$PWD/bin/release:$PATH"
        if [ -f examples/hello.spl ]; then
          simple build examples/hello.spl -o hello
          ./hello
          echo "✅ Build and run successful"
        else
          echo "⚠ hello.spl not found"
        fi
      continue-on-error: true

  # ============================================================================
  # Summary Job
  # ============================================================================
  summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [linux-validation, windows-x64, windows-arm64, linker-tests]
    if: always()

    steps:
    - name: Check results
      run: |
        echo "Linux validation: ${{ needs.linux-validation.result }}"
        echo "Windows x64 tests: ${{ needs.windows-x64.result }}"
        echo "Windows ARM64 cross-compile: ${{ needs.windows-arm64.result }}"
        echo "Linker tests: ${{ needs.linker-tests.result }}"

        # Only Linux validation is critical
        if [ "${{ needs.linux-validation.result }}" != "success" ]; then
          echo "❌ Linux validation failed"
          exit 1
        fi

        echo "✅ Critical tests passed"
        echo ""
        echo "Note: Windows tests are informational until native binaries are available"
