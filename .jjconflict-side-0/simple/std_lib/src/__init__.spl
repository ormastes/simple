# Simple Language Standard Library
# Default variant: async + no_gc + mutable
#
# Lint policy:
# - #[deny(primitive_api)] - No bare primitives in public APIs
# - #[deny(bare_string)] - No bare str/String in public APIs

#[deny(primitive_api)]
#[deny(bare_string)]
mod std

# Prelude - auto-imported into every file
export use prelude.*

# Core modules (variant-agnostic, pure)
common use core.*
common use core.option.*
common use core.result.*

# Unit type definitions
common use units.*

# Concurrency primitives (generators, futures, actors, channels)
common use concurrency.*

# Default async_nogc_mut profile
use host.async_nogc_mut.*

# System module (process control, stdio, args)
pub use sys.*

# UI Framework (shared TUI/GUI API)
pub use ui.*

# Infrastructure APIs (allocators, concurrency, atomics, etc.)
pub use infra.*

# Coverage analysis (condition/decision coverage, path coverage, MC/DC)
pub use coverage.*

# Multi-language tooling (build, test, deploy)
pub use tooling.*
