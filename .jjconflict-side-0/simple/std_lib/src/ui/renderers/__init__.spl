# Renderers - Platform-Specific UI Renderers
#
# Exports all platform-specific renderer implementations.
# Each renderer implements the RenderBackend trait.

mod renderers

# HTML renderer (Browser/Electron)
pub use html.*

# TUI renderer (Terminal)
pub use tui.*

# VSCode renderer (VSCode webview)
pub use vscode.*
