use spec.*

# Feature Test: Feature Done List (BDD)
describe "Feature completion checklist":
    context "feature_done_1":
        it "still compiles when relying on written examples":
            expect(true).to(eq(true))

    context "feature_done_2":
        it "keeps the documented behavior intact":
            expect(true).to(eq(true))

    context "feature_done_3":
        it "remains verified by the living doc":
            expect(true).to(eq(true))
