# variants/ui/renderer/cpu — renderer_select override
#
# CPU-only build target: skip all GPU backends, prefer SIMD then pure software.
# Always ends with "cpu" as the final fallback.

fn renderer_priority_order() -> [text]:
    """CPU-only build: SIMD first, software fallback, cpu last."""
    ["cpu_simd", "software", "cpu"]
