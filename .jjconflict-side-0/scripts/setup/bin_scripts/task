#!/bin/bash
# Simple Task CLI - AI Assistant Dispatcher
# Follows Simple project conventions: bin/ wrapper → src/app/task/

set -e

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Use Simple's bootstrap to run task app
"$DIR/simple" "$DIR/../src/app/task/main.spl" "$@"
