#!/bin/sh
# Turn the USB relay OFF (open circuit, cut power to the board).
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname "$0")" && pwd)"
. "$SCRIPT_DIR/relay_common.shs"

detect_relay_device
serial_send_noreply "$RELAY_CMD_OFF"
relay_log "Power OFF sent to $RELAY_DEVICE"
