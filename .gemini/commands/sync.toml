description = "Sync only the current isolated session branch; never move protected refs directly."
prompt = """
Verify session branch/worktree ownership, fetch the declared protected target, and record its exact SHA. Rebase only a private work branch; submitted changes require renewed review. Resolve policy conflicts semantically, rerun affected gates, and push only the owned branch with lease/CAS. Submit through integration authority. Reject main-worktree mutation, protected-ref rebase/update, unconditional force, and broad pushes.
"""

