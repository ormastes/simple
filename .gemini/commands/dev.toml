description = "Full feature development via SPipe: intake, research, design, specs, implementation, refactor, verify, and ship."
prompt = """
Run the full SPipe feature-development pipeline for the given request.

This command is a compatibility alias for /sp_dev.

Pipeline:
1. dev: refine the task into a clear goal and acceptance criteria
2. research: local and domain research, with Codex cooperative research when available
3. arch: architecture and UI design, with Gemini/Codex cooperative design when available
4. spec: SPipe BDD tests with built-in matchers only
5. implement: code changes following Simple language rules
6. refactor: duplication, size, coupling, and performance cleanup
7. verify: production readiness and SPipe quality gates
8. ship: VCS sync or release handoff when requested

Follow `.claude/skills/spipe.md` as the canonical procedure.
For broad lanes, record lower-model sidecars to use or merge (Codex Spark,
Claude Haiku, Claude Sonnet), or mark N/A. Normal/highest-capability review
must accept broad findings, generated-manual quality, coverage, done marks, and
release-blocking PASS.
Specs must generate readable doc/06_spec/... scenario manuals. Define shared
interface/manual helper names before implementation, and make temporary helper
placeholders fail explicitly with assert(false) or fail(...).
Before final verify, run `sh scripts/audit/direct-env-runtime-guard.shs --working`
and `sh scripts/audit/direct-env-runtime-guard.shs --staged` so app/gc env
reads and process-run calls use env/process facades.
Workflow/tooling, evidence-wrapper, generated-spec-shape, or verification-contract
changes must refresh matching doc/07_guide, doc/06_spec, .codex/skills/,
.agents/skills/, .claude/skills/, .claude/agents/spipe/, and .gemini/commands/
instructions before final verify.
"""
