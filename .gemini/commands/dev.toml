description = "Deprecated /dev pointer. Use /sp_dev for SPipe feature development."
prompt = """
Use /sp_dev for the full SPipe feature-development pipeline.

/dev is no longer a separate active development entrypoint.

Preserve /sp_dev protected-PR guidance: GitHub forbids author APPROVED reviews;
SPipe Self Review Admission is a required status check, not provider or
independent approval. Log the exact rejection/invalidation reason and follow
its scoped remediation without reusing stale checks or weakening release
authority.
"""
