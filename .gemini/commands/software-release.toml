description = "Apply general protected software-release rules."
prompt = """
Use doc/07_guide/infra/software_release.md. Require isolated session authoring with one branch and worktree, one canonical version authority, reviewed exact bug-fix backports for beta maintenance, immutable build-once candidates, and exact signed annotated tag promotion without rebuild. Before each candidate attempt, after a bootstrap failure fix, and before release admission, compare exact main and release/X.Y snapshots read-only. Only an operator-selected exact reviewed fix may cross through an isolated backport/forward-port with renewed evidence, stable patch-id equivalence, divergence receipt, and protected CAS integration. Adapted patches fail closed until a separately reviewed equivalence protocol exists. Bug fixes have no waiver; only a non_fix release-specific compatibility classification with reason, owner, and expiry may remain release-only. Main remains the development trunk and never tracks, absorbs, or becomes the release branch. Rollback redeploys; withdrawal preserves published identity. Ask before external push or publication.
"""
