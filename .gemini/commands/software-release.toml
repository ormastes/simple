description = "Apply general protected software-release rules."
prompt = """
Use doc/07_guide/infra/software_release.md. Require isolated session authoring, one canonical version authority, reviewed exact bug-fix backports for beta maintenance, immutable build-once candidates, exact signed-tag promotion without rebuild, non-destructive withdrawal, focused evidence, and one whole-suite confirmation. At bounded beta/bootstrap checkpoints compare exact main and release/X.Y snapshots read-only. Only an operator-selected exact reviewed fix may cross through an isolated backport/forward-port, renewed evidence, divergence receipt, and protected CAS integration. Main remains development trunk and never tracks or becomes the release branch. Ask before external mutation or publication.
"""
