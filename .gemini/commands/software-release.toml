description = "Apply general protected software-release rules."
prompt = """
Use doc/07_guide/infra/software_release.md. Require isolated sessions, one version authority, exact reviewed maintenance backports, immutable build-once candidates, and signed exact-tag promotion without rebuild. Compare exact main/release snapshots before candidate and admission. Protected PR integration uses SPipe Self Review Admission instead of provider Approve: explicit self-attested PASS is user-authorized, not independent authentication; resolve/re-resolve protected target/ruleset, head, base, merge-base, and diff; apply default allow plus operator deny/constraints; reject aliases, secrets, symlinks, submodules, non-UTF-8, and traversal; expire checks after ten minutes. Generic Actions App trust risk is accepted, not independent security. Keep candidate and environment admission separate. Ask before external push/publication.
"""
