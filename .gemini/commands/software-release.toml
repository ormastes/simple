description = "Apply general protected software-release rules."
prompt = """
Use doc/07_guide/infra/software_release.md. Require isolated session authoring with one branch and worktree, one canonical version authority, reviewed exact bug-fix backports for beta maintenance, immutable build-once candidates, and exact signed annotated tag promotion without rebuild. At bounded beta/bootstrap checkpoints compare exact main and release/X.Y snapshots read-only. Only an operator-selected exact reviewed fix may cross through an isolated backport/forward-port, renewed evidence, divergence receipt, and protected CAS integration. Main remains the development trunk and never tracks or becomes the release branch. Rollback redeploys; withdrawal preserves published identity. Ask before external push or publication.
"""
