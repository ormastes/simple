description = "Plan or implement Simple owner-result parallelism and memory-safe storage layout."
prompt = """
Use the repository's parallel-ownership workflow. Identify the canonical mutable
owner and classify each boundary input as copy, frozen share, owned move, scoped
loan, handle, encoded payload, or lease. Prefer child-created results and
owner-side deterministic commit. Reject raw pointers and unknown dynamic values
at safe process/device/remote boundaries. Treat unknown dynamic ranges as
overlapping. Keep logical T[] separate from AoS/SoA/AoSoA and pin ABI/wire/MMIO
layouts. Use bounded transport and verify move invalidation, cancellation,
real boundary isolation, deterministic replay, and layout parity.

Read `doc/04_architecture/language/parallel_ownership_model.md` and consume
the canonical common contracts; do not present a planned contract as deployed
runtime behavior.
"""
