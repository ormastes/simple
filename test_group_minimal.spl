# Minimal test

fn main():
    print "Starting..."

    val source = "var count = 0\nvar total = 0\nvar sum = 0"
    val lines = source.split("\n")

    print "Lines: {lines.len()}"

    var count = 0
    for line in lines:
        print "Line {count}: {line}"
        count = count + 1

    print "Done"

main()
