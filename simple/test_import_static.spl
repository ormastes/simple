# Test calling static method on imported class

import physics.core as core

fn main():
    print("Testing imported class static method...")

    # Test 1: Try core.Vector3::zero() (might not parse)
    # let v1 = core.Vector3::zero()

    # Test 2: Direct call without module prefix
    # This won't work because Vector3 is not in scope
    # let v2 = Vector3::zero()

    # Test 3: Try accessing the class first
    let vec_class = core.Vector3
    print("Got Vector3 class")

    # Can we instantiate it?
    let v3 = core.Vector3(1.0, 2.0, 3.0)
    print("Created instance")

    print("Tests completed!")
    return 0
