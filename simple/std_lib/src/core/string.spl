# String - UTF-8 string type
# Syntax: str and "..." create String objects
# Implements collection traits for character access
#
# Split into 4 modules for better organization:
# - string_core: Core type definition, error types, constructors
# - string_ops: Operations, internal helpers, mutation, search, transform
# - string_traits: Standard trait implementations (Clone, Eq, Ord, Hash, etc.)
# - string_utils: Iterators, Bytes type, helper functions

use core.traits.*
use core.collections.*

# Import all string modules
use string_core.*
use string_ops.*
use string_traits.*
use string_utils.*
