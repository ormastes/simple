# Dashboard Module
#
# Comprehensive dashboard system for tracking project metrics, status, and trends.
# Provides unified SDN database, CLI dashboard, web dashboard, and historical analysis.

# Re-export types
export {
    # Enums
    CollectionMode, Status, TrendStatus, TestStatus,

    # Data structures
    Feature, SspecTest, TodoItem, CoverageMetric, DuplicationMetric,
    TestExecution, VerificationStatus, VcsState, BuildTime, Dependency, Plan,
    Trend, Alert, DashboardData
} from tooling.dashboard.types

# Re-export database operations
export {
    DashboardDatabase,
    parse_features_sdn, parse_sspec_tests_sdn, parse_todos_sdn, parse_coverage_sdn,
    quote, unquote, parse_i32, parse_f64,
    status_to_string, string_to_status
} from tooling.dashboard.database

# Re-export cache operations
export {
    CacheEntry, DashboardCache, CacheStats,
    get_global_cache, get_cached_data, update_cache, invalidate_cache, cache_stats,
    DEFAULT_TTL_SECONDS, CACHE_FILE_TTL_SECONDS
} from tooling.dashboard.cache

# Re-export collector operations
export {
    DashboardCollector,
    collect_dashboard_data, collect_dashboard_data_mode,
    collect_quick, collect_full, collect_incremental
} from tooling.dashboard.collector

# Re-export snapshot operations
export {
    SnapshotManager, SnapshotData,
    create_daily_snapshot, load_recent_snapshots, cleanup_snapshots,
    RETENTION_DAYS
} from tooling.dashboard.snapshots

# Re-export trend analysis
export {
    TrendAnalyzer, TrendReport,
    analyze_weekly_trends, analyze_monthly_trends, analyze_metric,
    format_trend, format_trend_chart
} from tooling.dashboard.trends

# Re-export alert system
export {
    AlertConfig, AlertManager, AlertSummary,
    check_alerts, check_alerts_with_trends, should_block_build,
    format_alerts, get_critical_alerts
} from tooling.dashboard.alerts
