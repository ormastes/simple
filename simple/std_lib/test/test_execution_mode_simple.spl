# Simplified ExecutionMode for testing (without parse-error-causing syntax)

enum ExecutionMode:
    Interpreter
    JIT
    SMF_Cranelift
    SMF_LLVM

fn mode_to_string(mode: ExecutionMode) -> str:
    "interpreter"  # Simplified for testing
