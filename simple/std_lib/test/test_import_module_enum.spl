# Test importing enum from another module
import test_module_enum

# Test 1: Direct access to enum (should work with our fix)
val mode1 = TestMode.Mode1
print("Test 1 - Direct enum access: SUCCESS")

# Test 2: Use enum in different context
fn test_function():
    val mode2 = TestMode.Mode2
    print("Test 2 - Enum in function: SUCCESS")

test_function()

# Test 3: Qualified access should still work
val mode3 = test_module_enum.TestMode.Mode3
print("Test 3 - Qualified access: SUCCESS")

print("All module enum tests passed!")
