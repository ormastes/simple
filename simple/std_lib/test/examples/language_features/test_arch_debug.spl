# Debug test to see if imports are being tracked

use crate.forbidden.module

fn main() -> i64:
    return 0

# This should match since we're importing from forbidden.*
forbid pc{ import(*, crate.forbidden.*) } "Cannot import forbidden modules"
