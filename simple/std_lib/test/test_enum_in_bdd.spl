# Test enum accessibility in BDD blocks (original bug reproduction)
import test_module_enum

describe "Enum in BDD blocks":
    it "first test with enum":
        let mode = TestMode.Mode1
        print("First test: mode = {mode}")
        expect true

    it "second test with enum":
        let mode = TestMode.Mode2
        print("Second test: mode = {mode}")
        expect true

    it "third test with enum":
        let mode = TestMode.Mode3
        print("Third test: mode = {mode}")
        expect true
