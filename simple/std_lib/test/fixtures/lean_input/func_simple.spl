# Simple function for Lean codegen golden test

fn add(a: i32, b: i32) -> i32:
    a + b

fn is_positive(n: i32) -> bool:
    n > 0

fn factorial(n: i32) -> i32:
    @requires n >= 0
    @ensures result >= 1
    if n <= 1:
        1
    else:
        n * factorial(n - 1)
