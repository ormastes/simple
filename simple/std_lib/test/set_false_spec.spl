import std.spec
use core.*

describe "Set contains false":
    it "returns false for missing":
        val s = Set.new()
        s.add(42)
        val has99 = s.contains(99)
        print("Contains 99: " + has99.to_string())
        expect(has99).to(be_false())

