# Test different path syntaxes to understand what works

# Define a simple class with a static method
class Math:
    @staticmethod
    fn abs(x: int) -> int:
        if x < 0:
            return -x
        else:
            return x

fn main():
    print("Testing path syntax...")

    # Test 1: Direct static method call on local class
    val result1 = Math::abs(-5)
    if result1 == 5:
        print("✓ Test 1 passed: Math::abs(-5) = 5")
    else:
        print("✗ Test 1 FAILED")
        return 1

    # Test 2: Store class in variable
    val math_class = Math
    # Can we call static method through variable? (probably not)
    # val result2 = math_class::abs(-5)  # This will likely fail

    print("Tests completed!")
    return 0
