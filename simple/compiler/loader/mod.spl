# Loader module for Simple language.
#
# This module provides:
# - ModuleLoader for runtime SMF loading with type inference
# - JIT instantiation at load-time via ObjTaker
# - Symbol resolution with HM type inference
# - Hot-reload support
#
# Main entry point:
#   val loader = ModuleLoader.with_defaults()
#   loader.load("module.smf")
#   loader.resolve_symbol("my_function")

pub use jit_instantiator.*
pub use module_loader.*
