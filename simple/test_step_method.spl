class World:
    time: f64
    substeps: i32
    bodies: [i32]
    
    fn step(self, dt: f64):
        """Advance simulation."""
        let sub_dt = dt / self.substeps as f64

        for _ in range(self.substeps):
            for body in self.bodies:
                pass

        self.time += dt

    fn _apply_gravity(self):
        """Apply gravity."""
        return 0

fn main():
    return 0
