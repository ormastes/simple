# Basic test for torch.tensor() function
import io

fn main():
    io.println("Testing torch.tensor() function...")

    # This should work if we properly implement it as interpreter-only feature
    # For now, val's just test that the imports work
    io.println("Test passed!")
