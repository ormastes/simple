enum Device:
    CPU
    CUDA(i32)

fn device_code(device: Device) -> i32:
    match device:
        Device::CPU -> 0
        Device::CUDA(id) -> id + 1

fn main():
    val dev = Device::CPU
    val code = device_code(dev)
    print("Device code: ")
    print(code)
