# Unit tests for Simple language grammar

import spec.{describe, it, expect}
import parser.treesitter.grammar_simple as grammar_simple
import parser.treesitter.{TreeSitterParser}

describe("SimpleGrammar"):
    it("creates Simple grammar"):
        val grammar = grammar_simple.SimpleGrammar.new()

        expect(grammar.get_grammar().language).to_equal("simple")

    it("has source_file as entry rule"):
        val grammar = grammar_simple.SimpleGrammar.new()
        val g = grammar.get_grammar()

        expect(g.has_rule("source_file")).to_be(true)

describe("Function Definitions"):
    it("parses simple function"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "fn add(x: Int, y: Int) -> Int:\n    return x + y\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses function without return type"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "fn greet(name: String):\n    print(name)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses function with no parameters"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "fn main():\n    print(\"Hello\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Class Definitions"):
    it("parses simple class"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "class Point:\n    x: Int\n    y: Int\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses class with methods"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "class Counter:\n    count: Int\n    fn increment(mut self):\n        self.count = self.count + 1\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses generic class"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "class Box<T>:\n    value: T\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Struct Definitions"):
    it("parses simple struct"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "struct Rectangle:\n    width: Float\n    height: Float\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Enum Definitions"):
    it("parses simple enum"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "enum Color:\n    Red\n    Green\n    Blue\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses enum with tuple variants"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "enum Option<T>:\n    Some(T)\n    None\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses enum with struct variants"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "enum Shape:\n    Circle { radius: Float }\n    Rectangle { width: Float, height: Float }\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Import Statements"):
    it("parses simple import"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "import math\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses import with path"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "import std.collections.list\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses import with alias"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "import math as m\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Let Statements"):
    it("parses simple val"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val x = 42\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses val with type annotation"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val x: Int = 42\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses mutable val"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "var count: Int = 0\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("If Statements"):
    it("parses simple if"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "if x > 0:\n    print(\"positive\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses if-else"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "if x > 0:\n    print(\"positive\")\nelse:\n    print(\"non-positive\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses if-elif-else"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "if x > 0:\n    print(\"positive\")\nelif x < 0:\n    print(\"negative\")\nelse:\n    print(\"zero\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Match Statements"):
    it("parses simple match"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match x:\n    case 0: print(\"zero\")\n    case _: print(\"other\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses match with enum patterns"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match option:\n    case Some(value): print(value)\n    case None: print(\"nothing\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses match with guard"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match x:\n    case n if n > 0: print(\"positive\")\n    case _: print(\"other\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Loop Statements"):
    it("parses for loop"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "for i in 0..10:\n    print(i)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses while loop"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "while x < 10:\n    x = x + 1\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses infinite loop"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "loop:\n    print(\"infinite\")\n    break\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Expressions"):
    it("parses binary expressions"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val result = x + y * z\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses unary expressions"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val neg = -x\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses call expressions"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val result = add(1, 2)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses index expressions"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val item = list[0]\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses field access"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val x = point.x\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses lambda expressions"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val add = |x, y| x + y\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Literals"):
    it("parses integer literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val x = 42\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses float literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val pi = 3.14159\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses string literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val msg = \"Hello, World!\"\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses f-string literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val msg = f\"x = {x}\"\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses boolean literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val flag = true\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses nil literal"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val nothing = nil\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses array literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val numbers = [1, 2, 3, 4, 5]\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses tuple literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val pair = (1, 2)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses dict literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val ages = {\"Alice\": 30, \"Bob\": 25}\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses struct literals"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val p = Point(x: 10, y: 20)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Patterns"):
    it("parses identifier patterns"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match x:\n    case value: print(value)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses wildcard patterns"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match x:\n    case _: print(\"anything\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses tuple patterns"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match pair:\n    case (x, y): print(x, y)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses struct patterns"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match point:\n    case Point(x: px, y: py): print(px, py)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses enum patterns"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "match option:\n    case Some(val): print(val)\n    case None: print(\"nothing\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Types"):
    it("parses named types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val x: Int = 42\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses generic types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val list: List<Int> = [1, 2, 3]\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses function types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val f: fn(Int, Int) -> Int = add\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses tuple types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val pair: (Int, String) = (1, \"one\")\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses array types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val numbers: [Int] = [1, 2, 3]\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses dict types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val ages: {String: Int} = {\"Alice\": 30}\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses option types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val maybe: Option<Int> = Some(42)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

    it("parses result types"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val result: Int! = Ok(42)\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Complex Programs"):
    it("parses complete program"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = """
import std.io

class Counter:
    count: Int

    static fn new() -> Counter:
        Counter(count: 0)

    var fn increment():
        self.count = self.count + 1

    fn get() -> Int:
        return self.count

fn main():
    var counter = Counter.new()
    counter.increment()
    io.println(f"Count: {counter.get()}")
"""
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.kind).to_equal("source_file")

describe("Error Recovery"):
    it("recovers from missing colon"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "fn broken()\n    return 42\n"
        val tree = parser.parse(source).unwrap()

        # Should have ERROR node but still parse
        expect(tree.root_node.has_error).to_be(true)

    it("recovers from unclosed paren"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "val x = add(1, 2\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.has_error).to_be(true)

    it("recovers from unexpected token"):
        val parser = TreeSitterParser.new("simple").unwrap()
        val source = "fn test(): return @ 42\n"
        val tree = parser.parse(source).unwrap()

        expect(tree.root_node.has_error).to_be(true)
