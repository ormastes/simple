# Browser Standard Library
#
# Provides access to browser APIs for WebAssembly modules running in web browsers.
# This module includes DOM manipulation, console logging, fetch requests, and more.
#
# ## Modules
#
# - **console** - Console logging and debugging
# - **dom** - Document Object Model manipulation
# - **fetch** - HTTP requests and responses
#
# ## Example Usage
#
# ```simple
# import browser.console as console
# import browser.dom as dom
# import browser.fetch as fetch
#
# # Log to console
# console.log("Application started")
#
# # Get DOM element
# let button = dom.get_element_by_id("submit-btn")
#
# # Add event listener
# fn on_click(event: Event):
#     console.log("Button clicked!")
#     let data = await fetch.get_json("/api/data")
#     update_ui(data)
#
# button.add_event_listener("click", on_click)
# ```
#
# ## WASM Integration
#
# All browser APIs use `@extern` declarations that map to JavaScript functions
# via wasm-bindgen. The Simple compiler automatically generates the necessary
# bindings when compiling to wasm32-unknown-unknown target.
#
# ## Async Support
#
# Many browser APIs are asynchronous (fetch, timeouts, etc.). Use `@async`
# functions and `await` syntax for proper async handling:
#
# ```simple
# @async
# fn load_data():
#     let response = await fetch.get("/api/users")
#     let users = await response.json()
#     return users
# ```

# Re-export all browser modules
pub use console
pub use dom
pub use fetch
