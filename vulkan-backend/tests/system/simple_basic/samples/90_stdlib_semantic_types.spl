# 90_stdlib_semantic_types.spl - Semantic types and variants

# Stdlib goals: variants (async/sync, gc/no_gc, platform overlays) + semantic types in public APIs

# Example: "semantic types only" in the boundary
pub fn app_config_path() -> FilePath:
    return "/etc/simple/app.toml"_file

pub fn user_home() -> FilePath:
    return "/home/user"_file

# Using semantic types
config = app_config_path()
home = user_home()

print "config={config}"
print "home={home}"

# Variant usage is implementation-defined; treat as a config-driven import resolution demo.
# The stdlib spec describes the variant-aware layout/goal.

main = 0
