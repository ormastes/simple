# 50_traits_default_methods.spl - Traits with default methods

trait Printable:
    fn stringify() -> str
    fn print_self():
        print self.stringify()

struct User:
    name: str

impl Printable for User:
    fn stringify() -> str:
        return "User(name={self.name})"

let u = User(name: "Kim")
u.print_self()

main = 0
