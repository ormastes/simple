{
  "_comment": "MCP Servers Configuration (Linux/macOS)",

  "mcpServers": {
    "simple-mcp": {
      "_info": "104 tools (debug + debug_log + diag + vcs + api + cli + query + analysis + dialog + test_daemon)",
      "command": "bin/simple",
      "args": ["src/app/mcp/main.spl"],
      "env": {
        "SIMPLE_LIB": "src",
        "SIMPLE_LOG": "error",
        "RUST_LOG": "error"
      }
    }
  }
}
