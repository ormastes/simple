{
  "_comment": "MCP Servers Configuration",

  "mcpServers": {
    "simple-mcp": {
      "_info": "66 tools (debug + debug_log + diag + vcs + api + cli + query + analysis + lsp + code_query) - proxied stdio framing",
      "command": "bin/simple_mcp_server",
      "args": []
    }
  }
}
