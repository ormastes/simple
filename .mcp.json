{
  "_comment": "MCP Servers Configuration",

  "mcpServers": {
    "simple-mcp": {
      "_info": "33 tools (debug + debug_log + diag) - lazy subprocess dispatch",
      "command": "bin/release/simple",
      "args": ["src/app/mcp/main_lazy.spl"],
      "env": {
        "SIMPLE_LIB": "src",
        "RUST_LOG": "error"
      }
    }

  }
}
