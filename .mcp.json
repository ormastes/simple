{
  "_comment": "MCP Servers Configuration",

  "mcpServers": {
    "simple-mcp": {
      "_info": "34 tools (debug + debug_log + diag + api) - proxied stdio framing",
      "command": "bin/simple_mcp_server",
      "args": []
    },
    "simple-fileio": {
      "_info": "13 tools - file I/O protection with rule-based access control",
      "command": "bin/release/simple",
      "args": ["src/app/mcp/fileio_main.spl", "server"],
      "env": {
        "SIMPLE_LIB": "src"
      }
    },
    "simple-jj": {
      "_info": "75 tools (48 jj + 27 git compat) - Jujutsu VCS with Git compatibility",
      "command": "bin/release/simple",
      "args": ["src/app/mcp_jj/main.spl", "server"],
      "env": {
        "SIMPLE_LIB": "src"
      }
    }
  }
}
