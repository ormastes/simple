#!/bin/bash
# Thin wrapper — delegates to .spl implementation
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/release/simple" "${SCRIPT_DIR}/simple_lsp_server.spl" "$@"
