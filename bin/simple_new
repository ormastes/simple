#!/bin/sh
# Simple Language CLI - New (DEPRECATED: use ./simple instead)
#
# This wrapper is kept for backward compatibility.
# The default ./simple now runs the Simple-language CLI implementation.

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/simple" "$@"
