#!/bin/bash
# Simple MCP Server Wrapper
#
# Current runtime status in this checkout:
# - the legacy full MCP entrypoint under src/app/mcp/ does not start cleanly
# - the checked-in main.smf artifact is inert with the current runtime
#
# Use the working Simple LSP MCP executable as the MVP server path until the
# full Simple MCP server is repaired.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec "${SCRIPT_DIR}/simple_lsp_mcp_server" "$@"
