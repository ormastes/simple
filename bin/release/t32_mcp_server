#!/bin/sh
# Auto-generated by scripts/setup.sh — do not edit
set -eu
# Resolve through symlinks to find the real script location
SELF="$0"
while [ -L "$SELF" ]; do
  DIR="$(cd "$(dirname "$SELF")" && pwd)"
  SELF="$(readlink "$SELF")"
  case "$SELF" in /*) ;; *) SELF="${DIR}/${SELF}" ;; esac
done
SCRIPT_DIR="$(cd "$(dirname "$SELF")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
PREFERRED_BINARY=""
if [ -n "bin/release/t32_mcp_native" ]; then
  PREFERRED_BINARY="${REPO_ROOT}/bin/release/t32_mcp_native"
fi
HOST_KERNEL="$(uname -s 2>/dev/null || echo unknown)"
PREFER_NATIVE=0
if [ "${T32_MCP_USE_NATIVE:-0}" = "1" ]; then
  PREFER_NATIVE=1
elif [ "$HOST_KERNEL" = "FreeBSD" ]; then
  PREFER_NATIVE=1
fi
if [ "$PREFER_NATIVE" = "1" ] && [ -n "$PREFERRED_BINARY" ] && [ -x "$PREFERRED_BINARY" ]; then
  exec "$PREFERRED_BINARY" "$@"
fi
# Prefer the Rust release binary when present. Some local bin/simple links can
# point at bootstrap builds that do not reliably run MCP entrypoints.
RUNTIME="${REPO_ROOT}/src/compiler_rust/target/release/simple"
if [ ! -x "$RUNTIME" ]; then RUNTIME="${REPO_ROOT}/bin/simple"; fi
if [ ! -x "$RUNTIME" ]; then RUNTIME="${REPO_ROOT}/bin/release/simple"; fi
ENTRY="${REPO_ROOT}/src/app/t32_mcp_frontend/main.spl"
export SIMPLE_LIB="${REPO_ROOT}/src"
export SIMPLE_LOG=error
export SIMPLE_LIB="${REPO_ROOT}/examples/10_tooling/trace32_tools"
export SIMPLE_TIMEOUT_SECONDS=86400
cd "$REPO_ROOT"
RUST_LOG=error exec "$RUNTIME" "$ENTRY" "$@" 2>/dev/null
