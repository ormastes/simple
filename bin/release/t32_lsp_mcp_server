#!/bin/sh
# Auto-generated — simplified wrapper (no compile caching)
set -eu
SELF="$0"
while [ -L "$SELF" ]; do
  DIR="$(cd "$(dirname "$SELF")" && pwd)"
  SELF="$(readlink "$SELF")"
  case "$SELF" in /*) ;; *) SELF="${DIR}/${SELF}" ;; esac
done
SCRIPT_DIR="$(cd "$(dirname "$SELF")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
cd "$REPO_ROOT"
RUNTIME="${SIMPLE_BINARY:-}"
if [ -n "$RUNTIME" ] && [ ! -x "$RUNTIME" ]; then RUNTIME=""; fi
if [ -z "$RUNTIME" ]; then RUNTIME="${SIMPLE_RUNTIME:-${REPO_ROOT}/bin/simple}"; fi
if [ ! -x "$RUNTIME" ]; then RUNTIME="${REPO_ROOT}/bin/release/simple"; fi
if [ ! -x "$RUNTIME" ]; then RUNTIME="${REPO_ROOT}/src/compiler_rust/target/release/simple"; fi
if [ ! -x "$RUNTIME" ]; then RUNTIME="${REPO_ROOT}/src/compiler_rust/target/bootstrap/simple"; fi
if [ ! -x "$RUNTIME" ]; then echo "error: no runtime found for t32_lsp_mcp_server" >&2; exit 1; fi
ENTRY="${REPO_ROOT}/examples/10_tooling/trace32_tools/t32_lsp_mcp/claude_frontend.spl"
TRACE32_ROOT="${REPO_ROOT}/examples/10_tooling/trace32_tools"
STDERR_LOG="${REPO_ROOT}/.simple/logs/t32_lsp_mcp_stderr.log"
mkdir -p "$(dirname "$STDERR_LOG")"
export SIMPLE_LIB="${SIMPLE_LIB:-$TRACE32_ROOT}"
export SIMPLE_BINARY="$RUNTIME"
export SIMPLE_RUNTIME="${SIMPLE_RUNTIME:-$RUNTIME}"
export SIMPLE_LOG="${SIMPLE_LOG:-error}"
export SIMPLE_TIMEOUT_SECONDS=86400
export SIMPLE_MEMORY_LIMIT_MB=${SIMPLE_MEMORY_LIMIT_MB:-${SIMPLE_TEST_MEMORY_LIMIT_MB:-512}}
export T32_LSP_MCP_TOOL_RUNNER="${T32_LSP_MCP_TOOL_RUNNER:-examples/10_tooling/trace32_tools/t32_lsp_mcp/tool_runner.spl}"
TOOL_RUNNER_BIN="${REPO_ROOT}/bin/release/t32_lsp_mcp_tool_runner"
if [ -x "$TOOL_RUNNER_BIN" ]; then
  export T32_LSP_MCP_TOOL_RUNNER_BIN="${T32_LSP_MCP_TOOL_RUNNER_BIN:-$TOOL_RUNNER_BIN}"
fi
DAEMON_SOURCE="${REPO_ROOT}/examples/10_tooling/trace32_tools/cmm_lsp/mcp_daemon.spl"
export T32_LSP_MCP_TOOL_DAEMON="${T32_LSP_MCP_TOOL_DAEMON:-$DAEMON_SOURCE}"
CWD="$(pwd)"
SUFFIX="$(printf '%s' "$CWD" | sed 's#^/*##; s#[^A-Za-z0-9._-]#_#g')"
if [ -z "$SUFFIX" ]; then SUFFIX="workspace"; fi
export T32_LSP_MCP_TOOL_DAEMON_DIR="${T32_LSP_MCP_TOOL_DAEMON_DIR:-/tmp/t32_lsp_mcp_shared_${SUFFIX}}"

RUST_LOG="${RUST_LOG:-error}" exec "$RUNTIME" "$ENTRY" "$@" 2>>"$STDERR_LOG"
