#!/bin/sh
# Interpreter-mode wrapper using Rust seed with interpreter fixes
# The deployed bin/simple (self-hosted) lacks interpreter fixes from 2026-05-27
# Use this wrapper until bootstrap-from-scratch --deploy succeeds
exec "$(dirname "$0")/../src/compiler_rust/target/bootstrap/simple" "$@"
