#!/bin/bash
# Codex-only MCP alias for LSP-oriented Simple tools.
# This currently delegates to the main MCP server wrapper.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec "${SCRIPT_DIR}/simple_mcp_server" "$@"
