#!/bin/bash
# Coverage tool wrapper - runs Simple coverage app via runtime
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"

exec "$REPO_ROOT/target/debug/simple_old" "$REPO_ROOT/src/app/coverage/main.spl" "$@"
