use std.spec
use std.gc_async_mut.db.dbfs_driver.{DbFsDriver}

describe "gc_async_mut DBFS driver package facade":

    it "re-exports DbFsDriver from the package namespace":
        val driver = DbFsDriver.new_hosted()
        expect(driver.preferred_batch_bytes()).to_equal(4096)
