# 09_type_inference.spl - Type inference in functions and traits

# Type inference in functions - parameter and return types inferred
fn add_nums(a, b):
    a + b

result = add_nums(1, 2)
print "add result: {result}"

# Generic function with type inference
fn identity(x):
    x

num_val = identity(42)
str_val = identity("hello")

print "num_val={num_val} str_val={str_val}"

# Method chaining with type inference
chain_result = [1, 2, 3, 4, 5]
    .filter(\x: x > 2)
    .map(\x: x * 2)

print "chain result: {chain_result}"

main = result
