# 60_memory_gc_unique_shared_weak_handle.spl - Pointer kinds
# TODO: Pointer kinds (unique, shared, weak, handle) require advanced memory FFI

class Player:
    name: str
    hp: i32

# GC-managed (default)
val p: Player = Player { name: "Hero", hp: 100 }
print "gc player created"

# For now, just test basic class semantics
val q = p  # reference alias
print "reference alias"

main = p.hp
