# 10_types_mutability.spl - Struct/class mutability

# Structs are value types
struct Point:
    x: f64
    y: f64

# Struct with mutable fields
struct Cursor:
    x: f64
    y: f64

val p = Point { x: 1.0, y: 2.0 }
# p.x = 9  # would be an error (val binding is immutable)

var c = Cursor { x: 0.0, y: 0.0 }
c.x = 10.0
print "cursor set"

# Classes are reference types, mutable by default
class Person:
    name: str
    age: i32
    me birthday():
        self.age = self.age + 1

val a = Person { name: "Alice", age: 30 }
a.birthday()
print "alice birthday done"

main = a.age
