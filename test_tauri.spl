use common.ui.parse.sdn.{parse_ui_file}

fn main():
    print "Starting parse..."
    val result = parse_ui_file("examples/ui/demo.ui.sdn")
    match result:
        case Ok(tree):
            print "Parsed OK! Title: {tree.title}"
        case Err(e):
            print "Error: {e}"
