# Minimal decorator test
use spec.*
use core.decorators.*

describe "@cached decorator":
    it "caches function results":
        @cached
        fn square(x: i64) -> i64:
            return x * x

        result = square(5)
        expect(result).to_equal(25)
