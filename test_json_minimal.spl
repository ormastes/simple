# Minimal test to reproduce cargo test issue
import std.spec
import core.json

describe "JSON minimal":
    it "test 1":
        result = json.parse("null")
        expect result.is_ok() == true

    it "test 2":
        result = json.parse("true")
        expect result.is_ok() == true
