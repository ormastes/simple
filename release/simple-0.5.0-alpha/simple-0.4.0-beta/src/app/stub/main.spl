# Simple Settlement Stub (Written in Simple)
#
# Minimal executable stub for Simple settlement executables.
# When run, it loads the settlement data appended to itself and executes it.

# I/O imports
use app.io.mod (settlement_main)

fn main() -> i64:
    settlement_main()
