use std.spec

describe "Test use in it block":
    it "use at module level works":
        # spec module already imported at top
        expect(1 == 1)

    it "use inside it block":
        use std.fs as filesystem
        # Try to use the alias
        print "Filesystem alias: {filesystem}"
        expect true
