use std.spec

describe "Test mut in patterns":
    it "test mut in match":
        val result = Ok(42)
        match result:
            case Ok(mut value):
                print "Mut pattern works: {value}"
                expect true
            case Err(e):
                fail("Unexpected error")
