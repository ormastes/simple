val contexts = [{"examples": [1, 2, 3]}]

for context in contexts:
    print_raw("context")
    if val Some(example_list) = context.get("examples"):
        for example in example_list:
            print_raw(example)
    print_raw("done")
