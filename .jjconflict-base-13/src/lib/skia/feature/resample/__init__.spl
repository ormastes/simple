# Skia resample feature — public surface.
#
# Re-exports the SamplingFilter enum and the scale_bitmap function, mirroring
# SkImage::makeScaled + SkSamplingOptions.

use std.skia.feature.resample.scale.{SamplingFilter, scale_bitmap}

export SamplingFilter
export scale_bitmap
