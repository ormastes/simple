# Feature: Memory Leveling for GPU, NIC, DMA, and Swap

## Raw Request
$sp_dev impl memory leveling memory swap in simple os, make memory leveling with gpu and network memory and dma. research it for efficient memory model with gpu and nic. both simple language model and simpleq os. add memory leveling and optimized for gpu nic support memory model. research related papers. do full fix. go

## Task Type
feature

## Refined Goal
Provide coordinated but separately configured Simple language/runtime and SimpleOS memory-leveling models that safely promote, demote, pin, map, account, and reclaim CPU, GPU, NIC, DMA, and swap-backed memory under pressure while preserving device ownership and DMA correctness.

## Acceptance Criteria
- AC-1: A documented shared model defines CPU-hot, CPU-cold, GPU-local/coherent, NIC-buffer, DMA-pinned, and swap-backed tiers plus legal transitions and ownership states.
- AC-2: Simple code can request memory by domain and policy through stable owner-module APIs without importing raw runtime allocation or device hooks.
- AC-3: SimpleOS/SimpleQ tracks per-allocation size, tier, owner, pin count, DMA direction, device mapping state, access/recency signal, and reclaim eligibility.
- AC-4: Pressure-driven leveling chooses bounded promotion/demotion candidates and never swaps or relocates pinned, in-flight DMA, device-owned, or non-coherent dirty memory.
- AC-5: GPU and NIC handoff APIs enforce map/synchronize/ownership transitions before device access and before CPU reuse, with explicit failure for invalid transitions.
- AC-6: Swap-out and swap-in preserve allocation identity and contents, reject device-ineligible pages, and expose deterministic out-of-space and I/O failure states.
- AC-7: Per-domain reservations and high/low watermarks prevent GPU or NIC pools from starving general CPU memory and prevent general reclaim from consuming protected DMA capacity.
- AC-8: Telemetry reports bytes and transition counts per tier/domain, pressure level, failed promotions/demotions, pinned bytes, DMA mappings, and swap activity without full-memory scans on each query.
- AC-9: Unit and integration specs cover legal transitions, invalid ownership changes, pinning, pressure selection, watermarks, swap round trips, GPU/NIC DMA lifecycles, and failure recovery with absolute content/state oracles.
- AC-10: QEMU-backed SimpleOS evidence proves initialization, pressure-driven demotion, swap round trip, protected DMA/NIC/GPU allocations, and stable operation without host-side substitution; unsupported hardware is reported explicitly.
- AC-11: Representative benchmarks record allocation/transition latency, reclaim throughput, metadata overhead, and hot-path scan bounds before and after the change.
- AC-12: Architecture, design, requirements, operator guide, generated SPipe manual, and tracking artifacts describe the final policy, hardware-coherency assumptions, tuning controls, and verification commands.
- AC-13: Final verification passes focused interpreter/native specs, SimpleOS/QEMU memory evidence, direct runtime/env guards, generated-spec layout guard, and relevant source checks without stubs or placeholder passes.

## Scope Exclusions
- Transparent migration of opaque vendor GPU allocations that expose no host mapping or copy primitive.
- RDMA fabric policy beyond local NIC DMA registration and buffer ownership.
- Physical-board completion claims without board identity and serial/SSH evidence; QEMU-only evidence remains labeled QEMU-only.

## Cooperative Review
- Lower-model sidecars: memory-manager/local-code map; GPU/DMA ownership and coherency map; NIC/ring-buffer and networking map; external paper/prior-art synthesis.
- Merge owner: primary Codex session.
- Final reviewer: primary highest-capability model after sidecar merge.
- Shared interfaces: `MemoryTier`, `MemoryDomain`, `MemoryPageState`, `MemoryLevelingPolicy`, `MemoryAllocation`, `MemoryLevelingStats`, `memory_leveling_request`, `memory_leveling_release`, `memory_leveling_pin`, `memory_leveling_unpin`, `memory_leveling_map_device`, `memory_leveling_sync_for_device`, `memory_leveling_sync_for_cpu`, `memory_leveling_apply_pressure`.
- Manual flow helpers: `step("Create a tiered memory policy")`, `step("Allocate protected device memory")`, `step("Transfer ownership to the device")`, `step("Apply system memory pressure")`, `step("Restore swapped memory")`, `step("Inspect leveling telemetry")`.
- Setup/checker helpers: `memory_leveling_fixture`, `memory_leveling_pressure_fixture`, `expect_memory_allocation_state`, `expect_memory_leveling_stats`, `expect_memory_contents`.
- Any temporary scenario helper must fail with `assert(false)` or `fail(...)`; silent placeholders are forbidden.
- Generated-manual review owner: primary highest-capability model.

## Runtime Boundary Decision
- runtime_need: No new runtime hook is required. The implementation uses the existing PMM, explicit-address-space VMM, DMA, block-device, and CR3 owner interfaces.
- facade_checked: SimpleOS PMM/VMM, `SharedDmaBuffer`, device syscall, scheduler vmspace registry, and block-device facades were checked and reused.
- chosen_path: `reuse-facade`; one compiler-owner GVN mutation fix was required so the native build could progress beyond the proven lowering defect.
- rejected_shortcuts: Device-specific global allocators, fixture-only swap behavior, raw `rt_*` imports in feature code, unsafe relocation of pinned/device-owned memory, and host-only QEMU substitutes.

## Phase
verify-blocked-native-runner-and-source-check

## Log
- dev: Created state file with 13 acceptance criteria (type: feature).
- research: Extended the existing policy-only lane with local/domain research and feature/NFR options; user selection is required before design and implementation.
- research-review: Four lower-model lanes mapped PMM/VMM/swap, GPU/DMA, NIC rings, and prior docs; highest-capability review accepted the merged gaps and options.
- requirements: User selected Feature D + NFR 1 and clarified SimpleQ was a typo for SimpleOS; language/runtime and SimpleOS configurations remain separate while sharing backend capability vocabulary.
- design: Three independent lanes reviewed the shared contract, PMM/VMM/swap transactions, and DMA/GPU/NIC adapters. The primary reviewer fixed the boundary at two independently constructed configurations sharing only immutable contract values and request adapters.
- implementation: Added the allocation registry, bounded pressure policy, device ownership transitions, block-backed swap coordinator, PMM pressure polling, fault restore, process-space-aware PTE transactions, scheduler vmspace registration, CR3-based fault-space resolution, and an NVMe-tail ownership marker.
- implementation-review: Higher-model review found global-page-table rollback and munmap ownership hazards; fixes now use explicit process roots, release physical frames, remove failed multi-page registrations, and halt on unrecoverable fault-registration rollback.
- evidence: Native pressure/swap/process isolation passes 3/3, including two real sparse address spaces mapping the same virtual address to different frames; GPU/NIC/DMA system evidence passes 8/8; NVMe swap-tail geometry passes 1/1.
- sync: Latest focused evidence commit is `5907b31d5343`; JJ `main` and `main@origin` matched after fetch/push.
- qemu-evidence: A current bootstrap compiler builds the strict freestanding kernel without fallback stubs and QEMU emits all required swap, GPU, NIC/DMA, capability, and `TEST PASSED` markers. The checker uses a dedicated linker contract, canonical CRT0, and the x86_64 bare-metal runtime capsule.
- verify-blocker: Building a current pure-Simple CLI from the same source remains over the 900-second cap and emits no artifact. Canonical `bin/simple` verification and AC-13 remain incomplete; the Rust-built compiler is retained only as bootstrap evidence, not the normal tool.
- verify-blocker-update-2026-07-13: A fresh `--full-bootstrap --deploy --no-mcp` rebuilt the Rust seed/runtime, then remained in Stage 2 for more than one hour with the native worker at one full CPU and zero cached objects. The entry-closure O(n^2) scan fix is already present on current `main`, so this is a later self-host failure. Kernel evidence during the run recorded multiple native `simple` segfaults (including faults at addresses `0x8`, `0x11`, and `0x1b`); Stage 2 emitted no source diagnostic. The stalled process tree was terminated without clearing its cache. `scripts/check/check-simple-core-runtime-smoke.shs` cannot run from the canonical launcher because no deployed pure-Simple binary exists to advertise `--emit-archive`. AC-13 therefore remains blocked; do not substitute the Rust seed as a completion claim.
- verify-progress-2026-07-13: Localized the apparent Stage 2 hang to extremely slow interpreted parsing, then used the seed driver's explicit-host bootstrap route. The first artifact crashed through unresolved `rt_native_build`; bootstrap-only hosted-runtime selection is now allowed only for `bootstrap_main.spl`, while non-bootstrap hosted bundles remain rejected by passing Rust regressions. The resulting Stage 2 owns `rt_native_build` and advertises `--emit-archive`. The simple-core gate now applies its selected backend consistently, exports the generated archive path, and the pure-Simple core exports the required `rt_array_len_safe` ABI. `SIMPLE_CORE_BACKEND=cranelift sh scripts/check/check-simple-core-runtime-smoke.shs` passes archive, hello, standalone TUI, full TUI app, and closure-clean checks. LLVM remains unsuitable for this hosted bootstrap artifact because duplicate LLVM context/symbol material crashes in `LLVMModuleCreateWithNameInContext`; Cranelift is the verified bootstrap backend.
- verify-progress-2026-07-13-stage4: The canonical wrapper now uses the explicit host/bootstrap runtime route for Stage 2, prefers a valid Stage 2 when optional Stage 3 fails, and caps Stage 3/4 self-host workers at two after 16 workers reached about 12 GiB RSS without output. Stage 2 builds successfully. Three bounded Stage 4 cycles exposed and fixed discovery syntax in `simple_web_html_layout_renderer.spl` and `common/encoding/sfnt.spl`; `sfnt.spl` now parses directly. The third cycle stopped at `backend_vulkan_font.spl` with `Unexpected token: expected Comma, found Plus`. Computed-index assignment targets and casts inside its parameter array were normalized, advancing the direct parser error to `expected Comma, found As`, but the file is not yet parse-clean. Per the three-cycle guard, Stage 4 was not rerun. Canonical deployment and AC-13 remain incomplete; caches and logs are preserved under `build/native_probe/`.
- verify-progress-2026-07-13-stage4-parser: Three further bounded direct-parser cycles proved `backend_vulkan_font.spl` parse-clean after replacing the seed parser's ambiguous unparenthesized `as u32 | ...` byte reconstruction with established numeric conversion methods. Dependency discovery now stops in `common/encoding/sfnt_glyf.spl` with `expected Comma, found Plus`; computed array indices and several cast-followed-by-arithmetic forms were normalized, but another occurrence remains. A sidecar review also found that Stage 2 fallback selection must require a successful current Stage 2 build and a non-delegating capability check before Stage 4/deploy can trust it. No Stage 4 retry, deployment, commit, or push was performed after the three-cycle cap.
- verify-progress-2026-07-13-sfnt-glyf-parser: A fresh three-cycle direct-parser pass normalized the remaining cast-followed-by-arithmetic expressions, compact one-line statements, and inline conditionals in `sfnt_glyf.spl`. The diagnostic advanced from `expected Comma, found Plus` to `expected Comma, found Colon` but remains locationless. The bootstrap wrapper now conservatively selects the Rust seed whenever Stage 3 self-host verification is unavailable, avoiding promotion of an unverified Stage 2 artifact. Stage 4 was not rerun after the cycle cap; deployment, AC-13, commit, and push remain pending.
- verify-progress-2026-07-13-stage4-performance: Replacing the three remaining cast/comparison forms (`glyph_id as i64 <|>= ...`) with `.to_i64()` made the full Vulkan-font dependency parse probe pass; it now reaches the known later HIR fallback and exits zero. A cache-preserving two-worker Stage 4 through the conservative seed path stayed CPU-active near 1.3 GiB RSS for 900 seconds but produced no artifact, cache update, or diagnostic and was terminated at the established ceiling. A non-delegating Stage 2 capability probe against the existing one-file native hello fixture likewise produced zero cache files/artifact and grew to 3.2 GiB RSS in five minutes, so Stage 2 remains ineligible for fallback promotion. Both processes were terminated without clearing the main 616-object cache. The remaining blocker is self-host native-build/entry-closure progress, not font parser syntax; no retry, deployment, commit, or push followed.
- verify-progress-2026-07-13-native-build-boundary: Source inspection confirmed `_native_build_entry_closure` already computes a deduplicated import closure and passes only those files to the driver; changing `driver.spl` to repeat closure selection would be incorrect. A final 60-second Stage 2 probe enabled both `--verbose` and `SIMPLE_NATIVE_BUILD_TRACE_CLOSURE=1` for the one-file fixture, but emitted zero bytes before timeout: neither the native-build banner nor the closure-entry trace executed. The stall boundary is therefore command/runtime initialization before `cli_native_build`, not the closure queue itself. The three-cycle guard was reached; no speculative source change, Stage 4 retry, deployment, commit, or push followed.
- verify-progress-2026-07-13-hosted-argv-stage4: Corrected the prior boundary: Stage 2 is `bootstrap_main.spl` and calls hosted `rt_native_build`, so pure-Simple closure traces cannot execute. Hosted Rust tracing proved process argv was empty in the native binary; `rt_native_build` silently discarded its valid Simple array, substituted defaults, and discovered 10,879 files with `entry_closure=false`. It now uses process argv only when it contains `native-build`, otherwise falling back to the passed RuntimeValue array. After rebuilding `libsimple_native_all.a` and Stage 2, `native-build --help` returned immediately and a non-delegating one-file capability build discovered one file, compiled/linked in about 1.5 seconds, ran successfully, and printed `windows native hello`. The wrapper now performs this isolated runnable capability gate before promoting Stage 2. Stage 4 then refreshed 1,651 cached objects but core-C linking lacked hosted compiler/GPU symbols. The deeper runtime selector was fixed to authorize `SIMPLE_BOOTSTRAP_STAGE4=1` only for `src/app/cli/main.spl`, with a regression test, and the compiled Stage 2 branch now requests `rust-hosted`. The third cycle stopped immediately at the earlier `native_all` CLI bundle gate, which still checks only `SIMPLE_BOOTSTRAP`; it must also recognize `SIMPLE_BOOTSTRAP_STAGE4` before the entry-specific selector can enforce safety. No further retry, deployment, commit, or push followed.
- verify-progress-2026-07-13-stage4-runtime-coverage: Aligned the native-all CLI bundle parser with `SIMPLE_BOOTSTRAP_STAGE4` through a tested pure predicate; the exact-entry authorization remains enforced by the deeper selector. Stage 4 then selected native-all and reached link. Whole-archive alone did not help because the hosted archive does not contain the C runtime exports. The Stage 4-only linker now adds the existing core-C runtime archive after native-all; this reduced unique undefined references from 115 to 87 and removed the prior OpenCL and Engine2D SIMD unresolved set. Remaining unresolved coverage spans broader optional/runtime surfaces (ROCm, oneAPI, OpenGL, SQLite, font, JSON, SDL2, path/JIT helpers, and a few Simple symbols). The third cycle stopped at this link failure. Cache now holds 2,686 objects; no deployment, commit, or push followed.
- verify-progress-2026-07-13-stage4-runtime-manifest: The native-all front gate now accepts `SIMPLE_BOOTSTRAP_STAGE4`, the archive is selected, and whole-archive routing is active in both Stage 4 branches. Adding the existing core-C supplement after native-all reduced unique unresolved references from 115 to 87, confirming archive composition is the active boundary. The remaining set is 70 `rt_*` runtime exports plus 17 Simple/lowering symbols. `generate_stub_object` intentionally filters `is_system_symbol`/compiler-provided runtime names, so strict Stage 4 correctly refuses to hide missing runtime coverage. The remaining fix must extend the hosted runtime manifest/archive for real runtime exports and separately resolve the 17 Simple/import/lowering gaps; broad unresolved-ignore flags or per-symbol dummy stubs are not acceptable. The third cycle was exhausted at link; the 2,686-object cache and logs remain preserved, with no deployment, commit, or push.
- verify-progress-2026-07-13-stage4-rebased-runtime: Fetched GitHub and rebased the isolated working revision onto `main` (`5560658c1096`) without file conflicts or tracked-file loss. A refreshed Rust seed/native-all archive rebuilt Stage 2 successfully; its isolated direct native-build capability produced and ran `windows native hello`. Strict-mode stub generation incorrectly discarded compatibility trampolines even when a concrete mangled Simple definition was present; strict mode now retains only aliases proven against the linked definition set, with a focused passing regression. Stage 4 therefore resolved `run_check`, `run_arch_check`, and one iterator alias without weak-stubbing missing code. A Stage4-only C runtime archive now includes the existing real font and SQLite owners, while the core compatibility owner supplies real CRC32, exclusive-create, and fsync implementations; its archive-symbol regression passes. Across the three bounded Stage4 cycles, unique unresolved references fell from 88 to 61 (46 runtime, 15 Simple/import). Remaining runtime groups are unavailable/absent ROCm, oneAPI, OpenGL, SDL2, Intel Engine2D, HTTP/JIT/path/sleep, and bitmap-font APIs. Remaining Simple groups include two absent `theme_sync_*` modules wired into the CLI, re-export closure misses for JSON/mem-tracker/storage conversion, one iterator alias, and `danger`. No Stage4 artifact was deployed and no commit/push was made because AC-13 still fails. Logs and exact symbols are preserved in `build/native_probe/stage4-hosted-runtime.log` and `build/native_probe/stage4-hosted-runtime-unresolved.txt`.
- verify-progress-2026-07-13-stage4-closure-runtime-round2: Added real theme-sync modules; recursive function-local import discovery; `danger:` transparent-block parsing; text `rfind` builtin resolution; optional-backend unavailable contracts; hosted HTTP/path/glyph/sleep implementations; and compiler-owned JIT cleanup. Corrected CRC32/sync/exclusive-create to the native `(ptr,len)` text ABI. Focused parser/HIR/MIR, compiler check, JIT cleanup, strict C, embedded-NUL file, and local HTTP/download checks pass. Three bounded bootstrap cycles then advanced Stage 2 from parser failures in `ast_convert.spl` and reserved `function` locals through `from shared.contracts` dispatch, with each focused parser regression fixed. The third cycle reached semantic compilation but failed on `interpreter/core/contract.spl` (`ANY.error_postconditions` field inference), `interpreter/parser.spl` (exclusive capability alias), and `nogc_async_mut/perf.spl` (`ComponentStats.get_metric` returns f64 through an i64 signature). The underlying closure regression is the conservative bare-export fallback in `native_project/discovery.rs`: queuing every sibling of each reached `__init__.spl` pulls unrelated interpreter/perf modules into the bootstrap entry closure. Stage 4 therefore fell back to the seed worker path and again produced no object/cache progress before termination. The mandatory three-cycle cap is exhausted for this session. No Stage 4 artifact, canonical deployment, AC-13 PASS, commit, or push was produced. The next fix must resolve bare exports to their actual sibling owner symbols instead of including every sibling, then rebuild in a fresh bounded session.
- verify-progress-2026-07-13-stage4-precise-closure: Replaced all-sibling bare-export expansion with symbol-owner matching, recursive re-export facade following, and traversal of `Extend` plus other body-bearing AST nodes. Fixed terminal `danger:` pending-statement draining, Stage 2 capability fallback under `set -e`, native HTTP request-target control rejection, and failed exclusive-create cleanup. Focused parser, discovery, shell, strict-C/runtime, and combined Rust checks pass. One fresh `--full-bootstrap --deploy --no-mcp` cycle rebuilt the Rust seed/native-all, then passed Stage 2, Stage 3, and the isolated Stage 2 native-build capability check. Strict Stage 4 reached link and failed only on three standard-trait compatibility aliases, five mem-tracker exports hidden behind re-export facades, and LLVM terminfo symbols. Exact trait-alias lowering now reuses concrete builtin methods without double-evaluating receivers; mem-tracker facade discovery and Linux hosted-native-all terminfo linkage have focused passing regressions. Per the one-cycle continuation limit, Stage 4 was not rerun. Stage 2/3 artifacts exist, but no full CLI, canonical deployment, AC-13 PASS, commit, or push was produced. The next bounded cycle must rebuild these final Rust fixes and re-enter strict Stage 4.
- verify-review-2026-07-13-stage4-precise-closure: Highest-capability review found and the merge owner fixed four follow-up defects: top-level `danger:` EOF now drains pending statements; aliased re-exports expose only the alias; HTTP methods must be tokens and URLs reject spaces/control bytes; exclusive-create now copies the authoritative `(ptr,len)` path, rejects embedded NUL, and uses the same path for cleanup. Exact parser, negative facade-alias, receiver-once MIR, strict runtime focus, and both C-owner compile checks pass. No second bootstrap cycle was started.
- verify-progress-2026-07-13-stage4-cache-identity: A fresh full-bootstrap cycle again passed Stage 2, Stage 3, and the runnable Stage 2 capability probe. Strict Stage 4 now links terminfo and includes the precise mem-tracker Simple owner; unresolved symbols fell to four real `spl_memtrack_*` exports plus one stale `DoubleEndedIterator.rfind` reference in unchanged `access_store.spl`. The Stage4 hosted C archive now includes `runtime_memtrack.c`, with an exact archive-symbol regression passing. The remaining rfind object was reused from the native cache even though HIR already has a passing regression for the exact `text.replace(...).rfind(...)` source shape. The bootstrap-wide cache identity previously covered platform/env only, so compiler/runtime implementation changes could reuse stale objects. It now includes the complete Rust/runtime seed input fingerprint and all Simple compiler source hashes; the next source-consistent run will clear and rebuild the cache automatically. Three attempted MIR recovered-local test cycles did not prove an alternate workaround and the workaround was removed. No second bootstrap, deployment, commit, or push followed.
- verify-progress-2026-07-13-stage4-content-cache: Highest-capability review found stale archive-member and mtime-only reuse risks. Stage4 C archives now require a deterministic input-content fingerprint and delete the prior archive before replacement; the focused fingerprint regression passes. A source-consistent full bootstrap cleared the native cache, passed Stage 2, Stage 3, and the runnable Stage 2 capability check. Stage 4 resolved all four `spl_memtrack_*` exports and now fails only on `DoubleEndedIterator.rfind` from `access_store._parent_dir`, proving the earlier object was not solely stale. That path now uses the established text API `last_index_of("/") ?? -1`, avoiding the trait-default collision without weakening strict linking. The cycle cap is reached, so the final Stage 4 rerun, deployment, AC-13 PASS, commit, and GitHub push remain pending.
- verify-progress-2026-07-13-stage4-deployed: Three bounded bootstrap cycles fixed uppercase `String.trim().is_empty()` lowering and the pre-deploy source-check worker path. Stage 2, Stage 3, the runnable native-build capability check, strict Stage 4 link, candidate `-c`/source-check smokes, and canonical deployment now pass; `bin/simple` resolves to `bin/release/x86_64-unknown-linux-gnu/simple`. The GPU/NIC/DMA system spec passes 8/8 and the complementary policy/hardware-claim spec passes 19/19. Explicit-file test subprocess resolution now preserves `SIMPLE_BINARY` across the client, daemon, and single runner instead of executing `.spl` argv paths. Native integration verification then exposed that the Rust driver routes every explicit `.spl` file to the interpreter-only light client and drops `--mode=native`; pressure/swap consequently failed on interpreter-only `rt_mmio_write_u8`, not on a memory oracle. A mode-aware dispatch fix and focused unit regression are staged but not run because the three-cycle cap is reached. Native effects, QEMU, remaining gates, final review, commit, and push remain pending.
- verify-progress-2026-07-13-qemu-native-runtime: Modern VirtIO-GPU discovery incorrectly treated the optional `VIRTIO_PCI_CAP_PCI_CFG` capability (`cfg_type=5`) as an MMIO capability and failed on its zero BAR before reaching common/notify capabilities. Restricting BAR mapping to types 1 and 2 produced a full checker PASS with real VirtIO-block sector write/read swap, VirtIO-GPU backing/transfer/flush, and VirtIO-net RX/TX DMA lifecycle markers. Cached Stage 4 linked and was deployed after replacing one unsupported `ExactSizeIterator.is_empty` lowering in the lint helper with direct string length checks. The explicit-native dispatch Rust regression passes. The pure-Simple `--mode=native` path still compiles SMF rather than an ELF and timed out; direct real native compilation then proved `simple-core`/`core-c-bootstrap` lack `rt_bytes_alloc`, hosted `rt_invlpg`, and `serial_println`. Owner-level core-C wrappers and archive assertions are staged and syntax-clean, but native rebuild verification reached the three-cycle cap. Canonical `bin/simple check src/compiler` and `check src/lib` both segfault immediately, while Stage 2 `check src/compiler` reached the 300-second ceiling. Direct environment/runtime guards pass in working and staged modes, and the generated-spec layout guard is clean. AC-13, final review, commit, and push remain pending.
- verify-progress-2026-07-13-native-elf-runner: The core-C archive gate exposed five pre-existing required string-builder exports in addition to the three memory-leveling wrappers. A real amortized-growth C builder implementation, lifecycle tests, and exact missing-symbol diagnostics now make the focused core-C archive ABI test pass. The first direct pressure native-build reached its 29-file import map and crashed in LLVM module creation; GDB localized the fault to `LLVMModuleCreateWithNameInContext`/`llvm::DataLayout::reset` on `block_device.spl`. A process-wide LLVM mutex was tested, did not change the crash, and was removed to avoid a performance regression. A full-bootstrap refresh rebuilt the Rust seed/native-all and Stage 2 through Cranelift, then hit the outer 20-minute ceiling during active Stage 3 work. The refreshed Stage 2 reproduced the LLVM fault, exhausting the three native-build cycles. The pure-Simple runner no longer aliases native mode to SMF: compile mode retains SMF, while native mode builds a core-C Cranelift ELF, executes it directly under the existing limits, and removes its wrapper/binary artifacts. The focused runner source check passes with the bootstrap seed; the canonical self-hosted checker still segfaults. The required MCP interpreter smoke did not reach the spec because the deployed launcher delegated to the seed/light-client path and recursively invoked `simple test` at `SIMPLE_TEST_DEPTH=1`. The next cycle must rebuild this runner, execute the pressure/swap native test once, and repair or bypass that launcher recursion in the canonical smoke. No final review, commit, or push followed.
- verify-progress-2026-07-13-cross-module-native-traits: A direct Stage 2 Cranelift build compiled and linked the 29-file pressure/swap closure, proving the core-C ABI repairs are effective, but execution correctly failed the existing duck-dispatch trap because `BlockDevice` and `impl BlockDevice for MemBlockDevice` live in separate native compilation units. Native project discovery now records the project-wide trait-to-concrete-type map and seeds MIR dispatch analysis while leaving vtable generation owned by the implementation unit; the focused discovery regression passes. The link also warned that `unsafe_addr_of` was stubbed even though `vmm_copy_bytes_to_phys` requires the actual array-header address, so core-C now owns an identity ABI implementation with C and archive assertions. C syntax and the focused core-C archive test pass. Two attempted debug-driver proofs routed into the pure-Simple native-build app and were terminated; source inspection established that an explicit `--target x86_64-unknown-linux-gnu` is the documented switch that forces the rebuilt Rust handler. The three native-build attempts are exhausted for this cycle. Generated wrappers/binaries were removed. The next cycle must use that explicit target once, run the resulting ELF, then promote through bootstrap and the canonical runner. No final review, commit, or push followed.
- verify-progress-2026-07-13-optional-struct-native: The explicit-host Cranelift route compiles all 29 pressure/swap units with zero failures and no generated fallback symbols after adding the remaining core-C array, option, math, CR3, volatile, and barrier owners. Whole-project import discovery now retains named structs nested in optional/pointer return types, and `Some(mapping)` receives the concrete `MemoryRuntimeMapping` payload type across the real three-module import chain. `OptionSome` is materially wrapped by codegen, so `rt_enum_payload` remains required; a direct-subject experiment was disproved by codegen inspection and removed. The focused same-module and cross-module HIR regressions pass. Native tracing now proves `prepare_swap(allocation=1, owner=99)` and identifies the next owner: the test MMIO store retained page-table region metadata but lost nested array element updates after reallocation, leaving the leaf PTE zero. The MMIO test backend now uses sparse flat address/value stores for 8/64-bit values, matching its existing 16/32-bit design and avoiding nested native array mutation. Its source check passes. The three native cycles are exhausted before the sparse-store ELF rerun; no end-to-end PASS, deployment, commit, or push is claimed.
- verify-progress-2026-07-13-primitive-return-native: The sparse-MMIO ELF reached all eight swap-sector writes. GDB then proved `memory_vmm_commit_swap` received `current_pte=0x203007`, `source_phys_addr=0x203000`, and `source_pte=0x203007`, yet returned `stale-mapping`. Disassembly showed the compiler comparing raw `(vmm_read_pte(...) & PTE_ADDR_MASK)` against `txn.source_phys_addr << 3`: native-project import discovery recorded only named project return types, so the imported primitive `vmm_read_pte -> u64` degraded to `ANY` and triggered mixed tagged/raw equality lowering. The whole-project return map now retains every declared return type while preserving its existing conflicting-bare-name rejection; focused primitive-map and typed-comparison regressions pass 2/2, and the prior optional-struct return regression still passes 1/1. The rebuilt Rust driver is ready. The final bounded native attempt omitted the required explicit `--target x86_64-unknown-linux-gnu`, routed into the interpreted pure-Simple worker, and timed out before codegen. The three-cycle cap is exhausted, so no fourth build was run. The next cycle must invoke the rebuilt driver once with that explicit target, `--no-incremental`, and `core-c-bootstrap`, then run the produced ELF. No end-to-end PASS, commit, rebase, or push is claimed.
- verify-progress-2026-07-13-mmio-last-write-native: The explicit-target Rust handler compiled and linked the pressure/swap closure fresh (29/29, zero failures). Its first ELF proved the primitive-return fix: swap-out wrote all eight sectors, `stale-mapping` disappeared, the non-present-PTE assertion passed, restore returned reason `ok`, and `last_restore_code` became zero. The fault handler still returned `-1` because the post-restore PTE was corrupted (`0xf000000000040e00`) despite `vmm_map_page(virt=0x72000000, phys=0x207000, flags=0x7)`, proving native indexed overwrite of the flat global `u64` MMIO test array is unsafe. The test backend was converted to append-only values with latest-write lookup, but both reverse-loop implementations (`len - 1` with signed termination, then nonnegative countdown) failed to find the initial PTE in native mode and regressed swap-out to `source-not-present`. The MMIO owner source check passes; a new focused SPipe contract exists, but the seed direct test runner segfaulted before test output. Three native cycles are exhausted. Next retain append-only writes but restore the already-proven forward loop, scan all entries while remembering the last matching index, then run one explicit-target ELF. No end-to-end PASS, canonical gate, commit, rebase, or push is claimed.
- verify-progress-2026-07-13-typed-words-and-optional-returns-native: Forward last-match MMIO lookup restored swap-out and page-fault restoration. GDB then proved generic typed-word growth mixed tagged inline values with raw `rt_typed_words_u32/u64_push` fallback values. Core-C and simple-core now encode generic-array words as tagged runtime integers while retaining raw packed-array storage; focused C growth/get/set and packed-high-bit checks pass. A fresh 29/29 ELF advanced through critical pressure, one successful swap, non-present PTE, restore reason `ok`, and fault return zero. Its next trap was not manager replacement: `MemoryLevelingManager.get` returned a valid raw allocation pointer, then caller `.unwrap()` invoked `rt_enum_payload` and converted that non-enum pointer to nil. `get` and every successful `vmm_translate` path now return explicit `Some(...)`; both owner files pass source checks. The final bounded build was mistakenly run with stale `src/compiler_rust/target/bootstrap/simple` (built before the project-wide trait fix) and reproduced the old `BlockDevice` duck-dispatch trap before reaching swap. It is not evidence against the optional-return fix. The next single native cycle must use the current rebuilt `src/compiler_rust/target/debug/simple`, retain explicit `--target x86_64-unknown-linux-gnu`, `--no-incremental`, and `core-c-bootstrap`, then run the resulting ELF. No fourth build, end-to-end PASS, canonical gate, commit, rebase, or push is claimed.
- verify-review-2026-07-13-typed-word-abi: Highest-capability review found that core-C typed-word accessors decoded raw ABI index `8` as tagged index `1`, and pure-Simple unchecked/data-pointer word reads returned tagged slots without decoding. Typed u32/u64 at/set/known paths now retain raw indices; generic unchecked/data-pointer reads decode tagged slots while packed-u64 reads remain raw. C syntax, pure-Simple owner checks, and the focused C runtime executable pass, including u32/u64 get/set at raw index `8`. The pressure/swap native rebuild remains deferred to the next bounded session because this session's three native cycles are exhausted.
- verify-review-followup-2026-07-13-typed-word-abi: Final highest-capability review found generic negative u64 loads used unsigned heuristic decoding and core-C known data-pointer stores always wrote raw words. Generic u64 loads now use arithmetic tag decoding; known stores select tagged generic or raw packed representation from the supplied header. The focused C runtime executable passes signed `-1`, generic known-store/raw-slot, and packed known-store checks. No pressure-native retry was performed.
- verify-progress-2026-07-13-native-byte-layout: Three bounded explicit-target Cranelift cycles used the current debug driver and compiled the 29-file pressure/swap closure fresh with zero failures. The first release ELF no longer crashed: slot reuse passed and the primary scenario reached its final byte oracle. An unstripped cycle proved physical bytes were initially read as zero because Cranelift's trusted `[u8]` accessor forced packed addressing on a generic module-initialized array. Cranelift now skips only type validation while retaining the runtime packed/generic flag branch; core-C fallback byte accessors match, and focused compiler plus generic/packed C checks pass. The third ELF restored the exact expected bytes (`02 04 06 08 0a`) and every metadata, fault, state, and translation assertion passed, but the scenario still reports failure because `rt_bdd_expect_eq_rv` delegates distinct arrays to identity-only `rt_native_eq`. GDB proved both final arrays have flags `8`, length `5`, and byte-identical data. The memory/swap implementation is correct at this boundary; the remaining direct-contract blocker is the native SPipe `to_equal` array comparator. The three-cycle cap is exhausted, so no comparator edit/rebuild, canonical gates, commit, rebase, or push followed.
- verify-review-2026-07-13-native-array-equality: Highest-capability review confirmed the wrapper and byte-content oracle are correct and that general native value equality is the owner. Core-C `rt_native_eq` and Rust `heap_value_eq` both omit arrays, while the standard spec framework requires separately allocated equal arrays to compare structurally. The next bounded fix must add guarded structural array equality in both runtimes, route core-C BDD array operands through it, cover packed/generic and cycle-safety behavior, then rerun the pressure ELF once. Do not weaken the memory oracle or patch only the generated wrapper.
- verify-progress-2026-07-13-native-array-equality-pass: Core-C and Rust value equality now compare arrays structurally across packed-byte, packed-u64, and generic tagged storage, preserve float and enum semantics, and bound recursion with ancestor-pair cycle detection. Core-C raw byte/tuple indices no longer reinterpret positive multiples of eight as tagged integers, and little-endian byte access preserves packed/generic layout. Focused C coverage passes raw index `8`, signed/mixed words, float zero, enums, forged handles, cycles, and 300 sibling arrays; the Rust equality regression passes 1/1. The rebuilt debug driver compiled the 29-file explicit-target Cranelift/core-C pressure closure fresh, and its ELF passes exactly 2/2 scenarios with zero failures, including byte-exact swap restoration and slot reuse. Native pressure/swap is closed; canonical source/MCP/LSP gates, final review, commit, rebase, and push remain pending.
- verify-progress-2026-07-13-null-array-len-bootstrap: Highest-capability review and deployed disassembly identified the canonical checker crash as unchecked native inlining of `rt_array_len`: both Cranelift and LLVM masked the array tag and loaded offset `8` without guarding a raw-null module global. The backends now return zero before that load, and Rust `rt_array_len` matches the null-safe core-C ABI. A full bootstrap rebuilt the previously stale Rust seed, Stage 2 machine code contains the null branch, Stage 3/native-build and Stage 4 passed, and the corrected CLI was deployed. `bin/simple check src/compiler` now checks all six top-level files without crashing; its exit is the unrelated repo-hygiene gate (23 pre-existing forbidden-extension files plus Git-only checks in this jj worktree). Required lib/MCP/LSP checks no longer crash but expose a broader lexer token-text defect (`malformed integer literal` diagnostics across unrelated files), and the MCP interpreter smoke stops at the existing `SIMPLE_TEST_DEPTH=1` recursion guard. A fresh canonical QEMU checker run did not reach QEMU because kernel native-build segfaulted; prior VirtIO serial evidence is substantively complete but stale after the current `MemoryLevelingManager.get -> Some(...)` source change. The three rebuild/verify cycles are exhausted. AC-13, fresh QEMU provenance, final PASS, commit, rebase, and push remain open.
