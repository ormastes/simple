# Feature: Windows Full Bootstrap and Toolchain Suite

## Raw Request
$sp_dev and bootstrap, do phase 1 build and run test on compiler/interpreter, run test on tools, run tools in insterpreter, build binary and run tools primary features. with each phase simple. to full bootstrtap. and redeploy to local. check, mcp, spipe plugin, devhub, caret suite, ide, t32 mcp/cli. ocutionally check main about fixes. push when phase 1 pass with tools and phase 2 so on. when each phase compiler passed on windows to main to share fixes. self approve with admin.

## Task Type
todo

## Refined Goal
Produce, verify, incrementally publish, and locally deploy a provenance-bound full Simple bootstrap on Windows whose compiler, interpreter, essential CLI tools, MCP/SPipe/DevHub/Caret/IDE/T32 integrations, and primary binary workflows pass at every applicable phase without seed fallback or unreviewed conflict loss.

## Acceptance Criteria
- AC-1: Reconcile the current six jj conflicts against the fetched `main@origin`, preserving unrelated user/agent changes, and record a clean lane-specific diff plus pre/post tracked-file counts before any phase evidence is accepted.
- AC-2: Create a retained phase matrix naming Phase/Stage 1 through Stage 4 inputs, outputs, absolute binary paths, SHA-256 hashes, source revision, compiler provenance, supported commands, isolated cache/output paths, and promotion authority; every absent or stale field fails closed.
- AC-3: Run the smallest applicable target/provider/SCI projection from `doc/07_guide/compiler/minimal_bootstrap_configuration_composition.md`; Phase 1 must build successfully on Windows and retain its receipt without silently substituting the Rust seed for pure-Simple compiler, interpreter, test, run, SPipe, or deployment evidence.
- AC-4: Against the exact Phase 1 binary, run one focused compiler check/test set and one focused interpreter check/test set, including authenticated interpreter execution evidence where applicable; both must pass once with retained commands, exit codes, logs, binary hash, and source revision.
- AC-5: Against the exact Phase 1 binary, exercise supported tool paths in interpreter mode and test the tools themselves; unsupported commands must be explicitly reported and may not fall back to another compiler/runtime.
- AC-6: Build native binaries for the applicable Phase 1 tools and run each tool's primary feature with an output/behavior oracle, proving that the executed file hash matches the recorded phase artifact rather than a wrapper, raw source entrypoint, stale cache, or seed binary.
- AC-7: Repeat AC-3 through AC-6 for each promoted phase through the full Stage 4 bootstrap, consuming only the immediately admitted prior phase and retaining a lineage chain; each promotion stops on the first failing compiler, interpreter, tool, provenance, or no-fallback gate.
- AC-8: The Stage 4 candidate must pass `scripts/check/check-bootstrap-essential-tools-smoke.shs` with its required test-runner, lint, duplicate-check, and aggregate markers, plus the exact-candidate post-bootstrap SSpec using absolute `STAGE4_POST_BOOTSTRAP_BINARY` and adjacent provenance.
- AC-9: Verify the deployed candidate's core `check`, compiler, interpreter, and representative run/test behavior on Windows, including the required compiler/core/lib and MCP/LSP checks applicable to changed code, without counting a cross-build or Linux-only result as Windows evidence.
- AC-10: Verify the exact candidate's primary MCP server and Simple LSP MCP startup/request paths, cache/index behavior, invalidation, warm startup, representative request latency, and max RSS; build and run native MCP package smokes when packaging or deployment paths are changed.
- AC-11: Verify the SPipe plugin and `/sp_dev` routing, run representative SPipe execution/docgen behavior through the admitted Stage 4 CLI, and prove generated manuals remain operator-readable with no executable `*_spec.spl` under `doc/06_spec`.
- AC-12: Verify DevHub and the Caret suite's primary startup, routing, and representative request/message workflows with the exact candidate, retaining bounded logs and explicit failure or unavailable classifications.
- AC-13: Build and run the IDE's primary launch and one representative edit/check interaction with the exact candidate; GUI-host or platform prerequisites that are unavailable remain active blocked rows with an owner and exact resume command, never PASS by source inspection.
- AC-14: Build and run T32 MCP and T32 CLI primary features, including server/CLI startup and a non-destructive representative command; unavailable TRACE32/container/provider state must be reported as blocked with retained diagnostics and may not be replaced by a stub.
- AC-15: Before each phase publication, fetch GitHub and compare the exact tested head with `main@origin`; incorporate relevant fixes by linear jj rebase, preserve concurrent lanes, rerun only invalidated evidence, and enforce the tracked-file-count guard.
- AC-16: Publish each Windows phase only after its compiler plus phase-scoped tool gates pass: commit only intentional lane files, set/update `main` linearly, push the narrow bookmark/ref without force, and retain the pushed SHA plus CI/required-check result. A failed phase is not pushed as passing.
- AC-17: For protected publication, run `spipe self-review-guide`, perform a fresh exact-head high-effort review with zero P0/P1 findings, and use `SPipe Self Review Admission` rather than an author `APPROVED` review. Document that GitHub forbids author approval, admission is not provider/independent approval, ordinary code/text is eligible absent deny/constrain, and scopes are `code`, `text`, `file`, `directory_files`, and `directory_recursive`; drift/expiry requires fresh review+dispatch, deny requires policy-owner/independent review, scope rejection requires a smaller diff/new constraint, and unsafe material requires removal and credential rotation.
- AC-18: Follow bootstrap platform readiness Gate 1–6 in order: Stage 3 admission; x86_64 Linux Stage 4; frozen candidate sanity/hash; four essential-tool markers; deployment plus `sh scripts/bootstrap/rollback-bootstrap-deploy.shs <canonical-triple>` receipt; then selected native/QEMU/platform acceptance. Independent preparation or another Stage 3 owner cannot waive a missing admission, and unavailable non-Windows hosts remain open/blocked.
- AC-19: Redeploy only the admitted Stage 4 Windows candidate locally, retain pre/post/deployed hashes, run arithmetic and primary-tool smoke checks, then prove the rollback procedure via its dry-run/self-test and command/exit/pre/post/restored-hash receipt before declaring deployment ready.
- AC-20: Use default-off diagnostics for passing gates; use `--diagnostics=test` only for progress/coarse timing and `--diagnostics=debug` only for a scoped failure investigation. Any AOP trace requires a separate named pattern and rationale, and diagnostic output alone cannot promote a phase.
- AC-21: Stop after at most three distinct fix/verify cycles for one failure, never rerun an unchanged passing criterion or an identical failed command, and record every unresolved issue with exact failure, file/line when known, owner, unblock condition, retained artifacts, and resume command.
- AC-22: Update affected research, requirements, architecture, design, system-test plan/spec/manual, agent plan, and `doc/07_guide` bootstrap/tool documentation; update both feature- and layer-expert `skill.md` wiki entries; record unfixed gaps under `doc/08_tracking/bug`; and ensure must-check ledger v3 rows have named owners with actionable unblock conditions (`none` only for PASS).
- AC-23: Because this lane changes workflow/tooling/evidence contracts, update affected `doc/06_spec`, `.codex/skills`, `.agents/skills`, `.claude/skills`, `.claude/agents/spipe`, `.claude/commands`, and `.gemini/commands` artifacts, or mark each N/A with a concrete reason; generated SSpec documentation must be reviewed as an operator manual.
- AC-24: Final verification must run each applicable acceptance oracle once, including working/staged direct-env/runtime guards, required core/MCP/LSP checks, full release-bound `bin/simple test test --whole --mode=interpreter`, bootstrap readiness, Stage 4 essential-tool smoke, deployment/rollback evidence, and a completion audit covering every named suite; only then may the umbrella goal be marked complete.
- AC-25: Every eligible native/bootstrap build must set `SIMPLE_NATIVE_INCREMENTAL=1`, use a stable phase-specific `--cache-dir` or canonical incremental cache, and retain the `[native-incremental] N reused / M rebuilt` receipt; missing receipts or `N=0` are classified as cold/inconclusive rather than incremental, and `--fresh-cache`/clean rebuilds are forbidden except when an explicit final release gate requires them.

## Scope Exclusions
- Destructive physical-device writes, firmware flashing, and unsupported hardware operations are excluded unless separately authorized with exact identity/challenge safeguards.
- A Rust seed, stale deployed wrapper, raw-source entrypoint, cross-build, or synthetic/stub provider cannot serve as Stage 4, deployment, release, or unavailable-host evidence.
- Unavailable external hosts/providers are not excluded from the goal; they remain explicit open or blocked acceptance rows with resume plans.

## Cooperative Review
- Sidecar lanes after shared contracts are frozen: compiler/interpreter/bootstrap lineage; CLI/MCP/SPipe/DevHub/Caret; IDE/T32/platform/deployment evidence.
- Merge owner: primary Codex session `/root`.
- Final reviewer: normal/highest-capability Codex exact-head review; lower-model findings cannot authorize phase promotion, generated-manual acceptance, or completion.
- Shared interfaces: `BootstrapPhaseReceipt`, `ToolPrimaryFeatureReceipt`, `SuiteAcceptanceRow`, `DeploymentRollbackReceipt`.
- Manual flow helpers: `step("Admit the phase compiler and provenance")`, `step("Check compiler and interpreter behavior")`, `step("Run essential tools in interpreter and native modes")`, `step("Verify integrated tool suites")`, `step("Deploy and prove rollback")`, `step("Review and publish the exact phase head")`, `step_bootstrap_platform_handoff_readiness`.
- Setup/checker helpers: `setup_windows_bootstrap_phase`, `check_phase_provenance`, `check_phase_compiler_interpreter`, `check_phase_tool_matrix`, `check_integrated_suite_matrix`, `check_local_deploy_and_rollback`.
- Any temporary helper must fail explicitly with `assert(false)` or `fail(...)`; placeholders and unsupported rows cannot emit PASS.
- Generated-manual review owner: primary Codex session `/root`, after sidecar evidence is merged.

## Phase
design-done

## Log
- dev: Created state file with 24 acceptance criteria (type: todo); no build, conflict resolution, or readiness claim was performed in this refinement phase.
- research: User selected F1 strict incremental phase promotion and N1 release-grade provenance/bounded performance; final feature/NFR requirements written and option drafts removed.
- design: Highest-capability primary merged three read-only design slices; created receipt-state-machine architecture/TLDR, detail design, system-test and agent plans, intentionally RED executable SSpec, and authored operator manual with REQ-001..014 and NFR-001..010 coverage.
- implementation-policy: User required incremental builds always; AC-25 added and all future eligible builds must prove positive cache reuse.
- bootstrap-authority-review: Windows rustup authority parser fix reviewed and focused resolver regression passed. Legacy `default_host_tuple` and modern `default_toolchain` schemas are supported; conflicting, duplicate, malformed, missing, wrong-channel, and invalid-host settings remain fail-closed. No phase or acceptance criterion is complete from this review.
- bootstrap-incremental-attempt: One canonical Windows MSVC incremental attempt used `SIMPLE_NATIVE_INCREMENTAL=1`, `incremental-unlimited`, four jobs, and the stable `build/bootstrap` output/cache. It terminated with exit 1 at `starting -> fingerprint -> exit-1`; retained error is `resolver-frontend=absent`. No Cargo/native stage ran and no build/cache admission or `[native-incremental]` reuse receipt was produced. Existing cache contents were preserved.
- bootstrap-next-action: Three-cycle guard requires stopping this failure lane. Before any retry, add and validate a hostname-bound canonical POSIX PATH containing the installed rustup cargo frontend (`/c/Users/ormas/.cargo/bin/rustc` and `cargo`) while preserving the MSVC/LLVM tool authorities; then rerun only as a newly scoped attempt.
- bootstrap-run-74832-closeout: Canonical four-worker incremental MSVC run 74832 reached Rust seed/native/runtime/compiler-backfill completion, then terminated exit 1 at `bootstrap-from-scratch.sh:2597` during `starting -> fingerprint -> exit-1` because `resolver-frontend=absent`; retained logs/receipts remain under `build/bootstrap`, with no Stage 2/admission or cache-reuse receipt. Host diagnostics confirm GNU diffutils 3.12 `cmp` exists at ambient `/usr/bin/cmp`; the hostname-bound configuration now declares the canonical MSYS2 utility directory, but applied admitted-PATH proof is still open.
- comparator-fix-review: Shared canonical/hash-bound `cmp` authority, fail-closed status classification, caller/composite distinctions, receipt cleanup/atomicity, symlink/shadow/hash-drift rejection, and hermetic binary-safe fixtures are focused-tested PASS; independent review PASS. No phase or acceptance criterion is complete from this fix.
- bootstrap-three-cycle-stop: The comparator/resolver failure lane has reached the three-cycle guard; do not rerun unchanged checks or restart the failed command.
- bootstrap-next-action: Run exactly one newly scoped canonical host-env -> MSVC incremental bootstrap retry using the existing `build/bootstrap` output/cache, after validating the applied hostname-bound PATH. Preserve caches and retain the terminal milestone, first failure/PASS, and reuse receipt; do not rerun prior checks.
- bootstrap-git-authority-blocker: Retry session 91580 passed rustc/cargo/cmp binding and all Rust seed/native/runtime/compiler-backfill milestones, then failed before Stage 2 at `could not bind Stage 3 git HEAD/dirty state`; both the materializer's `not a git repository` warning and the authority failure were caused by `git`/`jj` absent from the filtered PATH, not by repository topology. Verified Git is `C:\dev\tool\Git\cmd\git.exe`; the hostname config now adds only `/c/dev/tool/Git/cmd` (Scoop jj shims intentionally excluded). Focused oracle pending: host-env -> MSVC chain must resolve Git, and `git -C` must report this repository's toplevel and HEAD; then capture `bootstrap_stage3_git_state` into a bounded temp receipt. No phase/AC completion or admission claim.
- bootstrap-stage2-retry-closeout: Retry session 91580 first cleared the rustc/cargo/cmp authority chain and completed the Rust seed, native-all, runtime, and compiler-backfill milestones. Adding canonical `/c/dev/tool/Git/cmd` to the hostname-bound PATH then fixed the missing-Git diagnosis: `git -C` resolved the repository toplevel and HEAD. The remaining Stage 2 admission failure is therefore not missing Git. On the Windows materialized checkout, HEAD contains 116 mode-120000 aliases; `git ls-files --others --exclude-standard` exposed 42,357 paths and the Git-state helper queued 24,801 regular files in `.files-only.1800`. The bounded external oracle ran from epoch 1788959100 through 1788959130, exited 124 after 30 seconds, emitted no final receipt, and left seven temporary residues. The bootstrap ended exit 1 with `could not bind Stage 3 git HEAD/dirty state`, so Stage 2 was not admitted and no cache-reuse or phase-promotion receipt was earned.
- materialization-receipt-part1: Receipt-producer work reached the three-cycle limit. The latest focused cycle still fails with the exact oracle `created directory link is invalid: dir-link`. Part 1 is not verified or complete; no materialization receipt may be consumed or cited as admission evidence. Per the three-cycle guard, do not retry the unchanged producer implementation.
- materialization-receipt-consumer: No consumer work has started. `bootstrap_stage3_git_state` has not been changed to verify a producer receipt, collapse admitted junction/hardlink aliases to their canonical Git symlink identities, or retain typed path-count/elapsed/failure cleanup evidence. Do not begin consumer edits while the producer's Windows junction identity is unresolved.
- bootstrap-next-action: **Astra review of Windows junction target identity/receipt producer before further edits.** The review must resolve the `dir-link` target-identity oracle and the receipt producer contract before another implementation or verification cycle. Phase remains `design-done`; AC-1 through AC-25 remain incomplete, including AC-3 Stage 2/Phase 1 admission and AC-25 positive incremental-reuse evidence.
- materialization-receipt-astra-closeout: Astra review sessions completed scoped source audits and retained the consolidated report at `.spipe/windows_full_bootstrap_toolchain_suite/astra_materialization_receipt_review.md`. The report confirms that the producer compares resolved junction path names rather than directory identity, identifies unsafe PowerShell path interpolation and non-byte-safe Git enumeration, incomplete failure/hash proof, predictable temporary/publication races, pending-fixture policy mismatch, and unresolved reparse-ancestor/provenance guarantees. The review was static where noted and does not constitute producer admission.
- materialization-receipt-producer-progress: Part 1 has positive fixture progress for materialized file aliases and receipt shaping, and sequential fixture failures were narrowed and corrected rather than run concurrently: fixture setup/index restoration, tracked-missing versus optional-pending classification, quoting/Unicode transport, and ordinary-directory rejection were treated as separate oracles. These corrections do not close the producer. The current receipt-ancestor oracle still fails exactly at `test ! -e .../real-output/new`.
- materialization-receipt-reviewer-finding: The remaining failure is explained by create-before-validate behavior in `Parents`: the implementation creates a missing destination suffix before it has finished validating and holding the existing ancestor prefix, so the negative receipt-ancestor fixture observes `.../real-output/new` on failure. The reviewer therefore cannot establish whether the created path inherited the intended provenance or crossed a substituted/reparse ancestor. No producer PASS, authenticity claim, or stable provenance claim is available from the current implementation.
- materialization-receipt-three-cycle-stop: The sequential producer fixture lane has reached its three-cycle limit. Stop without another unchanged rerun. Consumer work remains unstarted; `bootstrap_stage3_git_state` still does not consume or verify the materialization receipt. Stage 2 remains unadmitted, Phase remains `design-done`, and AC-1 through AC-25 remain incomplete.
- materialization-receipt-next-action: Add the failing `test ! -e .../real-output/new` precondition, then implement a two-phase parent algorithm: first validate and hold every existing prefix without creating anything; only after that validation succeeds, create and verify each missing suffix under the held parent identity. Perform one focused rerun only after both phases are implemented. Do not start the consumer or retry Stage 2 before this producer oracle passes.
- materialization-receipt-astra-two-phase-session: Astra completed the bounded two-phase producer session. The candidate path decomposition, double-component handling, and preflight ordering defects identified by the prior review were corrected so existing prefixes are selected and checked before any missing suffix is eligible for creation. These source changes are candidate work only; they do not establish a producer PASS or authorize consumer integration.
- materialization-receipt-two-phase-current-failure: The latest focused producer oracle fails exactly with `receipt.destination-probe:win32=3` while probing a destination whose suffix parent is intentionally absent. This is now the first unresolved boundary. The result does not prove that suffix creation is unsafe or that the destination is invalid; Win32 status 3 must first be attributed to the probe's expected missing-parent semantics versus an implementation error.
- materialization-receipt-two-phase-stop: This focused producer lane has reached the three-cycle limit. Do not rerun the producer fixture, begin a consumer test, or retry Stage 2 on the unchanged diagnosis. Producer admission remains incomplete, consumer work remains unstarted, Stage 2 remains unadmitted, Phase remains `design-done`, and AC-1 through AC-25 remain incomplete.
- materialization-receipt-next-action: **Astra read-only diagnosis of destination probe behavior when the suffix parent is intentionally absent.** The diagnosis must establish which existing-prefix handle/path the probe is authorized to query, how Win32 error 3 is classified before suffix creation, and the fail-closed transition into create-and-verify. No producer or consumer test rerun is permitted before that diagnosis is recorded.
- materialization-receipt-destination-probe-astra: Astra completed the read-only destination-probe diagnosis and the bounded follow-up fix. Win32 error 3 was attributed to probing the intentionally absent suffix parent rather than the already-existing held prefix. Candidate selection was corrected so missing suffixes do not become preflight probe targets. A persistent status-0 ambiguity was then investigated: the prior command/text route could report success without proving that an existing ancestor was not a reparse point.
- materialization-receipt-reparse-guard: The producer now uses a direct `GetFileAttributes` reparse-point guard on every existing ancestor selected during preflight, before suffix creation. This is candidate source work only. It narrows the intended oracle to `ancestor.reparse` and avoids treating a successful path lookup or status 0 as directory-identity/provenance proof.
- materialization-receipt-current-harness-blocker: The latest focused command exited 127 with the exact terminal `fail: command not found`. The test harness invoked an undefined failure helper before reaching the receipt-ancestor producer exercise. Therefore this cycle provides no positive or negative evidence about the direct reparse guard, suffix creation, receipt publication, or `ancestor.reparse` classification.
- materialization-receipt-harness-three-cycle-stop: The current focused lane has reached the three-cycle limit. Stop without changing the producer or rerunning the same harness. Producer status remains unverified; consumer work remains unstarted; Stage 2 remains unadmitted; Phase remains `design-done`; AC-1 through AC-25 remain incomplete.
- materialization-receipt-next-action: Define and use the focused test's existing failure helper, with no producer change, then perform exactly one focused rerun to verify that the receipt-ancestor fixture reaches the producer and fails with `ancestor.reparse`. Do not start consumer integration or retry Stage 2 before that single oracle is obtained.
- materialization-receipt-astra-win32-lexical-diagnosis: Astra's follow-up separated two causes that had been conflated. Win32 error 3 came from querying a destination component whose parent was intentionally absent, while a distinct lexical conversion defect emitted a one-backslash component form that changed how the Windows probe interpreted the candidate. The producer's component conversion was corrected to preserve the intended single path component without manufacturing an invalid probe target.
- materialization-receipt-ancestor-behavior: After the one-backslash component fix and harness-helper correction, the receipt-ancestor behavioral fixture reached the producer and passed its intended oracle: the reparse ancestor was rejected as `ancestor.reparse` before the missing suffix could be created or a passing receipt published. This is a focused behavioral PASS only, not full producer acceptance.
- materialization-receipt-current-structural-failure: The remaining focused harness failure is exactly line 274's stale `grep -Fc` assertion for the removed wrapper-owned `mkdir`. The current wrapper contract delegates missing-suffix creation to the two-phase producer after existing-prefix validation, so the old text-count assertion no longer describes the implementation even though the receipt-ancestor behavior is green.
- materialization-receipt-structural-three-cycle-stop: The focused producer lane has reached its three-cycle limit. Stop without another unchanged run. Producer status remains not fully PASS; consumer work remains unstarted; Stage 2 remains unadmitted; Phase remains `design-done`; AC-1 through AC-25 remain incomplete.
- materialization-receipt-next-action: Update only the line-274 structural assertion to the current wrapper/two-phase producer contract, with no producer change, then run the focused producer test exactly once. Do not begin consumer integration or retry Stage 2 until that focused producer test reaches a terminal PASS.
- materialization-receipt-focused-pass-and-enhancements: After updating the stale wrapper structural assertion, the focused producer test reached a terminal PASS milestone. Subsequent enhancement cycles added target metadata capture, strict 64-hex validation for the producer and policy digests, producer/policy rechecks before publication, and a final Git HEAD recheck. These later changes extend the evidence contract beyond the previously passed candidate, so that milestone does not qualify the current enhanced producer as fully PASS.
- materialization-receipt-current-fault-digest-failure: The exact current focused failure is the `fault=digest` fixture at its expected-diagnostic assertion: `grep -Fq 'invalid materialization record digest character' "$tmp/last.log"` returns nonzero. The injected invalid digest is being consumed by an earlier producer/policy or target-metadata hash call instead of the intended materialization-records hash boundary, so the fixture exits before emitting the asserted records-digest diagnostic. This is a fixture-hook scope failure, not evidence that invalid records digests are accepted.
- materialization-receipt-enhancement-remaining-work: Target metadata and the producer/policy 64-hex checks, rechecks, and final HEAD binding are implemented as candidate work. Remaining fixture/contract items are: define the exact four-entry, hash-bound pending-target allowlist; assert the exact decoded target metadata rather than only receipt shape; and scope both injected hash/digest hooks exclusively to the `*/records` operand so producer, policy, and target metadata hashing remain authoritative. The actual repository-wide 116-link materialization/receipt run has not been attempted and no claim is made for its runtime, completeness, or digest.
- materialization-receipt-enhancement-three-cycle-stop: The enhancement lane has reached the three-cycle limit. Stop without another unchanged run. The enhanced producer remains unverified; consumer work remains unstarted; Stage 2 remains unadmitted; Phase remains `design-done`; AC-1 through AC-25 remain incomplete.
- materialization-receipt-next-action: Patch only the focused fixture and pending-policy assertions: encode the exact four-entry allowlist with bound hashes, add exact decoded metadata assertions, and constrain both fault hash/digest hooks to `*/records`. Then run the focused producer test exactly once. Do not run the real 116-link checkout, start consumer integration, or retry Stage 2 until that focused enhanced-producer oracle passes.
- materialization-receipt-enhancement-synthetic-status: The producer-enhancement evidence remains synthetic-fixture-only. The exact four-entry pending-target allowlist is retained unchanged and hash-bound by policy; exact decoded target-metadata assertions and both digest/hash fault hooks scoped only to `*/records` are now implemented in the focused fixture. Producer/policy 64-hex validation, pre-publication digest rechecks, final HEAD recheck, and target metadata fields are present. None of this is an actual-tree admission: the repository's 116 mode-120000 links have not been run through the enhanced producer.
- materialization-receipt-astra-atomic-metadata: Astra completed the atomic target-metadata session and retained `.spipe/windows_full_bootstrap_toolchain_suite/astra_atomic_target_metadata_implementation.md`. The candidate advances the receipt to schema v2, captures target metadata from held handles, rejects reparse/replacement changes, encodes final UTF-8 `\\?\C:\...` paths, and records `-` as the directory content SHA. Consumers require explicit v2 support and none has started.
- materialization-receipt-atomic-metadata-test-setup-failure: The sole focused invocation failed during test setup before producer execution. Exact terminal evidence is `mkdir: cannot create directory ‘/c/Users/ormas’: Permission denied` followed by `FIRST FAILURE: case=positive line=21 command=mkdir -p "$repo" exit=1`. The attempted TMPDIR was repository-relative, so the MSYS fixture reconstructed an unauthorized `/c/Users/ormas` path. This run provides no producer, native metadata, replacement-race, receipt, or policy verdict.
- materialization-receipt-handle-lifetime-concern: Read-only review still cannot prove that each target handle used to capture v2 identity remains held through final HEAD/digest rechecks and receipt publication. If handles are released after row construction, target replacement can make individually accurate metadata stale before the receipt becomes authoritative. This provenance concern remains open for independent code review; schema v2 and final HEAD alone do not close it.
- materialization-receipt-atomic-metadata-three-cycle-stop: The enhancement lane remains at its three-cycle stop. Do not rerun with the same TMPDIR or infer PASS from source inspection. Producer v2 remains unverified; consumer work remains unstarted; Stage 2 remains unadmitted; Phase remains `design-done`; AC-1 through AC-25 remain incomplete.
- materialization-receipt-next-action: First perform an independent v2 code review, including target-handle lifetime through publication and the exact four-entry allowlist/decoded-metadata/records-only-hook boundaries. Only after that review, apply the canonical host environment and invoke the focused test once with a fresh explicit absolute `MATERIALIZER_TASK_TMP=/c/Users/ormas/AppData/Local/Temp/<task>` (exporting its `cygpath -u` value as TMPDIR). Do not run the actual 116-link tree, begin the consumer, or retry Stage 2 before that single focused result is classified.
- materialization-receipt-v2-producer-pass: The focused schema-v2 producer test reached terminal PASS under the exact canonical host temporary root `TMPDIR=/c/Users/ormas/AppData/Local/Temp/simple-materialize-root-canonical`. This establishes the synthetic producer fixtures only. Independent static review of the v2 contract and atomic publication is retained in `.spipe/windows_full_bootstrap_toolchain_suite/astra_atomic_target_metadata_review.md`; it does not replace the behavioral evidence or qualify the unresolved real checkout.
- materialization-receipt-consumer-part2: Part 2 implemented the strict schema-v2 consumer in `bootstrap_stage3_git_state`, including receipt environment wiring, native revalidation, directory-only exclusions, bounds, and atomic Git-state publication. Static review found the intended fail-closed contract present. The focused integration test then passed under exact `TMPDIR=/c/Users/ormas/AppData/Local/Temp/simple-materialize-part2-da7b08e1fcbf4873be7c013e20a9ce35` with terminal evidence `bootstrap_stage3_git_state_materialized=true`. This is synthetic integration evidence, not Stage 2 admission.
- materialization-receipt-actual116-blocked: The actual checkout's 116 mode-120000 entries were not passed through the producer. Nine of those paths are dirty ordinary leaves whose current bytes differ from their canonical targets, so automatic materialization would overwrite or redirect unresolved work. The exact inventory, hashes, timestamps, and decision boundaries are retained in `.spipe/windows_full_bootstrap_toolchain_suite/dirty_alias_owner_decision.md`; no owner could be reliably attributed from process/timestamp evidence.
- materialization-receipt-owner-decision-required: Before the actual-116 producer run, an owner must select independently for each of the nine paths: (1) preserve the ordinary leaf and keep bootstrap blocked; (2) after an independently verified backup, merge intended edits into the canonical target, then restore/materialize the alias and verify the merged target hash; or (3) after an independently verified backup, discard the ordinary leaf and restore/materialize the HEAD alias. No option is selected or authorized by this state update.
- materialization-receipt-next-action: Obtain and record the owner choice for every row in `dirty_alias_owner_decision.md`, execute only those approved backup/merge-or-discard/restore operations in a separately authorized lane, and re-inventory all nine before considering the actual-116 producer invocation. No phase or acceptance criterion is complete: Stage 2 remains unadmitted, Phase remains `design-done`, and AC-1 through AC-25 remain incomplete. No bootstrap retry or publication is authorized by the synthetic producer/consumer PASS results.
- dirty-alias-independent-review: Independent content/history review concludes that all nine dirty ordinary mode-120000 leaves are stale historical or conflicting snapshots. Six were directly classified from retained patch/history evidence, and three prior merge proposals were corrected to no-merge. The safe canonical-target merge hunk set is empty for every path. Retain `.spipe/windows_full_bootstrap_toolchain_suite/dirty_alias_patches/` and its recommendation/proposal/diff artifacts as recovery and review evidence.
- dirty-alias-restore-plan: Owner approval remains mandatory because replacement discards live worktree bytes. Immediately before action, revalidate the nine recorded hashes and confirm no active owner/open-write evidence. Only after explicit approval, replace each leaf with the exact HEAD placeholder target bytes via scoped `apply_patch` or the scoped mechanical restore owner, materialize the restored aliases, verify their canonical targets, and then run the actual 116-link schema-v2 producer to retain its hash-bound receipt. Any hash drift, owner claim, or new open-write evidence stops the action.
- dirty-alias-next-action: Obtain explicit owner approval for the destructive nine-path restore plan in `dirty_alias_owner_decision.md`; no merge into canonical targets is proposed. Until approval and restore/materialization complete, the actual-116 producer remains blocked, consumer evidence remains synthetic-only, Stage 2 remains unadmitted, Phase remains `design-done`, and AC-1 through AC-25 remain incomplete.
