**Recommend A for non-authorizing inventory; keep bootstrap admission blocked.** B is unsound for current-worktree fidelity if it preserves dirty bytes only as backups while replacing their active paths. An immutable snapshot is useful only if those ordinary files remain ordinary at the same paths.

The producer correctly refuses ordinary/tampered leaves ([producer:956](C:/Users/ormas/dev/simple/scripts/setup/materialize-symlinks-windows.shs:956)). V2 cannot accommodate blocked rows: its exact states and count equation require materialized or pending entries ([contract:78](C:/Users/ormas/dev/simple/doc/08_tracking/bug/windows_materialized_symlink_alias_git_state_timeout_2026-09-09.md:78)).

- **A:** Sound for preserving and fingerprinting actual dirty source; unsound if interpreted as successful alias materialization. Hash bytes **and actual file type**, retaining the HEAD symlink identity separately. A file replacing a directory alias cannot provide descendants. An ordinary file replacing a file alias supplies its own content, not its target’s. Hashing proves neither compiler usability nor symlink equivalence.
- **B:** A HEAD checkout alone omits dirty/untracked work. Copying then overwriting dirty alias paths changes effective source even when recovery copies exist. Corrected B preserves those active paths, independently copies bytes without hardlinks to the live tree, and materializes only proven placeholders. It therefore retains the nine conflicts.

**Minimal contract and responsibilities**

Introduce a separately versioned inventory schema; retain strict v2 admission unchanged initially. Require exactly one row per HEAD alias, sorted and unique. Blocked rows contain path, HEAD mode/blob/target bytes, observed ordinary-file identity/size/SHA-256, `state=blocked-dirty-ordinary`, and `authorizable=0`; no target-equivalence metadata. Header counts must reconcile, with `result=blocked` whenever any blocked row exists.

Producer preflights all leaves before mutation, preserves blocked bytes, and publishes atomically without replacement. Consumer independently verifies row coverage, schema, policy and filesystem identity; never collapses blocked paths. It hashes canonical target contents and genuine untracked inputs once. Directory identity is not a subtree digest ([contract:98](C:/Users/ormas/dev/simple/doc/08_tracking/bug/windows_materialized_symlink_alias_git_state_timeout_2026-09-09.md:98)).

Bind repository identity, HEAD, index entries/stages, working-tree types/deletions/raw bytes, untracked inventory, and explicit ignored-input policy into a source manifest. HEAD/diff alone cannot establish coherent capture. Require enforced quiescence or atomic capture; before/after equality misses intermediate changes. Snapshots additionally bind origin and effective manifests, transformation map, tool/policy digests and immutable root; builds read only that root, with outputs elsewhere.

Reject nested/escaping/cyclic aliases initially, including nested repositories and case collisions. Hardlinks differ from symlinks after target replacement; junctions require contained identity. Revalidate before use; pending absence remains mandatory ([contract:107](C:/Users/ormas/dev/simple/doc/08_tracking/bug/windows_materialized_symlink_alias_git_state_timeout_2026-09-09.md:107)).

**Migration/tests/stops:** Inventory all nine without modification. Owner resolution must explicitly retain ordinary source or merge edits into targets after verified independent backups; never auto-restore. Test byte preservation, directory conflicts, nested aliases, target replacement, concurrent capture, forged rows, snapshot isolation and recovery. Bound enumeration before alias expansion, batch hashing, enforce the documented 30-second limit; stop on races, unsupported topology, budget exhaustion or unresolved conflicts.

**Blockers:** consumer integration remains absent ([authority:2790](C:/Users/ormas/dev/simple/scripts/check/lib/bootstrap-stage3/authority.shs:2790)); retained v2 testing failed before execution ([report:13](C:/Users/ormas/dev/simple/.spipe/windows_full_bootstrap_toolchain_suite/astra_v2_contract_final_implementation.md:13)). No files changed or tests run.