# REVIEWED PATCH REVISION — NO TARGET HUNK
# Leaf: .codex/commands/research.md
# Target: .codex/skills/research/SKILL.md
# Decision: restore the alias; merge no leaf text into the target.
#
# The original proposal read the no-index diff backwards. The target already
# contains the claimed common-SPipe locator, authorized-scope, ownership/setup,
# and bounded-wiki guidance at lines 20-27. The leaf is the preceding snapshot
# and has no unique setup or authorization rule. The target matches AGENTS.md
# research ownership, self-sufficiency, and preserve-other-research policy.
#
# Safe merge hunks: none.
# Rejected: none as independent edits; differences are missing newer guidance.
# Leaf end state: mode 120000, exact text ../skills/research/SKILL.md with no
# trailing newline, after owner approval and hash revalidation.
