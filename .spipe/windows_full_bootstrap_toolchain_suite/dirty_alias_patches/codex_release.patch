diff --git a/.codex/commands/release.md b/.codex/commands/release.md
--- a/.codex/commands/release.md
+++ b/.codex/commands/release.md
@@
-# Release — Codex Version Bump and Tag
+# Protected Simple Release
@@
-Run `verify` skill first — must show STATUS: PASS.
+Use protected candidate, exact-head, and promotion checks before release.

Recommendation: stale command leaf versus protected release skill. Preserve no
unique leaf semantics: the canonical target supersedes the 4261-byte ordinary
copy. Owner should back up then restore/materialize the 4764-byte target.
