# REVIEWED PATCH REVISION — NO TARGET HUNK
# Leaf: .claude/commands/spipe.md
# Target: .claude/skills/spipe.md
# Decision: restore the alias; merge no leaf text into the target.
#
# The original proposal read the no-index diff backwards. The target already
# contains every claimed addition: Modern SSpec/manual-first (line 12), DB
# tiers (line 76), self-delegation diagnosis (line 98), protected self-review
# (line 151), typed evidence (line 842), and exact effect oracles (line 1524).
#
# Safe merge hunks: none. The leaf-only late in-development section duplicates
# the newer target contract at lines 718-722. Its RenderDoc instruction to
# build/use src/compiler_rust conflicts with AGENTS.md: default tools must use
# the pure-Simple self-hosted binary and the Rust seed is bootstrap-only.
# Other differences are older forms already revised in the target.
# Leaf end state: mode 120000, exact text ../skills/spipe.md with no trailing
# newline, after owner approval and hash revalidation.
