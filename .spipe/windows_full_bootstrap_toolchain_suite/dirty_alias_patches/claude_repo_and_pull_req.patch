# REVIEWED PATCH REVISION — NO TARGET HUNK
# Leaf: .claude/commands/repo_and_pull_req.md
# Target: .claude/skills/repo_and_pull_req.md
# Decision: restore the alias; merge no leaf text into the target.
#
# The original proposal read the no-index diff backwards. The target already
# contains the proposed search aliases and exact-head SPipe Self Review
# Admission routing at lines 8-13, plus newer L2/L3 semantics at lines 35-36
# and 45. The leaf's “bot-approves” and “wait for human approval” language is
# stale: same-author credentials cannot provide provider APPROVED, and account
# type is not proof of human operation.
#
# Safe merge hunks: none.
# Rejected: legacy L2/L3 and corresponding Jira help/error wording.
# Leaf end state: mode 120000, exact text ../skills/repo_and_pull_req.md with no
# trailing newline, after owner approval and hash revalidation.
