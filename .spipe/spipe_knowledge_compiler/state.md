# Feature: SPipe Knowledge Compiler

## Current authoritative status (2026-08-26)

- Overall: `IN PROGRESS`; full acceptance criteria are not satisfied.
- Wave 1 accepted and pushed: `185f3303282`.
- Wave 2 accepted and pushed: `deccbce964e`.
- Wave 3 accepted and pushed: `5e2a049eb89`.
- Wave 4 design accepted; implementation is partial. The checked common BM25
  scorer `2b9f25f8604` is an admitted foundation only.
- DBFS facade, canonical JSON decoder/emitter, Unicode/analyzer integration,
  full provider/parity work, and the native identity candidate are not admitted.
- Waves 5–11 are unstarted. All five system SSpecs/manuals remain honest RED:
  four use `DESIGN-SCAFFOLD` and provider parity uses `NOT-EVIDENCE`.
- Merge owner: `/root`. Final admission requires an independent
  highest-capability review and the exact focused runtime evidence for the lane.

This summary supersedes older attempt-local status wording below; the history
is retained as an append-only audit log and must not be read as current truth.

- implementation-wave4-raw-rrf-stop: Froze a pure two-file JavaScript raw-RRF
  kernel, excluding identity dominance, graph construction, semantic models,
  and adjusted reranking. Ordinary math/order passed static review, but the
  third and final cycle ended syntax PASS, focused 10/11, high review FAIL.
  Blockers are accessor/proxy validation TOCTOU, collapsed error precedence,
  incomplete closed-object validation, missing adversarial numeric/default
  coverage, and one incorrect reordered-source oracle. No commit/push; retained
  candidate `/tmp/spkc-js-rrf-wt` is `NOT-EVIDENCE`.
- implementation-wave4-raw-rrf-accepted: A fresh scoped session replaced the
  rejected candidate's TOCTOU/error/schema defects with once-only descriptor
  snapshots and complete hostile/default/boundary coverage. Evidence: static
  xhigh PASS; syntax PASS; focused 16/16 in cycle 2; independent final xhigh
  PASS. Exact two-file commit pushed as `595ba6e449`. This admits raw fusion
  only; identity dominance, graph candidates/proximity, bounded adjustments,
  integrated explanations, and AC-4 remain open.
- implementation-wave4-rerank-accepted: Added the authority-bound page-local
  fixed-policy reranker and root-level fusion unit-test discovery bridge. Evidence:
  syntax PASS, focused 13/13, full SPipe suite PASS (117/117 unit; Wave 2 9/9;
  Wave 3 25/25; Wave 4 9/9; legacy/perf PASS), and pre-runtime/final xhigh PASS.
  Exact three-file commit pushed as `44e65a6713`. Exact identity dominance,
  graph candidate production, exposed search/provider integration, global pool
  completeness, and AC-4 remain open.
- implementation-wave4-complete-pool-v2-accepted: Added additive complete-pool
  fusion/rerank v2 while preserving v1. Complete source/count/digest envelopes
  admit up to 3,000 internal hits; reranking precedes the 1,000 public cap.
  Evidence: syntax PASS, focused 38/38, full SPipe suite PASS, independent xhigh
  PASS, including raw-rank-1001 to final-rank-793 promotion. Exact four-file
  commit pushed as `32574ab884`. Identity/graph/provider integration and AC-4
  remain open.
- implementation-wave4-exact-identity-accepted: Added a snapshot/receipt/scope-
  bound exact resolver over authorized canonical UID/key/active-alias rows.
  Evidence: syntax PASS, focused 8/8, full SPipe suite/perf PASS, final xhigh
  PASS. Exact two-file commit pushed as `d1b601697f`. Graph/provider/fusion
  orchestration and AC-4 remain open.
- implementation-wave4-rerank-pair-v3-accepted: Added ordered edge/receipt pair
  evidence with shared-receipt support, derived display sets, evidence digest,
  and verifier implementation binding while preserving v1/v2. Evidence: static
  xhigh PASS, syntax PASS, focused 26/26, full package 142/142 plus Wave/legacy/
  perf PASS, final xhigh PASS. Exact two-file commit pushed as `f89b120be7`.
  Graph candidate generation and integrated AC-4 remain open.

## Raw Request
`$sp_dev imlldoc/01_research/infra/spipe/spipe_knowledge_compiler.md with pherallel agents with detail guide and higher model review. make design plan details. sync push often gh main`

## Task Type
feature

## Refined Goal
Implement the selected SPipe Knowledge Compiler direction end to end as a portable, dependency-free SPipe core with stable artifact identity, typed traceability, deterministic hybrid search and virtual views, transactional refactoring, constrained rebalancing, common-knowledge promotion, Simple-native provider integrations, operator documentation for every delivered command, and verified compatibility with standalone SPipe, the Simple host, legacy MCP stdio, negotiated MCP 2026 HTTP, file-only materialized-view clients, linked projects, and concurrent worktrees.

## Acceptance Criteria
- AC-1: Final feature and NFR requirements explicitly preserve every selected decision and acceptance criterion in `doc/01_research/infra/spipe/spipe_knowledge_compiler.md`, with stable REQ/NFR identifiers and no pending option selection.
- AC-2: Detailed architecture, ADRs, implementation plan, system-test plan, agent ownership plan, and detail design define the canonical lifecycle tree, immutable artifact/section identity, typed graph, provider boundaries, worktree/link behavior, security model, migration strategy, database adapters, and dependency-wave exit gates.
- AC-3: The dependency-free SPipe implementation parses and indexes canonical Markdown, SDN, SSpec, and supported source metadata into deterministic immutable snapshots plus per-worktree deltas, diagnoses duplicate/ambiguous identity, and never treats paths as identity.
- AC-4: Exact/alias lookup, deterministic fixed-point BM25, graph candidates, RRF fusion, explanations, and optional semantic-provider degradation satisfy shared golden-corpus ordering and clean-rebuild versus incremental-update parity.
- AC-5: Read-only `spipe://` lifecycle, feature, component, layer, matrix, trace, project, status, and diagnostic views are available through model-callable MCP list/read/search/resolve/trace/diagnostic tools and bounded materialized `.spipe/view/` output; artifact virtual files resolve to exactly one canonical artifact UID, while aggregate directory/search/trace/diagnostic outputs carry a synthetic projection UID bound to one immutable snapshot.
- AC-6: Legacy MCP stdio remains compatible and the target stateless MCP 2026 transport exposes deterministic pagination and visibility-safe cache hints without authorization leakage.
- AC-7: Approved artifact, section, tag, feature, and component rename/move operations use hash-preconditioned journals, atomic application, alias preservation, incremental reindexing, verification, recovery, and rollback; unapproved virtual-view writes fail closed.
- AC-8: Research-to-result traceability uses typed, provenance-bearing edges; strict profiles accept only explicit/generated accepted evidence, retain TRC231/TRC232 compatibility, diagnose missing/stale cross-project and cross-worktree evidence, and preserve accepted trace through refactors.
- AC-9: Tree audit and hybrid rebalancing preserve fixed lifecycle roots and all hard constraints, produce deterministic connected virtual communities without churn, and restrict physical moves to explainable approved proposals with rollback maps.
- AC-10: Common-knowledge discovery combines exact, fingerprint, lexical, structural, graph, and optional semantic evidence; promotion requires provenance, conflict review, consuming-project validation, and safe `extends`/override behavior.
- AC-11: One canonical skill/rule source generates the tracked Claude, Codex, Gemini, and `.agents` surfaces enumerated by the skill compiler manifest; generated files identify source UID/version/hash, semantic-equivalence fixtures cover every manifest target, and stale generated surfaces fail verification.
- AC-12: Simple provides canonical shared-search/provider/source-symbol and textual/embedded/server database adapters with deterministic parity, snapshot/capability isolation, exhaustive-versus-WAND equivalence, and no mandatory dependency from SPipe to Simple.
- AC-13: Executable SSpec scenarios and mirrored Markdown manuals cover navigation, search, trace, refactor failure injection, worktree/link isolation, rebalance stability, promotion safety, protocol compatibility, and provider parity using real assertions and built-in matchers; manuals are usable without opening spec source.
- AC-14: Performance, privacy, path traversal, symlink/junction escape, cache-scope, authorization, transaction recovery, and scale fixtures have named verification evidence; unavailable optional semantic or OS-mount providers degrade safely and are not counted as PASS.
- AC-15: Existing SPipe CLI/setup/link/doctor behavior remains compatible, no new baseline Node runtime dependency is introduced, and changed Simple code passes focused lint, duplicate, check, test, runtime-facade, MCP/LSP, and package smoke gates applicable to its scope.
- AC-16: Knowledge updates ship with implementation: the research document is preserved; architecture/design/plan/spec/manual/guide artifacts are current; feature- and layer-expert wiki entries describe reachable behavior; unresolved gaps have tracked bug/TODO records with file/line, owner, and actionable unblock condition; workflow surfaces under `.codex`, `.agents`, `.claude`, and `.gemini` are updated when their contract changes.
- AC-17: Final highest-capability review verifies all sidecar outputs, generated-manual quality, exclusions, done marks, security boundaries, and every AC against authoritative current-state evidence; at most three distinct verify/fix cycles are used and unchanged green checks are never rerun.

## Delivery Criteria
- DC-1: Each verified increment is committed alone, rebased linearly onto `main@origin` with before/after tracked-file count protection, and pushed to GitHub `main` with unrelated concurrent work excluded.

## Scope Exclusions
- FUSE/ProjFS implementation is deferred unless MCP resources/tools, materialized views, and editor-provider evidence prove insufficient; its adapter contract and decision gate remain designed.
- Remote embeddings and automatic LLM acceptance are optional providers and never required for core correctness or strict trace compliance.
- Physical tree moves and common-knowledge publication are never automatic.

## Cooperative Review
- Sidecar lanes: repository/current-state audit; requirements and trace matrix; architecture/security/worktree design; search/provider/database design; MCP/virtual-view design; refactor/rebalancer/promotion design; SSpec/manual/guide design; implementation lanes partitioned by published interfaces.
- Merge owner: primary Codex `/root`; only the merge owner edits shared schemas, state, cross-lane indexes, and integration manifests.
- Final reviewer: normal/highest-capability primary Codex after independent higher-capability review findings are returned.
- Shared interfaces: `KnowledgeCompiler`, `KnowledgeSnapshot`, `KnowledgeDelta` (the envelope over `ArtifactDelta`, `GraphDelta`, and `IndexDelta`), `ArtifactRecord`, `SectionRecord`, `TraceEdge`, `DiagnosticRecord`, `RefactorPlan`, `RebalanceProposal`, and `PromotionCandidate`. Internal orchestration uses `LexicalSearchPort`, `SemanticSearchPort`, `SymbolIndexPort`, and `ProjectionPort`; external accelerators implement `SearchProvider`, `SourceSymbolProvider`, or `ProjectionProvider` adapters for those ports.
- Manual `step("...")` flow helpers: `Index canonical knowledge artifacts`; `Browse virtual knowledge views`; `Search and trace artifacts`; `Apply a transactional refactor`; `Audit tree balance and promotion candidates`.
- Setup/checker helpers: `setup_spipe_knowledge_fixture`, `check_spipe_knowledge_compiler`, `check_spipe_provider_parity`, `check_spipe_refactor_recovery`, and `check_spipe_virtual_view_safety`.
- Fail-fast policy: temporary scenario or adapter placeholders must call `assert(false)` or `fail(...)`; placeholder passes, empty bodies, and inferred-only strict evidence are forbidden.
- Generated-manual review owner: primary Codex, after an independent sidecar checks operator readability and trace coverage.

## Phase
design-wave3-contract-review

## Log
- dev: Created state file with 18 acceptance criteria (type: feature); recorded parallel ownership, shared interfaces, manual steps, checker helpers, and highest-capability review.
- dev: `dev-done` denotes completion of goal refinement only, not research, design, implementation, verification, release, or the thread goal.
- review: Converted delivery-only AC-18 to DC-1 and tightened client, projection-identity, and generated-surface acceptance after independent highest-capability review.
- design: Produced final REQ/NFR, plans, architecture, 15 ADRs, integrating and focused designs, operator guide, five deliberately RED executable SSpec scaffolds, and five authored non-PASS mirrors.
- review-cycle-1: Failed on security, transaction, projection, provider, prompt-isolation, promotion, and missing executable-scaffold contracts; corrections applied.
- review-cycle-2: Failed on cross-artifact identity, interface, RRF, materializer, metric, wave, and stale-current-state inconsistencies; corrections applied.
- review-cycle-3: `STATUS: FAIL`; stopped at the mandatory three-cycle cap. Remaining blockers: canonical port/provider vocabulary mapping; one ProjectionUid prefix and example; refactor-safe filesystem capability/API; closed `trust_scope` contract; TLDR provisional-performance wording; DBFS lane/wave ownership; ADR-SPKC-005 trace to REQ-SPKC-014.
- resumed-review-cycle-1: Closed five prior blockers; failed on search adapter injection and exact safe-filesystem API/holder consistency.
- resumed-review-cycle-2: Closed adapter and API/holder contracts; failed on materializer capability issuer/holder wording in the MCP design.
- resumed-review-cycle-3: `STATUS: FAIL`; stopped at the fresh three-cycle cap. Remaining blockers: filesystem port signature/provider visibility must not expose `SafeFilesystem.Materializer`; TLDR must route issuance from `AuthorizationPort`; `RevisionId` dirty-overlay wording must be singular; Wave 10 must exclude completed DBFS migration; ADR-SPKC-013 must require registry-authorized `executable_policy`, not generic reviewed status.
- final-review-cycle-1: Closed provider isolation, RevisionId, Wave-10 DBFS, and ADR trust contradictions; failed only on TLDR capability-issuer wording.
- final-review-cycle-2: `STATUS: PASS`; independent highest-capability review found no implementation blockers. Design phase accepted.
- implementation-wave1-cycle-1: Extracted thin CLI/MCP entrypoints, cohesive command/protocol/transport modules, deterministic result/serialization helpers, and dependency-free compatibility fixtures. Integrated gate found legacy MCP error-ID regression; fixed.
- implementation-wave1-cycle-2: Expanded review found executable-mode loss, incomplete command splits, narrow compatibility evidence, no-ID dispatch drift, and startup regression; fixes applied. Retained P95 gates pass (`version=73.051ms`, `help=73.327ms`, `doctor=102.562ms`, each within 110% baseline).
- implementation-wave1-cycle-3: Compatibility and MCP security pass, but `fine-tune-create-retry a1 a2 retry goal fixture-app` fails because `commandFineTuneRecordRetune` is undefined in `src/cli/fine_tune_status.js:39`. Stopped at mandatory three-cycle cap; Wave 1 remains unaccepted and product changes are not pushed.
- implementation-wave1-resumed-cycle-1: Fixed `commandFineTuneRecordRetune`; integrated workflow exposed adjacent missing `commandFineTuneRecordRequirements`; fixed.
- implementation-wave1-resumed-cycle-2: Syntax, MCP unit, compatibility, and MCP security passed; workflow fixture incorrectly expected `fine-tune-status` exit 0 despite its blocker/WARN contract; corrected to exit 1.
- implementation-wave1-resumed-cycle-3: Syntax, eight MCP unit cases, compatibility, and MCP security passed; workflow fixture still incorrectly expects `fine-tune-next` exit 0 although output is `STATUS: WARN` with `readiness_blocker=model-artifact` and legacy exit is 1. Stopped at mandatory three-cycle cap; update the oracle before rerunning. Wave 1 remains unaccepted/unpushed.
- implementation-wave1-final-cycle: Corrected the `fine-tune-next` oracle, then highest-capability review found and guided fixes for retry-loop dispatch, registry-root preservation, authoritative package build paths, and deterministic transport/SDN serialization. Restored legacy registry formatting/stdout, added regression coverage, and obtained `STATUS: PASS`; build, 9 MCP/serializer unit cases, and expanded legacy workflow pass.
- implementation-wave2-cycle-1: Integrated schema/model, parser/identity, and workspace/storage lanes. Focused tests passed, but high-capability review rejected provisional identity detection, opaque UID validation, missing section deltas, document-controlled trust elevation, linked-project persistence/resolution safety, and bypassable numeric bounds.
- implementation-wave2-cycle-2: Added exact provisional identity handling, opaque canonical IDs, canonical section deltas/candidates, authority-shaped trust decisions, bounded parsers, delegated registry persistence, linked-project diagnostics, and consolidated project-relation keys. Functional evidence reached 36 unit and 8 integration cases; legacy compatibility passed. The legacy help P95 measurement was 101.955 ms against 100.162 ms and was retained without an unchanged retry.
- implementation-wave2-cycle-3: `STATUS: FAIL`; stopped at the mandatory three-cycle cap. Remaining blockers: explicit SSpec scenario/source-symbol IDs are not validated by typed record kind; trust elevation accepts a forgeable caller-created decision instead of a trusted authorization/registry port or verifiable receipt; section/scenario/symbol/alias limits are per-file rather than inventory-wide and inline SDN nesting bypasses the depth limit. Working-tree runtime guard also reports unrelated `src/app/cli/native_build_main.spl` direct runtime calls; staged guard passes. Wave 2 is not accepted or pushed.
- implementation-wave2-resumed-cycle-1: Added typed SS/SY validation and duplicate indexing; remaining-budget parser enforcement across files and metadata/inline SDN; Ed25519 registry receipts bound to canonical source set, project/worktree/revision, policy, capability, principal, expiry, and audit evidence. High review then rejected post-parse aggregate allocation, persisted-root/mount authorization, and relation-UID collision ordering.
- implementation-wave2-resumed-cycle-2: Switched to sequential remaining-budget parsing, validated trusted workspace/project roots and exact mounts through persistence authorization, and validated relation-UID identity before semantic deduplication. High review found linked-project resolution fail-open without an authorizer and authorization occurring after sensitive diagnostics.
- implementation-wave2-resumed-cycle-3: Made linked-project resolution require authorization and moved it before project, revision, trust, mount, and filesystem checks. `STATUS: FAIL`; stopped at the mandatory three-cycle cap because relation lookup still precedes authorization, allowing unauthorized callers to distinguish `relation_missing` from `authorization_denied`. Resume by authorizing the requested relation UID before `_relations.get`, then expose missing/existing state only to authorized callers. Wave 2 remains unaccepted and unpushed.
- implementation-wave2-final-cycle-1: Moved authorization ahead of relation lookup and added indistinguishability evidence for missing versus existing denied IDs. Correctness/security review passed; the sole failure was a noisy legacy doctor P95 result.
- implementation-wave2-final-cycle-2: Aligned the retained performance gate with its original 80-sample qualification and discarded three cold-cache warmups. Evidence: version 54.683 ms, help 56.102 ms, doctor 71.865 ms, all within qualified limits. Independent highest-capability review returned `STATUS: PASS`; Wave 2 accepted for scoped sync.
- sync-wave2: Accepted Wave 2 was integrated from fresh `origin/main` with a non-shrinking tracked-file guard and pushed as `deccbce964ec0b2bad4ba500c8865395b383a6a3`.
- design-wave3-contract: Parallel graph, extraction/diagnostics, and executable-test audits found unresolved node taxonomy, UID namespace, canonical trace-node, node-delta, and snapshot CAS/pin contracts. Merge owner froze ADR-SPKC-016 plus architecture/detail/test/ownership updates before implementation fan-out.
- design-wave3-review-stop: Stopped after the mandatory third high-model review cycle with STATUS FAIL. Remaining blockers are Wave 2 enum-to-graph projection mappings, fully typed trace schemas and canonical span shape, canonical base32 encoding, and deterministic schema-v1 W-to-WS/WT migration derivation. Wave 3 implementation has not started.
- design-wave3-resumed-review-stop: A fresh three-cycle highest-capability review corrected the prior four blockers but ended STATUS FAIL. Remaining blockers are ClassificationRecord/RecordType consistency, deterministic status/title/scenario marker construction, migrated-at revision semantics, architecture/detail provenance naming, explicit Wave 2 EdgeRecord migration/versioning, and source-ownership equality invariants. Wave 3 implementation remains unstarted.
- design-wave3-schema-review-stop: A second fresh three-cycle highest-capability review resolved classification typing, marker blocks, snapshot-based identity migration audit, provenance naming, total legacy-edge migration, and source-owner equality. STATUS remains FAIL on one blocker: Wave 2 Markdown `content_hash` covers normalized LF bytes, while the current SourceSpan text claims raw CRLF-preserving byte offsets. Wave 3 implementation remains gated until the design selects normalized parser-byte spans or introduces a separately migrated raw-object hash.
- design-wave3-accepted: Selected the backward-compatible normalized parser-byte contract. SourceLocation spans and SourceSymbolProvider coordinates use zero-based half-open UTF-8 offsets after CRLF/lone-CR normalization and bind ArtifactRecord.content_hash; other coordinate systems fail SPK406. Fresh highest-capability review returned STATUS PASS with no implementation-blocking contradiction. Wave 3 implementation may begin.
- implementation-wave3-cycle-1: Parallel model, graph/store, extraction/diagnostic, and acceptance-test lanes implemented the typed read-only graph. Initial high review rejected shallow canonical validation, receipt-only authority, early/empty graph publication, mutable capabilities, incomplete source-marker binding, lossy cursors, weak migration validation, memory-only replay, mixed diagnostics, and insufficient adversarial coverage.
- implementation-wave3-cycle-2: Hardened canonical models and endpoint kinds, signed edge receipts, graph-root publication, immutable store-issued stage/pin capabilities, normalized-byte source ownership, resumable traversal, durable replay, canonical diagnostics, and expanded tests. Resumed review found parent-envelope publication ordering, open marker grammars, query-bound gaps, Standard-policy authority, storage-boundary schema validation, and duplicate-trace handling.
- implementation-wave3-cycle-3: Deferred delta publication until the full KnowledgeDelta validates; staged a durable replay receipt; scoped Wave 2 compatibility deduplication while excluding duplicate Wave 3 trace identities; closed Markdown/SSpec marker schemas; required verified Standard evidence; bounded traversal/edge/matrix work with exact continuation; and enforced canonical closed graph-snapshot manifests. Final package verification found only stale storage fixtures; canonicalized their worktree/manifests and CAS sentinel. Evidence: JavaScript syntax PASS, expanded Wave 3 acceptance 25/25 PASS, and affected graph-store suite 10/10 PASS. Awaiting final highest-capability acceptance; no further verification reruns are permitted in this phase.
- implementation-wave3-resumed-accepted: Bound the deterministic canonical Wave 2 + Wave 3 diagnostic union into inventory results, `diagnostics_root`, and KnowledgeDelta diagnostics. Centralized canonical graph bytes as `{schema:1,nodes,edges}`, required the matching CAS object during snapshot publication, and added verified restart-safe graph recovery. Evidence: JavaScript syntax PASS, package verification reached one legacy assertion updated from `details` to canonical `arguments`, its targeted case passed, focused Wave 3/store evidence passed 35/35, and independent highest-capability review returned `STATUS: PASS`. Wave 3 is accepted for scoped sync.
- sync-wave3: Integrated the accepted Wave 3 commit from a fresh `origin/main` worktree with the non-shrinking tracked-file guard (119,571 after versus 119,542 before). JavaScript syntax and 103 Wave 2/Wave 3 unit/integration cases passed on the integration base. Push guards passed and GitHub `main` advanced to `5e2a049eb890c10cfe2f08dd72fbd6b9cb5e4a60`.
- design-wave4-cycle-1: Parallel provider-contract, Simple-search audit, and executable-test lanes froze the canonical `std.common.search` ownership, JS fallback/provider boundary, DBFS compatibility scope, golden corpus, exact scoring/explanation/delta/fallback evidence, and deferred Wave 10 database work. High review rejected uninstantiated Unicode tables, authorization-sensitive corpus statistics, incomplete canonical JSON bytes, underspecified checked BM25 evaluation, and provider/fusion identity conflicts.
- design-wave4-cycle-2: Froze UCD 17.0.0 generated tables/manifest/hash and NFC/lowercase pipeline; authorization-partitioned SearchScopeV1 roots/statistics; `spipe-canonical-json-v1`; checked-i128 BM25/fixed-ln pseudocode; and provider-versus-adapter identities. High review accepted those areas but rejected unbound error envelopes, non-closed per-operation schemas, and implementation-dependent integer acceptance.
- design-wave4-cycle-3: Added separate pre-binding and fully bound error schemas, closed request/result schemas for all ten protocol operations with Wave 4 capability narrowing, and cross-runtime safe JSON integers plus canonical bounded signed-i128 decimal strings. The final reviewer returned only `STATUS: FAIL` without a concrete blocker or evidence. Stopped at the mandatory three-cycle cap; Wave 4 design is detailed but unaccepted and remains uncommitted/unpushed pending a fresh scoped review that must provide falsifiable findings.
- design-wave4-fresh-cycle-1: Fresh independent and adversarial reviewers supplied falsifiable counterexamples. Closed nested explanation records, authorization-scoped document/stat/root shapes, signed query/operation receipt contracts, candidate apply/publish/abort/expiry lifecycle, exact arithmetic/protocol errors, cancellation/deadline/shutdown linearization, and a multi-provider conformance-suite context. Review still found global/scoped shape remnants, zero-TF explanation ambiguity, circular receipt IDs, incomplete error/outcome enums, and open initialization nesting.
- design-wave4-fresh-cycle-2: Made scoped documents/deltas/stats exclusive, introduced scoped content hashes, tagged absent/scored explanation contributions, domain-separated receipt preimages, unified the wire error table, closed candidate terminal states, and closed initialization capabilities/limits/IDs. Review accepted these but found terminal receipt/envelope and expiry-operation contradictions plus duplicate cancel outcomes and a missing legal pre-handshake error envelope.
- design-wave4-fresh-cycle-3-stop: `STATUS: FAIL`; stopped at the mandatory fresh three-cycle cap. Remaining exact blockers: (1) authority-driven expiry needs a separate closed signed authority-expiry record or deterministic `candidate_expire` operation identity; CAS-losing publish/abort requests must receive request-bound terminal errors rather than the winner's differently bound receipt, (2) unknown cancel target must be exactly one success or error outcome, and (3) pre-initialize `handshake_required` must be explicitly legal under a pre-binding response schema without an invented provider generation. All other Wave 4 design areas were accepted by both reviewers. Wave 4 remains uncommitted/unpushed until a new scoped cycle resolves and verifies these three points.
- design-wave4-wire-cycle-1: Added signed `CandidateExpiryReceiptV1`, request-bound CAS-loser handling, one unknown-cancel error, and legal pre-handshake responses. Review independently found and corrected a wrong expiry golden hash, then identified inconsistent authority/policy receipt fields, an unrepresentable receipt-bearing `stale_base`, duplicate initialize schemas, non-atomic terminal persistence, and missing durable loser replay.
- design-wave4-wire-cycle-2: Unified receipt authority/policy bindings, made stale-current-root a signed typed success, added durable terminal-error journaling, and committed terminal record plus receipt atomically. Review accepted those but required the published root pointer to join that same transaction, corrected publish/cancel linearization, aligned the legacy initialize example, and froze non-circular apply/publish payload and candidate-UID preimages.
- design-wave4-wire-cycle-3-stop: `STATUS: FAIL`; stopped at the mandatory three-cycle cap. Both reviewers independently reproduced all normative vector lengths/hashes, including the 880-byte expiry receipt, and accepted publication atomicity, cancellation, receipts, initialization, scoped documents, explanations, and prior contracts. Two exact blockers remain: remove `already_applied` from apply/publish result unions because replay must return the original byte-identical response, and freeze absent-delete behavior because the current successful-no-op promise conflicts with mandatory non-null before revision/hash preconditions. Wave 4 remains uncommitted/unpushed pending a fresh scoped correction and acceptance.
- design-wave4-accepted: Removed the synthetic `already_applied` replay status so identical replay returns the original complete canonical envelope, status, outcome, and signed receipt byte-for-byte across restart. Froze delete as a tagged precondition: non-null/non-null requires an exact present revision/hash, null/null asserts expected absence and yields a durable unchanged-root signed `no_op`, mixed pairs fail before replay or mutation, and a present document under expected-absence returns `precondition_conflict`. Two fresh independent reviewers returned `STATUS: PASS` for the full Wave 4 contract. Both reproduced the canonical absence-delete payload as 245 bytes and SHA-256 `9f3366f906834cadde2ea4b02494f47f8dc80f37418729785841e7209d1d9f08`; all earlier handshake, cancel, expiry, candidate, receipt, atomicity, canonical-byte, scope, explanation, error, bounds, and performance contracts remained accepted. Wave 4 design is accepted for scoped sync and implementation.
- sync-wave4-design: Integrated the accepted three-file Wave 4 design increment from fresh `origin/main`, preserved the tracked-file floor, passed diff/tree/conflict/runtime API push guards, and advanced GitHub `main` to `420c8a5547266c507f7488bb340ff766cec2db7e`.
- implementation-wave4-cycle-1: Parallel JavaScript, Simple common-search, DBFS, golden-fixture, Unicode, and Simple-provider lanes produced a first slice. Initial high review correctly rejected intentional FAIL/NOT-EVIDENCE conformance, incomplete protocol/durability/receipts/cursors/bounds, missing pinned Unicode behavior, lightweight Simple/DBFS contracts, absent provider app, and self-consistency-only goldens.
- implementation-wave4-cycle-2: Added real JS adapter conformance with locked roots/scores/order/explanation hashes; canonical scoped Simple records/provider vocabulary; DBFS exact-length/text-ID/scoped compatibility facade; package integration; pinned official UCD 17.0.0 sources, license, deterministic generator and generated JS/Simple tables; and a typed Simple provider app. Review still found unsigned/incomplete JS lifecycle, unavailable Unicode adapters, incomplete Simple arithmetic/analyzer parity, and partial process dispatch.
- implementation-wave4-cycle-3-stop: JavaScript now uses generated UCD 17 NFC/lowercase/category tables, authenticated bounded cursors, safe BigInt scoring, closed operations, Ed25519 receipts, atomic/fsynced durable candidate lifecycle, cross-process writer locking, reload signature/schema verification, terminal-error replay, and cancellation tracking. Parent evidence passed 24/24 JS/Unicode/integration cases including the complete UCD normalization corpus. Simple common search now uses the generated Unicode APIs, claims `spipe-unicode-lex-v1` only with the pinned hash, and exposes checked BM25 intermediate traces; DBFS focused evidence is 34/34. The Simple process provider gained canonical framing/JSON, exact initialization, injected authority/storage ports, scoped open/apply/publish/explain and signed response persistence, but remains unaccepted: restart reload parses no durable state, only one staged candidate exists, expiry/race/CAS terminal journaling is absent, explanations are incomplete, and before-hash cannot be verified. Golden Simple/DBFS cells and executable SSpec remain FAIL/NOT-EVIDENCE. Stopped this implementation phase without commit/push; resume with those exact process-provider and conformance gaps.
- implementation-wave4-resumed-stop: A scoped durability lane added multi-candidate staged/terminal state, atomic durable replay journals with rollback, restart parsing, receipt revalidation, signed query/operation/expiry receipts, scoped-content before-hash checks, field-level BM25 explanations, and restart/race/expiry tests. Source check passed, but focused evidence ended 10/15. A fresh bounded fix lane reproduced service 4/4 and protocol 3/3 while durable/framed behavior remained red; after retaining setup results and correcting optional JSON extraction, the final durable run was 0/3 (`write_response=false`, second-publish `handshake_required`, and expiry `candidate_missing`). The three-cycle guard is exhausted. Canonical five-field Simple/DBFS parity and the provider-parity SSpec also remain FAIL/NOT-EVIDENCE. Wave 4 implementation is not accepted, committed, or pushed; resume only in a fresh scoped session from these concrete state-persistence failures.
- implementation-wave4-resumed-2-stop: Hardened the parity harness with a closed emitted-evidence schema and exact validation of logical roots, five-field statistics, scores/order, explanation hashes, deltas, clean/incremental parity, and applicable-cell identity. Default integration is 8/8; acceptance correctly fails when probes emit no canonical record. Added a production DBFS exhaustive single-scope five-field adapter and machine probe; focused DBFS evidence is 35/35 with locked roots/statistics/scores/order/explanations and fail-before-read scope behavior, but direct probe execution still reports fail under a binary identifying as the Rust bootstrap seed and is not acceptance evidence. Provider diagnosis proved nested mutable dispatch loses state across `provider_dispatch_frame -> provider_dispatch_payload -> _dispatch_initialized`; wire remains 2/5 and durable 0/3, with the durable seed file unchanged. The three-cycle cap is exhausted for this resumed phase. No implementation commit/push; next fresh phase must replace nested mutable dispatch with a mutation-preserving owner API and then make Simple/DBFS probes emit the closed canonical record.
- implementation-wave4-resumed-3-stop: Added explicit owned-state dispatch results (`ProviderWireOwnedDispatchV1`, owned payload/frame entrypoints) and changed production `main` to replace its service owner after every dispatch. Receiver-style compatibility dispatch still loses mutations under the current interpreter; focused wire remains 2/5 and durable 0/3 because tests/callers and initialized dispatch do not yet thread returned ownership. The next fresh phase must migrate every caller and make initialized dispatch return the updated owner directly, then remove the unsafe mut/receiver surface. Hardened the parity runner to execute dedicated closed-record probes, honor `SPIPE_SIMPLE_BIN`, bind `SIMPLE_LIB`, and reject Rust-seed provenance before admission. The DBFS probe now computes the full applicable canonical record and type-checks, while default integration passes 8/8; admitted execution remains correctly failed because the selected binary is the Rust bootstrap seed and the dedicated Simple provider probe does not yet exist. Three-cycle cap reached; no implementation commit/push.
- implementation-wave4-resumed-4-stop: Migrated production and discovered tests to explicit owned dispatch returns and removed the old mut entrypoints, but Simple class/value copy behavior still drops initialized state at the next boundary; wire is 2/5 and durable remains unrun/red, so the provider is not accepted. The next phase must isolate the value-construction/copy semantics with a minimal owner-return regression before further provider edits. Added dedicated Simple and DBFS probes with executed per-cell ledgers; Simple uses configured durable authority/storage and fails closed on missing W4-SRCH-09 latency/RSS receipt evidence, while DBFS focused evidence is 35/35 with computed stats/deltas and real filtered queries. Independent review rejected earlier declarative cells, leading the harness to load a closed v2 schema, require `{matrix_id,oracle}` per cell, pin no-op/candidate/clean roots, and verify absolute Stage4 binary/provenance paths plus hashes through the canonical admission helper. Default harness evidence is 9/9. The DBFS emitter still needs the mechanical v2 cell-object/`clean_logical_root` update after concurrent harness changes. Read-only audit proved no admitted Linux Stage4 binary currently exists; all available candidates are Rust seeds, bootstrap stages, stale, or provenance-mismatched. Three-cycle cap reached; no implementation commit/push.
- implementation-wave4-resumed-5-stop: A provider-independent owner-result regression passes 3/3, and richer nested text/array/trait plus exact-service handoffs also pass; no general compiler/runtime defect is justified. The real canonical JSON initialization path alone loses `scope_digest` while initialized/generation/workspace/snapshot survive, localizing the defect to successive nested field assignments with the computed repeated-text binding. Next fresh phase must precompute all bindings and atomically reconstruct `SpipeProviderStateV1` and `SpipeProviderServiceV1`, then run wire/durable evidence once. Both probes now use harness-v2 `{matrix_id,oracle}` records, `clean_logical_root`, locked roots, and executed-ledger gating; type checks pass. Simple intentionally emits no record without W4-SRCH-09. Performance design added a five-argument helper, provenance/toolchain/collector hashes, operation-plan/reset schedule, functional receipt URI/hash, containment, and counter journal, but final high review still rejects three missing closed contracts: `qualified_search_profile_v1` fields/budgets/variance, per-timed-query functional result digests, and a canonical replayable counter-journal event protocol; ratio integer rounding/zero handling is also open. DBFS remains 35/35 focused, no admitted Stage4 binary exists, and no implementation commit/push was made.
- implementation-wave4-resumed-6-stop: Atomic `SpipeProviderStateV1` and full `SpipeProviderServiceV1` reconstruction preserves all configured fields before return but the exact `ProviderWireOwnedDispatchV1` boundary still returns empty/uninitialized service state; source check passes and wire remains 2/6, so initialization logic is exonerated and durable tests were correctly withheld. Next fresh phase must reproduce the exact large owner-envelope return ABI, then use a different proven return shape or fix the compiler/JIT owner if falsified. W4-SRCH-09 docs now define a closed qualification profile, timed expected/observed result hashes, fixed ratio arithmetic, closed functional-conformance receipt, benchmark-operation plan, and counter journal. Final review found one remaining cross-doc contradiction: plan/guide require `source_version`, `source_content_sha256`, `source_change_witness`, and `source_change`, while the detail design's authoritative closed event schema/enum omits them. Three-cycle cap reached; no implementation commit/push.
- implementation-wave4-resumed-7-stop: The exact production `ProviderWireOwnedDispatchV1` with a fully populated real service, ports, replay/candidates/arrays, computed bindings, tuple/class variants, and sibling outcome survives 6/6 diagnostic cases; no ABI/compiler defect is justified. Staged real-dispatch tracing localizes binding-text loss to initialized-service construction/return inside canonical payload dispatch, while flags/generation survive; wire remains 2/7 after three cycles, so the next phase must reduce the production parse/validation construction inputs rather than alter the proven envelope. The full W4-SRCH-09 qualification contract was closed across design/plan/guide: qualification profile, timed result hashes, arithmetic, functional receipt, operation plan, source-aware counter journal, acyclic matrix, and source/non-source digest sentinels. Fresh highest-capability review returned `STATUS: PASS`.
- sync-wave4-performance-design: Integrated the accepted two-file W4-SRCH-09 detail-design/operator-guide increment from fresh `origin/main` with tracked-file count unchanged at 119,694. Diff/tree/conflict/runtime API push guards passed and GitHub `main` advanced to `3012334da7ace0db170dab2a1916bd5ee51713f7`.
- implementation-wave4-resumed-8-stop: The apparent provider ownership failure was a test-fixture parser defect: a raw string ending in `}}` triggered the tracked Simple double-brace-collapse bug, erasing `logical_index`; production correctly rejected the malformed initialize request. Composing closing braces separately yields initialize composition 4/4 and wire 7/7. Independent review then found real production gaps. The merged uncommitted repair adds host-supplied validated binding, bound dispatch, single-authority query receipt encoding, owned expiry, atomic apply, full terminal rollback, lifecycle etag/CAS/reload-under-lock, system clock/key authority, production main composition through app.io facades, and removes hardcoded fixture bindings/diagnostic public APIs. Component evidence before merge included composition 4/4, lifecycle ports 2/2, main composition 3/3, wire 6/7 and durable 3/5 during intermediate integration. A fresh merged verifier invoked source plus all six focused specs exactly once, but its transport detached and lost terminal verdicts; per-cycle rules prohibit rerunning them in this phase, so integrated acceptance is indeterminate/FAIL. Provider-scoped env/process audit passes; repository working audit fails only on unrelated `src/app/cli/native_build_main.spl`. No implementation commit/push. Next phase must use a retained-verdict/session-aware runner once, then obtain high-capability source/runtime review.
- implementation-wave4-resumed-9-stop: Session-aware merged verification retained exit 1. Confirmed initialize composition 4/4, exact-envelope ABI 6/6, lifecycle ports 2/2, and main composition 3/3. Individual diagnostic evidence found wire 6/7 and durable 1/5. The final allowed fix cycle migrated legacy durable fixtures to host-bound dispatch and corrected result-node extraction; signing-failure behavior then passed and all remaining apply/publish/race/expiry failures converged on one production root: `validate_operation_receipt` rejects the receipt freshly created by `_operation_receipt` before durable commit, yielding `invalid_receipt` and no candidate/result. The exact signer/validator canonical-byte mismatch is not yet isolated. Three-cycle cap exhausted; no implementation commit/push. Next fresh phase must compare signer and validator unsigned canonical bytes/domain framing field-by-field, fix that single mismatch, then run wire+durable once and request high-capability acceptance.
- implementation-wave4-resumed-10-stop: Centralized operation-receipt unsigned canonical JSON/domain framing/receipt-ID derivation and proved signer/validator bytes match. The first mismatch was the signature: strict base64url decoding returned `text`, lossy UTF-8 conversion corrupted arbitrary 64-byte Ed25519 material before verification. Pure/System authorities now use local binary-safe strict base64url decode, canonical re-encode, and exact 64-byte validation; a focused receipt spec and diagnostic validation error were added. Pre-fix evidence explicitly reported `signature`; the final combined cycle still exited 1 with wire 6/7 and a downstream nil unwrap, while output truncation hid the focused receipt verdict. Independent review also identified remaining accepted-contract gaps: `provider_generation` must use canonical `pg-` plus 32 lowercase hex rather than an integer, validator binding must close policy/revocation/scope/root/operation/outcome formats, and a deterministic operation-receipt signing vector is absent. Three-cycle cap exhausted; no implementation commit/push. Next phase must capture the post-fix bound response before unwrap, close those receipt schema gaps, and run focused receipt+wire+durable once.
- implementation-wave4-resumed-11-stop: Raw apply diagnostics traced the next receipt rejection to an invalid shared SHA-256 validator path; restored local exact `sha256:` plus 64-lowercase-hex validation and fixed binary base64url accumulator overflow. Receipt schema now uses canonical `pg-` generation IDs, closed binding/operation/outcome/policy/revocation validation, and an independently computed operation vector. That vector exposed a pure Ed25519 defect: extended-coordinate doubling used `H=B-D` instead of RFC/EFD `H=D-B`. The pure crypto fix passes RFC/vector/long-message evidence 3/3 and produces a signature byte-identical to Node. Post-crypto provider evidence passes receipt 2/2, wire 7/7, and durable lifecycle 5/5 after making no-op candidate roots equal the base and terminalizing equal-root sibling publication as `stale_base`. Highest-capability provider review still returned FAIL: wire generation fields remain integer outside receipt records; candidate UID preimage includes forbidden apply operation ID; restore permits receipt transplantation; persisted candidate/terminal-error schemas diverge; cross-process CAS loser does not reload/re-evaluate to typed stale_base; publish time is hardcoded; legacy restore accepts arbitrary objects; wire.spl is 897 lines. No provider implementation commit/push.
- sync-ed25519-rfc-fix: Decoupled the Ed25519 regression from untracked provider code, froze the 1,021-byte message/digest/public-key/signature, passed 3/3 plus independent high-capability review, integrated from fresh `origin/main` with tracked files increasing 119,728 to 119,729, and pushed GitHub `main` to `58af6ecb70f6a50b47bce4154b822977b444104a`. The first push attempt failed closed because the fresh worktree lacked ignored `bin/simple`; the supported `SIMPLE_BIN` override ran the main-runnability gate and the same push completed.
- implementation-wave4-resumed-12-stop: Closed three former wire blockers: all provider generations are canonical `pg-` plus 32 lowercase hex, Candidate UID uses the accepted closed preimage/vector without apply operation ID, and terminal timestamps use authority time. Split the 897-line wire into a 17-line facade plus cohesive modules with `wire_core.spl` at 799 lines. Added reload/recompute/discard-signature/re-sign/retry-once CAS convergence; wire evidence passes 8/8. Durable source now persists closed-ish candidate/replay rows with bindings/receipt IDs/response hashes, rejects nonempty legacy object collections, validates several containing-row bindings, and exposes reload-after-conflict; service check passes. Durable runtime evidence remains indeterminate because the broad run detached; a temporary focused fixture proved legacy rejection but failed to create a transplant candidate and was removed. Fresh highest-capability source review accepts generation, candidate UID, and wire-size blockers but still returns FAIL: top-level/nested persistence is not fully closed and permits arbitrary current root/extra fields; terminal optionals use empty/zero instead of null; DurableTerminalErrorV1 and PublicationRecordV1 are absent; second CAS conflict lacks deterministic winner/loser reload; separate parent-owned monotonic transition authority/recorded_at is absent; legacy response/object branches remain permissive; service.spl is 813 lines. No provider implementation commit/push.
- implementation-wave4-resumed-13-stop: Added `durable_records.spl` and closed lifecycle/candidate/replay/publication/terminal-error serialization with bindings, receipt IDs, response hashes, null optionals, exact legacy allowlist, and nested key checks; reduced service.spl to 799 lines. Added distinct parent-owned monotonic transition authority and first/second CAS reload/replay/re-observe logic; source check passes 13 files, lifecycle ports 3/3, wire 8/8. Durable run reached 5/7; diagnosed staged restart hardcoded `applied` versus signed `applied|no_op` and stale expiry timestamp, patched both without rerun per cap. Fresh high review still returns FAIL: terminal error embeds only substring-validated error JSON; expired optional discriminator is permissive; duplicate candidate/replay/publication identities are not rejected; publication count can be satisfied by duplicates; zero current root may coexist with publications; restore does not recompute candidate UID/root/scoped document hashes; final staged second-CAS outcome is an unjournaled retry error; transition monotonic authority is not seeded from restored timestamps; wire_core grew to 904 lines. Runtime gaps include unverified post-fix durable result and missing forced double-CAS/restart-monotonic/corruption/fault matrices. No provider commit/push.
- implementation-wave4-resumed-14-stop: Closed request-bound terminal-error envelopes with true-null receipts, true-null candidate discriminators, replay/candidate/apply/publication uniqueness, publication set/ordered chain and current-root consistency, candidate UID recomputation, canonical operation/root and scoped-document preimage persistence/recomputation, restored timestamp seeding, forced double-CAS handling, and adversarial mutation coverage. Split dispatch further; service is 790, wire_core 791, durable_records 332. Retained merged evidence: combined wire+durable exit 0; durable 8/8 including double-CAS, restart replay, sibling, expiry; earlier receipt/init/lifecycle/main/envelope evidence remains green. Fresh highest review still returns FAIL on five source contracts: replay success envelope is not parsed/bound to its embedded exact receipt; restored document IDs/field/facet names and operation/document sets lack uniqueness/equality; candidate root preimage is not cryptographically tied to restored document set; abort/stale restore assumes base root but runtime may sign post-sibling current root; final still-staged second-CAS returns unjournaled fatal without generation quarantine/stable replay. No provider commit/push.
- implementation-wave4-resumed-15-evidence: Closed the final five source blockers in parallel. Signed replays now bind exact closed success envelopes and embedded receipts; candidate document/field/facet and operation sets are unique and exactly committed into the candidate root. Terminal records persist the exact signed result root, so sibling-driven abort/stale outcomes restore correctly. Two publication CAS misses now produce a signed `quarantined` terminal and atomically journal a stable request-bound error replay. Extracted helpers keep `service.spl` at 800 and `wire_core.spl` at 770. Fresh merged source check passes 17 files and durable lifecycle passes 9/9, including adversarial binding, sibling restart, and quarantine retry/restart. Highest-capability acceptance review remains required; no provider commit/push.
- implementation-wave4-resumed-15-review-ready: Closed all five resumed-14 source blockers: durable signed replay rows now persist the request ID and validate an exact closed success envelope whose embedded canonical receipt equals the row receipt; restored documents reject duplicate IDs/field names/facet names; a canonical candidate commitment binds the exact sorted resulting document ID/hash set plus the exact operation set, recomputes apply payload/root bindings, and proves the base-to-result transition; operation terminals persist their exact signed result root; and two final CAS misses durably quarantine the candidate/generation with stable terminal-error replay across restart. Owner-result extraction keeps `service.spl` at 800 lines and `wire_core.spl` at 770. Retained cycle-2 evidence: provider source check PASS for 17 files and durable lifecycle PASS 9/9, including recomputed-hash envelope drift, embedded-receipt drift, duplicate document/field/facet rejection, sibling roots, expiry, quarantine, and restart replay. Awaiting fresh highest-capability acceptance; no provider commit/push.
- implementation-wave4-resumed-16-replay-binding-evidence: Closed the final replay/document binding gaps from the resumed-15 high review. Restored documents now require the exact canonical five-field schema and fixed weights, so a persisted weight mutation cannot preserve valid search behavior. Every success replay persists a receipt-authority signature over workspace/snapshot/scope, operation identity, payload, request ID, response hash, and receipt ID; restore rejects row-plus-envelope request transplants. Stored responses must equal the provider's exact canonical serialization and their apply counts/expiry or publish previous/result roots must equal the durable candidate transition. Provider source check PASS for 18 files. The merged durable run executed all 9 cases: all Lane A adversarial cases passed; the sole failure was a concurrent provider-vs-candidate quarantine wording assertion, subsequently reconciled by its owner with the affected quarantine oracle PASS 2/2. The 443-second full suite was not repeated per the runaway guard. Highest-capability acceptance and admitted Stage 4 conformance remain outstanding; no commit/push.
- implementation-wave4-resumed-17-query-protocol-evidence: Closed the remaining protocol/query behavior blockers. Frame headers now accept lowercase hexadecimal only. Query execution supports validated sorted multi-value OR filters, signed snapshot/scope/generation/query-bound cursors, deterministic pagination without duplicates, stale/tampered cursor rejection, relative monotonic deadlines checked before read results and durable linearization, request admission/completion tracking with unknown-target rejection and `already_complete` truth, requested-root statistics, and five canonical field-stat rows. Bound transient errors now carry deterministic retryability. Extracted `wire_query.spl` keeps `wire_core.spl` at 625 lines. Provider source check passes 18 files; protocol passes 4/4, service state/cancel passes 2/2, and final wire behavior passes 10/10 after three bounded cycles. Evidence used the repository's Rust bootstrap-seed runner and is not admitted Stage 4 conformance. Highest-capability provider acceptance remains required; no commit/push.
- implementation-wave4-resumed-17-secret-durability-evidence: Replaced production seed admission's predicate/size/read TOCTOU with one owner-level `file_read_regular_no_follow_bounded` operation: one no-follow descriptor is fstat-validated as a regular file, bounded through EOF, and rejected on symlink, oversize, read, or text failure. Added the runtime symbol to Rust interpreter/runtime, C bootstrap/native providers, security metadata, Rust and pure-Simple ABI expansion, LLVM declarations, and I/O facades. `file_fsync` now calls the real runtime synchronization primitive; lifecycle commit and CAS commit atomically rename, fsync the renamed file, then fsync the parent directory before unlock. Provider and owner-file source checks pass; Rust runtime focused evidence passes 1/1; the affected durable suite passes 12/12 including the directory-entry durability path. The checked-in Rust bootstrap predates the new secure-read extern, so main/IO Simple execution correctly reports unknown extern and is not admitted evidence. The whole compiler scan was stopped after unrelated existing `simd_recipe_infra.spl:91-105` parser failures; the Rust compiler crate is separately blocked by unrelated import-loader/perf-counter/lowerer errors. No commit/push.
