# Feature: render-compute-hardening

## Raw Request
Harden vulkan, metal, optix, rocm, cpu simd(riskv,arm,x86) 2d rendering harden. 3d partial
cuda, hip, opencl, metal compute. make common simple programming like cuda/hip like. and generate cuda/hip/opencl/metal codes or binary.
make 2d optimization codes in simple and optimize the 2d rendering using it.
make gui rendering(include text) faster than gtk.

## Task Type
feature

## Refined Goal
Implement a hardened cross-backend rendering and compute stack that provides strict 2D backend diagnostics/parity, partial 3D coverage, portable CUDA/HIP/OpenCL/Metal-style compute code generation, Simple-authored 2D optimization kernels, and measured GUI/text rendering performance beyond the GTK baseline.

## Acceptance Criteria
- AC-1: Vulkan, Metal, ROCm, CUDA, WebGPU, and CPU SIMD Engine2D paths expose strict typed probe diagnostics and do not silently fall back in strict mode.
- AC-2: Core 2D primitives and text readback have parity coverage across available hardware backends and CPU/software references.
- AC-3: Partial 3D rendering has session-managed backend coverage and strict diagnostics for unsupported backend capabilities.
- AC-4: A common Simple compute surface can emit target-specific CUDA, HIP, OpenCL C, and Metal Shading Language code or a typed binary-format artifact.
- AC-5: Simple-authored 2D optimization kernels are wired into Engine2D provider metadata with hit/change counters.
- AC-6: CPU SIMD coverage includes x86, ARM, and RISC-V capability-gated paths or typed unavailable diagnostics.
- AC-7: GUI rendering, including text, has benchmark evidence showing faster frame/text throughput than a GTK baseline on comparable scenes.

## Scope Exclusions
None for the overall persistent goal. Individual turns may land smaller verified slices while this state remains active.

## Phase
implementation-in-progress

## Log
- dev: Created state file with 7 acceptance criteria (type: feature).
- implementation: Continued from the crash/session worktree scan in the main worktree; no separate registered git worktree existed, and latest crash logs were watchdog timeouts with older interpreter overflow reports.
- implementation: Extended `src/compiler/70.backend/backend/gpu_portable_compute.spl` with explicit source-to-binary compile plans plus Simple-authored portable 2D copy, alpha-blend, and scroll kernels for CUDA, HIP, OpenCL C, and Metal targets.
- verification: Focused codegen spec `test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter` passes with 9 scenarios, covering AC-4 source/binary artifact planning and AC-5 portable 2D optimization kernel emission.
- implementation: Wired Engine2D SIMD hit/change counters into `RenderingOptProvider` metadata and frame reports via `rendering_opt_provider_from_simd_counts`, `current_simd_rendering_opt_provider`, and `rendering_opt_report_from_simd`; added `record_simd_vectorize_change` for optimization change events.
- verification: `test/unit/lib/gpu/engine2d/rendering_opt_provider_spec.spl`, `simd_kernels_spec.spl`, and `backend_software_simd_spec.spl` pass in interpreter mode; touched SIMD provider/rendering provider files pass `bin/simple check`.
- implementation: Extended `common.ui.web_render_api` with explicit generated-WASM/no-JavaScript targets for Android, iOS, host WM, and SimpleOS WM, including target ABI mapping, validation, and binary artifact metadata.
- verification: `test/unit/lib/common/ui/web_render_api_spec.spl`, `test/unit/app/ui/web_render_backend_api_spec.spl`, `test/unit/app/ui/web_render_cache_spec.spl`, `test/unit/compiler/codegen/gpu_portable_compute_spec.spl`, `test/unit/lib/gpu/engine2d/rendering_opt_provider_spec.spl`, `test/unit/os/kernel/loader/disk_launch_manifest_spec.spl`, and `test/system/os/simpleos_ai_cli_js_node_port_spec.spl` pass in interpreter mode.
- implementation: Extended the Engine2D strict backend probe contract with typed ROCm/HIP, CPU SIMD, and OptiX diagnostics, status/backend constants, hardware classification, and no-fallback validation while preserving Vulkan, CUDA, Metal, and WebGPU shader-format diagnostics.
- verification: `test/unit/lib/gpu/engine2d/backend_probe_strict_spec.spl --mode=interpreter --clean` passes with 5 scenarios; `bin/simple check src/lib/gc_async_mut/gpu/engine2d/backend_probe.spl` passes.
- implementation: Added Engine2D generated 2D kernel dispatch metadata that maps compiler-emitted fill/copy/alpha/scroll kernels to CUDA/PTX, HIP/HSACO, OpenCL/SPIR-V, and Metal/metallib artifacts, and exposes the dispatch table as rendering optimization provider metadata.
- verification: `test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` passes with 5 scenarios; `test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` remains passing with 9 scenarios; `generated_kernel_dispatch.spl` checks cleanly.
- implementation: Extended `common.ui.window_scene` with backend-neutral WM chrome geometry for background, top command lane, content area, bottom taskbar, clock/right-icon metadata, taskbar icon metadata, DPI scaling, and inner-window drag clamping.
- verification: `test/unit/lib/common/ui/window_scene_spec.spl`, `test/unit/lib/common/ui/web_render_api_spec.spl`, `test/unit/app/ui/web_render_backend_api_spec.spl`, `test/unit/app/ui/web_render_cache_spec.spl`, `test/unit/compiler/codegen/gpu_portable_compute_spec.spl`, `test/unit/lib/gpu/engine2d/rendering_opt_provider_spec.spl`, and `test/system/os/simpleos_ai_cli_js_node_port_spec.spl` pass in interpreter mode; `bin/simple check src/lib/common/ui/window_scene.spl` passes.
- implementation: Added backend-neutral WM pointer dispatch for command-lane clock/right-icon hits, bottom taskbar pinned-app launch, running-window focus, titlebar drag start, body focus, and desktop-background clicks with focused-scene projection.
- verification: `test/unit/lib/common/ui/window_scene_spec.spl` now passes with 7 scenarios; adjacent web/WASM/render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Extended generated 2D kernel dispatch with runtime launch-plan metadata for CUDA, ROCm/HIP, OpenCL, and Metal, including entry/artifact names, launch API, grid/block geometry, argument layouts, and fail-closed unavailable plans for unsupported backends or invalid dimensions.
- verification: `test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` passes with 8 scenarios; `test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` remains passing with 9 scenarios; `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl src/lib/gc_async_mut/gpu/engine2d/mod.spl` passes with only pre-existing aggregate import warnings in `mod.spl` when checked without a source root.
- implementation: Added `common.ui.wm_runtime_dispatch` to convert shared WM dispatch results into stable runtime commands for launcher launch, window focus, drag-begin, command-lane clock/icon, and desktop-background handling.
- verification: `test/unit/lib/common/ui/wm_runtime_dispatch_spec.spl` passes with 4 scenarios; focused WM scene, web/WASM/render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Extended `common.ui.wm_runtime_dispatch` with a shared shell-state applier for launcher launches, window focus, drag-begin state, command-lane target state, desktop-background clearing/counting, and fail-closed ignored commands.
- verification: `test/unit/lib/common/ui/wm_runtime_dispatch_spec.spl` now passes with 7 scenarios; focused WM scene, web/WASM/render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Added generated 2D kernel execution-request binding that connects launch plans to prepared runtime handles for CUDA, ROCm/HIP, OpenCL, and Metal, exposes backend-specific call shapes, and rejects missing kernel, args, queue, or encoder handles before submission.
- verification: `test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` passes with 11 scenarios; `test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` remains passing with 9 scenarios; `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl src/lib/gc_async_mut/gpu/engine2d/mod.spl` passes with only the known aggregate import warnings in `mod.spl`; `sh scripts/setup/install-spipe-dev-command.shs --check` reports `STATUS: PASS`.
- implementation: Added `app.ui.web.wm_runtime_bridge` as a host-facing adapter that maps pointer coordinates through shared WM scene dispatch into runtime commands/wire payloads for taskbar launch/focus, titlebar drag, command-lane icon/clock, and ignored non-left events.
- verification: `test/unit/app/ui/wm_runtime_bridge_spec.spl` passes with 4 scenarios; focused shared WM dispatch/applier, web/WASM/render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Added architecture-specific strict CPU SIMD probe diagnostics for `cpu_simd_x86`, `cpu_simd_arm`, and `cpu_simd_riscv`, with explicit x86 SSE/AVX, ARM NEON/SVE, and RISC-V vector feature gates that remain on the requested backend and fail unavailable until runtime evidence exists.
- verification: `test/unit/lib/gpu/engine2d/backend_probe_strict_spec.spl --mode=interpreter --clean` passes with 6 scenarios; `bin/simple check src/lib/gc_async_mut/gpu/engine2d/backend_probe.spl` passes; adjacent generated dispatch and rendering optimization provider specs remain passing in interpreter mode.
- implementation: Added `os.desktop.wm_runtime_bridge` as a SimpleOS-facing adapter that maps framebuffer/compositor pointer coordinates through shared WM scene dispatch into runtime commands, shell-state updates, and serial-friendly markers for QEMU capture checks.
- verification: `test/unit/os/desktop/wm_runtime_bridge_spec.spl` passes with 4 scenarios; focused host WM bridge, shared WM dispatch/applier, web/WASM/render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Added `std.gpu.engine3d.backend_probe3d` as a strict partial-3D backend/session diagnostic contract for CPU, software, CUDA, ROCm, HIP, OpenCL, Metal, Vulkan, WebGPU, and OptiX, distinguishing usable CPU/software renderers, managed-but-unavailable GPU render sessions, OpenCL compute-only status, and OptiX ray-tracing-only status without fallback.
- verification: `test/unit/lib/gpu/engine3d/backend_probe3d_strict_spec.spl --mode=interpreter --clean` passes with 5 scenarios; `bin/simple check src/lib/gc_async_mut/gpu/engine3d/backend_probe3d.spl src/lib/gc_async_mut/gpu/engine3d/mod.spl src/lib/gc_async_mut/gpu/engine3d/__init__.spl` passes; existing `test/unit/gpu/graphics_3d_session_managed_backend_spec.spl --mode=interpreter --clean` remains passing with 24 scenarios. Adjacent `test/unit/lib/gpu/engine3d/types3d_spec.spl --mode=interpreter --clean` still fails 2 assertions in untouched type math coverage.
- implementation: Extended host and SimpleOS WM runtime bridges with direct event-loop step boundaries: host pointer steps now apply shared shell state, and both host/SimpleOS bridges emit loop-step markers for launcher/focus/drag/command-lane/background handling.
- verification: `test/unit/app/ui/wm_runtime_bridge_spec.spl` and `test/unit/os/desktop/wm_runtime_bridge_spec.spl` now pass with 5 scenarios each; focused shared WM dispatch/applier, web/WASM/render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Added generated 2D kernel submit-result status tracking so prepared runtime calls now distinguish not-ready requests, runtime-unavailable attempts, backend submit failures, and submitted calls without claiming device execution in environments without hardware.
- verification: `test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` passes with 12 scenarios; `test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` and `test/unit/lib/gpu/engine3d/backend_probe3d_strict_spec.spl --mode=interpreter --clean` remain passing; `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl src/lib/gc_async_mut/gpu/engine2d/mod.spl` passes with only the known isolated `mod.spl` aggregate import warnings.
- implementation: Added `common.ui.wasm_hello_gui` to materialize deterministic generated-WASM/no-JavaScript hello-world GUI artifacts for Android, iOS, host WM, and SimpleOS WM with target-specific ABI, module path, viewport, HTML body, and binary metadata.
- verification: `test/unit/lib/common/ui/wasm_hello_gui_spec.spl` passes with 4 scenarios; focused web render API, host/SimpleOS WM bridge, shared WM dispatch/applier, render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Added a common `WebRenderGtkBenchmarkEvidence` gate for GUI/text performance claims, requiring the same scene to have positive Simple/GTK samples and strictly faster Simple frame and text timings before reporting `faster_than_gtk`.
- verification: `test/unit/lib/common/ui/web_render_api_spec.spl --mode=interpreter --clean` passes with 14 scenarios; `bin/simple check src/lib/common/ui/web_render_api.spl test/unit/lib/common/ui/web_render_api_spec.spl` passes; `test/unit/app/ui/web_render_backend_api_spec.spl`, `test/unit/app/ui/web_render_cache_spec.spl`, and generated-kernel dispatch specs remain passing; `sh -n scripts/check/check-gtk-gui-size-speed-baseline.shs` passes.
- implementation: Bound generated-WASM hello GUI artifacts to deterministic compile plans with source entry, wasm32 target triple, wasm linker, output module path, ABI, readiness, and blocker diagnostics.
- verification: `test/unit/lib/common/ui/wasm_hello_gui_spec.spl` now passes with 6 scenarios; focused web render API, host/SimpleOS WM bridge, shared WM dispatch/applier, render-cache, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI contract specs pass in interpreter mode.
- implementation: Extended `scripts/check/check-gtk-gui-size-speed-baseline.shs` to emit machine-readable GTK evidence fields matching the common web render evidence gate: scene id, Simple/GTK frame and text timings, iteration counts, status, and fail-closed reason.
- verification: `sh -n scripts/check/check-gtk-gui-size-speed-baseline.shs` passes; a smoke run with a missing Simple binary generates `/tmp/simple-gtk-gui-smoke/report.md` and emits `gtk_benchmark_evidence_status=fail` with `gtk_benchmark_evidence_reason=missing-timings`; focused `web_render_api_spec.spl` passes with 14 scenarios and the touched web render API/spec check cleanly.
- implementation: Extended generated-WASM hello GUI support with deterministic compile request/result contracts that distinguish blocked compile plans, missing wasm linker, missing WASI sysroot, missing source entry, ready-to-invoke requests, and already-available compiled artifacts without pretending to execute a toolchain.
- verification: `bin/simple check src/lib/common/ui/wasm_hello_gui.spl` passes; `test/unit/lib/common/ui/wasm_hello_gui_spec.spl --mode=interpreter --clean` now passes with 10 scenarios; adjacent web render API, host/SimpleOS WM bridge, shared WM dispatch/applier, WM scene, render-cache/backend, portable compute, Engine2D provider/probe/dispatch, and SimpleOS AI CLI system specs pass in interpreter mode.
- implementation: Added `examples/ui/hello_wasm_gui.spl` as the concrete no-JavaScript hello GUI source entry referenced by the generated-WASM compile plans, including deterministic title/body/css output and button/scroll/text event response markers.
- verification: `bin/simple check examples/ui/hello_wasm_gui.spl` passes; `bin/simple run examples/ui/hello_wasm_gui.spl` emits title/body/css/event markers; `test/unit/lib/common/ui/wasm_hello_gui_spec.spl --mode=interpreter --clean` remains passing with 10 scenarios.
- verification: Ran a real GTK comparison smoke with the repo `bin/simple` binary: `SKIP_SIMPLE_NATIVE=1 SIMPLE_ITERATIONS=1 GTK_ITERATIONS=1 SIMPLE_RUN_TIMEOUT_SECS=120 REPORT_PATH=/tmp/simple-gtk-gui-real-evidence.md sh scripts/check/check-gtk-gui-size-speed-baseline.shs`. The run emitted `gtk_benchmark_evidence_status=pass` for scene `wm-static-text-grid` with Simple cached frame `74us`, Simple text/command plan `29us`, GTK frame/text `21121us`, and both iteration counts at 1. A 60-second timeout run failed earlier because JIT fell back to the interpreter and the benchmark completed after the timeout, not because the Simple renderer failed.
- implementation: Raised the GTK baseline script's default Simple run timeout from 20s to 120s so interpreter-fallback runs do not get recorded as missing Simple timings.
- verification: `sh -n scripts/check/check-gtk-gui-size-speed-baseline.shs` passes; rerunning the GTK comparison without a timeout override (`SKIP_SIMPLE_NATIVE=1 SIMPLE_ITERATIONS=1 GTK_ITERATIONS=1 REPORT_PATH=/tmp/simple-gtk-gui-default-timeout-evidence.md sh scripts/check/check-gtk-gui-size-speed-baseline.shs`) emits `gtk_benchmark_evidence_status=pass`, Simple cached frame `55us`, Simple text/command plan `29us`, and GTK frame/text `22702us`.
- implementation: Wired SimpleOS desktop shell live pointer-loop processing into the shared WM runtime command path. `Compositor` now records pending WM pointer steps from hosted and PS/2 input, and `DesktopShell.run` / `run_baremetal` consume those steps through `simpleos_wm_pointer_runtime_command` / `simpleos_wm_apply_pointer` before shell shortcuts, process reconciliation, and rendering.
- verification: Added `test/unit/os/desktop/shell_wm_runtime_loop_spec.spl` proving a live pending compositor pointer step is consumed by `DesktopShell`, updates shared runtime state, focuses/drags the real compositor window, and emits a serial-friendly `[simpleos-wm] loop-step` marker. Focused checks pass: `bin/simple test test/unit/os/desktop/shell_wm_runtime_loop_spec.spl --mode=interpreter --clean`, `test/unit/os/desktop/shell_launch_fallback_spec.spl`, `test/unit/os/desktop/shell_lifecycle_cleanup_spec.spl`, `test/unit/os/compositor/compositor_spec.spl`, `test/unit/os/desktop/wm_runtime_bridge_spec.spl`, and `test/unit/app/ui/wm_runtime_bridge_spec.spl`. `bin/simple check src/os/compositor/compositor.spl src/os/desktop/shell.spl test/unit/os/desktop/shell_wm_runtime_loop_spec.spl` passes with the pre-existing isolated `os.compositor.app_content` source-root warning in `shell.spl`.
- implementation: Fixed the Rust `simple compile` WASM routing bug so `--backend=wasm`, `--backend=wasm32`, and explicit WASM targets enter the existing WASM artifact emitter path instead of silently compiling an SMF file while reporting success; exposed the target-specific WASM helper for this path.
- verification: `cargo check -q -p simple-driver` passes; `cargo test -q -p simple-driver --lib wasm_artifact_emitter` passes with 2 unit tests; a rebuilt `cargo run -q -p simple-driver --bin simple -- compile examples/ui/hello_wasm_gui.spl --backend=wasm -o /tmp/hello_wasm_gui_cli.wasm` now fails closed with `WASM compilation failed: ... Support for this target has not been implemented yet` and leaves no output file instead of emitting SMF; focused hello GUI check/spec still pass.
- implementation: Wired host web WM pointer steps into the shared runtime loop. Browser taskbar/window mousedown now sends `host_wm_pointer` frames, `ui_routes` dispatches them through `WmBridge.handle_host_wm_pointer_loop_step`, the bridge projects a shared scene from the live session, applies shared runtime state, emits `[host-wm] loop-step` markers, and uses object-owned bridge state rather than interpreter-invisible module globals. Host taskbar runtime mutations now persist through explicit runtime saves.
- verification: Added `test/unit/app/ui/host_wm_runtime_loop_spec.spl` proving host titlebar pointer steps focus/drag a real bridged session window and unsupported gestures remain ignored. Targeted checks/tests pass: `bin/simple check src/app/ui.web/taskbar_runtime_part1.spl src/app/ui.web/wm_bridge.spl src/app/ui.web/ui_routes.spl test/unit/app/ui/host_wm_runtime_loop_spec.spl test/integration/app/ui.web/taskbar_runtime_test.spl test/integration/app/ui.web/wm_bridge_test.spl test/integration/app/ui.web/ui_routes_test.spl`, `node --check src/app/ui.web/wm.js`, `test/unit/app/ui/host_wm_runtime_loop_spec.spl`, `test/unit/app/ui/wm_runtime_bridge_spec.spl`, `test/integration/app/ui.web/web_runtime_adapter_test.spl`, `test/integration/app/ui.web/taskbar_runtime_test.spl`, `test/integration/app/ui.web/wm_bridge_test.spl`, and `test/integration/app/ui.web/ui_routes_test.spl`.
- implementation: Added CPU SIMD runtime execution evidence to the shared SIMD kernel facade. `cpu_simd_runtime_evidence()` now captures detected runtime arch/features, executes fill/copy/alpha/scroll kernels against real buffers, records hit-count deltas and a checksum, and exposes per-arch availability flags for x86, ARM, and RISC-V. Strict Engine2D arch-specific CPU SIMD probes consume this evidence and only initialize the matching arch path when runtime features and execution evidence agree; otherwise they stay unavailable with evidence-rich diagnostics.
- verification: `bin/simple check src/lib/nogc_sync_mut/gpu/engine2d/simd_kernels.spl src/lib/nogc_async_mut/gpu/engine2d/simd_kernels.spl src/lib/gc_async_mut/gpu/engine2d/backend_probe.spl test/unit/lib/gpu/engine2d/simd_kernels_spec.spl test/unit/lib/gpu/engine2d/backend_probe_strict_spec.spl` passes. Focused tests pass: `test/unit/lib/gpu/engine2d/simd_kernels_spec.spl` now has 18 scenarios including runtime evidence execution; `test/unit/lib/gpu/engine2d/backend_probe_strict_spec.spl`; adjacent `test/unit/lib/gpu/engine2d/rendering_opt_provider_spec.spl` and `test/unit/lib/gpu/engine2d/backend_software_simd_spec.spl` also pass.
- implementation: Tightened Rust WASM backend selection so default-feature builds fail before Cranelift for WASM targets with an actionable `--features wasm` / `--features wasm-wasi` diagnostic. The web compiler's default-feature minimal module fallback now recognizes the new diagnostic, preserving hydration/template tests while keeping `simple compile --backend=wasm` fail-closed.
- verification: `cargo check -q -p simple-compiler -p simple-driver` passes; `cargo test -q -p simple-compiler --lib wasm_target_without_llvm_feature_errors_before_cranelift` passes; `cargo test -q -p simple-driver --lib wasm_artifact_emitter` passes; rebuilt CLI `cargo run -q -p simple-driver --bin simple -- compile examples/ui/hello_wasm_gui.spl --backend=wasm -o /tmp/hello_wasm_gui_cli.wasm` now reports `WebAssembly targets require the LLVM/WASM backend; rebuild simple-driver with --features wasm or --features wasm-wasi` and leaves no output file. Attempting the real `--features wasm-wasi` build is currently blocked by Cargo's vendored `object/src/build/elf.rs` checksum mismatch in this worktree.
- implementation: Added generated 2D kernel execution evidence as a separate fail-closed layer above submit status. `Generated2DKernelExecutionEvidence` only reports `device_executed=true` after a successful submit, available readback, a positive expected checksum, and an exact readback checksum match; failed submit, missing readback, invalid expected checksum, and readback mismatch remain explicit non-execution statuses.
- verification: `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl` passes. `test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` now passes with 14 scenarios covering verified execution evidence and fail-closed missing-readback/failed-submit paths. Adjacent `test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` remains passing with 9 scenarios.
- implementation: Added partial Engine3D render evidence on top of strict backend probes. `Engine3DRenderEvidence` now distinguishes usable probe, positive frame dimensions, draw-call presence, readback availability, expected checksum validity, and exact checksum match before reporting `frame_rendered=true`. CPU/software reference evidence produces deterministic partial-frame checksums; CUDA/ROCm/HIP/OpenCL/Metal/Vulkan/WebGPU/OptiX remain fail-closed until real backend readback is supplied.
- verification: `bin/simple check src/lib/gc_async_mut/gpu/engine3d/backend_probe3d.spl test/unit/lib/gpu/engine3d/backend_probe3d_strict_spec.spl` passes. `test/unit/lib/gpu/engine3d/backend_probe3d_strict_spec.spl --mode=interpreter --clean` now passes with 8 scenarios covering CPU/software frame evidence and fail-closed GPU/missing-readback/mismatch paths. Adjacent `test/unit/gpu/graphics_3d_session_managed_backend_spec.spl --mode=interpreter --clean` remains passing with 24 scenarios.
- implementation: Repaired the tracked Cargo vendor checksum metadata for `object/src/build/elf.rs` so the existing vendored source can participate in the `wasm-wasi` feature build; the vendored source file itself remains outside the tracked edit set because the repo ignore rules hide `vendor/object/src/build/**`.
- verification: `RUSTC_BOOTSTRAP=1 cargo run -q -p simple-driver --features wasm-wasi --bin simple -- compile examples/ui/hello_wasm_gui.spl --target wasm32-wasi -o /tmp/hello_wasm_gui_feature.wasm` produces a real 1906-byte WASM module with `\0asm` magic, version 1, no SMF marker, and embedded `spl_main`, `hello_wasm_gui_title/body/css/event_response`, `hello_button`, `Generated WASM UI`, and click/scroll/text response strings. Running the same `wasm-wasi` feature build on stable without `RUSTC_BOOTSTRAP` still fails in vendored `compiler_builtins` nightly feature gates, so stable toolchain policy remains open.
- implementation: Added generated-WASM hello GUI target package contracts for Android, iOS, host WM, and SimpleOS WM. Each package now carries a no-JavaScript gate, target module path, native launcher entry, shell runtime binding, button/scroll/text event contract, and render contract, and compiled-artifact evidence now fails closed on empty output, bad WASM magic/version, SMF leakage, missing `spl_main`, missing GUI exports/strings, or missing event strings.
- verification: `bin/simple check src/lib/common/ui/wasm_hello_gui.spl test/unit/lib/common/ui/wasm_hello_gui_spec.spl` passes. `bin/simple test test/unit/lib/common/ui/wasm_hello_gui_spec.spl --mode=interpreter --clean` now passes with 13 scenarios covering target package contracts and compiled WASM evidence. Adjacent `test/unit/lib/common/ui/web_render_api_spec.spl --mode=interpreter --clean` passes with 14 scenarios, and `bin/simple check src/lib/common/ui/web_render_api.spl examples/ui/hello_wasm_gui.spl` passes.
- implementation: Hardened the TUI screen buffer so `Screen.put_text` clips inserted ANSI/plain text to the remaining visual row width instead of growing rows past the screen dimensions. The clipping preserves ANSI escape sequences and still terminates rows with reset escapes for styled edge writes.
- verification: `bin/simple check src/app/ui.tui/screen.spl test/unit/app/ui/screen_ansi_spec.spl` passes. `bin/simple test test/unit/app/ui/screen_ansi_spec.spl --mode=interpreter --clean` now passes with 17 scenarios, including right-edge plain and styled clipping. TUI facade specs pass for `gc_async_mut` and `nogc_async_mut` widget/facade packages, and `bin/simple check src/app/ui.render/tui_widgets_part1.spl src/app/ui.render/tui_widgets_part2.spl examples/ui/hello_tui.spl examples/ui/hello_async_tui.spl` passes.
- implementation: Tightened the GTK GUI/text evidence gate with a minimum sample confidence requirement. `WebRenderGtkBenchmarkEvidence` now records `min_iterations` and `sample_confident`, and `faster_than_gtk` can only pass when both Simple and GTK provide at least three measured samples in addition to same-scene frame/text speed wins. The GTK baseline script now emits matching `gtk_benchmark_evidence_sample_status` and `gtk_benchmark_evidence_min_iterations` fields and fails closed with `sample-count-too-low` for one-off timing observations.
- verification: `sh scripts/setup/install-spipe-dev-command.shs --check`, `sh -n scripts/check/check-gtk-gui-size-speed-baseline.shs`, `bin/simple check src/lib/common/ui/web_render_api.spl test/unit/lib/common/ui/web_render_api_spec.spl`, and `bin/simple test test/unit/lib/common/ui/web_render_api_spec.spl --mode=interpreter --clean` pass. A real low-sample smoke (`SKIP_SIMPLE_NATIVE=1 SIMPLE_ITERATIONS=1 GTK_ITERATIONS=1 ...`) now emits `gtk_benchmark_evidence_status=fail`, `gtk_benchmark_evidence_reason=sample-count-too-low`, and `gtk_benchmark_evidence_sample_status=low` while preserving observed Simple/GTK timings. A minimum-sample positive control (`SIMPLE_ITERATIONS=3 GTK_ITERATIONS=3`) emits `gtk_benchmark_evidence_status=pass`, `sample_status=ok`, Simple frame `53us`, Simple text `12us`, and GTK frame/text `7030us` on the same scene.
- implementation: Added compiler-side compiled-artifact evidence for the common CUDA/HIP/OpenCL/Metal portable compute emitter. `PortableComputeCompiledArtifactEvidence` validates that a compile plan produced non-empty artifact bytes, the target-specific binary magic (`.version`, `ELF`, `SPIR-V`, or `MTLB`) is present, and the expected generated kernel entry is exported before the artifact is considered valid.
- verification: `bin/simple check src/compiler/70.backend/backend/gpu_portable_compute.spl test/unit/compiler/codegen/gpu_portable_compute_spec.spl` passes. `bin/simple test test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` now passes with 11 scenarios covering valid PTX/HSACO/SPIR-V/metallib evidence and fail-closed missing bytes, bad magic, and missing symbol cases. Existing generated 2D dispatch evidence remains passing: `bin/simple test test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` passes with 14 scenarios.
- implementation: Added deterministic portable compute toolchain invocation and compile-result contracts for generated CUDA/HIP/OpenCL/Metal artifacts. `PortableComputeToolchainInvocation` records source/output paths, compiler tool paths, target-specific command lines (`nvcc --ptx`, `hipcc --genco`, `opencl-c-to-spirv -emit-spirv`, `metal` plus `metallib`), and missing-tool/path blockers. `PortableComputeCompileResult` only reports `compiled_artifact_verified` when the invocation is ready, the toolchain exit code is zero, and compiled-artifact evidence passes.
- verification: `sh scripts/setup/install-spipe-dev-command.shs --check`, `bin/simple check src/compiler/70.backend/backend/gpu_portable_compute.spl test/unit/compiler/codegen/gpu_portable_compute_spec.spl`, and `bin/simple test test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` pass. The portable compute spec now has 14 scenarios covering target command lines, missing primary/Metal secondary tools, successful compile evidence, failed toolchain exit, and rejected artifact evidence. Existing generated 2D dispatch evidence remains passing with 14 scenarios.
- implementation: Added `scripts/check/check-portable-compute-toolchains.shs` as a host evidence smoke for generated portable compute artifacts. The script writes a target-specific `simple_2d_optimization` CUDA/HIP/OpenCL/Metal module exporting `simple_2d_fill_u32`, `simple_2d_copy_u32`, `simple_2d_alpha_u32`, and `simple_2d_scroll_u32`, auto-selects a CUDA architecture from `nvcc --list-gpu-arch` when available, invokes installed toolchains, validates output bytes/magic/all exported entries, and emits machine-readable per-target statuses without marking missing tools as passing.
- verification: `sh -n scripts/check/check-portable-compute-toolchains.shs` passes. Running `REPORT_PATH=/tmp/portable-compute-toolchains.md BUILD_DIR=/tmp/portable-compute-toolchains sh scripts/check/check-portable-compute-toolchains.shs` on this host produces real shared CUDA PTX module evidence for the generated 2D operations: `cuda_status=compiled_artifact_verified`, `cuda_reason=pass`, `cuda_bytes=4486`, command `nvcc --ptx -arch=compute_75 ... simple_2d_optimization.cu`, and artifact `/tmp/portable-compute-toolchains/simple_2d_optimization.ptx`. HIP, OpenCL, and Metal report `unavailable` with `missing-primary-tool` on this host. Focused portable compute and generated dispatch specs remain passing.
- implementation: Repaired app-level TUI async/theme coverage. `common.ui.style` now exposes runtime-visible `UITheme` and `AnsiTheme.from_theme` values for dark/light TUI palettes, and interpreter/native channel close semantics now preserve buffered receives, make close idempotent, and treat sends after close as no-ops while retaining receiver state.
- verification: `bin/simple check src/lib/common/ui/style.spl test/unit/app/ui/tui_theme_spec.spl test/unit/app/ui/async_tui_spec.spl` passes. `bin/simple test test/unit/app/ui/tui_theme_spec.spl --mode=interpreter --clean` passes with 44 scenarios. `cargo test -q -p simple-compiler channel_close --lib` passes with 2 Rust tests for buffered close and send-after-close. The patched driver path, `cargo run -q -p simple-driver --bin simple -- test test/unit/app/ui/async_tui_spec.spl --mode=interpreter --clean`, passes with 18 scenarios. Rebuilt the ignored local bootstrap binary with `cargo build --profile bootstrap -p simple-driver --bin simple`, atomically refreshed `bin/simple` from `src/compiler_rust/target/bootstrap/simple`, and verified normal command-path TUI specs: `bin/simple test test/unit/app/ui/async_tui_spec.spl --mode=interpreter --clean` passes with 18 scenarios, `bin/simple test test/unit/app/ui/tui_theme_spec.spl --mode=interpreter --clean` passes with 44 scenarios, and `bin/simple test test/unit/app/ui/screen_ansi_spec.spl --mode=interpreter --clean` passes with 17 scenarios. `cargo check -q -p simple-compiler` passes with only existing warnings.
- implementation: Added a generated-artifact load evidence gate between compiled artifacts and runtime submit requests. `Generated2DKernelArtifactLoadEvidence` now requires a dispatch-ready launch plan, verified artifact evidence, runtime availability, and backend-specific module/queue plus kernel/pipeline handles before `generated_2d_execution_request_from_load` can produce a submittable request. Bad artifacts, missing runtimes, missing kernels/pipelines, and missing OpenCL/Metal queue/encoder handles fail closed before submit.
- verification: `sh scripts/setup/install-spipe-dev-command.shs --check`, `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl`, and `bin/simple test test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` pass. The generated dispatch spec now has 16 scenarios covering artifact-load success for CUDA/Metal and fail-closed bad artifact/runtime/handle paths. `test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` remains passing with 14 scenarios, and the portable compute toolchain smoke still produces verified CUDA PTX evidence on this host.
- implementation: Added `scripts/check/check-cuda-generated-2d-readback.shs` as a real CUDA generated-kernel load/submit/readback evidence smoke. It reuses or generates the portable `simple_2d_optimization.ptx` module artifact, builds a tiny CUDA Driver API loader, retains the primary CUDA context, loads the shared generated 2D module, launches generated fill/copy/alpha/scroll kernels, copies device memory back, and records aggregate plus per-operation checksum evidence.
- verification: `sh -n scripts/check/check-cuda-generated-2d-readback.shs` passes. Running `REPORT_PATH=/tmp/cuda-generated-2d-readback.md BUILD_DIR=/tmp/cuda-generated-2d-readback sh scripts/check/check-cuda-generated-2d-readback.shs` on this host passes with `cuda_generated_2d_readback_status=pass`, `reason=readback-checksum-matched`, CUDA code `0`, ops `fill,copy,alpha,scroll`, count `64`, matching aggregate checksum `274983770116`, and matching per-op checksums for fill `1082179840`, copy `66016`, alpha `273901410720`, and scroll `113540`. Focused checks also pass: `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl src/compiler/70.backend/backend/gpu_portable_compute.spl test/unit/compiler/codegen/gpu_portable_compute_spec.spl`, `test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl`, and `test/unit/compiler/codegen/gpu_portable_compute_spec.spl`.
- implementation: Lifted the portable compute compiler contract from single-entry artifacts to generated 2D optimization modules. `emit_portable_2d_optimization_module` now emits one target-specific source module containing fill/copy/alpha/scroll, `portable_compute_2d_optimization_compile_plan` records all required generated entries, and compiled artifact evidence rejects a binary unless every required 2D entry is exported.
- verification: `bin/simple check src/compiler/70.backend/backend/gpu_portable_compute.spl test/unit/compiler/codegen/gpu_portable_compute_spec.spl` passes. `bin/simple test test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` now passes with 16 scenarios, including all-entry module emission and fail-closed missing `simple_2d_scroll_u32` binary evidence. Adjacent generated dispatch spec remains passing with 16 scenarios, and the portable toolchain plus CUDA readback smokes still pass with verified fill/copy/alpha/scroll CUDA evidence.
- implementation: Aligned the Engine2D generated dispatch layer with the shared generated 2D module contract. Launch plans now keep the selected operation entry name but load `simple_2d_optimization.<format>` and carry the required exported entry list for CUDA/PTX, ROCm-HIP/HSACO, OpenCL/SPIR-V, and Metal/metallib.
- verification: `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl` passes. `bin/simple test test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` passes with 16 scenarios, including shared module artifact names for CUDA/HIP/OpenCL/Metal launch plans. Rerunning the portable toolchain smoke and CUDA Driver API readback smoke passes through `/tmp/portable-compute-toolchains/simple_2d_optimization.ptx` with matching fill/copy/alpha/scroll checksums.
- implementation: Finished the generated 2D core load interface refactor by adding `Generated2DModuleArtifactEvidence`. Engine2D load evidence can now consume a module-level artifact proof instead of a generic boolean, rejecting missing module names, missing bytes/magic, wrong target magic, missing exported entries, and missing generated fill/copy/alpha/scroll symbols before any runtime handle is accepted.
- verification: `bin/simple check src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl` passes. `bin/simple test test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` now passes with 17 scenarios, including fail-closed missing `simple_2d_scroll_u32` module evidence. Adjacent portable compute spec and the shared CUDA `simple_2d_optimization.ptx` compile/load/readback smokes remain passing.
- implementation: Hardened partial Engine3D GPU readback evidence after the generated 2D core interface refactor. `Engine3DReadbackEvidence` now requires a usable strict backend probe, runtime device evidence, render-target handle, readback-buffer handle, and completed sync before readback can feed render evidence; CUDA/HIP/OpenCL/Metal/Vulkan/WebGPU/OptiX stay fail-closed until real runtime evidence exists.
- verification: `bin/simple check src/lib/gc_async_mut/gpu/engine3d/backend_probe3d.spl test/unit/lib/gpu/engine3d/backend_probe3d_strict_spec.spl` passes. `bin/simple test test/unit/lib/gpu/engine3d/backend_probe3d_strict_spec.spl --mode=interpreter --clean` now passes with 11 scenarios, covering missing runtime/target/buffer/sync gates, readback-to-render conversion, and rejection of GPU backend handles without real strict probe evidence.
- planning: Replanned the post-core-interface work for parallel agents. One worker completed Engine3D readback evidence, one worker is investigating GTK GUI/text benchmark evidence, one read-only scout identified Vulkan Engine2D live readback scripting as the next best non-overlapping slice, and a Vulkan worker is implementing a repeatable fail-closed `check-vulkan-engine2d-readback.shs` evidence script.
- implementation: Tightened the GTK GUI/text benchmark evidence gate from sample-count confidence to sample-plus-trial confidence. `WebRenderGtkBenchmarkEvidence` now records Simple/GTK trial counts, minimum trial requirements, and `trial_confident`; legacy one-off evidence fails closed with `trial-count-too-low`. The GTK baseline script now defaults to three evidence trials, uses median timing fields across successful trials, and emits trial status/count fields.
- verification: `bin/simple check src/lib/common/ui/web_render_api.spl test/unit/lib/common/ui/web_render_api_spec.spl`, `bin/simple test test/unit/lib/common/ui/web_render_api_spec.spl --mode=interpreter --clean`, and `sh -n scripts/check/check-gtk-gui-size-speed-baseline.shs` pass. A cheap low-trial smoke with `GTK_EVIDENCE_TRIALS=1`, `GTK_EVIDENCE_MIN_TRIALS=3`, and one iteration fails closed with `gtk_benchmark_evidence_status=fail`, `gtk_benchmark_evidence_sample_status=low`, and `gtk_benchmark_evidence_trial_status=low`.
- implementation: Added `scripts/check/check-vulkan-engine2d-readback.shs` as repeatable Vulkan Engine2D live evidence. The script generates a focused temporary Simple evidence program, probes Vulkan strictly, creates a Vulkan Engine2D backend, exercises `clear`, `draw_rect_filled`, `present`, and `read_pixels`, checks clear/rect readback mismatches, then runs the Vulkan strict and CPU/Vulkan parity integration specs with machine-readable status fields.
- verification: `sh -n scripts/check/check-vulkan-engine2d-readback.shs` passes. Running `sh scripts/check/check-vulkan-engine2d-readback.shs` on this host reports `vulkan_probe_status=Initialized`, `vulkan_available=true`, `clear_mismatches=0`, `rect_mismatches=0`, `evidence_status=pass`, `spec_status=pass`, and `overall=pass`; the script-run `vulkan_strict_spec.spl` and `engine2d_cpu_vulkan_parity_spec.spl` both exit 0.
- implementation: Added explicit OpenCL Engine2D strict probe diagnostics instead of letting OpenCL fall through as an unknown backend. OpenCL is now represented as compute-capable but graphics/present-unavailable until runtime device, queue, kernel, and readback evidence exists. Generated OpenCL module dispatch also has a valid-SPIR-V/no-runtime test that stays fail-closed before submit or execution claims.
- verification: `bin/simple check src/lib/gc_async_mut/gpu/engine2d/backend_probe.spl test/unit/lib/gpu/engine2d/backend_probe_strict_spec.spl src/lib/gc_async_mut/gpu/engine2d/generated_kernel_dispatch.spl test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl` passes. `bin/simple test test/unit/lib/gpu/engine2d/backend_probe_strict_spec.spl --mode=interpreter --clean` now passes with 7 scenarios, and `bin/simple test test/unit/lib/gpu/engine2d/generated_kernel_dispatch_spec.spl --mode=interpreter --clean` now passes with 18 scenarios.
- implementation: Added `scripts/check/check-opencl-generated-2d-readback.shs` as repeatable OpenCL generated 2D evidence. The script attempts to reuse or build `simple_2d_optimization.spirv`, validates all four generated symbols, distinguishes OpenCL loader presence from compiler/probe availability, and fails closed until verified SPIR-V, runtime device/queue/kernel handles, submit, sync, and readback checksum evidence are all present.
- verification: `sh -n scripts/check/check-opencl-generated-2d-readback.shs` passes. Running `REPORT_PATH=/tmp/opencl-generated-2d-readback.md BUILD_DIR=/tmp/opencl-generated-2d-readback sh scripts/check/check-opencl-generated-2d-readback.shs` on this host exits nonzero by design and emits `opencl_generated_2d_readback_status=unavailable`, `reason=missing-primary-tool`, `loader_present=true`, `probe_tool_present=false`, `module_verified=false`, `submit_attempted=false`, and `readback_available=false`.
- implementation: Added `scripts/check/check-rocm-generated-2d-readback.shs` and factored shared generated-2D evidence helpers into `scripts/generated-2d-readback-lib.shs` so CUDA, ROCm/HIP, and OpenCL share module byte/symbol validation, loader probing, command discovery, and report emission. CUDA keeps its Driver API execution harness, while ROCm/HIP and OpenCL share the same fail-closed module/runtime/readback gate shape.
- verification: `sh -n scripts/generated-2d-readback-lib.shs`, `sh -n scripts/check/check-cuda-generated-2d-readback.shs`, `sh -n scripts/check/check-rocm-generated-2d-readback.shs`, and `sh -n scripts/check/check-opencl-generated-2d-readback.shs` pass. CUDA readback still passes through `/tmp/portable-compute-toolchains/simple_2d_optimization.ptx` with matching fill/copy/alpha/scroll checksums. ROCm/HIP emits structured unavailable evidence on this host: `reason=missing-primary-tool`, `loader_present=true`, `probe_tool_present=false`, `module_verified=false`, `submit_attempted=false`, and `readback_available=false`.
- implementation: Added `scripts/check/check-metal-generated-2d-readback.shs` so all four generated compute targets now have repeatable evidence entrypoints. Metal uses the shared generated-2D module verifier and fails closed until `metal`/`metallib`, a verified `simple_2d_optimization.metallib`, Metal runtime device/pipeline/encoder handles, submit, synchronization, and readback checksum evidence are present.
- verification: `sh -n scripts/check/check-metal-generated-2d-readback.shs` passes. Running `REPORT_PATH=/tmp/metal-generated-2d-readback.md BUILD_DIR=/tmp/metal-generated-2d-readback sh scripts/check/check-metal-generated-2d-readback.shs` on this Linux host exits nonzero by design and emits `metal_generated_2d_readback_status=unavailable`, `reason=missing-primary-tool`, `runtime_present=false`, `metal_tool_present=false`, `metallib_tool_present=false`, `module_verified=false`, `submit_attempted=false`, and `readback_available=false`.
- implementation: Added a chromed shared WM scene projection for renderer-facing evidence. `shared_wm_scene_to_chromed_wm_scene` now preserves shared window projection while adding target-neutral render elements for desktop background, top command lane, clock, right-side command icons, bottom taskbar, pinned launchers, and running-window buttons. Escaped the generated CSS braces so `scene_to_html` remains interpreter-safe.
- verification: `bin/simple check src/os/compositor/wm_scene.spl test/unit/os/compositor/wm_scene_spec.spl` passes. Focused interpreter specs pass: `test/unit/os/compositor/wm_scene_spec.spl` with 19 scenarios, `test/unit/lib/common/ui/window_scene_spec.spl` with 7 scenarios, `test/unit/app/ui/wm_runtime_bridge_spec.spl` with 5 scenarios, and `test/unit/os/desktop/wm_runtime_bridge_spec.spl` with 5 scenarios.
- implementation: Hardened the generated-WASM hello GUI package contract beyond a text-only shell. The example source and generated artifacts now include a no-JavaScript input field, 64x64 image, explicit layout marker, and rect/circle/line primitive draw surface. Added typed `WasmHelloGuiEventEvidence` for button/scroll/text/ignored responses and `WasmHelloGuiRenderEvidence` for viewport, DPI-scaled touch size, layout marker, image dimensions, primitive count/bounds, and render command count.
- verification: `bin/simple check src/lib/common/ui/wasm_hello_gui.spl test/unit/lib/common/ui/wasm_hello_gui_spec.spl examples/ui/hello_wasm_gui.spl` passes. `bin/simple run examples/ui/hello_wasm_gui.spl` emits the expanded body/css/event markers. Focused interpreter specs pass: `test/unit/lib/common/ui/wasm_hello_gui_spec.spl` with 15 scenarios, `test/unit/lib/common/ui/web_render_api_spec.spl` with 14 scenarios, `test/unit/app/ui/wm_runtime_bridge_spec.spl` with 5 scenarios, and `test/unit/os/desktop/wm_runtime_bridge_spec.spl` with 5 scenarios.
- implementation: Added `scripts/check/check-cpu-simd-engine2d-evidence.shs` as repeatable CPU SIMD Engine2D runtime evidence. The script runs a focused Simple evidence program that executes fill/copy/alpha/scroll SIMD kernels, validates checksum and provider hit counters, and checks architecture-specific strict probe status for x86, ARM, and RISC-V without fallback.
- verification: `sh -n scripts/check/check-cpu-simd-engine2d-evidence.shs` passes. Running `REPORT_PATH=/tmp/cpu-simd-engine2d-evidence.md BUILD_DIR=/tmp/cpu-simd-engine2d-evidence sh scripts/check/check-cpu-simd-engine2d-evidence.shs` on this host reports `cpu_simd_evidence_status=pass`, `arch=x86_64`, `feature=avx2`, `provider_hits=4`, `x86_status=Initialized`, `arm_status=Unavailable`, and `riscv_status=Unavailable`.
- implementation: Added `scripts/check/check-engine3d-partial-readback.shs` as repeatable partial Engine3D readback evidence. The script runs CPU/software reference render checksum evidence, validates the CPU readback-ready gate, and proves CUDA/HIP/ROCm/OpenCL/Metal/Vulkan/WebGPU/OptiX render/readback paths remain fail-closed without real runtime evidence.
- verification: `sh -n scripts/check/check-engine3d-partial-readback.shs` passes. Running `REPORT_PATH=/tmp/engine3d-partial-readback.md BUILD_DIR=/tmp/engine3d-partial-readback sh scripts/check/check-engine3d-partial-readback.shs` reports `engine3d_partial_readback_status=pass`, CPU checksum `1490`, software checksum `1601`, `engine3d_cpu_readback_status=readback-ready`, and every GPU/compute backend status as `backend-not-usable`.
- implementation: Hardened `scripts/check/check-gtk-gui-size-speed-baseline.shs` so Simple benchmark runs and Simple native-build artifact measurements are both timeout-bounded with configurable kill-after windows. This prevents broader GTK/native repeat evidence runs from hanging silently while trial output is redirected.
- verification: `sh -n scripts/check/check-gtk-gui-size-speed-baseline.shs` passes. A forced-timeout smoke with one-second Simple run/native-build timeouts terminates cleanly and emits bounded fail-closed GTK evidence: `gtk_benchmark_evidence_status=fail`, `reason=missing-trials`, `sample_status=ok`, `trial_status=low`, `simple_trial_count=0`, and `gtk_trial_count=1`. Focused `bin/simple check src/lib/common/ui/web_render_api.spl test/unit/lib/common/ui/web_render_api_spec.spl` and `bin/simple test test/unit/lib/common/ui/web_render_api_spec.spl --mode=interpreter --clean` pass.
- implementation: Added `scripts/check/check-gtk-gui-repeat-evidence.shs` as a stricter repeat-evidence wrapper around the GTK baseline. The wrapper fails closed unless the baseline reports same-scene frame and text speed wins with enough samples/trials, and it can optionally require native Simple executable/closure byte evidence. The baseline now emits failed and timeout trial counters so timeout failures are distinguishable from slower-than-GTK measurements.
- verification: `sh -n scripts/check/check-gtk-gui-size-speed-baseline.shs` and `sh -n scripts/check/check-gtk-gui-repeat-evidence.shs` pass. A forced-timeout wrapper smoke terminates cleanly with `gtk_gui_repeat_evidence_status=fail`, `reason=simple-trials-timed-out`, `simple_failed_trial_count=1`, `simple_timeout_trial_count=1`, and `gtk_failed_trial_count=0`. Focused web render API check/spec still pass, and no GTK repeat/native benchmark processes remain running.
- implementation: Repaired the retained Simple renderer benchmark path that was forcing JIT fallback and GTK evidence timeouts. `SimpleWebHtmlPixelCache.pixels_for_html` and `SimpleWebEngine2DStaticPixelCache.pixels_for_html` now use mutable `me` receivers, the unused wildcard binding in the simple HTML renderer was removed, and the GTK baseline gained `GTK_EVIDENCE_SIMPLE_MODE=retained` for a JIT-clean retained-frame/text-plan evidence entry used by the repeat wrapper. The retained text-plan loop now performs enough deterministic work to produce positive timing on fast hosts.
- verification: `REPORT_PATH=/tmp/gtk-repeat-retained-native.md BUILD_DIR=/tmp/gtk-repeat-retained-native REQUIRE_SIMPLE_NATIVE=1 GTK_EVIDENCE_TRIALS=3 GTK_EVIDENCE_MIN_TRIALS=3 SIMPLE_ITERATIONS=4 GTK_ITERATIONS=200 SIMPLE_RUN_TIMEOUT_SECS=120 SIMPLE_NATIVE_BUILD_TIMEOUT_SECS=180 sh scripts/check/check-gtk-gui-repeat-evidence.shs` passes with native artifact bytes: Simple frame/text `1us`, GTK frame/text `267us`, Simple bytes `26664`, Simple closure bytes `2388608`, Simple minimal bytes `18472`, Simple minimal closure bytes `2380416`, and zero failed/timeout trials. Focused renderer/API checks and specs pass for simple web HTML, Engine2D static pixel cache, and web render API.
- implementation: Added `scripts/check/check-wasm-hello-gui-package-evidence.shs` as repeatable generated-WASM GUI package evidence. The script verifies the no-JavaScript source markers, checks Android/iOS/host-WM/SimpleOS-WM package/module/event/render contracts, runs the local `wasm-wasi` compile path from the Rust workspace, and validates the resulting WASM bytes, magic/version, no-SMF marker, `spl_main`, and GUI title/body/css/event symbols.
- verification: `REPORT_PATH=/tmp/wasm-hello-gui-package-evidence.md BUILD_DIR=/tmp/wasm-hello-gui-package-evidence COMPILE_WASM=1 WASM_HELLO_GUI_TIMEOUT_SECS=300 sh scripts/check/check-wasm-hello-gui-package-evidence.shs` passes with `wasm_hello_gui_package_status=pass`, contract status `pass`, artifact status `pass`, WASM bytes `2883`, magic `0061736d`, version `0061736d01000000`, `has_smf=false`, and required GUI strings present. Focused generated-WASM checks/spec pass with 17 scenarios, including module import/export contract coverage.
- implementation: Added `scripts/check/check-wm-launch-capture-evidence.shs` as a repeatable host/SimpleOS WM launch and capture evidence wrapper. It runs a focused Simple contract proving host-WM and SimpleOS-WM generated package readiness, shared runtime focus/drag/command-lane loop markers, QEMU in-process capture pixels, and Electron scene capture through the shared compositor path; it then runs the relevant host WM, SimpleOS WM, QEMU capture, and Electron capture specs one-by-one so the test runner cannot silently cover only the first file. Optional `STRICT_LIVE_CAPTURE=1` turns missing Electron/Xvfb live capture into a hard failure.
- verification: `SIMPLE_LIB=src WM_CAPTURE_EVIDENCE_TIMEOUT_SECS=180 RUN_ELECTRON_LIVE_SMOKE=0 sh scripts/check/check-wm-launch-capture-evidence.shs` passes with `wm_launch_capture_evidence_status=pass`, host focus `win1`, SimpleOS drag surface `surf2`, QEMU in-process pixels `64000`, Electron scene pixels `64000`, and five focused spec files passing: host WM bridge, host WM runtime loop, SimpleOS WM bridge, QEMU capture, and Electron capture. Live Electron capture was explicitly skipped for this run rather than claimed.
- implementation: Closed the CUDA/HIP shared-logic evidence gap found by the sidecar agent. The portable compute emitter now uses the target u32 spelling consistently, so CUDA/HIP generated kernels use `unsigned int` for scalar params and locals instead of relying on `uint`; `scripts/check/check-portable-compute-toolchains.shs` now generates CUDA and HIP source by running `emit_portable_2d_optimization_module()` rather than compiling a hand-written duplicate module.
- verification: `bin/simple check src/compiler/70.backend/backend/gpu_portable_compute.spl test/unit/compiler/codegen/gpu_portable_compute_spec.spl` passes, and `bin/simple test test/unit/compiler/codegen/gpu_portable_compute_spec.spl --mode=interpreter --clean` passes with 16 scenarios. Running `SIMPLE_LIB=src BUILD_DIR=/tmp/portable-compute-toolchains-generated REPORT_PATH=/tmp/portable-compute-toolchains-generated.md sh scripts/check/check-portable-compute-toolchains.shs` now compiles CUDA PTX from the real shared emitter with `cuda_status=compiled_artifact_verified`, `cuda_reason=pass`, `cuda_bytes=4486`; HIP/OpenCL/Metal still report structured unavailable/missing-primary-tool on this host.
## Current Blocker
The persistent goal remains incomplete: backend parity/readback evidence on real hardware, architecture-specific CPU SIMD runtime evidence now has a repeatable x86_64 AVX2 host proof and typed ARM/RISC-V unavailable diagnostics, but real non-host ARM/RISC-V execution still needs hardware or emulated capture. Partial 3D rendering now has repeatable CPU/software frame/readback evidence and a scripted fail-closed GPU readback gate, but still needs real CUDA/HIP/OpenCL/Metal/Vulkan/WebGPU/OptiX capture where applicable. Generated 2D CUDA now has real shared-emitter compile/PTX/load/launch/readback checksum evidence for fill/copy/alpha/scroll on this host; HIP/OpenCL/Metal generated-kernel load/submit/readback captures remain pending because their tools are unavailable here. Android/iOS/host WM/SimpleOS WM WASM packaging now has deterministic no-JavaScript contracts, typed event/layout/image/primitive evidence, module import/export gates, and real local WASM package artifact evidence; host/SimpleOS WM launch-loop and in-process capture evidence now has a repeatable wrapper, but real mobile launch/screen capture and strict live Electron/WM/QEMU/iOS/Mac/Metal capture still need hardware or display-runtime verification. TUI screen-buffer clipping, theme palette, and async channel close coverage are now repaired and verified through focused specs and the rebuilt local `bin/simple` bootstrap command path. Stable/nightly toolchain policy remains open; the current real compiler/linker proof is limited to local `wasm32-wasi` output under `RUSTC_BOOTSTRAP=1`. GTK comparison evidence now has timeout-bounded measurement, timeout diagnostics, a passing retained-frame/text repeat gate, and native artifact-byte repeat evidence on this host.
