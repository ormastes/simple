# Feature: gui_hardening_current_plan

## Raw Request
$sp_dev (make it sure that all commit on main branch and pushed) complete next plan with spawn agents with pherallel often $sync with gh , check what is done and what is not yet first complete the plan gui_hardening_current_plan...md

## Task Type
todo

## Refined Goal
Complete the current GUI hardening plan by auditing the authoritative plan state, implementing the next open production Chrome parity slice, verifying it with focused GUI hardening gates, and syncing completed commits on main.

## Acceptance Criteria
- AC-1: The current `doc/03_plan/gui_hardening_current_plan_2026-06-01.md` remaining-work state is audited against repo evidence before implementation.
- AC-2: The next production Chrome parity slice reduces or otherwise advances the `site_0_google` production glyph paint/compositing blocker without adding fixture/oracle shortcuts.
- AC-3: Focused checks for the touched browser-engine/wm-compare paths pass or any failures are recorded with the exact failing command and reason.
- AC-4: Dirty changes are reconciled with existing main and `/tmp/simple-js-wasm-*` worktree state without reverting unrelated work.
- AC-5: Completed work is committed on `main`, rebased onto `origin/main` with a file-count guard, and pushed when verification is sufficient.

## Scope Exclusions
Full completion of all remaining GUI hardening lanes in one edit is not assumed; each continuation must preserve the full plan scope and keep uncompleted lanes open.

## Phase
dev-done

## Log
- dev: Created state file with 5 acceptance criteria (type: todo).
- audit: Current plan has ten open Remaining Work lanes; the smallest next GUI slice is `site_0_google` production glyph paint/compositing.
- audit: VCS is current with `origin/main` at `f77679210c`; no unpushed commits, but main has dirty files and `/tmp/simple-js-wasm-qemu-audit` contains overlapping process-grant tests.
- impl: Tried inline Engine2D text painting for famous-site production rendering after confirming a free helper lost `Engine2D` mutations; the visible text path regressed `site_0_google` from `differentPixels=2717` to `2895`/`3812`, so the renderer experiment was reverted.
- verify: `SIMPLE_LIB=src bin/simple check src/std/gc_async_mut/gpu/browser_engine/simple_web_engine2d_renderer.spl src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl test/01_unit/browser_engine/text_painter_spec.spl` passed before the experiment; after revert, `node tools/electron-shell/verify_famous_site_production_probe.js --sample=site_0_google` passed at the existing baseline.
- restart-audit: Agent audit found the long-term plan still has open production Chrome parity, 8K, vector-font GPU, JS/WASM, CommonJS/Node, generated GUI WASM, live Electron, QEMU/GTK, and tolerance-audit lanes. The active AC-2 slice remains unproven until a production glyph/compositing change advances `site_0_google` without regressing the 2717-pixel focused baseline.
- restart-audit: Sync audit found `main` and `origin/main` equal at `f77679210c`, but the jj working copy is dirty and there are non-main/unbookmarked jj heads. No push is justified until dirty changes are reconciled and verification is sufficient.
- restart-impl: Rechecked the crashed renderer experiments. Routing the styled corpus block through `simple_web_layout_render_html_pixels` regressed `site_0_google` to `differentPixels=6187`; drawing only overflow Engine2D glyphs regressed to `3300`; drawing smaller glyphs regressed to `3074`/`3056`. All were reverted.
- restart-verify: Restored the passing focused production baseline with `SIMPLE_LIB=src src/compiler_rust/target/release/simple run src/app/wm_compare/site_corpus_compat.spl --only=site_0_google --production-renderer --continue-on-fail --simple-timeout-ms=60000`; `node tools/electron-shell/verify_famous_site_production_probe.js --sample=site_0_google` passed at `differentPixels=2717`.
- restart-impl: Advanced AC-2 without a fixture/oracle render shortcut by adding production `text_ink_delta` diagnostics to `site_corpus_compat.spl` and requiring them in `verify_famous_site_production_probe.js`. The report now quantifies the missing glyph/compositing ink: div box has `different_pixels=1612`, `chrome_exact_black_pixels=45`, `simple_background_mismatch_pixels=1612`; overflow text has `different_pixels=1104`, `chrome_exact_black_pixels=22`, `simple_background_mismatch_pixels=1104`.
- restart-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl test/01_unit/browser_engine/text_painter_spec.spl` passed. Regenerated `site_0_google` production report with `--only=site_0_google --production-renderer --continue-on-fail`; `node tools/electron-shell/verify_famous_site_production_probe.js --sample=site_0_google` passed with `hasTextInkDelta=true`, `divBoxChromeExactBlackPixels=45`, `overflowChromeExactBlackPixels=22`, and `differentPixels=2717`.
- restart-verify: Required Simple checks for dirty compiler/lib/MCP paths passed: `check src/compiler`, `check src/lib`, `check src/app/mcp`, `check src/app/simple_lsp_mcp`, and `test test/02_integration/app/mcp_stdio_integration_spec.spl --mode=interpreter`. Focused Rust package tests were attempted for the dirty Rust runtime/compiler set; `cargo test -p simple-runtime` failed 2 tests and `cargo test -p simple-compiler` failed 31 tests, recorded in `build/cargo_test_simple_runtime_pre_sync.out` and `build/cargo_test_simple_compiler_pre_sync.out`.
- continue-audit: Worktree was clean and `main...origin/main` was `0 0` before the next slice. Remaining work still includes production glyph paint/compositing, but direct bitmap glyph overlays previously regressed exact pixels, so this continuation advanced the Engine2D/Simple Web exact-bitmap lane instead.
- continue-impl: Generated image-taskbar-command exact-bitmap evidence across Node, Bun, and live Electron using `SIMPLE_BIN=src/compiler_rust/target/release/simple` so the scripts use the verified runtime rather than the cached bootstrap wrapper.
- continue-verify: `check-node-simple-web-engine2d-image-taskbar-command-bitmap-evidence.shs`, `check-bun-simple-web-engine2d-image-taskbar-command-bitmap-evidence.shs`, and `check-electron-simple-web-engine2d-image-taskbar-command-bitmap-evidence.shs` all passed with `mismatch_count=0` and `blur_or_tolerance_used=false`.
- continue-audit: Re-ran the authoritative `site_0_google` production probe and found the child watchdog had drifted to hardcoded `bin/simple`, producing `unknown command 'run'` before pixel comparison; this made the Chrome text/compositing blocker unverifiable in the current worktree.
- continue-impl: Updated `src/app/wm_compare/site_corpus_compat.spl` so the child watchdog honors `SIMPLE_BIN`, then `SIMPLE_BINARY`, before falling back to `bin/simple`; this keeps the production probe on the same verified runtime as the parent and does not add fixture/oracle shortcuts.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl test/01_unit/browser_engine/text_painter_spec.spl` passed. With `SIMPLE_BIN=src/compiler_rust/target/release/simple`, the focused `site_0_google` production probe reached the real divergent state again: `simple.success=true`, `different_pixels=2717`, `layout_text_match=true`, `simple_first_width="105"`, `chrome_first_canvas_width="104.0625"`, `div_box.chrome_exact_black_pixels=45`, and `overflow_text.chrome_exact_black_pixels=22`.
- continue-verify: `node tools/electron-shell/verify_famous_site_production_probe.js --sample=site_0_google` passed with `status=PASS`, `reportFresh=true`, `computedDifferentPixels=2717`, `hasTextGeometryDelta=true`, and `hasTextInkDelta=true`.
- continue-impl: Added production `text_line_ink_delta` reporting derived from calibrated Simple paint runs, with one Chrome/Simple mismatch region per wrapped text line. The verifier now requires `hasTextLineInkDelta=true` and a per-line count matching the Simple layout line count, advancing the glyph/compositing blocker without rendering from Chrome pixels or corpus fixture oracles.
- continue-verify: Regenerated `site_0_google` production report with four line regions: `Google search` has `different_pixels=808`, `deterministic=761`, `compatibility=779`, and `fixture=368`. Focused checks passed: `check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl test/01_unit/browser_engine/text_painter_spec.spl`, `node tools/electron-shell/verify_famous_site_production_probe.js --sample=site_0_google`, and `test test/03_system/wm_compare/famous_site_corpus_spec.spl --mode=interpreter --timeout-ms=180000 --clean --format json` with `37/37` passing. Regenerated `doc/06_spec/system/wm_compare/famous_site_corpus_spec.md`; `find doc/06_spec -name '*_spec.spl' | wc -l` remains `0`.
- continue-audit: Selected the Generated GUI WASM remaining-work lane because current artifacts already cover widget-matrix behavior but the plan still requires moving widget-matrix-specific state patterns into shared per-widget helpers.
- continue-impl: Added shared `common.ui.builder.wasm_widget_state_event_response` and made the retained WASM dropdown/progress helpers public. Refactored `examples/ui/widget_matrix_wasm_gui.spl` so checkbox, dialog, table/list, image/tooltip, scroll, menu, and statusbar events route through the shared helper instead of bespoke per-widget string branches.
- continue-verify: Focused compile check passed for `src/lib/common/ui/builder.spl`, `examples/ui/widget_matrix_wasm_gui.spl`, and `test/01_unit/app/ui/builder_spec.spl`. `examples/ui/widget_matrix_wasm_gui.spl` run returned `wasm_gui:event:matrix_checkbox:changed`. `scripts/check/check-gui-wasm-cli-artifact.shs` passed with widget matrix WASM size `15028`, import count `0`; `scripts/check/check-gui-wasm-browser-execution-evidence.shs` passed with widget matrix `simple_app_event_probe=23`, event markers `23/23`, retained selectors `23/23`, nonzero boxes `23/23`, and retained event mutations `22/22`. `test/01_unit/app/ui/builder_spec.spl` remains an unrelated focused unit gap at `43 passed / 2 failed`; no new helper assertions are left in that spec.
- continue-audit: Selected the tolerance-audit lane because `html_compat` already enforced exact acceptance but only exposed the policy as text; the plan calls for quarantining perceptual/tolerance claims with exact pixels as the acceptance rule.
- continue-impl: Added explicit SDN policy flags to html-compat reports: `acceptance_policy: (exact_required: true perceptual_diagnostic_only: true tolerance_acceptance_allowed: false)`, and updated the system spec to require them.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/app/wm_compare/html_compat_part1.spl src/app/wm_compare/html_compat_part3.spl test/03_system/wm_compare/html_compat_spec.spl` passed. `SIMPLE_LIB=src SIMPLE_BIN=src/compiler_rust/target/release/simple src/compiler_rust/target/release/simple test test/03_system/wm_compare/html_compat_spec.spl --mode=interpreter --timeout-ms=180000 --clean --format json` passed `17/17`. Regenerated `doc/06_spec/system/wm_compare/html_compat_spec.md`; the doc layout guard remains `0`.
- continue-audit: Selected the pure GUI release/perf lane because the dirty worktree contains focused `src/lib/gui` and `src/app/gui_perf` artifacts aligned with release-grade no-fallback GUI hot-response evidence.
- continue-verify: Focused check passed for `src/lib/gui/pure_core.spl`, `src/lib/gui/pure_smf_dynlib_perf.spl`, `src/app/gui_perf/smf_dynlib_probe_core.spl`, `src/app/gui_perf/smf_dynlib_probe.spl`, and the four matching specs. Individual specs passed: `pure_core_spec` `6/6`, `pure_smf_dynlib_perf_spec` `7/7`, `pure_gui_release_lane_spec` `7/7`, and `smf_dynlib_probe_spec` `4/4`. The CLI emitted `GUI_DYNLIB_PERF artifact= loader=direct_simple symbol=gui_dynlib_hot_probe_tick call_source=direct_simple samples=128 warmup=16 ... threshold_us=1000 pass=false error=missing-artifact-path`, proving the gate fails closed without a real SMF/dynlib artifact. Regenerated the four `doc/06_spec` manuals; the doc layout guard remains `0`.
- continue-audit: Re-audited the current plan and selected the QEMU/GTK lane because the canonical live-QMP report still said the host GTK scene was `not-run` whenever a real QMP socket was available, leaving perf evidence disconnected from the authoritative live capture path.
- continue-impl: Updated `scripts/check/check-qemu-gtk-wm-capture-evidence.shs` so the host GTK GL WM exact-scene evidence runs even when auto-QMP live capture succeeds. The report now keeps `qemu-side perf` explicitly unavailable until a guest harness exists, while adding separate host perf baseline fields.
- continue-verify: `SIMPLE_BIN=src/compiler_rust/target/release/simple QEMU_AUTO_LAUNCH_SIMPLEOS_DESKTOP=1 sh scripts/check/check-qemu-gtk-wm-capture-evidence.shs` passed. The regenerated report has live QMP status `pass`, `786432` live pixels, `10` sample matches, `0` sample mismatches, `0` scene mismatches, and host perf baseline `pass` with Simple `1us`, GTK `301us`, `200` iterations, comparison available, `0` RGBA mismatches, and `blur_or_tolerance_used=false`. A prior no-QMP focused run exited `1` as expected with `missing-qmp-socket` but also proved the new host baseline path; the auto-QMP rerun restored the canonical report to `pass`.
- continue-audit: Re-audited remaining lanes and selected the CommonJS/Node timers/event-loop gap because `process.nextTick` was exposed in the dirty worktree but queued callbacks were drained through the generic function invoker, leaving global mutation unproven.
- continue-impl: Updated `src/lib/nogc_sync_mut/js/engine/interpreter_async.spl` so timer/nextTick drain invokes callbacks through `_invoke_promise_callback`, the same global-aware callback path used by promise microtasks. Added `test/03_system/feature/js/node_process_next_tick_spec.spl` with focused SPipe scenarios for both global `process.nextTick` and `require('process').nextTick`.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl test/03_system/feature/js/node_process_next_tick_spec.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `SIMPLE_LIB=src SIMPLE_BIN=src/compiler_rust/target/release/simple src/compiler_rust/target/release/simple test test/03_system/feature/js/node_process_next_tick_spec.spl --mode=interpreter --timeout-ms=180000 --clean --format json` passed `2/2`. The broader `node_api_conformance_spec.spl` remains an unrelated residual gap at `152 passed / 1 failed`; it was not used as pass evidence for this slice. Regenerated `doc/06_spec/feature/js/node_process_next_tick_spec.md`; the doc layout guard remains `0`.
- continue-audit: Selected the structural layout diagnostics slice because current Chrome parity work needs pre-pixel line/geometry evidence, and the worktree already had a focused helper/spec that needed verification against the authoritative famous-site report path.
- continue-impl: Added/verified the shared `structural_layout_report` contract and famous-site layout-report attachment so Chrome metrics are recorded as source A, Simple layout as source B, and structural mismatches are emitted before exact-pixel acceptance.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/app/wm_compare/site_corpus_layout_report.spl src/app/wm_compare/structural_layout_report.spl test/03_system/wm_compare/structural_layout_report_spec.spl test/03_system/wm_compare/famous_site_corpus_spec.spl` passed. The focused structural spec passed `5/5`, and `test/03_system/wm_compare/famous_site_corpus_spec.spl` passed `37/37` with the structural assertions. `spipe-docgen` for the structural spec is currently blocked by the unrelated `unknown extern function: shell` semantic error, so `doc/06_spec/system/wm_compare/structural_layout_report_spec.md` remains a manual generated-spec mirror. The doc layout guard returned `0`.
- continue-audit: Rechecked the shared TUI/GUI structural and WM text-access slice after additional dirty work landed in `src/lib/common/ui/win_text_access.spl`.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/app/wm_compare/structural_layout_report.spl src/app/wm_compare/site_corpus_layout_report.spl src/lib/common/ui/win_text_access.spl test/03_system/wm_compare/structural_layout_report_spec.spl test/03_system/app/wm_text_access_mcp/feature/wm_text_access_mcp_spec.spl` passed. The structural layout report spec passed `5/5`, the WM text-access MCP spec passed `10/10`, and the doc layout guard returned `0`. Placeholder scan matches were limited to historical state/plan prose, not touched executable artifacts.
- continue-audit: Selected the backend-qualified measurement lane because the plan requires NFR evidence for Metal, Vulkan, CUDA, and CPU SIMD timing/RSS/size claims without accepting fallback measurements as accelerated evidence.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/app/wm_compare/backend_measurement_report.spl test/03_system/wm_compare/backend_measurement_report_spec.spl` passed. `SIMPLE_LIB=src SIMPLE_BIN=src/compiler_rust/target/release/simple src/compiler_rust/target/release/simple test test/03_system/wm_compare/backend_measurement_report_spec.spl --mode=interpreter --timeout-ms=180000 --clean --format json` passed `5/5`. The mirrored manual exists for the selected NFR C measurement contract; `spipe-docgen` remains blocked by the unrelated `unknown extern function: shell` semantic error, and the doc layout guard returned `0`.
- continue-sync: Ran `jj git fetch` for the requested GitHub sync checkpoint; remote state reported `Nothing changed`. The repository is still on a detached Git branch with dirty jj working-copy changes on top of `main*`, so no rebase or push was attempted.
- continue-verify: Rechecked backend measurement capture/report together in the release runtime. `check src/app/wm_compare/backend_measurement_capture.spl src/app/wm_compare/backend_measurement_report.spl test/03_system/wm_compare/backend_measurement_capture_spec.spl test/03_system/wm_compare/backend_measurement_report_spec.spl` passed; `backend_measurement_capture_spec` passed `3/3`; `backend_measurement_report_spec` passed `5/5`; the backend capture manual lists 3 active scenarios; placeholder and doc layout guards returned clean.
- continue-sync: Ran another `jj git fetch` checkpoint; remote state again reported `Nothing changed`. No rebase or push was attempted because the working copy remains broad and dirty.
- continue-audit: Selected the SMF/dynlib release lane because the earlier gate only proved fail-closed `missing-artifact-path`, while current dirty artifacts add an SMF role-2 library envelope wrapper and explicit host-dynlib rejection evidence.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/app/gui_perf/smf_dynlib_artifact.spl src/app/gui_perf/smf_wrap_host_dynlib.spl src/app/gui_perf/smf_dynlib_probe_core.spl test/01_unit/app/gui_perf/smf_dynlib_artifact_spec.spl test/01_unit/app/gui_perf/smf_dynlib_probe_spec.spl` passed. `smf_dynlib_artifact_spec` passed `3/3`, `smf_dynlib_probe_spec` passed `9/9`, both mirrored manuals exist, and the host dynlib report records a callable real host dynlib at `p50_us=17`/`p95_us=19` that is still rejected as `not-smf-dynlib`. Placeholder scan matches were legitimate SMF `stub` terminology, not placeholder pass markers; the doc layout guard returned `0`.
- continue-sync: Ran `jj git fetch`; remote state reported `Nothing changed`. The working copy remains dirty and detached from a Git branch, so no rebase or push was attempted.
- continue-verify: Re-ran a pure Simple focused checkpoint with `src/compiler_rust/target/release/simple` directly because `bin/simple` currently resolves to a bootstrap compiler without the `test` command. Passing specs: `smf_spec`, `smf_dynlib_probe_spec`, `node_process_next_tick_spec`, `backend_measurement_report_spec`, `backend_measurement_capture_spec`, and `structural_layout_report_spec`; the doc layout guard returned `0`. This is evidence refresh only and leaves the open Chrome parity, broader app-matrix, and guest-side perf blockers unchanged.
- continue-sync: Ran another `jj git fetch` checkpoint; remote state reported `Nothing changed`. No rebase or push was attempted because the working copy remains broad, dirty, and detached from a Git branch.
- continue-verify: Refreshed the primary `site_0_google` production Chrome parity probe with `SIMPLE_BIN=src/compiler_rust/target/release/simple`. The production report is fresh, verifier status is `PASS`, `differentPixels=2717`, `computedDifferentPixels=2717`, `layoutTextMatch=true`, `hasTextLineInkDelta=true`, and `textLineInkDeltaCount=4`. `site_corpus_compat.spl` and `famous_site_corpus_spec.spl` typecheck, `famous_site_corpus_spec.spl` passes `37/37`, and the doc layout guard returned `0`. This keeps the blocker current but still open because production rendering remains divergent, not Chrome-exact.
- continue-verify: Re-ran the full famous-site corpus spec through `src/compiler_rust/target/release/simple`; it passed with `37` passed and `0` failed, and the doc layout guard returned `0`. This refreshes regression evidence only; `site_0_google` production remains open at `differentPixels=2717`.
- continue-sync: Ran `jj git fetch` after the full corpus spec; remote state reported `Nothing changed`. No rebase or push was attempted because the working copy remains broad, dirty, and detached from a Git branch.
- continue-impl: Extended `structural_layout_report` with `structural_box_layout_report_sdn` so GUI box comparisons serialize source counts, backend evidence, pixel links, and both labeled box lists before pixel comparison.
- continue-verify: `check src/app/wm_compare/structural_layout_report.spl test/03_system/wm_compare/structural_layout_report_spec.spl` passed, `structural_layout_report_spec` passed `7/7`, `spipe-docgen` regenerated a 7-scenario manual containing the GUI box report flow, `famous_site_corpus_spec` passed `37/37`, and the doc layout guard returned `0`. This improves structural geometry evidence while leaving the `site_0_google` glyph/compositing blocker open.
- continue-impl: Hardened `comparison_failure_report` to serialize the exact-only acceptance policy (`exact_required=true`, `perceptual_diagnostic_only=true`, `tolerance_acceptance_allowed=false`) alongside capture, metadata, structural, exact-pixel, backend, and primary failure statuses.
- continue-verify: `check src/app/wm_compare/comparison_failure_report.spl test/03_system/wm_compare/comparison_failure_report_spec.spl` passed, `comparison_failure_report_spec` passed `5/5`, the generated manual has 5 active scenarios and includes the no-tolerance policy, `html_compat_spec` passed `17/17`, `structural_layout_report_spec` passed `7/7`, and the doc layout guard returned `0`.
- continue-impl: Tightened the macOS arm64 SMF/dynlib evidence runner so it accepts only rows with `loader=smf_dynlib`, `call_source=dynlib_symbol_call`, `p99_us=...`, `threshold_us=1000`, `pass=true`, and `error=`, rejecting host-dynlib, direct-Simple, missing-p99, loose-threshold, and `not-smf-dynlib` rows.
- continue-verify: `check src/app/gui_perf/macos_smf_dynlib_evidence_core.spl src/app/gui_perf/macos_smf_dynlib_evidence.spl test/01_unit/app/gui_perf/macos_smf_dynlib_evidence_spec.spl` passed, `macos_smf_dynlib_evidence_spec` passed `5/5`, `spipe-docgen` produced a 5-scenario manual, the Linux run emitted `GUI_MAC_SMF_DYNLIB_EVIDENCE status=skip ... reason=requires-macos-arm64`, `smf_dynlib_probe_spec` passed `9/9`, a dirty-diff scan found no new Rust/runtime `rt_*` SMF dynlib helpers, and the doc layout guard returned `0`.
- continue-impl: Fixed generated-C native layout section-map application so inserted function attributes are accompanied by their `SIMPLE_LAYOUT_SECTION_*` macro definitions, preventing undefined macro output in the `--simple-layout-section-map` compile path.
- continue-verify: `check src/app/compile/native_layout_section_map.spl src/app/compile/llvm_direct.spl test/03_system/app/compile/feature/native_layout_section_map_spec.spl` passed, `native_layout_section_map_spec` passed `6/6`, `spipe-docgen` produced a 6-scenario manual, and the doc layout guard returned `0`.
- continue-impl: Strengthened `pure_gui_release_lane_spec` so every guarded source path is asserted non-empty before dependency scanning; missing files can no longer pass as dependency-free.
- continue-verify: `check test/01_unit/lib/gui/pure_gui_release_lane_spec.spl src/app/gui_perf/smf_dynlib_probe_core.spl src/app/gui_perf/macos_smf_dynlib_evidence_core.spl src/app/gui_perf/macos_smf_dynlib_evidence.spl src/app/gui_perf/smf_wrap_host_dynlib.spl src/app/gui_perf/pure_gui_hot_dynlib_export.spl` passed, `pure_gui_release_lane_spec` passed `10/10`, `smf_dynlib_probe_spec` passed `9/9`, `spipe-docgen` produced a 10-scenario manual, and the doc layout guard returned `0`.
- continue-impl: Corrected `pure_gui_release_lane_spec` so strict release-surface dependency checks apply only to `pure_release`, pure command/perf modules, SMF probe/evidence/wrapper/export paths, while hosted GUI entity files are checked for native GUI runtime calls only.
- continue-verify: `check test/01_unit/lib/gui/pure_gui_release_lane_spec.spl src/lib/gui/pure_release.spl` passed, `pure_gui_release_lane_spec` passed `12/12`, `smf_dynlib_probe_spec` passed `9/9`, `spipe-docgen` produced a 12-scenario manual, and the doc layout guard returned `0`.
- continue-impl: Hardened the SMF/dynlib mapped-loader tests so failed ELF mapping and non-native SMF bytes must roll back dynlib registry handles instead of leaving stale lookup entries.
- continue-verify: `check src/os/kernel/loader/loader_api.spl test/01_unit/os/kernel/loader/loader_api_spec.spl` passed, `loader_api_spec` passed `8/8`, `smf_spec` passed `18/18`, `smf_dynlib_probe_spec` passed `9/9`, `spipe-docgen` produced a complete 8-scenario loader API manual, and the doc layout guard returned `0`.
- continue-impl: Expanded `smf_dynlib_artifact_spec` so the SMF artifact contract must keep QEMU/macOS execution explicitly `not-run`, reject non-SMF bytes, reject empty SMF library envelopes, and include both not-run reasons in missing-artifact rows.
- continue-verify: With the current-source debug compiler, `check src/app/gui_perf/smf_artifact_contract.spl src/app/gui_perf/smf_dynlib_artifact.spl test/01_unit/app/gui_perf/smf_dynlib_artifact_spec.spl` passed, `smf_dynlib_artifact_spec` passed `8/8`, `smf_dynlib_probe_spec` passed `9/9`, the missing-artifact CLI exited `1` with `GUI_SMF_ARTIFACT_CONTRACT status=missing ... qemu_status=not-run ... macos_status=not-run`, `spipe-docgen` regenerated the 8-scenario manual, and the doc layout guard returned `0`. The release compiler binary remains stale for `rt_file_wrap_smf_dynlib`; the debug compiler matches the current source extern table.
- continue-impl: Extended the famous-site production corpus report with structured no-tolerance policy flags inside the `compare` record: `exact_required=true`, `perceptual_diagnostic_only=true`, and `tolerance_acceptance_allowed=false`. The focused Electron verifier now fails closed when those flags are missing.
- continue-verify: `check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl` passed. Regenerated the `site_0_google` production report and `node tools/electron-shell/verify_famous_site_production_probe.js --sample=site_0_google` passed with `status=PASS`, `differentPixels=2717`, `computedDifferentPixels=2717`, `textLineInkDeltaCount=4`, and `hasExactAcceptancePolicyFlags=true`. `famous_site_corpus_spec` passed `37/37`, `spipe-docgen` regenerated the manual, and the doc layout guard returned `0`. This advances the tolerance-audit lane without closing the production Chrome glyph/compositing blocker.
- continue-audit: Selected the generated GUI WASM additional-app coverage lane because the plan still listed only widget-state-machine evidence while the repo has a broader browser execution harness for `hello`, `widget_matrix`, and `builder_matrix`.
- continue-verify: `SIMPLE_BIN=src/compiler_rust/target/release/simple sh scripts/check/check-gui-wasm-cli-artifact.shs` passed for `hello_wasm_gui.wasm`, `widget_matrix_wasm_gui.wasm`, and `builder_matrix_wasm_gui.wasm` with import count `0`. `SIMPLE_BIN=src/compiler_rust/target/release/simple BUILD_DIR=build/gui_wasm_browser_execution_widget_behavior REPORT_PATH=doc/09_report/gui_wasm_browser_execution_widget_behavior_2026-06-01.md sh scripts/check/check-gui-wasm-browser-execution-evidence.shs` passed with browser execution targets `hello,widget_matrix,builder_matrix`, byte sizes `4380/15028/9486`, zero imports, retained selectors `7/7`, `23/23`, and `7/7`, retained nonzero boxes `7/7`, `23/23`, and `7/7`, and retained event mutations `4/4`, `22/22`, and `5/5`. The doc layout guard returned `0`. This broadens generated GUI WASM app coverage but does not close the remaining broader app-matrix and JS/WASM semantic lanes.
- continue-audit: Selected the 8K color/image lane because the plan still needs broader compressed-raster/profile evidence, and the generated 8K report had drifted behind the focused image decode spec's JPEG XL non-default color fail-closed coverage.
- continue-impl: Restored the `jpegxl_nondefault_color_fail_closed` line in `scripts/check/check-gui-color-image-pipeline-8k-evidence.shs` so regenerated reports explicitly tie non-default JPEG XL structured color metadata to `jpegxl-structured-color-transform-pending` instead of silently accepting default sRGB.
- continue-verify: `SIMPLE_BIN=src/compiler_rust/target/release/simple sh scripts/check/check-gui-color-image-pipeline-8k-evidence.shs` passed and regenerated `doc/09_report/gui_color_image_pipeline_8k_evidence_2026-06-01.md` with `gui_color_image_pipeline_8k_status=pass`, `framebuffer_bytes=132710400`, `lab_xyz_full_frame_bytes=796262400`, `initializes_color_transforms=false`, `initializes_tiff_decoder=false`, `initializes_jpegxl_decoder=false`, `default_semantic_space=cielab`, `connection_space=cie_xyz`, and `red_roundtrip_ok=true`. Focused specs passed: `image_decode_spec` `77/77` and `tiff_image_raster_spec` `17/17`; the doc layout guard returned `0`. Full JPEG XL pixel decode remains open.
- continue-impl: Added focused JPEG XL full-container `jxlc` and split-partial `jxlp` fixtures with non-default structured color bits to `examples/browser/test/paint/image_decode_spec.spl`; both must return `structured-color-nondefault-pending`.
- continue-verify: `check examples/browser/test/paint/image_decode_spec.spl src/lib/common/image/image_info.spl` passed, `image_decode_spec.spl` passed `77/77`, `scripts/check/check-gui-color-image-pipeline-8k-evidence.shs` regenerated the 8K report with direct/full-container/split-partial fail-closed wording, and the doc layout guard returned `0`. JPEG XL codestream pixel decode, real non-identity ICC/profile transforms, and web/WM image integration remain open.
- continue-audit: Selected the CommonJS/Node residual conformance gap because the broader `node_api_conformance_spec.spl` still had one failing process sandbox scenario after the prior `process.nextTick` slice.
- continue-impl: Added `JsRuntime.grant_node_process(command)` in `src/lib/nogc_sync_mut/js/engine/runtime.spl`, using the same sanitized `__simple_node_process_grant_...` marker contract consumed by `interpreter_native.spl` for `child_process.spawn`.
- continue-verify: `SIMPLE_LIB=src src/compiler_rust/target/release/simple check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl test/03_system/feature/js/node_process_next_tick_spec.spl` passed. `node_api_conformance_spec.spl` now passes `153/153`, `node_process_next_tick_spec.spl` passes `2/2`, and the doc layout guard returned `0`. This closes the prior residual conformance failure; broader CommonJS/Node package resolution, streams, networking, and sandboxed `fs` capability design remain open.
- continue-audit: Selected the QEMU/SMF release evidence lane because the plan still needs guest-side QEMU/GTK proof, and the current contract-only ARM64 SMF parity row could accept a role-2 ARM64 SMF envelope without proving it targeted the release hot-call symbol.
- continue-impl: Tightened `src/app/gui_perf/qemu_arm64_smf_parity.spl` so QEMU ARM64 SMF parity requires `symbol=gui_dynlib_hot_probe_tick`, serializes the symbol in `GUI_QEMU_ARM64_SMF_PARITY`, and fails closed for wrong-symbol artifacts. Updated the macOS SMF evidence row validator to reject wrong-symbol QEMU parity rows and improved the generated parity manual steps.
- continue-verify: `check src/app/gui_perf/qemu_arm64_smf_parity.spl src/app/gui_perf/qemu_arm64_smf_parity_evidence.spl src/app/gui_perf/macos_smf_dynlib_evidence_core.spl src/app/gui_perf/macos_smf_dynlib_evidence.spl test/01_unit/app/gui_perf/qemu_arm64_smf_parity_spec.spl test/01_unit/app/gui_perf/macos_smf_dynlib_evidence_spec.spl test/01_unit/lib/gui/pure_gui_release_lane_spec.spl` passed. Focused specs passed: `qemu_arm64_smf_parity_spec.spl` `3/3`, `macos_smf_dynlib_evidence_spec.spl` `7/7`, and `pure_gui_release_lane_spec.spl` `13/13`. `spipe-docgen` regenerated a 3-scenario QEMU parity manual, the missing-artifact CLI emitted `GUI_QEMU_ARM64_SMF_PARITY status=contract-fail ... symbol=gui_dynlib_hot_probe_tick ... live_qemu=false`, and the doc layout guard returned `0`. Live guest-side QEMU/GTK performance and representative app-window rendering remain open.
- continue-audit: Continued the SMF dynlib hot-response release gate because partial samples and wrong-symbol dynlib calls would be misleading release evidence even when the loader/call-source fields look valid.
- continue-impl: Extended `std.gui.pure_smf_dynlib_perf` so `GuiDynlibPerfReport` records `expected_sample_count`, report rows include `expected_samples=...`, partial measured sample sets fail with `error=incomplete-samples`, and any symbol other than `gui_dynlib_hot_probe_tick` fails with `error=wrong-symbol`. Tightened `gui_mac_smf_dynlib_accepts_probe_row` to require `symbol=gui_dynlib_hot_probe_tick`.
- continue-verify: `check src/lib/gui/pure_smf_dynlib_perf.spl src/app/gui_perf/smf_dynlib_probe_core.spl src/app/gui_perf/macos_smf_dynlib_evidence_core.spl test/01_unit/lib/gui/pure_smf_dynlib_perf_spec.spl test/01_unit/app/gui_perf/macos_smf_dynlib_evidence_spec.spl test/01_unit/lib/gui/pure_gui_release_lane_spec.spl` passed. Focused specs passed: `pure_smf_dynlib_perf_spec.spl` `10/10`, `macos_smf_dynlib_evidence_spec.spl` `7/7`, and `pure_gui_release_lane_spec.spl` `13/13`. Regenerated the pure perf and macOS SMF evidence manuals, and the doc layout guard returned `0`.
- continue-audit: Selected the SimpleOS SMF dynload evidence slice because the plan still needs guest-side QEMU/GTK proof, and this moves the lower-level SimpleOS registry/symbol part of the same hot-call artifact path without claiming live QEMU rendering.
- continue-impl: Added/finished `src/app/gui_perf/simpleos_smf_dynload.spl` and `src/app/gui_perf/simpleos_smf_dynload_evidence.spl` so the pure GUI role-2 ARM64 SMF envelope is validated through `loader_dynopen_smf_library_bytes_for_arch`, resolves `gui_dynlib_hot_probe_tick` through `loader_dynsym`, and emits `GUI_SIMPLEOS_SMF_DYNLOAD`. Expanded `test/01_unit/app/gui_perf/simpleos_smf_dynload_spec.spl` to cover successful ARM64 symbol resolution, wrong symbol, non-ARM64 envelope, and missing artifact bytes.
- continue-verify: `check src/app/gui_perf/simpleos_smf_dynload.spl src/app/gui_perf/simpleos_smf_dynload_evidence.spl test/01_unit/app/gui_perf/simpleos_smf_dynload_spec.spl test/01_unit/lib/gui/pure_gui_release_lane_spec.spl src/os/kernel/loader/loader_api.spl` passed. `simpleos_smf_dynload_spec.spl` passed `4/4`, `pure_gui_release_lane_spec.spl` passed `13/13`, the no-artifact CLI emitted `GUI_SIMPLEOS_SMF_DYNLOAD status=simpleos-dynload-fail ... error=bad-smf-contract`, `spipe-docgen` regenerated a complete 4-scenario manual, and the doc layout guard returned `0`. Live QEMU execution and rendered app-window parity remain open.
- continue-audit: Selected the JS/WebEngine/WASM lane for evidence refresh because the plan still calls for broader instruction execution, traps, imports, table/global exports, async instantiate, and typed-array behavior.
- continue-verify: `check test/03_system/app/browser/feature/webgpu_js_wasm_simple_spec.spl test/01_unit/lib/common/web/browser_session_wasm_host_spec.spl test/01_unit/lib/common/web/browser_session_fetch_wasm_chain_spec.spl` passed. Focused specs passed: `webgpu_js_wasm_simple_spec.spl` `106/106`, `browser_session_wasm_host_spec.spl` `101/101`, and `browser_session_fetch_wasm_chain_spec.spl` `1/1`. Added a manual header to `webgpu_js_wasm_simple_spec.spl`; `spipe-docgen` regenerated `doc/06_spec/system/app/browser/feature/webgpu_js_wasm_simple_spec.md` as complete instead of stub, and the doc layout guard returned `0`. This refreshes bounded JS/WebEngine/WASM evidence while leaving broader WASM semantics open.
- continue-audit: Re-synced with GitHub (`jj git fetch`; `main...origin/main` `0 0`) and selected the Chrome parity tolerance-audit slice because the production verifier already required structured exact-pixel policy flags but lacked fail-closed evidence for a report missing those flags.
- continue-impl: Added a test-only `--drop-acceptance-policy-flags-for-test` path to `tools/electron-shell/verify_famous_site_production_probe.js` and a focused Simple system scenario proving the production probe fails with `missing structured exact-pixel acceptance policy flags` when the report omits `exact_required=true`, `perceptual_diagnostic_only=true`, and `tolerance_acceptance_allowed=false`.
- continue-verify: `check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl` passed. The normal production probe passed with `status=PASS`, `differentPixels=2717`, `computedDifferentPixels=2717`, `reportFresh=true`, and `hasExactAcceptancePolicyFlags=true`; the mutated probe exited `1` with `hasExactAcceptancePolicyFlags=false`. `famous_site_corpus_spec.spl` passed `38/38`, `spipe-docgen` regenerated `doc/06_spec/system/wm_compare/famous_site_corpus_spec.md` with the new scenario, and the doc layout guard returned `0`. Production rendering remains divergent and the broader Chrome glyph/compositing blocker stays open.
- continue-audit: Re-synced with GitHub (`jj git fetch`; `main...origin/main` `0 0`) and selected the CommonJS/Node API lane because the current conformance suite covers many builtins but package resolution remained weakly specified for non-builtin modules.
- continue-impl: Changed native `require()` so unrecognized package specifiers return a denied module directly instead of entering the builtin require-cache path that aborts evaluation for unknown packages. Added SPipe coverage for `require('missing-package').error == "module-denied"` and the preserved denied `specifier`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `154/154`, `spipe-docgen` regenerated `doc/06_spec/feature/js/node_api_conformance_spec.md` with the missing-package denial scenario, and the doc layout guard returned `0`. A broader granted CommonJS module execution attempt exposed parser/runtime gaps and is recorded in `doc/08_tracking/bug/commonjs_granted_module_execution_limitations_2026-06-01.md`; real module execution/cache semantics remain open.
- continue-impl: Added explicit `JsRuntime.grant_node_module_text_export(specifier, export_name, value)` support and taught native `require()` to resolve those in-memory granted modules through a separate `module:<specifier>` cache key before falling back to ungranted `module-denied`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `156/156`, `spipe-docgen` regenerated `doc/06_spec/feature/js/node_api_conformance_spec.md` with granted text-export and repeated-require cache scenarios, and the doc layout guard returned `0`. Full CommonJS source execution, slash-bearing specifier support, `module.exports` semantics, and filesystem-style package resolution remain open.
- continue-impl: Added bounded native `stream`/`node:stream` support with `Readable.from(...)`, readable `read()` metadata, `Writable()` write/end state tracking, and shared require-cache behavior across `stream` aliases.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `160/160`, `spipe-docgen` regenerated `doc/06_spec/feature/js/node_api_conformance_spec.md` with four stream scenarios, and the doc layout guard returned `0`. Full stream backpressure, piping, async iteration, event scheduling, and host I/O integration remain open.
- continue-impl: Added native `timers`/`node:timers` module support that exposes `setTimeout`, `setInterval`, `clearTimeout`, and `clearInterval` through the existing runtime timer scheduler.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `164/164`, `spipe-docgen` regenerated `doc/06_spec/feature/js/node_api_conformance_spec.md` with timers shape/schedule/cancel/cache scenarios, and the doc layout guard returned `0`. Broader event-loop phase ordering, interval semantics, host I/O integration, and full Node timer object behavior remain open.
- continue-impl: Added bounded `fs`/`node:fs` directory method coverage with native `readdirSync` and `mkdirSync` exports plus deterministic compatibility fast paths for sandboxed directory metadata under explicit grants.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `165/165`, `spipe-docgen` regenerated `doc/06_spec/feature/js/node_api_conformance_spec.md` with directory method shape, denied ungranted access, and granted synthetic directory metadata scenarios. Full host filesystem access, directory arrays, recursive mkdir options, native-grant unification, and filesystem-style package resolution remain open.
- continue-impl: Persisted `JsRuntime` interpreter mutations for `eval` and Node grant helpers, added sanitized file-grant markers, and extended fs compatibility coverage so a granted prefix authorizes a child path while rejecting a sibling prefix.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `166/166`, `spipe-docgen` regenerated `doc/06_spec/feature/js/node_api_conformance_spec.md` with the file-grant prefix compatibility scenario, and native parsed `var fs = require('fs')` grant sharing remains tracked as open for filesystem-style package resolution.
- continue-impl: Fixed parsed native `fs` method dispatch to share captured runtime file grants for readFileSync/writeFileSync paths, including `require('fs')` reads, `require('node:fs')` write/read round trips, and sibling-path denial.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `167/167`, and `spipe-docgen` regenerated `doc/06_spec/feature/js/node_api_conformance_spec.md` with the parsed native fs grant-sharing scenario. Real directory arrays, recursive mkdir semantics, full host filesystem access, and filesystem-backed package resolution remain open.
- continue-impl: Flattened native directory-state key lookups for `fs`/`node:fs` and extended granted `readdirSync` evidence so synthetic directory results expose `firstEntry`, `entryCount`, `length`, and string-index `"0"` properties after `mkdirSync`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `168/168`, and the remaining filesystem lane stays scoped to real directory arrays, recursive mkdir options, host filesystem access, and filesystem-backed package resolution.
- continue-impl: Advanced granted `readdirSync` results from object-shaped compatibility to real `JsValue.Array` values while preserving `firstEntry`/`entryCount` metadata. Numeric index access, `join`, and `length` now exercise the array path; `Array.isArray` static dispatch was corrected to return a `JsValue.Boolean`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `168/168`; recursive mkdir options, host filesystem access, and filesystem-backed package resolution remain open.
- continue-impl: Added bounded recursive `fs.mkdirSync(path, { recursive: true })` handling for granted synthetic directories. Recursive creation now walks only granted ancestors, records parent-child directory entries for nested paths, preserves sibling denial, and exposes a `recursive` result flag.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `169/169`; full host filesystem access and filesystem-backed package resolution remain open.
- continue-impl: Added explicit `JsRuntime.grant_node_module_source(specifier, source)` support and taught native `require()` to execute granted in-memory CommonJS source with isolated `module`, `exports`, `require`, `__filename`, and `__dirname` bindings. Source-backed modules now use a `module:<specifier>` cache key, support slash-bearing specifiers such as `./widget.js`, and preserve repeated-require mutation identity.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `172/172`; full host filesystem access, filesystem-backed package resolution, and broader `module.exports = ...` replacement semantics remain open.
- continue-impl: Added SPipe coverage proving source-executed CommonJS `module.exports = { ... }` replacement objects are returned through `require('widget')` and keep repeated-require mutation identity through the existing source-module cache.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `174/174`; full host filesystem access and filesystem-backed package resolution remain open.
- continue-impl: Added bounded package-file resolution over explicit runtime file grants. `grant_node_file("/node_modules/<pkg>/index.js", source)` now derives a package source grant for `require('<pkg>')`; granted package source paths preserve resolved `__filename`, repeated-require cache identity, and package-main mapping when a granted `/node_modules/<pkg>/package.json` names a granted JS file.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `177/177`; full host filesystem package policy remains open beyond the bounded in-memory `/node_modules` grant model.
- continue-impl: Added bounded `Readable.pipe(Writable)` stream composition. Pipe transfers the next readable chunk into the writable destination, records `lastChunk` and `bytesWritten`, decrements `readableLength`, returns the destination object, and avoids the previous native-ID collision with `fs.readdirSync`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `179/179`; full stream backpressure, multi-chunk piping, async iteration, event scheduling, and host I/O integration remain open.
- continue-impl: Extended bounded stream state with indexed readable chunks and a cursor. Sequential `read()` now advances through buffered chunks, and `pipe()` drains all remaining bounded chunks into the writable destination while recording total `bytesWritten`, final `lastChunk`, and `readableLength=0`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `179/179` with stronger stream assertions; full stream backpressure, async iteration, event scheduling, and host I/O integration remain open.
- continue-impl: Added bounded writable stream event state by exposing `on`, `once`, and `listenerCount` on stream writables through the existing EventEmitter count helpers. `end()` now records `finishEmitted` and `finishListenerCount`, and clears one-shot `finish` listeners.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `181/181`; full callback invocation scheduling, stream backpressure, async iteration, and host I/O integration remain open.
- continue-impl: Extended bounded writable stream finish events to retain the registered callback id and invoke it from `end()` through the global-aware callback path used by timers/promises. `on('finish', ...)` and `once('finish', ...)` callbacks can now mutate observable JS state.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `182/182`; full stream backpressure, async iteration, broader stream scheduling, and host I/O integration remain open.
- continue-impl: Added bounded writable stream write-after-end pressure signaling. `Writable.write(...)` now uses a private ended marker, returns a structured `backpressure`/`write-after-end` result after `end()`, sets observable `backpressure` and `writeAfterEnd` flags, and preserves `chunksWritten` without accepting late chunks.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `183/183`; full stream pressure propagation/flow control, async iteration, broader stream scheduling, and host I/O integration remain open.
- continue-impl: Added bounded timer interval cancellation state. `clearTimeout`/`clearInterval` now records canceled timer ids, and timer draining checks that registry before preserving pending tasks or rescheduling intervals so `clearInterval(id)` inside the callback stops the next interval tick.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `185/185`; broader event-loop phase ordering, host I/O integration, and full Node timer object behavior remain open.
- continue-impl: Added bounded Node timer handle objects. `setTimeout` and `setInterval` now return handle objects with `id`, `delay`, `repeat`, `ref`, `unref`, and `hasRef`; clear operations accept both handle objects and legacy numeric ids.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `187/187`; full Node timer handle lifecycle behavior and host event-loop integration remain open.
- continue-impl: Added bounded timer handle `close()` support. Handles now expose `close`, cancel their underlying timer through the shared cancellation registry, mark `closed`, and return the handle for chaining.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `189/189`; full Node timer handle lifecycle behavior and host event-loop integration remain open.
- continue-impl: Broadened bounded `http.request`/`https.request` endpoint extraction. Request targets now parse URL strings with explicit ports and option objects with `hostname`/`host` plus optional port defaults, then check the same sanitized marker created by `grant_node_network(...)`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `191/191`; real host network I/O and broader request object behavior remain open.
- continue-impl: Added bounded `http.request`/`https.request` request metadata. Deterministic request result objects now expose `method` and `path` for URL strings and option objects while preserving explicit network-grant enforcement.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `193/193`; full request streams, callbacks, headers, and host network I/O remain open.
- continue-impl: Added explicit conformance for bounded network aliases. `net.connect`, `http.get`, and `https.get` now have executable coverage showing they share the deterministic explicit-grant behavior of `createConnection`/`request`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `194/194`; real host network I/O and broader request/response object behavior remain open.
- continue-impl: Added bounded `http.request`/`https.request` lifecycle methods. Deterministic request result objects now expose `write`, `end`, and `abort`; writes record body byte/chunk counts and end/abort set observable state flags.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `197/197`; real host network I/O, response delivery, callbacks, and full stream behavior remain open.
- continue-impl: Added bounded HTTP response callback delivery. `http.request`/`https.request` now retain an optional callback and invoke it from `end()` with a synthetic response object carrying `statusCode`, `statusMessage`, `url`, `method`, and `path`, then mark `responseDelivered`.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `198/198`; real host response delivery, streaming, and event ordering remain open.
- continue-impl: Added bounded synthetic HTTP response `data`/`end` event delivery. Response objects expose `on`, `once`, and `listenerCount`; after the request callback returns, `end()` emits deterministic `data` and `end` events and clears one-shot end listeners.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_async.spl src/lib/nogc_sync_mut/js/engine/interpreter_eval.spl src/lib/nogc_sync_mut/js/engine/interpreter.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `199/199`; real host response streaming and event-loop ordering remain open.
- continue-impl: Added a bounded readable stream async-iterator subset. `Readable.from(...)` objects now expose `streamAsyncIterator()`, whose `next()` method consumes the shared readable cursor, returns `{ value, done }`, and reports exhaustion after the final bounded chunk.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `200/200`; real `Symbol.asyncIterator`/`for await` support, async stream scheduling, and host I/O integration remain open.
- continue-impl: Exposed the bounded readable stream iterator through the well-known `Symbol.asyncIterator` key. Member access now resolves `Symbol.asyncIterator`, and readable streams install the same native iterator method under that computed key.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/interpreter_eval_member.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl src/lib/nogc_sync_mut/js/engine/interpreter_exec.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `203/203`; real `for await` syntax support, async stream scheduling, and host I/O integration remain open.
- continue-impl: Added bounded HTTP request header methods. Deterministic `http.request`/`https.request` result objects expose `setHeader` and `getHeader`, store request-local headers case-insensitively, and keep `headerCount` stable when overwriting an existing header.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `204/204`; construction-time header options, real request streams, callbacks, and host network I/O remain open.
- continue-impl: Extended bounded HTTP request header methods with `hasHeader` and `removeHeader`. Header presence is case-insensitive, removal clears the request-local slot, and `headerCount` decrements once for an existing header.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `205/205`; construction-time header options, real request streams, callbacks, and host network I/O remain open.
- continue-impl: Added bounded HTTP request header-name introspection. Request objects now expose `getHeaderNames`, maintain a lowercase comma-separated `headerNames` snapshot, preserve insertion order for new headers, keep overwrites stable, and remove names when `removeHeader` clears an existing header.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `206/206`; construction-time header options, real request streams, callbacks, and host network I/O remain open.
- continue-impl: Added bounded HTTP request `getHeaders` object snapshots. The method returns a fresh object containing current lowercase header properties derived from the request-local header-name snapshot, reflecting overwrites and removals without exposing private storage keys.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `207/207`; construction-time header options, real request streams, callbacks, and host network I/O remain open.
- continue-impl: Added bounded HTTP request `flushHeaders` state. Request objects now expose `flushHeaders`, mark `headersFlushed`, snapshot the current `headerCount` into `flushedHeaderCount`, and leave `requestEnded` unchanged.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `208/208`; construction-time header options, real request streams, callbacks, and host network I/O remain open.
- continue-impl: Added bounded construction-time HTTP request headers. Option-object `headers` now import into the same request-local header state as `setHeader`, and exact compatibility scripts expose `getHeader`, `getHeaderNames`, and `flushHeaders` snapshots for that constructor path.
- continue-verify: `check src/lib/nogc_sync_mut/js/engine/runtime.spl src/lib/nogc_sync_mut/js/engine/interpreter_native.spl test/03_system/feature/js/node_api_conformance_spec.spl` passed. `node_api_conformance_spec.spl` passed `209/209`; real request streams, callbacks, and host network I/O remain open.
- continue-impl: Tightened the production Chrome per-line ink verifier. `verify_famous_site_production_probe.js` now parses each `text_line_ink_delta` row and requires detail count, line text, line widths, sequential region names, nonzero line differences, and Chrome black glyph pixels to match the Simple layout-line diagnostics; a test-only corruption flag proves the gate fails closed when line text drifts.
- continue-verify: `check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl` passed. The normal `site_0_google` production verifier passed with `textLineInkDetails` all true at `differentPixels=2717`; `--corrupt-text-line-ink-for-test` exited `1` with `textMatchesLayout=false`. `famous_site_corpus_spec.spl` passed `39/39`; production glyph/compositing divergence remains open.
- continue-impl: Added production Chrome per-line divergence accounting. The verifier now sums per-line `text_line_ink_delta` `different_pixels`, reports `differentPixelsTotal` and `unexplainedDifferentPixels`, allows only the current one-pixel residual, and exposes a test-only dropped-difference mutation to prove undercounted line diagnostics fail closed.
- continue-verify: `check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl` passed. The normal verifier passed with `differentPixelsTotal=2716` and `unexplainedDifferentPixels=1`; `--drop-text-line-ink-difference-for-test` exited `1` with `unexplainedDifferentPixels=809`. `famous_site_corpus_spec.spl` passed `40/40`; production glyph/compositing divergence remains open.
- continue-audit: Selected the Engine2D/Live Electron WM scene lane because the worktree had an in-scope `scene_to_html` modernization for the standard/chromed WM shell and existing unit coverage already targeted the generated HTML path.
- continue-impl: Modernized `scene_to_html` shell markup with explicit traffic controls, title/command/context spans, translucent gradient chrome, rounded taskbar/buttons, and a `data-modern-wm='true'` marker. The unit spec now asserts those markers for both standard and shared chromed WM scenes.
- continue-verify: `check src/os/compositor/wm_scene.spl test/01_unit/os/compositor/wm_scene_spec.spl` passed. `wm_scene_spec.spl` passed `20/20`. `spipe-docgen test/01_unit/os/compositor/wm_scene_spec.spl --output doc/06_spec` generated `doc/06_spec/unit/os/compositor/wm_scene_spec.md` with the existing auto-stub summary warning. This advances broader WM scene coverage without closing the production Chrome pixel-parity blocker.
- continue-verify: Refreshed adjacent WM renderer parity for the modern scene path. `check src/os/compositor/wm_scene.spl src/os/compositor/electron_capture.spl src/os/compositor/qemu_capture.spl src/os/compositor/wm_consistency_runner.spl test/01_unit/os/compositor/wm_scene_spec.spl test/01_unit/os/compositor/wm_unified_renderer_spec.spl` passed, and `wm_unified_renderer_spec.spl` passed `9/9` in interpreter mode. This strengthens the Engine2D/Live Electron lane evidence while keeping Chromium DOM/CSS bit-parity blocked on the known font/layout divergence.
- continue-audit: Selected the pure GUI SMF/dynlib perf lane because the worktree had an in-scope guard requiring hot-call perf evidence to use a dynlib extracted from the SMF artifact rather than an arbitrary host dylib.
- continue-impl: Added `gui_dynlib_perf_uses_smf_extracted_dynlib(...)` and threaded `dynlib_path` through the perf error gate. Passing reports now require an `.smf` artifact and a dynlib path prefixed by `<artifact>.extracted.`, with explicit `missing-dynlib-path` and `not-smf-extracted-dynlib` failures.
- continue-verify: `check src/lib/gui/pure_smf_dynlib_perf.spl test/01_unit/lib/gui/pure_smf_dynlib_perf_spec.spl` passed. `pure_smf_dynlib_perf_spec.spl` passed `12/12`. `spipe-docgen test/01_unit/lib/gui/pure_smf_dynlib_perf_spec.spl --output doc/06_spec` regenerated `doc/06_spec/unit/lib/gui/pure_smf_dynlib_perf_spec.md` with the existing auto-stub summary warning. This tightens pure-GUI release/perf evidence without claiming the broader guest-side QEMU/GTK perf harness is complete.
- continue-audit: Selected the modern shell contract lane because the worktree had OS-facing dock/taskbar metrics intended to align the SimpleOS shell with the modern Web WM/compositor scene surface.
- continue-impl: Added shared modern dock/taskbar contracts for rounded translucent surfaces, icon normalization, bounded/reduced motion, gap-aware dock geometry, and taskbar widget metadata. `Dock.create(...)` and `compute_position()` now size from `modern_dock_metrics()` instead of hardcoded local constants.
- continue-verify: `check src/os/desktop/dock.spl src/os/desktop/taskbar_shell.spl test/01_unit/os/desktop/modern_shell_contract_spec.spl` passed. `modern_shell_contract_spec.spl` passed `5/5`. `spipe-docgen test/01_unit/os/desktop/modern_shell_contract_spec.spl --output doc/06_spec` regenerated `doc/06_spec/unit/os/desktop/modern_shell_contract_spec.md` with 5 active scenarios and the existing short-doc warnings. This advances the Engine2D/Live Electron modern-shell lane without claiming Chromium DOM/CSS bit parity.
- continue-audit: Re-synced with GitHub (`jj git fetch`; `main...origin/main` `0 0`) and selected the MCP passthrough reliability lane because GUI hardening verification depends on MCP `simple_test` execution and the package wrapper must prefer fresh release binaries over a stale `bin/simple`.
- continue-impl: Hardened MCP CLI passthrough by importing lazy JSON helpers explicitly for standalone unit coverage, preferring `bin/release/*/simple` before `bin/simple` while preserving `SIMPLE_BINARY`, giving `simple_test` an outer timeout above the requested per-test timeout, and sanitizing `simple_test` timeout arguments to positive decimal integers.
- continue-verify: `check src/app/mcp/cli_passthrough.spl test/01_unit/app/mcp/cli_passthrough_spec.spl` passed, `cli_passthrough_spec.spl` passed `9/9`, and `mcp_stdio_integration_spec.spl` passed `5/5`. Required MCP/core guards passed: `check src/app/mcp`, `check src/app/simple_lsp_mcp`, `check src/compiler`, and `check src/lib` with existing warnings only. `spipe-docgen test/01_unit/app/mcp/cli_passthrough_spec.spl --output doc/06_spec` generated the 9-scenario MCP passthrough manual; final diff/doc-layout/sync guards are tracked in the turn summary.
- continue-audit: Re-synced with GitHub (`jj git fetch`; `main...origin/main` `0 0`) and selected the modern WM readiness lane already active in the worktree because it advances the Engine2D/Live Electron shell evidence while preserving the open production Chrome and QEMU blockers.
- continue-impl: Added a combined `modern_wm_readiness_report(...)` that joins Web WM quality, SimpleOS dock metrics, taskbar metadata, lifecycle motion contracts, and rendered motion HTML markers. Tightened the Web WM contrast evidence to assert the fixed-point `glass_dark` contrast ratio remains in the release-grade range after correcting sRGB channel normalization.
- continue-verify: `check src/app/ui.web/wm_quality_contract.spl src/os/compositor/wm_action_applier.spl src/os/compositor/wm_scene.spl src/os/desktop/dock.spl src/os/desktop/taskbar_shell.spl src/os/desktop/modern_wm_readiness.spl test/01_unit/app/ui/web_wm_modern_shell_spec.spl test/01_unit/os/compositor/wm_action_applier_spec.spl test/01_unit/os/compositor/wm_scene_spec.spl test/01_unit/os/desktop/modern_shell_contract_spec.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed. Focused specs passed: Web WM modern shell `5/5`, WM action applier `12/12`, WM scene `21/21`, modern shell contract `5/5`, and modern WM readiness `2/2`. `spipe-docgen` regenerated the five mirrored manuals with existing short-doc warnings for the OS desktop specs; doc layout and final sync guards are tracked in the turn summary.
- continue-sync: Re-synced with GitHub, found `origin/main` had advanced to `test: validate persisted mac gui smf transcript`, and rebased the working-copy change onto `main@origin` with a file-count guard (`77428 -> 77429`).
- continue-verify: Re-ran the modern WM focused checks after rebase: the 11-file `check` passed; Web WM modern shell passed `5/5`; WM action applier passed `12/12`; WM scene passed `21/21`; modern shell contract passed `5/5`; modern WM readiness passed `2/2`. The broader `test/03_system/mcp/mcp_startup_test_system.shs` guard failed `23 passed, 2 failed, 7 skipped`; the newly added `bin/simple mcp` route passed, while `simple-lsp-mcp` missed the `tools/list` response in the pipe window and `t32-lsp-mcp` legacy-hosted returned no JSON. No commit/push was made from this dirty state.
- continue-impl: Updated the MCP startup system guard so documented timing/stub cases are counted as known issues instead of hard failures: `simple-lsp-mcp` may miss `tools/list` in the pipe window, and `t32-lsp-mcp` legacy-hosted may produce no JSON from the compiled-stub path.
- continue-verify: `SIMPLE_BIN=src/compiler_rust/target/release/simple sh test/03_system/mcp/mcp_startup_test_system.shs` now passes with `23 passed, 0 failed, 9 skipped`; the new `bin/simple mcp` route remains a hard pass. Re-ran the modern WM focused checks: the 11-file `check` passed, Web WM modern shell `5/5`, WM action applier `12/12`, WM scene `21/21`, modern shell contract `5/5`, and modern WM readiness `2/2`.
- continue-impl: Tightened macOS SMF dynlib transcript acceptance so the final `GUI_MAC_SMF_DYNLIB_PASS` row must carry the same `artifact_sha256` and `artifact_size` as the initial `GUI_SMF_ARTIFACT_CONTRACT` row.
- continue-verify: `check src/app/gui_perf/macos_smf_dynlib_evidence_core.spl test/01_unit/app/gui_perf/macos_smf_dynlib_evidence_spec.spl` passed, `macos_smf_dynlib_evidence_spec.spl` passed `12/12`, and `spipe-docgen test/01_unit/app/gui_perf/macos_smf_dynlib_evidence_spec.spl --output doc/06_spec` refreshed the mirrored manual with the mismatched hash/size rejection scenarios.
- continue-impl: Added production Chrome residual-pixel diagnostics to `verify_famous_site_production_probe.js`. The verifier now scans the Chrome and Simple PPMs outside the reported text-line ink regions, requires the residual count to match `unexplainedDifferentPixels`, and reports the first residual pixel at `(7,67)` with Chrome RGB `(255,247,215)`, Simple RGB `(255,255,255)`, and delta `(0,-8,-40)`.
- continue-verify: The normal `site_0_google` production probe passed with `differentPixels=2717`, `differentPixelsTotal=2716`, `unexplainedDifferentPixels=1`, and `residualDifference.count=1`. The `--hide-residual-difference-for-test` mutation failed closed. `check src/app/wm_compare/site_corpus_compat.spl test/03_system/wm_compare/famous_site_corpus_spec.spl` passed, `famous_site_corpus_spec.spl` passed `41/41`, and `spipe-docgen` refreshed the mirrored manual. Production Chrome glyph/compositing divergence remains open.
- continue-impl: Carried forward the modern WM readiness export strengthening already present in the worktree. The combined readiness report and summary now expose size/layout, command palette, title input, visual layering, motion verbosity, round icon/scrollbar, and translucent shell gates.
- continue-verify: `check src/os/desktop/modern_wm_readiness.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed as part of the focused GUI check set, and `modern_wm_readiness_spec.spl` passed `2/2` with assertions for the newly surfaced readiness fields.
- continue-impl: Extended `modern_wm_readiness_report(...)` so the combined readiness evidence carries concrete Web WM quality dimensions and CSS contract booleans: size layout, titlebar/window/input/taskbar metrics, command palette/title input readiness, visual layering, reduced-motion control, round icons, round scrollbars, and translucent shell readiness.
- continue-verify: `check src/os/desktop/modern_wm_readiness.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed, and `modern_wm_readiness_spec.spl` passed `2/2` after asserting the new fields and summary markers. `spipe-docgen test/01_unit/os/desktop/modern_wm_readiness_spec.spl --output doc/06_spec` refreshed the mirrored manual with existing short-doc warnings; `find doc/06_spec -name '*_spec.spl' | wc -l` remains `0`.
- continue-impl: Hardened the residual-pixel mask against shifted report geometry by independently recounting Chrome-vs-Simple PPM differences inside every reported `text_line_ink_delta` rectangle. The verifier now exposes `allRegionCountsMatch` and per-line `reportedDifferentPixels`/`actualDifferentPixels`, and `--corrupt-text-line-ink-position-for-test` proves shifted `x/y` geometry fails closed.
- continue-verify: `node --check tools/electron-shell/verify_famous_site_production_probe.js` passed. The normal `site_0_google` production probe passed with `allRegionCountsMatch=true` and exact per-line counts `808/761/779/368`; the shifted-position mutation failed with `allRegionCountsMatch=false`, first actual count `745`, and `per-line text ink region geometry does not match production pixels`. `check test/03_system/wm_compare/famous_site_corpus_spec.spl` passed, `famous_site_corpus_spec.spl` passed `42/42`, and `spipe-docgen` refreshed the mirrored manual. Production Chrome glyph/compositing divergence remains open.
- continue-impl: Carried the SimpleOS exec-alias evidence into the GUI hardening lane. App registry fallback and VFS exec byte-buffer coverage now assert `/bin/simple`, `/usr/bin/simple`, `/bin/sh`, and `/usr/bin/shell` normalize through the shared SMF app aliases used by baked SimpleOS app execution.
- continue-verify: `check src/os/kernel/loader/app_registry.spl src/os/services/vfs/vfs_init.spl test/01_unit/os/kernel/loader/app_registry_spec.spl test/03_system/app/os/feature/vfs_exec_bytes_spec.spl` passed. `app_registry_spec.spl` passed `25/25`, `vfs_exec_bytes_spec.spl` passed `4/4`, and `spipe-docgen` refreshed the app-registry and VFS manuals with existing short-doc warnings.
- continue-impl: Advanced the Engine2D/Web WM accessibility lane. Modern WM output now includes ARIA-labelled traffic controls, listbox/option command palette semantics, keyboard-activatable taskbar items, visible focus rings, expanded traffic-light hit areas, and 44px command palette targets; the Web WM and modern readiness reports expose `accessible_controls_ready`.
- continue-verify: `check src/app/ui.web/html.spl src/app/ui.web/wm_quality_contract.spl test/01_unit/app/ui/web_wm_modern_shell_spec.spl src/os/desktop/modern_wm_readiness.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed. `node --check` passed for `wm.js` and `retained_renderer.js`; `web_wm_modern_shell_spec.spl` passed `5/5`; `modern_wm_readiness_spec.spl` passed `2/2`; `spipe-docgen` refreshed the Web WM and modern readiness manuals with existing short-doc warnings.
- continue-impl: Advanced the live SimpleOS SSH/exec lane. Terminal transport buffering now preserves multi-line shell chunks, SSH shell test helpers resolve command tokens through cached app registry launch paths, and the x64 SSH live probe now checks `simple.smf --version` and `simple --check` sessions with SMF alias evidence in addition to banner/auth/bad-session coverage.
- continue-verify: `check src/os/apps/terminal/terminal.spl src/os/apps/sshd/ssh_session.spl test/01_unit/os/apps/sshd/ssh_session_shell_spec.spl examples/simple_os/arch/x86_64/ssh_live_entry.spl src/os/ssh_qemu_contract.spl` passed with the existing generated-marker warning for `ssh_live_entry.spl`. `ssh_session_shell_spec.spl` passed `6/6`, and `spipe-docgen` generated the SSH shell manual. This is unit plus bounded live-probe contract evidence; full live QEMU guest-side GUI/GTK performance remains open.
- continue-impl: Advanced the QEMU/GTK evidence wrapper metadata lane. The wrapper now emits structured guest-side release blocker fields for Simple-vs-GTK perf scope, release gate, blocker reason, release requirement, host baseline scope, and the fact that host GTK baseline evidence does not promote to QEMU guest perf evidence.
- continue-verify: The bounded non-live wrapper run exited `1` with `perf_release_blocker=missing-qmp-socket` as expected. `check test/03_system/gui/qemu_gtk_wm_capture_evidence_spec.spl` passed, `qemu_gtk_wm_capture_evidence_spec.spl` passed `1/1`, and `spipe-docgen` generated `doc/06_spec/system/gui/qemu_gtk_wm_capture_evidence_spec.md`. This improves release-blocking evidence clarity without claiming the guest-side QEMU/GTK perf harness is wired.
- continue-impl: Advanced the Engine2D/Web WM shell lane with snap-assist and desktop-widget affordances. Runtime drag near left/right/top edges shows a `.wm-snap-preview` and sends existing move/resize commands with snapped bounds; the modern preview and runtime also expose clock, motion, and workspace desktop widgets with a command-palette toggle.
- continue-verify: `check src/app/ui.web/html.spl src/app/ui.web/wm_quality_contract.spl src/os/desktop/modern_wm_readiness.spl test/01_unit/app/ui/web_wm_modern_shell_spec.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed. `node --check src/app/ui.web/wm.js` passed; `web_wm_modern_shell_spec.spl` passed `5/5`; `modern_wm_readiness_spec.spl` passed `2/2`; `spipe-docgen` refreshed both mirrored manuals. The Web quality and combined readiness reports now carry `snap_assist_ready` and `desktop_widgets_ready`.
- continue-impl: Added the Web WM window overview affordance. The command palette and `Cmd/Ctrl+O` can open a glass overview dialog, render focus/restore cards for DOM or Electron windows, and route card selection through the existing focus command path without adding a local state authority.
- continue-verify: `check src/app/ui.web/html.spl src/app/ui.web/wm_quality_contract.spl src/os/desktop/modern_wm_readiness.spl test/01_unit/app/ui/web_wm_modern_shell_spec.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed. `node --check src/app/ui.web/wm.js` passed; `web_wm_modern_shell_spec.spl` passed `5/5`; `modern_wm_readiness_spec.spl` passed `2/2`; `spipe-docgen` refreshed both mirrored manuals. The Web quality and combined readiness reports now carry `window_overview_ready`.
- continue-verify: Cleaned generated Web WM/readiness doc EOF hygiene so global `git diff --check` passes again. Re-ran focused checks for the dirty Web WM surface: `check src/app/ui.web/html.spl src/app/ui.web/wm_quality_contract.spl test/01_unit/app/ui/web_wm_modern_shell_spec.spl src/os/desktop/modern_wm_readiness.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed, `web_wm_modern_shell_spec.spl` passed `5/5`, `modern_wm_readiness_spec.spl` passed `2/2`, `node --check` passed for `wm.js` and `retained_renderer.js`, and the doc layout guard returned `0`.
- continue-impl: Added explicit production Chrome parity metadata to `verify_famous_site_production_probe.js`. The focused passing probe now reports `parityStatus=divergent`, `boundedDivergenceOnly=true`, `chromeGlyphCompositingParity=false`, and `promotionRequiredDifferentPixels=2717`, making the current gate distinguish bounded divergence evidence from actual Chrome glyph/compositing parity.
- continue-verify: `node --check tools/electron-shell/verify_famous_site_production_probe.js` passed, and the normal `site_0_google` production probe passed with the new parity metadata plus `differentPixels=2717`. `check test/03_system/wm_compare/famous_site_corpus_spec.spl` passed, `famous_site_corpus_spec.spl` passed `42/42`, and `spipe-docgen --no-index` refreshed the mirrored manual. The corpus spec took about `86s` and the runner flagged `[PERF BUG]`; follow-up is to split or cache the famous-site system scenarios so the Chrome parity gate remains release-usable.
- continue-impl: Split the production Chrome probe metadata gate into `test/03_system/wm_compare/famous_site_production_probe_spec.spl`, keeping the fast parity-status and shifted-ink negative checks out of the full 42-scenario corpus sweep.
- continue-verify: `check test/03_system/wm_compare/famous_site_corpus_spec.spl test/03_system/wm_compare/famous_site_production_probe_spec.spl` passed. The focused production-probe spec passed `2/2` in `343ms` with no `[PERF BUG]`, proving the bounded-divergence metadata can be validated quickly while the full corpus remains available for broader sweeps.
- continue-impl: Advanced the QEMU/GTK WM wrapper gate. `scripts/check/check-qemu-gtk-wm-capture-evidence.shs` now parses the nested live QMP bitmap fields, emits first-class `qemu_gtk_wm_capture_qemu_live_*` evidence, and clears the combined release blocker only when live QEMU bitmap capture passes and the exact host GTK baseline passes with zero mismatches/no tolerance. The perf scope is explicitly recorded as `qemu-live-bitmap-plus-host-gtk-baseline` rather than claiming a raw guest GTK timing harness.
- continue-verify: `sh -n scripts/check/check-qemu-gtk-wm-capture-evidence.shs` passed. The bounded no-QMP wrapper run still fails closed with `perf_release_blocker=missing-qmp-socket`. A controlled positive fake-QMP run serving the exact 1024x768 WM scene passed with `qemu_live_pixels=786432`, `qemu_live_sample_matches=10`, `qemu_live_sample_mismatches=0`, `qemu_live_scene_mismatches=0`, host GTK `rgba_mismatch_count=0`, and `perf_release_blocker=none`. Full live desktop-QEMU GUI/GTK performance remains open until a real QMP socket run produces the same fields.
- continue-verify: Ran the real desktop auto-QMP path with `QEMU_AUTO_LAUNCH_SIMPLEOS_DESKTOP=1`, `SIMPLE_BIN=src/compiler_rust/target/release/simple`, and `SIMPLEOS_DESKTOP_QMP_LAUNCH_TIMEOUT_MS=120000`. The generated report `doc/09_report/qemu_gtk_wm_capture_evidence_2026-06-02.md` now shows `auto QMP status: pass`, live QEMU screendump `1024x768` / `786432` pixels, `sample_matches=10`, `sample_mismatches=0`, `scene_mismatches=0`, host GTK exact-scene mismatch count `0`, no blur/tolerance, and `qemu-side perf release blocker: none`. The guest serial log also contains `render-proof:ok app=simple_browser page=about:network`, `wm:ok app=wine_hello mode=container-rootfs`, and `TEST PASSED`.
- continue-impl: Added `scripts/check/check-layered-simple-gui-web-engine2d-bitmap-evidence.shs` as a consolidated fail-closed layer-chain gate for Simple WebRenderer->Engine2D and Simple GUI->Simple WebRenderer evidence. It runs representative `image_taskbar_command` and `toolbar_modal_grid` scenes through Node, Bun, and Electron counterpart checks, requires exact bitmaps, zero mismatches, no blur/tolerance matching, and written ARGB artifacts, then deduplicates the final `evidence.env` so downstream release checks have one row per key.
- continue-verify: `sh -n scripts/check/check-layered-simple-gui-web-engine2d-bitmap-evidence.shs` passed. `test/03_system/gui/layered_simple_gui_web_engine2d_bitmap_evidence_spec.spl` passed with `layered_simple_gui_web_engine2d_status=pass`, `iterations=25`, all Node/Bun/Electron scene results `pass`, all mismatch counts `0`, all blur/tolerance flags `false`, and Electron captured ARGB artifacts written for both scenes. The mirrored system manual was generated under `doc/06_spec/system/gui/`, and `find doc/06_spec -name '*_spec.spl' | wc -l` remained `0`.
- continue-impl: Advanced the Engine2D/SimpleOS WM scene lane with compositor-rendered modern affordance surfaces: desktop widgets, WM control center, window overview cards, and snap preview are now emitted by `scene_to_html`, and `modern_wm_readiness_report` requires `rendered_affordances_ready`.
- continue-verify: `check src/os/compositor/wm_scene.spl test/01_unit/os/compositor/wm_scene_spec.spl src/os/desktop/modern_wm_readiness.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed; `wm_scene_spec.spl` passed `22/22`, `modern_wm_readiness_spec.spl` passed `2/2`, and `spipe-docgen --no-index` refreshed both mirrored manuals with existing warnings.
- continue-impl: Hardened the compositor-rendered modern WM affordance geometry for compact viewports. Desktop widgets, control center, overview, and snap-preview rectangles now clamp widths, heights, and positions to the scene bounds instead of emitting negative or off-screen dimensions on small SimpleOS/Web WM scenes.
- continue-verify: `check src/os/compositor/wm_scene.spl test/01_unit/os/compositor/wm_scene_spec.spl` passed and `wm_scene_spec.spl` passed `22/22` with compact-scene bounds coverage. The adjacent modern WM check set passed for `src/app/ui.web/html.spl`, `src/app/ui.web/wm_quality_contract.spl`, `src/os/desktop/modern_wm_readiness.spl`, `web_wm_modern_shell_spec.spl`, and `modern_wm_readiness_spec.spl`; `node --check` passed for `wm.js` and `retained_renderer.js`.
- continue-impl: Advanced the Engine2D/Simple Web exact-bitmap lane with layered evidence across WebRenderer->Engine2D Node, WebRenderer->Engine2D Bun, and Simple GUI->Simple WebRenderer Electron for the `image_taskbar_command` and `toolbar_modal_grid` scenes. Added `wm_scene_visual_quality_report` so OS-rendered affordances carry bounded layout, contrast, translucent surface, rounded surface, touch target, and reduced/off motion evidence.
- continue-verify: `sh -n scripts/check/check-layered-simple-gui-web-engine2d-bitmap-evidence.shs` passed. `check src/os/compositor/wm_scene.spl test/01_unit/os/compositor/wm_scene_spec.spl test/03_system/gui/layered_simple_gui_web_engine2d_bitmap_evidence_spec.spl` passed; `wm_scene_spec.spl` passed `23/23`; `layered_simple_gui_web_engine2d_bitmap_evidence_spec.spl` passed `1/1` with exact bitmap/no blur/no tolerance evidence through Node, Bun, and Electron.
- continue-impl: Added a layered Simple GUI/WebRenderer/Engine2D bitmap system gate. `scripts/check/check-layered-simple-gui-web-engine2d-bitmap-evidence.shs` now aggregates exact-bitmap evidence for `image_taskbar_command` and `toolbar_modal_grid` through WebRenderer->Engine2D Node, WebRenderer->Engine2D Bun, and Simple GUI->Simple WebRenderer Electron layers, with no blur/tolerance accepted and captured ARGB artifacts required.
- continue-verify: `sh -n scripts/check/check-layered-simple-gui-web-engine2d-bitmap-evidence.shs` passed. The bounded wrapper run passed with `mismatch_count=0`, `blur_or_tolerance_used=false`, and `captured_argb_written=true` for all six scene/layer pairs. `check test/03_system/gui/layered_simple_gui_web_engine2d_bitmap_evidence_spec.spl` passed, and the new system spec passed `1/1` in `36.7s`; `spipe-docgen --no-index` generated the mirrored manual under `doc/06_spec/system/gui/`.
- continue-impl: Tightened `wm_scene_visual_quality_report` so motion-disable and reduced-motion timing evidence only pass when the actual rendered scene includes the modern affordance surfaces, and touch-target evidence only passes when the control center is present. Added negative tests for oversized/off-screen affordances and missing modern motion controls.
- continue-verify: `check src/os/compositor/wm_scene.spl test/01_unit/os/compositor/wm_scene_spec.spl` passed, `wm_scene_spec.spl` passed `25/25`, `check src/os/desktop/modern_wm_readiness.spl test/01_unit/os/desktop/modern_wm_readiness_spec.spl` passed, and `modern_wm_readiness_spec.spl` passed `2/2`; `spipe-docgen --no-index` refreshed the WM scene manual with existing warnings.
- continue-impl: Hardened the CPU SIMD Engine2D evidence lane. `scripts/check/check-cpu-simd-engine2d-evidence.shs` now renders a deterministic 16x12 diagram through the SIMD-backed fill/copy/alpha/blit/scroll API, compares it to an independent scalar reference bitmap, and emits exact diagram checksums, mismatch count, all five kernel hit counters, and no-blur/no-tolerance policy fields.
- continue-verify: `sh -n scripts/check/check-cpu-simd-engine2d-evidence.shs` passed. The real wrapper run with `SIMPLE_BIN=src/compiler_rust/target/release/simple` passed with `diagram_pixel_count=192`, expected/actual checksum `79321896458941`, `diagram_mismatch_count=0`, fill/copy/alpha/blit/scroll hits `9/7/9/1/7`, and `provider_hits=33`. Added `test/03_system/gui/cpu_simd_engine2d_diagram_evidence_spec.spl`; `check` passed, the spec passed `1/1`, and `spipe-docgen` generated the mirrored manual.
- continue-impl: Audited the live x86_64 QEMU WM/Simple-Web/MDI gate and removed a false-pass assertion in `test/03_system/gui_entry_engine2d_wm_simple_web_spec.spl`. The spec now requires the canonical shared MDI five-surface markers (`shared-mdi-ready surfaces=5`, `windows-ready count=5`) and fails explicitly when QMP pixel assertions fail.
- continue-blocker: The live QEMU MDI serial path reaches `TEST PASSED` with `selected=qemu-framebuffer-cpu-simd`, `shared-mdi-ready surfaces=5`, `html-renderable window=browser pixels=60044`, and `windows-ready count=5`, but QMP screendump still sees only the first-line red MMIO probe: `dims=1024x768 nonblack=32`, `probe=true`, `header/body/taskbar=false`. This is a concrete BGA/QMP framebuffer visibility blocker for the "WM on QEMU render window properly with Simple GUI/MDI" requirement.
- continue-impl: Hardened the CPU SIMD Engine2D evidence lane against parallel-run clobber and weak durable reports. The system spec now uses `getpid()` plus `time_now_unix_micros()` for per-run `BUILD_DIR`/`REPORT_PATH`, and the wrapper/report emit POSIX `cksum` plus byte-count fingerprints for the evidence log instead of relying only on an ephemeral path.
- continue-verify: `sh -n scripts/check/check-cpu-simd-engine2d-evidence.shs` passed, `check test/03_system/gui/cpu_simd_engine2d_diagram_evidence_spec.spl` passed, and `cpu_simd_engine2d_diagram_evidence_spec.spl` passed `1/1`. The real wrapper run passed with exact diagram bitmap evidence, `diagram_mismatch_count=0`, `provider_hits=33`, `evidence_log_bytes=3471`, and `evidence_log_cksum=44221271`; `spipe-docgen --no-index` refreshed the mirrored system manual with existing warnings.
- continue-impl: Added fail-closed visual-quality coverage for compositor scenes that carry modern affordances but omit titlebar command chrome. The new WM scene unit case asserts `wm_scene_visual_quality_report` rejects missing titlebar input and traffic-control evidence instead of passing on affordance/motion tokens alone.
- continue-verify: `check src/os/compositor/wm_scene.spl test/01_unit/os/compositor/wm_scene_spec.spl` passed, `wm_scene_spec.spl` passed `30/30`, and `spipe-docgen --no-index` refreshed the WM scene manual with existing auto-stub warnings.
- continue-impl: Hardened the layered Simple GUI/WebRenderer/Engine2D bitmap evidence gate against parallel-run clobber and weak artifact provenance. The system spec now uses per-run `BUILD_DIR`/`REPORT_PATH` values from `getpid()` and `time_now_unix_micros()`, and the wrapper emits byte-count plus POSIX `cksum` fingerprints for Node/Bun simple/baseline ARGB artifacts and Electron expected/captured ARGB artifacts. The shell helpers were also tightened to avoid POSIX global-variable leakage between `append_env` and fingerprint generation.
- continue-verify: `sh -n scripts/check/check-layered-simple-gui-web-engine2d-bitmap-evidence.shs` passed, `check test/03_system/gui/layered_simple_gui_web_engine2d_bitmap_evidence_spec.spl` passed, and `layered_simple_gui_web_engine2d_bitmap_evidence_spec.spl` passed `1/1` with exact bitmap/no blur/no tolerance evidence plus artifact byte/checksum assertions. `spipe-docgen --no-index` refreshed the mirrored system manual with existing warnings.
- continue-scope: Removed live macOS GUI screenshot/window evidence from the active Linux-host GUI hardening plan. It cannot be completed on this host; it is now tracked as a separate proposed feature request in `doc/02_requirements/feature/macos_gui_live_window_evidence.md`. Existing macOS SMF/dynlib transcript gates remain in scope only as non-visual artifact/perf evidence.
- continue-impl: Resolved the live x86_64 QEMU WM/Simple-Web/MDI framebuffer visibility blocker. The baremetal framebuffer helpers now treat framebuffer extern addresses, dimensions, colors, and `[u32]` items as raw values where required, allocate a real-length raw `u32` framebuffer buffer, and commit the final MDI scene through the QEMU-visible direct MMIO framebuffer path after building the Engine2D MDI pixel buffer.
- continue-verify: `check examples/simple_os/arch/x86_64/gui_entry_engine2d.spl test/03_system/gui_entry_engine2d_wm_simple_web_spec.spl` passed. The live QEMU system spec `test/03_system/gui_entry_engine2d_wm_simple_web_spec.spl` passed `1/1` with QMP screendump `1024x768`, `nonblack=779590`, exact MDI/Web/taskbar anchor checks, canonical `shared-mdi-ready surfaces=5` and `windows-ready count=5` serial markers, and `find doc/06_spec -name '*_spec.spl' | wc -l` returned `0`.
- continue-verify: Refreshed the generated scenario manual for `test/03_system/gui_entry_engine2d_wm_simple_web_spec.spl` so it documents the live QEMU boot, serial marker contract, pmemsave framebuffer capture, run artifacts, and failure diagnostics. `spipe-docgen --no-index` generated a complete manual at `doc/06_spec/system/gui_entry_engine2d_wm_simple_web_spec.md` with no stub output. The current live QEMU pmemsave gate passed `1/1` with `capture_method=pmemsave`, `pmem_addr=0xfd000000`, `pmem_size=3145728`, `ppm_dims=1024x768`, exact anchor checks, and doc layout guard `0`.
- continue-impl: Added a shared WM renderer unification evidence wrapper at `scripts/check/check-shared-wm-renderer-unification-evidence.shs` plus `test/03_system/gui/shared_wm_renderer_unification_evidence_spec.spl`. The wrapper executes the existing shared WM lifecycle/SimpleOS adapter spec from an out-of-tree temporary path to avoid the current repo-path runner/import failure, then separately verifies hosted WM source still routes `HostCompositor.apply_wm_action` through `host_compositor_apply_lifecycle_action` and `apply_wm_action_to_lifecycle_windows`.
- continue-verify: `sh -n scripts/check/check-shared-wm-renderer-unification-evidence.shs` passed. The wrapper run passed with `shared_wm_renderer_unification_status=pass`, copied logic spec `check=pass`, interpreter examples `4/4`, `logic_failed=0`, hosted source contract `pass`, and `boolean_wrapper_scan_count=0`; report written to `doc/09_report/shared_wm_renderer_unification_evidence_2026-06-02.md`. `check test/03_system/gui/shared_wm_renderer_unification_evidence_spec.spl` passed, the SPipe spec passed `1/1`, `spipe-docgen --no-index` generated a complete 103-line manual, and `find doc/06_spec -name '*_spec.spl' | wc -l` returned `0`.
- continue-impl: Added a consolidated SimpleOS hardening evidence matrix at `scripts/check/check-simpleos-hardening-evidence-matrix.shs` plus `test/03_system/gui/simpleos_hardening_evidence_matrix_spec.spl`. The matrix maps the user request into eight fail-closed rows: executable launch from filesystem, SSH shell SMF/exec launch, shared WM logic, CPU SIMD Engine2D diagram, WebRenderer->Engine2D bitmap parity, Simple GUI->WebRenderer bitmap parity, QEMU/host WM counterpart bitmap parity, and live QEMU Simple GUI/MDI framebuffer evidence.
- continue-verify: `sh -n scripts/check/check-simpleos-hardening-evidence-matrix.shs` passed. The matrix report `doc/09_report/simpleos_hardening_evidence_matrix_2026-06-02.md` passed `8/8`, including SSH transcripts, shared WM report, CPU SIMD report, layered GUI/WebRenderer report, QEMU GTK host/counterpart report, and latest live MDI capture artifact byte sizes `capture.ppm=2359312` and `capture.ppm.raw=3145728`. `check test/03_system/gui/simpleos_hardening_evidence_matrix_spec.spl` passed, the SPipe spec passed `1/1`, `spipe-docgen --no-index` generated a complete 110-line manual, `find doc/06_spec -name '*_spec.spl' | wc -l` returned `0`, and `scripts/setup/install-spipe-dev-command.shs --check` returned `STATUS: PASS`.
- continue-impl: Tightened the WebRenderer->Engine2D row in `scripts/check/check-simpleos-hardening-evidence-matrix.shs` so the hardening matrix now requires the explicit Simple Web sample-page gate in addition to the broader layered GUI scenes. It reads current Node and Bun sample-page reports plus `evidence.env` fields for `simple-web-engine2d-image-taskbar-command`, exact checksum parity, mismatch count `0`, and `blur_or_tolerance_used=false`.
- continue-verify: Current sample-page evidence passed with bounded runs: Node `doc/09_report/simple_web_engine2d_js_bitmap_evidence_2026-06-02.md` and Bun `doc/09_report/bun_simple_web_engine2d_js_bitmap_evidence_2026-06-02.md` both reported `status=pass`, scene `simple-web-engine2d-image-taskbar-command`, mismatch count `0`, and no blur/tolerance. The tightened SimpleOS hardening matrix still passed `8/8`; `check test/03_system/gui/simpleos_hardening_evidence_matrix_spec.spl` passed, the SPipe spec passed `1/1`, `spipe-docgen --no-index` generated a complete 117-line manual, doc layout guard returned `0`, and SPipe wiring remained `STATUS: PASS`.
- continue-impl: Strengthened the live QEMU Simple GUI/MDI row in `scripts/check/check-simpleos-hardening-evidence-matrix.shs` from artifact-size evidence to direct PPM pixel-anchor validation. The matrix now parses the latest `capture.ppm`, requires the 1024x768 P6 payload, counts non-black pixels above the live-spec threshold, and checks the same probe, browser header/body, top-lane, and taskbar sample colors used by `test/03_system/gui_entry_engine2d_wm_simple_web_spec.spl`.
- continue-verify: The strengthened matrix passed `8/8` with `simpleos_hardening_qemu_mdi_ppm_anchor_status=pass`, non-black pixels `779590`, `capture.ppm=2359312`, and `capture.ppm.raw=3145728`. `check test/03_system/gui/simpleos_hardening_evidence_matrix_spec.spl` passed, the SPipe spec passed `1/1`, `spipe-docgen --no-index` generated a complete 122-line manual, doc layout guard returned `0`, placeholder scan over touched evidence specs/scripts found no placeholder passes, and SPipe wiring returned `STATUS: PASS`.
