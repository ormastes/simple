# Feature: restart12-render-cli-plan-completion

## Raw Request
$sp_dev with pherallel agents with guide and higher model review complete the plan doc

## Task Type
todo

## Refined Goal
Complete the canonical restart12 render-performance and self-hosted CLI entry-closure plan so every remaining acceptance item has an evidence-backed status, executable implementation/resume instructions, guide and expert-wiki knowledge, parallel ownership, and independent highest-capability review.

## Acceptance Criteria
- AC-1: The canonical plan identifies the current source revision and reconciles every restart12 acceptance item as proved, contradicted, blocked, or missing without treating the prior WARN handoff as feature completion.
- AC-2: Every open item names its pure-Simple owner, exact implementation scope, prerequisite, deterministic verification command, retained evidence path, maximum three-cycle stop rule, and no-seed/no-stale-artifact fail-closed policy.
- AC-3: The self-hosted CLI dispatcher/native-build blocker is linked to its tracked bug and the plan distinguishes direct source execution, cached native entry-closure construction, Stage 4 candidate admission, deployment, and sparse DrawIR execution as separate gates.
- AC-4: The sparse 7680x4320 DrawIR row retains viewport, 256x128 dynamic damage, 20 frames, two considered/512 culled commands, nonzero readback, zero mismatches, checksum, backend/fallback, execution mode, p50/p95 <= 12.5 ms, max RSS, binary/source identity, and honest executor-only scope.
- AC-5: The plan includes a parallel task matrix with disjoint ownership, dependencies, merge owner `/root`, final highest-capability reviewer, and collision guidance; no sidecar edits the same canonical plan or shared production file concurrently.
- AC-6: Cooperative findings are reviewed by the merge owner and a separate highest-capability reviewer before done marks, broad exclusions, generated-manual quality claims, or blocker classifications are accepted; review findings and dispositions are recorded in the plan.
- AC-7: Knowledge is current: the relevant `doc/03_plan` and `doc/09_report` references are aligned; a reachable `doc/07_guide` page explains the production cached-entry workflow and current blocker without advertising unavailable commands; feature-expert and layer-expert `skill.md` entries exist or are updated; every unfixed gap has a `doc/08_tracking/bug` record with owner and unblock condition.
- AC-8: Workflow/tooling changes are N/A because this lane changes planning/knowledge only; `doc/06_spec`, `.codex/skills`, `.agents/skills`, `.claude/skills`, `.claude/agents/spipe`, `.claude/commands`, and `.gemini/commands` remain unchanged unless review proves their existing contract stale, in which case they become required updates.
- AC-9: No executable SSpec is created merely to test prose. If implementation/spec work is later resumed, shared interface is `CachedRenderEntryClosureV1`; manual flow steps are `Admit the exact Stage 4 CLI`, `Build the cached sparse DrawIR carrier`, `Execute the retained 8K damage corpus`, and `Validate identity, correctness, and budget receipts`; setup/checker helpers are `setup_cached_render_entry_closure` and `check_cached_render_entry_closure`; any scaffold placeholder must use `fail("cached render entry closure not implemented")`.
- AC-10: Focused documentation guards, rendering-source coupling, direct-env guard, numbered-artifact guard, link/path checks, and `git diff --check` pass once; the final work is committed and integrated under `/tmp/simple-main-restart12-push.lock` without force-push or branch creation, then proven reachable from refreshed `origin/main` with a clean tree.

## Scope Exclusions
- Implementing or redeploying the Stage 4 CLI, changing renderer/compiler production source, rerunning full-frame CPU 8K, or claiming presentation/physical-scanout performance.
- Auto-selecting new feature/NFR options; no requirement-option documents are created by this plan-completion lane.

## Cooperative Review
- Sidecar A: current-plan acceptance/status audit; read-only recommendations.
- Sidecar B: pure-Simple CLI/entry-closure ownership and verification-path audit; read-only recommendations.
- Sidecar C: guide, feature-expert, layer-expert, bug-record, and SPipe freshness audit; read-only recommendations.
- Merge owner: `/root`; only the merge owner edits the canonical plan and shared knowledge artifacts.
- Final reviewer: separate highest-capability Codex reviewer after integration.
- Shared interface: `CachedRenderEntryClosureV1`.
- Manual flow steps: `Admit the exact Stage 4 CLI`; `Build the cached sparse DrawIR carrier`; `Execute the retained 8K damage corpus`; `Validate identity, correctness, and budget receipts`.
- Setup/checker helpers: `setup_cached_render_entry_closure`; `check_cached_render_entry_closure`.
- Fail-fast placeholder: `fail("cached render entry closure not implemented")`.
- Generated-manual review owner: final highest-capability reviewer; currently N/A unless a later implementation creates SSpec/manual artifacts.

## Phase
dev-done

## Log
- dev: Created state file with 10 acceptance criteria (type: todo).
- dev: Three disjoint read-only sidecars audited plan truth, CLI ownership, and knowledge freshness; `/root` integrated dispositions.
- dev: Highest-capability `gpt-5.6-sol`/xhigh review returned initial FAIL; corrected draft submitted for read-only re-review.
- dev: Focused diff, working/staged direct-env, and numbered-artifact layout guards each passed once.
