# Feature: recent-plan-cleanup

## Raw Request
$sp_dev new cleanup plan for those plan files complete them and mark as done with agent teams with apark after normal llm review. pherallel works

## Task Type
code-quality

## Refined Goal
Create and execute an evidence-backed cleanup workflow for recent unfinished plan files, excluding GPU web/DB offload and platform-only gates, so completed plans are marked done and incomplete plans have owned parallel-agent closure tasks plus normal-LLM review.

## Acceptance Criteria
- AC-1: A cleanup plan exists under `doc/03_plan/agent_tasks/` and lists the recent unfinished plan files in scope, excluding GPU web/DB offload and Mac/Windows/QEMU/BSD/other external-platform-only blockers.
- AC-2: The cleanup plan assigns disjoint parallel Spark agent lanes and a normal-LLM review lane, with write scopes and conflict rules.
- AC-3: Each in-scope plan is classified as `mark-done`, `needs-evidence`, `needs-requirement-selection`, `needs-implementation`, or `superseded/merge`.
- AC-4: A plan may be marked done only when the cleanup plan names concrete evidence proving its requirements, tests, docs, and follow-up state are complete.
- AC-5: Plans that cannot be proven done keep their original status and receive a smallest next closure action plus an owner lane.
- AC-6: The generated-spec layout guard returns `0`.
- AC-7: The SPipe dev command check passes or the failure is recorded in this state file.

## Scope Exclusions
- GPU web/DB offload benchmark, fixture, recovery, and handoff plans.
- Plans whose only remaining work is Mac, Windows, ROCm/HIP, DirectX, BSD, QEMU, FPGA, board, or other external host/platform evidence.
- Implementing the product features described by every unfinished plan in one pass.
- Marking a plan done based only on intent, stale notes, or absence of obvious TODOs.

## Phase
dev-done

## Log
- dev: Created state file with 7 acceptance criteria (type: code-quality).
- spark: Ran parallel compiler, runtime, and app/UI audits. No candidate was safe to mark done from plan text alone; all rows require evidence, requirement selection, implementation, or supersede/merge work.
- integrator: Added `doc/03_plan/agent_tasks/recent_unfinished_plan_cleanup.md` with classifications, owner lanes, and review gates.
- review: Normal LLM review found no false done marks, but required an auditable discovery query and named omission handling.
- spark: Ran a second review-discovered Spark audit for reliable mode, alpha search, and alpha compression plans. No additional safe done marks were found.
- integrator: Added discovery cutoff/query, exclusion notes, review-discovered wave rows, and exact next closure actions.
- verify: `sh scripts/setup/install-spipe-dev-command.shs --check` passed with `STATUS: PASS spipe-dev-command wiring`.
- verify: `find doc/06_spec -name '*_spec.spl' | wc -l` returned `0`.
- runtime: Refreshed `doc/03_plan/agent_tasks/runtime_large_arraybuffer_probe_resume.md` evidence after `SIMPLE_LIB=src bin/simple test test/01_unit/lib/common/web/browser_session_large_arraybuffer_spec.spl --mode=interpreter` reported 1 passed file and 0 failures from the unchanged-test cache.
- review: Normal LLM review rejected marking the large-ArrayBuffer plan done until the lane cites requirements/NFR and guide-update applicability evidence, so the cleanup row remains `needs-evidence`.
- integrator: Searched `doc/02_requirements`, `doc/07_guide`, `doc/08_tracking/bug`, and the generated spec for large ArrayBuffer/Uint8Array evidence. Generated spec and bug context exist, but no final requirements/NFR artifact was found for this lane.
- runtime: Checked `doc/03_plan/runtime/process_safety/plan.md` recurrence item. User journal query since `2026-06-11 15:32:00` found 26 `exit.target` hits and system boot was `2026-05-31 02:41`, so the plan remains `needs-evidence` with a narrower recurrence-diagnostic next step.
- runtime: Refreshed `doc/03_plan/runtime/loader_shared_core_refactor.md` evidence. `metadata_symbols_spec`, `unload_ownership_spec`, and `module_loader_crash_fix_spec` passed, but `module_loader_spec.spl` failed 14 tests; later cleanup refresh located the canonical design guidance under `doc/05_design/compiler/architecture/loader_shared_core_refactor.md`, so the remaining closure blocker is primary module-loader implementation stability.
- lib: Completed the Phase 0 search type-barrier evidence for `custom_type_alpha_search_team_plan_2026-06-15.md`; `src/lib/common/search/types.spl` contains the listed search-domain types and passed `SIMPLE_LIB=src bin/simple check`.
- lib: Completed the Phase 0 compression type-barrier evidence for `custom_type_alpha_compression_team_plan_2026-06-15.md`; added `FseTable`, `Plaintext`, and `Compressed`, exported them from `common.compress.typed`, and extended `test/01_unit/lib/common/compress/typed/types_spec.spl` to 28 passing tests.
- lib: Refreshed search Phase 1/2 evidence. Focused search specs passed for types, exact, multi, prefilter, SIMD scan, inverted index, ranking, ANN, and roaring; the search plan remains open for glob, real SIMD specialization or documented scalar alpha boundary, C-oracle parity, broad test/lint, and verify gates.
- lib: Refreshed compression Phase 1 typed-codec evidence. Focused typed compression specs passed for types, LZ4, DEFLATE/gzip, Zstd, and LZMA2; the compression plan remains open for facade unification, C-oracle parity, deferred FSE/LZMA items, broad test/lint, runtime smoke, and verify gates.
- app/mcp: Attempted the MCP scenario-manual row. The generated manual at `doc/06_spec/integration/app/mcp_stdio_integration_spec.md` preserves a richer five-scenario operator flow, but current `test/02_integration/app/mcp_stdio_integration_spec.spl` is a single combined scenario and `doc/06_spec/test/02_integration/app/mcp_stdio_integration_spec.md` is stale/thin. A recovered split-source patch type-checked, but focused execution failed because source-mode `bin/simple_mcp_server` emitted no protocol stdout for initialize/tools-list JSONL input; `build/bootstrap/mcp-package/simple_mcp_server` answered initialize/tools-list but still returned `Missing tool name` for `tools/call` with `params.name`. The cleanup row remains open as `needs-evidence` until the MCP runtime evidence path is fixed and the split/manual doc is regenerated.
- app/spipe: Marked `doc/03_plan/app/spipe/sspec_api_capture_helpers.md` done for the Current API Slice after normal review. Evidence: `src/lib/common/spec/scenario_helpers.spl` covers checker/evidence helpers plus exec/binary/UI/API/protocol/redaction helpers; `src/lib/common/spec/scenario_evidence.spl` covers capture-policy resolution from step through built-in defaults; `SIMPLE_LIB=src bin/simple test test/01_unit/lib/common/spec/scenario_helpers_spec.spl --mode=interpreter` passed 53 tests; `SIMPLE_LIB=src bin/simple test test/01_unit/lib/common/spec/scenario_evidence_spec.spl --mode=interpreter` passed 23 tests; generated manuals exist for both specs. Deferred provider wiring remains explicitly out of scope.
- verify: `find doc/06_spec -name '*_spec.spl' | wc -l` returned `0`, and `sh scripts/setup/install-spipe-dev-command.shs --check` returned `STATUS: PASS spipe-dev-command wiring` after the SSpec API capture helper cleanup update.
- runtime: Ran the narrowed process-safety recurrence diagnostic. `journalctl --user --since '2026-06-11 15:32:00' --no-pager -o short-iso | rg 'Activating special unit exit\.target|Reached target exit\.target' | wc -l` returned `26`; representative windows showed post-deploy user-manager exits stopping `tmux-spawn-*` scopes on 2026-06-12, 2026-06-14, 2026-06-16, 2026-06-17, and 2026-06-18. `loginctl show-user ormastes -p Linger -p State -p Sessions` returned `Linger=yes`, so the row changes from `needs-evidence` to `needs-implementation`: fix or document the user-manager/tmux scope lifetime boundary before another recurrence check can close it.
- runtime: Completed `doc/03_plan/agent_tasks/runtime_large_arraybuffer_probe_resume.md` for the bounded sparse large-buffer runtime slice. Added final traceability docs at `doc/02_requirements/feature/runtime_large_arraybuffer_probe_resume.md` and `doc/02_requirements/nfr/runtime_large_arraybuffer_probe_resume.md`, cited implementation/spec/manual evidence, and recorded that no `doc/07_guide` update is required because this lane changes no public API, command, wrapper, or operator process.
- review: Normal review approved the large ArrayBuffer `DONE` mark for the explicitly bounded sparse `ArrayBuffer`/`Uint8Array` runtime slice. Review confirmed the requirements exclude full WebAssembly.Memory/WebGPU/browser parity, the NFR thresholds match the focused spec, implementation evidence covers sparse construction plus lazy reads/write-through normalization, and the `doc/07_guide` omission is reasonable for an internal runtime behavior change.
- runtime: Advanced `doc/03_plan/runtime/default_native_runtime_shift_phase2_plan.md` but did not mark it done. Added the missing simple-core string-builder ABI (`rt_string_builder_new/push/finish/len/free`) and a no-op `rt_pool_safepoint` for compiler-inserted loop safepoints in the bootstrap/simple-core lane. `SIMPLE_LIB=src bin/simple check src/runtime/simple_core` passed for 14 files, and `timeout 180 sh scripts/check/check-simple-core-runtime-smoke.shs` passed with `simple_core_hello=true`, `simple_core_standalone_tui=true`, `simple_core_tui_app=true`, and `simple_core_closure_clean=true`.
- runtime: The same default-native row remains `needs-evidence` because `timeout 120 sh scripts/check/check-mcp-native-smoke.shs` failed. Evidence: direct-rt gates were true and startup timings were under budget (`mcp_startup_ms=348`, `lsp_mcp_startup_ms=92`), but `mcp_stale_stamp_reprobe_ok=false`; validator output also reported false MCP/LSP JSON, framing, schema, and tool-count checks. Next closure action is fix the stale-stamp/full-tools capture or native server output path, then rerun the MCP/LSP package smoke before any done mark.
- review: Normal LLM review rejected two ABI details in the default-native slice: `rt_pool_safepoint` needed to return `i64`, and invalid `rt_string_builder_finish(0)` needed to preserve nil semantics. Both were fixed, then `SIMPLE_LIB=src bin/simple check src/runtime/simple_core` and `timeout 180 sh scripts/check/check-simple-core-runtime-smoke.shs` passed again.
- runtime: Fixed the MCP native smoke path by making `scripts/check/check-mcp-native-smoke.shs` explicitly opt into native wrappers for its package-handshake checks and by removing an unsupported nested `.find` shape from `scripts/check/validate_mcp_native_smoke.spl`. The validator now gates framing by exact content-length stream reconstruction with declared byte lengths. `timeout 120 sh scripts/check/check-mcp-native-smoke.shs` then passed with `mcp_startup_ms=72`, `lsp_mcp_startup_ms=45`, `mcp_second_start_ok=true`, `mcp_stale_stamp_reprobe_ok=true`, valid MCP/LSP framing/schema JSON, `mcp_tools_count=151`, and `lsp_tools_count=11`.
- runtime: Ran default package-shape native-build commands for MCP and Simple LSP MCP to `/tmp/tmp.5u1gvxNwfu/`; both built stripped binaries without explicit hosted runtime selection and a string scan found no `libsimple_native_all`, `rust-hosted`, `_Unwind`, or `unwind` markers. The default-native cleanup row changes from `needs-evidence` to `needs-implementation` because both package builds still emitted unresolved-stub warnings and the production wrappers still document native `tools/call` blockers.
- app/mcp: Completed `doc/03_plan/app/mcp/mcp_scenario_manual_quality.md` for the MCP stdio operator-flow slice. Restored `test/02_integration/app/mcp_stdio_integration_spec.spl` to five executable scenarios, added the docgen `# @manual-layout: manual-first` opt-in with default/opt-in unit coverage, regenerated `doc/06_spec/test/02_integration/app/mcp_stdio_integration_spec.md` so it opens directly on visible operator scenarios, removed stale duplicate `doc/06_spec/integration/app/mcp_stdio_integration_spec.md`, and verified `SIMPLE_LIB=src bin/simple test test/02_integration/app/mcp_stdio_integration_spec.spl --mode=interpreter` passed 5 scenarios. Also verified `bin/simple run test/01_unit/app/tooling/spipe_docgen_scenario_body_spec.spl` printed 57 examples, 0 failures; `SIMPLE_LIB=src bin/simple check src/app/spipe_docgen --mode=interpreter` passed; `find doc/06_spec -name '*_spec.spl' | wc -l` printed `0`; and `sh scripts/setup/install-spipe-dev-command.shs --check` printed `STATUS: PASS spipe-dev-command wiring`. Normal LLM review first blocked on the old folded summary-card output, then re-review approved after the manual-first fix and test update; the cleanup row is `mark-done`.
- compiler: Advanced `doc/03_plan/compiler/dependency_analysis/plan.md` W2-A2 from implementation-needed to evidence-needed. W2-A2 is checked in the source plan, and `SIMPLE_LIB=src bin/simple test test/01_unit/compiler/interpreter/module_loader_lazy_spec.spl --mode=interpreter` reported `Passed: 4`, `Failed: 0` (`Skipped 1 unchanged test(s) (cached)`). The cleanup row remains open until `SIMPLE_LAZY_PARSE=1` completion evidence and full dependency-flow smoke are recorded.
- runtime: Re-ran `SIMPLE_LIB=src bin/simple test test/01_unit/compiler/loader/module_loader_spec.spl --mode=interpreter` for the loader-refactor lane (2026-06-18 bounded refresh); it remains `8 passed, 14 failed`, so cleanup action changes from evidence collection to implementation-focused primary module-loader spec stability.
- compiler: Refreshed `doc/03_plan/compiler/jit/compiler_jit_rendering_loops.md` cleanup evidence with a focused run `env SIMPLE_EXECUTION_MODE=jit bin/simple run examples/06_io/ui/engine2d_shapes_gui.spl`. Result was non-fatal JIT bootstrap failure (`Cranelift JIT compile: Module error: unresolved external symbol 'rt_len'`), then interpreter fallback; command timed out as a result. Cleanup row changes to `needs-implementation` with a narrower next action to fix JIT external-symbol coverage (`rt_len` first-class path) and rerun phase-4 AC checks.
- app/ui: Reviewed `doc/03_plan/ui/graphics/backends/shared_ui_semantic_contract.md`. Finalized requirement selection is still missing (`doc/02_requirements/feature/shared_ui_semantic_contract.md` and `doc/02_requirements/nfr/shared_ui_semantic_contract.md` not present), so the row remains `needs-requirement-selection` with the next action set to create those final artifacts before Agent C/E closure evidence.
- runtime: Verified `doc/03_plan/runtime/rust_runtime_minimization.md` still blocks on selection and missing final artifacts; no final files exist yet at `doc/02_requirements/runtime/rust_runtime_minimization.md` or `doc/02_requirements/nfr/rust_runtime_minimization.md` (only `*_options.md` are present). The cleanup row remains `needs-requirement-selection` with next action to create/populate those two final requirement artifacts before Phase 0.
- app/ui: Verified `doc/03_plan/ui/graphics/backends/graphics_backend_acceleration.md` still blocks on selection and missing final artifacts; option files exist at `doc/02_requirements/ui/graphics/graphics_backend_acceleration_options.md` and `doc/02_requirements/nfr/graphics_backend_acceleration_options.md`, but final selected files are missing at `doc/02_requirements/ui/graphics/graphics_backend_acceleration.md` and `doc/02_requirements/nfr/graphics_backend_acceleration.md`.
- compiler: Closed `doc/03_plan/compiler/dependency_analysis/plan.md` for the dependency-analysis, lazy-parse, and MCP handshake evidence lane. Fixed deps CLI argument normalization in `src/app/deps/scanner.spl` and routed `src/app/deps/main.spl` through it so the advertised `bin/simple deps ...` and run-file forms strip `deps`, app-path prefixes, and `--` before dispatch; added CLI normalization coverage in `test/01_unit/app/deps/deps_tool_spec.spl`. Evidence: `SIMPLE_LIB=src bin/simple deps fast src/app/mcp/main.spl` reports 9 direct imports plus cycle status; `normal` reports shared/exclusive counts; `deep` reports 38 files with SCRIPT/SMF/NATIVE sections; `deps_tool_spec` reports 20 passed assertions; `deps_deep_spec` reports 14 passed assertions; `SIMPLE_LIB=src bin/simple check src/app/deps --mode=interpreter` passes; default and `SIMPLE_LAZY_PARSE=1` `module_loader_lazy_spec` runs each report 4 passed assertions; and `timeout 120 sh scripts/check/check-mcp-native-smoke.shs` passes with `mcp_startup_ms=67`, `lsp_mcp_startup_ms=46`, `mcp_tools_count=151`, and `lsp_tools_count=11`.
