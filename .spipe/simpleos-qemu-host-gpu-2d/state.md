# Feature: SimpleOS QEMU Host-GPU 2D Hardening

## Raw Request
`$sp_dev harden simple 2d on qemue, specially x86,aarch,riscv. research simple os on qemu can use host gpu? metal,cuda,vulkan for process. rendering metal,directx,vulkan. make simple os qemu to use underlying gpu processing/rednering.`

## Task Type
feature

## Refined Goal
Make SimpleOS use a real QEMU-exposed host-accelerated GPU path for Simple 2D rendering and portable processing on x86_64, AArch64, and RISC-V where the host/QEMU stack supports it, with explicit capability detection, safe software fallback, and evidence that distinguishes device-backed execution from emulation or CPU rendering.

## Acceptance Criteria
- AC-1: Research documents identify the actual QEMU device, guest-driver, and host-renderer/compute paths available for Linux Vulkan/CUDA, macOS Metal, and Windows DirectX/Vulkan hosts, and explicitly classify unsupported combinations instead of claiming backend parity from API names alone.
- AC-2: Final selected requirements define pass/fail behavior for x86_64, AArch64, and RISC-V QEMU guests, including host prerequisites, capability negotiation, fallback behavior, rendering correctness, processing correctness, latency/RSS targets, and device-backed evidence.
- AC-3: Each target architecture boots the applicable SimpleOS QEMU image, probes the exposed GPU capability, and reports a stable backend/reason pair; an unavailable accelerated path selects the existing software/CPU fallback without boot failure or silent false-positive acceleration.
- AC-4: On every host/QEMU combination classified as supported, Simple 2D renders the canonical fixture through a guest-visible accelerated device, reads back the produced frame, and matches the CPU oracle using the repository's exact comparison contract.
- AC-5: Portable processing runs the same ProcessingIR fixture through the supported device-backed path and CPU oracle with matching results; CUDA, Vulkan, and CPU remain backend choices below ProcessingIR rather than separate public APIs.
- AC-6: Evidence proves real guest-to-QEMU-to-host GPU execution using negotiated virtio-gpu/virgl/venus/rutabaga or another researched supported device path plus host-side backend markers; screenshots, configured command-line flags, synthetic handles, and CPU mirrors alone fail the gate.
- AC-7: One canonical SPipe wrapper and scenario matrix cover x86_64, AArch64, and RISC-V, publish per-row `pass`, `unsupported`, `blocked`, or `fail` status with reasons, and feed `scripts/check/check-simpleos-hardening-evidence-matrix.shs` without weakening its existing 26/26 completion contract.
- AC-8: Focused tests cover capability negotiation, unsupported-device fallback, frame readback parity, processing parity, and fail-closed evidence parsing; executable specs live under `test/` and their generated operator-readable manuals live under `doc/06_spec/` as Markdown.
- AC-9: Final verification runs each acceptance check at most once after convergence, reports architecture/host limitations honestly, passes the direct runtime/env guards and generated-spec layout guard, and refreshes all changed architecture, plan, guide, report, SPipe-manual, skill/agent/command, and feature-tracking artifacts before any completion claim.

## Scope Exclusions
- Direct PCIe GPU passthrough/VFIO setup is excluded unless research shows it is required for a selected supported row; mediated/paravirtual QEMU GPU access is the default scope.
- A new public rendering or compute API is excluded; reuse Engine2D backend lanes and ProcessingIR.
- Unsupported host/guest combinations are not to be disguised with software rendering, API translation labels, or synthetic evidence.

## Cooperative Review
- Sidecar `qemu-gpu-domain`: QEMU/virtio-gpu/virgl/venus/rutabaga and Linux/macOS/Windows host-backend feasibility, including authoritative external sources.
- Sidecar `simpleos-gpu-local`: existing SimpleOS virtio-gpu, Engine2D, ProcessingIR, QEMU boot, and evidence-wrapper inventory; no edits.
- Sidecar `multiarch-evidence`: x86_64/AArch64/RISC-V boot and current hardening-matrix coverage/gaps; no edits.
- Merge owner: primary Codex `/root`.
- Final reviewer: primary normal/highest-capability Codex after sidecar evidence is merged; it owns requirement-option quality, exclusions, and generated-manual acceptance.
- Shared interfaces: existing `Engine2D` backend lane and `ProcessingIR`; capability/result records must use one shared guest-visible backend/reason contract rather than architecture-specific public APIs.
- Manual flow helpers: `step("Probe the QEMU guest GPU capability")`, `step("Render and read back the Simple 2D parity fixture")`, `step("Run the ProcessingIR parity fixture")`, and `step("Report device-backed host acceleration evidence")`.
- Setup/checker helpers: reuse `scripts/check/check-simpleos-hardening-evidence-matrix.shs`; the lane-owned focused wrapper name is `scripts/check/check-simpleos-qemu-host-gpu-2d.shs` only if no existing wrapper already covers all rows.
- Fail-fast placeholders: temporary scenario/helper bodies must use `fail("simpleos-qemu-host-gpu-2d not implemented")` and may not use passing stubs or synthetic acceleration markers.
- Generated-manual review owner: primary Codex `/root`.

## Phase
implementation-in-progress

## Log
- dev: Created state file with 9 acceptance criteria (type: feature); implementation and verification are intentionally deferred to subsequent SPipe phases.
- research: Restored current local/domain research after the earlier shared-checkout artifacts disappeared; current source still proves CPU raster plus VirtIO-GPU 2D scanout only.
- requirements: User selected Feature B (cross-host QEMU GPU service) and NFR B (balanced desktop target); finalized `doc/02_requirements/feature/simpleos_qemu_host_gpu_2d.md` and `doc/02_requirements/nfr/simpleos_qemu_host_gpu_2d.md`.
- design: Selected one file-backed QEMU `ivshmem-plain` data plane after sidecar review found no proven TCP path across all three guests; recorded x86 BAR2 and ARM/RISC-V ECAM/BAR prerequisites.
- implementation: Added the conflict-free common protocol and focused unit spec in dedicated jj workspace `/tmp/simpleos-qemu-host-gpu-2d` because concurrent main-workspace snapshots deleted new files during verification.
- implementation: Recovered the exact jj snapshot after a concurrent stale-workspace rewrite, then added syscall 88 `MapDeviceBar`: exact BDF/BAR capability checking, caller-bound grant-token validation, safe 64-bit BAR-pair sizing/restoration, kernel-derived mapping, and a minimal userspace wrapper without changing the `DeviceGrant` ABI.
- implementation: Added the aligned ivshmem wire contract, owner-facade guest publisher, strict host `Engine2D` executor for render commands and processing `fill_u32`, device-readback enforcement, packed readback, QEMU scenario constructors, ivshmem discovery, ARM64/RISC-V64 ECAM access, and per-architecture high-MMIO BAR assignment.
- verification: Protocol source passed `bin/simple check`; the spec still reports parser errors at `describe`/`it` after the third bounded fix/check cycle, so no PASS is claimed and the command was not rerun.
- verification: The initial BAR mapping slice was left unverified when that bounded pass reached its three-cycle cap; later implementation added assignment, ECAM, host service, and guest bridge without claiming live evidence.
- verification: In the next bounded pass, `src/app/simpleos_gpu_host/main.spl` and `src/os/lib/gpu_bridge/host_gpu_ivshmem.spl` passed source checks. The kernel device-syscall check was terminated with exit 143 before a result; it was not retried. Live QEMU boot/readback and the host/guest matrix remain incomplete, so no feature PASS is claimed.
- implementation: Hardened host provenance so Vulkan accepts only discrete/integrated adapters, Metal/CUDA require native device names, and receipts separate a stable device-identity hash from the allocation handle. Added `--self-test=<backend>` and narrowed I/O imports to keep compiler/CLI code out of the daemon closure.
- verification: The live host self-test reached the mmap boundary but the interpreter lacks `rt_mmap`. The canonical native build then hit the tracked compiler `CODEGEN-AMBIGUOUS-METHOD` in `FixApplicator.apply`, fell back to the interpreter, and made no progress for several minutes; it was terminated under the runaway guard. Three bounded verify/fix cycles were consumed, so the narrowed native build was not retried. No live GPU or QEMU PASS is claimed.
- verification: A fresh bounded pass reproduced the same native-build blocker. Replacing `file_reps.push(rep)` with array concatenation passed the focused source check, but native lowering converted it back to the same ambiguous `push`; the ineffective change was reverted. The third cycle again ended at `rt_enum_discriminant`, so no further retry was made. A future fix must change native array-method resolution or use a proven non-`push` collection primitive, not syntax sugar.
- verification: Production-style `core-c-bootstrap`/`src/app` entry closure did not bypass the blocker. Subcommand `--no-jit` was ignored and still JIT-compiled, global `--no-jit` was parsed as a source filename, and `SIMPLE_BOOTSTRAP=1` still entered JIT; all bounded routes reproduced `FixApplicator.apply` ambiguous `push`. The next lane is compiler method-resolution/native array append, not another native-build flag retry.
- implementation: Removed array append/push lowering from `FixApplicator.apply` itself. Replacements are grouped in nested dictionaries and applied by a bounded highest-offset selection scan, preserving descending edits and overlap rejection without reaching ambiguous native `push` resolution. The O(n²) ceiling is documented for future large fix batches.
- verification: The rewritten compiler source passed `bin/simple check src/compiler/90.tools/fix/main.spl`. This pass consumed three cycles (append experiment, native reproduction, dictionary rewrite check), so FixApplicator behavioral specs and the host native build were not rerun yet.
- verification: `test/01_unit/app/fix/short_grammar_fix_spec.spl` passed 66/66 against the dictionary-based implementation. Both normal and `--clean` host native builds still emitted a generated bare `push` for `FixApplicator.apply`; clean-cache reproduction rules out stale source. The remaining blocker is compiler-generated collection/dictionary lowering or its method metadata, not FixApplicator behavior. No fourth build was run.
- verification: `SIMPLE_DEBUG_METHOD_DISPATCH=1` captured the exact failing MIR call: `bare 'push' call: receiver ty = Any (local-table fallback also unnamed)` in `FixApplicator.apply`. This proves receiver-type erasure in MIR lowering/codegen is the active blocker; source behavior and cache are cleared. The next fix must preserve/recover the collection receiver type before native method resolution.
- verification: A post-inline MIR dump traced the failing call to `local6.push(rep)` immediately after `by_file[rep.file]`; this exactly matches the pre-`a9a5fb455214` array implementation, not the current nested-dictionary source. The deployed self-hosted `bin/simple` is therefore stale and is JIT-compiling its embedded/pre-fix FixApplicator body. Do not change method lowering: bootstrap and deploy the current pure-Simple compiler, then rerun the host build once.
- implementation: A global deploy was intentionally avoided because another active session was using `bin/release/x86_64-unknown-linux-gnu/simple`. An isolated dynload bootstrap reused the existing Rust seed/runtime read-only and built `build/bootstrap/full/x86_64-unknown-linux-gnu/simple`; Stage 4 reports 1194 compiled, 0 failed. The wrapper exited before its smoke success banner, so the lane-local CLI still requires one direct smoke plus the focused FixApplicator regression before it may build the GPU host daemon.
- verification: The lane-local full CLI cannot stage `-c` snippets (`failed to stage -c snippet`), but its file-based EasyFix regression passed 66/66, proving the current dictionary implementation executes and the stale `FixApplicator.apply` push blocker is cleared. Its first GPU-host native-build no longer reports ambiguous `push`; it instead aborts with `runtime error: field access on nil receiver` and signal-derived exit 132. This pass reached the three-cycle cap. Next use the proven Stage 2 `run src/app/cli/native_build_main.spl -- ...` bootstrap entrypoint (the same route that linked Stage 4) to distinguish the incomplete full-CLI runtime from a target-source failure.
- verification: The Stage 2 wrapper avoided the nil-receiver crash but its interpreted worker widened into unrelated dashboard/debugger/compiler modules and timed out at 600s. Forcing the supported in-process path (`SIMPLE_EXECUTION_MODE=jit`, no worker timeout argument) removed that unresolved-module flood and emitted no target error, but still produced no binary before a 360s guard. No ambiguous `push`, target-source diagnostic, or host artifact appeared. The next pass must measure/trace the Stage 2 entrypoint startup itself or build a minimal host-only compiler closure; merely increasing target-build time is not evidence-driven.
- implementation: Isolated the delay to Stage 2's generic `run` launcher (`native_build_main.spl --help` itself timed out at 180s), then used Stage 2's direct `native-build` command—the exact successful Stage 4 route. It built `build/simpleos_gpu_host/simpleos_gpu_host.direct` in 39s: 167 compiled, 0 failed, 11.6 MiB. The artifact exposes the expected host entrypoints and mmap/GPU runtime surface; some volatile/Vulkan-readback symbols remain dynamic and require live-path proof. A no-file `--self-test=vulkan` correctly rejected the missing 8 MiB `--shm` region with exit 2, so GPU execution was not yet exercised.
- implementation: A real 8 MiB self-test reached the mmap gate and proved `rt_mmap` was a 3-byte linked stub. Rebuilding with explicit `--runtime-bundle core-c-bootstrap` produced a byte-identical binary because the pure-Simple CLI validated then discarded the selected bundle. `cli_native_build` now normalizes the stable aliases, exports `SIMPLE_NATIVE_RUNTIME_BUNDLE` around the driver call, and restores the prior value on every result path. The source check passes. The focused dispatch contract currently reports 8/9 with one assertion still unidentified at this pass's three-cycle cap; do not bootstrap/retest the daemon until that contract is green.
- verification: The sole contract failure was a stale bootstrap ABI expectation (`rt_cli_get_args` instead of bootstrap_main's minimal `rt_get_args`), unrelated to the runtime-bundle change. Corrected the context-specific assertion; `cli_native_build_main_contract_spec.spl` now passes 9/9, including explicit bundle propagation/restoration. The next pass may bootstrap this CLI fix and rebuild the host daemon with `core-c-bootstrap`.
- verification: Rebootstrapped the CLI fix successfully (Stage 2 + self-hosted Stage 3). Both the explicit CLI flag and a process-level `SIMPLE_NATIVE_RUNTIME_BUNDLE=core-c-bootstrap` now reach a successful host link, but the verbose link command proves runtime selection still inserts `src/compiler_rust/target/bootstrap/deps/libsimple_runtime.a` twice. That Rust archive supplies 3-byte `rt_mmap`/`rt_munmap` stubs, so the rebuilt binary is unchanged and cannot map ivshmem. The next fix belongs in core-C runtime archive discovery/build selection; do not modify the daemon or add an mmap workaround.
- implementation: Fixed shared runtime selection so `core-c-bootstrap` no longer treats the ambiguous root/deps archive beside the Rust seed as core C. It now accepts only an explicitly named core-C lane or builds `src/runtime` into a fresh archive; the Rust-runtime fallback was removed. Updated the selector regression to reject the root archive and require the core ABI. Crate-wide fmt check remains dirty only in unrelated pre-existing files; the two local suggestions were applied. Two focused cargo-test attempts compiled the change but exhausted 300s linking the large test binary before test execution, so behavioral PASS is still pending; use the repo's faster linker path next rather than increasing the guard.
- verification: Shared Cargo output was being invalidated by concurrent sessions, so the selector test was rebuilt in an isolated target with clang+lld. The 500 MiB-class lib-test binary linked successfully in 3m31s, but the leaf-only `--exact` filter matched zero tests (3114 filtered). No behavioral PASS is claimed. The reusable binary is `build/cargo-selector-target/debug/deps/simple_compiler-ca8dde13dff5d3ac`; next list its full test name and execute that binary directly, with no Cargo rebuild.
- verification: Executed the selector test directly with its full canonical name; selection correctly rejects the Rust archive but fails the core-ABI assertion. The pre-existing dedicated core-C archive ABI test fails identically. A manual canonical six-object archive diff identifies the absent core-required functions as `rt_string_builder_{new,push,finish,len,free}` (the init/shutdown names were present; the first diff normalized leading underscores differently). Independently, the host-required `rt_mmap`/`rt_munmap`, volatile read/write, and barrier primitives are absent from core C. The next implementation must complete the shared core-C ABI/host primitive capsule before rebuilding the seed or daemon.
- implementation: Completed `runtime_native.c` with the five amortized string-builder ABI functions, portable hosted mmap/unmap, u8/u16/u32/u64 volatile reads/writes, and acquire/release/seq-cst fences; declarations live in `runtime.h`. The canonical six-object archive compiles with `-Werror`, has every `CORE_REQUIRED_RUNTIME_SYMBOLS` entry, and exports real host primitives (`rt_mmap` size 0x87, not a 3-byte stub). Direct execution of both the core-archive ABI test and core-C selector regression passes (1/1 each); the archive test source additionally requires the host primitives. Next full-bootstrap the Rust selector locally without deploying shared tooling.
- verification: Lane-local full bootstrap passed (new Rust seed plus self-hosted Stage 2/3). The daemon now links the freshly built core-C archive, shrinks from 11.6 MiB to 6.7 MiB, and exports real mmap/volatile/barrier implementations. Its live 8 MiB Vulkan self-test still reports `mmap failed`. Root cause is the shared extern ABI: native `text` expands to `(path_ptr,path_len)` (e.g. `rt_file_exists` is registered as two args), while `rt_mmap` has no runtime SFFI spec and the C header/implementation accept only `(path,size,offset,readonly)`. Next align the runtime spec and C/Rust mmap signature; do not alter permissions, backing-file creation, or daemon validation.
- implementation: Aligned `rt_mmap` across Cranelift, LLVM, core C, platform C, and Rust SFFI: the path is expanded to `(path_ptr,path_len)`, the native signature is registered explicitly, and the language-level `readonly` argument remains the native `i64` ABI value. Added a focused runtime-spec regression. Both bounded full-bootstrap runs reached successful self-hosted Stage 3; the first exposed and the second fixed the `i8`/`i64` LLVM declaration mismatch.
- verification: The dependency-scoped Cranelift daemon build completed with 167 files, zero failures, and the core-C bootstrap runtime. A live `strace` of its 8 MiB Vulkan self-test proves `openat(..., O_RDWR)` and `mmap(NULL, 8388608, PROT_READ|PROT_WRITE, MAP_SHARED, ...)` succeed. The process then stalls after mapping and before completing request seeding; it was terminated after the bounded observation window. The mmap ABI blocker is closed, while the post-map self-test stall and cross-architecture QEMU matrix remain pending.
- implementation: Removed the compiled volatile facade's hardcoded no-op gate, decoded tagged indices in shared core-C `rt_index_get`/`rt_index_set`, and made the three ivshmem layout limits compile-time arithmetic rather than cross-global runtime initializers. This fixes native `for` loops beyond index zero, preserves all CLI arguments, and writes payload/control words to their intended shared-memory offsets.
- verification: The final bounded Cranelift/core-C daemon build compiled 167 files with zero failures. Its live Vulkan self-test recognizes both CLI arguments, maps 8 MiB, writes the complete 12-word payload at offset 4096, passes protocol validation, and reaches backend selection. The host reports backend unavailable (wire status 2/reason 2), then the diagnostic print aborts with `field access on nil receiver`; no further fix was attempted after the third cycle. Next trace the unavailable Vulkan core-C SFFI lane and the nil-safe receipt print before the x86/AArch64/RISC-V QEMU matrix.
- implementation: Reused the existing Rust Vulkan runtime rather than duplicating it in core C. Implemented its previously stubbed `rt_mmap`/`rt_munmap` with `memmap2`, added native volatile u8/u16/u32/u64 reads/writes, and added a writable shared-mapping lifecycle regression. An isolated `simple-runtime --features vulkan` release archive was built; a build-only `CC` override proved it can replace the generated core-C archive without changing Simple objects.
- verification: The mmap lifecycle regression passes. Host evidence reports Vulkan 1.3.275 with discrete NVIDIA TITAN RTX and RTX A6000 devices. The Vulkan-linked daemon grows from 6.3 MiB to 7.8 MiB and contains the real Vulkan implementation strings, but strict Engine2D creation still returns wire status 2/reason 2 (`backend unavailable`). The nested receipt print then aborts with `field access on nil receiver`, hiding the backend's detailed error. At the third-cycle cap, the next bounded pass is: make the receipt/error print nil-safe, expose `VulkanBackend.last_error`, then fix the single reported init failure before promoting the temporary archive substitution into a supported host-GPU build wrapper.
- implementation: Made strict Engine2D Vulkan creation return the runtime error, simplified daemon receipt output, and aligned Rust Vulkan `device_name`, `device_type`, and `get_last_error` exports with Simple's tagged-text ABI instead of raw C strings. A direct C ABI probe against the same archive proves `available=1`, `init=1`, and `device_count=3` on this host.
- verification: The rebuilt Vulkan-linked daemon now exposes the real failure: `compile_spirv: bad SPIR-V magic 0x20000008`. This proves instance creation, device enumeration/selection, and error propagation work; the shader entrypoint is reading the tagged Simple byte-array handle as a raw byte pointer. The simplified receipt line still reaches a separate nil-receiver abort after the backend error. At the third-cycle cap, next decode the runtime array with `rt_array_data_ptr_u8`/`rt_array_len` inside `rt_vulkan_compile_spirv`, then remove or separately isolate the nonessential receipt print before the device-readback proof.
- implementation: `rt_vulkan_compile_spirv` now decodes tagged Simple `[u8]` values with the runtime array data/length APIs and validates the exact header length; a focused packed-byte-array regression passes. Removed value interpolation from the nonessential receipt print. Added the previously declared-but-missing `rt_vulkan_api_version` tagged tuple ABI after a native stack trace found the null call inside `vulkan_device_info`.
- verification: The Vulkan-linked daemon passes the prior SPIR-V magic and device-info failures. It now aborts with the existing compiled duck-typed virtual-call guard: the entry-closure unit lacks a concrete RenderBackend vtable (`trait has no impl Trait for ... in unit`, tracked by `jit_game2d_backend_method_dispatch_sigsegv_2026-07-02`). This is a compiler/closure reachability blocker after real Vulkan instance/device/shader setup, not host GPU unavailability. At the third-cycle cap, next capture the exact missing method/type from an unstripped stack and include the concrete backend impl in native entry-closure reachability; do not fall back to interpreter for the production daemon.
- implementation: Native-project discovery now records cross-module `impl Trait for Type` pairs and forwards them into HIR/MIR dependency reachability without duplicating vtable emission. Focused regression `test_build_import_map_records_cross_module_trait_impls` passes (1 passed; 3116 filtered).
- verification: Three bounded daemon builds all stopped before target lowering on the pre-existing conflict marker in `src/app/io/cli_ops.spl` (`TripleLt`). Explicit source roots, an app mirror omitting that file, and running from a filtered project cwd still resolved the original repository file. No Vulkan executable or receipt was produced, and the unrelated file was not modified. Resume after its owning lane resolves the conflict; do not retry the same build or disguise it with a source/runtime alias.
- verification: A third-turn build-only isolation pass found a safe route around the unrelated conflict without editing it. Relocating the seed and mirroring compiler/lib alone was insufficient because missing `app.io.cli_ops` fell through to the original resolver root; adding a resolved copy inside the ignored filtered app tree advanced compilation past parsing. The third bounded build then stopped at `could not create native-build cache directory build/native_cache`. That parent now exists for the next pass. No target executable or GPU receipt was produced, and no fourth build was run.
- verification: With the cache parent present, the default Rust-seed `native-build` route was killed by its 60-second worker timeout; `--timeout 300` proved the interpreted worker still spends the full budget loading the compiler/LLVM graph and produced no target. The documented in-process selection (`SIMPLE_EXECUTION_MODE=jit` without a timeout argument, externally bounded to 300 seconds) reached that path but crashed with SIGSEGV before output. Three cycles were consumed. Source inspection found the existing no-code next route: an explicit `--target=x86_64-unknown-linux-gnu` makes `dispatch_command` call the Rust `handle_native_build` directly, bypassing both Pure-Simple wrapper modes. Use that once next; do not repeat either worker path.
- verification: The explicit host target routed directly to Rust native-build and succeeded: 164 compiled, 0 failed, Vulkan runtime linked, 7.8 MiB output, 31.6 seconds. The live 8 MiB Vulkan self-test then crashed with SIGSEGV before receipt completion (`completion_generation=0`, native handle/checksum/device identity all zero, readback pixels blank). GDB proves `PC=0`; disassembly identifies zero-argument wrapper `0xa00340` calling a null pointer from `0xb9ae68`. The stripped binary cannot name that wrapper. Three cycles were consumed (build, live receipt, gdb). Next build the same direct-host-target artifact without `--strip`, then capture the named null wrapper before changing code.
- verification: The unstripped direct-host-target build succeeded (1 compiled, 163 cached, 0 failed). Named gdb evidence shows `Engine2D.shutdown` dispatching to `WebGpuBackend.init`, then null `webgpu_sffi_is_available`. A pre-call memory probe proves the Vulkan object stores `__vtable__VulkanBackend__for__Engine2DExtended` at offset 0 instead of `__vtable__VulkanBackend__for__RenderBackend`; shutdown slot 2 therefore reads into the adjacent WebGPU table. Root cause is `emit_vtable_data_objects`: both `vtable_data_ids` maps are keyed only by struct/type and silently overwrite the first trait vtable with the last. Three cycles were consumed (unstripped build, named stack, live vtable memory). Next fix canonical multi-trait vtable selection at this owner boundary with a focused two-trait regression; do not add WebGPU runtime symbols or alter the daemon.
- implementation: Added widest-trait primary-vtable selection for the current one-pointer object ABI, with a documented trait-fat-pointer upgrade path. The first focused two-trait regression passed (1/1), and a lane-local Rust driver rebuilt successfully. The live Vulkan daemon still stored the secondary vtable and crashed, proving native project invokes vtable emission separately per module/call and the initial selection map was too local. The fix now persists primary slot counts in `CodegenBackend`, and the regression emits primary and secondary trait tables in separate calls. Per the three-cycle cap, this corrected regression and daemon receipt remain for the next pass; no fourth execution was run.
- verification: The cross-call regression passed and the lane-local driver rebuilt, but the rebuilt Vulkan daemon still crashed before receipt completion with the same zeroed wire fields. This disproves TypeId-keyed persistence: native HIR units assign distinct TypeIds to the same concrete struct while the struct-name vtable map still overwrites. The correction now keys primary selection by struct name, records every observed TypeId alias for that name, and remaps all aliases to the widest table; the regression uses different TypeIds across separate emission calls. Three cycles were consumed, so the corrected test and receipt are pending and the compiler change remains uncommitted.
- cooperative-review: Three user-requested sidecars reviewed the lane. Both compiler reviewers rejected widest-primary-vtable selection as language-correct: `RenderBackend.name` and `Engine2DExtended.draw_text_bg` both occupy slot 0, MIR erases trait identity, and local per-unit vtables cannot represent both through one object-header pointer. The passing map regression therefore proved only alias remapping. The uncommitted workaround was removed. Accepted compiler path: preserve trait identity through coercion/call MIR, create `{data_ptr, vtable_ptr}` trait wrappers, make per-(type,trait) vtables link-visible across native units, and add a real two-file native regression that calls overlapping slots through both traits.
- cooperative-review: The multiarch sidecar found no runnable guest row yet: the three host-GPU scenarios are unregistered, the guest ivshmem bridge has no caller, the system spec contains seven fail-fast placeholders, the wrapper is absent, and expected kernels are absent. ARM64 must use `virt,highmem-ecam=off` because its DTB ECAM then matches the current guest PCI address `0x3F000000`. After x86 host receipt, the minimum guest slice is one boot probe, three scenario registrations with shared-memory path plumbing, the ARM flag, and one canonical wrapper retaining serial/daemon/wire/readback/parity/time/RSS evidence.
- implementation: Replaced the rejected widest-vtable workaround with an explicit native trait-value ABI. Traits retain distinct HIR TypeIds; MIR inserts `MakeTraitObject` coercions for typed locals, assignments, fields, and arguments; Cranelift represents a trait value as a tagged 16-byte `{data_ptr, vtable_ptr}` wrapper; per-(type, trait) vtables are exported/imported by canonical symbol; and virtual calls preserve trait identity plus distinguish wrapper receivers from the temporary legacy concrete-object header fallback. Added a two-file native overlapping-slot regression (`Primary.a/b`, `Secondary.x`) whose compiled executable exits 0.
- verification: `cargo check -p simple-compiler` passed and the two-file native regression compiled 2 files with zero failures and exited 0. A fresh 164-file Vulkan daemon initially moved the crash from `Engine2D.shutdown` to concrete `VulkanBackend.init`; adding the MIR receiver-representation bit fixed that concrete fallback crash. The next fresh daemon completed normally instead of SIGSEGV, but returned wire status 2/reason 7 (`SOFTWARE_ADAPTER`). Scanning every `vulkan_device_info(index)` still returned no Simple-visible discrete/integrated adapter even though the linked native Vulkan probe reports available=1, init=1, count=3. Three live build/run cycles were consumed; stop here. Next isolate the tagged-text/device-type SFFI result per index with a focused native fixture, not another daemon retry.
- cooperative-review: The compiler ABI sidecars accepted `{data_ptr,vtable_ptr}` and rejected widest-table selection; the regression sidecar supplied the cross-unit overlapping-slot fixture. The QEMU sidecar found a separate honest-evidence blocker: x86 syscall 88 is Fat32ReadFile, ARM case 82 does not fill `DeviceGrant` and has no 88, and RV64 has no 80/82/88 shim. Before guest scenarios are enabled, expose one kernel-owned ivshmem BAR2 mapping facade, then add three thin probe entries, register all three scenario dispatch sites, pass `SIMPLEOS_HOST_GPU_SHM`, use ARM `virt,highmem-ecam=off`, and run the persistent daemon before the guest (not daemon `--once`).
- runtime_need: map and synchronize a QEMU shared-memory device.
- facade_checked: `app.io.file_ops`, `app.io.volatile_ops`, kernel MMIO/barriers, `DeviceGrant`, and Engine2D facades.
- chosen_path: `reuse-facade` plus the smallest owner-owned multi-BAR grant extension.
- rejected_shortcuts: raw `rt_*`, arbitrary physical mapping, synthetic handles, CPU-mirror pass, QEMU-flag-only proof, fixture-only branches, direct backend pokes, and a second TCP protocol.
- implementation: Proved Vulkan device text/type decoding independently with a two-file native facade probe: count=3, TITAN RTX=discrete, RTX A6000=discrete, llvmpipe=cpu. The prior status 2/reason 7 was not SFFI text corruption: GDB showed `VulkanBackend.init` never called Vulkan because exact concrete receivers still used the legacy one-vtable object header. MIR now devirtualizes every exact concrete trait-method receiver to `Type.method`; the cross-unit regression additionally calls `dual.a()` and `dual.x()` directly and still exits 0 alongside both trait-wrapper calls.
- implementation: Removed the nonexistent `rt_vulkan_read_buffer_bytes` extern and reused `rt_vulkan_copy_from_buffer` in the existing Simple facade. At the runtime owner, Vulkan upload/download now decode tagged Simple `[u8]` values with the same `rt_array_len`/`rt_array_data_ptr_u8` pattern already used by the SPIR-V compiler, reject unsupported upload offsets, and bounds-check downloads instead of treating RuntimeValue bits as host pointers.
- verification: Fresh compiler build and fresh two-unit native regression pass. A fresh 164-file Vulkan daemon now performs real Vulkan init/selection/pipeline work and reaches `VulkanBackend.present`; the first run exposed the missing readback symbol, and the next exposed the tagged-array pointer bug. After rebuilding the Vulkan runtime, a GDB-controlled run completes without SIGSEGV and publishes status 3/reason 6 (`NON_DEVICE_READBACK`); an ordinary run remains nondeterministically inside NVIDIA teardown before publishing. The remaining deterministic owner bug is `VulkanBackend.present()` clearing `dirty`, after which `read_pixels_with_source()` relabels the just-downloaded device buffer as `host_cache_after_device_present` with handle 0. Three cycles were consumed. Next preserve same-frame device-readback provenance/handle for the presented cache, then isolate teardown ordering if the non-GDB crash remains; do not retry unchanged v29.
- implementation: Added `host_buf_device_readback` to `VulkanBackend`; it becomes true only after a complete device download, is cleared for initialization/shutdown/CPU-upload paths, preserves the native handle for a presented device cache, and leaves `dirty` set when download length is wrong. Added failure-only host receipt diagnostics. Fixed imported class/struct method registration so `Engine2D.read_pixels_with_source -> Engine2DReadback` remains concrete across native units instead of degrading to `Any` and resolving `source` through the wrong global field offset.
- verification: Fresh compiler build plus a fresh 164-file daemon closes the wrong-field crash. The x86 Vulkan self-test exits 0 and publishes completion=1, status=PASS, reason=0, native_handle=1, output_bytes=12288, elapsed_us=5474, readback_source=device, and discrete device_identity=1299669760. Pixel audit rejects this as completion evidence: output_checksum=1 and the 3072-pixel readback is entirely zero despite the seeded clear `0xFF224466` plus fill-rect `0xFFCC2020`. Source inspection identifies the next owner: `rt_vulkan_push_constants` in `vulkan_graphics_runtime_compute.rs` still builds a byte slice from the tagged Simple `[u8]` RuntimeValue itself, exactly the ABI bug already fixed for SPIR-V and buffer copies. Three cycles were consumed; next decode/length-check push constants, then require exact clear/rect pixel parity before accepting the x86 receipt. Do not accept status-only v32.
- implementation: `rt_vulkan_push_constants` now decodes and bounds-checks the tagged Simple `[u8]`; Vulkan storage buffers now include the transfer flags required by the runtime's public staging upload/download APIs. Three user-requested sidecars reviewed validation, diff scope, and the next multiarch QEMU lane; final review rejected a fabricated descriptor binding and an `end_compute` no-op, so neither remains in the lane.
- verification: v33 still returned 3072 zero pixels. Vulkan validation then exposed the actual remaining runtime failures: hand-assembled Engine2D shaders use set 0/binding 0 while the reflected pipeline layout has zero bindings, the SFFI helper and compute submitter both end the command buffer, and the framebuffer originally lacked `TRANSFER_SRC`. A third-cycle diagnostic removed the descriptor/compute errors but accidentally moved the missing-end error to transfer submission; v35 therefore still returned 3072 zeros. The accidental lifecycle edits were removed and no fourth rebuild was run. Next fix descriptor layout ownership without inventing bindings, preserve real `rt_vulkan_end_compute` semantics with a distinct already-ended submit path, then run one validation-enabled exact-pixel receipt. The sidecar also flags exclusive-sharing ownership if compute and transfer queue families differ.
- cooperative-review: Three user-requested Spark/small-model lanes traced descriptor reflection, command lifecycle, and existing regression surfaces. Final review accepted: rspirv parsing of only declared StorageBuffer variables; exact set-0 binding recreation; a distinct already-ended compute submit path; and reuse of the existing descriptor-free Vulkan test plus Engine2D oracle. It rejected vendored spirv-reflect edits, guessed binding 0, command mocks, and weakening `rt_vulkan_end_compute`.
- implementation: The Vulkan runtime now merges real StorageBuffer decorations missed by vendored spirv-reflect 0.2.3, preserves sparse set-0 binding numbers/counts in recreated descriptor layouts, keeps descriptor-free shaders descriptor-free, decodes push-constant byte arrays, enables staging transfer flags on storage buffers, and separates internal auto-end submission from the public explicit end/submit contract. The parser regression and descriptor-free Vulkan compile regression pass; cargo check with Vulkan passes.
- verification: v36 and v37 run clean under `VK_LAYER_KHRONOS_validation` with completion/status/device provenance fields populated, but exact audit still rejects both because all 3072 pixels are zero. GDB proves the first clear push constant is 64 bytes with exact `66 44 22 ff`, width 64, height 48; extracted SPIR-V is 924 bytes, passes `spirv-val --target-env vulkan1.1`, and disassembles to a correct set0/binding0 StorageBuffer clear kernel. `vulkaninfo` proves the selected NVIDIA device has distinct compute and dedicated-transfer families. Concurrent buffer sharing alone did not create the required inter-queue memory dependency. At the third-cycle cap, the runtime now selects the compute-capable family for staging transfers so queue order supplies the dependency; this final synchronization correction is intentionally unverified until the next bounded pass. Do not accept v36/v37 as pixel evidence.
- cooperative-review: A second three-lane Spark review checked the staged queue fix, the complete diff, and the post-x86 BAR2 path. Both Vulkan reviewers independently required one shared queue mutex plus explicit TRANSFER_WRITE→COMPUTE_SHADER and COMPUTE_SHADER→TRANSFER_READ buffer barriers; queue wait-idle alone is not a memory dependency. The BAR2 guide reaffirmed one kernel-owned mapper and one shared probe, not three architecture syscall shims.
- implementation: Staging transfers now use the compute-capable queue family and its single queue mutex. Upload records a transfer-write to shader-read/write buffer barrier; download records a shader-write to transfer-read barrier. This removes dedicated-family ownership/semaphore complexity while preserving EXCLUSIVE buffers. Per the three-cycle guard this synchronization patch is staged but not rebuilt or claimed green. The next single validation must enable synchronization validation and require the exact histogram `bg=2112 rect=960 other=0`.
- verification: v38 rebuilt the single-queue/barrier runtime and ran clean with core plus synchronization validation, but exact audit remained `bg=0 rect=0 other=3072`; queue synchronization is not the last zero-frame cause. GDB proves both dispatches return success at every SFFI step, clear dispatch is 12x1x1, Vulkan receives a 64-byte push range with exact color/64/48 bytes, and raw downloaded bytes are already zero before Simple pixel conversion. The existing Rust upload/download round trip passes.
- verification: Added a direct real-Vulkan compute regression in `pipeline.rs`: rspirv emits a sparse set0/binding3 storage-buffer shader that writes `0xA5C37E19`; `ComputePipeline::execute` downloads the exact value. It passes after correcting the fixture to omit the StorageBuffer from the SPIR-V 1.3 entry-point interface (the vendored reflector aborts on that unsupported interface storage class). This proves shader execution, sparse descriptor discovery, queue submission, barriers, and staging readback work below the SFFI assembly path.
- implementation: The remaining structural difference is now removed: SFFI descriptor sets allocate from `ComputePipeline`'s actual descriptor-set layout instead of constructing a second nominally compatible layout and retaining it in a side registry. `DescriptorSet::new_from_layout` reuses the existing allocator with the pipeline-owned raw layout. Per the third-cycle cap this descriptor-ownership correction is staged but not rebuilt; next run exactly one v39 validation/histogram before any commit.
- verification: v39 rebuilt the pipeline-owned-layout path and still returned `bg=0 rect=0 other=3072` under synchronization validation, disproving descriptor layout identity as the zero-frame owner. The existing interpreter oracle could not execute because the interpreter registry lacks `rt_vulkan_copy_from_buffer`; all seven scenarios fail at semantic resolution, so no blob claim comes from that lane.
- verification: Added and passed a direct C-symbol SFFI regression using the generated sparse-binding constant-write shader: initialize runtime, compile SPIR-V, create pipeline/buffer/descriptor, bind/dispatch/end/submit, copy through a tagged packed byte array, and assert `0xA5C37E19`. This proves the full SFFI descriptor/dispatch/readback assembly works when no push constants are involved. Two compile-only corrections were needed for module privacy, then the focused test passed; the three-cycle cap is reached. The remaining discriminator is one equivalent SFFI constant-write shader whose stored value is loaded from a push-constant block. Do not retry v39 unchanged.
- verification: The generated sparse-binding push-constant SFFI regression passed exact `0xA5C37E19` readback. A stricter regression then parsed the repository's exact 924-byte `spirv_clear`, explicitly selected device 0, bound a 12,288-byte set0/binding0 framebuffer, pushed color/64/48 in the real 64-byte block, dispatched 12x1x1, and asserted all 3,072 pixels; it passes. This jointly exonerates the hand clear blob, scale/workgroup behavior, explicit device selection, and the complete public SFFI sequence.
- verification: Binary-level v39 GDB evidence matches creation and use identities: the first clear shader is handle 2, its pipeline creation returns handle 12 with push size 64, the first dispatch binds pipeline 12, and the next draw binds framebuffer handle 1 (matching receipt provenance). No source constructor swap exists. The zero frame is therefore not a wrong Engine field handle; the remaining discriminator must mirror the daemon's multi-pipeline/resource lifecycle or Simple wrapper/control-flow context. No v39 retry was used as pixel evidence.
- verification: The focused Rust SFFI regression now allocates the framebuffer first, compiles ten shader handles, creates ten pipeline handles, then dispatches the original clear pipeline over 64x48; all 3,072 pixels match exactly. Spark plus highest-model review found no first-request pipeline/shader/pool lifetime path capable of invalidating the clear pipeline. Multi-resource count/order is exonerated; the conditional `rt_vulkan_select_device` live-resource replacement hazard and descriptor-pool leak are real follow-up hardening issues but do not explain the first request.
- verification: Added a build-local native Simple facade discriminator that imports only the existing `sffi_vulkan` owner and exact `spirv_clear`, checks every dispatch result, and validates all 3,072 pixels. The self-hosted source check crashed while loading the heavy Vulkan graph (exit 139). Three lane-local native invocations produced binaries whose backtrace stops at a null `rt_vulkan_init`; `nm` proves the attempted builds retained unresolved `rt_vulkan_{init,select_device,compile_spirv,push_constants}` because the core-C archive was linked and the Vulkan linker wrapper did not substitute its archive. This is invalid build evidence, not a facade ABI failure. The next bounded pass must pass `--runtime-path build/simpleos_gpu_host/vulkan-runtime-target/release` (the proven daemon archive directory) and run the probe once; do not retry the three invalid linker forms.
- verification: Spark/source review found the current native selector accepts only `<runtime-path>/core-c-bootstrap[/deps]/libsimple_runtime.a`, not a root archive. A build-local symlink exposed the already-built Vulkan archive through that supported shape; no runtime copy or wrapper was added. The native Simple facade probe then linked at 8.36 MiB with real defined `rt_vulkan_{init,select_device,compile_spirv,push_constants}` symbols, ran without a crash, and printed `SIMPLE_VULKAN_FACADE_PROBE: FAIL` (exit 1). This is valid isolation evidence: the same exact shader/PC/buffer/dispatch succeeds through Rust direct C-symbol calls but fails when compiled from the existing Simple facade. The zero-frame owner is now confined to compiled Simple facade execution. At the three-cycle cap, next instrument the existing probe to report the first false stage and readback length/first pixel; do not rebuild the runtime or retry the daemon.
- runtime_need: Vulkan readback needs a stable mutable packed `[u8]` destination; generic Simple `[0u8; n]` currently exposes only a temporary TLS-packed read pointer.
- facade_checked: No public Simple packed-byte constructor exists; `sffi_vulkan.spl` is already the owner module for the raw Vulkan/array runtime boundary.
- chosen_path: add-smallest-owner-facade (`rt_byte_array_new_len` declaration used only by the existing Vulkan readback helper) plus fail-closed packed-destination validation in the runtime owner.
- rejected_shortcuts: Do not copy scratch bytes back in the daemon, add backend field pokes, accept zero pixels, or broaden this fix into compiler-wide `[u8; n]` repeat lowering without a separate language requirement.
- verification: Instrumented native facade probe reports handles `1,2,3,4,<cmd>`, every bind/push/dispatch/end/submit call `true`, and readback length exactly 12,288, yet the pixel oracle fails. GDB proves its tagged push array is length 64 with exact bytes `{0xA5C37E19,64,48}`. Spark disassembly found the first false boundary: `[0u8; byte_count]` lowers through `rt_array_repeat` to generic RuntimeValue slots; `rt_array_data_ptr_u8` returns a TLS scratch packed copy, so `rt_vulkan_copy_from_buffer` writes GPU bytes into disposable scratch and returns success while the Simple array remains zero.
- implementation: `vulkan_sffi_read_buffer_bytes` now rejects negative lengths and allocates with the existing packed `rt_byte_array_new_len` runtime constructor at the Vulkan SFFI owner. Runtime mutable decoding now accepts only real packed byte arrays, failing closed for generic arrays instead of reporting a successful scratch write. The focused representation test passes 1/1; the initial shortened filter matched zero and was corrected by listing and executing the full canonical test name directly. At the bounded-pass cap, rebuild the Vulkan archive and native facade probe next, then require all 3,072 probe pixels before rebuilding the daemon.
- verification: Rebuilt the Vulkan runtime archive and correctly linked native Simple facade probe. It reports real handles, all seven dispatch calls true, 12,288 readback bytes, and `SIMPLE_VULKAN_FACADE_PROBE: PASS` with all 3,072 pixels exact. Spark ABI review accepts the `i64 -> [u8]` packed constructor declaration and fail-closed runtime guard.
- verification: Fresh v40 daemon built from the filtered conflict-free source roots with the current hardlinked `src/lib` and rebuilt Vulkan archive. Under core plus synchronization validation it exits 0 with `simpleos_gpu_host request complete`; direct shared-memory pixel audit is exactly `bg=2112 rect=960 other=0`. This is the first accepted x86 host Vulkan render receipt and closes the zero-frame owner without CPU mirroring or weakened evidence.
- implementation: Extracted the existing owner-verified 64-bit PCI BAR sizing/assignment/mapping body into `_map_device_bar64`. Added one exported kernel facade `map_qemu_host_gpu_ivshmem_bar2()` that scans the existing PCI cache for exact `1af4:1110 class 05:00` and maps only BAR2; syscall 88 retains its caller-bound token checks and delegates to the same helper. No new syscall number or architecture shim was added.
- verification: The lane-local self-hosted checker crashes on the kernel import graph (exit 139); it was not retried. The Rust seed syntax diagnostic passes the modified `syscall_device.spl` as one file. Live x86/AArch64/RV64 mapping is still required before accepting AC-3/4/5/6/7. Next add the shared baremetal probe, real RV64 fence stubs, target routing, then x86 live before registering all three scenario names.
- implementation: Hardened the direct kernel mapper to scan only PCI bus 0 devices/functions instead of the global 256-bus cache; this avoids probing beyond ARM virt's low ECAM window under `highmem-ecam=off` while retaining exact ivshmem identity and multifunction handling.
- implementation: Added one shared baremetal host-GPU entry for x86_64/AArch64/RV64. It maps BAR2 through the kernel facade, submits an exact 64x48 Vulkan clear+rect request, requires device provenance and histogram `2112/960/0` plus correlated checksum, submits a second 256-word `fill_u32(7)` request, requires checksum `1792`, and emits architecture-correlated fail/pass markers. No CPU mirror or status-only pass exists.
- implementation: Added strong RV freestanding runtime definitions for all volatile widths plus `fence ir,iorw`, `fence iorw,ow`, and `fence iorw,iorw`; these replace generated weak/no-op stubs for ivshmem MMIO ordering.
- verification: Self-hosted source check of the shared entry crashes on the heavy import graph (exit 139) and was not retried. Rust-seed syntax diagnostic passes the entry. Cross-clang compiles the RV freestanding runtime for `riscv64-unknown-elf`; `llvm-nm` reports strong `T` symbols for `rt_volatile_{read,write}_u64` and all three barrier functions. Live target builds remain pending.
- cooperative-review: Three Spark lanes reviewed runner routing, boot-runtime discovery, scenario marker gates, and commit `ae6b`. Higher-model review accepted a single shared probe behind three two-line architecture entries; it rejected pointing the target directly at `arch/common` because native boot C discovery is relative to the entry directory. It also retained ARM `virt,highmem-ecam=off`, x86 `isa-debug-exit`, persistent-daemon sequencing, and exact success markers.
- implementation: Added the three architecture-owned probe targets and wrappers, canonical `SIMPLEOS_HOST_GPU_SHM` resolution, all three scenario registry/lookup routes, explicit scenario-to-target routing, x86 wrapper profile reuse, non-x86 minimal build environment, 60s scenario timeout, and architecture-correlated map/render/process/pass marker gates.
- verification: Rust-seed syntax diagnostics passed the shared entry, x86 wrapper, and QEMU runner module. Two bounded production x86 builds through the configured self-hosted compiler both crashed in about 1.2s before target lowering (`timeout: the monitored command dumped core`). Rust-debug diagnostics linked but exposed the wrapper import as unresolved because `examples.simple_os...` was searched below the entry directory; wrappers were corrected to the resolvable `arch.common...` path. The three-cycle cap is reached, so the corrected import has not been rebuilt and no live x86 guest PASS is claimed. Next session: run exactly one production x86 build, then the persistent v40 daemon + scenario if it succeeds.
- verification: The single corrected-import production build still crashed before target lowering. GDB identified the deployed self-hosted compiler owner: `io.cli_ops.env_set -> rt_env_set` passes `0x12` (the key length) as the value pointer, then libc `setenv/strlen` segfaults. The newer lane-local self-hosted CLI instead stops with its known `field access on nil receiver`. The concrete blocker is recorded in `doc/08_tracking/bug/self_hosted_native_build_rt_env_set_text_abi_2026-07-11.md`; no further build retry was made.
- cooperative-review: Three Spark lanes accepted the corrected `arch.common` wrapper resolution, scenario registration, marker gate, and persistent-daemon audit, then found the direct probe's kernel mapper used uninitialized generic x86 VMM state on every ISA. Higher-model review rejected initializing/switching a second VMM just for the probe and rejected weakening userspace `MapDeviceBar` to identity mapping.
- implementation: Kept userspace `_map_device_bar64` and its original assignment window on the capability-checked VMM path. A separate `direct_boot` window always relocates the exact QEMU ivshmem BAR2 for the kernel probe. x86 reuses crt0's existing `0xC000000000..0xC03fffffff` mapping and returns its `0xFFFFC00000000000` alias. ARM64/RV64 return the physical BAR only after verifying SCTLR.M is clear or SATP mode is Bare; other direct-boot ISAs fail closed. Rust-seed source check passes the modified owner file. Live proof remains pending behind the compiler blocker.
- cooperative-review: Rust-backend review initially classified the GDB trace as stale because Rust Cranelift/LLVM already expand `rt_env_set` text indices `[0,1]`. A separate Pure-Simple backend trace found the actual deployed owner: `cranelift_codegen_adapter.spl::cl_translate_call` emitted one machine value per text aggregate and declared only two `i64` runtime parameters. Higher-model source review confirmed text MIR operands are stack addresses to `{ptr,len}` and the external runtime branch never used its existing `type_is_text` helper.
- implementation: The Pure-Simple Cranelift external-call branch now expands `rt_env_set(text,text)` to four pointer/length scalars with an `i8` boolean return. The focused fixture verifies set/read/restore/read through scalar `rt_env_get_i64(text,i64)`, whose mixed text/scalar ABI is expanded separately; this avoids pretending the distinct `text` aggregate-return/sret gap is fixed. Imported Simple wrapper calls retain their internal aggregate-address convention. The runner must pre-seed `SIMPLE_NATIVE_IMPORTED_ENV_WRAPPER=17`, which the fixture restores before any failure return. Rust-seed source checks passed the initial fixture shape; native execution and final revised fixture syntax await the next bounded bootstrap/build pass.
- verification: Lane-local Stage 2 and self-hosted Stage 3 completed. Stage 3 native-built the two-file imported env-wrapper fixture with zero failures; with `SIMPLE_NATIVE_IMPORTED_ENV_WRAPPER=17`, the fixture exited 0 and reported `env_abi_exit=0 parent_value=absent`. This closes the `rt_env_set` four-scalar ABI blocker from `51f`; the separate `rt_env_get -> text` aggregate-return gap remains outside this proof.
- verification: Three bounded x86 target-build cycles passed the former `setenv`/`strlen` crash. The first reached only the inherited `src/app/io/cli_ops.spl` conflict; the conflict-free filtered-root build then dumped core, and GDB classified the repeat as SIGSEGV in `llvm::DataLayout::setAlignment` on a named parallel compile worker while other native-project workers were active. Three Spark reviews plus higher-model review classify this as an independent Rust LLVM worker/global-initialization lifecycle defect, not a regression in `51f`. The cap is reached: no serial retry was run. Next bounded discriminator is the exact build once in a fresh process/cache with `--threads 1`; a pass may produce the x86 artifact but is not accepted as a parallel compiler fix.
- verification: The fresh-cache `--threads 1` discriminator still dumped core after about eight minutes and produced no x86 ELF. A second serial run under GDB additionally forced `RAYON_NUM_THREADS=1` and `SIMPLE_BOOTSTRAP_THREADS=1`; exactly one Rayon worker and one active `compile-__init_` thread reproduced `LLVMModuleCreateWithNameInContext -> llvm::Module::Module -> DataLayout::reset/setAlignment`. This falsifies the parallel-only initialization hypothesis and rejects the proposed `OnceLock` fix for this crash. The stale `app.ffi_gen.intern_codegen` discovery warning is unreachable from the target roots and incidental. No source patch or QEMU run was attempted. Next inspect deterministic worker-local LLVM context/module-construction corruption with sanitizer or a focused reproducer.
- implementation: Higher-model call-chain review found why the explicitly requested Cranelift build entered LLVM: Stage 3 uses `native_all::rt_native_build`, whose adapter parsed `--backend cranelift` but preserved a stale `SIMPLE_BACKEND=llvm`; native object dispatch reads only that environment value. The Rust driver already overwrites it for every explicit backend. Native-all now matches that behavior with one unconditional assignment. `cargo check --manifest-path src/compiler_rust/native_all/Cargo.toml --lib` passes; only two unrelated existing unused-assignment warnings remain. This removes the target misrouting but does not claim to fix the latent LLVM module-construction SIGSEGV. At the bounded cap, next bootstrap Stage 3, run the tiny inherited-LLVM/explicit-Cranelift discriminator, then retry x86 once if green.
- implementation: Extended the existing opt-in `SIMPLE_NATIVE_BUILD_RUST_TRACE` with one worker-side `backend=<name> file=<path>` line so backend override evidence cannot false-pass on parser output or cache hits. Normal builds remain silent. An isolated LLVM-feature native-all archive build exposed the unrelated current-tree `MakeTraitObject` non-exhaustive LLVM match and was not patched; the required Cranelift-only archive then built successfully without touching canonical bootstrap artifacts.
- verification: Fresh isolated Stage 2 built Stage 3 while inheriting `SIMPLE_BACKEND=llvm` and explicitly requesting Cranelift. The trace proves one uncached `bootstrap_main.spl` worker selected `backend=cranelift`, followed by `Build complete: 1 compiled, 0 cached, 0 failed`; Stage 3 hash is `704db6705fe392c2916d68bdeace9990d6b6d5fb98a0d190f432ad7613cec343`. This closes the backend-override regression itself. Stage 3 is not accepted as a driver: a focused invocation reports `raw args=[]`, defaults to 10,853 full-CLI files, and times out at 300 seconds with no requested artifact. The separate defect is recorded in `doc/08_tracking/bug/self_hosted_stage3_native_build_empty_argv_2026-07-12.md`. Three Spark lanes plus higher-model review accept fresh Stage 2 as the next bounded x86 builder because it demonstrably compiled Stage 3; they forbid claims of Stage 3/bootstrap convergence. The slice reached its three-cycle cap, so no x86 build was started. Next run the one-shot fresh-cache x86 build through Stage 2 with inherited LLVM, explicit Cranelift, and worker trace, then proceed to the persistent-daemon QEMU audit only if the canonical ELF passes freshness checks.
- verification: The one-shot Stage 2 x86 build proves backend routing with 244 uncached Cranelift workers and zero LLVM workers, but produces no ELF. Entry-closure compiled `arch/arm64/cpu.spl` and `arch/riscv64/cpu.spl`; their valid `wfi` and `sfence.vma zero, zero` blocks contaminated the x86 aggregate C sidecar and failed assembly. The parser had discarded `@cfg` attributes from `UseStmt`/`MultiUse`, so discovery followed inactive imports from `syscall_device.spl`. Import nodes now retain attributes and shared cfg stripping runs before dependency extraction. The focused parser regression passes and `cargo check -p simple-compiler --lib` passes. The native-project unit binary remains blocked by seven unrelated stale `MethodCallVirtual` fixtures. This root fix is recorded in `native_entry_closure_cfg_import_arch_asm_leak_2026-07-12.md`.
- blocker: The same build skipped `arch/x86_64/paging.spl`, `memory/vmm.spl`, and seven `arch_adapt/x86_64/paging.spl` bodies because stale `VmFlags` property syntax targets fields that no longer exist. Stub fallback is forbidden; the owner fix is recorded in `simpleos_x86_vmflags_property_codegen_2026-07-12.md`. The three-cycle cap is reached. Next update those two concrete files to explicit accessors/bit preservation, rebuild the isolated Stage 2 with both fixes, run the focused cfg-import gate, and attempt one fresh x86 build. QEMU remains deferred until a zero-skip canonical ELF exists.
- implementation: Replaced stale x86 `VmFlags` property reads with the existing accessor methods in `arch/x86_64/paging.spl`. Both concrete W^X paths now preserve the full bitfield and set `VM_NO_EXECUTE`; obsolete boolean-field constructors were deleted. No adapter or stub fallback changed. Both focused Simple source checks pass. Rebuilt the isolated Cranelift-only native-all archive, Rust seed, and Stage 2 with the cfg-import and paging fixes; fresh Stage 2 SHA-256 is `1f784c159f755efdc125e9339f26b1eab839ff4ff67f4a97771c6be34bb26049`. The canonical x86 zero-skip build and QEMU audit remain pending after the user-requested GitHub sync.
