# Feature: SimpleOS QEMU Host-GPU 2D Hardening

## Raw Request
`$sp_dev harden simple 2d on qemue, specially x86,aarch,riscv. research simple os on qemu can use host gpu? metal,cuda,vulkan for process. rendering metal,directx,vulkan. make simple os qemu to use underlying gpu processing/rednering.`

## Task Type
feature

## Refined Goal
Make SimpleOS use a real QEMU-exposed host-accelerated GPU path for Simple 2D rendering and portable processing on x86_64, AArch64, and RISC-V where the host/QEMU stack supports it, with explicit capability detection, safe software fallback, and evidence that distinguishes device-backed execution from emulation or CPU rendering.

## Acceptance Criteria
- AC-1: Research documents identify the actual QEMU device, guest-driver, and host-renderer/compute paths available for Linux Vulkan/CUDA, macOS Metal, and Windows DirectX/Vulkan hosts, and explicitly classify unsupported combinations instead of claiming backend parity from API names alone.
- AC-2: Final selected requirements define pass/fail behavior for x86_64, AArch64, and RISC-V QEMU guests, including host prerequisites, capability negotiation, fallback behavior, rendering correctness, processing correctness, latency/RSS targets, and device-backed evidence.
- AC-3: Each target architecture boots the applicable SimpleOS QEMU image, probes the exposed GPU capability, and reports a stable backend/reason pair; an unavailable accelerated path selects the existing software/CPU fallback without boot failure or silent false-positive acceleration.
- AC-4: On every host/QEMU combination classified as supported, Simple 2D renders the canonical fixture through a guest-visible accelerated device, reads back the produced frame, and matches the CPU oracle using the repository's exact comparison contract.
- AC-5: Portable processing runs the same ProcessingIR fixture through the supported device-backed path and CPU oracle with matching results; CUDA, Vulkan, and CPU remain backend choices below ProcessingIR rather than separate public APIs.
- AC-6: Evidence proves real guest-to-QEMU-to-host GPU execution using negotiated virtio-gpu/virgl/venus/rutabaga or another researched supported device path plus host-side backend markers; screenshots, configured command-line flags, synthetic handles, and CPU mirrors alone fail the gate.
- AC-7: One canonical SPipe wrapper and scenario matrix cover x86_64, AArch64, and RISC-V, publish per-row `pass`, `unsupported`, `blocked`, or `fail` status with reasons, and feed `scripts/check/check-simpleos-hardening-evidence-matrix.shs` without weakening its existing 26/26 completion contract.
- AC-8: Focused tests cover capability negotiation, unsupported-device fallback, frame readback parity, processing parity, and fail-closed evidence parsing; executable specs live under `test/` and their generated operator-readable manuals live under `doc/06_spec/` as Markdown.
- AC-9: Final verification runs each acceptance check at most once after convergence, reports architecture/host limitations honestly, passes the direct runtime/env guards and generated-spec layout guard, and refreshes all changed architecture, plan, guide, report, SPipe-manual, skill/agent/command, and feature-tracking artifacts before any completion claim.

## Scope Exclusions
- Direct PCIe GPU passthrough/VFIO setup is excluded unless research shows it is required for a selected supported row; mediated/paravirtual QEMU GPU access is the default scope.
- A new public rendering or compute API is excluded; reuse Engine2D backend lanes and ProcessingIR.
- Unsupported host/guest combinations are not to be disguised with software rendering, API translation labels, or synthetic evidence.

## Cooperative Review
- Sidecar `qemu-gpu-domain`: QEMU/virtio-gpu/virgl/venus/rutabaga and Linux/macOS/Windows host-backend feasibility, including authoritative external sources.
- Sidecar `simpleos-gpu-local`: existing SimpleOS virtio-gpu, Engine2D, ProcessingIR, QEMU boot, and evidence-wrapper inventory; no edits.
- Sidecar `multiarch-evidence`: x86_64/AArch64/RISC-V boot and current hardening-matrix coverage/gaps; no edits.
- Merge owner: primary Codex `/root`.
- Final reviewer: primary normal/highest-capability Codex after sidecar evidence is merged; it owns requirement-option quality, exclusions, and generated-manual acceptance.
- Shared interfaces: existing `Engine2D` backend lane and `ProcessingIR`; capability/result records must use one shared guest-visible backend/reason contract rather than architecture-specific public APIs.
- Manual flow helpers: `step("Probe the QEMU guest GPU capability")`, `step("Render and read back the Simple 2D parity fixture")`, `step("Run the ProcessingIR parity fixture")`, and `step("Report device-backed host acceleration evidence")`.
- Setup/checker helpers: reuse `scripts/check/check-simpleos-hardening-evidence-matrix.shs`; the lane-owned focused wrapper name is `scripts/check/check-simpleos-qemu-host-gpu-2d.shs` only if no existing wrapper already covers all rows.
- Fail-fast placeholders: temporary scenario/helper bodies must use `fail("simpleos-qemu-host-gpu-2d not implemented")` and may not use passing stubs or synthetic acceleration markers.
- Generated-manual review owner: primary Codex `/root`.

## Phase
design-implementation-in-progress

## Log
- dev: Created state file with 9 acceptance criteria (type: feature); implementation and verification are intentionally deferred to subsequent SPipe phases.
- research: Restored current local/domain research after the earlier shared-checkout artifacts disappeared; current source still proves CPU raster plus VirtIO-GPU 2D scanout only.
- requirements: User selected Feature B (cross-host QEMU GPU service) and NFR B (balanced desktop target); finalized `doc/02_requirements/feature/simpleos_qemu_host_gpu_2d.md` and `doc/02_requirements/nfr/simpleos_qemu_host_gpu_2d.md`.
- design: Defined one bounded architecture-neutral VirtIO-channel request/receipt capsule, x86_64 Linux/Vulkan first slice, unchanged AArch64/RISC-V adapters, fail-closed evidence rules, test plan, agent lanes, executable fail-fast scenarios, and operator manual. Shared memory remains deferred until measured copy cost violates an NFR.
- design-docgen: Current deployed `bin/simple` is the Rust bootstrap seed and `spipe-docgen` stops at the pre-existing `rt_cli_arg_count` link gap; the manual is maintained explicitly until the pure-Simple tool is deployed. No generated-manual PASS is claimed.
- implementation-audit: x86_64 has real Vulkan MAP/HELLO/RENDER receipts; ARM64 and RV64 guest paths need no source workaround but require a correctly deployed self-host compiler before fresh ELFs can be trusted. The existing ARM64 ELF contains x86-selected cfg values.
- runtime_need: the freestanding ivshmem bridge requires a cross-ISA publication/receipt memory fence.
- facade_checked: hosted `app.io.volatile_ops` is not a safe SimpleOS baremetal dependency; the existing MMIO owner currently lacks a fence facade.
- chosen_path: `add-smallest-owner-facade` is tracked as TODO 534; the required direct fence remains temporarily at the provider boundary.
- rejected_shortcuts: no feature-local `rt_*` shim, architecture-specific command protocol, synthetic receipt, CPU-mirror acceleration claim, or fixed-address/hardcoded linker workaround.
- bootstrap-verification: the full-bootstrap run proved Stage 2 uses the interpreted pure-Simple LLVM linker, not Rust `NativeProjectBuilder`. That linker suppressed all normal runtime objects under `SIMPLE_BOOTSTRAP`, causing unresolved `rt_text_eq_any` and `__simple_intrinsic_bounds_check`. The suppression and generated partial C support stub are removed; fresh bootstrap verification is deferred to the next bounded cycle.
- bootstrap-verification-2026-07-12: Stage 2 and Stage 3 now pass with the pure-Simple compiler. Stage 4 correctly uses Stage 2 rather than the seed, then fails on a separate full-CLI imported-call qualification plus hosted-runtime selection gap filed in the bootstrap bug DB and TODO 535. No deployment PASS is claimed.
- multiarch-build-2026-07-12: the verified Stage 2 compiler reaches zero unexpected ARM64 freestanding symbols, then exposes an LLD `--defsym` argv-quoting bug when the workspace path contains hyphens. Direct LLD quoting works, but quotes added in Simple source are stripped before expression parsing even after a fresh-cache Stage 2/3 rebuild; the ineffective source change was reverted and TODO 536 records the owner fix. RV64 additionally requires separating test-array MMIO helpers from its baremetal closure. No multiarch receipt PASS is claimed.
- multiarch-entry-followup: the failed ARM command mixed overlay source roots with the original-tree entry path, forcing an absolute external module symbol containing workspace hyphens. The next bounded build must use the entry inside the overlay root; no quote, rename, workspace move, or hardcoded alias change is retained.
- rv64-closure-followup: copying a focused overlay is rejected because it duplicates the canonical entry-closure resolver. The real remaining boundary is allocating MMIO test support mixed into the production module; TODO 537 tracks separation, with SBI `unsafe`/inline-asm lowering as a conditional compiler-owner follow-up. No `rt_*` shim is accepted.
- arm64-link-followup: `strace` proves freestanding targets use Rust `NativeProjectBuilder`, not the pure-Simple ARM linker. The canonical original roots plus original entry let boot autodiscovery include ARM CRT/runtime and now build a zero-unexpected-symbol 267 KiB ELF directly. The focused overlay omitted `boot/` and is rejected; the unused Simple-linker edit/TODO 538 were removed.
- arm64-qemu-followup: explicit serial routing proves the ELF boots through CRT/runtime/PCI and reaches `spl_start` with and without ivshmem. The device appears at bus-0 `00:02.0` as `1af4:1110`; QEMU exposes raw 64-bit prefetchable 8 MiB BAR2, but the guest returns conflated mapper code `-19`. TODO 539 now tracks PCI config probe/programming and reason separation. `highmem=off` faults early and is not retained. No GPU receipt PASS is claimed.
- arm64-discovery-followup: refreshed compiler builds prove the direct ARM ELF path and repeatedly return distinct `-2`, while the ARM C scanner and QEMU independently see `1af4:1110`. Removing class/subclass from the matcher does not change the result and was reverted; the remaining mismatch is in the earlier Simple ECAM read/enumeration path. Fresh raw guest-observed config evidence is required.
- arm64-ecam-root-fix: AArch64 ELF disassembly proved native cfg-global resolution selected the later RISC-V ECAM value `0x30000000`. The PCI owner now uses target-gated functions, the rebuilt ELF contains `0x4010000000`, and fresh QEMU evidence reaches `HOST_GPU_MAP_OK isa=aarch64 device=2`. The host daemon then exits with signal 139, so HELLO/RENDER/ProcessingIR remain unproved. Compiler cfg-global repair is tracked as TODO 540.
- rv64-mmio-closure: production PCI/host-GPU consumers now use a raw MMIO owner, leaving allocating injection state in the test facade. The canonical RV64 link no longer reports dynamic array/test-storage support; it now reaches pre-existing freestanding runtime selection plus SBI `unsafe` lowering gaps. No local runtime shim or copied overlay is accepted.
- runtime-boundary-followup: existing `rt_mmio_*` declarations moved into `os.kernel.boot.mmio_hardware`; no new runtime capability was added. The separate publication fence remains tracked by TODO 534 rather than being expanded into this change.
- host-daemon-closure-followup: current `simpleos_gpu_host` startup was blocked by two owner defects, not Vulkan/QEMU: importing broad `app.io.cli_ops.get_args` pulled compiler modules into the daemon closure, and native runtime-path selection rejected the Vulkan archive for optional lifecycle/scalar-argv symbols. The daemon now uses the narrow aggregate-argv owner `app.io.args_ops`, matching the archive's required `rt_cli_get_args` ABI without scalar argv calls; the selector's existing optional-lifecycle test passes `1/1`. Existing Stage2 remains stale until TODO 541's fresh bootstrap deployment, so no new ARM HELLO/RENDER receipt is claimed.
- runtime_need: small native host services need argv without importing the compiler-facing CLI closure.
- facade_checked: `std.io_runtime.get_args` requires optional scalar argv symbols; `app.io.cli_ops.get_args` exports the compiler command graph; neither matches the Vulkan archive's aggregate argv ABI and small closure.
- chosen_path: `add-smallest-owner-facade` in `app.io.args_ops`, using the already-required aggregate `rt_cli_get_args` host capability with `sys_get_args` fallback.
- rejected_shortcuts: no daemon-local extern, argv hardcoding, historical binary substitution, generated unresolved stub, or Rust-seed compilation evidence.
- host-daemon-strict-link: three bounded bootstrap cycles converged: Stage2/Stage3 pass, versioned glibc termios calls remain system-owned, and the current 37-module daemon links under `SIMPLE_NO_STUB_FALLBACK=1` with zero undefined `rt_*` symbols. Its one-shot empty-region probe reaches the selected runtime and exits 3 because `rt_mmap` is a hardcoded zero placeholder. TODO 543 owns the real file-backed mapping implementation; no fourth bootstrap cycle or QEMU receipt is attempted.
- vulkan-array-abi-followup: the mmap owner and strict daemon now work. The Vulkan runtime decoded Simple `[u8]` arguments as raw pointers; the shared byte-array owner now bounds shader, upload, readback, and push-constant payloads, removes the unsafe 64 MiB SPIR-V scan, and makes captured ARM HELLO replay pass with render mask 1 and processing mask 0. Three bounded live ARM runs then exposed `commands.len() = 0x4000000002`, proving a separate 32-bit len/cap versus 64-bit compiler RuntimeArray header mismatch; no fourth QEMU run is attempted this cycle.
- arm-array-and-vulkan-dual-storage-followup: ARM64 now uses the compiler's 64-bit len/cap/backing-pointer array header and raw `rt_array_get` index ABI. Live QEMU publishes the correct 12-word render payload and reaches a device receipt, then honestly rejects an all-zero readback. GDB proved Vulkan push constants were a valid generic tagged-byte array, not byte-packed storage; the shared runtime owner now decodes and writes both native `[u8]` representations. Its focused test and Vulkan-feature build pass, and the strict daemon links without stubs. The three-run QEMU cap is reached, so the final pixel receipt is deferred to the next bounded cycle.
- vulkan-storage-transfer-owner-followup: `VulkanBuffer` now enforces `TRANSFER_SRC|TRANSFER_DST` for every device-local storage buffer because its public upload/download paths always stage copies. A focused no-device Vulkan test passes and the Vulkan runtime archive rebuilds. The post-sync strict daemon relink now fails closed on unrelated missing closure/runtime symbols (`FONT_CHARSET` methods, Engine2D SIMD hooks, `rt_vulkan_api_version`, and `str.substring`); no stub or hardcoded workaround was accepted, so the final ARM pixel receipt remains open.
- rv64-live-host-gpu-followup: the RV64 freestanding runtime now owns the same full publication fence as x86_64/ARM64 (`fence rw, rw`). The canonical 237 KiB RV64 probe links with the real runtime and a live QEMU/OpenSBI run reaches `HOST_GPU_MAP_OK isa=riscv64 device=1`, `HOST_GPU_HELLO_OK` with render mask 1 and processing mask 0, and publishes the two-command render request. Readback fails honestly because the fixed Vulkan runtime has not yet been relinked. Object inspection proves the current LLVM build emits one monolithic `.text` per module and retains dead backend symbols, while the earlier working cache uses Cranelift per-function `.text.subsection` sections; the next bounded cycle must rebuild strictly with `--backend cranelift` and the fixed Vulkan archive. No ProcessingIR or render PASS is claimed.
- vulkan-layout-and-multiarch-render-followup: a strict Cranelift daemon now links from current sources with zero stubs. Validation replay proved `spirv-reflect` returned no descriptor bindings for the hand-assembled kernels, creating an incompatible zero-binding pipeline layout; the compute-pipeline owner now applies the SFFI contract's binding-0 storage fallback. ARM64 live QEMU device readback passes exactly (`checksum=405589360`, background 2112, rectangle 960, other 0). RV64 receives the same device checksum and exact framebuffer bytes, but its guest `u32 == u32` color comparisons report background 0, rectangle 0, other 3072; this is filed as a compiler-owner blocker rather than hidden with probe casts. ProcessingIR remains honestly unavailable.
- rv64-mmio-width-followup: disassembly proved Rust boot autodiscovery linked `examples/09_embedded/simple_os/arch/riscv64/boot/baremetal_stubs.c`, whose `uint32_t` return followed the RV64 ABI's sign-extension rule (`lw`) while the generic Simple extern call compared a 64-bit value. The boot owner now returns zero-extended `uint64_t`; the rebuilt ELF uses `lwu`, and live RV64 QEMU reports the exact device oracle (`checksum=405589360`, background 2112, rectangle 960, other 0). No probe cast or compiler overlap with the active value-semantics lane was accepted. ProcessingIR remains honestly unavailable.
- processing-ir-runtime-decision: `runtime_need` is none; `facade_checked` found the existing typed Vulkan SFFI and checked-in validated fill kernel; `chosen_path` is `reuse-facade`; `rejected_shortcuts` remain direct `rt_*`, Engine2D-clear relabeling, backend-specific public APIs, CPU-mirror provenance, and hardcoded fixture output.
- processing-ir-fill-u32-followup: the first real runtime-neutral ProcessingIR value now validates bounded `FillU32`, executes an independent CPU oracle, and routes through a dedicated Vulkan SFFI executor rather than Engine2D. A strict 43-module Cranelift daemon links with zero stubs. Fresh x86_64, AArch64, and RV64 QEMU probes all negotiate processing mask 1 and report device-backed `HOST_GPU_PROCESS_OK checksum=1792 mismatches=0` after exact render parity. The canonical wrapper and Metal/DirectX/CUDA host rows remain open, so the goal is not complete.
- canonical-wrapper-followup: `scripts/check/check-simpleos-qemu-host-gpu-2d.shs` now owns the three-ISA Linux/Vulkan run, exact correlated receipt parser, explicit non-Linux classifications, cached-artifact mode, and fail-closed self-test. Named `_QemuRunner` scenarios own fresh guest builds. Current-source x86_64 and AArch64 builds pass; the pure-Simple RV64 scenario exposes TODO 537's unresolved freestanding runtime/SBI symbols, so no Rust-seed fallback was accepted. One cached-artifact wrapper run produced exact PASS logs for all three ISAs; Metal/DirectX/CUDA remain unsupported and the goal remains active.
- cuda-processing-followup: the host executor now selects native CUDA for ProcessingIR when negotiated and preserves Vulkan as a real device-backed fallback; native CUDA FillU32 passed with independent CPU-oracle parity. Existing three-ISA Vulkan receipts remain cached evidence; refreshed CUDA-tagged guest receipts and non-Linux native rows remain open.
- draw-ir-wire-runtime-decision: `runtime_need` is none for the canonical codec and correlated receipt fields; the guest reuses the MMIO owner, and TODO 534 is closed by moving the publication fence behind that facade. `chosen_path` is a bounded canonical plain-RECT DrawIR subset with fail-closed decode, exact re-encode/checksum, host Engine2D execution, and device-readback provenance. TEXT is rejected because the current Vulkan path CPU-rasterizes glyphs before upload; extended command fields are rejected because the current SDN skin does not preserve them. Rejected shortcuts are backend-specific DrawIR, direct leaf `rt_*`, GROUP-only production content omission, CPU mirrors labeled accelerated, and synthetic device receipts.
- production-draw-ir-followup: high-model and Spark audits identified the pre-refactor insertion seam as `DesktopShell.render_baremetal_frame_and_blit`; the source path now routes through `render_baremetal_frame` and `Engine2dWmFrameExecutor`. Web/GUI content arrives as IMAGE pixels while the first wire subset intentionally rejects IMAGE. TODO 549 records bounded image/resource attachments; production compositor offload is not claimed until the same complete composition can run through host Engine2D or the existing local fallback.
- provenance-hardening-followup: completion correlation is snapshotted under the request acquire fence and validated centrally by the guest bridge. Vulkan render and DrawIR receipts now use a stable hash of the live device name plus driver identity rather than reusing a transient buffer handle. TODO 550 tracks the pre-existing Vulkan ProcessingIR result contract, which still lacks its own stable identity field.
- bounded-check-2026-07-13: the deployed pure-Simple binary exited 139 immediately for both focused unit specs and for checks of the codec, daemon, and guest bridge. Each command was attempted once; no retry, Rust-seed fallback, or PASS claim is accepted. TODO 548 now records the crash form in addition to the earlier progress-stall form.
- production-local-draw-ir-runtime-decision: `runtime_need` is none; `facade_checked` found the existing `DrawIrComposition`, `Engine2dResolvedDrawIrImage`, and Engine2D presentation path; `chosen_path` is `reuse-facade`. No new `rt_*`, backend poke, cache, full-frame copy, or backend-specific public API was added.
- production-local-draw-ir-followup: the source path adds `Engine2dWmFrameExecutor` at the x86 production shell seam. `DesktopShell` no longer builds a raw full-frame pixel buffer and `memcpy`s it to scanout; it submits one canonical composition with unique, revision/dimension/pixel-count/checksum-valid top-level IMAGE resources. Bad, duplicate, unresolved, unreferenced, or nested content fails closed. Core `draw_ir_adv.spl` is separated from host runtime-queue ownership in `draw_ir_runtime_adv.spl`, so this baremetal closure does not import host-queue `rt_*`. The host wire remains plain RECT, and the compiler check is blocked below, so TODO 549 and production host offload remain open.
- cooperative-review-followup: Spark audits traced the existing resolved-IMAGE path and confirmed the stale x86 entry/signature mismatch. Merge owner is Codex primary; high-capability static review and generated/manual review remain required before acceptance.
- production-entry-check-2026-07-13: the isolated worktree has no deployed `bin/simple`; the shared `bin/release/x86_64-unknown-linux-gnu/simple` identifies itself as a Rust bootstrap seed and exits 1 on `unknown extern function: rt_cli_arg_count`. No retry, seed fallback, compile PASS, or QEMU claim is accepted; TODO 548 remains open.
- vulkan-processing-identity-followup: the Vulkan ProcessingIR result source now hashes the runtime-selected driver identity (device name plus vendor/device/driver/API metadata) through the GC-async Vulkan facade, and HELLO advertises Vulkan processing only after a real one-element ProcessingIR probe returns a positive handle and stable identity. The host daemon no longer substitutes a transient storage-buffer handle for identity. A pure deterministic identity spec is added, but the crashed/stale deployed compiler still blocks execution; TODO 550 remains open until that focused spec and a fresh live receipt pass.
- pure-simple-runner-followup: the QEMU runner now rejects the bootstrap seed by its own version marker and no longer lists Rust compiler artifacts as target candidates. The deployed release artifact still reports itself as a Rust bootstrap seed, so TODO 548 remains open until a fresh pure-Simple compiler is deployed and the focused checks run once.
- lossless-draw-ir-sdn-followup: the existing Draw IR SDN owner now round-trips all typed command metadata, including styled rects, resolved-font text, image URI, hierarchy, path points, and edge material. The host-GPU codec admits the production RECT/TEXT/IMAGE semantic subset while nested GROUP remains fail-closed. TODO 549 remains open for bounded IMAGE pixel resources, session wiring, and device-native image execution.
- bounded-image-resource-wire-followup: the shared codec now transports unique top-level IMAGE pixels as canonical little-endian records with bounded URI/dimensions/counts, aggregate and per-pixel checksums, zero padding, exact UTF-8 roundtrip, and two-pass validation before pixel expansion. The guest writes resources before the release fence; the daemon snapshots them before execution and rechecks request generation before every shared-arena output overwrite. Spark implemented the codec/tests and a high-capability agent reviewed the integrated guest/host path. No compiler, QEMU, or device-native IMAGE PASS is claimed while TODO 548 blocks fresh execution; TODO 549 remains open for production session wiring, clipped nested content, and native Vulkan image execution/provenance.
- native-vulkan-image-source-followup: the existing validated `spirv_blit`, two-buffer descriptor helper, and push-constant ABI now own exact-size opaque IMAGE execution for fresh host device compositions. Eligibility is bounded by exact pixels, framebuffer/clip containment, and no mask; scaled/transparent/general embedded images remain on the existing CPU/offscreen path and set sticky non-device provenance. Fenced tri-state submission prevents CPU replay or dependency release when completion is unknown; TODO 551 tracks runtime-owned dependent-resource quarantine. Spark implemented the two scoped lanes and high-capability review rejected unsafe intermediate forms before the merge owner hardened ownership and clip/alpha boundaries. TODO 548 still blocks fresh compiler/Vulkan/QEMU evidence, and TODO 549 remains open for production guest session/presentation wiring.
- cached-report-validation-followup: `scripts/check/check-simpleos-qemu-host-gpu-2d.shs --validate-report` now rejects status-only or duplicate/malformed cached evidence, validates all nine host/ISA rows and backend classifications, and permits Linux `pass` only with three existing serial logs that satisfy the same correlated render/Draw IR/ProcessingIR receipt parser as a live run. The hardening matrix delegates to that owner for both live and cached reports without changing its 26-row contract. Manual steps are `step("Validate the cached host-GPU report")` and `step("Report malformed evidence without promoting it")`; Spark review and higher-capability/manual admission review PASS, with merge owner `/root`. The canonical guide, plan, and manuals were refreshed; broader pipeline skill/command docs are N/A because their workflow and wrapper references did not change.
- shared-vulkan-wm-surface-followup: the existing Draw IR embedded-batch owner now creates a transparent per-window Vulkan framebuffer by retaining the parent `VulkanSession`, requires checked child device readback, applies canonical `opacity_milli` through the checked parent src-over dispatch, and always shuts the child down. Fresh-device preflight admits only bounded named surfaces, canonical metadata-only RECT styles (with one nonzero-alpha initializer on a fresh transparent child), exact IMAGE, and resolved TEXT; later translucent/effect RECTs and software/unknown-completion children remain ineligible. Vulkan translucent filled RECTs reuse checked native src-over while opaque RECTs retain the direct kernel. One integration scenario projects the real unfocused WM window plus content IMAGE at offset `(10,36)` and opacity `930`, proves parent/child session identity and balanced retain/release, discriminates checked alpha fill, and requires exact CPU/Vulkan parity plus final device provenance. Spark ownership/production reviews and higher-capability/manual admission review PASS; merge owner is `/root`. TODO 554 records the pre-existing clipped/overwritten shadow geometry; TODO 548 still blocks compiler execution and fresh QEMU receipts; production guest session selection and p95 evidence remain open.
- production-host-selection-followup: the x86 production entry maps the full ivshmem region into the active VMM through the existing PCI owner and rolls partial mappings back on failure. `Engine2dWmFrameExecutor` builds one AUTO composition, derives every generation from an idle wire slot so shell value copies cannot stale session state, reuses the bounded Draw IR/image bridge, requires the centralized device receipt, and synchronously presents checksum-validated MMIO output before returning success. Missing capacity, mapping, negotiation, receipt, or presentation falls through to the existing local Engine2D call. The default 3840x2160 frame remains local under TODO 552; TODO 548 still blocks compiler execution and fresh production QEMU evidence. No new direct `rt_*` or parallel GUI/Web/2D path was added.
- native-metal-draw-ir-followup: strict native Metal creation now initializes fresh parent/child framebuffers transparently on-device, retains one shared `MetalSession` for embedded WM surfaces, and applies canonical opacity through a checked MSL src-over pipeline. Every admitted clear/primitive/blit/composite requires encode-through-wait success; known paths remove runtime encoder/command handles and unknown completion quarantines all dependencies under TODO 555. The daemon accepts Metal Draw IR only with exact backend naming, checked device readback, positive framebuffer handle, and stable device-name/memory identity; raw render remains Vulkan-only, and DirectX software emulation remains rejected. Spark implemented and reviewed the bounded owner lanes; higher review forced the runtime cleanup facade. TODO 548 blocks compiler execution and a prepared macOS host is still required for a fresh receipt.
- native-metal-processing-ir-followup: ProcessingIR now has a dedicated Metal FillU32 executor rather than relabeling Engine2D clear. It compiles one bounded MSL kernel, binds output/dummy/params through the synchronous Metal compute owner, requires terminal completed/no-error command status, uses the canonical pointer readback facade, and returns exact values with separate positive handle and metadata identity. The daemon probes a real nonzero operation before negotiating the Metal processing bit and still checks the independent CPU oracle. Spark split runtime-owner and executor lanes; TODO 548 blocks Simple execution and a prepared macOS host is required before claiming a receipt.
- compiler-recovery-function-lookahead-2026-07-13: the Rust seed recovery pass falsely treated every valid Simple identifier named `function` as a TypeScript declaration. Detection now requires identifier lookahead, normal parser advancement buffers that lookahead without changing token order, and the pure-Simple recovery API/spec uses the same rule. Four focused Rust parser tests and higher-capability review pass. The final allowed canonical Stage-4 attempt then exceeded ten minutes at one CPU with zero cache objects, output, or compiler phase markers, versus a retained 229.2-second full-build reference. It was stopped under the three-cycle cap. Follow-up found the canonical seed branch used `run native_build_main.spl` without the explicit host target, so it could never reach the Rust bootstrap handler that retained evidence proves completes in about four minutes; the branch now calls `native-build --target ${PLATFORM}` directly and an integration source-contract check locks that seam in. Fresh instrumented full-CLI verification remains open under `doc/08_tracking/bug/native_build_stage4_pre_object_spin_2026-07-13.md` and TODO 548. No compiler, focused host-GPU check, or QEMU PASS is claimed.
- stage4-strict-link-followup-2026-07-13: direct detected-target seed dispatch now discovers 1,043 modules; a bounded four-thread run compiled all 1,043 real objects with zero compile failures and preserved the cache. Strict link fails honestly on 120 unique undefined symbols (102 runtime/provider owners and 18 non-runtime Simple closure/import owners), none supplied by the automatically selected native-all or adjacent runtime archive. An explicit `rust-hosted` hypothesis was rejected by policy and removed; it is not a valid fix. TODO 535 owns the two real artifact/closure fixes. The three-cycle cap is reached, so no fourth build, focused host-GPU check, QEMU run, or PASS is claimed.
- stage4-check-import-owner-followup-2026-07-13: the native import-map builder no longer discards every source path containing `check.spl`; that blanket rule suppressed the real CLI check and arch-check owners after discovery. A focused production-filename regression proves `run_check` resolves to its qualified owner. Sidecar audits classify the remaining non-runtime failures as package/local import closure plus three lowering defects, and the runtime group as incomplete fresh-C/native-all ownership. The strict full-CLI link was not rerun, so no remaining-count, compiler, host-GPU, or QEMU PASS is claimed.
- stage4-bare-package-owner-followup-2026-07-13: native entry closure now discovers bare facade exports through sorted cfg-stripped sibling ASTs, prefers explicit ownership over a unique implicit sibling definition, and rejects ambiguous providers. Import indexing records direct and forwarded owners under the parent package prefix through a deterministic owner-set fixed point; qualified imports no longer use first-global-candidate fallback. Higher review rejected the intermediate overwrite form and required final singleton-only materialization, then passed the corrected diff. The expanded adversarial regression executes and passes with a reachable same-name decoy, an independently reachable implicit sibling, an internal non-exported import, inactive wrong-arch definitions, delayed conflicting forwarders, and named glob forwarding; bare package `export *` fails explicitly rather than silently producing an incomplete closure. Both direct-runtime guards pass and no `rt_*` use was added. The preserved Stage-4 strict link, remaining function-local import/lowering owners, host-GPU checks, and QEMU rows remain unrun and no broader PASS is claimed.
- stage4-function-local-import-followup-2026-07-13: entry-closure discovery and native use-map indexing now share a recursive AST walk, so imports nested in declaration bodies, statement blocks, metadata expressions, macros, and expression-owned blocks can contribute their real owner instead of requiring a source hoist. Higher review rejected the first partial visitor; the final bounded fix cycle closes its concrete omissions and the synthetic `if -> let -> lambda -> do-block -> use` fixture passes 1/1. Final higher review passes the executable traversal and confirms the remaining pattern/type/signature metadata cannot hide a lowered nested `use`. TODO 556 tracks the separate module-wide alias collision ceiling. The working-tree direct-runtime guard passes. No symbol hardcode, direct `rt_*`, strict Stage-4 relink, host-GPU check, or QEMU PASS is claimed.
- stage4-danger-block-lowering-followup-2026-07-13: the Rust seed now parses contextual `danger:` and canonical `unsafe:` as the same retained `UnsafeBlock` boundary used by the pure compiler instead of a call to an undefined `danger` symbol; ordinary `danger(...)` remains a call. HIR retains the marker, material analysis/closure/hygiene visitors recurse through it, and MIR erases only the marker while lowering its lexical body once. Focused parser/HIR tests pass; the interpreter tail-value/mutation/continue regression passes 1/1 and the driver crate checks. TODO 557 records the missing Rust safety pass, so authorization enforcement is not claimed. The preserved strict Stage-4 relink, two primitive method-dispatch fixes, hosted runtime composition, host-GPU checks, and QEMU rows remain unrun.
- stage4-primitive-method-lowering-followup-2026-07-13: parsing now retains every dotted call as receiver-bearing method syntax because spelling cannot distinguish an ALL_CAPS typed constant from an acronym type such as `TCB`; HIR/interpreter semantic resolution owns value-versus-type dispatch. Native MIR mangling no longer replaces a receiver-qualified call with the sole same-named method of an incompatible type; bare calls keep their existing unique-candidate fallback. Focused parser, semantic HIR, and adversarial mangling regressions pass 1/1, covering `TCB.empty`, the retained `DIRECTX_FRAME_HEADER_WORDS.to_u32`, and `str.rfind` failures without symbol/method hardcodes. Sidecar review found the interpreter's legacy Path-only `Bitfield.new` route; MethodCall now reuses that constructor, restores imported-enum fallback, and its focused interpreter regression passes 1/1. Four recent Vulkan device-property wrappers also convert owned strings into the shared-text value owner without new `rt_*` use. Final higher review passes. The preserved strict Stage-4 relink, hosted runtime composition, host-GPU checks, and QEMU rows remain unrun.
- stage4-facade-type-ownership-followup-2026-07-13: refreshed bootstrap seed/native-all artifacts build, then three bounded retained-cache strict runs exposed deterministic facade ambiguity before compilation. `di.spl` now imports canonical `LogConfig`/`ExecutionConfig` from `config.spl` and deletes stale duplicate nominal forward declarations. Root `Table` is the columnar API paired with `Column`; the incompatible SDN persistence `db.Table` remains qualified-only, and the facade explicitly sources `Table from table`. The final source qualification was applied after the third run and is therefore unverified, uncommitted, and unsynced under the cap. No ambiguity guard relaxation, first-candidate fallback, symbol hardcode, host-GPU check, or QEMU PASS is accepted.
- stage4-frontend-owner-cascade-followup-2026-07-13: three retained-cache strict runs verified the explicit `Table` owner, canonical `types.spl` imports for eight registry accessors, and the semantics-preserving AOP identifier-helper rename with `lexer_chars.spl` as sole public `is_alpha` owner. The third run advanced to two incompatible `mono_cache_register` definitions. The type-erasure-local helper is renamed, the public cache export is sourced from `monomorphize.spl`, and stale `type_tag_name` ownership is imported from `types.spl`; these final edits are unverified after the cap. Small-agent ownership traces and higher review agree; no resolver weakening, hardcode, direct `rt_*`, host-GPU check, or QEMU PASS is accepted.
- stage4-parser-and-typed-receiver-followup-2026-07-13: three retained-cache strict runs verified the post-cap monomorphization/type owner repair, removed duplicate root-facade paths while retaining `parser.spl` compatibility, and compiled the typed `CompiledModule` receiver through the existing checked `emit_object` wrapper. TODO 558 records the underlying `Result<T,E>.unwrap()` inference gap. Strict link now has 112 unique undefined symbols (101 `rt_*`, four `spl_memtrack_*`, seven non-runtime), down from 120; runtime/provider composition, one untyped `str.rfind`, six source owners, host-GPU checks, and QEMU rows remain open. No fourth build or PASS claim is accepted.
- stage4-source-owner-restoration-followup-2026-07-13: the deleted Stitch serializer/diff modules and font-atlas program-version contract are restored from their canonical historical owners, with explicit public theme APIs and the shared CUDA/Metal/OpenCL/Vulkan fail-closed validator. Rust HIR retains text type across `replace`, and pure-Simple MIR has an executable owner-selection regression; TODO 559 tracks the bootstrap fallback's remaining Optional-not-found ABI debt. Two bounded retained-cache links compiled all 1,043 sources and converged at 105 undefined runtime-owned symbols (101 `rt_*`, four `spl_memtrack_*`) with zero non-runtime names. Runtime/provider composition under TODO 535 still gates every fresh x86_64/AArch64/RISC-V QEMU and host-backend row; no QEMU or GPU PASS is claimed.
- stage4-runtime-composition-audit-2026-07-13: raw native-all is rejected as a compiler-only backfill because it overlaps 252 current core-C definitions and the needed `rt_cli_run_file` archive member bundles 453 globals. Link order, whole-archive, duplicate-definition flags, and path-name provider filters are not accepted fixes. TODO 535 now requires an ABI-disjoint compiler-hook archive with a zero-overlap construction gate before exact Stage4 core-plus-backfill selection; provider profiles and cache fingerprinting remain a separate design lane.
- stage4-multiline-rhs-lexer-followup-2026-07-13: small-agent traces found the safe owner at the lexer boundary, not in GUI/Web/DrawIR or per-precedence parser workarounds. One shared `token_requires_rhs` contract now drives both CoreLexer and the legacy module scanner, suppressing only continuation newline/layout while preserving the enclosing indentation stack and open-ended `..`. The existing pure-Simple continuation spec now includes the exact same-indent bitwise-OR shape from `launch_metadata.spl`; diff checks and higher review pass. The focused executable gate is blocked before the spec starts because the canonical release binary identifies as the Rust bootstrap seed and aborts on unresolved `rt_cli_arg_count`; no seed fallback, fourth Stage4 cycle, parser PASS, host-GPU check, or QEMU PASS is claimed. TODO 561 remains open until a pure-Simple deployment runs the regression.
