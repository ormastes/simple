# Feature: MC/DC and HAL Runtime Hardening

## Raw Request
`$sp_dev mc/dc coveraage feature only planed but not impled. 1. impl mc dc fully with perf/memory concern and aspect dynload to prevent overhead when it is not used and fully disable too. since aspect dynload can give this intensive log, so support statically on/off 0 overhead and aspect dynload with min overhead. 2. rt(hal) tag add. which can do pherallel with c/rust/pure impl when pure is not mature. compare io. it is already exists and let tag to do it. imporve. no config but can add config to costomizing. 3. when normal mode and obove mc/dc coverage 100%. and env tests of the rt(hal) cause hal interaction. infra to extract rt tests to env access instractions. and env do each interactions. and tag to skip some not producable scenarios. (except skiped with reason) mc/dc except the skip. make proper way to skip scenario express.(it must express why it skipped) harden rt`

Follow-up: `and add rt to promote to mission critical mode or higher unless specified. so prevent memory alloc. may needs extensive interface updates. first warning later to error.`

## Task Type
feature

## Refined Goal
Deliver production MC/DC coverage and runtime/HAL differential-testing infrastructure that can be compiled completely out, enabled statically, or loaded dynamically with bounded overhead; routes tagged operations through Pure Simple, C, and Rust providers for deterministic comparison; and drives environment interactions from extracted instructions while allowing only explicit reasoned exclusions from the 100% normal-mode MC/DC gate.

## Acceptance Criteria
- AC-1: Local and domain research identify the existing coverage, aspect/dynload, runtime/HAL tag, dual-backend, I/O comparison, environment-test, and skip/exclusion owners; final feature and NFR requirements are written only after the user selects options with pros, cons, and effort, and unchosen option files are deleted.
- AC-2: The compiler and runner collect true decision and condition outcomes for every eligible Boolean decision, including short-circuit and nested conditions, and a focused oracle suite detects cases where branch or statement coverage is 100% but MC/DC is not.
- AC-3: Coverage reports define stable decision/condition identities, witnessed independence pairs, source locations, eligible/excluded totals, and deterministic merge behavior across processes and repeated runs; normal mode fails unless eligible MC/DC is exactly 100%.
- AC-4: MC/DC supports three explicit build/runtime policies: static-off emits no probes, branches, tables, allocations, logging calls, dynload checks, or linked coverage payload; static-on uses compiled instrumentation; dynamic mode loads the coverage aspect only when requested and remains unloaded otherwise.
- AC-5: Static-off native artifacts pass a code/IR or symbol-level equivalence gate against an instrumentation-free build and a representative benchmark demonstrates zero measurable instrumentation work; static-on and dynamic-idle/active paths meet the user-selected latency, throughput, binary-size, peak-RSS, allocation, and log-volume NFR budgets on realistic fixtures.
- AC-6: Dynamic coverage uses the canonical aspect/dynload ownership path, performs no repeated full-tree scan or per-event load lookup, bounds buffers and log output, unloads/settles safely, and preserves complete coverage data on normal shutdown and defined failure paths.
- AC-7: A canonical `rt(hal)` tag declares operation identity, provider availability, comparison semantics, normalization rules, side-effect authority, and environment requirements without introducing per-OS app implementations or raw runtime shortcuts outside owner facades.
- AC-8: Tagged operations can run Pure Simple, C, and Rust implementations under the canonical dual-backend modes: `alpha` runs all applicable providers and stops on a difference, `beta` runs all applicable providers and emits a bounded critical difference report, and `normal` runs only the configured preferred provider; provider execution may be parallel only where isolation and deterministic parent-authoritative comparison make side effects safe.
- AC-9: Existing I/O comparison is migrated or adapted to the canonical tag path, demonstrates equal results and error semantics for representative file, stream, process, and environment interactions, and supports optional configuration for provider priority and comparison policy while preserving a documented no-config default.
- AC-10: Runtime/HAL tests are split into a pure extraction stage that emits typed, versioned environment-access instructions and an environment executor that performs each requested interaction, records inputs/results/provenance, and returns them for deterministic provider comparison without hidden direct environment access in the test logic.
- AC-11: The environment instruction protocol covers every runtime/HAL interaction required by the selected scope, rejects unknown or malformed instructions, bounds payloads/time/resources, prevents unsafe replay or duplicate side effects, and has contract tests plus at least one live current-host integration path per producible interaction class.
- AC-12: Scenario exclusion has one canonical structured expression requiring a stable reason code and human-readable reason, identifies the unavailable capability or intrinsically unproducible condition, appears as excluded (never PASS), and is removed from the MC/DC denominator only when a checker validates the reason; blank, generic, stale, or locally producible skips fail verification.
- AC-13: Executable SSpec scenarios and their generated/manual documents trace every requirement, include deliberate-red fixtures for incomplete MC/DC and invalid skips, contain no placeholder passes, and read as an operator manual with clear setup, action, comparison, exclusion, and evidence steps.
- AC-14: Correctness evidence covers interpreter and applicable native/JIT paths, static-off/static-on/dynamic modes, provider disagreement, provider absence, concurrent comparison, instruction extraction/execution, malformed input, bounded logging, shutdown/failure persistence, and every current-host runtime/HAL capability row; unavailable external-host rows remain blocked with linked Todo DB entries and exact resume plans rather than being counted as skipped or complete.
- AC-15: Runtime and compiler changes pass the focused unit/integration/system/performance gates, the direct environment/runtime guards, required compiler/lib/MCP/LSP checks when affected, and the release-bound whole SPipe suite once; verification reports requirement-by-requirement evidence and stops after at most three distinct fix/verify cycles.
- AC-16: Knowledge is refreshed in the same lane: research, requirements, architecture, system-test plan, detail design, agent-task plan, generated/manual specs, coverage/runtime developer guides, and both feature- and layer-expert `skill.md` entries are updated; every discovered but unfixed gap gets a `doc/08_tracking/bug/` record with file:line and unblock condition. Workflow/tooling contracts in `.codex/skills/`, `.agents/skills/`, `.claude/skills/`, `.claude/agents/spipe/`, `.claude/commands/`, and `.gemini/commands/` are updated when affected, or explicitly marked N/A with reasons.
- AC-17: Every `rt(hal)` operation defaults to the mission-critical assurance profile or a stricter profile unless its declaration explicitly names and justifies a lower profile; the compiler/checker reports unclassified legacy runtime operations as warnings during a bounded migration period and, at the selected enforcement milestone, rejects them as errors.
- AC-18: Mission-critical-or-higher runtime/HAL paths perform no dynamic memory allocation after their declared initialization boundary, including instrumentation, provider dispatch/comparison, environment instruction capture/replay, logging, error, timeout, and shutdown paths; fixed-capacity requirements and overflow behavior are explicit and fail closed, and allocation-trace evidence proves the invariant.
- AC-19: Required interface migrations cover operation tags, caller-supplied storage/arenas, capacity/result types, provider capabilities, and fallible overflow/error results across all affected facades and consumers; compatibility shims emit actionable deprecation warnings, have a documented removal milestone, and are removed or promoted to hard errors at that milestone without introducing raw runtime aliases.

## Scope Exclusions
Release/version bump and push are excluded until the implementation receives an independent `STATUS: PASS`. Native evidence on an unavailable external host is not excluded; it remains an open blocked acceptance row with a resume plan.

## Cooperative Review
This is a broad compiler/runtime/tooling lane. Before any sidecar work, the architecture owner must define the shared interfaces `McdcPolicy`, `McdcDecisionId`, `McdcWitness`, `HalOperationTag`, `HalProvider`, `HalComparison`, `EnvAccessInstruction`, and `ScenarioExclusion`; manual flow helpers `step_configure_coverage_policy`, `step_execute_tagged_providers`, `step_extract_environment_instructions`, `step_execute_environment_instructions`, `step_validate_mcdc_witnesses`, and `step_record_reasoned_exclusion`; and setup/checker helpers `setup_mcdc_fixture`, `setup_hal_provider_matrix`, `check_static_off_zero_overhead`, `check_dynamic_idle_budget`, `check_provider_equivalence`, and `check_exclusion_reason`. Any scaffold must fail with `assert(false)` or `fail(...)`. Research/verification may use lower-model sidecars for bounded inventories and platform rows; `/root` is merge owner, a normal/highest-capability Codex pass is final reviewer, and that reviewer owns generated-manual quality and all done marks.

## Phase
design-done-system-spec-red-phase-next

## Log
- dev: Created state file and extended it to 19 acceptance criteria (type: feature); preserved the full MC/DC, dynload, HAL-provider, environment-instruction, reasoned-exclusion, performance, memory, mission-critical default, allocation-free, interface-migration, and hardening scope.
- research: Completed parallel compiler, runtime/HAL, environment-test, documentation, and domain research; wrote local/domain findings and selectable feature/NFR options. Awaiting the required user selection before final requirements or design.
- requirements: User selected the full feature with efficient, safe performance/memory behavior and parallel execution; resolved as B/M2/P2/E1/R3/NFR-C, wrote final feature/NFR requirements, and deleted option drafts.
- design: Completed parallel architecture reviews and merged the compiler MC/DC, aspect dynload, isolated HAL provider, typed environment replay, R3 migration, and NFR-C evidence design. Wrote architecture/TLDR, detail design, system-test plan, and agent-task plan. Executable red SSpec and implementation remain active.
- worktree: Lane moved to dedicated jj-colocated worktree `/mnt/data/worktrees/simple-mcdc-hal`; first checkpoint commit is `475295a55bcd04e1f12f388e948ff09928c888a7`. GitHub push is temporarily blocked by the concurrent mandatory-check rollout because `config/check/must_check_gates.sdn` and `doc/08_tracking/check/must_check_db.sdn` are not yet present in the pushed commit lineage; no hook bypass was used.
- impl-contracts: Added frozen pointer-free HAL provider and environment-instruction V1 contracts with unit specs. Execution evidence is pending because the isolated worktree has no deployed CLI and `/mnt/data/worktrees/simple-main/bin/simple --version` identifies the only shared candidate as a Rust bootstrap seed. Resume with an admitted pure-Simple Stage 4 CLI: `<admitted-simple> test test/01_unit/lib/common/structural/hal_provider_contracts_spec.spl --no-session-daemon --timeout 180`, then the matching environment-instruction spec. The seed was not used as substitute evidence.
- impl-contract-review: Independent rereview hardened the slice before commit: caller-owned operation bounds now constrain provider result frames; beta difference reports and logs have explicit byte capacities; overflow evidence is typed with first-loss/count provenance; trace digests participate in equivalence; success/error status invariants fail closed; and result-region bounds are compositional. Static patch checks pass. Executable evidence remains pending the admitted pure-Simple CLI above, so this slice is not marked verified.
- impl-env-trace: Added the fixed-scalar `EnvTraceV1` seal/consumption contract. Completion now requires bounded counts, exact instruction/observation/consumption equality, a sealed trace, and matching plan/replay digests; unit scenarios deliberately cover missing, extra, incomplete, unsealed, and divergent traces. This is contract evidence only until the focused spec can run on an admitted CLI.
- impl-mcdc-contracts: Added pointer-free stable decision IDs, correlated evaluated/truth masks, unique-cause witness validation, verifier-receipt-shaped masking evidence, and truthful non-empty eligible count gates. Independent review reduced signed masks to 62 conditions, required multiline-expression parentheses, and removed the earlier semantic overclaim that an opaque masking digest itself proves non-influence. The actual masking proof verifier, identity-level exclusion/gate join, collector storage, and compiler/backend integration remain open; these contract specs are not a full REQ-001..005 done mark and remain unexecuted without an admitted CLI.
- impl-mcdc-c-collector: Added a caller-owned C MC/DC vector collector with a fixed-width shared V1 header, ABI size/offset assertions, explicit session/owner identities, deterministic owner sequence fields, seal-before-snapshot lifecycle, sticky typed statuses, and bounded snapshot copies. The hot record path performs no allocation, does not touch the legacy coverage lock, and fails immediately with `BUSY` rather than blocking on collector contention. Focused C11 `-Werror` plus ASan/UBSan selfcheck passed after review hardening. Compiler/SFFI integration, per-owner sharding, deterministic merge/analyzer consumption, and allocation-trace evidence remain open, so mission-critical completion is not claimed.
- impl-mcdc-simple-boundary: Added the `std.nogc_sync_mut.io` raw MC/DC runtime facade with fixed-width scalar signatures and explicit caller-owned event/snapshot pointers. It exposes initialization, correlated vector recording, sealing, snapshot, and reset without adding a convenience allocator. Pure compiler call emission and runtime archive resolution remain to be verified with the admitted self-hosted toolchain.
- impl-mcdc-interpreter-producer: Wired interpreter control decisions to the bounded vector ABI. Active sessions retain static leaf ordinals across short-circuiting, emit correlated evaluated/truth masks plus outcome, preserve outer frames across nested decisions, use sticky first-failure status and checked sequence exhaustion, and reject >62-condition vectors without losing program semantics. Dynamic-disarmed execution selects the legacy evaluator once per decision and avoids leaf counting/mask work. Decision identity is now 64-bit and includes a canonical two-lane digest of path plus source content registered before evaluation; it no longer relies on backend-divergent runtime path hashing. The runtime atomically claims the single interpreter producer owner and rejects live reinitialization, while isolated processes remain the parallel-provider model. The widened ABI/lifecycle passed focused C11 `-Werror` ASan/UBSan selfchecks. Simple behavioral specs remain unexecuted pending an admitted compiler, and native/JIT producers remain open.
- impl-mcdc-unique-analyzer: Added caller-owned canonical sorting and a strict unique-cause analyzer over sealed V1 vectors. It rejects malformed/reserved ABI fields, duplicate or unsorted receipts, conflicting condition counts for one stable decision, overlap/misalignment, and arithmetic overflow. Pair selection is lexicographically deterministic after sorting; one pair scan covers all conditions, and an explicit pair budget fails closed instead of allowing unbounded analysis. Witness/output exhaustion retains truthful required counts and returns a typed failure. C11 ASan/UBSan edge selfchecks cover permutation sorting, happy witness, zero output capacity, exhausted budget, conflicting identity, and malformed reserved bytes; the shared header also passes C++17 syntax. Masking-proof analysis and identity/exclusion joins remain open.
- impl-mcdc-policy-config: Added the explicit `McdcPolicy` states `off`, `static`, `dynamic`, and fail-closed `invalid`; the real compile CLI now maps legacy `--coverage`/`--mcdc` to static instrumentation and accepts `--mcdc-mode` in split or equals form, while `SIMPLE_MCDC_MODE` remains the compiler transport. CLI-scoped transport restores the prior process value on every return. Compile-context setup records distinct manifest, direct-probe, dynamic-patchpoint, and static-payload gates; invalid policy is rejected before compilation. Native cache scope includes the resolved policy, and the common cache hash exposes a policy-aware primitive with separation tests. Unit scenarios cover default-off, static, dynamic, unknown, missing, and cache-key separation. This checkpoint still defines gates rather than backend emission: explicit policy transport through `CompileOptions`, SMF cache integration without loader layering inversion, probe/table elimination, dynamic aspect ownership, and static-off symbol/IR equivalence evidence remain open.
- impl-mcdc-policy-transport: Added explicit `mcdc_policy` transport to `CompileOptions`, canonicalizes it once into `CompileContext`, and includes the canonical value in options hashing. Watcher requests preserve policy through daemon serialization, reject invalid modes, and use one canonical value for freshness, compilation, and manifest evidence. SMF manifests advance to v4 with an appended policy column, default v1-v3 rows to `off`, and promote legacy manifests on mutation. Compatibility APIs snapshot legacy environment policy only at their boundary; explicit requests do not reread MC/DC environment state. Focused specs cover watcher round-trip, old-request default-off, dynamic manifest round-trip, legacy-row default-off, and manifest upgrade. Loader-owned validation still needs relocation of the cache-key primitive to a lower shared layer before it can accept explicit policy without a 99.loader-to-80.driver inversion; backend gate consumers and CLI-global compatibility removal remain open.
- impl-mcdc-mir-vector-draft: Plumbed the resolved policy into MIR lowering and added distinct direct/dynamic correlated decision-vector opcodes. Instrumented `if`, `if`/`elif` chains, and `while` use two fixed function-entry i64 stack slots, assign source-order leaf ordinals, and update evaluated/truth bits only on paths where a short-circuit leaf executes; static-off performs none of this work. A decision emits one atomic payload with condition count, evaluated mask, truth mask, and outcome, avoiding heap allocation and unsafe runtime-side event correlation. Optimizers preserve all three operands and every executable backend remains explicitly fail-closed until ABI lowering is admitted. Independent review confirmed ordinary short-circuit placement and found the initially omitted `IfChain`, which was then instrumented. This remains a draft, not executable completion: stable closure-wide semantic/dense IDs, complete decision-kind discovery, source digest, runtime-call/patchpoint lowering, dynamic aspect ownership, and focused self-hosted execution evidence remain open; the isolated worktree still lacks an admitted pure-Simple CLI.
- impl-mcdc-static-producer: Static policy now lowers each correlated decision vector directly to `rt_mcdc_record_compiled_vector_v1`; dynamic policy retains a distinct fail-closed patchpoint opcode and off policy performs neither digest lookup nor MIR instrumentation. The driver supplies the pre-captured whole-source content hash without rereading or rehashing source text, including cached-HIR fallback lookup, and semantic IDs add the enclosing function. Runtime adds a single compiled-owner lifecycle with internally sequenced events and an atomic sticky first-failure status; recording uses the existing caller-owned fixed collector and try-lock path with no allocation. The runtime capsule gate now requires all compiled-producer symbols. GCC and Clang C11 `-Werror` ASan/UBSan selfchecks passed, including configuration collision, session mismatch, capacity overflow, sticky status, release, seal, snapshot, and analysis. Independent review found and drove fixes for unconditional off-mode hashing, missing digest lookup on fallback lowering, and traversal-dependent sequential IDs; source-offset IDs are now deterministic and paired with source digest, while closure-freeze dense report-slot remapping remains open. Pure-Simple compiler execution, runner lifecycle/status integration, LLVM/native artifact execution, dynamic patchpoint lowering, and static-off symbol equivalence remain unverified/open.
- impl-mcdc-dynamic-patchpoint-draft: Dynamic policy now emits a direct call to a dormant runtime patchpoint rather than an unlowered MIR opcode. Its idle path is allocation-free and performs bounded atomic reader admission, one target read, and a branch; active calls dispatch the same six-scalar correlated vector ABI. Bind/unbind use an even/odd publication epoch, active-reader count, distinct retry versus draining statuses, and settle-before-unmap ordering to prevent torn publication and ABA unload/rebind. A fixed eight-entry runtime registry turns loader-supplied executable addresses into opaque `u64` handles; raw registration/binding externs are confined to loader modules, unregister refuses bound, draining, or active handles, and checked collector reset refuses live owners or aspect activity. `McdcAspectBinding` enforces increasing generations. `LazyModuleLoader` now auto-registers/binds a relocated module exporting the canonical `rt_mcdc_aspect_vector_v1` function plus its exact `u64,u32,u64,u64,u64,u8 -> i32` ABI marker, retires that binding before ordinary unmap or live replacement, binds the promoted generation, and reports only completed semantic invalidations when readers are still draining. Independent review found and drove fixes for the initial load-before-reader-count race, function-pointer/i64 mismatch, unsafe reset, ambiguous BUSY settlement, public raw binding, unprovenance-checked handles, absent activation call sites, live-replacement bypass, silent pending unload reporting, and missing callable-signature validation. The earlier C selfcheck passed before the final epoch/registry hardening; per the three-cycle cap those final changes are intentionally not claimed executed. Aspect-pack payload bytes still need the existing executable-module mapping path to export the canonical target and ABI marker, and compatibility-loader integration, concurrent stress/TSAN, static-off equivalence, and measured dynamic-idle NFR evidence remain open.
- impl-mcdc-masking-analyzer: Checkpoint `4f84a31bd8fd` adds a fixed postfix Boolean-program ABI and bounded analyzer that prefers unique cause, then exhaustively validates masking completions under a caller proof budget. It emits deterministic masking witnesses and rejects malformed, aliased, misaligned, or exhausted inputs. The focused C masking selfcheck and a final syntax-only compile passed once. Compiler manifest emission of the frozen Boolean program remains open.
- impl-mcdc-identity-exclusion-gate: Checkpoint `9ff38f137859` joins exclusions to the full stable decision identity, admits only fresh governed `CapabilityUnavailable` exclusions, and requires Normal mode, enabled coverage, a nonempty denominator, exact eligible 100%, zero unexplained exclusions, and exact excluded-condition accounting. Static checks passed; focused Simple execution remains unclaimed because this worktree has no admitted deployed CLI.
- impl-hal-isolation-replay-contracts: Checkpoint `d5c6cd2dd461` adds pointer-free Pure/C/Rust isolation receipts, sealed caller-owned storage admission with the default 4 MiB event-plus-log ceiling, deterministic expected-provider/completion/Alpha-Beta commit rules, and an allocation-free exact environment replay cursor that rejects reordering, changed, extra, or incomplete observations. Focused specs were reported passing with the shared pure-Simple release interpreter, while the local wrapper still fails its bounded identity probe. This is contract-level admission only: process spawning/sandboxing, C/Rust ABI executors, physical environment execution, deadline/cancel/reap, and runtime allocation instrumentation remain open.
- sync: The lane was fetched and rebased onto current `main@origin` after these checkpoints. Push remains blocked without bypass because `config/check/must_check_gates.sdn` and `doc/08_tracking/check/must_check_db.sdn` are still absent from the upstream lineage. Concurrent unrelated working-copy edits in runtime/compiler-Rust tracking files were preserved and excluded.
- impl-mcdc-frozen-programs: Checkpoint `17055b24c951` emits bounded source-order postfix CONDITION/NOT/AND/OR programs aligned with probe ordinals, deterministically freezes complete duplicate-free manifests, binds a SHA-256 identity, and persists an aligned discoverable `.mcdc_expr_v1` Note section in every SMF mode. Loader admission validates header/ranges; static-off emits no manifest. No admitted Simple execution evidence exists. Non-SMF native transport remains open.
- impl-mcdc-runtime-manifest-bridge: Checkpoint `0b7f7572e43e` validates little-endian MCDP bytes, computes exact caller-storage requirements, decodes into caller-owned aligned arrays, and invokes the bounded masking analyzer without allocation. Empty and unobserved decisions retain truthful uncovered accounting. Focused GCC/Clang sanitizer and hardened checks passed in three bounded cycles. A native loader owner still must invoke the bridge, and compiler identity needs trusted cryptographic receipt binding.
- impl-mcdc-perf-gate: Checkpoint `eae02ae22b6b` adds a fail-closed five-policy evidence gate for static-off binary/text/section equivalence and static-on/dynamic-idle/dynamic-armed median, p95, RSS, allocation, capacity, pack, overflow, and evidence-loss limits with binary/compiler identities and 30+ rotated samples. No real NFR measurement is claimed; the last deterministic selfcheck adjustment is unexecuted after the three-cycle cap.
- impl-hal-process-coordinator: Checkpoint `643ca66b2c42` launches requested Pure/C/Rust piped workers concurrently through the canonical process facade, performs bounded partial writes and fair drains, parses a fixed HALRES1 response, enforces a shared deadline, cancels/reaps, revokes results from nonterminal workers, and applies deterministic parent comparison. It intentionally reports `isolation_proven_mask = 0` and `critical_ready = false`: the generic process facade cannot prove sandbox, environment, or inherited-handle isolation, so production commit remains fail-closed pending a trusted launcher.
- impl-env-physical-replay: Checkpoint `47d750016f22` adds a sealed physical executor policy, caller-owned argument/result buffers, exact digest-verified replay, allocation-free sandbox execution, real process polling, and pre-seal canonical environment capture served without allocation. The direct environment/runtime guard passed. File/stream, spawn/kill, sockets, time/random, IRQ/MMIO/DMA effects and production coordinator wiring remain open.
