# Feature: Simple Compiler Performance and Memory Efficiency Hardening

## Raw Request
`$sp_dev make deep design update and plan creations. do pherallel agents. update compiler and lint warnung error system and harden in simple. remove perf and mem bug from simple and tools. simple_compiler_performance_memory_efficiency_audit.md`

## Task Type
code-quality

## Refined Goal
Make Simple's optimizer activation, performance diagnostics, shared MIR analysis, compiler/tool hot paths, and memory-efficiency behavior truthful, semantics-preserving, measurable, and production-hardened through one staged pure-Simple design and implementation program derived from `simple_compiler_performance_memory_efficiency_audit.md`.

## Acceptance Criteria
- AC-1: The supplied audit is retained at `doc/01_research/local/simple_compiler_performance_memory_efficiency_audit.md`, is bound to commit `37bd406e219cc35cae049b4130f5167c21801864`, distinguishes observed facts from derived risks and measurement-required claims, and is extended rather than overwritten by later research.
- AC-2: Local and domain research identify the current optimizer registry/dispatch truth table, lint diagnostic model, shared/repeated compiler analyses, compiler and tool hot paths, existing performance evidence, and relevant external compiler precedents; feature and NFR option documents give 2-4 selectable options with pros, cons, effort, correctness policy, compile-time overhead targets, runtime targets, and memory targets, and no final requirements are written until the user selects them.
- AC-3: The effective optimizer pipeline represents every registered pass with structural `PassStatus` and `PassExpectation` semantics (or selected equivalent names), excludes or explicitly labels non-operational transforms, reports requested versus effective pipelines, and has a fail-closed compiler self-check that detects an active identity transform or empty active module transform.
- AC-4: Every transformation activated by this lane has a positive sentinel, negative legality cases, semantic differential evidence, idempotence evidence, and adversarial coverage for applicable overflow, floating-point, traps, zero-trip loops, dominance, aliasing, exceptional control flow, unsafe pointers, and target/backend behavior; unsafe or incomplete passes remain explicitly disabled, skeleton, analysis-only, or remark-only.
- AC-5: A cached `PerfFacts` service (or selected equivalent) owns reusable CFG predecessor/successor data, reverse-postorder, dominators/post-dominators, loop forest, def-use, induction/range facts, region alias/effect facts, and MemorySSA-lite facts with explicit preservation/invalidation; compiler passes and lint rules do not rebuild equivalent whole-function facts independently.
- AC-6: The lint/diagnostic system supports stable performance and memory rule identities, severity/profile policy, source spans, structured evidence, uncertainty/suppression reasons, human text, and machine-readable output; warnings are reserved for actionable likely defects, missed optimization is a remark, and transforms run only with legality and profitability proof.
- AC-7: The selected first-wave loop/collection rules cover nested dynamic iteration, repeated linear lookup, repeated materialization, sequential indexing, repeated sort/setup, unbounded flat-map, accidental Cartesian product, missing index, same-domain repeated traversal, I/O/wait/lock effects in loops, and the audit's required suppression cases without warning merely because multiple loops exist.
- AC-8: The selected first-wave memory rules cover allocation in dynamic loops, missing reserve, repeated immutable concatenation, needless materialization, avoidable clone/copy and large by-value movement, temporary buffers, index reconstruction, closure capture, stack/frame/layout risks, and retained/unbounded collections, with automatic rewrites limited to proven semantics-preserving cases.
- AC-9: Escape analysis fails closed: unresolved/unknown flows do not become `NoEscape`; return, field, aggregate, closure, indirect/unknown-call, global, size, alignment, and lifetime flows have proof reasons and regression coverage before any allocation placement consumes the result.
- AC-10: General loop fusion is not enabled until real loop, dependence, alias, effect/order, early-exit, numeric-order, and profitability proofs exist; high-level `CollectionPlan` producer/consumer and multiple-reduction fusion is designed as the first fusion layer, and every rejected hot candidate can emit a precise structured missed-optimization reason.
- AC-11: Compiler and tool compile-time regressions identified by the audit are removed or recorded as concrete tracked blockers: repeated parsing, repeated CFG/loop construction, quadratic worklists/membership, definitions-by-uses scans, textual expression keys, repeated array concatenation, and redundant full-tree/file/process work have before/after evidence on realistic fixtures.
- AC-12: Relevant baselines are captured before optimization; touched `.spl` files are evaluated with `bin/simple run src/app/optimize/main.spl <file> --full --level=O3`; correctness tests and the same performance measurements run once after changes, and any unmet runtime/compiler parity or material regression is recorded under `doc/08_tracking/bug/` with measurements and an unblock condition.
- AC-13: Executable SSpec scenarios trace every selected `REQ-NNN`, use canonical built-in matchers and real fail-closed assertions, cover diagnostic output and transformation legality, and mirror to reviewed operator-quality Markdown under `doc/06_spec`; no executable `_spec.spl` exists under `doc/06_spec`.
- AC-14: The smallest applicable pure-Simple compiler target/provider/SCI projection and admitted binary provenance are recorded for focused checks; Rust-seed fallback is forbidden, unsupported Stage 2/3 commands fail closed, and Stage 2/3 evidence is not promoted to Stage 4, general SPipe/docgen/test, release, convergence, or cross-host proof.
- AC-15: Knowledge updates ship with implementation: research, requirements, plans, architecture, detail design, generated/manual specs, and the compiler performance guide are current; `doc/00_llm_process/feature_expert/compiler_performance/skill.md` and the applicable compiler/lint layer-expert skill are created or updated; every discovered unfixed gap has a `doc/08_tracking/bug/` record with file:line evidence and unblock condition. Workflow skill/agent/command files are updated only if their contracts change, otherwise the plan records them as N/A with reasons.
- AC-16: Final verification covers compiler/core/lib and affected MCP/LSP/tool checks required by repository policy, direct env/process facade guards, lint and duplicate checks for owned files, optimizer sentinel integrity, requirements-to-test traceability, NFR measurement evidence, generated-manual quality, and cooperative-review completion; it reports `STATUS: PASS` only when all selected requirements are proven.

## Scope Exclusions
- No blind bulk activation of dormant MIR passes.
- No C/Rust rewrite to claim performance improvement; pure-Simple owners are inspected and fixed first, with runtime edits allowed only after boundary evidence proves the defect is below them.
- No generic source rewrite that changes observable ordering, errors, allocation identity, ABI, persistence formats, hashing/equality, worst-case guarantees, concurrency semantics, or public API without an explicitly selected requirement.
- No release/version bump or push unless separately requested after verification passes.

## Cooperative Review
- Parallel sidecar lanes: optimizer registry/dispatch and pass-legality audit; lint/diagnostic and CollectionPlan rule audit; shared MIR analysis/escape-analysis audit; compiler/tool compile-time and allocation hot-path audit; existing tests/docs/evidence audit; external compiler/research precedent audit.
- Merge owner: `/root` primary Codex agent.
- Final reviewer: `/root` on the highest-capability model; sidecar findings are advisory until source-evidence checked and merged.
- Shared interface names: `PassStatus`, `PassExpectation`, `PassRunRecord`, `PerfFacts`, `LoopFact`, `MemoryRegion`, `OperationSummary`, `CostExpr`, `PerfDiagnostic`, and `PerfRuleId`. These are design anchors; final requirements may select equivalent names deliberately.
- Manual flow steps: `Load the effective optimizer pipeline`; `Reject dishonest active transforms`; `Analyze one function with shared performance facts`; `Report actionable performance and memory diagnostics`; `Preserve semantics while applying a proven transform`; `Compare compiler and runtime evidence against the baseline`.
- Setup/checker helpers: `setup_optimizer_integrity_fixture`, `setup_perf_diagnostic_fixture`, `check_effective_pipeline_status`, `check_perf_facts_reuse`, `check_perf_diagnostic_record`, `check_semantic_differential`, and `check_perf_budget`.
- Fail-fast placeholder policy: incomplete executable scenarios use `fail("implementation pending: <REQ-ID>")` or `assert(false)` and can never count as PASS; no `pass_todo`, empty body, or always-true assertion is permitted.
- Generated-manual review owner: `/root`; sidecars may draft scenarios or mappings but cannot accept manual quality or mark requirements done.

## Phase
implementation-in-progress

## Log
- dev: Created state file with 16 acceptance criteria (type: code-quality); recorded parallel lanes, shared interfaces, helpers, and fail-closed review policy.
- research: Preserved both research intakes, completed local/domain validation, and recorded the user-selected full staged feature and strict fail-closed NFR requirements.
- design: Added canonical architecture and TLDR, detail design, system-test plan, agent task plan, fail-fast executable SSpec scaffold, and reviewed operator manual. Parallel design drafts were reconciled against existing compiler layer ownership and the frozen interface/test contract.
- implementation: Added structural `PassStatus`/`PassExpectation` truth and fail-closed dispatch for dormant/proof-incomplete MIR passes; hardened active vector loop recognition to exact integer unit steps in the actual loop region; made escape finalization preserve `Unknown`; added focused pass, vector, and escape regression coverage.
- implementation: Split requested from effective pipelines, added backend-aware inactive reasons and registry integrity checks, introduced shared CFG `PerfFacts`, and rewired loop detection to reuse successors/predecessors/indexes while removing slice-based worklist pops.
- implementation: Added structured lint evidence/uncertainty to human and JSON records; mapped `COLL*` to `collection_performance`; capped unresolved AST fallback findings at warning; removed their unproved machine fixes; retained robust/critical deny policy for future typed high-confidence producers.
- implementation: Repaired GC escape integration for `Ret(value)`, made field store/load region keys consistent, and propagated proven allocation sizes while rejecting invalid sizes.
- knowledge: Added the compiler performance operator guide, feature-expert skill, and MIR optimization layer-expert skill.
- verification-blocker: The isolated worktree has no admitted pure-Simple Stage 4 binary. A nearby executable identified itself as a Rust bootstrap seed and was rejected as acceptance evidence; blocker recorded under `doc/08_tracking/bug/simple_compiler_perf_audit_missing_stage4_binary.md`.
- implementation: Reclassified auto-vectorization as `AnalysisOnly` and canonical collection optimization as `Disabled`; removed hoisting claims, made both hoisting facades fail closed, and excluded unproved collection rewrites pending ownership/alias/effect/differential gates.
- implementation: Corrected loop exits to retain `(from,to)` edges and made trip counts unknown until SCEV-lite proves initialization, step, direction, branch polarity, nowrap, and finiteness.
- implementation: AST-only collection findings remain advisory under Robust/Critical profiles with explicit uncertainty and no machine fix; typed proven diagnostics retain the future deny-policy lane.
- review: Parallel warning/error, MIR-safety, tool-hot-path, and completion audits were source-checked by the merge owner. Remaining requirements stay explicitly incomplete.
- implementation: Added deterministic `simple.opt-pipeline-report/v1` planning evidence, typed backend outcomes, structured `PassRunRecord` integrity checks, stable positive/negative witness contracts for the four admitted active transforms, and fail-closed witness registry validation. Planning counters remain explicitly unavailable rather than implying execution.
- implementation: Reclassified target narrowing and inline facades as `Skeleton` and read-ahead hoisting as `AnalysisOnly`; updated effective-pipeline and hotspot-plan expectations so quarantined passes are never advertised as runnable.
- verification-blocker: General post-pass MIR verification and dispatcher-owned candidate/change/rejection counters do not yet exist. Planning reports therefore use `run_outcome=not-run` with null counters; the follow-up is tracked under `doc/08_tracking/bug/optimizer_pass_outcome_counters_and_verify_each_missing.md`.
- implementation: Added `run_named_pass_with_record` and a fail-closed structural MIR receipt. The boundary validates non-empty block sets, unique non-negative block/local identities, entry membership, and all normal/unwind CFG targets both before and after a function pass; it records instruction deltas and serialized-MIR changed/unchanged truth without fabricating unavailable native candidate statistics.
- implementation: Extended shared `PerfFacts` with one-pass definition/use sites, dense declared-local buckets that avoid per-use dictionary-array COW rebuilding, explicit incomplete coverage and invalid-local counters, and conservative per-pass preservation/invalidation contracts. Unmodeled MIR variants cannot authorize def-use-dependent transformations.
- optimization: Removed repeated `uses = uses + ...` and per-block `all_accesses = all_accesses + ...` rebuilding from vectorizer analysis. Operand and array-access facts now append into one owned buffer; a 32-argument call fixture pins count and order. Full vectorizer migration to shared complete `PerfFacts` remains pending.
- optimization: Replaced the vectorizer's private function/loop def-use rebuild with a loop projection over shared `PerfFacts`. Incomplete opcode or local coverage now yields `analysis_complete=false` and rejects the candidate. Replaced definitions-by-uses Cartesian dependency emission with extrema plus adjacent-WAW summaries, bounding records linearly; a 256-definition/use fixture pins the bound.
- optimization: Replaced typed-storage per-candidate whole-function use scans with one shared `PerfFacts` build and O(1) local-bucket queries. Storage-access structural-load classification also consumes shared counts; incomplete opcode/local coverage marks summaries unknown and blocks rewriting. Test fixtures now declare referenced locals, and an injected unmodeled ownership instruction proves fail-closed behavior.
- optimization: Replaced COLL fallback warning-by-warning full-file function/fix location scans with one `LintSourceLocationIndex` traversal. The index preserves first function declarations and file-unique COLL001/COLL007 fix semantics. This removes avoidable warning×line work but does not claim to fix the measured parser-dominated lint latency; shared typed frontend sessions remain required.
- implementation: Added `LintParsedRevision` and `lint_cli_source_with_parsed_revision`, a parser-free lint bridge for compiler/LSP callers that already own the AST. It validates source path, SHA-256 digest, and live AST arena generation before consuming declaration indices; stale revisions emit deny-level `LINTREV001`. Standalone lint parses once and delegates to the same parsed-results owner.
- implementation: Hardened the query/LSP AST projection to consume `parse_module_silent_checked` by-value, restore prior parser state, and emit deny-level `PARSE001` with `NOT LINTED` wording instead of silently returning a clean result. Untyped `COLL*` projection records are now warning-only and disclose unresolved type/effect/alias/mutation facts.
- correctness/optimization: Replaced per-header SCC-style loop membership with dominance-scoped reverse predecessor construction from all proven latches. Nested inner loops no longer absorb outer latch/header regions, multiple latches union deterministically, and two full CFG reachability walks plus a backedge rescan per header are removed.
- implementation: Replaced Boolean changed-as-one candidate telemetry with exact native outcomes for active `write_coalesce` and `syscall_batch` function adapters. Multi-candidate runs now report exact candidate/transformed counts and stable witness reasons, zero-candidate runs remain unchanged, and count-versus-serialized-change disagreement or a newly active adapter without exact outcomes fails closed.
- implementation/optimization: Extended shared `PerfFacts` with bounded flat block USE/DEF and live-in/live-out matrices, CFG completeness, a storage-reusing predecessor worklist, explicit visit/memory budgets, and preservation dependencies. Unknown opcodes/locals, duplicate or dangling CFG identities, and exhausted budgets fail closed. The dormant DCE implementation now reuses shared reachability/liveness in one backward sweep; its private CFG reconstruction and quadratic later-instruction scans were removed while the public pass remains `Skeleton`.
- implementation/optimization: Extended the canonical MIR access visitor across ownership, async, pipeline, probe, SIMD/warp, GPU, VHDL, nested-place, and inline-assembly families. Inline-assembly outputs validate local destinations; result-match ghost metadata, text-only GPU launch arguments, and hidden VHDL process-body edges remain explicitly incomplete. Dormant DCE conservatively retains modeled effectful operations and remains `Skeleton`.
- implementation/optimization: Active write-coalesce and syscall-batch adapters now derive exact outcomes from hints actually emitted during the rewrite, eliminating their duplicate candidate scan. Exact zero/one/multiple fixtures pin native counters and reason cardinality. A generated-registry source contract requires every MIR opcode to declare a named PerfFacts access arm and forbids wildcard admission.
- implementation/hardening: Added explicit lint evidence tiers and one O(1) severity projection. Source-pattern, parsed-structural, and incomplete performance findings remain advisory under robust/critical and `--deny-all`; only typed-proven evidence may escalate. CLI text/JSON now agree on effective severity and confidence, and query/LSP collection diagnostics support `Allow` suppression plus structured confidence/evidence/uncertainty without another parse or traversal.
- optimization: Reused the query lint path's existing split source view and added one-pass callable/use-star/export-star location indexes. Collection and wildcard projections now use constant-time lookups instead of rescanning every line per warning; standalone W0407 uses the shared fallback index, and its tiny per-line COLL code array allocation was removed. Exact causal spans and GC-boundary fallback migration remain tracked rather than claimed complete.
- optimization: Folded query GC-boundary import collection and first-line recovery into the same source-location index. The warning producer no longer performs an additional import scan followed by a full-file scan per violation; a 64-import decoy fixture pins the indexed line while diagnostic policy remains unchanged.
- optimization: Replaced UNUSED001's per-declaration backward function-boundary search and full-function use scan with one forward function partition and one identifier-frequency table per function. Declaration order, same-line exclusion, text compatibility semantics, severity, and locations are retained; a 32-declaration fixture and source contract prevent recurrence of declaration-count multiplied by function-size scans.
- optimization: Replaced RET001's forward suffix scan per call candidate with one reverse monotonic indentation index. Each candidate now performs an O(1) last-statement lookup while blank-line, sibling, dedent, and end-of-file semantics remain unchanged; a 128-level fixture prevents the former deep-nesting quadratic shape from recurring.
- optimization: Removed CLOS001's outer-variable by closure-body Cartesian scan. Outer declarations are counted in a dictionary, each body line yields at most one assignment target, and lookup is O(1); legacy duplicate-declaration multiplicity and warning order are retained. Per-closure backward scope discovery remains explicitly tracked.
- optimization: Indexed MEXH duplicate-arm and enum-variant membership. Per-match arm order remains available for diagnostics, but duplicate checks and missing-variant checks are O(1) membership queries; enum inference no longer scans every variant for every arm. A 128-variant fixture pins the indexed fact shape while typed scrutinee inference remains the final owner.
- optimization: Generalized the reverse indentation analysis into caller-owned last-statement and next-same-or-shallower facts. RET001 and UNREACH001 now share one index in the primary lint path, and query-check uses indexed return successors too, removing per-return suffix scans without duplicating the index per rule.
- implementation/optimization: Extended optimizer boundary receipts with canonical instruction/terminator access coverage and declared-local validation for every modeled DEF/USE. Block/local identity sets now use dictionary membership rather than quadratic growing arrays; malformed instruction and return operands fail closed before or after active pass dispatch. SSA dominance, type, ownership, loop, module-pass, and pipeline-wide verify-each proof remain open.
- implementation: Added exact function ABI-local verification to optimizer receipts. Argument locals must have unique in-range indices, cover every signature parameter, and match its MIR type; return slots must be unique and match the declared return type. Broader opcode typing remains explicitly unproved.
- optimization: Removed the optimizer receipt's per-block temporary CFG-target array. Goto, branch, switch, and normal/unwind call edges are validated directly against the indexed block set; a 128-case switch fixture pins the allocation-free high-fanout path.
- implementation: Added stable machine-readable `MIRV001`-`MIRV019` verifier codes parallel to human receipt errors, with cardinality fixtures across CFG, operand, terminator, and ABI failures. `MIRV999` explicitly identifies an unclassified future error while preserving fail-closed behavior.
- implementation: Added deterministic module structural receipts. Sorted function symbols are checked against map keys, child function receipts and counts are aggregated, and `MIRVM001` identifies key/symbol disagreement. This supplies the verifier surface for future module-pass records without claiming module execution telemetry.
- implementation/optimization: Added a fail-closed checked module runner and wired canonical dispatch to it under cached `SIMPLE_MIR_VERIFY_EACH=1`. Function-scoped active passes use their exact recorded boundary; invalid output preserves the last input module. Normal builds pay one process-wide environment lookup and an O(1) cached comparison per pass, while module scans, deterministic symbol sorting, receipts, and diagnostic allocations remain opt-in. Exact module-pass counters and broader SSA/type/ownership/loop proof remain open.
- implementation/optimization: Extended the admitted optimizer receipt with bounded SSA definition/use dominance. Stable `MIRV020`-`MIRV024` failures cover undefined values, multiple definitions, same-block use-before-def, non-dominating cross-block uses, and unavailable dominance. Call-terminator results are defined on the normal edge. A verifier-only `PerfFacts` builder reuses CFG/def-use/dominators while omitting O(blocks*locals) liveness matrices; normal builds remain unaffected behind cached verify-each dispatch. Opcode typing, ownership, loop proof, and exact module-pass telemetry remain open.
- implementation/optimization: Added a partial, machine-honest opcode type receipt in the existing structural instruction traversal. `MIRV025`-`MIRV027` validate constants, copies/moves, and casts/bitcasts; function/module receipts count checked and unproved instructions. One O(locals) type dictionary and structural `MirTypeKind` comparisons avoid serialized type keys and a second MIR scan. Remaining opcode families, ownership, and loop proof stay explicitly unproved.
- optimization: Replaced lint configuration and file-attribute membership via repeated `all_lint_names().contains` with allocation-free exhaustive `lint_name_is_known` dispatch. K authored names no longer allocate K full registry arrays or perform K*N linear membership scans. Enumeration remains for bulk policy operations, and a parity/source-contract fixture prevents registry drift and recurrence. Collection diagnostics retain their existing stable `collection_performance` suppression owner.
- optimization: Removed per-diagnostic default-policy reconstruction. `LintConfig` now owns one immutable effective-default dictionary, refreshes it only when the profile changes, and shares it with child configs. `get_level` performs allocation-free O(1) override/default lookup instead of rebuilding O(rule-count) base/profile dictionaries—twice for common collection diagnostic policy projection. Project-SDN parse caching remains open.
- optimization: Removed repeated target-policy parsing inside one lint request. File-parsed `LintConfig` records its exact `source_path`; resolution skips rereading the same manifest, and `Linter` retains only the immediately resolved path/config for parsed AST append. The next file overwrites this state and mismatches fall back to resolution, avoiding stale process-global caching. Normal single-file policy read/parse falls from up to three target passes to one; bounded directory/LSP cache work remains open.
- optimization: Consolidated per-diagnostic suppression and severity projection into `LintPolicyDecision`. Central source/EasyFix filtering and parsed AST append now perform one code mapping and one effective-level lookup rather than paired keep/apply calls. Allow/warn/deny, unknown-code, typed-proven escalation, and unproved performance warning caps remain pinned; compatibility wrappers remain for staged external migration.
- optimization: Removed duplicate source splitting between text/EasyFix lint and parsed AST append. The combined entrypoint retains one COW line view, parsed projection reuses it, and all success/non-Simple/parse-error/revision-mismatch exits explicitly release it. Ordinary `lint_source` retains neither source lines nor resolved policy, bounding long-lived tool memory to no prior file contents.
- optimization: Extended the canonical source-line view through file-attribute policy and seven line-oriented rule families: parameter tags, raw-runtime fix offsets, stale diagrams, LLVM type guards, accessor/parent names, freestanding checks, and WM boundary checks. Normal lint removes up to eight additional source splits and line-header arrays per applicable file; compatibility string wrappers remain for external callers. EasyFix/traceability owners remain future migration boundaries.
- optimization: Passed the same canonical source-line view to feature tracking, SPipe quality, and all three raw typed-UI checks. Five additional full-source splits and transient line-header arrays are removed on applicable files with unchanged predicates, ordering, and location accounting. EasyFix registry owners remain the next migration boundary.
- correctness/optimization: Removed duplicate direct `primitive_api` and `simple_script_required` execution after the full EasyFix registry, which already owns both rules. Each applicable rule now scans once and emits one diagnostic/fix sequence instead of duplicating traversal, allocation, policy projection, and output. Registry-wide shared line/context construction remains open.
- optimization: Added a canonical-lines EasyFix registry entrypoint and shared one request-owned `LineContext` array across four SPipe rules; missing-docstring analysis consumes the same lines directly. Normal spec lint removes one extra source split, three duplicate context arrays, and repeated trim/indent/offset derivation. Non-spec files allocate no SPipe contexts, compatibility entrypoints retain early path gates, and broader registry-family migration remains open.
- optimization: Generalized the EasyFix request view to one context array shared by resource, struct-construction, annotation, module-boundary, and SPipe rules, with canonical lines shared by non-exhaustive-match and bypass checks. Duplicate-typed-argument analysis now reuses lines/contexts and no longer allocates a line array per candidate signature. Existing scan order and standalone wrappers remain intact; a name-indexed one-pass call-site fact and remaining split-based rules stay open.
- optimization: Migrated the remaining compiler-owned EasyFix split consumers—star import, wide public, bare bool, primitive API, short grammar, and script language—to canonical registry lines/contexts. Primitive allow suppression reuses lines instead of splitting again, and compatibility wrappers retain pre-split path gates. Normal lint removes seven more line arrays plus redundant short-grammar context work; stdlib-owned contextual/deprecation/stub scanners remain open.
- architecture/optimization: Made stdlib EasyFix helpers the canonical `LineContext`/`EasyFixSourceView` owner. Compiler helpers re-export that type instead of maintaining a structural duplicate, and contextual-keyword, deprecated-if-let, stub, and compiler rules share the registry's single request-scoped context array. Normal lint removes three stdlib context arrays plus the former compiler/stdlib duplicate while preserving offsets, ordering, and standalone wrappers.
- optimization: Replaced duplicate-typed-argument candidate×source call matching with one indexed lexical pass. Unique `(name, arity)` targets are admitted, ambiguous overloads remain rejected, and flat replacements are deterministically ordered by signature/source sequence. Cost changes from O(signatures*source) matching to O(source + signatures + replacements log replacements), memory is O(signatures + replacements), and obsolete per-signature scans were removed.
- correctness/optimization: Replaced per-annotation decorator/attribute whitelist arrays with exact match dispatch. Replaced per-fix `split(":")` ID parsing with one semantic decoder for namespaced `L|E:code:location` and direct `code:location` forms. Direct W0404/W0406 IDs now retain their actual policy code instead of using the line component, malformed IDs remain visible as unknown codes, and no parts/whitelist arrays are allocated on these hot paths.
- correctness: Restored policy reachability for EasyFix rules already declared in lint names/defaults. Exact mapping now covers export/init/bypass, SPipe, resource, annotation, stub, duplicate-argument, primitive, bool, and script codes, and W0406 joins `visibility_boundary`. Configured/default deny promotion and explicit allow suppression now apply through the central one-pass policy decision instead of being silently bypassed.
- correctness: Registered configurable warning policy for previously orphaned EasyFix families: parser contextual keywords, deprecated `if let`, struct-construction parentheses, short-grammar rewrites, raw-unit postfix, and SIMD opportunity. Four short-grammar wire codes map to one stable policy owner; all emitted EasyFix families can now be allowed, warned, or denied through central policy.
- correctness/optimization: Replaced dual raw-source `unknown_decorator`/`unknown_attribute` registry scans with one union-based `unknown_annotation` fallback. Known names no longer cross-trigger the other category, truly unknown names emit one advisory/fix instead of two, and normal cost drops from two context traversals to one. The generic policy is registered and aliases both legacy settings bidirectionally; legacy standalone rule functions remain available pending typed-HIR classification.
- correctness/optimization: Removed both unreachable collection-hoisting rewrite bodies and their private-only legality helpers from behind fail-closed returns. The compatibility entrypoints remain identities, unsafe loop-header insertion can no longer be resurrected mechanically, and roughly 190 lines of parsed/compiled dead transform logic are gone. Scalar/invariance predicates remain analysis scaffolding only; real LICM still requires preheader, dominance, memory/effect, zero-trip, and speculatability proofs.
- correctness/optimization: Removed the unreachable comparison-bound trip-count recognizer and its private-only operand helpers from behind the loop detector's fail-closed `nil` return. Natural-loop and exit-edge facts remain active, but exact trip counts cannot be resurrected mechanically without SCEV-lite proofs for initialization, signed step, polarity, nowrap, and finiteness. This also removes parsed/compiled dead source from the compiler.
- correctness/optimization: Removed the dormant unsafe TCO implementation while preserving its `Skeleton` factory, statistics, and identity compatibility entrypoints. The deleted rewrite sequentially assigned recursive-call arguments to parameter locals and could clobber permutations such as `f(a,b) -> f(b,a)`; activation now requires parallel temporaries and explicit arity/type/ownership/effect/unwind/debug proofs. Dead compiler parse/compile work and IR memory are reduced.
- research/design: A parallel lint hot-path audit found repository lint reconstructs descriptors and repeatedly discovers/parses policy per file. The next tooling tranche is a command-scoped `LintSession` with bounded manifest/config caches and one shared source read per file, including optional SIMD analysis.
- optimization: Repository lint now constructs one command-scoped `Linter` instead of rebuilding roughly 56 rule descriptors for every file. `run_lint_file_with_linter` owns a bounded last-project parsed-policy cache and clones cached configuration before per-file/CLI overrides; standalone lint retains a one-file wrapper. This removes `O(files*rules)` transient allocation and repeated adjacent manifest parsing without process-global stale state. Directory discovery, critical policy, SIMD, and fix-source reuse remain open.
- optimization/hardening: Repository lint now reads each source once through the tagged lint reader and shares the exact payload with ordinary lint plus optional SIMD opportunity analysis. A valid empty file remains distinct from a read failure. Source-owned and file-owned linter entrypoints coexist for batch and compatibility callers; mutating fixes still reread before atomic replacement to avoid stale edits.
- optimization: Dynamic-capability lint now lazily snapshots its effective critical-mode policy once per command-owned `Linter`. Repository lint no longer reads, splits, and parses `config/critical_mode.sdn` for every source—especially the common disabled case that emits nothing. Only the scalar mode is retained, bounding memory and keeping command-to-command invalidation automatic.
- optimization: Repository lint now uses a command-owned bounded directory-to-manifest cache. Ancestor discovery consults cached common roots, retains hits and misses up to 4096 entries, and remains correct without caching after the cap. A prepared-path token removes the second per-file ancestor walk in `lint_source`; file attributes still apply to an isolated child policy.
- optimization: Replaced lint's adjacent-only parsed-manifest slot with a bounded command cache of up to 256 unique `simple.sdn` policies. A path-to-integer index addresses flat config storage, avoiding struct-valued dictionary retrieval; hits clone the cached base before overrides, and saturation falls back to correct uncached parsing. Alternating multi-project traversal no longer reparses retained manifests.
- optimization: Manifest-free lint files now clone one command-owned base policy instead of calling `LintConfig.new()` per file. Child configs share the immutable effective-default dictionary and own their mutable overrides; profile changes replace child defaults without touching the base. This removes rule-count-proportional default-map allocation from each no-manifest file.
- optimization: Active storage-layout advisory construction now deduplicates field IDs with indexed membership and an explicit count, removing a growing ID array and quadratic membership scans. Canonical text rows use the standard deterministic sort instead of handwritten selection sort. Identity format and fail-closed overlap semantics are preserved; the separate semantic overlap pair proof remains pending an interval-sweep design.
- correctness/optimization: Removed the dormant incomplete string-builder implementation while preserving its `Skeleton` class, factory, statistics, and identity entrypoint. The deleted body emitted a push to an undeclared/uninitialized parts local and never constructed the final join/result. Mechanical activation can no longer create malformed MIR; unused loop-detector/local-ID state and dead compiler work are gone.
- correctness/optimization: Closed the direct-call bypass around disabled strength reduction. The exported class and `reduce_block` are now fail-closed identities; signed division/remainder, decomposition, and fixed synthetic-local rewrite bodies are removed. Provider proof metadata and zero statistics remain for compatibility, while legacy fixtures now assert quarantine rather than executing unsafe transformations.
- correctness/optimization: Removed the dormant GVN implementation behind its `Skeleton` wrapper. Direct class calls can no longer choose leaders by MIR storage order or reuse field loads without memory-version/alias invalidation. Value-number dictionaries, interpolated signature allocation, and rewrite bodies are gone; class/factory/dependency/statistics compatibility remains inert pending dominator and MemorySSA-lite proofs.
- correctness/optimization: Closed direct-call bounds-check-elimination bypasses. Exported function/block entrypoints are now identities; global loop-proof pre-seeding, textual key allocation, and check deletion are removed. Proof record types and zero counters remain compatible. Legacy simulated-elimination tests now assert quarantine pending dominance-scoped SSA/range/mutation and exceptional-control-flow proofs.
- correctness/optimization: Closed direct-call bypasses for disabled LICM and loop unrolling. Preheader synthesis, predecessor redirection, instruction movement/duplication, per-pass loop detectors, and combined class chaining are removed. Compatibility classes/counters/thresholds/factories remain identities; legacy simulated-transform fixtures now assert quarantine pending canonical loop, SSA, memory/effect, zero-trip, and profitability proofs.
- correctness/optimization: Quarantined the dormant generator state-machine rewrite while preserving exported yield analysis. Class transform/build entrypoints are identities; dispatcher, signature/local mutation, state-block construction, and segment lowering are removed. Yield analysis now uses one forward definition walk rather than rescanning the function per yield, with conservative `O(Y*locals)` snapshots explicitly barred from authorizing frame layout.
- correctness/optimization: Quarantined dormant body outlining. Function and module entrypoints remain identities, while cold-group discovery, repeated linear MIR lookups, live-variable extraction, CFG rewriting, exit remapping, and synthetic-function construction were removed. Compatibility types/counters remain, mechanical activation is closed, and roughly 600 lines of dead compiler parse/compile and allocation footprint are eliminated pending canonical region, SSA, ownership, unwind/debug, module-construction, profitability, and differential proofs.
- correctness/optimization: Quarantined local CSE's direct rewrite bypass. The Skeleton no longer rewrites Copy/Move or mutable field expressions using incomplete invalidation, nor allocates text keys and rebuilt MIR arrays through its pass path. Exported representation/table/class/factory/statistics surfaces remain fail-closed. Parallel review keeps copy propagation, constant folding, and DCE inactive pending exhaustive operand and ownership handling, shared arithmetic/result typing, and an explicit trap/effect plus sparse-liveness budget respectively.
