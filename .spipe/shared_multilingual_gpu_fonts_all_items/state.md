# Feature: Shared Multilingual GPU Fonts — All Remaining Items

## Raw Request

`$sp_dev remake plan do the all item tasks in pherallel`

## Task Type

todo

## Refined Goal

Finish every selected shared-multilingual-font requirement and NFR from the
current branch, using parallel owner lanes where dependencies permit, without
promoting temporary Stage 2 diagnostics or unavailable native-host evidence,
and finish with one independently reviewed verification result and a synced
GitHub checkpoint.

## Acceptance Criteria

- AC-1: A regenerated current-worktree matrix covers REQ-001–016 and
  NFR-001–008. Every row is `pass`, `active`, or `blocked`; every non-pass row
  has an owner, dependency, exact command/evidence, retained artifact path, and
  final reviewer. Counts are derived from the current tree, not copied from
  historical 42/46-command snapshots.
- AC-2: Current GSUB/GPOS and selected-shaping code has no superseded duplicate
  lane, uses the shared `FontRenderer`/`FontRenderBatch` path, and has focused
  executable coverage for selected scripts and negative inputs.
- AC-3: Runtime-dependent font evidence uses one hash-bound, current-source,
  pure-Simple full CLI and matching core-C runtime admitted once by the
  essential-tools smoke plus deliberate-red and zero-example calibration.
  Stage 2 successor3 is diagnostic/producer evidence only. No Rust-seed
  acceptance and no full bootstrap unless incremental production cannot
  satisfy the prerequisite.
- AC-4: Each current focused font spec and its manual is inventoried once,
  contains real assertions and frozen primary-flow steps, reports `0 stubs`,
  and leaves no executable `.spl` under `doc/06_spec`.
- AC-5: REQ-001–005 and NFR-001/003 prove deterministic manifest, licensing,
  exact bytes, packaging/archive, and SimpleOS projection.
- AC-6: REQ-006–011/015/016 prove exact-face shaping, GSUB/GPOS selection,
  bounded cache/lifecycle, shared batch/config policy, Draw IR round-trip, and
  Engine2D/Web/GUI/hosted-WM/current-host SimpleOS routing without a private
  font path.
- AC-7: REQ-012/013 and NFR-002/005–008 prove actual selected native promotion
  through real Engine2D and Engine3D texture/upload/bind/draw/fence/device-origin
  readback. Every unavailable native-host row retains an exact blocker and
  resume contract, but that blocked state does not satisfy promotion. Simulation
  and CPU mirrors are never native PASS evidence.
- AC-8: NFR-004–006 evidence records fixture, warmup/samples, cache hit rate,
  p95, CPU/GPU comparison, RSS/VRAM, upload behavior, hashes, host, device, and
  driver and enforces the selected thresholds.
- AC-9: Blocker hygiene keeps unavailable host/capability rows visible as
  `blocked` or `unsupported`, never skipped/PASS, with prerequisite, resume
  command, artifacts, owner, and reviewer. Satisfying this bookkeeping
  criterion does not complete AC-7, AC-8, or the overall hardware requirements.
- AC-10: Eligible host-independent tasks run in parallel after interfaces are
  frozen; agents do not overwrite unrelated work and `/root` integrates and
  reviews their bounded handoffs.
- AC-11: Final highest-capability verification maps every requirement to
  current evidence and runs each applicable guard/check at most once. It emits
  `STATUS: PASS` only when all selected required rows pass, otherwise precise
  blockers remain.
- AC-12: The isolated branch is fetched/rebased safely, file-count guarded,
  committed, and pushed after verification; unrelated worktrees remain
  untouched.

## Scope Exclusions

- Multicolor emoji/COLR/CBDT/SVG, CFF/CFF2, arbitrary variable axes, MSDF, direct GPU outline rasterization, and new dependencies remain excluded by the selected requirements.
- No release or version bump is part of this goal.
- Unavailable hardware may be documented as blocked, but postponement does not complete AC-7, AC-8, AC-9, or the overall goal.

## Cooperative Review

- Merge owner: `/root` in `/tmp/simple-font-sync-20260727`.
- Final reviewer and generated-manual acceptance owner: highest-capability
  primary model after all lane handoffs.
- Parallel lanes: runtime admission/resume; manifests/distribution;
  shaping/material/configuration; production surfaces/SimpleOS; native
  2D/3D/performance; specs/manuals/evidence inventory.
- Frozen owners/interfaces: `FontRenderer`, `FontRenderQuad`, `FontRenderBatch`, `FontRenderConfig`, `FontExecutionPolicy`, `emit_portable_font_atlas_composite_kernel`, `draw_text_hud`, and `draw_text_world`. No parallel renderer, emitter, atlas, cache, or runtime facade.
- Frozen manual steps: `step("Load the pinned multilingual font manifest")`, `step("Accept exact-face-bound simple-script shaping")`, `step("Prepare one shared font batch for 2D and 3D")`, `step("Emit the selected font composite program and plan compilation")`, `step("Prove native submission and device readback")`, `step("Render legacy Web GUI and WM text through DrawIR")`, `step("Capture SimpleOS pinned-font pixels")`, and `step("Measure warm font rendering and resource bounds")`.
- Frozen setup/checkers: `setup_shared_font_fixture`, `expect_font_license`, `expect_language_coverage`, `expect_shared_font_batch`, `expect_selected_unicode_shaping`, `expect_backend_emission`, `expect_font_render_parity`, `expect_engine3d_font_readback`, `expect_simpleos_font_pixel_oracle`, and `expect_font_perf_budget`.
- Temporary helpers must call `assert(false)` or `fail(...)`; lower-model or sidecar output cannot accept done marks, exclusions, generated-manual quality, or broad verification.

## Runtime Boundary Decision

- runtime_need: one hash-bound pure-Simple full CLI that passes the essential-tools admission gate is a hard enabling dependency for font execution evidence. Minimal HirBlock, lowering-error collection, native-arena, and direct-entry fixes are retained solely to produce that prerequisite; they are not font evidence and do not promote a REQ/NFR row.
- facade_checked: every product/test lane must reuse `std.io_runtime`, app process/env facades, `FontRenderer`, Draw IR, Engine2D, and existing native backend facades.
- chosen_path: `reuse-facade`; then `add-smallest-owner-facade` only with evidence.
- rejected_shortcuts: raw local `rt_*` aliases, fixture-only device success, environment-only GPU proof, stale binaries, Rust-seed production evidence, CPU mirrors promoted as native, and direct backend field pokes.

### Vulkan identity/synchronization completion decision (2026-07-28)

- runtime_need: expose the selected Vulkan physical-device UUID/LUID as one stable text identity shared by Engine2D and Engine3D, and retain the exact Vulkan error from failed fence-wait/wait-idle calls so device loss is observable after the call returns.
- facade_checked: the existing driver-identity string/hash and `rt_vulkan_get_last_error` facade were checked. Driver identity cannot distinguish two identical physical adapters, while wait-fence/wait-idle currently log and discard their errors instead of updating the existing last-error owner.
- chosen_path: `runtime-owned-change`; extend the existing Vulkan runtime/device facade and its existing Simple wrappers, then consume those owner fields. No parallel runtime alias or backend-local identity derivation is added.
- rejected_shortcuts: device name/driver hash presented as UUID, logical/physical handles presented as stable identity, fixture/env identity injection, backend label equality, and consumer-side parsing of log text.

## Phase

active-plan-remade

## Current Active Delivery Scope — SimpleOS Stage 2

The superseding delivery goal is `SIMPLEOS_STAGE2_FONT`: exactly ten focused
SimpleOS specs and ten canonical manuals. The current manual inventory is
`0 missing / 9 stale / 1 source-current / 0 accepted receipts`; only
`selected_arabic_spec` has a source-current mirror, and that does not replace a
sealed zero-stub receipt. The existing standalone Stage 2 docgen is diagnostic
only. Current source now prevalidates all 53 image files before staging
mutation, checks the exact 59-source pin set, uses native-safe staged
FontRenderer material in fresh-device Draw IR, and provides a reviewed
provenance-only `--stop-after-stage2` plus sealed scoped-tool producer. These
changes remain execution-unverified. Completion is blocked on a clean
checkpoint, fresh canonical Stage 2, sealed runner/docgen,
followed by all ten focused executions and x86/RV64 SimpleOS QEMU
framebuffer/input evidence. Stage 3, Stage 4, non-SimpleOS GPU hosts, and the
broader cross-platform matrix are deferred from this delivery scope. Current
status is `SIMPLEOS_STAGE2_FONT: BLOCKED`; the broader `STATUS: FAIL` is
unchanged.

## Compiler-enablement boundary

`HIR-BOOTSTRAP-NIL-001` is not a font requirement. Its minimal current-source
repairs and focused source contracts are retained only because the admitted
pure-Simple CLI cannot otherwise be produced. The authoritative diagnostic is
`doc/08_tracking/bug/shared_font_stage4_stale_compiler_backfill_2026-07-26.md`;
delivery and essential-tools admission of one usable pure-Simple full CLI remain
blocking prerequisites for runtime-dependent font evidence.

## Log

Historical resume/cache/bridge imperatives below are superseded by
`current-truth-2026-07-28` and `compiler-perf-repair-2026-07-28`: no listed
historical artifact is an eligible parent or cache, and this window permits no
Rust-seed producer/acceptance run, fourth producer, or full bootstrap.

- active-simpleos-stage2-scope-2026-07-29: Supersedes the broad current-delivery
  interpretation without rewriting its history. The active goal is
  `SIMPLEOS_STAGE2_FONT` over ten SimpleOS specs/manuals. Inventory is
  `0 missing / 9 stale / 1 source-current / 0 accepted receipts`; the existing
  standalone docgen is diagnostic only. A fresh sealed Stage 2 runner/docgen,
  ten focused executions, and x86/RV64 QEMU framebuffer/input evidence remain
  blocking. Stage 3/4 and cross-platform GPU rows are deferred for this
  delivery; `SIMPLEOS_STAGE2_FONT: BLOCKED` and broader `STATUS: FAIL` remain.
- active-simpleos-stage2-host-closures-2026-07-29: Parallel implementation and
  review closed the host-side staging and fresh-device material defects,
  added exact Arabic/Hindi batch identity checks, and added reviewed
  provenance-only Stage 2 stop/scoped-tool evidence contracts. No runtime,
  docgen, image, or QEMU command ran. The next eligible action is a clean
  checkpoint followed by one bounded `--stop-after-stage2` producer.
- dev-remake-2026-07-29: Replaced stale hard-coded 42-manual/46-command
  acceptance counts with a current-tree inventory requirement, preserved the
  frozen shared owners and fail-fast helper policy, and assigned six bounded
  parallel audit lanes before implementation. Current pushed handoff HEAD is
  `90cef240c91c4ec31ffd1aebbb95520d4cebec86`; successor3 was built from source
  checkpoint `502b70b54602bc451ca11f10d8935723bbad5018`. Temporary Stage 2 successor3
  `dd7e747ad1e22bb71d46c5737d20d2d250146af70fdd3c621f66d7ab57ca26cf`
  is diagnostic only. The Option admission smoke exhausted its three-cycle cap
  and must not be repeated in this session.
- plan-reconcile-2026-07-29: current isolated head is `f289a4529aa`, not the
  historical successor3 checkpoint. Static receiver ABI repairs let a fresh
  Stage-2 build finish 693/0 and restore a valid LLVM module/function header.
  The permanent `Option<Box>` probe now stops at `llc` because `%l2` is used
  before its struct aggregate is emitted. P0 remains compiler-only and blocked
  pending a fresh root-cause repair; no font evidence, Stage 3/4, or docgen is
  promoted.
- parallel-plan-audit-2026-07-29: Six bounded agents independently audited
  runtime admission, manifests, shaping, production surfaces, native/performance,
  and canonical manuals. The current graph is 46 commands
  (preflight+B6+C18+D12+E9); 42 changed canonical font manuals are 19 missing
  and 23 stale, and four compiler-prerequisite manuals are missing. Static
  routing is source-complete except the still-blocked REQ-011 compatibility
  bitmap row; no speculative product edit is assigned before admitted runtime
  evidence. Unavailable host/device rows remain blocked.
- option-admission-static-handoff-2026-07-29: Six read-only compiler lanes made
  the first `.?`/changed MIR `ExistsCheck` arm the shared static candidate for
  both successor3-driven smoke-compilation SIGILLs; the exact first executed
  violation remains unproven. The tracked blocker freezes three required
  invariants: presence-before-unwrap with raw payload-or-nil result, never
  decode the outer Option handle as its payload, and decode optional-struct
  `if val` independently of an I64-only MIR-type gate. One fresh A/B/C
  ExistsCheck/if-val/field diagnostic batch distinguishes the first repair
  owner; this turn ran no fourth smoke, build, or source fix.
- dev: Remade the goal around all selected requirements and NFRs with twelve testable acceptance criteria, six parallel lanes, frozen shared interfaces, and explicit unavailable-host policy.
- verify: At HEAD `7a161abfabb` plus the current working changes, no fresh Stage 4 CLI is admitted. The frozen B4+C13+D5+E4 inventory is 26 executable sources, 18 present mirrors, eight missing mirrors, 12 stale mirrors, six same-revision but unverified mirrors, and zero retained docgen logs; no requirement is marked pass. Static scans found no prohibited placeholder pass, and `doc/06_spec` contains no executable `_spec.spl`.
- bootstrap: `e331a5700ab`, integrated as `7a161abfabb`, fixed impl-method accumulation and added direct 0+2/1+2 regression coverage. The final cycle-3 Stage 4 check advanced `compiler.spl` from count 0 to count 15 and localized the remaining nil receiver after `driver:errors-read:done`, inside the error collector. A typed-index collector fix and direct recovered/fatal error regression now exist in the working tree but are bootstrap-unverified. The three-check cap is reached, no further retry is permitted this session, and the full CLI is absent.
- implementation: The working tree also canonicalizes HIP to ROCm on the prepared font batch, makes degenerate Simple Web parsing fail closed instead of presenting blank success, and lowers nested WM content frames as clipped IMAGE commands. These changes and their direct specs remain unverified and do not change any REQ/NFR status.
- runtime: The only retained pure-Simple full CLI (`04a38e21d6fbd86149d46d3ee2d761349f8ad29b02c5037a8eb589b6a1b9e4e0`) crashes in `test`, `run`, and docgen because its stale two-argument `rt_env_set` code is ABI-incompatible with current callers. No healthy prebuilt replacement was found; no bootstrap was started by this goal.
- repair: Bounded Rust-seed diagnostics exposed and led to removal of a stray `EOF` token, two multiline shaping parse defects, and a forgeable selected-memory face sentinel. The selected path and actual parsed blob are now revalidated against the pinned registry SHA-256 at shape time, with arbitrary-byte, fabricated-sentinel, and path-substitution negatives. These diagnostics are repair evidence only, not acceptance PASS.
- verify-static: At rebased source checkpoint `deb90cd8a9c`, both direct-runtime guards, both numbered-artifact guards, `git diff --check`, and the zero-executable-specs-under-`doc/06_spec` layout gate passed. An independent third-cycle review accepted the selected-memory trust-boundary repair.
- verify-runtime: The third bounded Rust-seed repair diagnostic still reports `Unexpected token: expected expression, found Dedent` in `ot_layout_apply.spl`; retained log: `build/test-artifacts/shared_multilingual_gpu_fonts/repair-diagnostic/simple_web_html_layout_result_run.err`. The three-cycle cap is reached, so this parser result, deployed-runtime recovery, docgen, native readback, QEMU, and performance remain open rather than triggering another retry.
- runtime-external: The external Stage4 log spells the source as `src/std/nogc_sync_mut/env/variables.spl:364`, but `src/std` is the tracked `src/lib` symlink; the canonical tracked path is therefore `src/lib/nogc_sync_mut/env/variables.spl:364`. Its stale expression-position match was `val start = match dollar_pos: Some(offset): pos + offset; nil: break`. A clean current checkout already uses the safe Option form (`val dollar_idx = dollar_pos ?? 0` after a non-nil guard), proving the external dirty workspace was stale rather than identifying a new font or parser change. The external workspace `/home/ormastes/dev/pub/simple-bootstrap` produced compiler-only Stage3 SHA-256 `704f67af420bd8788dda809b46112d0a9a76cec64601ebfe2a6958a894aa380f`, then failed that stale source. No full CLI was produced. Retained log: `/home/ormastes/dev/pub/simple-bootstrap/build/mini_builds/bootstrap-memory-lexer-fix-stage4-cycle2.log`, SHA-256 `1a6e74e630d3341898cecfe9e785f9802527b96ae2372583531c8f52d17f09a9`. Resume with a clean current compiler checkout and one Stage4 full-CLI admission (`test`/`run`/docgen); this font goal must not retry the stale external workspace or bootstrap blindly.
- runtime-clean-current: The isolated clean-current lane at HEAD `16ebfdb6410` continued past the earlier advisory memory warning by reusing retained pure-Simple Stage3 compiler `/home/ormastes/dev/pub/simple-bootstrap/build/bootstrap-memory-lexer-fix/stage3/x86_64-unknown-linux-gnu/simple` and one preserved incremental cache. After two bounded compatibility repairs in that isolated workspace, cycle 3 reached `src/app/office/pptx_export.spl:515` and failed because the retained parser treats local identifier `ce` as a reserved token in `if ce < 0`; no object or full CLI was produced. The three-cycle cap is reached. Authoritative log: `/tmp/simple-clean-cli-20260726/build/mini_builds/full_cli_incremental_cycle3.log`, SHA-256 `0834487c195ae382dc59844d78f5578e4e67cbde3c71f44b2b7c977949b0feb9`. A separate compiler/runtime owner must resume from the retained cache, resolve that admission blocker, and run `scripts/check/check-bootstrap-essential-tools-smoke.shs` against the exact fresh binary before focused font verification resumes.
- audit-2026-07-27: Recomputed the branch inventory as 32 changed/new executable font specs with 13 missing mirrors, 19 stale mirrors, zero current mirrors, and zero retained docgen logs after adding the four REQ-016 full-layout specs and changing the selected-Devanagari policy spec. The rebased and pushed branch checkpoint is `1d75d521b775c94a27b5703b02894eba6dc74aff`, matching `origin/codex/shared-font-all-items-20260726`; AC-12 still requires its completion-time fetch/rebase and file-count guard. No REQ, NFR, acceptance criterion, phase, or overall status is promoted to PASS by this audit.
- sync-2026-07-27: The reviewed checkpoint was rebased and pushed as `397afaaee3bb3d2a5db79fd3ce5b0341901936c8`, matching `origin/codex/shared-font-all-items-20260726`; its tracked-file guard increased from 106,913 to 106,979. `origin/main` continued advancing afterward, so AC-12 remains open for the completion-time fetch/rebase/file-count gate. The deterministic docgen manifest now contains all 32 changed specs, including the four REQ-016 focused specs and `selected_devanagari_spec.spl`.
- p0-runtime-admission-2026-07-27: The isolated worktree `/tmp/simple-pure-cli-font-20260727` at HEAD `1d75d521b775c94a27b5703b02894eba6dc74aff` ran three bounded incremental full-CLI cycles with retained pure-Simple Stage3 compiler SHA-256 `704f67af420bd8788dda809b46112d0a9a76cec64601ebfe2a6958a894aa380f`. No candidate ELF and no native-cache object/file were produced. Cycle 3 stopped in `src/std/nogc_sync_mut/compression/gzip/lz77.spl`: line 104 binds the reserved keyword in `val match = ...`, and line 105 uses it in `val distance = match[0]`, which the retained parser reads as a `match` expression and diagnoses first as `expected :, got Newline`; the line-106 `length` diagnostics are recovery cascades, and `length` is not reserved. Retained and clean-current token tables are byte-identical at SHA-256 `cfea0c9e2063eae474913ee9cbfd585d29dfd50323c24c375d23656b884119da`, and the three logs have a stable parse prefix through 801 of 1,309 unique physical sources. Logs are `build/mini_builds/full_cli_incremental_cycle1.log` (`82a5b6bf68efc867e7e8cf4107ebe29f14590ec422e7646a87eac10f1fdad389`), `full_cli_incremental_cycle2.log` (`c1e4e61d1cf919478793017859c09cc021938863a651ab198e48f37759f1f8dd`), and `full_cli_incremental_cycle3.log` (`4bab47a3a0ff2164508db9ada5433cfbe85b8fdeb100756328b8869450e39dc7`). Next session must batch-preflight the canonicalized closure, build a noncommitted compatibility bridge in a fresh detached worktree, then use that bridge to build and admit the clean font commit. Only the two genuine current-language `class` local corrections in `src/lib/skia/feature/shaper/ot_layout_gpos.spl:123,602` belong in the font branch; the 13-file bridge overlay remains isolated, uncommitted, and unmerged, and unrelated `gradlew.bat` work is not part of that count. The three-cycle cap is reached, admission remains blocked, and no runtime-dependent font row or overall status is promoted.
- p0-runtime-bridge-2026-07-27: Detached worktree `/tmp/simple-cli-bridge-20260727-2` at `397afaaee3bb3d2a5db79fd3ce5b0341901936c8` used the same Stage3 SHA-256 `704f67af420bd8788dda809b46112d0a9a76cec64601ebfe2a6958a894aa380f` and an isolated, uncommitted compatibility bridge. Three bounded cycles cleared the prior reserved-keyword/multiline-boolean blockers and the first `FileTreeState` HIR gap, but produced no ELF and zero cached objects. Cycle 3 raised the per-file limit to 180 seconds and stopped because 9,716-line `src/app/office/sheets/formula.spl` still timed out and `src/lib/editor/70.backend/gui_backend.spl` next lowered `SettingsViewState.categories` as `ANY` in `gui_render_settings_html`. Log SHA-256 values: cycle 1 `1a4c04ee995bb80ac55e3650e7088e773326f8b16bc25d9c8952d016b3886def`, cycle 2 `43e9dff528a8ce0f33746be503368bcbd416c6eb22c85c43867bcc06d89adc7a`, cycle 3 `09e3e54a99ab7d76681ca2a27cd285b8ebb71932b27fed65a15ee133c0508c12`. No smoke ran because no candidate existed. The cap is reached; a fresh compiler/runtime owner must resume from these blockers without merging the bridge into the font branch.
- p0-runtime-bridge-continued-2026-07-27: Detached worktree `/tmp/simple-cli-bridge-20260727-3` at `fefcfe011fc0053d0ab3e01a13005bb841db5023` used the same retained Stage3 compiler and an isolated bridge that removed eager Office/IDE entry closure, added the proven GUI type imports, and selected the complete Rust runtime archives. Cycle 1 retained 1,417 objects before a link-only failure exposed the incomplete default runtime bundle and missing GSUB `_sub_end`; cycle 2 cleared those blockers and stopped on one canonical CSS import plus the duplicate `ant-trace`/`ant_trace` module; cycle 3 cleared the full import/collision preflight and parsed 806 of 1,190 unique files before the retained parser rejected `loop.induction_var` in `auto_vectorize_analysis.spl`. Log SHA-256 prefixes are `b5db2444`, `567d1e0d`, and `7732687e`. No ELF or smoke result exists. The genuine bounded GSUB helper is in the font branch; all remaining compatibility edits stay isolated. The three-cycle cap is reached, and a fresh runtime owner must resume from the retained cache with the already-applied `loop` to `loop_info` bridge rename.
- p0-runtime-admission-final-2026-07-27: Detached lane `/tmp/simple-cli-admission-20260727-4` preserved all 106 sparse ABI values through exhaustive enum-to-number converters and reused 1,417 cached objects with retained Stage3 SHA-256 `704f67af420bd8788dda809b46112d0a9a76cec64601ebfe2a6958a894aa380f`. Cycle 1 cleared three tuple-destructuring loops (log SHA-256 `769acbbb1a10cc1cb825f1704a7e563118e42a3725290fcaff5a508fc6e4a7ae`). Cycles 2 and 3 both parsed the 1,190-file closure and lowered all 28 functions in `src/lib/gc_async_mut/gpu/engine2d/color.spl`, then trapped with `field access on nil receiver`, exit 132 (log SHA-256 values `024699a05dc5ebcd6452f0539b1f361294679b9a7b3039a7f0a8eee8df5f05ad` and `c63e11b391f2254971bb767a12f500d2107635ad230e8774528bb138874b68a3`). No Stage4 ELF, Stage5, or smoke exists; the three-cycle cap is reached and the bridge stays isolated.
- p0-runtime-diagnosis-2026-07-27: The earlier read-only inference that localized the failure to `HirLowering.lower_module`'s final diagnostic `eprint` is retained as superseded history. The authoritative kernel trap records instruction pointer `0x559924`, which disassembles to the `ud2` immediately after `MethodResolver.resolve_expr` masks its incoming `expr` argument and detects nil or a low-tag-only value. `color.spl` completed all 28 HIR functions; its first resolution-order function, `color_black`, has the Call tail `rgb(0, 0, 0)`. The stale `resolve_block` consumer treated `HirBlock.value` as `Option` even though `HirBlock` is desugared to `has: bool` plus a mandatory `HirExpr`, allowing that Call tail to reach `resolve_expr` as an invalid extracted value. Current source integrates the ten-site invariant repair: five consumers now gate on `block.has`, and five synthetic constructors now provide explicit `has` plus a typed tail or `NilLit` sentinel. The typed indexed lowering-error collector is also integrated. These source fixes are not execution-verified: this correction ran no test or build, did not rebuild Stage3/Stage4, and did not retry admission. No Stage4 ELF or smoke exists, the pure-Simple CLI gate remains blocking, `verify-pending` remains unchanged, and overall verification remains `STATUS: FAIL`.
- req-016-full-layout-2026-07-27: REQ-016, four focused specs, and the changed selected-Devanagari policy spec expand the authoritative manual scope to 32. Source integration now rejects nested type-7 ExtensionSubst, composes nested contextual GSUB index remaps, threads GPOS ppem/coordinates/LangSys and one shared data budget, separates post-scale Device pixels from VariationIndex design units, resolves PairPos offsets from the owning subtable, preserves device fields across GSUB/rollback, and keeps unsupported high-level complex-script preprocessing fail-closed. Independent P0/P1 review is clean after the focused regressions, but no admitted-runtime result or regenerated manual exists, so REQ-016 and the overall goal remain open.
- spec-hardening-2026-07-27: Independent review accepted the shaping/material and surface/native stacks only after making selected font identity non-vacuous, asserting Arabic/Urdu/Hindi direction, and carrying exact Web advance widths through the proof validator. The first manifest/distribution rewrite was rejected because it imported private production symbols and added heavyweight fixed-`/tmp` staging to unit specs; its replacement retains only frozen step vocabulary and canonical matcher changes. Behavioral distribution evidence remains blocked on an admitted runtime and a sound public entrypoint. These are source-quality results only and do not promote any runtime-dependent row.
- evidence-graph-audit-2026-07-27: Read-only checkpoint audit validated 32 unique changed docgen sources, 19 present/13 missing canonical mirrors, and unique retained log names. The corrected runtime graph has 37 unique focused executions: B6, C17, D10 including the previously omitted Engine2D prerequisite, and E4. Essential-tools lint/duplicate probes execute only inside their admission gate; expected-failure calibration is `set -e` safe and retains commands, identity, streams, and exits under the canonical `runner-calibration` path; every focused run retains command/streams/exit and refuses overwrite. Runner source commit `2ac3be166b2` forwards only the ten reviewed native variables. Web receives its immutable run ID, admitted Aetheric Simple binary, and absolute retained production proof; x86 and RV64 receive their exact child-process inputs, while their live wrappers execute only inside their focused specs. This is an execution-contract correction only: no command ran, no manual was generated, and `verify-pending` / `STATUS: FAIL` remain unchanged.
- p0-runtime-repaired-compiler-cap-2026-07-27: Direct bootstrap compiler paths now use the existing native-AST-arena guard, with source-contract and embedded-NUL regressions; independent review found no P0/P1 issue. The third and final bounded compiler cycle supplied `SIMPLE_NATIVE_ARENA_DECLS=1` to retained pure-Simple Stage3, clearing the prior Rust environment NUL panic and reaching HIR lowering, but then stopped with exit 132 and `runtime error: field access on nil receiver`. No candidate ELF or smoke exists. Retained log: `/tmp/simple-cli-admission-20260727-6.isfZoU/build/mini_builds/minimal_repaired_compiler_final_fb09.log`, SHA-256 `5cd89facfb881ee5a5f5003941e9bdf486f87b90dc0fe36573ec6e7482b5e034`; the exclusive cache remained at 675 objects. The three-cycle cap is reached, so no retry or full-CLI build ran; `verify-pending` and `STATUS: FAIL` remain unchanged.
- evidence-runbook-correction-2026-07-27: Supersedes the historical 32-manual/37-command accounting without rewriting those earlier log entries. The authoritative scope is 34 manuals (14 missing, 20 stale) and 39 focused commands: one runner-contract preflight, B6, C17, D11, and E4. The focused helper now returns any nonzero spec exit before marker validation. Docgen uses clean-checkpoint and admitted CLI/core-C validation, immutable attempt/refusal semantics, command/stdout/stderr/exit retention, spec and generated-manual SHA-256 fields, and an explicit complete/`0 stubs` gate. The newly accounted sources are `test/01_unit/lib/test_runner_result_wrapper_spec.spl` and `test/01_unit/os/drivers/framebuffer/simpleos_wm_qemu_evidence_contract_spec.spl`; no command ran, no manual was generated, and `verify-pending` / `STATUS: FAIL` remain unchanged.
- host-independent-hardening-2026-07-27: The remaining interpreter HirBlock consumer now gates only on `block.has`; the live VHDL direct compiler path requires native-arena enablement and exact set/unset restoration; and the manifest spec uses public registry APIs plus the real bundle-root validator. Bundle validation now rejects root, intermediate `assets`, and `assets/fonts` symlinks before traversal. Focused source/behavioral regressions are present, but no admitted-runtime command ran, so no REQ/NFR or overall status is promoted.
- completion-audit-hardening-2026-07-27: Parallel completion review found GSUB 1–8 and GPOS 1–9 implementation scope complete, then added explicit REQ-016 trace links and the missing ExtensionPos-wrapped MarkToBase regression. The canonical Stage2/Stage3 producer and manifest replay now hash-bind `SIMPLE_NATIVE_ARENA_DECLS=1`; nil environment restoration removes the variable, and nil-span lowering diagnostics fail safely. The evidence runbook now rejects duplicate calibration, unchanged stale manuals, mixed checkpoints, and unsealed result sets through one aggregate 39-focused/34-docgen checker. These are source and evidence-contract repairs only: no build, focused command, docgen, manual acceptance, native receipt, or performance row ran, so `verify-pending` and `STATUS: FAIL` remain unchanged.
- p0-stage3-producer-audit-2026-07-27: The branch was rebased onto `origin/main` base `28130161c8c5eb92b4ce8fb25c2f4f299bd2b980` at synced HEAD `616454901c666f87e2cb7d70719d8d076ec81a1d`; the tracked-file guard moved from 107,659 to 107,718, and completion-time sync remains required. Source audit found the wrapper's Stage2→Stage3 transcript sets `SIMPLE_NATIVE_BUILD_RUST=1` and explicit `--source`/`--entry`, so `bootstrap_main` delegates to `rt_native_build`; advertised self-host provenance is not yet pure. The required unverified repair is one positional `bootstrap_main.spl` invocation with no Rust override/source/entry, mirrored in the args hash, manifest replay, and contracts. No build, PASS, phase/AC change, or requirement/NFR promotion occurred.
- p0-incremental-profile-2026-07-28: Rebased source on `958db10638d` produced pure-Simple Stage3 SHA-256 `a920123d919c4a4c384161e16fe35a1853d6e3da6bfd3a4a4e7291a2c072f04d` with 45 compiled, 647 cached, zero failed. The third bounded Stage4 cycle parsed 1,340 unique files and completed 50 HIR modules by 15m38s. Filtering retained module symbols to local ownership reduced observed RSS from about 21.7 GiB to about 7.0 GiB, but repeated eager package-sibling symbol registration remained too slow; no ELF or smoke exists. Log `build/native_probe/rebased-stage4-cycle3-final.log`, SHA-256 `92efd6d06e9c5e27ad45e98f472a953873bc78943bed43e2cb3e5855f2656fea`. The three-cycle cap is reached; runtime-dependent font evidence and overall PASS remain blocked.
- p0-lazy-package-siblings-2026-07-28: Replaced eager per-module registration of every direct sibling declaration with module-scoped demand registration for expression, named-type, pattern, async-type, and impl-owner lookups; genuine misses are cached and the existing import/re-export/type-method/trait-default owner remains unchanged. The focused unit regression proves used bare sibling names resolve while `unused` stays absent, and a disk fixture records the native package shape. One pure-Simple incremental compiler build compiled 693 files with zero failures in 647.3s (SHA-256 `16f89715874448595f91a6a39043222c1967e320e9b73a9d519e61db4ab2c4c4`), but its 43 compatibility stubs made the produced bootstrap compiler crash on a second native-build invocation. The full-Stage4 cap remains reached; no full CLI, smoke, focused font execution, requirement promotion, or overall PASS follows.
- current-truth-2026-07-28: The implementation checkpoint under verification is `269f46387e1`, rebased on `origin/main` and merge-base `62e71c81f1e`, with the tracked-file guard stable at 107,946 -> 107,946; current evidence working changes follow it without self-recording an uncreated commit hash. Current source contains the one-pass package sibling index and Stage3 pure positional/native-all/environment-forwarding repair; it also contains, but has not runtime-verified, GPOS VariationIndex `LONG_WORDS`, degenerate Web fail-closed behavior, and ancestor-clip/nested-frame tests. All three bounded producer cycles are exhausted; the last stopped after 17m29s at maximum RSS 23,943,204 KiB without an output, and no fourth cycle is permitted. The external `c167e250` Stage4 ended `EXIT=143`/`SIGTERM` with no full output; its log SHA-256 is `5a49ab01a7f7db6fc112c77c605c4760ff1a68f6929e5cf1e7037deef8d1c1d7`. Its stale Stage3 `01ef253d...3c3c7` records `full_cli_status=separate-not-proven` and is historical only, so no eligible external parent or admission route exists. The executable-manual inventory is 34 total: 14 missing, 20 stale, and zero current; four frozen-step corrections and nine canonical-matcher fixes are source-complete but runtime-unverified, with assertions preserved and no Simple run. The corrected immutable graph is 39 commands with C17. The matrix remains zero pass, 15 active, and nine blocked; phase remains `verify-pending` and overall verification remains `STATUS: FAIL`.
- compiler-perf-repair-2026-07-28: Corrected the SPipe/bootstrap guidance so focused or incremental builds are the default and a full bootstrap is required only when changed seed/runtime inputs demand it; the installed-command consistency check passed. Bounded inventory found no eligible current full CLI, proven external parent, or reusable cache with a valid parent/source receipt, so no fourth producer is permitted. Source fixes now forward and exactly restore focused Stage4 low-memory state, build one direct package-sibling owner index per lowering pass with the legacy re-export fallback retained, and replace per-access qualified-function key materialization with a direct index. Independent low-memory, sibling-index, qualified-index, combined, and static reviews passed. Rust-seed diagnostics are diagnostic only: `symbol_display_name_spec.spl` passed, while `bootstrap_focused_native_build_spec.spl` and `resolve_import_symbols_spec.spl` stopped before examples on the retained seed parser error in `module_lowering.spl` (`expected Comma, found FString([Literal("}")])`). No pure-Simple acceptance test, measured performance gain, font command, or docgen ran. The font graph remains 39 commands/34 manuals; all three canonical prerequisite perf-repair mirrors outside that graph are missing (a stale legacy-path resolve-import copy does not satisfy the canonical path). Phase remains `verify-pending`; `STATUS: FAIL`.
- continuation-host-independent-audit-2026-07-28: Parallel audits fixed owner-relative GPOS PairPos Device lookup, honest hosted compatibility provenance, typed selected-font SimpleOS rejection, Engine3D one-slot font ownership/caller-owned plan and batch transfer, and the public out-parameter export chain; independent reviews report no remaining P0/P1 in those paths. Lane E now requires hardware device type plus stable physical identity, keeps AA values as limits, uses immutable attempt roots, and fails its aggregate before focused admission with `font-owner-fault-producer-unavailable` until tracked Vulkan font-owner fault production and a public device-loss facade exist. The inventory is still 34 font manuals (14 missing, 20 stale, zero current) plus three missing compiler-prerequisite mirrors. No admitted full CLI exists, no runtime/docgen/native/QEMU/perf row ran, and the matrix is `0 pass / 0 active / 24 blocked`. The working checkout has 47 changed/new paths at tracked checkpoint `24a77be3c89a` and is 70 ahead / 74 behind `origin/main`; AC-12 completion sync remains open. Phase stays `verify-pending`; `STATUS: FAIL`.
- font-owner-runtime-state-2026-07-28: Supersedes the prior owner-producer blocker without promoting NFR-007. Engine2D and Engine3D now retain one native-safe scalar `i64` fault code/mask/sequence contract, device-loss sequence, post-fault owner identity, and committed CPU-fallback count on real production failure paths. Transactional atlas replacement keeps the prior live atlas until fresh upload and fenced use commit; unknown completion poisons the backend and retains potentially in-flight resources for shutdown recovery. Source review closed stale reset, unmapped production reasons, nested aggregate mutation, and direct-caller completion-safety defects; bounded allocation cleanup is source-only. The exact aggregate blocker is now `font-owner-fault-runtime-proof-unavailable`: no admitted current pure-Simple CLI/runtime receipt exists, and Vulkan3D fence-wait/wait-idle errors are not yet propagated by the pure-Simple runtime facade. No fourth producer, runtime spec, docgen, native, QEMU, or perf command ran. The matrix remains `0 pass / 0 active / 24 blocked`; phase remains `verify-pending`; `STATUS: FAIL`.
- font-owner-runtime-source-closure-2026-07-28: Supersedes only the source-gap clauses above. The 72-path working overlay adds stable Vulkan UUID/LUID identity through native/interpreter/runtime-symbol/SFFI owners and Engine2D/Engine3D evidence, retains wait-fence/wait-idle and submit failure text including `VK_ERROR_DEVICE_LOST`, reuses private plan/attempt/projection/atlas-alpha scratch, and keeps public attempt receipts detached. Owners, queued focused commands, immutable attempt roots, and `/root` review are assigned. These are source and static-contract results only: no admitted current-source CLI, runtime execution, docgen, native/QEMU/perf receipt, or completion sync exists. Counts remain REQ/NFR `0 pass / 0 active / 24 blocked` and AC `1 pass / 4 active / 7 blocked`; phase remains `verify-pending`; `STATUS: FAIL`.
- current-inventory-closure-2026-07-28: Supersedes the current 34-manual/39-command clauses without rewriting their historical records. Seven retained font source-contract specs expand docgen to 41 manuals (18 missing, 23 stale, zero current); six of them are new focused executions because `font_compat_spec.spl` was already in C17, expanding the graph to 45 commands (preflight, B6, C17, D12, E9). Owners, exact commands, immutable focused-attempt roots, and `/root` review are recorded in the current plan/report. No command or docgen ran, so phase and matrix status remain unchanged.
- current-inventory-refresh-2026-07-28: Supersedes only the current 41-manual/45-command accounting above. The changed `test/01_unit/lib/gpu/engine2d/font_runtime_config_spec.spl` is owned by lane C and adds one focused `run_focused_spec` plus one `run_docgen_spec`, expanding the exact graph to 46 commands (preflight, B6, C18, D12, E9) and 42 font manuals (19 missing, 23 stale, zero current). Its immutable roots remain `build/test-artifacts/shared_multilingual_gpu_fonts/focused/attempt-$FOCUSED_ATTEMPT` and `build/test-artifacts/shared_multilingual_gpu_fonts/docgen/attempt-$DOCGEN_ATTEMPT`, with `/root` as final reviewer. The compiler prerequisite manual set is separately four missing mirrors after adding `test/01_unit/compiler/driver/bootstrap_context_mir_source_spec.spl`; its exact `run_docgen_spec` uses `build/test-artifacts/shared_multilingual_gpu_fonts/compiler-perf-prerequisite-docgen/attempt-$DOCGEN_ATTEMPT`, owned and reviewed by `/root`. No runtime or docgen command ran, so phase and matrix status remain unchanged.
- settled-static-gate-2026-07-28: The final 75-path/18-spec host-independent gate passes diff/whitespace, canonical matcher and placeholder, Stage3 per-invocation cache-shape, font allocation/lifetime, current-count, and working direct-runtime ownership checks. Independent perf re-review passes after clearing non-promoted Vulkan fallback pixels before any ROCm/CPU fallback, bounding completion-gated Vulkan3D vertex buffers, and batching deferred Engine3D CPU fallback into one framebuffer snapshot/upload. Exact Rust UUID/LUID and device-loss classification tests pass as non-acceptance evidence. No admitted current-source pure-Simple CLI exists, so the matrices remain REQ/NFR `0 pass / 0 active / 24 blocked` and AC `1 pass / 4 active / 7 blocked`; phase remains `verify-pending`; `STATUS: FAIL`.
- producer-overlay-binding-2026-07-28: Supersedes the stale detached-checkpoint wording for the next producer only. A future fresh window must build the isolated overlay rooted at `24a77be3c89a`, verifying tracked `src scripts test` binary-diff SHA-256 `c5233e73b817e1ca915aa768f62856200b7fc43b542b2715d03ed7c5eab218b1` plus untracked owner `src/lib/common/gpu/font_owner_fault_receipt.spl` SHA-256 `032978d4654af8011f0d0bd084119dc7ed035bf8710d03c6928318c56a33817b`. The three-cycle cap remains exhausted; this records a future resume input and authorizes no producer in the current window.
- resumed-blocker-audit-1-2026-07-28: The external pure-Simple producer in `/home/ormastes/dev/pub/simple-fw-push-20260726` is no longer running. Its 14,096,524-byte Stage4 log ends without an exit marker or executable after 2,532,578 ms in HIR (`resolve_import_symbols:start` for `src/std/nogc_sync_mut/io.spl`, heap registry 313,996,479); log SHA-256 is `055553e27677de5f71bead29d7c8daa1156f2068551b29c2de83b8a61020423e`. Its retained Stage3 belongs to older non-ancestor source and is inadmissible. No post-gate candidate, core-C receipt, or overlay-matching artifact appeared in the goal roots or bounded known bootstrap roots. This is the first audit after goal resume; the prerequisite remains active and no producer is authorized in this exhausted window.
- resumed-blocker-audit-2-sync-2026-07-28: Delta-only admission checks found no post-09:22 artifact and no live producer targeting the exact overlay. `git fetch origin --prune` advanced `origin/main` to `55df7f16e54`, leaving the isolated branch 87 behind / 70 ahead. Independent scope review classifies all 75 paths as goal-owned with no conflicts. Push stops before commit/rebase: the sync contract forbids feature-branch pushes and the verification report remains `STATUS: FAIL` without the admitted pure-Simple runtime evidence required for main. This is resumed blocker audit 2; no producer, commit, rebase, or push ran.
- resumed-blocker-audit-3-2026-07-28: Delta-only checks after the audit-2 state record found no newer CLI, matching core-C, provenance/exit/status receipt, goal evidence artifact, or live producer targeting `/tmp/simple-font-sync-20260727` or tracked source-overlay hash `c5233e73b817e1ca915aa768f62856200b7fc43b542b2715d03ed7c5eab218b1`. Independent completion review confirms no authorized host-independent task remains: the 46 focused runs, 42+4 docgens, pure-runtime checks, native/QEMU/performance evidence, matrix promotion, and completion sync all depend on an admitted current-source pure-Simple CLI/core-C. This is the third consecutive resumed audit with the same prerequisite and the three-cycle producer cap still exhausted; the goal is blocked until external artifact state changes or a future producer window is authorized.
- current-overlay-stage4-window-2026-07-28: User resume authorized one fresh three-cycle producer window. The exact overlay, pure-Simple parent `a920123d...f04d`, runtime `822f4ef4...0605`, exclusive retained cache, and bounded full-CLI command were pinned. Cycle 1 reproduced the old parent's hot-loop profile overhead and was stopped at 24,951,108 KiB max RSS; cycle 2 was externally selected by host `earlyoom` at 10,623,772 KiB after unrelated jobs exhausted RAM/swap; resource-admitted cycle 3 completed HIR finalization but the 32 GiB cgroup OOM-killed `simple` at 33,483,972 KiB anonymous RSS before codegen/link. No CLI exists. The SPipe skill and bootstrap rule now accept a successful incremental Stage 4 directly and forbid a redundant clean replay, but this window produced no artifact. The three-cycle guard is exhausted; essential-tools admission, calibration, 46 focused runs, 42+4 docgens, native/QEMU/perf, sync, and push remain blocked. Phase is `verify-pending`; `STATUS: FAIL`.
- stage4-dead-flat-store-repair-2026-07-28: Parallel ownership, allocator, low-memory, evidence, test-design, and independent reviews localized a Stage 4-only duplicate owner: canonical HIR/MIR was retained together with legacy `_bootstrap_hir_*` and `_bootstrap_mir_functions` globals that only pre-Stage4 bootstrap consumes. Current source gates those dead accumulators off under Stage 4 without changing canonical module population or return-type registration. Unsafe deep-free was rejected because RuntimeValue assignment is pointer-aliasing and portable ownership cannot be proven. `SIMPLE_COMPILER_MEMORY_PROFILE=1` now emits coarse module/phase heap receipts without expression/function trace traffic. Seed-only focused HIR diagnostics passed 4/4; working and staged direct-runtime guards pass; the second seed diagnostic ended without a retained summary and is not claimed. No pure-Simple producer retry is authorized, so measured RSS, essential-tools admission, matrices, sync, and push remain blocked; `STATUS: FAIL`.
- stage4-memory-repair-sync-2026-07-28: GitHub fetch completed with `origin/main=f38be9fb072b`; the isolated feature branch remains 70 ahead / 156 behind with 77 tracked changes and two untracked goal artifacts. This checkout is not a jj workspace, verification remains `STATUS: FAIL`, and sync policy forbids feature-branch push or dirty rebase. No commit, rebase, bookmark move, or push ran.
- stage4-memory-repair-audit-2026-07-28: A second guided small-model review found no further safe Stage 4 high-RSS accumulator deletion: all large legacy flat HIR/MIR stores are now gated, while the remaining return/shape/parameter registries are required by cross-function type resolution. The canonical build guide now documents `SIMPLE_COMPILER_MEMORY_PROFILE=1`, and the RSS bug record pins one exact future incremental producer using `src/app/cli/main.spl`. The retained pure parent is compiler-only and rejects `test`, so its attempted guard run produced no acceptance claim. The producer cap remains exhausted; `STATUS: FAIL` and the push block are unchanged.
- stage4-memory-repair-blocked-audit-2-2026-07-28: Delta-only parallel checks found no new current-source CLI/core-C, provenance receipt, or live producer; all eight remaining verification groups still require that prerequisite. The sole worktree-related process was an orphaned 2 MiB resource monitor with no producer; it was terminated and its PID file cleaned. Remote `main` is locally current at `d84070b37309b86f7109103e3394c35223162a3a`, leaving the feature branch 70 ahead / 161 behind. Verification remains `STATUS: FAIL`, so commit/rebase/push remain ineligible. This is the second consecutive audit after the resumed memory-repair block.
- stage4-memory-repair-blocked-audit-3-2026-07-28: Final delta-only parallel checks found no current-source CLI/core-C, provenance receipt, live producer, compatible external binary, or newly unlocked host-independent acceptance task. Four retained external CLIs match none of the 30 current changed compiler/lib/app source files; retained local executables are older Stage3 parents, including one with 43 unresolved compatibility stubs. GitHub fetch advanced `origin/main` to `7750863be69`, leaving the feature branch 70 ahead / 164 behind, but `STATUS: FAIL` still forbids commit/rebase/push. This is the third consecutive audit with the same exhausted producer-window prerequisite; progress now requires an external eligible artifact or authorization of a future bounded producer window.
- stage4-memory-repair-producer-window-2-2026-07-28: User authorization opened one new three-cycle cache-preserving window bound to tracked `src scripts test` diff SHA-256 `f70d291e17b28eaf79e219fb8c4374ed8b68cbf945e508fbcfca8afce25a7946`, old pure parent `a920123d...f04d`, and core-C `822f4ef4...0605`. Cycle 1 was stopped at 12.92 GiB when an unrelated 32+ GiB build crossed the host safety floor. Cycle 2 ran cleanly for 1,197 seconds but was deliberately stopped at 26.98 GiB after proving that the old parent cannot execute the new compiler-source flat-store guard. Cycle 3 corrected the route to canonical positional Stage 3 (`src/app/cli/bootstrap_main.spl`, no source/entry flags) but systemd records a cgroup OOM kill after 1,409 seconds, with 31.16 GiB observed peak under the 32 GiB cap and no output. The window is exhausted. The next prerequisite is a current Stage 3 parent produced in a separately authorized bounded window; only that child can exercise the memory repair during Stage 4. Essential tools, all focused/docgen/native evidence, final PASS, sync, and push remain blocked; `STATUS: FAIL`. Remote tracking is current at `origin/main=0688048a2882`, leaving this branch 70 ahead / 186 behind, so no fetch or push ran.
- stage3-break-glass-memory-window-2026-07-28: User authorization opened a three-cycle Stage-3-first window with the retained positional cache. A 48 GiB cycle was manually stopped at 43.00 GiB maximum RSS when host residual crossed the declared floor; its warmed retry was cgroup OOM-killed at 47.95 GiB after 2,442 seconds. The final 64 GiB cycle was paused/resumed through host contention and used a calibrated 36 GiB residual floor, but systemd records another cgroup OOM kill after 3,222 seconds at a 63.73 GiB peak. No candidate was produced. The old Stage 3 path must retain flat HIR, so these failures do not exercise the Stage 4 flat-store repair. A follow-up root audit fixed a second no-GC leak by clearing/reusing twelve MIR function-local scratch containers instead of allocating replacements per function; two aggregate-valued dictionaries retain replacement initialization because seed construction can leave them nil. Its focused source regression passes 1/1 under bounded seed diagnostics only. The window is exhausted; pure-Simple runtime evidence and every downstream acceptance/sync/push gate remain blocked; `STATUS: FAIL`.
- stage3-current-parent-memory-result-2026-07-29: Current source removes recursive enum exemplar allocation from HIR type/statement and MIR statement/prescan dispatch; the focused source regression passes 1/1 and both direct-runtime guards pass. A bootstrap-only seed build produced pure-Simple Stage 2 `d7c8f500...4873` in 4m32.50s at 428,700 KiB max RSS. Its canonical positional pure-Simple Stage 3 run exited normally after 7m24.84s at 11,756,032 KiB (11.21 GiB), replacing the prior 63.73 GiB cgroup OOM with 6,859 ordinary HIR unresolved-name/type diagnostics. The memory prerequisite is cleared for the exercised path, but no Stage 3/CLI artifact exists; downstream verification remains `STATUS: FAIL` behind the separate HIR-resolution blocker.
- stage2-temporary-producer-stage3-cap-2026-07-29: At clean checkpoint `33b6658b0eaa94085125ce70a76e096f785c0ca3`, a temporary old-runtime compatibility overlay restored three old-parent-compatible source forms, keyed all entry-closure parse/cache sites by physical source identity, and used the old runtime's mutating `Dict.set` form. Its incremental Stage 2 producer is `build/native_probe/memory-dispatch-fix/stage2-goal-rootfix2-simple`, SHA-256 `0b1f741c9272a23c066ec42356949bfca7dca96605f8da4333fe0a8958b380ae`; it reused 689 modules, rebuilt 4, linked with zero failures in 1m18.72s at 304,068 KiB max RSS, and was never deployed. It is temporary producer evidence only and cannot qualify `run`, `test`, SPipe docgen, or release. The third and final positional Stage 3 cycle passed physical-source parsing and entered HIR, then exited 139 after `src/compiler/types/_TypeLayout/arch_and_verify.spl` declaration at 56.00s and 863,972 KiB max RSS. No artifact exists. Retained log `build/native_probe/memory-dispatch-fix/stage3-goal-rootfix2.log`, SHA-256 `456aa8bdd3b83e1ac6be0f092902491379c1d125ebf755af7091dcb84d260a79`. Independent P0/P1 review rejected shipping that old-runtime-only `Dict.set` workaround: the current six-file source/test diff (binary SHA-256 `aa209d0a2b51d523064f21529c32bcdeb17b2999d2d43a308dd55d06e0416901`) restores current dictionary rebinding and extends the existing physical-source dedup contract for all three physical-key sites; it is build-unverified because the cap is exhausted. Essential-tools admission, calibration, 46 focused runs, 42+4 docgens, native/QEMU/performance evidence, completion sync, and push remain blocked. Counts remain REQ/NFR `0 pass / 0 active / 24 blocked` and AC `1 pass / 4 active / 7 blocked`; phase remains `verify-pending`; `STATUS: FAIL`.
- simpleos-stage2-parser-runtime6-2026-07-29: Runtime6 passes from a clean provenance snapshot. Its archive and repeated archive share SHA-256 `a6d21c8fcf88d1ca788577a799564df022e917762abca1bad7736d3babb52782`; manifest SHA-256 is `6fd885abd620d180981f7a3f17be4328b2ded7dd802b41d4a347e222eff015a7`; `rt_string_free` self-check output SHA-256 is `143283b26f5acc4162b35b2294b91df0943f8876294e773248f7b0c38fc5879e`; coverage self-check receipt/output SHA-256 values are `a757b96d2692f285d0b4703e387129cb6945793896624bdc427c3a78ff7a4180` and `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. Each of `rt_dir_list`, `rt_dir_remove`, `rt_file_copy`, `rt_file_hash_sha256`, `rt_file_rename`, and `rt_process_exists` has exactly one strong `T` provider, `runtime_native.o`, clearing the prior asset/RV64 link blocker. Runtime6 supersedes Runtime5/Runner5. The third/final parser probe advanced Stage 2 discovery past `ot_layout_apply.spl:91:1` to `ot_layout_gpos_basic.spl:27`, exhausting that three-cycle lane. Runner6 calibration and focused execution remain pending; 0/10 focused specs are accepted. Counts/phase are unchanged; `SIMPLEOS_STAGE2_FONT: BLOCKED`; `STATUS: FAIL`.
- simpleos-stage2-runner6-calibration-2026-07-29: Runner6 SHA-256 `5f5245fdfb151c74436ee0c7f0cdd75808dcd7199da5298b738710633d80cb80` built 33/0 against the admitted Runtime6 archive/manifest. Calibration receipt SHA-256 is `22bf1bf5850c333677621672b023b4106f7378a394545d730a7c24c4c22af93d`; deliberate-red and zero-example contracts each passed exactly once. Runtime6/Runner6 supersede Runtime5/Runner5 and the older Runner4 calibration. The parser lane remains capped at `ot_layout_gpos_basic.spl:27`, so focused execution is still pending and 0/10 specs are accepted. Counts/phase are unchanged; `SIMPLEOS_STAGE2_FONT: BLOCKED`; `STATUS: FAIL`.
- simpleos-stage2-runner8-acceptance-2026-07-29: Accepted Runner8 supersedes Runner6 while retaining the Runner6 calibration entry above as history. Runner8 binary SHA-256 is `8096d0897994d7602b23a8eadc6252ed1f7ea00bb811ebfc5a0f3050cf282440`; its source SHA-256 is `093e013427a070f79889dc7dcb710063551533ae6cfc993e474d233f173a4c9e`; admitted Runtime6 remains `a6d21c8fcf88d1ca788577a799564df022e917762abca1bad7736d3babb52782`. Green receipt SHA-256 `6afa15355dd3e1a4c05183b0a9d552c4757a01384b07d092b141510f54be05df` records 1 example / 0 failures / exit 0. Provider receipt SHA-256 `b70fa412075a5a0a51593b68c02213ab9ce736115440f2259be0f8b9c2482466` records the runtime capsule at 7 examples / 0 failures / exit 0 and core I/O at 3 examples / 0 failures / exit 0. The parser lane remains capped at `ot_layout_gpos_basic.spl:27`; focused execution is still pending and 0/10 specs are accepted. Counts/phase are unchanged; `SIMPLEOS_STAGE2_FONT: BLOCKED`; `STATUS: FAIL`.
- stage2-runtime-authority-link-2026-07-29: A late post-commit Stage 2 link produced `build/native_probe/p0-admission-find-local-20260729/stage2-find-local-runtime-authority-core-simple`, SHA-256 `f2db67c629f1fe1505e8374f1c4d701d23a5d1868820f58df02d115d475dc075`; retained receipts record 690 reused, 3 rebuilt, 0 failed, and exit 0. The adjacent receiver repair makes `find_local_hir_type`, `is_runtime_value_local`, and `local_enum_type_id` explicit `me` methods with focused source-contract assertions. The artifact remains diagnostic and unadmitted without hash-bound provenance or A/B/C results, and the producer window remains exhausted, so no rerun or promotion is authorized. Counts, phase, and `STATUS: FAIL` are unchanged.
- current-inventory-refresh-2026-07-29: Supersedes only the current 46-command/42-font-manual/four-prerequisite accounting without rewriting historical entries. The changed REQ-011 `simple_web_window_renderer_spec.spl` adds one D focused execution and one stale canonical mirror, yielding 47 commands (preflight, B6, C18, D13, E9) and 43 font manuals (19 missing, 24 stale, zero current; A1+B6+C17+D12+E7). The changed `option_variant_order_source_spec.spl` adds a fifth, currently missing compiler-prerequisite manual outside the font graph. Stage 2 receipt machinery is source-present but no current manifest/sidecar is accepted; Stage 4 requires receipt-check PASS plus essential/aggregate sealing. No runtime or docgen command ran; phase and `STATUS: FAIL` are unchanged.
- current-inventory-draw-ir-refresh-2026-07-29: Supersedes only the current 47-command/43-font-manual accounting above. The changed `test/01_unit/lib/gc_async_mut/gpu/engine2d/draw_ir_adv_spec.spl` adds one lane-C focused execution and one stale canonical mirror, yielding 48 commands (preflight, B6, C19, D13, E9) and 44 font manuals (19 missing, 25 stale, zero current; A1+B6+C18+D12+E7). The five compiler-prerequisite manuals remain separate and missing. No runtime or docgen command ran; phase and `STATUS: FAIL` are unchanged.
- rv64-stage2-desktop-parser-window-2026-07-29: Canonical Stage2 attempt 6 passed at checkpoint `49673723101b` (binary SHA-256 `028f6ccb368a76a5911c07c87563980051418845647280ef7e575e3085043a64`; provenance SHA-256 `8b705d957f084577dea82864e60605bac0088821059185e2b68d5d1d3eeebf82`). Three bounded RV64 diagnostics removed the over-broad management-console closure, unused reserved-name `process.spawn`, and obsolete DMA match syntax, then stopped at `src/os/tools/pkg/pkg_repository.spl:158`, where Stage2 rejects an uninitialized `Result<text,text>`. Attempts 4–6 are retained and the exact resume is in `doc/08_tracking/bug/stage2_rv64_desktop_pkg_repository_result_initializer_2026-07-29.md`. No RV64 ELF, crop, exact-ten receipt, or manual was produced; `SIMPLEOS_STAGE2_FONT: BLOCKED` and broader `STATUS: FAIL` remain.
- rv64-stage2-desktop-nil-receiver-window-2026-07-29: Supersedes only the current blocker in the preceding entry. The package initializer is resolved; attempts 7 and 8 advanced through the network and driver-error parser blockers. Attempt 9 parsed those repairs, then Stage2 terminated with `runtime error: field access on nil receiver` (wrapper exit `132`, signal `4`, elapsed `34.59s`, maximum RSS `486380 KiB`, cache file count `0`). No RV64 ELF exists. Disassembly maps the `0x43c8ad` nil trap to `Config.set`; caller `0x8a40e0` is `CompilerDriver.parse_all_impl` line 693, which incorrectly assigned the `Dict.set` result and poisoned `entry_modules`. A one-line bracket mutation plus flipped existing source-contract assertions landed post-run and remain build-unverified. The RV64 `syscall6` repair also landed after the failed run and needs fresh-session verification. Attempts 7–9 are exhausted; exact resume is in `doc/08_tracking/bug/stage2_rv64_desktop_stage2_nil_receiver_2026-07-29.md`. Crop/QEMU, exact-ten, and manual evidence remain absent; `SIMPLEOS_STAGE2_FONT: BLOCKED` and broader `STATUS: FAIL` remain.
