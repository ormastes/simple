# Feature: Stage 4 SDK Capsule Bootstrap

## Raw Request
`$sp_dev with smaller model with guide. run pherallel. use full resource, incremental and debug mode. add test improve, bootstrap arch robust, and x86, arm/mac and simple os on qemu(uno q) bootstrap done. see bootstrap_sdk_capsule.md stage4_spdev.md`

Follow-up constraint: `do work on separate worktree and sync only cherrypick bug fix until x86 pass phase 4`

LLVM provider override: use the repo-managed Clang/LLVM 23.1 provider for the
pure-Simple LLVM backend and related platform lanes; do not admit LLVM 18
fallback evidence. The current reproducible provider is the signed
`llvmorg-23.1.0-rc2` pin until upstream publishes final 23.1.0.

## Task Type
todo

## Refined Goal
Produce a robust, reproducible pure-Simple Stage 4 bootstrap SDK capsule whose exact Linux x86_64 CLI passes Phase/Stage 4 admission and essential-tool verification before any ARM, macOS, or SimpleOS QEMU execution lane is allowed to advance.

## Acceptance Criteria
- AC-1: All work is performed on the dedicated `codex/stage4-x86-phase4` branch in `/home/ormastes/dev/pub/simple-stage4-x86-phase4`, with no files copied from or committed out of the shared dirty checkout.
- AC-2: Until x86_64 Stage 4 passes, synchronization consists only of individually reviewed and recorded `git cherry-pick` operations for relevant bootstrap bug-fix commits; no fetch/rebase merge, broad integration commit, documentation-only sync, or unrelated feature commit is admitted.
- AC-3: The lane records each cherry-picked source commit, its reproduced x86 blocker, dependency rationale, owned files, and focused regression evidence; a cherry-pick with unrelated changes is split or rejected.
- AC-4: The canonical Stage 3 bootstrap compiler is refreshed incrementally with `SIMPLE_NO_STUB_FALLBACK=1`, stable cache reuse, the bootstrap entrypoint, exact `simple-bootstrap 1.0.0-beta` identity, no generated-stub/failure markers, the required `run` rejection, and unchanged candidate-frontend admission hash.
- AC-5: A full-resource Linux x86_64 Stage 4 run uses retained debug/progress/RSS evidence, preserves reusable caches, runs each unchanged acceptance command at most once, and stops after at most three distinct verify/fix cycles.
- AC-6: Every newly exposed Stage 4 blocker is claimed before source edits, reproduced exactly, repaired at the pure-Simple owner before Rust/runtime, and covered by the exact reproducer plus one adjacent root-cause regression (or a recorded technical reason no adjacent case exists).
- AC-7: The exact fresh x86_64 Stage 4 binary passes candidate sanity and `scripts/check/check-bootstrap-essential-tools-smoke.shs`, including `essential_test_runner_smoke=true`, `essential_lint_smoke=true`, `essential_duplicate_checker_smoke=true`, and `bootstrap_essential_tools_smoke=true`; Rust seed, wrapper, stale, or stub-fallback artifacts are rejected.
- AC-8: The passing x86 candidate has a retained absolute path, SHA-256, build/debug/RSS logs, cache statistics, and rollback record; deployment is prohibited until AC-7 passes against that exact hash.
- AC-9: After AC-7, the reviewed future plan at `doc/03_plan/design/bootstrap_sdk_capsule.md` is integrated without bypassing the bug-fix-only x86 sync gate; it defines complete SHB authority, deterministic capsule admission, opt-in driver consumption, two-generation atomic promotion, performance inventory, and target-specific platform capsules without narrowing the full-source Stage 4 proof.
- AC-10: No post-x86 execution row advances before AC-7; afterward, Linux AArch64, macOS x86_64/AArch64, FreeBSD QEMU, SimpleOS x86_64 QEMU, SimpleOS AArch64 QEMU, and scoped RISC-V rows remain individually visible with prerequisites, exact commands, retained artifacts, and honest `pass`, `unsupported`, or `blocked` evidence. QEMU/cross results never substitute for native macOS or native AArch64 bootstrap PASS.
- AC-11: A SimpleOS QEMU compiler-in-filesystem PASS proves the Simple-native compiler/interpreter/loader payload and required filesystem paths, in-guest `/usr/bin/simple --version`, and in-guest compile-and-run of `hello world`; host compilation, fixed-command stubs, and marker-only apps fail closed.
- AC-12: Focused unit/integration/SSpec coverage is added or improved for capsule admission, cache invalidation, failure rollback, exact candidate identity, and the platform matrix; generated Markdown manuals are readable without opening the source spec and contain no placeholder pass.
- AC-13: Final verification checks the changed `doc/07_guide`, `doc/06_spec`, `.codex/skills/`, `.agents/skills/`, `.claude/skills/`, `.claude/agents/spipe/`, and `.gemini/commands/` contracts, plus the generated-spec layout and direct env/process runtime guards, before any done mark.

## Scope Exclusions
- Before AC-7 passes, do not execute or claim ARM, macOS, FreeBSD, RISC-V, or SimpleOS bootstrap completion; only host-independent inspection and explicit handoff preservation are allowed.
- Before AC-7 passes, do not implement or cherry-pick the post-Stage-4 SDK capsule plan; it is an inspected future contract, not a substitute for the current full-source x86 proof.
- Do not release, push, deploy, or combine unrelated dirty work as part of this lane.
- Do not edit Rust/runtime code unless evidence proves the pure-Simple boundary delegates correctly and the blocker is below it.

## Cooperative Review
- Lower-model sidecar `x86-fix-audit`: identify the minimal ordered cherry-pick set and reject unrelated commits; read-only until merge-owner approval.
- Lower-model sidecar `bootstrap-test-audit`: map x86 admission, exact/adjacent regression, cache, rollback, and essential-tools coverage gaps; read-only until merge-owner approval.
- Lower-model sidecar `capsule-platform-audit`: review the missing capsule architecture artifact and preserve gated ARM/macOS/FreeBSD/SimpleOS/RISC-V rows without advancing them; read-only until x86 PASS.
- Merge owner: primary Codex agent in `/home/ormastes/dev/pub/simple-stage4-x86-phase4`.
- Final reviewer: primary normal/highest-capability Codex after the exact x86 binary passes AC-7; lower-model findings cannot mark a row done or accept generated-manual quality.
- Current evidence interfaces: `Stage4CandidateEvidence` and `BootstrapPlatformEvidenceRow`. Frozen future capsule interfaces from the reviewed plan: `BootstrapSdkManifest`, `BootstrapSdkModuleInterface`, `BootstrapSdkBodyArchive`, and `BootstrapSdkProvenance`; sidecars must not rename them.
- Manual flow helpers: `step("Prepare the incremental Stage 3 compiler")`, `step("Build the full-resource Stage 4 x86 candidate")`, `step("Admit the exact Stage 4 CLI and essential tools")`, `step("Package the reproducible bootstrap SDK capsule")`, and `step("Preserve the post-x86 platform evidence matrix")`.
- Setup/checker helpers: `setup_stage4_x86_candidate`, `setup_bootstrap_sdk_capsule`, `check_stage4_exact_candidate`, `check_bootstrap_sdk_capsule`, and `check_stage4_platform_matrix`.
- Temporary helpers must use `fail("stage4 bootstrap helper is not implemented")` or `assert(false)` and may not return a passing placeholder.
- Generated-manual review owner: primary normal/highest-capability Codex.

## Phase
blocked

## Log
- dev: Created isolated Stage 4 SDK capsule bootstrap state with 13 acceptance criteria (type: todo); x86 exact-candidate PASS is the hard gate for all later platform execution.
- integration: Reviewed and cherry-picked the minimal local bug-fix chain needed to reach the retained x86 frontier, in source order: `06547b0fba9`, `31d01cd322d`, `4d2762f589e`, `8c98e07b05c`, `3b8c0a2a3c1`, `ff349710a09`, `5e7c57e9c89`, `e1ca20d3c81`, `72c0538e04e`, `036ed36ece9`, `5fb18e69c0e`, `e5ba8af64ee`, `13c42480aca`, `98354c3a0c5`, `a5f86301593`, `1a218e04c43`, `cfef9087884`, `7fa67048898`, `bc2d9bd760e`, `f5c3dec0ff9`, `2ac87065cab`, `37bf8c83c50`, `8786c846d9b`, `556fea9a444`, `f485c7dfe3e`, and `4505aec902a`.
- integration: The chain covers directory/facade/import ownership, driver payload identity, parser parity, stable target headers, legal boolean-pointer conversion, staged SSA stores, parser field tags, enum payload closure, trait terminal identity, cache preservation, and HIR progress. Documentation-only sync, SDK work, broad observability, and unrelated runtime/backend commits were excluded.
- integration: Modify/delete conflicts restored only the bug records carried by each fix. `bug_db.sdn` kept the current branch's curated database/CRC instead of importing unrelated historical rows. The sibling-enum test conflict combined the new enum-materialization assertions with the current direct optional-value matcher style.
- dev: Stage 4 cycle 1 loaded 2,116 sources and released all 1,431 surfaces, then exposed a strict nominal collision between compiler-local and shared EasyFix `LintLevel`/`LintCategory` terminals. Claimed `stage4_lint_enum_terminal_collision_2026_08_03.md` before production edits.
- dev: Kept `std.tooling.easy_fix.types` as the canonical enum owner, explicitly preserved the compiler facade, removed only the duplicate compiler enums, and narrowed EasyFix wildcards. The focused real-route contract changed from exit 70 before repair to exit 30 after repair; adjacent same-terminal and distinct-terminal HIR probes pass without weakening the resolver.
- dev: Full-resource x86 cycle 2 passed the lint collision, admitted refreshed Stage 3 `f09c014b96c69b5299b777f7385f9a15fc2fa076f07d1a70dcf7ab8469a6ea59`, loaded 2,116 sources, released 1,431 surfaces, reached HIR progress 64, and stopped on `app.devhub.cmd_wiki: unresolved name: process_run` after 24m25.47s at 7,364,992 KiB max RSS.
- integration: Cherry-picked isolated bug fix `dd6e502672b` as `53381757447`, preserving the curated branch bug database. It hoists every function-local no-op import in `cmd_wiki`; focused SMF then crossed the process blocker.
- dev: Added the pure-Simple `std.json` root compatibility facade with explicit canonical JSON owner re-exports. The compatibility/canonical contract compiled eight modules and exited 30; the capped real wiki SMF route contained no process or JSON error and exposed the separately claimed TUI terminal helper blocker.
- blocked-audit: Three focused TUI variants cleared the local HIR names but failed on staged-MIR substring/slice or the expanded string-core join/merge closure. Unverified TUI source edits were restored and cycle 3 was not started, per the mandatory three-attempt cap.
- dev: In a fresh scoped session, repaired the claimed TUI owner with an explicit typed `print_raw` ABI and `_terminal_fit_line`. Independent lower-model review rejected a superficially passing `for ch in text` variant because that loop is a recorded staged-native corruption path; the accepted code uses the established indexed `content.len()`/`content.char_at(i)` pattern.
- dev: Focused LLVM/core-C-bootstrap evidence compiled two modules with one cache hit in 1.82s at 157,184 KiB max RSS. The fixture exited 30, emitted exact stdout `abc`, preserved UTF-8 `café`, padding, and zero-width behavior, and wrote no stderr. Retained under `build/focused/stage4-tui-terminal/`; full x86 cycle 3 remains pending.
- integration: Committed only the TUI owner, focused native fixture, and claimed bug record as `6e9300b345a`; the persistent Android CRLF anomaly and lane state remain outside the bug-fix commit.
- blocked-audit: Final full-resource x86 cycle 3 passed the prior lint/wiki/JSON/TUI frontiers and reached HIR beyond progress 224, then failed in `compiler.backend.backend.compiler` because legacy `compiler.backend.backend_types` and modern `compiler.backend.backend.backend_types` contributed conflicting `BackendKind` and `CompiledSymbolKind` enum payload terminals. Claimed `stage4_backend_type_facade_enum_payload_collision_2026_08_03.md`; no production edit or cycle 4 was started.
- evidence: Cycle 3 exited 1 after 27m41.98s at 11,417,520 KiB max RSS. Refreshed Stage 3 SHA-256 is `63de1446a3a5ca95056f35c1ce79653fb8b251d91f713ba80256c4ac9ab2beac`. No Stage 4 candidate exists, essential-tool smoke was not run, and every post-x86 platform row remains blocked.
- dev: Fresh-session backend facade audit found no safe cherry-pick. Replaced the root modern-backend wildcard with explicit `CodegenTarget`, `OptimizationLevel`, and `CompiledModule` reexports, preserving the legacy physical owners and excluding modern `BackendKind`, `CompiledSymbol`, and `CompiledSymbolKind` collisions.
- evidence: The real `CompilerBackendImpl` focused route crossed both enum collisions. Its first two diagnostic attempts exposed separately claimed flat-registration gaps in `backend/env.spl` and `backend/codegen.spl`; the temporary environment import was restored after the focused cap. The bounded facade contract compiled 223 modules with zero failures in 58.08s at 1,430,848 KiB max RSS and exited 30 with empty stdout/stderr.
- integration: Committed the isolated backend-facade repair, focused native contract, and three directly related bug records as `4c03aca49e6`. No lane state, Android CRLF anomaly, unrelated source, or deferred import experiment entered the commit.
- evidence: Full-resource x86 fresh cycle 1 crossed the repaired backend compiler and reached HIR 448, then failed in `compiler.tools.fix.rules.impl_.lint_module` on non-canonical type annotation `int`. It exited 1 after 41m52.57s at 24,343,824 KiB max RSS; no Stage 4 candidate exists and essential-tools/platform execution remains gated.
- dev: Claimed `stage4_lint_module_unresolved_int_type_2026_08_03.md` before source edits. The repair owner is the EasyFix lint rule; resolver/runtime widening is excluded.
- dev: Replaced only `_brace_delta`'s non-canonical `int` return with `i64` and added the strict native `stage4_fix_lint_module_hir_contract.spl`. Final focused attempt rebuilt 1 module, reused 13 cached modules, failed 0, linked in 2.86s at 160,032 KiB max RSS, and exited 30 with empty output.
- blocked-audit: Executing the public EasyFix lint rules hung in two focused variants, including empty/early-return inputs. Claimed `easyfix_lint_rule_native_execution_hang_2026_08_03.md`, kept that production diagnosis outside the current type repair, and stopped focused iteration at the mandatory third-attempt cap.
- integration: Committed only the canonical EasyFix type repair, strict native HIR contract, bootstrap blocker record, and adjacent runtime-hang record as `69757e3aae7`. Lane state and the persistent Android CRLF anomaly remain outside the commit.
- evidence: Full-resource x86 fresh cycle 2 crossed the backend and EasyFix blockers, reached HIR progress 608, and stopped on unsupported method-level generics `Future<T>.map<U>` and `then<U>`. It exited 1 after 50m32.31s at 33,580,496 KiB max RSS; no Stage 4 candidate or smoke exists.
- dev: Claimed `stage4_future_generic_method_native_gate_2026_08_03.md` before production edits. Compiler-gate weakening, erased-generic whitelisting, facade narrowing, and `Any` substitution are excluded; the repair boundary is compiled/interpreter conditional ownership plus explicit immediate poll/match behavior for in-tree compiled consumers.
- evidence: Focused attempt 1 compiled the contained owner but exited 42 because `Future.from_value(41).poll()` returned a corrupted ready payload under native generic class/enum erasure. Claimed `native_generic_future_value_erasure_2026_08_03.md`; no false behavioral PASS will be attached to the declaration-containment repair.
- blocked-audit: Attempt 2's declaration-only fixture compiled nine modules and exited 30, but attempt 3 proved the proposed compiled/interpreter containment did not fail closed: an unsupported native `future.map(...)` call compiled with zero failures and its executable silently exited 1. The production Future, buffered-I/O, docs, and repository test experiment were restored after the mandatory third focused cycle.
- evidence: An interpreter regression command reported 25/25 tests green, but the executable self-identified as the Rust bootstrap seed despite its release path; that result is rejected under the pure-Simple tooling rule. No final full Stage 4 cycle, candidate smoke, capsule work, or post-x86 platform execution was started.
- dev: Fresh-session audit ruled out activating `compiler/40.mono`: parser/HIR drop method type arguments, specialization is identity/incomplete, generated functions are discarded, and generic class/enum layouts are not specialized. Claimed the existing Task #145 record for the observed bootstrap-flat fatal-error drop before new source edits.
- dev: The next focused repair will explicitly mark unresolved methods fatal and propagate fatal `MirLowering` diagnostics from all bootstrap flat/global/extra function-lowering paths. This is required before retrying interpreter-only `Future.map<U>`/`then<U>` containment; advisory MIR diagnostics remain non-fatal.
- dev: Implemented the claimed fail-closed MIR boundary and restored interpreter-only `Future.map<U>`/`then<U>` containment. Rewrote `Future.timeout` and all eight buffered-I/O consumers to explicit immediate poll/match snapshots; no native generic behavior is claimed.
- evidence: The fresh positive native contract built in 3.6s at 156,928 KiB max RSS and exited 30 with empty output, including a supported non-generic method control. The retained pre-repair Stage 3 compiler still accepted the negative `Future.map` build; that executable segfaulted with exit 139 and empty output. Compile-time rejection remains pending against the rebuilt Stage 4 compiler.
- evidence: The retained Stage 3 bootstrap executable exposes `native-build` but not the repository `test` command, so the new source regression could not be executed through that binary. It remains tracked adjacent evidence for the exact candidate test runner; the failed unsupported-command invocation is not counted as a test result.
- blocked-audit: Full-resource x86 fresh cycle 3 rebuilt/admitted Stage 3 but stopped in Stage 4 surface extraction on eight nested `Future.pending<Result<..., IoError>>()` parser errors in buffered I/O. It exited 1 after 13m56.18s at 1,719,308 KiB max RSS. Typed-local inference repaired that compact-expression parser boundary; the changed positive native contract rebuilt in 2.9s at 157,696 KiB and exited 30 with empty output.
- evidence: A focused Stage 2-3 refresh containing a narrow resolved generic-call metadata gate passed in 11m54.06s at 1,653,456 KiB; Stage 3 SHA-256 is `7faae3d57d6504aae2e685f6d78d87557fb56622aded2ab12f87ef3a609ff110`.
- blocked-audit: The third fresh negative probe still built and produced an executable. `nm` showed `Future.from_value` and `Future.is_ready` but no `Future.map`, so the resolved call still loses specialization metadata and targets a removed body. The experiment remains uncommitted/unaccepted. Per the three-cycle cap, no second full Stage 4, exact smoke, sync, or post-x86 platform work was started.
- evidence: A continuation rebuilt the pure-Simple Stage 2-3 chain twice. The final admitted Stage 3 SHA-256 is `8d767b0beb80e16da42f0a172fc8ebbc9f168b2e59f5fad3f1b342d687122517`; the second refresh passed in 11m31.73s at 1,643,712 KiB max RSS.
- blocked-audit: Three new negative probes tested explicit native-specialization markers, class/actor method declaration surfaces, and a chained-owner pre-MIR gate. All three still linked. Disassembly proves `spl_main` calls `Future.from_value`, `rt_option_map`, and `Future.is_ready`; the native worker's Rust LLVM `MethodCallStatic("map")` leaf fallback bypasses the pure-Simple gates. All experiments remain uncommitted/unaccepted. No full Stage 4, smoke, sync, or platform work was started.
- evidence: Fresh exact x86 Phase 4 cycle 1 reached HIR after 59m50.82s at 21,946,576 KiB max RSS and failed closed on `compiler.frontend.core.closure_analysis: unresolved name: range`. Cycle 2 crossed that local workaround, then reached the adjacent shared symptom `compiler.frontend.core.call_graph: unresolved name: range` after 1h10m20s at 22,105,500 KiB max RSS. The local workaround was removed; the repair now belongs to HIR call lowering.
- dev: `range(start, end)` is lowered before prelude name lookup to the existing `HirExprKind.Range`, so counted loops reach the existing MIR `lower_for_range` path. A standalone adjacent fixture compiled with the preserved pure-Simple Stage 3 compiler in 6.4s, linked without stub fallback, and both independent range loops executed with exit 0.
- dev: Claimed the Stage 3 provenance rename-scan performance bug. Authority hashing now disables Git rename inference with `--no-renames`; the focused integration test proves the argument is present and a tracked edit still changes the SHA-256 dirty fingerprint.
- integration: Fetched GitHub `origin/main` to `3004f398afb`. The validation worktree parent is a Jujutsu conflict snapshot with 1,541,469 pre-existing staged resolution paths, so it is not publishable. After x86 admission, only reviewed Phase 4 files will be recreated/cherry-picked onto a clean branch rooted at `origin/main`; the inherited index and Android CRLF anomaly remain excluded.
- evidence: One exact LLVM 18 x86 Phase 4 cycle remains under the fresh-session three-cycle cap. Essential-tool smoke, push, LLVM 23.1.0-rc2 migration, and all post-x86 platform rows remain gated on that result.
- integration: User replaced the LLVM 18 admission target with the completed GitHub Clang/LLVM 23.1 lane. The LLVM 18 cycle was interrupted after its admitted Stage 3 reached Stage 4 and completed source closure 1,355/1,355; it is retained only as diagnostic rollback evidence and cannot satisfy AC-7.
- integration: Created clean branch/worktree `codex/stage4-x86-phase4-llvm23-integrated` at the latest `origin/codex/clang-23-1-browser-demo`, replayed the 33 original clean Phase 4 bug-fix commits with semantic conflict review, and integrated the LLVM 23.1 bootstrap/SPipe/wiki contract commit. The `.jjconflict-*` parent tree and Android CRLF anomaly remain excluded.
- sync: Pushed the three-file documentation contract as GitHub branch `codex/llvm23-bootstrap-docs`, commit `fe5d2ba2db6`, after exact tree-size, conflict-tree, and conflict-marker guards passed.
- evidence: Parallel ad-hoc platform checks retained honest boundaries: FreeBSD 14.4 QEMU smoke PASS; RISC-V ELF object emission PASS but link/QEMU blocked by sudo-required cross zlib/zstd/tinfo; SimpleOS AArch64 QEMU readiness and compiler-admission self-test PASS but the full producer lacks isolated output; macOS ARM64 Mach-O object emission PASS but native macOS bootstrap remains unavailable on this Linux host.
- dev: The authoritative remaining path is now: finish focused regression review, build/admit the signed LLVM 23.1 RC2 provider, bootstrap the pure-Simple compiler through the required Cranelift seed boundary, then use that compiler with `SIMPLE_LLVM_PREFIX` for LLVM 23.1 native candidate and essential-tools evidence.
- evidence: Built and admitted the repo-managed LLVM provider at `build/toolchains/llvm-23.1.0-rc2` from signed tag `llvmorg-23.1.0-rc2`, commit `561093d94eb7156dea780c1c71a779824ef90e5b`. Verify-only reported Clang `23.1.0-rc2`, LLD `23.1.0`, llvm-config `23.1.0-rc2`, llvm-ar `23.1.0`, and llvm-objcopy present. Automatic backend/loader discovery was narrowed to LLVM 23/23.1 candidates; LLVM 18 and other legacy fallbacks are not admitted.
- blocked-audit: Exact x86 Phase 4 cycle 1 rebuilt the Cranelift Rust seed and failed closed during Stage 2 linking because `driver_hir_pipeline_lowering` called an unbound `log_build_progress`. Restoring the canonical append-only progress helper and explicit import repaired that blocker. Cycle 2 passed Stage 2 and Stage 3 sanity, then Stage 4 loaded the 1,726-source closure and segfaulted immediately after `phase2:surface:file:released path=src/app/cli/main.spl seq=1`.
- blocked-audit: A refreshed Stage 2/3 producer reproduced the identical first-surface segfault. The proposed lexer-lifetime hypothesis was therefore rejected and its source/test experiment was removed; the open bug record retains the exact logs and points at the remaining nested-carrier lifetime boundary. No third full cycle was started under the mandatory three-cycle cap, no x86 Stage 4 candidate exists, and essential-tool smoke remains blocked.
- evidence: The isolated FreeBSD 14.4 QEMU wrapper smoke passed with LLVM 23.1 after runtime overlay, PID, log, and cloud-init paths were made caller-isolatable. Linux-host ARM evidence passed macOS AArch64 Mach-O object emission and SimpleOS AArch64 QEMU readiness, but does not claim a native macOS or full SimpleOS bootstrap. The RISC-V aggregate retry is blocked until a valid new x86 candidate exists; the stale compiler failed in the aggregate's required ARMv7 capability probe and is rejected as evidence.
- dev: Native GDB localized the first-surface crash to lazily allocated AST mode slots retained by module globals after caller-owned transient-scope reclamation. Persistent one-element carriers for expression, statement, generation-hardening, and declaration modes repair that lifetime boundary; the next exact run released every surface and completed HIR.
- dev: The expanded full-CLI Stage 4 closure then exposed four command-only `use lazy` owners and two omitted CLI facade helpers. Ordinary entry closure retains its lazy-import exclusion; Stage 4 one-binary closure now includes lazy imports explicitly, with focused ordinary-versus-Stage-4 source tests.
- evidence: The first continuation Phase 4 cycle released 1,726 surfaces and stopped on ten lazy-owner/helper names. After the closure repair, cycle 2 reached 742 surfaces and rejected valid decimal `14695981039346656037u64` through the signed parser guard.
- dev: Decimal literals carrying the exact `u64` suffix now validate through `u64::MAX` and preserve their wrapped signed AST bit pattern. Unsuffixed and other suffixed literals keep the signed ceiling; the streaming lifecycle spec covers the FNV basis and maximum u64 as synthetic module source.
- evidence: LLVM 23.1 Stage 2/3 refresh passed sanity and provenance with SHA-256 `3704501018a1ba2da65d60d8d317d1f2ba3066de534b0a7eaa95ea1dc8527396` and `45b741c490d550464930c4a559f36c866ef6a92ae2c7868ec07c35c958e6776d`. Final Phase 4 cycle parsed and released all 1,351 closure surfaces, crossed the decimal-u64 blocker, and stopped in HIR on `check_simd_opportunities` and `easyfix_id_text` unresolved in `src/app/io/cli_lint_commands.spl` after 3m33.28s at 973,120 KiB max RSS.
- blocked-audit: The continuation exhausted its mandatory three Phase 4 verify/fix cycles. No fourth attempt, essential-tool smoke, post-x86 bootstrap, or `1.0.0-rc` release is permitted until a fresh scoped session resolves the two CLI lint import/closure names and produces an exact admitted x86 candidate.
- dev: Fresh bounded continuation replaced the CLI lint rules facade with the physical SIMD rule owner and canonical EasyFix accessors. Its focused graph crossed HIR/object generation; the deliberately narrow first contract stopped only on unrelated core-bundle runtime symbols.
- evidence: Full Phase 4 cycle 1 parsed all 1,351 surfaces and crossed the prior CLI lint blocker, then stopped on `check_file` and `cli_compile` in `_CliCommands/handler_commands.spl` after 3m03.87s at 974,304 KiB max RSS.
- dev: Removed the handler's cyclic `app.io.cli_commands.*` self-facade and imported run, compile, check, and compile-result symbols from physical owners. The focused contract compiled 724 modules, linked, and exited 30.
- evidence: Full Phase 4 cycle 2 crossed both handler names and stopped on `interpret_file` and `MemLeakEntry` in `compiler.tools.leak_check.main` after 2m59.83s at 973,876 KiB max RSS.
- dev: Replaced the shadowed empty `compiler.driver` facade with the in-process interpreter owner, bound `MemLeakEntry` to the exact `nogc_sync_mut` type returned by `parse_leak_dump`, and repaired adjacent external/internal runner owner imports. The focused contract compiled 736 modules, linked, and exited 30.
- evidence: Refreshed LLVM 23.1 Stage 2/3 passed sanity and provenance; SHA-256 values are `2d168cd80f92a3ab72e6bdca62172431a19ce5cf78184fd65f0438162f96bbc1` and `349bd00d7e180d72180a04eb414822d51619c6b3b018952bec69d6e8b85aa09a`.
- blocked-audit: Full Phase 4 cycle 3 crossed every repaired owner, parsed all 1,351 surfaces, and stopped on four non-canonical `float(...)` conversions in `compiler.tools.duplicate_check.main` after 2m57.79s at 973,224 KiB max RSS. The fresh-session three-cycle cap is exhausted; no fourth build, x86 candidate smoke, post-x86 platform run, or release was started.
- dev: Fresh continuation replaced all four duplicate-check `float(text)` constructors with canonical optional text parsing and explicit invalid sentinels. The focused native shard crossed HIR/object generation and stopped only at the narrow core bundle's unrelated `rt_http_request` link boundary; production cycle 1 crossed the repaired module.
- evidence: LLVM 23.1 Stage 2/3 refresh passed sanity and provenance in 10m39.08s at 1,370,280 KiB max RSS. SHA-256 values are `eb833cf10b0a7a5c5a9f73765ae6b68fd3f0b0d163813fb1f4858df3c7a328fd` and `3b86545d1092fabc12a23ca86a1531a501b679d4c85ff47f8cfa9bf43d2f89db`.
- evidence: Phase 4 cycle 1 crossed duplicate-check and stopped in `test_runner_main.spl` on physical IO owners, free `to_int`, and daemon `duration_ms` scope after 3m09.48s at 982,848 KiB max RSS.
- dev: Bound test-runner time/process/atomic-write calls to physical nogc-sync owners, used the canonical optional integer method, and measured daemon duration in shared branch scope. Cycle 2 crossed the module and stopped in `run_commands.spl` on lazy rule-facade names and undeclared raw output after 3m01.81s at 974,776 KiB max RSS.
- dev: Bound SIMD and short-grammar rules to physical lazy leaf owners and routed raw CLI output through canonical `Stdout.write_text`; added focused compilation contracts for duplicate-check, test-runner docgen, and CLI run commands.
- blocked-audit: Final Phase 4 cycle 3 crossed all repairs and stopped in `nogc_sync_mut/db_atomic.spl` on unresolved `_`, `file_atomic_write`, `file_lock`, `file_unlock`, and `parse` after 3m08.62s at 1,416,128 KiB max RSS. The three-cycle cap is exhausted. No exact x86 candidate, essential-tools smoke, platform promotion, full release verification, or `1.0.0-rc` release is permitted in this session.
- dev: Fresh continuation bound db-atomic IO and common-SDN dependencies to physical owners, replaced unsupported point-free row mapping, and migrated table extraction to the current two-field SDN payload. Its focused native shard crossed HIR/object generation in 2.12s and stopped only at the narrow core bundle's missing `rt_file_atomic_write`.
- evidence: Phase 4 cycle 1 crossed db_atomic and stopped in `_CliCompile/compile_targets.spl` on omitted `_driver_entry_import_dirname` plus three direct `rt_env_get` calls after 3m29.15s at 1,374,704 KiB max RSS.
- dev: Added the physical driver helper to the existing owner import and routed all compile-target environment reads through `app.io.env_ops`; the direct-env working-tree guard passed. Added a bounded help-path native contract.
- evidence: Phase 4 cycle 2 crossed compile_targets and stopped in `std.tooling.easy_fix.types` on 13 legacy `Int`, five legacy `Bool`, and one misplaced `FixReport` method block after 3m11.74s at 1,374,112 KiB max RSS.
- dev: Canonicalized only the reported EasyFix scalars and restored `FixReport.empty/format` to `impl FixReport`. The focused contract compiled and linked two modules in 1.47s, but execution retained an independent nil-receiver bug at exit 132 instead of weakening the assertions.
- blocked-audit: Final Phase 4 cycle 3 crossed all three repairs and stopped in `compiler.tools.formatter.main` on legacy `Int`/`Bool` annotations after 3m07.03s at 1,374,672 KiB max RSS. The three-cycle cap is exhausted. No x86 candidate, essential-tools smoke, platform promotion, full release verification, or `1.0.0-rc` release is permitted in this session.
- dev: Fresh continuation canonicalized the formatter's 11 integer and six boolean annotations, made the reassigned diff bound mutable, and refreshed the existing formatter source contract. A temporary focused contract passed HIR/object generation; strict `core-c-bootstrap` linking then exposed the separately tracked missing `rt_file_atomic_write` owner, so no executable PASS is claimed and the invalid narrow-runtime contract was not retained.
- evidence: Stable LLVM 23.1 Stage 2/3 refresh passed sanity, provenance, and Stage 2 native-build capability in 10m47.51s at 1,266,668 KiB max RSS. SHA-256 values are `389df5bcb6b64cb13186b39fd5803add1652bb96555af30f7a4a3f19b9b5823f` and `93411d66c06dda2bdb6e2a78e084b599c005ceba6c9e3b8104778afd54f38f7c`.
- evidence: Exact x86 Phase 4 cycle 1 crossed the formatter and stopped in `compiler.tools.fix.main` on legacy `Bool`/`Int` annotations after 4m29.78s at 1,373,112 KiB max RSS. The fix tool now uses canonical `bool`/`i64`, with its existing synchronous-entry source contract refreshed.
- evidence: Cycle 2 crossed the fix tool and stopped in `compiler.frontend.core.lexer_struct` because local generic `rt_array_len_safe<T>` reached the fail-closed native monomorphization gate after 4m58.64s at 4,205,368 KiB max RSS. The compiler gate remains intact; the lexer now uses concrete `bool`, `text`, and `i64` slot-length helpers.
- blocked-audit: Final cycle 3 crossed the concrete lexer helpers and stopped in `nogc_sync_mut.database.test` on unresolved `rt_timestamp_now` after 10m43.50s at 12,119,288 KiB max RSS. The database extension now binds time and process identity to their physical nogc-sync owners, pending a fresh bounded Phase 4 session. No fourth cycle, exact candidate smoke, platform promotion, full verification, or `1.0.0-rc` release was started.
- evidence: Stable current-head LLVM 23.1 Stage 2/3 refresh passed sanity, provenance, and Stage 2 native-build capability in 16m32.71s at 1,235,800 KiB max RSS. SHA-256 values are `423b899455be99c7410a096c04f4217026701533a29d4aa78a4c3d1c9791f88d` and `26d5d82d0b4b85a42d2e8900aa8f0d04db918cfc024b08feae603fd09edcf075`; manifest Git head is `38f27b8c5293f887a212fd88efb65cb8a8f4fd71`.
- evidence: Fresh exact x86 Phase 4 cycle 1 crossed the database owner repair and stopped in `nogc_sync_mut.cli.cli_util` on unresolved `cwd` after 14m55.75s at 13,325,392 KiB max RSS. The utility now binds cwd/file/CLI/exit operations to physical owners; its focused contract compiled, linked, and exited 30 while checking empty-manifest and quoted-CSV behavior.
- evidence: Cycle 2 crossed the CLI utility and stopped in `app.play.wm_access_cli` on unresolved semantic alias `AccessOutputMode` after 12m52.31s at 12,125,284 KiB max RSS. The physical grammar now explicitly exports both access aliases; the WM consumer imports aliases through the access hub and constants through the grammar owner.
- blocked-audit: Final cycle 3 reproduced `AccessOutputMode` because the first focused contract omitted the real access-hub plus grammar import topology. The strengthened topology contract compiled, linked, and exited 30, but the full three-cycle cap is exhausted. No fourth Phase 4 build, exact candidate smoke, platform promotion, release verification, or `1.0.0-rc` release was started.
- evidence: Current-head LLVM 23.1 Stage 2/3 refresh passed sanity, provenance, and Stage 2 native-build capability in 19m00.15s at 1,347,548 KiB max RSS. SHA-256 values are `34677da3a39f836a834603fea08c3e3508bc2236795a4976387a4e37d2d9175a` and `cf092a2f8b9a495634edbb5991115698ff610a6c1b82ab9dc203ccfdb7722088`; manifest Git head is `a60f27ade826f78ea8c740ab005f808533a8c9a4`.
- evidence: Fresh Phase 4 cycles 1 and 2 reproduced the WM `AccessOutputMode` terminal after 12m26.44s and 17m02.04s. The final containment uses the alias's exact `text` representation at the WM leaf boundary, and the strengthened 44-module contract linked and exited 30.
- blocked-audit: Final cycle 3 crossed the WM repair and stopped later in `app.mcp_t32.session_tools` on unresolved `thread_sleep` after 10m48.23s at 12,125,480 KiB max RSS. The session module now imports the physical synchronous thread owner; its 41-module focused contract linked and exited 30. The three-cycle cap is exhausted. No fourth Phase 4 build, exact candidate smoke, platform promotion, full release verification, or `1.0.0-rc` release was started.
