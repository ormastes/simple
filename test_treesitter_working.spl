# Demonstration that tree-sitter runtime exports work correctly
use parser.treesitter.tree.{Span, NodeId}

print "=== Tree-Sitter Runtime Export Test ==="
print ""

# Test 1: Import struct types
print "✅ Successfully imported Span and NodeId types"

# Test 2: Create Span instance
val span = Span(
    start_byte: 0,
    end_byte: 10,
    start_line: 1,
    end_line: 1,
    start_column: 1,
    end_column: 11
)
print "✅ Created Span instance"

# Test 3: Access fields
print "   Start: line " + span.start_line.to_string() + ", column " + span.start_column.to_string()
print "   End: line " + span.end_line.to_string() + ", column " + span.end_column.to_string()

print ""
print "🎉 Runtime export issue is FIXED!"
print "   Tree-sitter struct types are accessible and usable."
