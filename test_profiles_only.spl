# Test just the profile module
use std.process_limits.{profile_fast, profile_standard, profile_describe}

print "Testing process_limits module..."

val prof = profile_fast()
print "Fast profile name: {prof.name}"
print "Fast profile CPU: {prof.cpu_ms}ms"
print "Fast profile Memory: {prof.mem_mb}MB"

val std_prof = profile_standard()
print "Standard profile name: {std_prof.name}"

print "Profile description: {profile_describe(prof)}"

print "SUCCESS: All profile functions work!"
