"""
# No-GC Async RwLock Facade

Read-write lock operations are synchronous lock primitives, so the async family
reuses the no-GC sync runtime-hook owner instead of declaring duplicate hooks.
"""

export use std.nogc_sync_mut.concurrent.rwlock.{RwLock, rwlock_new}
