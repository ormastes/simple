description = "Collaborate through canonical LLM Caret rooms and bounded context."
prompt = '''
Use the llm-caret-messaging MCP tools. Inspect profiles with chat_who, read only
bounded authorized context, assign stateful tasks, publish significant progress
transitions and artifacts, preserve idempotency/correlation IDs, and never claim
a native read receipt when evidence is only a local cursor. Agent updates do not
implicitly wake other agents, and private-room content remains private.
'''
