use compiler.tools.duplicate_check.config.{load_config}
use compiler.tools.duplicate_check.detector_files.{collect_detector_files}

val config = load_config()
val files = collect_detector_files("test/fixtures/duplicate_check", config)
print "collect_detector_files returned {files.len()} files"
for f in files:
    print "  {f}"
