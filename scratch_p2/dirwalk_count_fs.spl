use std.nogc_sync_mut.file_system.dir_ops.{dir_walk, dir_walk_files}

fn main():
    val entries = dir_walk("src/app/cli")
    print("all count: " + entries.len().to_text())
    val files = dir_walk_files("src/app/cli")
    print("files count: " + files.len().to_text())
