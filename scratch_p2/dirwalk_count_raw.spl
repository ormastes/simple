extern fn rt_dir_walk(path: text) -> [text]

fn main():
    val a = rt_dir_walk("src/app/cli")
    print("raw relative: " + a.len().to_text())
    val b = rt_dir_walk("src/app/cli/")
    print("raw trailing-slash: " + b.len().to_text())
