use app.svim.core.*


fn main() -> i64:
    var session = SvimSession.new()
    session.execute_named("set-mode", "insert", 1, "")
    session.execute_named("insert-text", "ok", 1, "")
    if session.current_cursor().col == 2:
        return 0
    1
