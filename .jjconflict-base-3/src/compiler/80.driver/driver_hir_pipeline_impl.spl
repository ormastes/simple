# CompilerDriver HIR-pipeline method registration facade.

pub use compiler.driver.driver_hir_pipeline_passes.{
    run_typecheck_warn_pass,
    run_safety_warn_pass
}
use compiler.driver.driver_hir_pipeline_lowering.*
use compiler.driver.driver_hir_pipeline_passes.*
