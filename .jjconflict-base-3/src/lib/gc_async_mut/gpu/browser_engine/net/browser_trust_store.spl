"""
Browser trust store path helpers.

Pure-Simple policy keeps this in-browser module local and deterministic.
"""

pub fn browser_system_ca_bundle_paths() -> [text]:
    [
        "/etc/ssl/certs/ca-certificates.crt",
        "/etc/pki/tls/certs/ca-bundle.crt",
        "/etc/ssl/cert.pem"
    ]
