# ============================================================================
# common/bytes — shared byte-manipulation foundation (Phase 0 barrier)
#
# Behavior-carrying custom types that replace ad-hoc [u8]/i64 primitive usage
# across wire protocols, search, and compression. See
# doc/03_plan/lib/crypto/custom_type_alpha_crypto_team_plan_2026-06-15.md §Phase-0.
# ============================================================================

export use std.common.bytes.span.{ByteSpan, ByteBuffer}
export use std.common.bytes.ints.{U16le, U32le, U64le, U16be, U32be, U64be}
export use std.common.bytes.bits.{BitWriter, BitReader}
export use std.common.bytes.window.{RingWindow}
export use std.common.bytes.checksum.{Crc32, Adler32}
