# Fixture f01 — CONTROL: no lambda anywhere in the module.
#
# Expectation: the JIT closure guard (`first_lambda_function_impl`,
# src/compiler_rust/compiler/src/codegen/jit.rs:196) finds no
# `MirInst::ClosureCreate`, so this module JIT-compiles and NO
# "falling back to interpreter" line appears.
#
# Run: bin/simple run test/fixtures/repro/compiler/jit_closure/f01_baseline_no_lambda.spl

fn add_one(x: i64) -> i64:
    x + 1

fn main():
    print("f01 marker result={add_one(41)}")
