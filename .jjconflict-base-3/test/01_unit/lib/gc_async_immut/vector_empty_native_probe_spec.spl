use std.gc_async_immut.{PersistentVec}

describe "gc_async_immut vector empty native probe":
    it "constructs an empty vector":
        val base = PersistentVec.empty()
        expect(base.len()).to_equal(0)
