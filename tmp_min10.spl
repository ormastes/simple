use tmp_myfacade.{env_get}
extern fn rt_stderr_write(s: text)
fn main() -> i64:
    unsafe(capabilities: [ffi]):
        rt_stderr_write("A\n")
    val v = env_get("OS")
    unsafe(capabilities: [ffi]):
        rt_stderr_write("B v={v}\n")
    0
main()
