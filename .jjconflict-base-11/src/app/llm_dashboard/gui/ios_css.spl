fn ios_css_overrides() -> text:
    """
    iOS-friendly touch and scroll overrides for the LLM dashboard.
    """
    """
:root {
  --ios-accent: #007AFF;
}

body {
  -webkit-overflow-scrolling: touch;
}

button,
.tab-btn,
.touch-target {
  min-height: 44px;
}
"""

export ios_css_overrides
