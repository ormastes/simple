#!/usr/bin/env simple
# Try to use extern functions with manual library path

# This tests if the runtime can find extern functions at all
fn main():
    print "Testing if we can use extern fn at runtime..."
    
    # Try calling a built-in extern function (should work)
    use app.io.{env_get}
    val home = env_get("HOME")
    print "env_get works: HOME = {home}"
    
    print ""
    print "Now testing torch FFI..."
    print "(This will likely fail with 'unknown extern function')"

export main
