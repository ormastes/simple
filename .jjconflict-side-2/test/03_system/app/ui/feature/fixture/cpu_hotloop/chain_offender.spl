# Fixture: an idiomatic .for_each escape — must be flagged as CHAIN (new=1).
# The `.map[` field index below must NOT be flagged (CHAIN requires call-form).
fn paint_all(pixels: [i32]):
    pixels.for_each(\p: p + 1)

fn read_field(cache: LutCache) -> i32:
    cache.map[0]
