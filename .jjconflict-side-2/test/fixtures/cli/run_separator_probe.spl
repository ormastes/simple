use std.nogc_sync_mut.sffi.cli.{cli_get_args}
use std.io_runtime.{env_get}

fn main() -> i64:
    print "evidence=" + (env_get("SIMPLE_TEST_RESULT_FILE") ?? "")
    print "args=" + cli_get_args().join("|")
    0
