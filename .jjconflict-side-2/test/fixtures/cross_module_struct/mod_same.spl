## mod_same.spl — Variant C: same-module struct construction (control, should always work)
## Fixture for interp_cross_module_struct_return_unit_2026-06-13 diagnosis

struct Bar:
    y: i64

fn make_bar(n: i64) -> Bar:
    Bar(y: n)

fn main():
    val b = make_bar(99)
    print(b.y)
