"""
simple_db_if — Shared trait contracts for the Simple DB two-tier architecture.

@tag:stdlib
@tag:api

Defines the interface between the two Simple DB tiers:
  - Embedded (stdlib):  std.database — SDN file storage, single-process
  - Full engine:        examples/11_advanced/simple_db/ — MVCC, WAL, TOAST, buffer pool

Application code targets these traits and can swap backends.
Concrete impls NEVER live here.

Submodules:
  - types:        opaque aliases (Rel, BlkNo, Lsn, TxnId, PhysPtr).
  - storage_api:  trait bundle — buf/wal/pmap/temp/checkpoint/blob/vacuum.

See:
  - doc/07_guide/simple_db.md (guide)
  - doc/05_design/simple_db_design.md §2.1 (MDSOC), §5 (API), §6 (WAL).
  - src/lib/nogc_sync_mut/database/mod.spl (embedded tier)
"""

# All submodules are automatically available.
