"""
# No-GC Async Cache Types

Compatibility facade over the no-GC sync cache backend.
"""

export use std.nogc_sync_mut.cache.types.{
    _get_timestamp, _hash_key, _find_key_index, _find_key_index_in,
    _remove_at_index, _rebuild_key_index
}
