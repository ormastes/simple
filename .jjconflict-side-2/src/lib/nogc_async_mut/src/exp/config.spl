"""
# No-GC async experiment config

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.src.exp.config.{
    ConfigValue, ExpConfig,
    load_exp_config, load_sdn_config, merge_configs, deep_merge,
    resolve_interpolation, resolve_values, resolve_value,
    interpolate_string, lookup_path, parse_cli_overrides,
    set_nested, parse_config_value, serialize_config, serialize_kv,
    serialize_value, sdn_to_config_values, sdn_value_to_config,
    file_exists, file_read, parse_sdn, get_env_optional, sha256_hex,
    sha256_rotr, sha256_word_hex, char_to_byte,
    try_parse_int, try_parse_float
}
