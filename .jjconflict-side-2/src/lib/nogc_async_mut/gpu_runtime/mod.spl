"""
# No-GC async gpu runtime module

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.gpu_runtime.mod.{
    gpu_available, gpu_backend_name, gpu_device_count,
    gpu_tensor_zeros, gpu_tensor_ones, gpu_tensor_to_cuda,
    gpu_tensor_is_cuda, gpu_tensor_numel,
    gpu_stream_create, gpu_stream_sync, gpu_stream_query,
    gpu_alloc_zeros, gpu_alloc_ones, gpu_ctx_info,
    gpu_ctx_alloc_zeros, gpu_ctx_alloc_ones,
    gpu_async_upload_batch, gpu_async_wait
}
