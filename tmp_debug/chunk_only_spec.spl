use std.spec.*
use std.gc_async_mut.gpu.browser_engine.net.h1_client.{h1_chunk_size_value}

fn main():
    describe "chunk size only":
        it "parses simple":
            expect(h1_chunk_size_value("1")).to_equal(1)
            expect(h1_chunk_size_value("0")).to_equal(0)
