"""
# No-GC async desktop updater

Compatibility facade over the no-GC sync backend.
"""

export use std.nogc_sync_mut.desktop.updater.{
    UpdateInfo, UpdateConfig,
    check_for_update, download_update, apply_update, split_lines
}
