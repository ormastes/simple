#!/bin/sh
# Test double for /usr/bin/time used only by the gate contract self-check.
set -u
[ "$#" -ge 5 ] || exit 64
[ "$1" = -f ] || exit 64
shift 2
[ "$1" = -o ] || exit 64
output=$2
shift 2
"$@"
rc=$?
printf '1.00\t1024\n' >"$output" || exit 65
exit "$rc"
