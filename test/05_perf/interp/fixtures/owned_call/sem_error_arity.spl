# Error path: more arguments than the method declares must still be rejected.
class One:
    n: i64
    me take(self, a: i64):
        self.n = a
fn main():
    var o = One(n: 0)
    o.take(1, 2)
    print("RESULT elapsed_us=1 acc=unreached iters=0")
