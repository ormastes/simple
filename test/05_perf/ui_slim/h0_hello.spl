"""H0 launch-timing fixture: runtime/loader floor.

Workload H0 of the slim-UI benchmark design
(`doc/01_research/ui/slim_kernel_plugin/simple_slim_tui_gui_kernel_plugin_design_parallel_plan_2026-09-05.md`
section 8.1): "native empty process and output-only hello, without UI". It is
the floor the T0/T1 terminal lanes are measured against, so it must contain
NOTHING but one write and an exit -- no imports, no UI, no allocation.

Driven by `scripts/check/check-ui-slim-startup.shs --lane H0`, which asserts the
marker line below appears on stdout; a run without it is a FAIL, not a sample.
"""

fn main():
    print "ui-slim-h0-hello"
