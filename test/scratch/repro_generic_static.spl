# Reproducer: static method call with explicit generic params
# Bug: Foo<Int>.static_method() may parse as comparison

class Container<T>:
    val value: T

    fn wrap(v: T) -> Container<T>:
        Container<T>(value: v)

val c = Container<i64>.wrap(42)
print c.value
