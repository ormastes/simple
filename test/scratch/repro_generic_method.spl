# Reproducer: Generic method call parsing
# Bug: Foo<Int>.method() may parse as (Foo < Int) > .method()

class Box<T>:
    val value: T

    fn get() -> T:
        me.value

val b = Box<i64>(value: 42)
val v = b.get()
print v
