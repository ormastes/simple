# Soundness corpus: struct construction + field access.
# expected stdout:
# 7
# 12
struct Point:
    x: i64
    y: i64

fn main():
    val p = Point { x: 3, y: 4 }
    print(p.x + p.y)     # 7
    print(p.x * p.y)     # 12
