# Tool-qual NEGATIVE case: constructing a struct type that is never declared.
# MUST be rejected (non-zero exit, diagnostic on stderr) and MUST NOT silently
# produce stdout. TOR-FM-02: a silent accept here would ship undefined-behaviour.
fn main():
    val p = NoSuchStruct(x: 1)
    print(p.x)
