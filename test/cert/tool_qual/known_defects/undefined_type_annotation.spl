# OPEN DEFECT repro (NOT gated by the runner). See README.md.
# `val x: Nonexistent = 5` names a type that does not exist; this SHOULD be
# rejected. On the deployed binary it is silently ACCEPTED: prints 5, exit 0.
fn main():
    val x: Nonexistent = 5
    print(x)
