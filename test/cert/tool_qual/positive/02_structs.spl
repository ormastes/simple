# Tool-qual positive case: struct construction + field access.
# Hand-computed: p.x=3, p.y=4 -> 3+4=7, 3*4=12.
struct Point:
    x: i64
    y: i64

fn main():
    val p = Point { x: 3, y: 4 }
    print(p.x + p.y)     # 7
    print(p.x * p.y)     # 12
