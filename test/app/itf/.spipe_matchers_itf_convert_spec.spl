# @cover src/app/itf/convert_storage.spl 70%

use app.itf.convert_storage.{storage_to_markdown, markdown_to_storage}

describe "ITF storage conversion":

    context "storage_to_markdown":
        it "converts headings":
            val storage = "<h1>Title</h1>"
            val md = storage_to_markdown(storage)
            expect (md).contains("# Title")

        it "converts h2":
            val storage = "<h2>Subtitle</h2>"
            val md = storage_to_markdown(storage)
            expect (md).contains("## Subtitle")

        it "converts bold":
            val storage = "<p>This is <strong>bold</strong> text</p>"
            val md = storage_to_markdown(storage)
            expect (md).contains("**bold**")

        it "converts italic":
            val storage = "<p>This is <em>italic</em> text</p>"
            val md = storage_to_markdown(storage)
            expect (md).contains("*italic*")

        it "converts code":
            val storage = "<p>Use <code>hello()</code> here</p>"
            val md = storage_to_markdown(storage)
            expect (md).contains("`hello()`")

        it "converts list items":
            val storage = "<ul><li>First</li><li>Second</li></ul>"
            val md = storage_to_markdown(storage)
            expect (md).contains("- First")
            expect (md).contains("- Second")

        it "converts hr":
            val storage = "<hr />"
            val md = storage_to_markdown(storage)
            expect (md).contains("---")

    context "markdown_to_storage":
        it "converts headings":
            val md = "# Title"
            val storage = markdown_to_storage(md)
            expect (storage).contains("<h1>Title</h1>")

        it "converts h2":
            val md = "## Subtitle"
            val storage = markdown_to_storage(md)
            expect (storage).contains("<h2>Subtitle</h2>")

        it "converts bold":
            val md = "This is **bold** text"
            val storage = markdown_to_storage(md)
            expect (storage).contains("<strong>bold</strong>")

        it "converts italic":
            val md = "This is *italic* text"
            val storage = markdown_to_storage(md)
            expect (storage).contains("<em>italic</em>")

        it "converts list items":
            val md = "- First\n- Second"
            val storage = markdown_to_storage(md)
            expect (storage).contains("<li>")
            expect (storage).contains("First")

        it "converts hr":
            val md = "---"
            val storage = markdown_to_storage(md)
            expect (storage).contains("<hr />")

        it "wraps paragraphs in p tags":
            val md = "Hello world"
            val storage = markdown_to_storage(md)
            expect (storage).contains("<p>")

    context "round-trip":
        it "preserves confluence macros through round-trip":
            val original = "<h1>Title</h1>\n<p>Text</p>"
            val md = storage_to_markdown(original)
            expect (md).contains("# Title")
            expect (md).contains("Text")

        it "preserves fenced raw blocks":
            val md = "# Title\n\n~~~confluence-raw\n<ac:structured-macro ac:name=\"toc\"></ac:structured-macro>\n~~~\n\nParagraph"
            val storage = markdown_to_storage(md)
            expect (storage).contains("ac:structured-macro")
            expect (storage).contains("<h1>Title</h1>")