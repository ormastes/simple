extern fn rt_env_get(key: text) -> text

describe "env check":
    it "SIMPLE_SYSTEM_TEST is not 1":
        val sys = rt_env_get("SIMPLE_SYSTEM_TEST")
        val is_one = sys == "1"
        expect(is_one).to_equal(false)
