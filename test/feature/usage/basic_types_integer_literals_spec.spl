"""
## Purpose and audience

# Integer Literals Specification

**Feature IDs:** #200-210
**Category:** Syntax
**Difficulty:** 1/5
**Status:** Implemented

## Overview

Integer literals in Simple support multiple base formats (decimal, hexadecimal, binary, octal),
underscore separators for readability, type suffixes for explicit sizing, and user-defined unit
suffixes for semantic meaning. All integers default to 64-bit signed (`i64`) unless explicitly
typed with a suffix.

## Syntax

### Base Formats

```simple
val decimal = 42                # Decimal (base 10)
val hex = 0xFF                  # Hexadecimal (base 16)
val binary = 0b1010             # Binary (base 2)
val octal = 0o77                # Octal (base 8)
```

### Underscore Separators

```simple
val million = 1_000_000         # Decimal with underscores
val hex_color = 0xFF_00_FF      # Hex with underscores
val binary_byte = 0b1111_0000   # Binary with underscores
```

### Type Suffixes

```simple
val byte = 255u8                # Unsigned 8-bit
val short = 1000i16             # Signed 16-bit
val int = 42i32                 # Signed 32-bit
val long = 1000000i64           # Signed 64-bit (default)
```

### Unit Suffixes

```simple
val port = 8080_port            # Port number
val ip = 0x7F000001_ip          # IP address (127.0.0.1)
val distance = 100_km           # Distance in kilometers
```

## Key Concepts

| Concept | Description |
|---------|-------------|
| Base Format | Decimal, hex (0x), binary (0b), octal (0o) |
| Underscore Separator | Visual grouping, stripped during parsing |
| Type Suffix | Explicit integer size (i8, i16, i32, i64, u8, u16, u32, u64) |
| Unit Suffix | User-defined semantic units (_port, _ip, _km) |
| Default Type | i64 (64-bit signed integer) |

## Behavior

- **Decimal**: Standard base-10 notation, supports underscores
- **Hexadecimal**: `0x` or `0X` prefix, case-insensitive digits
- **Binary**: `0b` or `0B` prefix, only 0 and 1 digits
- **Octal**: `0o` or `0O` prefix, digits 0-7
- **Underscores**: Ignored during parsing, cannot be consecutive or at boundaries
- **Type Suffixes**: Parsed as token metadata, affect type inference
- **Unit Suffixes**: Parsed as separate concept, provide semantic meaning

## Related Specifications

- [Type Inference](../type_inference/type_inference_spec.md) - Integer type deduction
- [Arithmetic Operators](../operators_arithmetic/operators_arithmetic_spec.md) - Integer operations
- [Basic Types](../basic_types/basic_types_spec.md) - Type system overview

## Implementation Notes

**Lexer:** `src/parser/src/lexer/numbers.rs`
- `scan_number()` - Main entry point for all numeric literals
- `scan_radix_digits()` - Collects digits with underscore handling
- `parse_radix_integer()` - Parses non-decimal bases
- `scan_numeric_suffix()` - Extracts type/unit suffixes

**Token Types:**
- `TokenKind.Integer(i64)` - Plain integer without suffix
- `TokenKind.TypedInteger(i64, NumericSuffix)` - With type or unit suffix

**Performance:** Direct parsing into `i64` with zero-copy where possible.
Underscores are skipped during scanning (no allocation needed).

## Examples

```simple
# Decimal literals
val x = 42
val large = 1_000_000

# Hexadecimal
val color = 0xFF00FF
val addr = 0x7F000001

# Binary
val flags = 0b1111_0000
val mask = 0b11111111

# Octal
val perms = 0o755
val mode = 0o644

# With type suffixes
val byte = 255u8
val port = 8080u16

# With unit suffixes
val timeout = 5000_ms
val size = 1024_bytes
```

This specification is maintained for compiler and library engineers reviewing FEATURE-USAGE behavior, and for operators verifying a build.

## Operator workflow

Run `bin/simple test --no-session-daemon test/feature/usage/basic_types_integer_literals_spec.spl` and confirm the Results line reports 0 failed. Each scenario names the requirement it traces to and starts with a Verify step.

## Compatibility and limitations

Covers only the behavior explicitly asserted below on the current toolchain. No external standard or cross-version contract is claimed; numeric oracles are explained inline where they appear.

## Recovery and troubleshooting

If a scenario fails, re-run the single spec file, read the first failing Verify step, and compare the reported value against the inline oracle comment before bisecting recent changes.
"""
use std.spec.{step}
# @manual: primary
# REQ-FEATURE-USAGE-001
# doc-path: doc/01_research/app/
# doc-path: doc/03_plan/app/
# doc-path: doc/04_architecture/app/
# doc-path: doc/05_design/app/
# ============================================================================
# Decimal Integer Literals
# ============================================================================

describe "Integer Literals - Decimal":
    # ## Decimal Integer Literals
    #
    # Decimal (base 10) is the default notation for integer literals.
    # Supports underscore separators for readability.

    context "basic decimal literals":
        # ### Scenario: Basic Decimal Integers
        #
        # Standard decimal integer notation.
        #
        # ```simple
        # val x = 42
        # val y = 0
        # val z = 1000000
        # ```

        it "parses single digit":
            # @req: REQ-FEATURE-USAGE-001
            step("Verify: parses single digit")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 5
            expect(x).to_equal(5)  # oracle: expected value 5 is the behavior asserted by this regression spec

        it "parses zero":
            # @req: REQ-FEATURE-USAGE-002
            step("Verify: parses zero")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0
            expect(x).to_equal(0)  # oracle: expected value 0 is the behavior asserted by this regression spec

        it "parses multi-digit":
            # @req: REQ-FEATURE-USAGE-003
            step("Verify: parses multi-digit")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 123456
            expect(x).to_equal(123456)  # oracle: expected value 123456 is the behavior asserted by this regression spec

        it "parses large number":
            # @req: REQ-FEATURE-USAGE-004
            step("Verify: parses large number")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 999999999
            expect(x).to_equal(999999999)  # oracle: expected value 999999999 is the behavior asserted by this regression spec

    context "with underscore separators":
        # ### Scenario: Underscore Separators
        #
        # Underscores improve readability for large numbers.
        #
        # ```simple
        # val million = 1_000_000
        # val billion = 1_000_000_000
        # ```

        it "parses with single underscore":
            # @req: REQ-FEATURE-USAGE-005
            step("Verify: parses with single underscore")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 1_000
            expect(x).to_equal(1000)  # oracle: expected value 1000 is the behavior asserted by this regression spec

        it "parses with multiple underscores":
            # @req: REQ-FEATURE-USAGE-006
            step("Verify: parses with multiple underscores")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 1_000_000
            expect(x).to_equal(1000000)  # oracle: expected value 1000000 is the behavior asserted by this regression spec

        it "parses with arbitrary grouping":
            # @req: REQ-FEATURE-USAGE-007
            step("Verify: parses with arbitrary grouping")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 12_34_56
            expect(x).to_equal(123456)  # oracle: expected value 123456 is the behavior asserted by this regression spec


# ============================================================================
# Hexadecimal Integer Literals
# ============================================================================

describe "Integer Literals - Hexadecimal":
    # ## Hexadecimal Integer Literals
    #
    # Hexadecimal (base 16) uses `0x` or `0X` prefix.
    # Digits: 0-9, a-f, A-F (case insensitive).

    context "basic hex literals":
        # ### Scenario: Basic Hexadecimal
        #
        # Hex literals start with 0x prefix.
        #
        # ```simple
        # val x = 0xFF
        # val y = 0x10
        # ```

        it "parses lowercase hex":
            # @req: REQ-FEATURE-USAGE-008
            step("Verify: parses lowercase hex")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0xff
            expect(x).to_equal(255)  # oracle: expected value 255 is the behavior asserted by this regression spec

        it "parses uppercase hex":
            # @req: REQ-FEATURE-USAGE-009
            step("Verify: parses uppercase hex")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0xFF
            expect(x).to_equal(255)  # oracle: expected value 255 is the behavior asserted by this regression spec

        it "parses mixed case":
            # @req: REQ-FEATURE-USAGE-010
            step("Verify: parses mixed case")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0xAb
            expect(x).to_equal(171)  # oracle: expected value 171 is the behavior asserted by this regression spec

        it "parses single hex digit":
            # @req: REQ-FEATURE-USAGE-011
            step("Verify: parses single hex digit")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0xF
            expect(x).to_equal(15)  # oracle: expected value 15 is the behavior asserted by this regression spec

        it "parses multi-digit hex":
            # @req: REQ-FEATURE-USAGE-012
            step("Verify: parses multi-digit hex")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0x1A2B
            expect(x).to_equal(6699)  # oracle: expected value 6699 is the behavior asserted by this regression spec

    context "with underscore separators":
        # ### Scenario: Hex with Underscores
        #
        # Underscores can group hex digits.
        #
        # ```simple
        # val color = 0xFF_00_FF
        # val addr = 0x7F00_0001
        # ```

        it "parses hex with underscores":
            # @req: REQ-FEATURE-USAGE-013
            step("Verify: parses hex with underscores")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0xFF_00
            expect(x).to_equal(65280)  # oracle: expected value 65280 is the behavior asserted by this regression spec

        it "parses byte grouping":
            # @req: REQ-FEATURE-USAGE-014
            step("Verify: parses byte grouping")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0xFF_FF_FF_FF
            expect(x).to_equal(4294967295)  # oracle: expected value 4294967295 is the behavior asserted by this regression spec


# ============================================================================
# Binary Integer Literals
# ============================================================================

describe "Integer Literals - Binary":
    # ## Binary Integer Literals
    #
    # Binary (base 2) uses `0b` or `0B` prefix.
    # Digits: 0 and 1 only.

    context "basic binary literals":
        # ### Scenario: Basic Binary
        #
        # Binary literals start with 0b prefix.
        #
        # ```simple
        # val x = 0b1010
        # val y = 0b1111
        # ```

        it "parses simple binary":
            # @req: REQ-FEATURE-USAGE-015
            step("Verify: parses simple binary")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b1010
            expect(x).to_equal(10)  # oracle: expected value 10 is the behavior asserted by this regression spec

        it "parses all ones":
            # @req: REQ-FEATURE-USAGE-016
            step("Verify: parses all ones")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b1111
            expect(x).to_equal(15)  # oracle: expected value 15 is the behavior asserted by this regression spec

        it "parses all zeros":
            # @req: REQ-FEATURE-USAGE-017
            step("Verify: parses all zeros")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b0000
            expect(x).to_equal(0)  # oracle: expected value 0 is the behavior asserted by this regression spec

        it "parses single bit":
            # @req: REQ-FEATURE-USAGE-018
            step("Verify: parses single bit")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b1
            expect(x).to_equal(1)  # oracle: expected value 1 is the behavior asserted by this regression spec

        it "parses byte value":
            # @req: REQ-FEATURE-USAGE-019
            step("Verify: parses byte value")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b11111111
            expect(x).to_equal(255)  # oracle: expected value 255 is the behavior asserted by this regression spec

    context "with underscore separators":
        # ### Scenario: Binary with Underscores
        #
        # Underscores can group binary digits (nibbles, bytes).
        #
        # ```simple
        # val byte = 0b1111_0000
        # val word = 0b1111_1111_0000_0000
        # ```

        it "parses nibble grouping":
            # @req: REQ-FEATURE-USAGE-020
            step("Verify: parses nibble grouping")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b1111_0000
            expect(x).to_equal(240)  # oracle: expected value 240 is the behavior asserted by this regression spec

        it "parses byte pairs":
            # @req: REQ-FEATURE-USAGE-021
            step("Verify: parses byte pairs")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b1010_1010
            expect(x).to_equal(170)  # oracle: expected value 170 is the behavior asserted by this regression spec


# ============================================================================
# Octal Integer Literals
# ============================================================================

describe "Integer Literals - Octal":
    # ## Octal Integer Literals
    #
    # Octal (base 8) uses `0o` or `0O` prefix.
    # Digits: 0-7 only.

    context "basic octal literals":
        # ### Scenario: Basic Octal
        #
        # Octal literals start with 0o prefix.
        #
        # ```simple
        # val perms = 0o755
        # val mode = 0o644
        # ```

        it "parses simple octal":
            # @req: REQ-FEATURE-USAGE-022
            step("Verify: parses simple octal")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0o10
            expect(x).to_equal(8)  # oracle: expected value 8 is the behavior asserted by this regression spec

        it "parses unix permissions":
            # @req: REQ-FEATURE-USAGE-023
            step("Verify: parses unix permissions")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0o755
            expect(x).to_equal(493)  # oracle: expected value 493 is the behavior asserted by this regression spec

        it "parses all sevens":
            # @req: REQ-FEATURE-USAGE-024
            step("Verify: parses all sevens")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0o777
            expect(x).to_equal(511)  # oracle: expected value 511 is the behavior asserted by this regression spec

        it "parses single octal digit":
            # @req: REQ-FEATURE-USAGE-025
            step("Verify: parses single octal digit")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0o7
            expect(x).to_equal(7)  # oracle: expected value 7 is the behavior asserted by this regression spec


# ============================================================================
# Type Suffixes
# ============================================================================

describe "Integer Literals - Type Suffixes":
    # ## Type Suffixes
    #
    # Explicit type suffixes specify integer size and signedness.
    # Signed: i8, i16, i32, i64
    # Unsigned: u8, u16, u32, u64

    context "signed type suffixes":
        # ### Scenario: Signed Type Suffixes
        #
        # Signed integers with explicit size.
        #
        # ```simple
        # val byte = 127i8
        # val short = 1000i16
        # val int = 100000i32
        # val long = 1000000000i64
        # ```

        it "parses i32 suffix":
            # @req: REQ-FEATURE-USAGE-026
            step("Verify: parses i32 suffix")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 42i32
            expect(x).to_equal(42)  # oracle: expected value 42 is the behavior asserted by this regression spec

        it "parses i64 suffix":
            # @req: REQ-FEATURE-USAGE-027
            step("Verify: parses i64 suffix")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 1000i64
            expect(x).to_equal(1000)  # oracle: expected value 1000 is the behavior asserted by this regression spec

    context "unsigned type suffixes":
        # ### Scenario: Unsigned Type Suffixes
        #
        # Unsigned integers with explicit size.
        #
        # ```simple
        # val byte = 255u8
        # val port = 8080u16
        # val count = 4294967295u32
        # ```

        it "parses u8 suffix":
            # @req: REQ-FEATURE-USAGE-028
            step("Verify: parses u8 suffix")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 255u8
            expect(x).to_equal(255)  # oracle: expected value 255 is the behavior asserted by this regression spec

        it "parses u16 suffix":
            # @req: REQ-FEATURE-USAGE-029
            step("Verify: parses u16 suffix")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 1000u16
            expect(x).to_equal(1000)  # oracle: expected value 1000 is the behavior asserted by this regression spec

        it "parses u32 suffix":
            # @req: REQ-FEATURE-USAGE-030
            step("Verify: parses u32 suffix")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 12345u32
            expect(x).to_equal(12345)  # oracle: expected value 12345 is the behavior asserted by this regression spec


# ============================================================================
# Mixed Formats
# ============================================================================

describe "Integer Literals - Mixed Formats":
    # ## Mixed Format Expressions
    #
    # Different integer literal formats can be used in the same expression.

    context "combining formats":
        # ### Scenario: Multiple Formats
        #
        # Mix decimal, hex, binary, octal in calculations.
        #
        # ```simple
        # val result = 10 + 0xA + 0b1010 + 0o12
        # ```

        it "combines decimal and hex":
            # @req: REQ-FEATURE-USAGE-031
            step("Verify: combines decimal and hex")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val result = 10 + 0xA
            expect(result).to_equal(20)  # oracle: expected value 20 is the behavior asserted by this regression spec

        it "combines decimal and binary":
            # @req: REQ-FEATURE-USAGE-032
            step("Verify: combines decimal and binary")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val result = 5 + 0b101
            expect(result).to_equal(10)  # oracle: expected value 10 is the behavior asserted by this regression spec

        it "combines all formats":
            # @req: REQ-FEATURE-USAGE-033
            step("Verify: combines all formats")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val result = 1 + 0xF + 0b1111 + 0o17
            expect(result).to_equal(46)  # oracle: expected value 46 is the behavior asserted by this regression spec


# ============================================================================
# Edge Cases
# ============================================================================

describe "Integer Literals - Edge Cases":
    # ## Edge Case Handling
    #
    # Special cases and boundary conditions.

    context "with zero":
        # ### Scenario: Zero in Different Bases
        #
        # Zero can be represented in any base.
        #
        # ```simple
        # val dec_zero = 0
        # val hex_zero = 0x0
        # val bin_zero = 0b0
        # val oct_zero = 0o0
        # ```

        it "parses decimal zero":
            # @req: REQ-FEATURE-USAGE-034
            step("Verify: parses decimal zero")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0
            expect(x).to_equal(0)  # oracle: expected value 0 is the behavior asserted by this regression spec

        it "parses hex zero":
            # @req: REQ-FEATURE-USAGE-035
            step("Verify: parses hex zero")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0x0
            expect(x).to_equal(0)  # oracle: expected value 0 is the behavior asserted by this regression spec

        it "parses binary zero":
            # @req: REQ-FEATURE-USAGE-036
            step("Verify: parses binary zero")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b0
            expect(x).to_equal(0)  # oracle: expected value 0 is the behavior asserted by this regression spec

        it "parses octal zero":
            # @req: REQ-FEATURE-USAGE-037
            step("Verify: parses octal zero")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0o0
            expect(x).to_equal(0)  # oracle: expected value 0 is the behavior asserted by this regression spec

    context "with one":
        # ### Scenario: One in Different Bases
        #
        # One is represented the same in all bases.

        it "parses decimal one":
            # @req: REQ-FEATURE-USAGE-038
            step("Verify: parses decimal one")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 1
            expect(x).to_equal(1)  # oracle: expected value 1 is the behavior asserted by this regression spec

        it "parses hex one":
            # @req: REQ-FEATURE-USAGE-039
            step("Verify: parses hex one")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0x1
            expect(x).to_equal(1)  # oracle: expected value 1 is the behavior asserted by this regression spec

        it "parses binary one":
            # @req: REQ-FEATURE-USAGE-040
            step("Verify: parses binary one")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b1
            expect(x).to_equal(1)  # oracle: expected value 1 is the behavior asserted by this regression spec

        it "parses octal one":
            # @req: REQ-FEATURE-USAGE-041
            step("Verify: parses octal one")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0o1
            expect(x).to_equal(1)  # oracle: expected value 1 is the behavior asserted by this regression spec

    context "with maximum values":
        # ### Scenario: Large Integer Values
        #
        # Large numbers within i64 range.

        it "parses large decimal":
            # @req: REQ-FEATURE-USAGE-042
            step("Verify: parses large decimal")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 2147483647
            expect(x).to_equal(2147483647)  # oracle: expected value 2147483647 is the behavior asserted by this regression spec

        it "parses large hex":
            # @req: REQ-FEATURE-USAGE-043
            step("Verify: parses large hex")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0x7FFFFFFF
            expect(x).to_equal(2147483647)  # oracle: expected value 2147483647 is the behavior asserted by this regression spec


# ============================================================================
# Uppercase vs Lowercase Prefixes
# ============================================================================

describe "Integer Literals - Case Insensitivity":
    # ## Case Insensitivity
    #
    # Hex, binary, and octal prefixes are case-insensitive.

    context "hex prefix case":
        # ### Scenario: Hex Prefix Case Variations
        #
        # Both 0x and 0X are valid.

        it "accepts lowercase 0x":
            # @req: REQ-FEATURE-USAGE-044
            step("Verify: accepts lowercase 0x")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0xff
            expect(x).to_equal(255)  # oracle: expected value 255 is the behavior asserted by this regression spec

        it "accepts uppercase 0X":
            # @req: REQ-FEATURE-USAGE-045
            step("Verify: accepts uppercase 0X")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0XFF
            expect(x).to_equal(255)  # oracle: expected value 255 is the behavior asserted by this regression spec

    context "binary prefix case":
        # ### Scenario: Binary Prefix Case Variations
        #
        # Both 0b and 0B are valid.

        it "accepts lowercase 0b":
            # @req: REQ-FEATURE-USAGE-046
            step("Verify: accepts lowercase 0b")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0b1010
            expect(x).to_equal(10)  # oracle: expected value 10 is the behavior asserted by this regression spec

        it "accepts uppercase 0B":
            # @req: REQ-FEATURE-USAGE-047
            step("Verify: accepts uppercase 0B")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0B1010
            expect(x).to_equal(10)  # oracle: expected value 10 is the behavior asserted by this regression spec

    context "octal prefix case":
        # ### Scenario: Octal Prefix Case Variations
        #
        # Both 0o and 0O are valid.

        it "accepts lowercase 0o":
            # @req: REQ-FEATURE-USAGE-048
            step("Verify: accepts lowercase 0o")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0o77
            expect(x).to_equal(63)  # oracle: expected value 63 is the behavior asserted by this regression spec

        it "accepts uppercase 0O":
            # @req: REQ-FEATURE-USAGE-049
            step("Verify: accepts uppercase 0O")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val x = 0O77
            expect(x).to_equal(63)  # oracle: expected value 63 is the behavior asserted by this regression spec
