"""
## Purpose and audience
Purpose: Prove that Feature Completion Tracking.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
# @manual: primary
REQ-FEATURE-FEATURE-DONE-001
doc/01_research/feature/REQ-FEATURE-FEATURE-DONE-001.md
doc/03_plan/feature/REQ-FEATURE-FEATURE-DONE-001.md
doc/04_architecture/feature/REQ-FEATURE-FEATURE-DONE-001.md
doc/05_design/feature/REQ-FEATURE-FEATURE-DONE-001.md
"""


"""
# Feature Completion Tracking Specification

**Feature IDs:** #FEATURE-DONE
**Category:** Infrastructure
**Status:** Implemented

This specification documents the living documentation pattern where features
remain verified through executable examples and behavior tests. It ensures that
completed features maintain their documented behavior and remain compilable
over time as the codebase evolves.

## Overview

The feature completion tracking system provides:
- Executable specifications that verify feature behavior
- Automatic testing against documented examples
- Living documentation that stays synchronized with actual behavior
- Regression detection through continuous verification

## Behavior

- Features marked as "done" must have executable tests
- Tests verify that documented examples still work
- Changes to the codebase are caught immediately if they break completed features
- Test failures indicate either: (1) incorrect changes, or (2) need to update documentation
"""



use std.spec.step

extern fn rt_file_read_text(path: text) -> text

fn feature_doc() -> text:
    rt_file_read_text("doc/02_requirements/language/features/feature_done.md")

describe "Feature Completion Tracking":
    it "documents the completed feature with its stable feature id":
        # @req: REQ-FEATURE-FEATURE-DONE-001
        step("Verify: documents the completed feature with its stable feature id")
        val doc = feature_doc()
        expect(doc).to_contain("#FEATURE-DONE")
        expect(doc).to_contain("Status:** Implemented")

    it "binds this spec as the living source of the generated document":
        # @req: REQ-FEATURE-FEATURE-DONE-001
        step("Verify: binds this spec as the living source of the generated document")
        val doc = feature_doc()
        expect(doc).to_contain("test/03_system/feature/usage/feature_done_spec.spl")

    it "keeps an overview section synchronized with the spec":
        # @req: REQ-FEATURE-FEATURE-DONE-001
        step("Verify: keeps an overview section synchronized with the spec")
        val doc = feature_doc()
        expect(doc).to_contain("## Overview")
