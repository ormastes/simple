# Purpose and audience: executable specification evidence for the owning engineering team.
# @req REQ-SSPEC-FEATURE
# research: doc/01_research/domain/sspec_documentization_maintenance.md ; plan: doc/03_plan/sspec_modernization_plan.md ; architecture: doc/04_architecture/sspec_documentization_maintenance.md ; design: doc/05_design/infra/sspec/modern_sspec_typed_evidence_design.md

"""
# Native-Only Mode Tests

**Category:** Testing Framework
**Status:** Active

## Overview

Tests restricted to native compiled mode via `@mode: native` annotation.
"""

This specification is maintained for compiler and library engineers reviewing FEATURE-MODE_FILTER behavior, and for operators verifying a build.

## Operator workflow

Run `bin/simple test --no-session-daemon test/feature/mode_filter/native_only_spec.spl` and confirm the Results line reports 0 failed. Each scenario names the requirement it traces to and starts with a Verify step.

## Compatibility and limitations

Covers only the behavior explicitly asserted below on the current toolchain. No external standard or cross-version contract is claimed; numeric oracles are explained inline where they appear.

## Recovery and troubleshooting

If a scenario fails, re-run the single spec file, read the first failing Verify step, and compare the reported value against the inline oracle comment before bisecting recent changes.
"""
use std.spec.{step}
# @manual: primary
# REQ-FEATURE-MODE_FILTER-001
# doc-path: doc/01_research/app/
# doc-path: doc/03_plan/app/
# doc-path: doc/04_architecture/app/
# doc-path: doc/05_design/app/
# @mode: native

describe "Native-only features":
    it "runs compiled code":
        # @req REQ-SSPEC-FEATURE
        step("runs compiled code")
        # evidence(protocol_json): asserted result fields below are the complete typed oracle
        expect(42).to_equal(42)  # oracle: 42 must equal 42 — authoritative contract constant
