# SDoctest Init Script: std_imports
#
# Common imports prepended to code blocks that use :init=std_imports modifier.
# This provides standard library access without requiring each code block
# to include its own import statements.

use std.collections.*
use std.string.*
use std.math.*
