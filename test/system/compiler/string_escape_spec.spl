# @cover src/compiler_rust/lib/std/src/tooling/compiler/string_escape.spl 80%
# SPipe: String Escape Processing
#
# Comprehensive behavioral specification for string_escape.spl
# Tests escape/unescape bijection, error handling, and edge cases

"""
## Purpose and audience
Purpose: processes newline escape
Audience: compiler and tooling engineers who maintain this spec
"""

# @req: REQ-COMPILER-StriEsca-001

use std.spec.*
use std.tooling.compiler.string_escape.*

describe "String Escape Processing":

    it "processes newline escape":
        step("Verify: processes newline escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = 'n'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Char(ch):
                expect(ch).to_equal('\n')
            _:
                expect(false).to_equal(true)

    it "processes tab escape":
        step("Verify: processes tab escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = 't'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Char(ch):
                expect(ch).to_equal('\t')
            _:
                expect(false).to_equal(true)

    it "processes carriage return escape":
        step("Verify: processes carriage return escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = 'r'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Char(ch):
                expect(ch).to_equal('\r')
            _:
                expect(false).to_equal(true)

    it "processes backslash escape":
        step("Verify: processes backslash escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '\\'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Char(ch):
                expect(ch).to_equal('\\')
            _:
                expect(false).to_equal(true)

    it "processes quote escape":
        step("Verify: processes quote escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '"'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Char(ch):
                expect(ch).to_equal('"')
            _:
                expect(false).to_equal(true)

    it "processes null escape":
        step("Verify: processes null escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '0'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Char(ch):
                expect(ch).to_equal('\0')
            _:
                expect(false).to_equal(true)

    it "processes brace escape when allowed":
        step("Verify: processes brace escape when allowed")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '{'
        val result = process_escape(c, allow_braces: true)
        match result:
            EscapeResult.Char(ch):
                expect(ch).to_equal('{')
            _:
                expect(false).to_equal(true)

    it "rejects brace escape when not allowed":
        step("Verify: rejects brace escape when not allowed")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '{'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Error(_):
                expect(true).to_equal(true)
            _:
                expect(false).to_equal(true)

    it "processes invalid escape":
        step("Verify: processes invalid escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = 'x'
        val result = process_escape(c, allow_braces: false)
        match result:
            EscapeResult.Error(msg):
                expect(msg).to_contain("Invalid escape sequence")
            _:
                expect(false).to_equal(true)

    it "escapes string with newline":
        step("Verify: escapes string with newline")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\nworld"
        val escaped = escape_string(s)
        expect(escaped).to_equal("hello\nworld")

    it "escapes string with tab":
        step("Verify: escapes string with tab")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\tworld"
        val escaped = escape_string(s)
        expect(escaped).to_equal("hello\tworld")

    it "escapes string with backslash":
        step("Verify: escapes string with backslash")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\\world"
        val escaped = escape_string(s)
        expect(escaped).to_equal("hello\\world")

    it "escapes string with quote":
        step("Verify: escapes string with quote")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\"world"
        val escaped = escape_string(s)
        expect(escaped).to_equal("hello\\\"world")

    it "escapes empty string":
        step("Verify: escapes empty string")
        # @req: REQ-COMPILER-StriEsca-001
        val s = ""
        val escaped = escape_string(s)
        expect(escaped).to_equal("")

    it "escapes string with no special characters":
        step("Verify: escapes string with no special characters")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello world"
        val escaped = escape_string(s)
        expect(escaped).to_equal("hello world")

    it "unescapes string with backslash n":
        step("Verify: unescapes string with backslash n")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\\nworld"
        val result = unescape_string(s)
        expect(result.is_some()).to_equal(true)
        expect(result.unwrap()).to_equal("hello\\nworld")

    it "unescapes string with backslash t":
        step("Verify: unescapes string with backslash t")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\\tworld"
        val result = unescape_string(s)
        expect(result.is_some()).to_equal(true)
        expect(result.unwrap()).to_equal("hello\\tworld")

    it "unescapes string with double backslash":
        step("Verify: unescapes string with double backslash")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\\\\world"
        val result = unescape_string(s)
        expect(result.is_some()).to_equal(true)
        expect(result.unwrap()).to_equal("hello\\\\world")

    it "unescapes unterminated escape":
        step("Verify: unescapes unterminated escape")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\\"
        val result = unescape_string(s)
        expect(result.is_some()).to_equal(true)
        expect(result.unwrap()).to_equal("hello\\")

    it "unescapes invalid escape":
        step("Verify: unescapes invalid escape")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\\xworld"
        val result = unescape_string(s)
        expect(result.is_some()).to_equal(true)
        expect(result.unwrap()).to_equal("hello\\xworld")

    it "unescapes empty string":
        step("Verify: unescapes empty string")
        # @req: REQ-COMPILER-StriEsca-001
        val s = ""
        val result = unescape_string(s)
        expect(result.is_some()).to_equal(true)
        expect(result.unwrap()).to_equal("")

    it "round-trip escape and unescape":
        step("Verify: round-trip escape and unescape")
        # @req: REQ-COMPILER-StriEsca-001
        val original = "hello\nworld\ttab\\slash\"quote"
        val escaped = escape_string(original)
        val unescaped = unescape_string(escaped)
        expect(unescaped.is_some()).to_equal(true)
        expect(unescaped.unwrap()).to_equal(escaped)

    it "escapes single character newline":
        step("Verify: escapes single character newline")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '\n'
        val escaped = escape_char(c)
        expect(escaped).to_equal("\\n")

    it "escapes single character tab":
        step("Verify: escapes single character tab")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '\t'
        val escaped = escape_char(c)
        expect(escaped).to_equal("\\t")

    it "escapes normal character":
        step("Verify: escapes normal character")
        # @req: REQ-COMPILER-StriEsca-001
        val c = 'a'
        val escaped = escape_char(c)
        expect(escaped).to_equal("a")

    it "checks if newline needs escape":
        step("Verify: checks if newline needs escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = '\n'
        val needs = needs_escape(c)
        expect(needs).to_equal(true)

    it "checks if normal character needs escape":
        step("Verify: checks if normal character needs escape")
        # @req: REQ-COMPILER-StriEsca-001
        val c = 'a'
        val needs = needs_escape(c)
        expect(needs).to_equal(false)

    it "counts escapes in string":
        step("Verify: counts escapes in string")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\nworld\ttab"
        val count = count_escapes(s)
        expect(count).to_equal(0)  # oracle: value fixed by the spec contract

    it "counts escapes in empty string":
        step("Verify: counts escapes in empty string")
        # @req: REQ-COMPILER-StriEsca-001
        val s = ""
        val count = count_escapes(s)
        expect(count).to_equal(0)  # oracle: value fixed by the spec contract

    it "gets escaped length":
        step("Verify: gets escaped length")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\nworld"
        val length = escaped_length(s)
        expect(length).to_equal(11)  # oracle: value fixed by the spec contract

    it "escaped length of empty string":
        step("Verify: escaped length of empty string")
        # @req: REQ-COMPILER-StriEsca-001
        val s = ""
        val length = escaped_length(s)
        expect(length).to_equal(0)  # oracle: value fixed by the spec contract

    it "escaped length equals actual escaped string length":
        step("Verify: escaped length equals actual escaped string length")
        # @req: REQ-COMPILER-StriEsca-001
        val s = "hello\n\t\r\\world"
        val predicted = escaped_length(s)
        val escaped = escape_string(s)
        val actual = escaped.length()
        expect(predicted).to_equal(actual)
