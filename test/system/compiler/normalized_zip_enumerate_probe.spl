fn main() -> i64:
    val left = [1, 2]
    val right = [10, 20]
    var total = 0
    for item in left.zip(right).enumerate():
        val (i, pair) = item
        val (a, b) = pair
        total = total + i + a + b
    print(total)
    if total != 34:
        return 1
    0
