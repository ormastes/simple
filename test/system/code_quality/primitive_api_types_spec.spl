"""
## Purpose and audience
Purpose: AC-1: ActorId wraps i64 and exposes .value
Audience: compiler and tooling engineers who maintain this spec

# Primitive API Types Spec — Canonical Wrapper Types

**Feature IDs:** fix-primitive-api-suppressions
**Category:** Tooling
**Difficulty:** 1/5
**Status:** In Progress
**Requirements:** N/A
**Plan:** N/A
**Design:** N/A
**Research:** N/A
**Artifacts:** N/A

## Overview

Tests that the canonical wrapper types in `src/lib/common/types.spl` (existing
and new ones added by Team W in Phase 0) can be constructed and have their
inner value accessed.  Existing wrappers should pass today; new wrappers
(ExitCode, Handle, ErrorCode, MemAddr, DurationMs) will FAIL until Team W
extends types.spl.
"""

# @req: REQ-CODE_QUALITY-PrimApiType-001

use std.spipe.*
# The wrappers are declared in src/lib/common/types.spl. `src/lib/common/` has
# no `__init__.spl`, so a package-root import (`use std.common.{...}`) resolves
# nothing and every constructor below fails with `function X not found` — the
# submodule path is the one that works.
use std.common.types.{
    ActorId, MessageId, ProcessId, BinaryId, RefId,
    ByteSize, Capacity, Count,
    Millis, Nanos, Timestamp,
    Reductions, RefCount, Offset, Length,
    LineNumber, ColumnNumber, IndentLevel,
    PortNumber, HitRate, StringId, SenderId
}


# ============================================================================
# Test Group 1: Existing wrapper types (should pass today)
# ============================================================================

use std.spec.step
describe "canonical wrapper types — existing (types.spl)":
    """
    ## Existing Wrappers

    Verifies construction and `.value` field access for all wrappers already
    present in `src/lib/common/types.spl`.  These specs should PASS today;
    they lock the existing contract so it cannot regress.
    """

    it "AC-1: ActorId wraps i64 and exposes .value":
        step("Verify: AC-1: ActorId wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val id = ActorId(value: 42)
        expect(id.value).to_equal(42)  # oracle: value fixed by the spec contract

    it "AC-1: MessageId wraps i64 and exposes .value":
        step("Verify: AC-1: MessageId wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val id = MessageId(value: 7)
        expect(id.value).to_equal(7)  # oracle: value fixed by the spec contract

    it "AC-1: ProcessId wraps i64 and exposes .value":
        step("Verify: AC-1: ProcessId wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val id = ProcessId(value: 1)
        expect(id.value).to_equal(1)  # oracle: value fixed by the spec contract

    it "AC-1: ByteSize wraps i64 and exposes .value":
        step("Verify: AC-1: ByteSize wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val sz = ByteSize(value: 4096)
        expect(sz.value).to_equal(4096)  # oracle: value fixed by the spec contract

    it "AC-1: Capacity wraps i64 and exposes .value":
        step("Verify: AC-1: Capacity wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val cap = Capacity(value: 128)
        expect(cap.value).to_equal(128)  # oracle: value fixed by the spec contract

    it "AC-1: Count wraps i64 and exposes .value":
        step("Verify: AC-1: Count wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val c = Count(value: 10)
        expect(c.value).to_equal(10)  # oracle: value fixed by the spec contract

    it "AC-1: Millis wraps i64 and exposes .value":
        step("Verify: AC-1: Millis wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val ms = Millis(value: 500)
        expect(ms.value).to_equal(500)  # oracle: value fixed by the spec contract

    it "AC-1: Nanos wraps i64 and exposes .value":
        step("Verify: AC-1: Nanos wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val ns = Nanos(value: 1000000)
        expect(ns.value).to_equal(1000000)  # oracle: value fixed by the spec contract

    it "AC-1: Offset wraps i64 and exposes .value":
        step("Verify: AC-1: Offset wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val off = Offset(value: 64)
        expect(off.value).to_equal(64)  # oracle: value fixed by the spec contract

    it "AC-1: Length wraps i64 and exposes .value":
        step("Verify: AC-1: Length wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val len = Length(value: 32)
        expect(len.value).to_equal(32)  # oracle: value fixed by the spec contract

    it "AC-1: LineNumber wraps i64 and exposes .value":
        step("Verify: AC-1: LineNumber wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val ln = LineNumber(value: 1)
        expect(ln.value).to_equal(1)  # oracle: value fixed by the spec contract

    it "AC-1: ColumnNumber wraps i64 and exposes .value":
        step("Verify: AC-1: ColumnNumber wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val col = ColumnNumber(value: 0)
        expect(col.value).to_equal(0)  # oracle: value fixed by the spec contract

    it "AC-1: PortNumber wraps i32 and exposes .value":
        step("Verify: AC-1: PortNumber wraps i32 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val p = PortNumber(value: 8080)
        expect(p.value).to_equal(8080)  # oracle: value fixed by the spec contract


# ============================================================================
# Test Group 2: New wrappers added by Team W (WILL FAIL until Phase 0 lands)
# ============================================================================

describe "canonical wrapper types — Team W new additions (types.spl)":
    """
    ## New Wrappers (Phase 0, Team W)

    These wrapper types will be added to `src/lib/common/types.spl` by
    Team W before parallel Teams A–E start.  These specs WILL FAIL until
    Team W extends types.spl with ExitCode, Handle, ErrorCode, MemAddr, DurationMs.
    """

    it "AC-1: ExitCode wraps i64 and exposes .value":
        step("Verify: AC-1: ExitCode wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val ec = ExitCode(value: 0)
        expect(ec.value).to_equal(0)  # oracle: value fixed by the spec contract

    it "AC-1: Handle wraps i64 and exposes .value":
        step("Verify: AC-1: Handle wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val h = Handle(value: 3)
        expect(h.value).to_equal(3)  # oracle: value fixed by the spec contract

    it "AC-1: ErrorCode wraps i64 and exposes .value":
        step("Verify: AC-1: ErrorCode wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val err = ErrorCode(value: 22)
        expect(err.value).to_equal(22)  # oracle: value fixed by the spec contract

    it "AC-1: MemAddr wraps i64 and exposes .value":
        step("Verify: AC-1: MemAddr wraps i64 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val addr = MemAddr(value: 0xdeadbeef)
        expect(addr.value).to_equal(0xdeadbeef)

    it "AC-1: DurationMs wraps i32 and exposes .value":
        step("Verify: AC-1: DurationMs wraps i32 and exposes .value")
        # @req: REQ-CODE_QUALITY-PrimApiType-001
        val dur = DurationMs(value: 250)
        expect(dur.value).to_equal(250)  # oracle: value fixed by the spec contract
