# Diagnostic: test each module-level array var individually
use std.cli_output.buffer.{log_point_start, log_point_has_active}

# log_point_has_active checks _lp_ids.len() - works
print "has_active works:"
print log_point_has_active()

# Now try log_point_start which touches _lp_buffer.len() at line 70
print "trying log_point_start..."
val id = log_point_start("test")
print id
