# Test: Call .len() once on module-level [i64] array via import.
# Expected: Works fine, prints 0.
use std.cli_output._test_len.{get_i64_len}

val result = get_i64_len()
print "i64_arr.len() once:"
print result
