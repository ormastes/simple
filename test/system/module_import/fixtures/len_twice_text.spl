# Test: Call .len() twice on module-level [text] array via import.
# KNOWN BUG: Second .len() call corrupts the variable (becomes i64(0)).
use std.cli_output._test_len.{get_text_len_twice}

val result = get_text_len_twice()
print "text_arr.len() twice:"
print result
