# Test: Call .len() once then .push() on module-level [text] array via import.
# Expected: Works fine since .len() is only called once. Prints 0.
use std.cli_output._test_len.{len_then_push}

val result = len_then_push()
print "len_then_push result:"
print result
