# Test: Call .len() on multiple different arrays (one .len() per array).
# Expected: Works fine since each array only has .len() called once.
use std.cli_output._test_len.{get_multi_arr_lens}

val result = get_multi_arr_lens()
print "multi array lens sum (0+0+0):"
print result
