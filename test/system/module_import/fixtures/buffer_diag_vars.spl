# Diagnostic: check which module-level array vars are broken
use std.cli_output.buffer.{log_point_has_active, log_print, log_point_start}

# First test: log_print works (calls _lp_ids.len() internally)
log_print("test line via log_print")

# Second test: log_point_has_active also calls _lp_ids.len()
print log_point_has_active()

# Third test: log_point_start calls _lp_check_stale then _lp_ids.len()
val id = log_point_start("test")
print "started point with id:"
print id
