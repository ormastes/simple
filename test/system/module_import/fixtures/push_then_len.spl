# Test: Call .push() then .len() on module-level [text] array via import.
# Tests whether .push() also triggers corruption when followed by .len().
use std.cli_output._test_len.{push_then_get_len}

val result = push_then_get_len()
print "push_then_len result:"
print result
