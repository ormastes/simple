# Test: Cache .len() in local var and use cached value twice (THE WORKAROUND).
# Expected: Works fine since .len() is only called once, result is reused.
use std.cli_output._test_len.{get_text_len_cached}

val result = get_text_len_cached()
print "cached len (0 + 0):"
print result
