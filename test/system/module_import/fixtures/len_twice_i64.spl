# Test: Call .len() twice on module-level [i64] array via import.
# KNOWN BUG: Second .len() call corrupts the variable (becomes i64(0)).
use std.cli_output._test_len.{get_i64_len_twice}

val result = get_i64_len_twice()
print "i64_arr.len() twice:"
print result
