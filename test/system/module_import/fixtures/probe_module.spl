fn probe_value() -> i64:
    42

# `simple run` rejects a declaration-only module ("no `main` function and no
# top-level statements"), so the direct-run probe needs an entry point to be
# able to prove the file loads.
fn main():
    print probe_value()
