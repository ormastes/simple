# Test: Call .len() once on module-level [text] array via import.
# Expected: Works fine, prints 0.
use std.cli_output._test_len.{get_text_len}

val result = get_text_len()
print "text_arr.len() once:"
print result
