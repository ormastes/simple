# Test direct file imports vs module imports

# Test 1: Import formatter directly (this works)
use app.formatter.main.FormatConfig

describe "Direct formatter import":
    it "creates FormatConfig":
        val config = FormatConfig(
            indent_size: 4,
            max_line_length: 100,
            use_tabs: false,
            blank_lines_between_items: 2,
            continuation_indent: 8
        )
        expect config.indent_size == 4
