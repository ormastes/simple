"""
# System Test Batch

**Feature IDs:** #SYS
**Category:** Testing
**Status:** Implemented
"""

use std.spec.{check}

describe "System Test":
    slow_it "test 1":
        check(1 + 1 == 2)
    slow_it "test 2":
        check("a" == "a")
    slow_it "test 3":
        val x = 5
        check(x > 0)
    slow_it "test 4":
        val arr = [1, 2, 3]
        check(arr.len() == 3)
    slow_it "test 5":
        for i in 0..10:
            check(i >= 0)
    slow_it "test 6":
        val opt = Some(42)
        check(opt.?)
    slow_it "test 7":
        match Some(1):
            Some(x): check(x == 1)
            nil: check(false)
    slow_it "test 8":
        if true:
            check(true)
        else:
            check(false)
    slow_it "test 9":
        val d = {"k": "v"}
        check(d["k"] == "v")
    slow_it "test 10":
        var sum = 0
        for i in 0..5:
            sum = sum + i
        check(sum == 10)
