fn main() -> i32:
    # Deliberately no compiler import, struct, array, or method receiver.
    # This is the minimum native-build baseline for the admitted Stage 2 tool.
    val answer: i64 = 40 + 2
    val baseline_ok = answer == 42
    print "stage3_plain_scalar_native_baseline={baseline_ok}"
    if baseline_ok:
        return 0
    1
