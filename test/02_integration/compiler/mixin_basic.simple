# Basic mixin test - Phase 1: Parsing only

mixin Timestamp
    created_at: i64
    updated_at: i64
    
    fn get_created() -> i64
        return self.created_at

mixin Serializable<T>
    fn to_json() -> str
        return "{}"

class User
    id: i64
    name: str

fn main()
    println("Mixin parsing test")
