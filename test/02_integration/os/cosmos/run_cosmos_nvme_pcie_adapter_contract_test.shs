#!/bin/sh
# The adapter contract now requires the admitted pure-Simple policy object.
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../../.." && pwd)
exec "$root/scripts/check/check-cosmos-nvme-pcie-adapter-policy.shs"
