"""
## Purpose and audience
Purpose: Prove that Collections.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
# @manual: primary
REQ-FEATURE-COLLECTIONS-001
doc/01_research/feature/REQ-FEATURE-COLLECTIONS-001.md
doc/03_plan/feature/REQ-FEATURE-COLLECTIONS-001.md
doc/04_architecture/feature/REQ-FEATURE-COLLECTIONS-001.md
doc/05_design/feature/REQ-FEATURE-COLLECTIONS-001.md
"""
# @req: REQ-FEATURE-COLLECTIONS-001


"""
# Collections (Interpreter)

**Category:** Runtime
**Status:** In Progress

## Overview

Tests collection type handling in the interpreter including lists, maps, and
iteration. Verifies that collection operations produce correct results when
executed in interpreted mode with proper element access and mutation.
"""
# # Collections Specification
#
# **Feature IDs:** #COLLECTIONS
# **Category:** Core
# **Status:** Implemented
#
# Tests Python-inspired collection types and operations in Simple.
# Covers lists, dictionaries, sets, and common collection methods.

use std.spec.step

describe "Collections":
    # ## Collection Types and Operations
    #
    # Verifies Python-like collection behavior:
    # - List creation and manipulation
    # - Dictionary key-value operations
    # - Collection methods (map, filter, etc.)
    # - List comprehensions

    context "list operations":
        it "creates list literal":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: creates list literal")
            val items = [1, 2, 3]
            expect items.len() == 3

        it "accesses by index":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: accesses by index")
            val items = ["a", "b", "c"]
            expect items[0] == "a"
            expect items[2] == "c"

        it "supports negative indexing":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: supports negative indexing")
            val items = [10, 20, 30]
            expect items[-1] == 30

        it "slices list":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: slices list")
            val items = [1, 2, 3, 4, 5]
            expect items[1:4] == [2, 3, 4]

    context "dictionary operations":
        it "creates dict literal":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: creates dict literal")
            val ages = {"alice": 30, "bob": 25}
            expect ages["alice"] == 30

        it "checks key existence":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: checks key existence")
            val data = {"key": "value"}
            expect data.has("key") == true
            expect data.has("missing") == false

    context "collection methods":
        it "maps over list":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: maps over list")
            val nums = [1, 2, 3]
            val doubled = nums.map(_ * 2)
            expect doubled == [2, 4, 6]

        it "filters list":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: filters list")
            val nums = [1, 2, 3, 4, 5]
            val evens = nums.filter(_ % 2 == 0)
            expect evens == [2, 4]

    context "list comprehensions":
        it "creates list with comprehension":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: creates list with comprehension")
            val squares = [for x in 0..5: x * x]
            expect squares == [0, 1, 4, 9, 16]

        it "filters in comprehension":
            # @req: REQ-FEATURE-COLLECTIONS-001
            step("Verify: filters in comprehension")
            val evens = [for x in 0..10 if x % 2 == 0: x]
            expect evens == [0, 2, 4, 6, 8]
