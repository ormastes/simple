"""
## Purpose and audience
Purpose: Prove that Simple Interpreter.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
# @manual: primary
REQ-FEATURE-INTERPRETER-SAMPLE-001
doc/01_research/feature/REQ-FEATURE-INTERPRETER-SAMPLE-001.md
doc/03_plan/feature/REQ-FEATURE-INTERPRETER-SAMPLE-001.md
doc/04_architecture/feature/REQ-FEATURE-INTERPRETER-SAMPLE-001.md
doc/05_design/feature/REQ-FEATURE-INTERPRETER-SAMPLE-001.md
"""


"""
# Interpreter Sample Programs

**Category:** Runtime
**Status:** In Progress

## Overview

Tests the Simple language interpreter with representative sample programs covering
arithmetic operations, comparison operators, boolean logic, and string handling.
Verifies that complete programs execute correctly in interpreted mode.
"""
# System test for the Simple language interpreter.
#
# Tests core interpreter functionality including arithmetic operations,
# comparison operators, boolean logic, and string handling.


use std.spec.step

describe "Simple Interpreter":
    # Verifies the interpreter correctly evaluates expressions, handles
    # operators, and processes string operations including interpolation.
    context "when evaluating expressions":
        it "handles arithmetic operations":
            # @req: REQ-FEATURE-INTERPRETER-SAMPLE-001
            # @req: REQ-FEATURE-INTERPRETER-SAMPLE-001
            step("Verify: handles arithmetic operations")
            expect(1 + 1).to_equal(2)  # oracle: 2 — pinned expected value for this behavior
            expect(10 - 3).to_equal(7)  # oracle: 7 — pinned expected value for this behavior
            expect(4 * 5).to_equal(20)  # oracle: 20 — pinned expected value for this behavior
            expect(15 / 3).to_equal(5)  # oracle: 5 — pinned expected value for this behavior

        it "handles comparison operations":
            # @req: REQ-FEATURE-INTERPRETER-SAMPLE-001
            step("Verify: handles comparison operations")
            expect(5 > 3).to_equal(true)
            expect(2 < 10).to_equal(true)
            expect(5).to_equal(5)  # oracle: 5 — pinned expected value for this behavior
            expect(3 != 4).to_equal(true)

        it "handles boolean operations":
            # @req: REQ-FEATURE-INTERPRETER-SAMPLE-001
            step("Verify: handles boolean operations")
            expect(true and true).to_equal(true)
            expect(true and false).to_equal(false)
            expect(true or false).to_equal(true)
            expect(not false).to_equal(true)

    context "when working with strings":
        it "supports string concatenation":
            # @req: REQ-FEATURE-INTERPRETER-SAMPLE-001
            step("Verify: supports string concatenation")
            val s = "Hello" + " " + "World"
            expect(s).to_equal("Hello World")

        it "supports string interpolation":
            # @req: REQ-FEATURE-INTERPRETER-SAMPLE-001
            step("Verify: supports string interpolation")
            val name = "Alice"
            val age = 30
            val msg = "Name: {name}, Age: {age}"
            expect(msg).to_equal("Name: Alice, Age: 30")
