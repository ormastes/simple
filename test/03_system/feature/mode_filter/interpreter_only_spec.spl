# @cover src/lib/gc_sync_mut/js/engine/interpreter.spl 0%
"""
## Purpose and audience

Purpose: the behavior asserted in this spec  Audience: engineers reading this spec to confirm the behavior still holds.

## Operator workflow

1. Run `bin/simple test test/03_system/feature/mode_filter/interpreter_only_spec.spl`.
2. Every scenario must pass; a failure is a regression in the behavior under test.

## Compatibility and limitations

Covers the behavior asserted here; platform-specific behavior is out of scope.
"""
use std.spec.step

# doc-path: doc/01_research/local/interpreter_only_spec.md
# doc-path: doc/03_plan/sys_test/interpreter_only_spec.md
# doc-path: doc/04_architecture/interpreter_only_spec.md
# doc-path: doc/05_design/interpreter_only_spec_oracles.md
# @manual: primary

"""
# Interpreter-Only Mode Tests

**Category:** Testing Framework
**Status:** Active

## Overview

Tests that are restricted to interpreter mode via `@mode: interpreter` annotation.
"""

# @mode: interpreter
# Tests that only work in interpreter mode

describe "Interpreter-only features":
    it "can run in interpreter mode":
        # @req: REQ-SSPEC-LOCAL-001
        step("Verify: can run in interpreter mode")
        expect(1 + 1).to_equal(2)
