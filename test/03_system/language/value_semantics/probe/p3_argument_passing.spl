# Cross-engine value-semantics probe P3: struct passed as argument.
struct Flat:
    a: f64
    b: i64

fn mut_arg(s: Flat):
    s.a = 55.0

fn main():
    print("PROBE|CONTROL={40 + 2}")
    var g = Flat(a: 1.0, b: 2)
    mut_arg(g)
    print("PROBE|S3 g.a={g.a}")

main()
