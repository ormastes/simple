# Cross-engine value-semantics probe P9: text value semantics (T1).
# Text is a value type everywhere — expected-AGREE anchor probe.
fn main():
    print("PROBE|CONTROL={40 + 2}")
    var t1 = "abc"
    var t2 = t1
    t2 = t2 + "X"
    print("PROBE|T1 t1={t1} t2={t2}")

main()
