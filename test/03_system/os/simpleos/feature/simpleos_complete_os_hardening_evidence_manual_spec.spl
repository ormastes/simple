"""
## Purpose and audience
Purpose: Prove that REQ-001: capability ledger.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
# @manual: primary
REQ-OS-COMPLETE-OS-001
doc/01_research/os/REQ-OS-COMPLETE-OS-001.md
doc/03_plan/os/REQ-OS-COMPLETE-OS-001.md
doc/04_architecture/os/REQ-OS-COMPLETE-OS-001.md
doc/05_design/os/REQ-OS-COMPLETE-OS-001.md
"""
# @req: REQ-OS-COMPLETE-OS-001


# codex-system-test
# @tag:system
# @evidence-display: links
# @req REQ-001 REQ-019 REQ-020 NFR-001 NFR-007 NFR-008 NFR-009 NFR-012 NFR-013 NFR-014

"""# Capability ledger, ownership, and evidence manual

Defines fail-closed acceptance for ledger truth, canonical ownership, three environment classes, freshness, duplication, stubs, convergence, and manual quality.

This is an explicit BLOCKED traceability spec. Each checker validates a
structurally complete blocked candidate through the production capability-ledger
owner and reports its executable owner, exact expected receipt, and resume
command. A row cannot pass until that receipt is produced and admitted.

**TUI Captures:** build/test-artifacts/03_system/os/simpleos/feature/simpleos_complete_os_hardening_evidence_manual_spec/
"""

use std.spec.*
use test.helpers.simpleos_complete_os_hardening_steps.{
    check_simpleos_capability_matrix,
    check_simpleos_perf_duplication,
    step_boot_target,
    step_measure_resource_budget
}

use std.spec.step

describe "REQ-001: capability ledger":
    # @capture(text)
    # @capture(api)
    it "should accept complete live evidence for capability ledger":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for capability ledger")
        step_boot_target()
        check_simpleos_capability_matrix("REQ-001", "happy")

    # @manual: folded
    it "should preserve the selected boundary for capability ledger":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for capability ledger")
        step_boot_target()
        check_simpleos_capability_matrix("REQ-001", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for capability ledger":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for capability ledger")
        step_boot_target()
        check_simpleos_capability_matrix("REQ-001", "rejection")

describe "REQ-019: canonical ownership and duplicate removal":
    # @capture(artifact)
    it "should accept complete live evidence for canonical ownership and duplicate removal":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for canonical ownership and duplicate removal")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("REQ-019", "happy")

    # @manual: folded
    it "should preserve the selected boundary for canonical ownership and duplicate removal":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for canonical ownership and duplicate removal")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("REQ-019", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for canonical ownership and duplicate removal":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for canonical ownership and duplicate removal")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("REQ-019", "rejection")

describe "REQ-020: evidence, manuals, and knowledge":
    # @capture(artifact)
    it "should accept complete live evidence for evidence, manuals, and knowledge":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for evidence, manuals, and knowledge")
        step_boot_target()
        check_simpleos_capability_matrix("REQ-020", "happy")

    # @manual: folded
    it "should preserve the selected boundary for evidence, manuals, and knowledge":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for evidence, manuals, and knowledge")
        step_boot_target()
        check_simpleos_capability_matrix("REQ-020", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for evidence, manuals, and knowledge":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for evidence, manuals, and knowledge")
        step_boot_target()
        check_simpleos_capability_matrix("REQ-020", "rejection")

describe "NFR-001: architecture evidence":
    # @capture(artifact)
    it "should accept complete live evidence for architecture evidence":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for architecture evidence")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-001", "happy")

    # @manual: folded
    it "should preserve the selected boundary for architecture evidence":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for architecture evidence")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-001", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for architecture evidence":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for architecture evidence")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-001", "rejection")

describe "NFR-007: parallel ownership safety":
    # @capture(artifact)
    it "should accept complete live evidence for parallel ownership safety":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for parallel ownership safety")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-007", "happy")

    # @manual: folded
    it "should preserve the selected boundary for parallel ownership safety":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for parallel ownership safety")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-007", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for parallel ownership safety":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for parallel ownership safety")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-007", "rejection")

describe "NFR-008: duplication gate":
    # @capture(artifact)
    it "should accept complete live evidence for duplication gate":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for duplication gate")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-008", "happy")

    # @manual: folded
    it "should preserve the selected boundary for duplication gate":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for duplication gate")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-008", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for duplication gate":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for duplication gate")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-008", "rejection")

describe "NFR-009: coverage and stub prevention":
    # @capture(artifact)
    it "should accept complete live evidence for coverage and stub prevention":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for coverage and stub prevention")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-009", "happy")

    # @manual: folded
    it "should preserve the selected boundary for coverage and stub prevention":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for coverage and stub prevention")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-009", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for coverage and stub prevention":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for coverage and stub prevention")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-009", "rejection")

describe "NFR-012: evidence freshness":
    # @capture(artifact)
    it "should accept complete live evidence for evidence freshness":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for evidence freshness")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-012", "happy")

    # @manual: folded
    it "should preserve the selected boundary for evidence freshness":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for evidence freshness")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-012", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for evidence freshness":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for evidence freshness")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-012", "rejection")

describe "NFR-013: convergence guard":
    # @capture(artifact)
    it "should accept complete live evidence for convergence guard":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for convergence guard")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-013", "happy")

    # @manual: folded
    it "should preserve the selected boundary for convergence guard":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for convergence guard")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-013", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for convergence guard":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for convergence guard")
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-013", "rejection")

describe "NFR-014: documentation quality":
    # @capture(artifact)
    it "should accept complete live evidence for documentation quality":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should accept complete live evidence for documentation quality")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-014", "happy")

    # @manual: folded
    it "should preserve the selected boundary for documentation quality":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should preserve the selected boundary for documentation quality")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-014", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for documentation quality":
        # @req: REQ-OS-COMPLETE-OS-001
        step("Verify: should reject missing stale substituted or invalid evidence for documentation quality")
        step_boot_target()
        check_simpleos_capability_matrix("NFR-014", "rejection")
