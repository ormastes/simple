# codex-system-test
# @tag:system
# @evidence-display: links
# @req REQ-017 REQ-018 NFR-002 NFR-003 NFR-004 NFR-005

"""# Window manager and production campaigns

Defines fail-closed live acceptance for canonical WM interaction/readback, observability, strict performance, fuzz, soak, and lifecycle bounds.

This is an explicit BLOCKED traceability spec. Each checker validates a
structurally complete blocked candidate through the production capability-ledger
owner and reports its executable owner, exact expected receipt, and resume
command. A row cannot pass until that receipt is produced and admitted.

**Screenshots:** doc/06_spec/image/03_system/os/simpleos/feature/simpleos_complete_os_hardening_wm_perf_campaign_spec/
"""

use std.spec.*
use test.helpers.simpleos_complete_os_hardening_steps.{
    check_simpleos_perf_duplication,
    check_simpleos_wm,
    step_exercise_window_manager,
    step_measure_resource_budget
}

describe "REQ-017: production SimpleOS window manager":
    # @capture(gui)
    # @capture(protocol)
    it "should accept complete live evidence for production SimpleOS window manager":
        step_exercise_window_manager()
        check_simpleos_wm("REQ-017", "happy")

    # @manual: folded
    it "should preserve the selected boundary for production SimpleOS window manager":
        step_exercise_window_manager()
        check_simpleos_wm("REQ-017", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for production SimpleOS window manager":
        step_exercise_window_manager()
        check_simpleos_wm("REQ-017", "rejection")

describe "REQ-018: observability and performance ownership":
    # @capture(artifact)
    it "should accept complete live evidence for observability and performance ownership":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("REQ-018", "happy")

    # @manual: folded
    it "should preserve the selected boundary for observability and performance ownership":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("REQ-018", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for observability and performance ownership":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("REQ-018", "rejection")

describe "NFR-002: strict performance budgets":
    # @capture(artifact)
    it "should accept complete live evidence for strict performance budgets":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-002", "happy")

    # @manual: folded
    it "should preserve the selected boundary for strict performance budgets":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-002", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for strict performance budgets":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-002", "rejection")

describe "NFR-003: reproducible measurement":
    # @capture(artifact)
    it "should accept complete live evidence for reproducible measurement":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-003", "happy")

    # @manual: folded
    it "should preserve the selected boundary for reproducible measurement":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-003", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for reproducible measurement":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-003", "rejection")

describe "NFR-004: mission-critical robustness":
    # @capture(artifact)
    it "should accept complete live evidence for mission-critical robustness":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-004", "happy")

    # @manual: folded
    it "should preserve the selected boundary for mission-critical robustness":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-004", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for mission-critical robustness":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-004", "rejection")

describe "NFR-005: static core and dynamic application bounds":
    # @capture(artifact)
    it "should accept complete live evidence for static core and dynamic application bounds":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-005", "happy")

    # @manual: folded
    it "should preserve the selected boundary for static core and dynamic application bounds":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-005", "boundary")

    # @manual: folded
    it "should reject missing stale substituted or invalid evidence for static core and dynamic application bounds":
        step_measure_resource_budget()
        check_simpleos_perf_duplication("NFR-005", "rejection")
