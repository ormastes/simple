"""
## Purpose and audience
Purpose: Prove that Compile smoke.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
# @manual: primary
REQ-INFRASTRUCTU-COMPILE-SMOKE-001
doc/01_research/infrastructu/REQ-INFRASTRUCTU-COMPILE-SMOKE-001.md
doc/03_plan/infrastructu/REQ-INFRASTRUCTU-COMPILE-SMOKE-001.md
doc/04_architecture/infrastructu/REQ-INFRASTRUCTU-COMPILE-SMOKE-001.md
doc/05_design/infrastructu/REQ-INFRASTRUCTU-COMPILE-SMOKE-001.md
"""
# @req: REQ-INFRASTRUCTU-COMPILE-SMOKE-001


# Minimal spipe that the --compile / --mode=native pipeline should handle
# end-to-end without touching the known-broken wrapper paths:
#   - no class definitions
#   - no skip() / pending
#   - no top-level let/val bindings outside fn/describe
# If this fails under `bin/simple test --compile`, the native-mode exec
# wiring itself is broken. If it passes, the wiring is correct and any
# larger failure is in preprocess_spipe_for_smf or spipe runtime linkage.

use std.spec.step

describe "Compile smoke":
    it "evaluates literal equality":
        # @req: REQ-INFRASTRUCTU-COMPILE-SMOKE-001
        step("Verify: evaluates literal equality")
        expect 1 + 1 == 2

    it "evaluates string equality":
        # @req: REQ-INFRASTRUCTU-COMPILE-SMOKE-001
        step("Verify: evaluates string equality")
        expect "ok" == "ok"
