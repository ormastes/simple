# Fixture: annotation on the loop HEADER line exempts it (new=0). Proves the
# per-loop-header annotation placement required by design §6.1 / item #5.
fn fill(buf: [i32], w: i32):
    var x = 0
    while x < w:  # cpu-lane-loop-ok: header-annotated fixture
        buf[x] = 0
        x = x + 1
