"""Frozen RED gate for the admitted native ContextStoreV1 sort receipt."""

use std.spec.{describe, it, step}

describe "ContextStoreV1 bounded read ordering native evidence":
    it "should require an admitted exact GDB comparison receipt":
        step("Bound context store ordering comparisons on admitted native execution")
        # No authenticated receipt parser is admitted yet. Do not accept an
        # arbitrary status=PASS string from a hand-written manifest.
        fail("Missing admitted native sort comparison receipt")
