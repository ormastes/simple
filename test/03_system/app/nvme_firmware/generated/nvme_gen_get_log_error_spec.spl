# GENERATED FILE - do not edit by hand.
# Generated by: src/app/nvme_spec_import (bin/simple run src/app/nvme_spec_import/main.spl generate)
# Regenerate after changing src/app/nvme_spec_import/obligations.spl or feature_map.spl.
# Purpose and audience: executable specification evidence for the owning engineering team.
# @req REQ-SSPEC-SYSTEM
# @tag: system, nvme, generated, mandatory-feature

"""
Mandatory NVMe feature: Get Log Page - Error Information log (class 1).

One block per test obligation. Evidence level available today:
host-equivalent transport (SQE in queue memory, doorbell, phase-tagged CQE poll)

Extracted clause records mapped onto this feature: 9
(see spec/nvme/clauses.sdn; the clause ledger carries identifiers and
generated paraphrases only - no specification prose is in this repository).

CLAIM BOUNDARY. This file does not prove NVMe conformance. It proves that
the named in-tree programs ran and printed the named verdict tokens, on a
host-model Simple run. It does not prove a linked firmware image, a QEMU or
board execution, PCIe interoperability, byte-accurate Identify or log-page
layouts, real 4 KiB payloads, or power-loss durability at media fidelity.
Blocks named BLOCKED assert an admitted gap in the coverage ledger; they are
not evidence that the feature works.
"""

use std.spec.step

use std.spec.*
use std.io_runtime.{process_run}
use std.fs.{read_file}

fn _run(cmd: text) -> (text, text, i64):
    process_run("/bin/sh", ["-c", cmd])

describe "NVMe mandatory feature: Get Log Page - Error Information log":

    # @manual scenario evidence
    it "should issue the well-formed command and observe the spec-shaped completion and state effect (positive)":
        # @req REQ-SSPEC-SYSTEM
        step("Obligation positive for Get Log Page - Error Information log")

        step("Run the bound in-tree evidence program")
        val (out, err, rc) = _run("bin/simple run examples/09_embedded/simpleos_nvme_fw/fw/admin_transport_check.spl 2>&1")
        val log: text = out + err

        step("A silent exit is INCONCLUSIVE, not a pass: bin/simple run exit codes are")
        step("garbage for fn main() -> (), so the verdict token is the only sound signal")
        expect(log).to_contain("all 13 admin opcodes travelled exec_admin")

        # EVIDENCE GRADE: aggregate. This token is a whole-program verdict
        # covering several features at once. It proves the path ran and the
        # program's own expectations held; it does not isolate this feature.

    # @manual scenario evidence
    it "BLOCKED: cannot yet issue the malformed variant and observe the correct error status with NO state change (negative)":
        # @req REQ-SSPEC-SYSTEM
        step("Obligation negative for Get Log Page - Error Information log has no in-tree evidence")

        step("Assert the coverage ledger still records this cell as pending, with this blocker")
        # Not a device assertion - there is no device behaviour to assert while
        # this cell is blocked. This goes red if the ledger is edited without
        # regenerating, which is what keeps the blocker text from rotting.
        val ledger: text = read_file("spec/nvme/coverage.sdn")
        expect(ledger).to_contain("get_log_error")
        expect(ledger).to_contain("no in-tree program drives the negative obligation for this feature to a named verdict token yet; the firmware path exists but nothing asserts it")

    # @manual scenario evidence
    it "BLOCKED: cannot yet perform the operation, reset, and observe the spec's reset-survival rules (reset)":
        # @req REQ-SSPEC-SYSTEM
        step("Obligation reset for Get Log Page - Error Information log has no in-tree evidence")

        step("Assert the coverage ledger still records this cell as pending, with this blocker")
        # Not a device assertion - there is no device behaviour to assert while
        # this cell is blocked. This goes red if the ledger is edited without
        # regenerating, which is what keeps the blocker text from rotting.
        val ledger: text = read_file("spec/nvme/coverage.sdn")
        expect(ledger).to_contain("get_log_error")
        expect(ledger).to_contain("no in-tree program drives the reset obligation for this feature to a named verdict token yet; the firmware path exists but nothing asserts it")

    # @manual scenario evidence
    it "should perform the operation under injected failure and observe an error status plus correct health accounting (fault)":
        # @req REQ-SSPEC-SYSTEM
        step("Obligation fault for Get Log Page - Error Information log")

        step("Run the bound in-tree evidence program")
        val (out, err, rc) = _run("bin/simple run examples/09_embedded/simpleos_nvme_fw/fw/nvme_emu_recovery_check.spl 2>&1")
        val log: text = out + err

        step("A silent exit is INCONCLUSIVE, not a pass: bin/simple run exit codes are")
        step("garbage for fn main() -> (), so the verdict token is the only sound signal")
        expect(log).to_contain("NVME EMU RECOVERY OK")

        # EVIDENCE GRADE: aggregate. This token is a whole-program verdict
        # covering several features at once. It proves the path ran and the
        # program's own expectations held; it does not isolate this feature.

    # @manual scenario evidence
    it "BLOCKED: cannot yet perform the operation, power-cycle, and observe that durable effects survive (persistence)":
        # @req REQ-SSPEC-SYSTEM
        step("Obligation persistence for Get Log Page - Error Information log has no in-tree evidence")

        step("Assert the coverage ledger still records this cell as pending, with this blocker")
        # Not a device assertion - there is no device behaviour to assert while
        # this cell is blocked. This goes red if the ledger is edited without
        # regenerating, which is what keeps the blocker text from rotting.
        val ledger: text = read_file("spec/nvme/coverage.sdn")
        expect(ledger).to_contain("get_log_error")
        expect(ledger).to_contain("no in-tree program drives the persistence obligation for this feature to a named verdict token yet; the firmware path exists but nothing asserts it")
