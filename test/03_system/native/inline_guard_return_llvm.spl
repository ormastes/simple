# Regression guard for `native-build --backend llvm`: an inline guarded return
# must lower as a return statement, rather than a global load named `return`.

fn guarded(value: i64) -> bool:
    if value < 0: return false
    true

fn main() -> i64:
    if guarded(-1) or not guarded(0):
        return 1
    0
