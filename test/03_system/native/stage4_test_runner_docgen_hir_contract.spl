# Stage 4 HIR ownership regression for the production test-runner module.

use app.test_runner_new.test_runner_main.{generate_spipe_docs_for_results}

extern fn rt_exit(code: i32)

fn main() -> i64:
    # Importing the public docgen hook lowers its bounded process call and the
    # surrounding runner module without spawning a subprocess in this probe.
    rt_exit(30)
    0
