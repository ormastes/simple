# Stage 4 HIR ownership regression for the physical CLI handler module.

use app.io._CliCommands.handler_commands.{cli_handle_web}

extern fn rt_exit(code: i32)

fn main() -> i64:
    val status = cli_handle_web([])
    if status != 1:
        rt_exit(41)
    rt_exit(30)
    0
