struct Box:
    value: i64

fn apply(f: fn(i64) -> i64, x: i64) -> i64:
    return f(x)

fn main() -> i64:
    var n = 5
    val add_n = \x: x + n
    n = 100
    print(apply(add_n, 3))

    val box = Box(value: 40)
    val add_box = \x: box.value + x
    print(apply(add_box, 2))
    return 0
