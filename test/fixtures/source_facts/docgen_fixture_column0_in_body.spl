describe "body with a fixture":
    it "keeps every line after a column-zero fixture":
        val src = """
fn main():
    print(73)
"""
        val out = run(src)
        expect(out).to_equal(seventy_three())
        expect(after_fixture_marker()).to_equal(true)
