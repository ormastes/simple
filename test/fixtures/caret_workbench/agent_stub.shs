#!/bin/sh
# Stand-in agent CLI for the caret_workbench end-to-end oracle.
# Ignores the provider argv the launch plan builds and stays alive so the
# manager observes a REAL running child. `exec` so process_kill signals sleep
# itself, not a shell wrapper that would leave it orphaned.
exec sleep 60
