use test.fixtures.interpreter_global_index_persistence.state.{get_slot, shadow_slot}

fn main() -> i64:
    if shadow_slot(["input"]) != "local":
        return 1
    if get_slot() != "":
        return 2
    0
