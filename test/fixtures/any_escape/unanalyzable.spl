# Selftest fixture: a source the checker CANNOT analyze.
#
# Exists to pin the census denominator. Before this fixture, nothing proved
# that an unparseable file is COUNTED as unanalyzable rather than silently
# dropped -- and a dropped file lowers the Any-site and escape counts, which
# reads as progress. That is the fail-open shape recorded in
# doc/08_tracking/bug/any_escape_census_undercounts_2026-08-21.md.
#
# Deliberately malformed: the block after `fn` is never opened.
fn broken( -> Any
    val x =
