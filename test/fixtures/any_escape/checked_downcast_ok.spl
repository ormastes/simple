# Fixture: `Any` exists only inside a granted `type_erasure` region and leaves
# only as the concrete result of a checked conversion -> zero findings.
@unsafe(reason: "representation boundary", capabilities: [type_erasure])
fn checked_downcast(value: Any) -> i64:
    0

@unsafe(reason: "decode legacy plugin payload", capabilities: [type_erasure])
fn decode() -> i64:
    var decoded = 0
    unsafe:
        val raw: Any = 1
        decoded = checked_downcast(raw)
    decoded
