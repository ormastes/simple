# A tuple destructure lowers to `HirStmtKind.Let(sym, nil, init)` -- the
# desugared bindings carry NO declared type. The `Any` escape checker must
# tolerate that instead of reading `.kind` off nil.
fn split_pair() -> i64:
    val pair = (1, 2)
    val (first, second) = pair
    first + second
