# Fixture: block form that asks for `raw_ptr` ONLY. Any inside is NOT licensed
# (§8.1: only `type_erasure` permits erasure) -> E-MC-ANY-001.
fn poke() -> i64:
    unsafe(reason: "device register", capabilities: [raw_ptr]):
        val raw: Any = 1
    4
