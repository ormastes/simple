# Fixture: block form grants type_erasure, but the erased value is returned
# out of the region -> E-MC-ANY-002 (escape), not an origin report.
fn leak_raw() -> Any:
    unsafe(reason: "decode", capabilities: [type_erasure]):
        val raw: Any = 1
        return raw
    return 0
