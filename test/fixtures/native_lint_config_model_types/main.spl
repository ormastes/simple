use compiler.tools.lint._LintMain.config_and_model.{parse_lint_profile}

fn main():
    if val _ = parse_lint_profile("critical"):
        print("lint config model resolved: true")
    else:
        print("lint config model resolved: false")
