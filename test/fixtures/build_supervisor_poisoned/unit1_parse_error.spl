# POISONED FIXTURE unit 1 of 6 — expected outcome: ERROR
#
# A real parse error: the parameter list is never closed and the body is
# nonsense. The compiler must exit non-zero with diagnostics and WITHOUT dying
# by a signal, so the supervisor classifies this ERROR, not CRASHED.
#
# Do NOT "fix" this file. Its brokenness is the fixture.

fn poisoned_unit_one(a: i64, b: i64 -> i64:
    a + + )
