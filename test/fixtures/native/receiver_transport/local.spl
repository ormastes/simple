use support.{ReceiverTransport}

fn main() -> i64:
    val receiver: ReceiverTransport = ReceiverTransport(marker: 100)
    val value = receiver.read_marker(7)
    print "local={value}"
    if value != 107:
        return 1
    0
