# This production import contributes an unrelated optional-return join method.
use std.nogc_sync_mut.concurrent.thread.{ThreadHandle}

fn main() -> i64:
    val rendered = "projections:\n" + [for projection in ["VERSION"]: "  - " + projection].join("\n") + "\n"
    if rendered != "projections:\n  - VERSION\n":
        return 1
    print("typed array join PASS")
    0
