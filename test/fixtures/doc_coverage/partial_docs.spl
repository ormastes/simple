# Partially Documented Module - Mixed Coverage

# This function has an inline comment
fn with_inline(x: i64) -> i64:
    x * 2

fn with_docstring(x: i64) -> i64:
    # This function has a docstring but no inline comment.
    x + 1

# This function has both inline comment and docstring
fn with_both(x: i64) -> i64:
    # This function is well documented with both.
    #
    # sdoctest:
    # val result = with_both(5)
    # expect(result).to_equal(10)
    x * 2

fn no_docs(x: i64) -> i64:
    x - 1

# Documented struct
struct DocumentedStruct:
    # A struct with documentation.
    field: i64

struct UndocumentedStruct:
    field: i64

export with_inline, with_docstring, with_both, no_docs, DocumentedStruct, UndocumentedStruct
