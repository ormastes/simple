use app.test_cache_shared.{_dep_hash}

fn main() -> i64:
    val fingerprint = _dep_hash("src/app/test_cache_shared.spl", [])
    val passed = fingerprint.len() > 0
    print("test cache file stat facade resolved: {passed}")
    if passed: 0 else: 1
