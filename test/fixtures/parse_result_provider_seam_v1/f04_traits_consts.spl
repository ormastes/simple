# Frozen corpus entry: trait, impl, constants, type alias.
const LIMIT: i64 = 64

trait Shape:
    fn area(self) -> i64

struct Square:
    side: i64

impl Shape for Square:
    fn area(self) -> i64:
        self.side * self.side
