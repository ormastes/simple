# Frozen corpus entry: plain functions, params, defaults, returns.
fn add(a: i64, b: i64) -> i64:
    a + b

fn greet(name: text, times: i64) -> text:
    var out = ""
    var i = 0
    while i < times:
        out = out + name
        i = i + 1
    out

pub fn entry() -> i64:
    add(2, 3)
