# Frozen corpus entry: struct, class, enum declarations.
struct Point:
    x: i64
    y: i64

enum Shade:
    Light
    Dark

class Counter:
    value: i64
    fn bump(self):
        self.value = self.value + 1
