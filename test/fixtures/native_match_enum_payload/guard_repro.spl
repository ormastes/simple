enum Result:
    Ok(i64)
    Err(text)

fn classify(r: Result) -> i64:
    match r:
        case Result.Ok(v) if v > 10:
            print("big-ok:" + v.to_text())
            return v
        case Result.Ok(v):
            print("small-ok:" + v.to_text())
            return -100
        case Result.Err(msg):
            print("err:" + msg)
            return -1

fn main() -> i32:
    let a = Result.Ok(42)
    let b = Result.Ok(3)
    let c = Result.Err("boom")
    let ra = classify(a)
    let rb = classify(b)
    let rc = classify(c)
    if ra == 42 and rb == -100 and rc == -1:
        print("PASS")
        return 0
    else:
        print("FAIL")
        return 1
