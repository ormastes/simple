# BOOT-11 fixture half 2: an unrelated struct whose FIRST field is also named
# `size`. A by-name / colliding-SymbolId field index resolves `size` to this
# layout's offset 0 instead of FileFingerprint's offset 24.
pub struct TypeLayout:
    size: i64
    align: i64
    name: text
