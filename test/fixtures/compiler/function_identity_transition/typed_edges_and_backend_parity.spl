# FunctionIdentity transition fixture: typed edges and backend parity.
# @fixture fi_bitcast_f64
# @fixture fi_optional_array_len
# @fixture fi_incompatible_signature_fail_closed
# @fixture fi_cranelift_llvm_parity
# @bitcast source: f64
# @bitcast destination: i64
# @optional-array parameter: [f64]?
# @optional-array operation: len
# @negative incompatible: f32 -> f64
# @backend cranelift
# @backend llvm
# @oracle exact-f64-bits

fn fi_bitcast_f64(value: f64) -> f64:
    value

fn fi_optional_array_len(values: [f64]?) -> i64:
    values.len()

# This declaration is intentionally described as a negative fixture. The
# transition must reject a selected f32 declaration when the call site asks
# for f64; a name-only fallback is forbidden.
# extern fn fi_incompatible(value: f32) -> f32
# call fi_incompatible(value: f64) -> f64  # expected: fail closed

fn main() -> i32:
    0
