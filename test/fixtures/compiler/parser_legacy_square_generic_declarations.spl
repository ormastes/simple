# Legacy square generic declarations remain accepted during migration.

class LegacyBox[T]:
    item: T

    fn wrap[U](value: U) -> [U]:
        [value]

fn legacy_identity[T](value: T) -> T:
    value

fn canonical_identity<T>(value: T) -> T:
    value
