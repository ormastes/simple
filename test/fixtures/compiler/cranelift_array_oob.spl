"""Runtime bounds-check probe for Cranelift arrays."""

fn runtime_index() -> i64:
    var index = 0
    index = index + 1
    index

fn main():
    val values = [7]
    print(values[runtime_index()])
