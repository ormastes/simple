struct OptionSource:
    value: i64

impl OptionSource:
    fn maybe(self) -> i64?:
        self.value

fn forward(source: OptionSource) -> i64?:
    val payload = source.maybe()?
    payload

fn main() -> i64:
    print(forward(OptionSource(value: 6)).unwrap_or(0))
    0
