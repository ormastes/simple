# PERF-COW-002 NEGATIVE — must NOT be flagged.
# Two different fields are not a self-alias, and the in-place form is correct.
class ByValueNegative:
    xs: [i64]
    ys: [i64]

    me add(v: i64):
        self.xs.push(v)

    me derive():
        self.xs = derive_from(self.ys)
