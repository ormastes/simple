# NEGATIVE fixture: `lane_definitely_absent_probe` is declared extern and is
# implemented NOWHERE — not in the Rust runtime, not in the C runtime, not in
# any Simple source. Building this MUST fail with a diagnostic naming the
# symbol. It must never link to a weak stub that fabricates a return value.
extern fn lane_definitely_absent_probe(x: i64) -> i64

fn main():
    print("got " + lane_definitely_absent_probe(3).to_string())
