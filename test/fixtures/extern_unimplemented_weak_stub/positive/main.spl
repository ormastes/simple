# POSITIVE control: `rt_string_len` is a real exported runtime symbol, so this
# fixture MUST build and print `got 4`. It exists so the negative fixture's
# spec cannot pass vacuously by "every extern build fails".
extern fn rt_string_len(s: text) -> i64

fn main():
    print("got " + rt_string_len("abcd").to_string())
