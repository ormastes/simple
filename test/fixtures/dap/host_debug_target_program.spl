# Fixture program driven by the host DebugTarget adapter spec.
# Line numbers are load-bearing -- the spec asserts against them.
# Line 1-3 are comments; line 4 is the first executable line.
val alpha = 10
val beta = 20
val gamma = 30
val delta = 40
