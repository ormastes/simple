# =====================================================================
# DELIBERATE FIXTURE — NOT A PRODUCT DEFECT.
# Lane: unstable_test_mode (lane C, test/fixtures/unstable_mode/**).
# This spec INFINITE-LOOPS ON PURPOSE so the runner's TIMEOUT outcome
# class has something honest to detect. TIMEOUT is UNVERIFIED, never a
# failure. Do not "fix" it.
#
# @skip  <- keeps it out of the default suite; run this directory with
#           `--only-skipped` to execute it.
# =====================================================================
use std.spec.*

describe "unstable mode timeout fixture":
    it "spins forever until the runner kills it":
        var n = 0
        while true:
            n = n + 1
        expect(n).to_equal(0)
