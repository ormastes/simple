# Compute the arithmetic mean of a list of measurement samples.
fn average_samples(samples: [f64]) -> f64:
    var total = 0.0
    for s in samples:
        total = total + s
    total / samples.len()
