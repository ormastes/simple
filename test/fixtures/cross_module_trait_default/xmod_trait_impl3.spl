# Fixture: impl in a DIFFERENT module overriding ONLY m3 (last method),
# mirroring default_trait_dispatch_spec.spl's DispatchTarget but split across
# a module boundary from the trait.

use test.fixtures.cross_module_trait_default.xmod_trait_def3.{XMod3Methods}

struct XMod3Target:
    n: i64

impl XMod3Methods for XMod3Target:
    fn m3() -> text:
        "override-m3"

export XMod3Target
