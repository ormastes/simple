# Fixture: trait with default method bodies, defined in its own module.
# Used by xmod_trait_impl.spl (impl in a DIFFERENT module) to test cross-module
# trait default dispatch in the interpreter (task #55, interpreter half).

export XModDftGreeter

trait XModDftGreeter:
    fn greet() -> text:
        "default-greet"
    fn farewell() -> text:
        "default-farewell"
