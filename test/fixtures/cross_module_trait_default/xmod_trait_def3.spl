# Fixture: trait with THREE default method bodies (mirrors
# default_trait_dispatch_spec.spl's ThreeMethods trait exactly), defined in
# its own module for the cross-module positional-dispatch regression check.

export XMod3Methods

trait XMod3Methods:
    fn m1() -> text:
        "default-m1"
    fn m2() -> text:
        "default-m2"
    fn m3() -> text:
        "default-m3"
