use test.fixtures.bootstrap_windows_global_relocation.provider.{RELOCATION_LABEL, advance_counter}

fn main() -> i64:
    if RELOCATION_LABEL != "module-global":
        return 10
    if advance_counter() != 42:
        return 11
    print "bootstrap-global-relocation: pass"
    0
