# NEGATIVE fixture: waiver with no `expires:` -> E-PIN-002.
project:
  name: fixture-core
  profile: critical
  dependencies:
    - package: fixture-base

assurance:
  waivers:
    - rule: MC-ANY-001
      owner: compiler-core
