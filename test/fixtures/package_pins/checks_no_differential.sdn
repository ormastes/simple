# NEGATIVE support fixture: every gate is mandatory but none is differential,
# so precondition 4 fails with E-PIN-006. This is the shape the real tree is in
# today: `check-seed-selfhost-parity.shs` (Phase 7) does not exist yet.
checks:
  - name: check-fixture-structure.shs
    status: mandatory
    differential: false
    covers:
      - fixture-core
