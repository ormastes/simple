# =====================================================================
# DELIBERATE FIXTURE — NOT A PRODUCT DEFECT.
# Lane: smf-compile degrade (test/fixtures/runner_degrade/**), driven by
# test/01_unit/app/test_runner_new/smf_compile_error_degrades_spec.spl.
# This spec is the CONTROL: it names no identifier the SMF lowering path
# cannot resolve, so it must report outcome=OK with every example executed
# whether or not the file degraded to the interpreter.
# =====================================================================
"""
## Purpose and audience
Control fixture for the runner's SMF-compile degrade lane: proves a healthy
spec is unchanged by the degrade branch. Audience: test-runner owners.
"""
use std.spec.step

use std.spec.*

describe "runner degrade control":
    # @manual scenario evidence
    it "passes a healthy assertion":
        # @req REQ-TESTRUNNER-SMF-COMPILE-DEGRADE
        step("assert a healthy comparison so the runner records a pass")
        expect(1 + 1).to_equal(2)  # oracle: deliberately trivial — this fixture exists to pass

    it "passes a second healthy assertion":
        # @req REQ-TESTRUNNER-SMF-COMPILE-DEGRADE
        step("assert a second healthy comparison")
        expect("a" + "b").to_equal("ab")  # oracle: deliberately trivial — control fixture
