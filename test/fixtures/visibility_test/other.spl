# Module that tries to access items from test_case

use test_case.TestCase     # Should work - auto-public
use test_case.Utils        # Should work - explicit pub
# use test_case.Helper     # Should warn - private

# Function using public types
fn use_test_case():
    val tc = TestCase(id: 1, name: "test")
    val util = Utils(x: 10)

    print "TestCase ID: {tc.get_id()}"
    print "Utils double: {util.double()}"

# Trying to use private type (commented out to avoid compile error)
# fn use_helper():
#     val h = Helper(data: 5)  # Would warn: accessing private type
#     print "Helper: {h.process()}"
