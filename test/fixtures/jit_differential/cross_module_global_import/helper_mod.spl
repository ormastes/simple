# Part of a NEW gap found by diffing run_file_jit's pipeline against
# native_project::compiler.rs's whole-program pipeline (2026-07-30). See
# doc/08_tracking/bug/jit_run_file_pipeline_gaps_2026-07-30.md ("cross-module
# global value import reads uninitialized/raw under JIT").
#
# HELPER_Y is a plain LITERAL initializer (not a function call, so this is
# deliberately NOT gap 8's "val from function call" shape) defined in this
# non-entry, imported file.
val HELPER_Y = 99

fn get_helper_y() -> i64:
    HELPER_Y
