# Regression-guard sentinel: both engines are expected to agree AND match
# ground truth on ordinary arithmetic. If this ever disagrees, that is a NEW
# defect, not one of the known/pinned bugs this corpus otherwise tracks.
# Expected (both engines, correct): sum=30
fn main():
    val a = 10
    val b = 20
    print("sum={a + b}")
