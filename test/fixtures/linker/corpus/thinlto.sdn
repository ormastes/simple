# Lane A5 corpus row: thinlto.
# Research doc §11 lists ThinLTO as a required corpus class. Grepped the whole
# backend/linker surface (`src/compiler/70.backend/linker/`, `src/app/cli/*.spl`)
# for "thinlto"/"thin-lto"/"lto" case-insensitively on 2026-09-18: zero hits.
# `compile`/`native-build --help` expose no LTO flag of any kind.
corpus:
  name: "thinlto"
  class: "not_applicable"
  producer: "none"
  inputs: "none"
  linker_script: "none"
  target_triple: "none"
  expected_outcome: "not_applicable"
  status: "not_applicable"
  status_reason: "this build has no LTO profile: no ThinLTO/LTO flag exists anywhere in the compiler CLI (compile/native-build --help) or in src/compiler/70.backend/linker/**; there is nothing to recipe until an LTO capability lands. Re-check this row when one does."
