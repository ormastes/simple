# Native cross-module call must consume the provider's real bool result.
use provider.{imported_bool_helper}

fn main() -> i32:
    if not imported_bool_helper():
        return 7
    print "IMPORTED BOOL RETURN: PASS"
    0
