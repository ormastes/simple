use std.spec.*

fn make_bytes() -> [u8]:
    var bytes: [u8] = []
    bytes.push(0u8)
    bytes

describe "indexed u8 failure propagation":
    it "fails deliberately after reading a helper-built byte":
        val bytes = make_bytes()
        expect(bytes[0]).to_equal(99u8)
