fn next_pair(value: u32) -> (data: u32, valid: bool):
    (data: value + 1, valid: true)

@clocked(clk, reset_n)
@hardware
fn clocked_labeled_tuple(value: u32) -> u32:
    val next = next_pair(value)
    if next.valid:
        return next.data
    0
