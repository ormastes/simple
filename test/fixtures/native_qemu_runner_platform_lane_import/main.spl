use os.qemu_runner.{riscv64_hosted_required_marker_fragments}

fn main() -> i64:
    val markers = riscv64_hosted_required_marker_fragments()
    val passed = markers.len() > 0
    print("qemu runner platform lane imports resolved: {passed}")
    if passed: 0 else: 1
