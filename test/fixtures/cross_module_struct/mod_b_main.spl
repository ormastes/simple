## mod_b_main.spl — imports Foo and make_foo from mod_a, calls cross-module constructor fn
## Fixture for interp_cross_module_struct_return_unit_2026-06-13 diagnosis
## Variant A: cross-module constructor fn call (expected to FAIL — reproduces the bug)

use test.fixtures.cross_module_struct.mod_a.{Foo, make_foo}

fn main():
    # Variant A: struct returned via imported constructor fn
    val f = make_foo(7)
    print(f.x)

    # Variant B: direct struct literal in module B (should work if Foo is registered)
    val g = Foo(x: 42)
    print(g.x)
