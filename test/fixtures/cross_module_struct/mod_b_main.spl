## mod_b_main.spl — imports Foo and make_foo from mod_a, tests cross-module struct return
## Fixture for interp_cross_module_struct_return_unit_2026-06-13 diagnosis
##
## Variant A: cross-module constructor fn call — PASSES (no "unit" param, no collision)
## Variant B: direct struct literal in module B — PASSES
## See mod_unit_param.spl for the ACTUAL failing variant (uses "unit" as parameter name).

use test.fixtures.cross_module_struct.mod_a.{Foo, make_foo}

fn main():
    # Variant A: struct returned via imported constructor fn (no "unit" keyword collision)
    val f = make_foo(7, "hello")
    print(f.x)
    print(f.label)

    # Variant B: direct struct literal in module B (should work if Foo is registered)
    val g = Foo(x: 42, label: "world")
    print(g.x)
    print(g.label)
