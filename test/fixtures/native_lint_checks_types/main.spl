use compiler.tools.lint.main.{Linter}

fn main():
    val linter = Linter.new()
    val results = linter.lint_source(
        "/tmp/native_lint_checks_types.spl",
        "fn main() -> i64:\n    0\n"
    )
    print("lint checks resolved: {results.len() >= 0}")
