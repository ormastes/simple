# Fixture f06 — a NAMED function passed as a value, no lambda literal.
#
# This is the load-bearing question for any workaround/lint advice: if a named
# fn reference does NOT lower to `ClosureCreate`, then "replace the lambda with
# a named fn" is a real fix. If it DOES, the only fix is removing the
# higher-order call entirely.

fn add_one(x: i64) -> i64:
    x + 1

fn apply(f: (i64) -> i64, x: i64) -> i64:
    f(x)

fn main():
    print("f06 marker result={apply(add_one, 41)}")
