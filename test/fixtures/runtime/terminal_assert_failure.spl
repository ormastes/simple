use std.tui.terminal.*

fn main() -> i64:
    val scope = terminal_signal_scope_begin()
    assert scope > 0, "terminal signal scope must be active"
    assert terminal_enter_raw_mode(), "terminal raw mode must be active"
    assert false, "intentional terminal restoration assertion"
    99
