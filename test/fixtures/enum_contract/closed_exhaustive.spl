@closed
enum Color:
    Red
    Green
    Blue

fn name_of(c: Color) -> text:
    match c:
        case Color.Red: "red"
        case Color.Green: "green"
        case Color.Blue: "blue"

fn main():
    print(name_of(Color.Red))
