use loader.loader.owner.{native_probe_value}
use loader.owner.{native_probe_value as direct_owner_value}

fn main() -> i64:
    if native_probe_value() == direct_owner_value() + 1:
        0
    else:
        1
