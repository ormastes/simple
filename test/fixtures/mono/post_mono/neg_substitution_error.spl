# @expect: error_from_substitution=1
# Substitution manufactured `HirTypeKind.Error` inside a specialization.
use compiler.hir.hir.*
use compiler.verify.post_mono_verify.*
use post_mono_fixture_support.*

fn main():
    val f = fx_fn(6, "box_it$i64", [], [fx_param(60, "v", fx_i64())],
                  fx_type(HirTypeKind.Error), fx_block([], fx_expr(HirExprKind.NilLit)))
    fx_emit(post_mono_verify(fx_module("neg_error", [f])))
