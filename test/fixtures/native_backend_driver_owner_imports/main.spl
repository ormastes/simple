# Entry-closure fixture for backend/driver canonical owner imports.
# The imports are intentionally referenced through the real implementation
# modules so a native entry-closure build checks every module's named imports.
use compiler.backend.backend.compiler.{CompilerBackendImpl}
use compiler.backend.backend.vhdl.vhdl_design_catalog.*
use compiler.driver.driver_source_pipeline_loading.*
use compiler.driver.driver_aot_smf_output.*

fn main() -> i64:
    # Keep the entry executable while the imported modules are checked and
    # lowered through the same closure as a native compiler build.
    0
