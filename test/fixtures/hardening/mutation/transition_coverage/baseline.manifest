# check-compiler-transition-coverage.shs validates the REAL transition tables
# against the real producer/consumer universe extracted from the compiler
# sources, so a toy table cannot exercise it. Copy the real artifact read-only.
spec/compiler_schema/transitions
