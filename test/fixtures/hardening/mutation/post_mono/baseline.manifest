# check-post-mono-invariants.shs runs the real post-mono verifier over a corpus
# of fixtures that import real compiler modules, so a toy corpus cannot drive
# it. Copy the real corpus read-only; the mutation then makes a fixture that
# declares `@expect: clean` actually leave an unresolved TypeParam behind.
test/fixtures/mono/post_mono
