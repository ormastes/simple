# Fixture mimicking the nullable env_get contract (e.g. src/lib/gc_async_mut/env/variables.spl):
# (text) -> text? returning nil. Must be loaded AFTER io_like so that it wins
# the flat last-write-wins bare-name function table — the hijack hazard.
fn env_get(key: text) -> text?:
    nil

export env_get

fn probe_own_env_get() -> text?:
    env_get("HOME")

export probe_own_env_get
