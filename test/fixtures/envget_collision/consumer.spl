# Fixture consumer: explicitly imports env_get from io_like, exactly like
# src/lib/common/encoding/font_registry.spl does `use std.io_runtime.{env_get}`.
use test.fixtures.envget_collision.io_like.{env_get}

fn probe_via_explicit_import() -> text:
    env_get("HOME")

export probe_via_explicit_import
