# Fixture consumer through a FACADE: explicitly imports env_get from
# facade_like, which re-exports io_like's definition.
use test.fixtures.envget_collision.facade_like.{env_get}

fn probe_via_facade_import() -> text:
    env_get("HOME")

export probe_via_facade_import
