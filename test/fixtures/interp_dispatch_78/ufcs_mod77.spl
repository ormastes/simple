## ufcs_mod77.spl — module-level free fn defined in a SEPARATE module from the
## spec that calls it via UFCS. Fixture for task #77: UFCS free-function calls
## where the fn is imported (not module-local to the it-block's file).

fn triple_it_77(x: i64) -> i64:
    x * 3

export triple_it_77
