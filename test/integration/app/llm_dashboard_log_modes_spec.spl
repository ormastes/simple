"""
## Purpose and audience
Purpose: shows shared log options in help
Audience: compiler and tooling engineers who maintain this spec
"""

# @req: REQ-APP-LlmDashLogMode-001

use std.spec.{describe, it, expect}

extern fn rt_process_run(cmd: text, args: [text]) -> (text, text, i64)

fn _run_llm_dashboard(args: [text]) -> (text, text, i64):
    val joined = args.join(" ")
    rt_process_run("/bin/sh", [
        "-c",
        "REPO=$(pwd); SIMPLE_LIB=\"$REPO/src\" \"$REPO/bin/simple\" run \"$REPO/src/app/llm_dashboard/main.spl\" " + joined
    ])

use std.spec.step
describe "llm dashboard log mode CLI options":
    it "shows shared log options in help":
        step("Verify: shows shared log options in help")
        # @req: REQ-APP-LlmDashLogMode-001
        val (out, err, code) = _run_llm_dashboard(["--help"])
        expect(code).to_equal(0)  # oracle: value fixed by the spec contract
        expect(out).to_contain("LLM Agent Dashboard")
        expect(out).to_contain("--log-mode")
        expect(out).to_contain("--progress")

    it "supports log-mode json for usage output":
        step("Verify: supports log-mode json for usage output")
        # @req: REQ-APP-LlmDashLogMode-001
        val (out, err, code) = _run_llm_dashboard(["--log-mode=json"])
        expect(code).to_equal(0)  # oracle: value fixed by the spec contract
        expect(out).to_contain("\"command\":\"llm-dashboard\"")
        expect(out).to_contain("\"status\":\"usage\"")

    it "supports dot progress for help output":
        step("Verify: supports dot progress for help output")
        # @req: REQ-APP-LlmDashLogMode-001
        val (out, err, code) = _run_llm_dashboard(["--progress=dot", "--help"])
        expect(code).to_equal(0)  # oracle: value fixed by the spec contract
        expect(out).to_start_with(".")
        expect(out).to_contain("LLM Agent Dashboard")

    it "rejects invalid log mode":
        step("Verify: rejects invalid log mode")
        # @req: REQ-APP-LlmDashLogMode-001
        val (out, err, code) = _run_llm_dashboard(["--log-mode=noisy"])
        expect(code).to_equal(1)  # oracle: value fixed by the spec contract

    it "preserves gui mode argument parsing with shared options stripped":
        step("Verify: preserves gui mode argument parsing with shared options stripped")
        # @req: REQ-APP-LlmDashLogMode-001
        val (out, err, code) = _run_llm_dashboard(["--log-mode=json", "--help"])
        expect(code).to_equal(0)  # oracle: value fixed by the spec contract
        expect(out).to_contain("LLM Agent Dashboard")
        expect(out).to_contain("--gui, --web")
