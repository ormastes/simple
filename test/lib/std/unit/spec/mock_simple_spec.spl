# Minimal mock import test
import spec.{describe, it, expect}
import spec.{Mock}

describe "Mock Simple Import":
    it "imports Mock":
        val m = Mock.new("test")
        expect(m.name == "test")
