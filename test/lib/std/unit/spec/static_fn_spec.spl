# Test static fn methods
import spec.{describe, it, expect}
import spec.{CallEventRecorder}

describe "Static Method Import":
    it "can call static fn new on CallEventRecorder":
        val recorder = CallEventRecorder.new()
        expect(recorder.events.len() == 0)
