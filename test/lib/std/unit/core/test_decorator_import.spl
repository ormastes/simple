use decorators.{cached}

describe "Test decorator import":
    it "should load decorators":
        fn square(x):
            return x * x
        val c = cached(square)
        expect(true).to_equal(true)
