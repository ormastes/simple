# Simple trait for Lean codegen golden test

trait Eq:
    fn eq(other: Self) -> bool

trait Comparable:
    fn compare(other: Self) -> i32

impl Eq for i32:
    fn eq(other: Self) -> bool:
        self == other
