# Test calling app.io functions
use app.io

describe "Test app.io calls":
    it "calls file_exists":
        val result = file_exists("/tmp/test.txt")
        print "Result: {result}"
        check(true)
