# Test if app.io imports work from test/intensive/
use app.io.{file_exists, file_delete}

describe "Test app.io imports":
    it "can call file_exists":
        val exists = file_exists("/tmp/test.txt")
        print "file_exists worked! Result: {exists}"
        check(true)
