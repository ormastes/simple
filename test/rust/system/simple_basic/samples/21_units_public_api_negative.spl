#[allow(primitive_api)]  # Intentional: negative test for linter
# 21_units_public_api_negative.spl - Expected warnings/errors for unit rules

# These are intended "negative tests" for the unit-type rules.
# Should produce warnings or errors during compilation.

# WARNING (or error if deny enabled): bare primitive return in public API
pub fn bad_get_user_id() -> i64:
    return 42

# WARNING (or error if deny enabled): bare string in public API
pub fn bad_get_path() -> str:
    return "/etc/config"

# These should trigger lints about primitive types in public APIs
bad_id = bad_get_user_id()
bad_path = bad_get_path()

print "bad_id={bad_id} bad_path={bad_path}"

main = bad_id
