use std.spec.{describe, it, expect}
use test.fixtures.chunk08_stale_probe.foo_g.{bump, get_counter, g_counter}

describe "stale global read probe":
    it "bumps":
        bump()
        expect(g_counter).to_equal(1)
        expect(get_counter()).to_equal(1)
