#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd -P)
checker="$repo_root/scripts/check/check-bs5-rust-dependency-trim.shs"

output=$(sh "$checker")
test "$output" = 'BS5_RUST_DEPENDENCY_TRIM_STATIC_PASS'

printf 'BS5_RUST_DEPENDENCY_TRIM_TEST_PASS\n'
