#!/bin/sh
# Unit-test entrypoint for the CI-wired bootstrap fingerprint contract guard.
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd -P)
exec sh "$root/scripts/check/check-bootstrap-fingerprint-diff-contract.shs"
