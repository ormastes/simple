use std.spec.{describe, it, expect}
use test.01_unit._tmp_chunk08_probe.foo_g.{bump, get_counter, _g_counter}

describe "stale global read probe":
    it "bumps":
        bump()
        expect(_g_counter).to_equal(1)
        expect(get_counter()).to_equal(1)
