# tag: only-compiled

import std.spec
use std.hardware.rv32i_rtl.sv32_address.sv32_full_vpn

describe "Sv32 address decomposition":
    it "extracts the full VPN through the production hardware owner":
        val vaddr: u32 = 0x80000000
        expect(sv32_full_vpn(vaddr)).to_equal(0x80000)
