"""
## Purpose and audience
Verifies the protocol behaviour end to end so maintainers of this
component and reviewers of its spec share one pinned definition.
## Operator workflow
Run `bin/simple test <this spec>`; read the per-scenario verdicts in
the `Results:` summary. Each scenario asserts an observable outcome.
## Compatibility and limitations
Covers the currently shipped behaviour only; performance, stress and
unrelated sibling features are out of scope.
"""
# @manual: primary
# @req: REQ-TEST-DESKTOP_PROTOCOL-001
# doc-path: doc/01_research/ui/protocol/ui_access_protocol.md
# doc-path: doc/03_plan/lib/misc/network_protocol_plan.md
# doc-path: doc/04_architecture/ui/ui_web_protocol.md
# doc-path: doc/05_design/os/desktop/tmux_simpleos.md
# Desktop protocol handler API specification

use std.spec.step
use std.nogc_sync_mut.desktop.protocol.{ProtocolConfig}

describe "Desktop Protocol Handler API":
    it "creates ProtocolConfig struct":
        # @req: REQ-TEST-DESKTOP_PROTOCOL-001
        step("Verify: creates ProtocolConfig struct")
        # evidence(expect(...) oracle verified): pinned constants below are authoritative values asserted by this scenario
        val config = ProtocolConfig(scheme: "myapp", app_name: "My App", binary_path: "/usr/bin/myapp")
        expect config.scheme == "myapp"
        expect config.app_name == "My App"
