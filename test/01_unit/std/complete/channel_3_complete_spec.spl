"""
## Purpose and audience
Purpose: Verify Channel queue construction through executable runtime oracles against the real implementation.
Audience: library and compiler engineers who run the spec suite.

## Operator workflow
Run `bin/simple test /tmp/mod13/test/unit/std/complete/channel_3_complete_spec.spl` and require the final Results line to report 0 failed.

## Compatibility and limitations
Executed on the interpreter test lane; asserts observable values only and makes no performance claims.
"""

# @req: REQ-LIB-CHANNEL-001 — stable requirement identity for this spec.
# doc-path: doc/01_research/local/channel_behavior.md
# doc-path: doc/03_plan/sys_test/channel_behavior.md
# doc-path: doc/04_architecture/lib/channel.md
# doc-path: doc/05_design/lib/channel_oracles.md
# @manual: primary

use std.spec
use std.concurrent.{mpsc_queue_new}
use std.spec.step

describe "Channel queue construction":
    it "a fresh channel queue is non-nil and starts empty of errors":
        step("Verify: a fresh channel queue is non-nil and starts empty of errors")
        # @req: REQ-LIB-CHANNEL-001
        expect(mpsc_queue_new().?).to_equal(true)  # oracle: constructing a channel succeeds

