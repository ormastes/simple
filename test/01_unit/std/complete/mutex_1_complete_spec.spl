"""
## Purpose and audience
Purpose: Verify Once-latch construction through executable runtime oracles against the real implementation.
Audience: library and compiler engineers who run the spec suite.

## Operator workflow
Run `bin/simple test /tmp/mod13/test/unit/std/complete/mutex_1_complete_spec.spl` and require the final Results line to report 0 failed.

## Compatibility and limitations
Executed on the interpreter test lane; asserts observable values only and makes no performance claims.
"""

# @req: REQ-LIB-MUTEX-001 — stable requirement identity for this spec.
# doc-path: doc/01_research/local/mutex_behavior.md
# doc-path: doc/03_plan/sys_test/mutex_behavior.md
# doc-path: doc/04_architecture/lib/mutex.md
# doc-path: doc/05_design/lib/mutex_oracles.md
# @manual: primary

use std.spec
use std.concurrent.{once_new}
use std.spec.step

describe "Once-latch construction":
    it "a once-latch constructs before first use":
        step("Verify: a once-latch constructs before first use")
        # @req: REQ-LIB-MUTEX-001
        expect(once_new().?).to_equal(true)  # oracle: the one-time latch constructor yields a value

