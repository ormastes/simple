"""
## Purpose and audience
Purpose: Verify Atomic flag construction through executable runtime oracles against the real implementation.
Audience: library and compiler engineers who run the spec suite.

## Operator workflow
Run `bin/simple test /tmp/mod13/test/unit/std/complete/atomic_1_complete_spec.spl` and require the final Results line to report 0 failed.

## Compatibility and limitations
Executed on the interpreter test lane; asserts observable values only and makes no performance claims.
"""

# @req: REQ-LIB-ATOMIC-001 — stable requirement identity for this spec.
# doc-path: doc/01_research/local/atomic_behavior.md
# doc-path: doc/03_plan/sys_test/atomic_behavior.md
# doc-path: doc/04_architecture/lib/atomic.md
# doc-path: doc/05_design/lib/atomic_oracles.md
# @manual: primary

use std.spec
use std.concurrent.{atomic_flag_new}
use std.spec.step

describe "Atomic flag construction":
    it "an atomic flag constructs in the clear state":
        step("Verify: an atomic flag constructs in the clear state")
        # @req: REQ-LIB-ATOMIC-001
        expect(atomic_flag_new().?).to_equal(true)  # oracle: the constructor yields a usable flag value

