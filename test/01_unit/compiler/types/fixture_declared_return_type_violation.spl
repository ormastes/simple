# Fixture for declared_return_type_not_enforced_2026-08-09.
#
# `ret_text` declares `-> bool` and returns a text literal. This is a type
# error. Every engine currently accepts it and exits 0, each producing a
# DIFFERENT wrong value:
#
#   jit (default)                     -> <special:177>
#   SIMPLE_EXECUTION_MODE=interpreter -> not-a-bool
#
# Do not "fix" this file — the violation is the fixture.

fn ret_text() -> bool:
    return "not-a-bool"

fn main():
    val b = ret_text()
    print("ret_text => " + b.to_text())
