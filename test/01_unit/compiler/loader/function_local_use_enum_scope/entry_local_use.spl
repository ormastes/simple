# Fixture entry: the import lives INSIDE the function that needs it (the
# sanctioned lazy-import pattern used by src/app/mcp/bootstrap/main_lazy_v2.spl
# and 157 other sites). The imported closure's enum must be in scope.

fn report() -> text:
    use .enum_owner.{default_level, describe_level}
    describe_level(default_level())

fn main() -> i64:
    print "level={report()}"
    0
