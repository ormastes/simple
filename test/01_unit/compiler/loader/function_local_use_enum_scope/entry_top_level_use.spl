# Control fixture: the same import at module scope, which was never broken.
# Any difference between this and entry_local_use.spl is the defect.

use .enum_owner.{default_level, describe_level}

fn report() -> text:
    describe_level(default_level())

fn main() -> i64:
    print "level={report()}"
    0
