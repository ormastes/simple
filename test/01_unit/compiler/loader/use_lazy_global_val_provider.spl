# Fixture for use_lazy_global_first_use_spec.spl.
# Provides a module-level val global and no functions, so the spec can verify
# that a `use lazy` import materializes the global on its first read.
val answer = 73
export answer
