# The imported name is never used: a deferred loader may skip the module
# entirely, an eager one must still produce exactly this output.
use .helper.{helper_message}

fn main() -> i64:
    print "message=unused"
    0
