# Fixture module for the `use lazy` equivalence pins. Deliberately free of
# top-level side effects: laziness is an optimization, so a module whose mere
# loading printed something would make "eager and lazy agree" false by
# construction rather than by the loader behaving correctly.

fn helper_message() -> text:
    "helper"

export helper_message
