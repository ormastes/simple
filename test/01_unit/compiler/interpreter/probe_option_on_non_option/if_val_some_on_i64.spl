# if-val Some() against a NON-Option scrutinee. Must be REJECTED at compile time.
fn main():
    val n = 6
    if val Some(k) = n:
        print("bound=" + k.to_text())
    else:
        print("nomatch")
