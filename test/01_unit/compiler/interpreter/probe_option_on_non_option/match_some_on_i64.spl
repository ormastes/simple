# Some(_) pattern against a NON-Option scrutinee. Must be REJECTED at compile time.
fn main():
    val n = 6
    match n:
        case Some(i): print("some=" + i.to_text())
        case _: print("wild")
