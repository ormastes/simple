describe "interpreter while-body scope":
    it "does not overwrite an enclosing binding":
        var shadow = 99
        var i = 0
        while i < 2:
            val shadow = i + 5
            i = i + 1
        expect(i).to_equal(2)
        expect(shadow).to_equal(99)
