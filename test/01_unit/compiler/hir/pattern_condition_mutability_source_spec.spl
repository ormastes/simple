"""
## Purpose and audience
Purpose: Verify HIR pattern condition collection mutability.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-COMPILER-HIR-001
doc/01_research/local/REQ-COMPILER-HIR-001.md
doc/03_plan/sys_test/REQ-COMPILER-HIR-001.md
doc/04_architecture/REQ-COMPILER-HIR-001.md
doc/05_design/REQ-COMPILER-HIR-001.md
"""
# @req: REQ-COMPILER-HIR-001

# @tag: unit, compiler, hir, source-contract

use std.spec.{describe, it, expect}
use std.io_runtime.{file_read}

# expressions.spl is a re-export shim since the _Expressions split; the
# collectors this contract guards live in match_desugaring.spl.
val SOURCE = "src/compiler/20.hir/hir_lowering/_Expressions/match_desugaring.spl"

use std.spec.step

describe "HIR pattern condition collection mutability":
    it "keeps all five recursive pattern collectors mutable":
        # @req: REQ-COMPILER-HIR-001
        step("Verify: keeps all five recursive pattern collectors mutable")
        val source = file_read(SOURCE)
        expect(source.split("var conditions: [HirExpr] = []").len() - 1).to_equal(5)  # oracle: 5 — named expected value from the requirement
        expect(source.contains("val conditions: [HirExpr] = []")).to_equal(false)
