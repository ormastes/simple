"""Fixture for the header-scoped import predicate.

The next line looks exactly like an import and is NOT one -- it is string
content inside this docstring. A whole-file grep for `DocstringOnlyType`
matches it; a header-scoped predicate must not.

use compiler.fixture.not_real.{DocstringOnlyType}
"""

use compiler.fixture.real_provider.{RealHeaderType}

fn fixture_marker() -> text: "fixture"
