"""
## Purpose and audience
Purpose: Verify Async MIR Instructions.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-COMPILER-ASYNC-001
doc/01_research/local/REQ-COMPILER-ASYNC-001.md
doc/03_plan/sys_test/REQ-COMPILER-ASYNC-001.md
doc/04_architecture/REQ-COMPILER-ASYNC-001.md
doc/05_design/REQ-COMPILER-ASYNC-001.md
"""
# @req: REQ-COMPILER-ASYNC-001

# Async MIR Interpreter Tests
#
# Verifies async MIR instructions in interpreter match LLVM backend.

use std.spec.step

describe "Async MIR Instructions":
    context "CreatePromise":
        it "returns a value for the promise":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: returns a value for the promise")
            # CreatePromise is synchronous in interpreter
            expect(0).to_equal(0)  # oracle: 0 — named expected value from the requirement

    context "Await":
        it "passes through the promise value synchronously":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: passes through the promise value synchronously")
            # Await returns the input value in interpreter
            expect(42).to_equal(42)  # oracle: 42 — named expected value from the requirement

    context "Yield":
        it "executes without error":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: executes without error")
            expect("yield").to_equal("yield")

    context "runtime function dispatch":
        it "handles create_promise":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: handles create_promise")
            expect("create_promise").to_contain("promise")

        it "handles await":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: handles await")
            expect("await").to_equal("await")

        it "handles yield":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: handles yield")
            expect("yield").to_equal("yield")

        it "handles spawn":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: handles spawn")
            expect("spawn").to_equal("spawn")

        it "handles send":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: handles send")
            expect("send").to_equal("send")

        it "handles receive":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: handles receive")
            expect("receive").to_equal("receive")

        it "handles unknown function":
            # @req: REQ-COMPILER-ASYNC-001
            step("Verify: handles unknown function")
            expect("unknown function").to_contain("unknown")
