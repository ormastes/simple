use compiler.frontend.flat_ast_bridge.{parse_and_build_module}

describe "Flat AST binding spans":
    it "keeps a bare-expression-only module safe without an arena span":
        val module = parse_and_build_module("print(1)\n", "bare_expression.spl")
        expect(module.functions.has("main")).to_equal(true)
