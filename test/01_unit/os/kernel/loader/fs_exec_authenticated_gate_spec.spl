use std.spec.{describe, it, expect}
use os.kernel.loader.fs_exec_spawn.{fs_exec_spawn}

describe "filesystem executable authenticated gate":
    it "rejects a path-only kernel launch without consuming loader authority":
        val result = fs_exec_spawn("/sys/apps/unsigned.smf", [], [])
        expect(result).to_equal(-13)
