# The catalogue net payload must consume an assigned grant; it must not grow a
# userspace PCI-enumeration or caller-selected device authority path.

use os.services.netstack.net_service_main.{net_service_main}

describe "root catalogue network payload contract":
    it "exposes a freestanding service entry":
        expect(net_service_main).not.to_be_nil()
