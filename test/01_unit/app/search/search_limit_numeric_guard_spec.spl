"""
## Purpose and audience
Purpose: Verify search limit numeric guard.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-APP-SEARCH-001
doc/01_research/local/REQ-APP-SEARCH-001.md
doc/03_plan/sys_test/REQ-APP-SEARCH-001.md
doc/04_architecture/REQ-APP-SEARCH-001.md
doc/05_design/REQ-APP-SEARCH-001.md
"""
# @req: REQ-APP-SEARCH-001

extern fn rt_file_read_text(path: text) -> text?

use std.spec.step

describe "search limit numeric guard":
    it "defaults malformed limit values":
        # @req: REQ-APP-SEARCH-001
        step("Verify: defaults malformed limit values")
        val source = rt_file_read_text("src/app/search/main.spl") ?? ""

        expect(source).to_contain("fn parse_limit_or_default(value: text) -> i64")
        expect(source).to_contain("val parsed = trimmed.to_int() ?? 20")
        expect(source.contains("val parsed = trimmed.to_int()\n")).to_equal(false)
