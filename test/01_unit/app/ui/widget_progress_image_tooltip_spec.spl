"""
## Purpose and audience
Purpose: Verify Progress widget builder.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-APP-UI-001
doc/01_research/local/REQ-APP-UI-001.md
doc/03_plan/sys_test/REQ-APP-UI-001.md
doc/04_architecture/REQ-APP-UI-001.md
doc/05_design/REQ-APP-UI-001.md
"""
# @req: REQ-APP-UI-001

# Per-Widget SPipe Tests: Progress, Image, Tooltip
#
# Comprehensive tests for three UI widgets covering:
# - Builder creation (kind, props, children)
# - HTML rendering (classes, tags, attributes, content)

use common.ui.widget.{WidgetNode, UITree, UIState}
use common.ui.builder.{progress, image, tooltip, column}
use common.ui.state.{init_state}
use app.ui.render.html_widgets.{render_html_widget}


# ============================================================================
# Progress Widget - Builder
# ============================================================================

use std.spec.step

describe "Progress widget builder":
    context "creation":
        it "creates widget with kind progress":
            # @req: REQ-APP-UI-001
            step("Verify: creates widget with kind progress")
            val p = progress("prog_create_1", 75)
            expect p.kind to_equal "progress"

        it "assigns the correct id":
            # @req: REQ-APP-UI-001
            step("Verify: assigns the correct id")
            val p = progress("prog_create_2", 50)
            expect p.id to_equal "prog_create_2"

    context "value property":
        it "stores value as string prop":
            # @req: REQ-APP-UI-001
            step("Verify: stores value as string prop")
            val p = progress("prog_val_1", 75)
            expect p.get_prop("value") to_equal "75"

        it "stores zero value":
            # @req: REQ-APP-UI-001
            step("Verify: stores zero value")
            val p = progress("prog_val_2", 0)
            expect p.get_prop("value") to_equal "0"

        it "stores full value":
            # @req: REQ-APP-UI-001
            step("Verify: stores full value")
            val p = progress("prog_val_3", 100)
            expect p.get_prop("value") to_equal "100"

        it "stores small value":
            # @req: REQ-APP-UI-001
            step("Verify: stores small value")
            val p = progress("prog_val_4", 1)
            expect p.get_prop("value") to_equal "1"

    context "children":
        it "has no children":
            # @req: REQ-APP-UI-001
            step("Verify: has no children")
            val p = progress("prog_child_1", 60)
            expect p.child_count() to_equal 0

    context "visibility":
        it "is visible by default":
            # @req: REQ-APP-UI-001
            step("Verify: is visible by default")
            val p = progress("prog_vis_1", 33)
            expect p.visible to_equal true

    context "layout":
        it "has default vbox layout":
            # @req: REQ-APP-UI-001
            step("Verify: has default vbox layout")
            val p = progress("prog_lay_1", 50)
            expect p.layout to_equal "vbox"


# ============================================================================
# Progress Widget - HTML Rendering
# ============================================================================

describe "Progress widget HTML rendering":
    context "structure":
        it "contains widget-progress class":
            # @req: REQ-APP-UI-001
            step("Verify: contains widget-progress class")
            val node = progress("prog_html_1", 75)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "widget-progress"

        it "starts with div tag":
            # @req: REQ-APP-UI-001
            step("Verify: starts with div tag")
            val node = progress("prog_html_2", 50)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_start_with "<div"

        it "contains progress-bar inner div":
            # @req: REQ-APP-UI-001
            step("Verify: contains progress-bar inner div")
            val node = progress("prog_html_3", 40)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "progress-bar"

    context "percentage display":
        it "contains percentage text for 75":
            # @req: REQ-APP-UI-001
            step("Verify: contains percentage text for 75")
            val node = progress("prog_pct_1", 75)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "75%"

        it "contains percentage text for 0":
            # @req: REQ-APP-UI-001
            step("Verify: contains percentage text for 0")
            val node = progress("prog_pct_2", 0)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "0%"

        it "contains percentage text for 100":
            # @req: REQ-APP-UI-001
            step("Verify: contains percentage text for 100")
            val node = progress("prog_pct_3", 100)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "100%"

    context "style width":
        it "contains width style for 75":
            # @req: REQ-APP-UI-001
            step("Verify: contains width style for 75")
            val node = progress("prog_sty_1", 75)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "width: 75%"

        it "contains width style for 0":
            # @req: REQ-APP-UI-001
            step("Verify: contains width style for 0")
            val node = progress("prog_sty_2", 0)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "width: 0%"

        it "contains width style for 100":
            # @req: REQ-APP-UI-001
            step("Verify: contains width style for 100")
            val node = progress("prog_sty_3", 100)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "width: 100%"

    context "id attribute":
        it "contains the widget id in the output":
            # @req: REQ-APP-UI-001
            step("Verify: contains the widget id in the output")
            val node = progress("prog_id_1", 55)
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "prog_id_1"


# ============================================================================
# Image Widget - Builder
# ============================================================================

describe "Image widget builder":
    context "creation":
        it "creates widget with kind image":
            # @req: REQ-APP-UI-001
            step("Verify: creates widget with kind image")
            val img = image("img_create_1", "photo.png", "A photo")
            expect img.kind to_equal "image"

        it "assigns the correct id":
            # @req: REQ-APP-UI-001
            step("Verify: assigns the correct id")
            val img = image("img_create_2", "pic.jpg", "Picture")
            expect img.id to_equal "img_create_2"

    context "src property":
        it "stores src prop":
            # @req: REQ-APP-UI-001
            step("Verify: stores src prop")
            val img = image("img_src_1", "photo.png", "A photo")
            expect img.get_prop("src") to_equal "photo.png"

        it "stores url src":
            # @req: REQ-APP-UI-001
            step("Verify: stores url src")
            val img = image("img_src_2", "https://example.com/img.png", "Remote")
            expect img.get_prop("src") to_equal "https://example.com/img.png"

        it "stores empty src":
            # @req: REQ-APP-UI-001
            step("Verify: stores empty src")
            val img = image("img_src_3", "", "No image")
            expect img.get_prop("src") to_equal ""

    context "alt property":
        it "stores alt prop":
            # @req: REQ-APP-UI-001
            step("Verify: stores alt prop")
            val img = image("img_alt_1", "photo.png", "A photo")
            expect img.get_prop("alt") to_equal "A photo"

        it "stores empty alt":
            # @req: REQ-APP-UI-001
            step("Verify: stores empty alt")
            val img = image("img_alt_2", "pic.jpg", "")
            expect img.get_prop("alt") to_equal ""

        it "stores alt with spaces":
            # @req: REQ-APP-UI-001
            step("Verify: stores alt with spaces")
            val img = image("img_alt_3", "logo.svg", "Company Logo Image")
            expect img.get_prop("alt") to_equal "Company Logo Image"

    context "children":
        it "has no children":
            # @req: REQ-APP-UI-001
            step("Verify: has no children")
            val img = image("img_child_1", "a.png", "alt")
            expect img.child_count() to_equal 0

    context "visibility":
        it "is visible by default":
            # @req: REQ-APP-UI-001
            step("Verify: is visible by default")
            val img = image("img_vis_1", "b.png", "alt")
            expect img.visible to_equal true

    context "has_prop":
        it "reports src exists":
            # @req: REQ-APP-UI-001
            step("Verify: reports src exists")
            val img = image("img_has_1", "c.png", "alt")
            expect img.has_prop("src") to_equal true

        it "reports alt exists":
            # @req: REQ-APP-UI-001
            step("Verify: reports alt exists")
            val img = image("img_has_2", "d.png", "alt text")
            expect img.has_prop("alt") to_equal true

        it "reports missing prop as false":
            # @req: REQ-APP-UI-001
            step("Verify: reports missing prop as false")
            val img = image("img_has_3", "e.png", "alt")
            expect img.has_prop("nonexistent") to_equal false


# ============================================================================
# Image Widget - HTML Rendering
# ============================================================================

describe "Image widget HTML rendering":
    context "structure":
        it "contains widget-image class":
            # @req: REQ-APP-UI-001
            step("Verify: contains widget-image class")
            val node = image("img_html_1", "photo.png", "A photo")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "widget-image"

        it "starts with img tag":
            # @req: REQ-APP-UI-001
            step("Verify: starts with img tag")
            val node = image("img_html_2", "pic.jpg", "Picture")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_start_with "<img"

        it "is a self-closing tag":
            # @req: REQ-APP-UI-001
            step("Verify: is a self-closing tag")
            val node = image("img_html_3", "icon.svg", "Icon")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_end_with "/>"

    context "src attribute":
        it "contains src attribute with value":
            # @req: REQ-APP-UI-001
            step("Verify: contains src attribute with value")
            val node = image("img_src_html_1", "photo.png", "Photo")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "src=\"photo.png\""

        it "contains src for url":
            # @req: REQ-APP-UI-001
            step("Verify: contains src for url")
            val node = image("img_src_html_2", "https://cdn.example.com/img.jpg", "Remote")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "src=\"https://cdn.example.com/img.jpg\""

        it "contains empty src attribute":
            # @req: REQ-APP-UI-001
            step("Verify: contains empty src attribute")
            val node = image("img_src_html_3", "", "Empty src")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "src=\"\""

    context "alt attribute":
        it "contains alt attribute with value":
            # @req: REQ-APP-UI-001
            step("Verify: contains alt attribute with value")
            val node = image("img_alt_html_1", "photo.png", "A photo")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "alt=\"A photo\""

        it "contains alt for multi-word text":
            # @req: REQ-APP-UI-001
            step("Verify: contains alt for multi-word text")
            val node = image("img_alt_html_2", "banner.png", "Welcome Banner Image")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "alt=\"Welcome Banner Image\""

        it "contains empty alt attribute":
            # @req: REQ-APP-UI-001
            step("Verify: contains empty alt attribute")
            val node = image("img_alt_html_3", "decorative.png", "")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "alt=\"\""

    context "id attribute":
        it "contains the widget id":
            # @req: REQ-APP-UI-001
            step("Verify: contains the widget id")
            val node = image("img_id_html_1", "x.png", "X")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "img_id_html_1"


# ============================================================================
# Tooltip Widget - Builder
# ============================================================================

describe "Tooltip widget builder":
    context "creation":
        it "creates widget with kind tooltip":
            # @req: REQ-APP-UI-001
            step("Verify: creates widget with kind tooltip")
            val tt = tooltip("tt_create_1", "Help text", "target_btn")
            expect tt.kind to_equal "tooltip"

        it "assigns the correct id":
            # @req: REQ-APP-UI-001
            step("Verify: assigns the correct id")
            val tt = tooltip("tt_create_2", "Info", "some_target")
            expect tt.id to_equal "tt_create_2"

    context "content property":
        it "stores content prop":
            # @req: REQ-APP-UI-001
            step("Verify: stores content prop")
            val tt = tooltip("tt_content_1", "Help text", "target_btn")
            expect tt.get_prop("content") to_equal "Help text"

        it "stores empty content":
            # @req: REQ-APP-UI-001
            step("Verify: stores empty content")
            val tt = tooltip("tt_content_2", "", "btn")
            expect tt.get_prop("content") to_equal ""

        it "stores content with special characters":
            # @req: REQ-APP-UI-001
            step("Verify: stores content with special characters")
            val tt = tooltip("tt_content_3", "Press Ctrl+S to save", "save_btn")
            expect tt.get_prop("content") to_equal "Press Ctrl+S to save"

    context "target property":
        it "stores target prop":
            # @req: REQ-APP-UI-001
            step("Verify: stores target prop")
            val tt = tooltip("tt_target_1", "Help text", "target_btn")
            expect tt.get_prop("target") to_equal "target_btn"

        it "stores different target id":
            # @req: REQ-APP-UI-001
            step("Verify: stores different target id")
            val tt = tooltip("tt_target_2", "Click here", "submit_button")
            expect tt.get_prop("target") to_equal "submit_button"

    context "children":
        it "has no children":
            # @req: REQ-APP-UI-001
            step("Verify: has no children")
            val tt = tooltip("tt_child_1", "tip", "tgt")
            expect tt.child_count() to_equal 0

    context "visibility":
        it "is visible by default":
            # @req: REQ-APP-UI-001
            step("Verify: is visible by default")
            val tt = tooltip("tt_vis_1", "tip", "tgt")
            expect tt.visible to_equal true

    context "has_prop":
        it "reports content exists":
            # @req: REQ-APP-UI-001
            step("Verify: reports content exists")
            val tt = tooltip("tt_has_1", "text", "tgt")
            expect tt.has_prop("content") to_equal true

        it "reports target exists":
            # @req: REQ-APP-UI-001
            step("Verify: reports target exists")
            val tt = tooltip("tt_has_2", "text", "tgt")
            expect tt.has_prop("target") to_equal true

        it "reports missing prop as false":
            # @req: REQ-APP-UI-001
            step("Verify: reports missing prop as false")
            val tt = tooltip("tt_has_3", "text", "tgt")
            expect tt.has_prop("label") to_equal false


# ============================================================================
# Tooltip Widget - HTML Rendering
# ============================================================================

describe "Tooltip widget HTML rendering":
    context "structure":
        it "contains widget-tooltip class":
            # @req: REQ-APP-UI-001
            step("Verify: contains widget-tooltip class")
            val node = tooltip("tt_html_1", "Help text", "target_btn")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "widget-tooltip"

        it "starts with div tag":
            # @req: REQ-APP-UI-001
            step("Verify: starts with div tag")
            val node = tooltip("tt_html_2", "Tip", "btn")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_start_with "<div"

        it "ends with closing div tag":
            # @req: REQ-APP-UI-001
            step("Verify: ends with closing div tag")
            val node = tooltip("tt_html_3", "Info", "link")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_end_with "</div>"

    context "data-target attribute":
        it "contains data-target attribute":
            # @req: REQ-APP-UI-001
            step("Verify: contains data-target attribute")
            val node = tooltip("tt_tgt_html_1", "Help", "target_btn")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "data-target"

        it "contains target id value in data-target":
            # @req: REQ-APP-UI-001
            step("Verify: contains target id value in data-target")
            val node = tooltip("tt_tgt_html_2", "Hint", "my_button")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "data-target=\"my_button\""

        it "contains different target id":
            # @req: REQ-APP-UI-001
            step("Verify: contains different target id")
            val node = tooltip("tt_tgt_html_3", "Note", "save_icon")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "data-target=\"save_icon\""

    context "content text":
        it "contains tooltip content":
            # @req: REQ-APP-UI-001
            step("Verify: contains tooltip content")
            val node = tooltip("tt_cnt_html_1", "Help text", "btn")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "Help text"

        it "contains multi-word content":
            # @req: REQ-APP-UI-001
            step("Verify: contains multi-word content")
            val node = tooltip("tt_cnt_html_2", "Click this button to submit", "submit_btn")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "Click this button to submit"

        it "renders empty content without error":
            # @req: REQ-APP-UI-001
            step("Verify: renders empty content without error")
            val node = tooltip("tt_cnt_html_3", "", "btn")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "widget-tooltip"

    context "id attribute":
        it "contains the widget id":
            # @req: REQ-APP-UI-001
            step("Verify: contains the widget id")
            val node = tooltip("tt_id_html_1", "Tip", "tgt")
            val tree = UITree.new(node)
            val state = init_state(tree)
            val html = render_html_widget(node, state)
            expect html to_contain "tt_id_html_1"


# ============================================================================
# Cross-Widget Integration
# ============================================================================

describe "Progress, Image, Tooltip integration":
    context "as children in a container":
        it "progress can be a child of a column":
            # @req: REQ-APP-UI-001
            step("Verify: progress can be a child of a column")
            val p = progress("integ_prog_1", 80)
            val col = column("integ_col_1", [p])
            expect col.child_count() to_equal 1

        it "image can be a child of a column":
            # @req: REQ-APP-UI-001
            step("Verify: image can be a child of a column")
            val img = image("integ_img_1", "pic.png", "Pic")
            val col = column("integ_col_2", [img])
            expect col.child_count() to_equal 1

        it "all three widgets in one container":
            # @req: REQ-APP-UI-001
            step("Verify: all three widgets in one container")
            val p = progress("integ_prog_2", 50)
            val img = image("integ_img_2", "logo.png", "Logo")
            val tt = tooltip("integ_tt_1", "Help", "integ_prog_2")
            val col = column("integ_col_3", [p, img, tt])
            expect col.child_count() to_equal 3

    context "prop isolation":
        it "progress and image have independent props":
            # @req: REQ-APP-UI-001
            step("Verify: progress and image have independent props")
            val p = progress("iso_prog_1", 42)
            val img = image("iso_img_1", "file.png", "File")
            expect p.get_prop("value") to_equal "42"
            expect img.get_prop("src") to_equal "file.png"
            expect p.get_prop("src") to_equal ""
            expect img.get_prop("value") to_equal ""

        it "tooltip and progress have independent props":
            # @req: REQ-APP-UI-001
            step("Verify: tooltip and progress have independent props")
            val tt = tooltip("iso_tt_1", "Tip text", "target")
            val p = progress("iso_prog_2", 99)
            expect tt.get_prop("content") to_equal "Tip text"
            expect p.get_prop("value") to_equal "99"
            expect tt.get_prop("value") to_equal ""
            expect p.get_prop("content") to_equal ""
