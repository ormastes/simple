"""
## Purpose and audience
Purpose: Verify test runner daemon readiness.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-APP-TESTRUNNERNE-001
doc/01_research/local/REQ-APP-TESTRUNNERNE-001.md
doc/03_plan/sys_test/REQ-APP-TESTRUNNERNE-001.md
doc/04_architecture/REQ-APP-TESTRUNNERNE-001.md
doc/05_design/REQ-APP-TESTRUNNERNE-001.md
"""
# @req: REQ-APP-TESTRUNNERNE-001

use std.spec.*

extern fn rt_file_read_text(path: text) -> text

use std.spec.step

describe "test runner daemon readiness":
    it "requires a responsive daemon at both routing gates":
        # @req: REQ-APP-TESTRUNNERNE-001
        step("Verify: requires a responsive daemon at both routing gates")
        val source = rt_file_read_text("src/app/test_runner_new/test_runner_main.spl") ?? ""

        expect(source.split("test_daemon_ensure_responsive(daemon_config)").len()).to_equal(3)  # oracle: 3 — named expected value from the requirement
        expect(source.contains("test_daemon_ensure_running")).to_equal(false)
