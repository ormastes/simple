#!/bin/sh
set -eu

if ! command -v clang-tidy >/dev/null 2>&1; then
    echo "CPP_LINT_INSTALLED_CLANG: SKIP clang-tidy unavailable"
    exit 0
fi

version=$(clang-tidy --version 2>&1 | head -1)
[ -n "$version" ]
echo "CPP_LINT_INSTALLED_CLANG: PASS $version"
