"""
## Purpose and audience
Purpose: Verify macos smf dynlib evidence numeric guard.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-APP-GUIPERF-001
doc/01_research/local/REQ-APP-GUIPERF-001.md
doc/03_plan/sys_test/REQ-APP-GUIPERF-001.md
doc/04_architecture/REQ-APP-GUIPERF-001.md
doc/05_design/REQ-APP-GUIPERF-001.md
"""
# @req: REQ-APP-GUIPERF-001

extern fn rt_file_read_text(path: text) -> text?

use std.spec.step

describe "macos smf dynlib evidence numeric guard":
    it "falls back when evidence numeric fields fail to parse":
        # @req: REQ-APP-GUIPERF-001
        step("Verify: falls back when evidence numeric fields fail to parse")
        val source = rt_file_read_text("src/app/gui_perf/macos_smf_dynlib_evidence_core.spl") ?? ""

        expect(source).to_contain("gui_mac_smf_dynlib_row_value(row, key).to_int() ?? -1")
        expect(source).to_contain("value.to_int() ?? -1")
        expect(source.contains("to_int() as i64")).to_equal(false)
