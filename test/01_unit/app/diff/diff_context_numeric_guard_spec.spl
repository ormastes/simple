"""
## Purpose and audience
Purpose: Verify diff context numeric guard.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-APP-DIFF-001
doc/01_research/local/REQ-APP-DIFF-001.md
doc/03_plan/sys_test/REQ-APP-DIFF-001.md
doc/04_architecture/REQ-APP-DIFF-001.md
doc/05_design/REQ-APP-DIFF-001.md
"""
# @req: REQ-APP-DIFF-001

extern fn rt_file_read_text(path: text) -> text?

use std.spec.step

describe "diff context numeric guard":
    it "defaults malformed context values":
        # @req: REQ-APP-DIFF-001
        step("Verify: defaults malformed context values")
        val source = rt_file_read_text("src/app/diff/main.spl") ?? ""

        expect(source).to_contain("fn parse_context_or_default(value: text, default_value: i64) -> i64")
        expect(source).to_contain("return trimmed.to_int() ?? default_value")
        expect(source.contains("return trimmed.to_int()\n")).to_equal(false)
