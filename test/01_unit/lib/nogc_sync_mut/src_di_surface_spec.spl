use std.spec.*
use lib.nogc_sync_mut.src.di.{InterpreterBackend, Value}

describe "nogc_sync_mut DI surface":
    it "loads backend and value helpers":
        expect(InterpreterBackend().name()).to_equal("InterpreterBackend")
        expect(Value.none()).to_equal(Value.Nil)
