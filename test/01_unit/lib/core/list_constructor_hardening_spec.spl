use core.collections.{List}

describe "List constructor hardening":
    it "constructs and mutates a direct generic List":
        val items = List<i64>()
        items.push(7)

        expect(items.len()).to_equal(1)
        expect(items.get(0)).to_equal(7)
