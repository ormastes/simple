"""
## Purpose and audience
Purpose: Verify BrowserSession HTML section and heading tag text semantics.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-LIB-COMMON-WEB-001
doc/01_research/local/REQ-LIB-COMMON-WEB-001.md
doc/03_plan/sys_test/REQ-LIB-COMMON-WEB-001.md
doc/04_architecture/REQ-LIB-COMMON-WEB-001.md
doc/05_design/REQ-LIB-COMMON-WEB-001.md
"""
# @req: REQ-LIB-COMMON-WEB-001

# codex-system-test
# @req REQ-WEB-BROWSER-002
"""
# BrowserSession HTML section text projection

Projects the supported section, heading, and address semantics to visible
text. This is focused text-projection evidence, not complete HTML rendering.
"""
use std.spec.*

use std.gc_async_mut.web.browser_session_html.{html_to_text}

describe "BrowserSession HTML section and heading tag text semantics":

    it "should separate body headings hgroup and address into readable lines":
        # @req: REQ-WEB-BROWSER-002
        step("Verify: should separate body headings hgroup and address into readable lines")
        step("Project supported HTML semantics to visible text")
        val html = "<body><hgroup><h1>Title</h1><p>Subtitle</p></hgroup><h2>Chapter</h2><h3>Section</h3><h4>Topic</h4><h5>Detail</h5><h6>Leaf</h6><address>Contact</address></body>"
        expect(html_to_text(html)).to_equal("Title\nSubtitle\nChapter\nSection\nTopic\nDetail\nLeaf\nContact")

    it "should keep adjacent headings from collapsing into one token":
        # @req: REQ-LIB-COMMON-WEB-001
        step("Verify: should keep adjacent headings from collapsing into one token")
        step("Project supported HTML semantics to visible text")
        val html = "<h1>One</h1><h2>Two</h2><h3>Three</h3><h4>Four</h4><h5>Five</h5><h6>Six</h6>"
        expect(html_to_text(html)).to_equal("One\nTwo\nThree\nFour\nFive\nSix")
