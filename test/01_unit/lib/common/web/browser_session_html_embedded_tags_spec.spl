"""
## Purpose and audience
Purpose: Verify BrowserSession HTML embedded tag text alternatives.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-LIB-COMMON-WEB-001
doc/01_research/local/REQ-LIB-COMMON-WEB-001.md
doc/03_plan/sys_test/REQ-LIB-COMMON-WEB-001.md
doc/04_architecture/REQ-LIB-COMMON-WEB-001.md
doc/05_design/REQ-LIB-COMMON-WEB-001.md
"""
# @req: REQ-LIB-COMMON-WEB-001

# codex-system-test
# @req REQ-WEB-BROWSER-002
"""
# BrowserSession HTML embedded fallback text projection

Projects supported embedded-content alternatives and fallback content to
visible text. This is not media loading, layout, or pixel evidence.
"""
use std.spec.*

use std.gc_async_mut.web.browser_session_html.{html_to_text}

describe "BrowserSession HTML embedded tag text alternatives":

    it "should use image alt text inside picture source fallback groups":
        # @req: REQ-WEB-BROWSER-002
        step("Verify: should use image alt text inside picture source fallback groups")
        step("Project supported HTML semantics to visible text")
        val html = "<picture><source srcset='hero.avif' type='image/avif'><source srcset='hero.webp' type='image/webp'><img src='hero.png' alt='Hero image'></picture>"
        expect(html_to_text(html)).to_equal("Hero image")

    it "should use area alt text while preserving embedded fallback text":
        # @req: REQ-LIB-COMMON-WEB-001
        step("Verify: should use area alt text while preserving embedded fallback text")
        step("Project supported HTML semantics to visible text")
        val html = "<map name='m'><area href='/a' alt='Area label'></map><iframe>Frame fallback</iframe><object>Object fallback</object><video><track kind='captions' src='captions.vtt'>Video fallback</video><audio>Audio fallback</audio><embed src='plugin.bin'>"
        expect(html_to_text(html)).to_equal("Area labelFrame fallbackObject fallbackVideo fallbackAudio fallback")
