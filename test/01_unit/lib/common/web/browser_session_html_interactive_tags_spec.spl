"""
## Purpose and audience
Purpose: Verify BrowserSession HTML interactive tag text semantics.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-LIB-COMMON-WEB-001
doc/01_research/local/REQ-LIB-COMMON-WEB-001.md
doc/03_plan/sys_test/REQ-LIB-COMMON-WEB-001.md
doc/04_architecture/REQ-LIB-COMMON-WEB-001.md
doc/05_design/REQ-LIB-COMMON-WEB-001.md
"""
# @req: REQ-LIB-COMMON-WEB-001

# codex-system-test
# @req REQ-WEB-BROWSER-002
"""
# BrowserSession HTML interactive text projection

Projects the supported `details` and `dialog` visibility semantics to visible
text. This is focused text projection, not event or interaction evidence.
"""
use std.spec.*

use std.gc_async_mut.web.browser_session_html.{html_to_text}

describe "BrowserSession HTML interactive tag text semantics":

    it "should show summary text when details is closed and content when open":
        # @req: REQ-WEB-BROWSER-002
        step("Verify: should show summary text when details is closed and content when open")
        step("Project supported HTML semantics to visible text")
        val closed_html = "<details><summary>More</summary><p>Hidden detail</p></details>"
        val open_html = "<details open><summary>More</summary><p>Visible detail</p></details>"
        expect(html_to_text(closed_html)).to_equal("More")
        expect(html_to_text(open_html)).to_equal("MoreVisible detail")

    it "should hide closed dialog content and expose open dialog fallback text":
        # @req: REQ-LIB-COMMON-WEB-001
        step("Verify: should hide closed dialog content and expose open dialog fallback text")
        step("Project supported HTML semantics to visible text")
        val closed_html = "<p>Before</p><dialog>Hidden dialog</dialog><p>After</p>"
        val open_html = "<p>Before</p><dialog open>Visible dialog</dialog><p>After</p>"
        expect(html_to_text(closed_html)).to_equal("BeforeAfter")
        expect(html_to_text(open_html)).to_equal("BeforeVisible dialogAfter")
