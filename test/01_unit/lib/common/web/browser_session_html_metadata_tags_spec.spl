"""
## Purpose and audience
Purpose: Verify BrowserSession HTML metadata tag text semantics.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-LIB-COMMON-WEB-001
doc/01_research/local/REQ-LIB-COMMON-WEB-001.md
doc/03_plan/sys_test/REQ-LIB-COMMON-WEB-001.md
doc/04_architecture/REQ-LIB-COMMON-WEB-001.md
doc/05_design/REQ-LIB-COMMON-WEB-001.md
"""
# @req: REQ-LIB-COMMON-WEB-001

# codex-system-test
# @req REQ-WEB-BROWSER-002
"""
# BrowserSession HTML metadata text projection

Keeps supported document metadata outside visible text projection. This is
focused visibility evidence, not complete metadata processing or rendering.
"""
use std.spec.*

use std.gc_async_mut.web.browser_session_html.{html_to_text}

describe "BrowserSession HTML metadata tag text semantics":

    it "should keep document metadata out of visible text extraction":
        # @req: REQ-WEB-BROWSER-002
        step("Verify: should keep document metadata out of visible text extraction")
        step("Project supported HTML semantics to visible text")
        val html = "<!DOCTYPE html><html><head><title>Hidden title</title><base href='https://example.com/'><link rel='stylesheet' href='site.css'><meta name='description' content='Hidden meta'><style>body { color: red; }</style></head><body>Visible body</body></html>"
        expect(html_to_text(html)).to_equal("Visible body")

    it "should keep standalone title and style contents hidden":
        # @req: REQ-LIB-COMMON-WEB-001
        step("Verify: should keep standalone title and style contents hidden")
        step("Project supported HTML semantics to visible text")
        val html = "<title>Hidden title</title><style>.hidden { display: none; }</style><p>Visible paragraph</p>"
        expect(html_to_text(html)).to_equal("Visible paragraph")
