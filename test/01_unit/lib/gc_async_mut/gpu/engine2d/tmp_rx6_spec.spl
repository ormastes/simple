use std.spec.{describe, it, expect}

fn take_i(v: i32) -> i32:
    v

describe "vk":
    it "annotated i32 inside if":
        val cond = true
        if cond:
            val x: i32 = 5
            expect(take_i(x)).to_equal(5)
        else:
            expect(2).to_equal(2)
