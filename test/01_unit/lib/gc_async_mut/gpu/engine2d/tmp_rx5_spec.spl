use std.spec.{describe, it, expect}

describe "vk":
    it "unused annotated u32 inside if":
        val cond = true
        if cond:
            val red: u32 = 5u32
            expect(1).to_equal(1)
        else:
            expect(2).to_equal(2)
