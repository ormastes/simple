use std.spec
use std.database.sql.stmt_cache.{StatementCache}

describe "SQL statement cache cardinality":
    it "reports empty capacity without materializing entry keys":
        val cache = StatementCache.new(0)

        expect(cache.size()).to_equal(0)
        expect(cache.capacity).to_equal(0)
