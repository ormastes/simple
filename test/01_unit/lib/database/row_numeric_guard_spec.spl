"""
## Purpose and audience
Purpose: Verify database row numeric guard.
Audience: compiler and tooling engineers who maintain this spec.
## Operator workflow
Run this spec with the test runner and read the per-scenario verdict lines;
a failing scenario pinpoints the behavior that regressed.
## Compatibility and limitations
Covers the pinned behavior only; fixture data is local to this spec.
Troubleshooting: a red scenario here means the pinned contract changed —
check verification guidance in the linked design docs before editing oracles.
# @manual: primary
REQ-LIB-DATABASE-001
doc/01_research/local/REQ-LIB-DATABASE-001.md
doc/03_plan/sys_test/REQ-LIB-DATABASE-001.md
doc/04_architecture/REQ-LIB-DATABASE-001.md
doc/05_design/REQ-LIB-DATABASE-001.md
"""
# @req: REQ-LIB-DATABASE-001

extern fn rt_file_read_text(path: text) -> text?

use std.spec.step

describe "database row numeric guard":
    it "returns nil for malformed row integer fields":
        # @req: REQ-LIB-DATABASE-001
        step("Verify: returns nil for malformed row integer fields")
        val source = rt_file_read_text("src/lib/nogc_sync_mut/database/core.spl") ?? ""

        expect(source).to_contain("val parsed = value.to_int()")
        expect(source).to_contain("if parsed == nil:")
        expect(source).to_contain("return nil")
        expect(source.contains("Some(value.to_int())")).to_equal(false)
