# Purpose and audience: executable specification evidence for the owning engineering team.
# @req REQ-SSPEC-LIB
# research: doc/01_research/domain/sspec_documentization_maintenance.md ; plan: doc/03_plan/sspec_modernization_plan.md ; architecture: doc/04_architecture/sspec_documentization_maintenance.md ; design: doc/05_design/infra/sspec/modern_sspec_typed_evidence_design.md

# Purpose and audience: executable specification evidence for the owning engineering team.
# @req REQ-SSPEC-LIB
# research: doc/01_research/domain/sspec_documentization_maintenance.md ; plan: doc/03_plan/sspec_modernization_plan.md ; architecture: doc/04_architecture/sspec_documentization_maintenance.md ; design: doc/05_design/infra/sspec/modern_sspec_typed_evidence_design.md

use std.spec.step

use std.spec
use std.spec.step
use std.gc_sync_mut.sanitizer.lsan.mod.{lsan_is_enabled, lsan_check_since, lsan_bytes_since, lsan_suppress_tag, lsan_is_suppressed, lsan_error_count, lsan_reset}
use std.gc_sync_mut.sanitizer.lsan.types.{leak_checkpoint}


describe "gc_sync_mut sanitizer lsan facade":
    # @manual scenario evidence

    it "re-exports leak sanitizer disabled-state checks and records":
        # @req REQ-SSPEC-LIB
        step("re-exports leak sanitizer disabled-state checks and records")
        lsan_reset()
        expect(lsan_is_enabled()).to_equal(false)
        expect(lsan_check_since("missing")).to_equal(0)
        expect(lsan_bytes_since("missing")).to_equal(0)
        expect(lsan_error_count()).to_equal(0)

        val checkpoint = leak_checkpoint("before", 7)
        expect(checkpoint.name).to_equal("before")

    it "re-exports suppression tags":
        # @req REQ-SSPEC-LIB
        step("re-exports suppression tags")
        lsan_reset()
        expect(lsan_is_suppressed("fixture")).to_equal(false)
        lsan_suppress_tag("fixture")
        expect(lsan_is_suppressed("fixture")).to_equal(true)
