# Test static fn methods
use std.spec.*
use std.diagram.CallEventRecorder

describe "Static Method Import":
    it "can call static fn new on CallEventRecorder":
        val recorder = CallEventRecorder.new("static_fn")
        expect(recorder.get_events().len() == 0)
