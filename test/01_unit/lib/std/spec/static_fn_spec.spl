# Test static fn methods
use std.spec.*
use std.common.diagram.CallEventRecorder

describe "Static Method Import":
    it "can call static fn new on CallEventRecorder":
        val recorder = CallEventRecorder.new("test")
        expect(recorder.event_count() == 0)
