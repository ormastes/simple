describe "native u8 regression":
    it "compares direct u8 index with u8 literal":
        var enc: [u8] = []
        enc.push(0x17u8)

        expect(enc[0].to_i64()).to_equal(23)
        expect(enc[0] == 0x17u8).to_equal(true)
        assert_equal(enc[0], 0x17u8)
