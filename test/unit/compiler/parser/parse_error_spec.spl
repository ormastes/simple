"""
# Parser Error Handling Specification

**Feature IDs:** #TBD
**Category:** Compiler
**Status:** In Progress

## Overview

Tests that the parser handles valid Simple code with no errors.
"""

use std.spec
use compiler_core.parser.*
use compiler.parser_types.*
use compiler_core.lexer.*
use compiler.blocks.*
use compiler.treesitter.*


describe "Parser Error Handling":
    it "parses valid Simple code with no errors":
        val src = "fn main() -> i64:\n    0\n"
        var parser = Parser.new(src)
        val ast = parser.parse()
        expect(parser.errors.len()).to_equal(0)
