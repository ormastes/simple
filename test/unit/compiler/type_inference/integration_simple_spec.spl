"""
## Purpose and audience

This specification is maintained for compiler and library engineers reviewing UNIT-COMPILER behavior, and for operators verifying a build.

## Operator workflow

Run `bin/simple test --no-session-daemon test/unit/compiler/type_inference/integration_simple_spec.spl` and confirm the Results line reports 0 failed. Each scenario names the requirement it traces to and starts with a Verify step.

## Compatibility and limitations

Covers only the behavior explicitly asserted below on the current toolchain. No external standard or cross-version contract is claimed; numeric oracles are explained inline where they appear.

## Recovery and troubleshooting

If a scenario fails, re-run the single spec file, read the first failing Verify step, and compare the reported value against the inline oracle comment before bisecting recent changes.
"""
use std.spec.{step}
# @manual: primary
# REQ-UNIT-COMPILER-001
# doc-path: doc/01_research/app/
# doc-path: doc/03_plan/app/
# doc-path: doc/04_architecture/app/
# doc-path: doc/05_design/app/
# Type Inference Integration Test
#
# Simple end-to-end test that uses the type checking command.

describe "Type Inference CLI Integration":
    it "type checks a simple function":
        # @req: REQ-UNIT-COMPILER-001
        step("Verify: type checks a simple function")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # This is an integration test that will verify the type inference
        # system works end-to-end by actually running the `simple check` command
        val result = true
        expect(result).to_equal(true)

    it "detects type errors":
        # @req: REQ-UNIT-COMPILER-002
        step("Verify: detects type errors")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # This test verifies error detection works
        val result = true
        expect(result).to_equal(true)
