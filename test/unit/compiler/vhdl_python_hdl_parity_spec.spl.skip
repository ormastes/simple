"""
# VHDL Python-HDL Parity Deferrals

**Feature ID:** VHDL-PYHDL-PARITY
**Category:** Compiler/VHDL
**Status:** planned

Tracks only parity work that remains outside the selected Python-HDL parity
milestone. Completed facade/backend lanes are covered by runnable VHDL tests
and must not be re-listed here as pending.
"""
use std.spec

fn main():
    describe "VHDL Python-HDL parity deferrals":
        skip "pure Simple source-of-truth path replaces compatibility source-facade fallback":
            nil

        skip "payload enums lower to tagged records when representation support is implemented":
            nil

        skip "floating-point hardware values lower through an explicit fixed-point or float IP contract":
            nil

        skip "dynamic heap allocation and pointer-like addressing lower through explicit memory interfaces":
            nil

        skip "general stateful MIR and dynamic address or indirect call instructions lower to synthesizable VHDL":
            nil

        skip "unconstrained memories outside explicit ROM and RAM templates infer with vendor-safe policies":
            nil

        skip "broad HLS helper inference outside the selected pure combinational helper subset is specified":
            nil

        skip "anonymous same-type hardware outputs become a stable public VHDL API":
            nil
