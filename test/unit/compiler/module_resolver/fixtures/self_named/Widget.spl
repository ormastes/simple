# Fixture: module file named after the class it exports (`Widget.spl` / `class Widget`).
# Used by group_import_self_named_module_spec.spl.

pub class Widget:
    pub val size: i64

    pub fn kind() -> text:
        return "widget"

pub fn make_widget(n: i64) -> Widget:
    return Widget(size: n)
