use std.testing.{describe, it, expect}

describe "empty array map lambda":
    it "maps wildcard lambda over empty array":
        expect([].map(\_: 0)).to_equal([])

    it "maps named lambda over empty array":
        expect([].map(\f: f)).to_equal([])
