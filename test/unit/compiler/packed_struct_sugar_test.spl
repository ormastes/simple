use std.spec.{describe, it, expect}

@packed
struct PciStatus:
    vendor: u32:16
    command: u32:4

describe "@packed struct sugar":
    it "parses and creates bitfield-like access":
        var s: PciStatus = PciStatus.new(0)
        s.command = 5
        expect(s.command).to_equal(5)
