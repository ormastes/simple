
## Operator workflow

1. Run `bin/simple test test/01_unit/compiler/common/effects_spec.spl`.
2. Every scenario must pass; a failure is a regression in the
   behavior under test.

## Compatibility and limitations

Covers the interpreter and native lanes for the constructs
asserted here; platform-specific behavior is out of scope.
"""

    # NOTE: EffectEnv.set_effect is defective at runtime ("semantic: type
    # mismatch: cannot convert dict to int", probe 2026-08-26) so the dirty-set
    # fixed-point scenario cannot be asserted green yet; the module is also
    # documented NOT WIRED in its header. Re-add the dirty-set scenario once
    # set_effect executes.
