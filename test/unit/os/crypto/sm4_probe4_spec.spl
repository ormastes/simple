use std.spec.*
use os.crypto.sm4.*

describe "SM4 probe4":
    it "rotl32 works":
        val r = _sm4_rotl32(1, 2)
        expect(r.to_i64()).to_equal(4)
    it "tau works":
        val sbox = _sm4_sbox()
        val r = _sm4_tau(sbox, 0x01234567)
        expect(r.to_i64()).to_be_greater_than(0)
