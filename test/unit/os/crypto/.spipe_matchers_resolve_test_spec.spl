use std.spec.*
use os.crypto.serpent.{serpent_encrypt_block}

fn _zero16() -> [u8]:
    var b: [u8] = []
    var i: i64 = 0
    while i < 16:
        b.push(0)
        i = i + 1
    b

describe "resolve":
    it "len":
        expect serpent_encrypt_block(_zero16(), _zero16()).len() == 16