use std.spec.*

fn _my_L(x: u32) -> u32:
    x ^ 1

fn do_call() -> u32:
    _my_L(42)

describe "fn uppercase":
    it "calls fn with uppercase in name":
        val r = do_call()
        expect(r.to_i64()).to_equal(43)
