# Atom — CAS Reference Tests
#
# Tests for the Atom class from nogc_async_immut/atom.
# Atom provides lock-free mutable reference to an immutable value
# using compare-and-swap semantics.

"""
## Purpose and audience
Purpose: Verify Atom.
Audience: compiler and tooling engineers who maintain this spec.
"""

# @req: REQ-LIB-ATOM-001 — stable requirement identity for this spec.

use std.atom.{Atom, Atom__new, Atom__of}

use std.spec.step

describe "Atom":

    describe "creation":
        it "creates with initial value":
            step("Verify: creates with initial value")
            # @req: REQ-LIB-ATOM-001
            val a = Atom.new(42)
            expect(a.deref()).to_equal(42)  # oracle: authoritative expected value documented by this spec's contract

        it "starts at version 0":
            step("Verify: starts at version 0")
            val a = Atom.new(0)
            expect(a.version()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract

        it "Atom__of is alias for Atom__new":
            step("Verify: Atom__of is alias for Atom__new")
            val a = Atom.of("hello")
            expect(a.deref()).to_equal("hello")

        it "creates with nil initial value":
            step("Verify: creates with nil initial value")
            val a = Atom.new(nil)
            expect(a.deref()).to_be_nil()

        it "creates with string value":
            step("Verify: creates with string value")
            val a = Atom.new("test")
            expect(a.deref()).to_equal("test")

    describe "deref":
        it "reads the current value":
            step("Verify: reads the current value")
            val a = Atom.new(100)
            expect(a.deref()).to_equal(100)  # oracle: authoritative expected value documented by this spec's contract

        it "returns same value on repeated reads":
            step("Verify: returns same value on repeated reads")
            val a = Atom.new(77)
            expect(a.deref()).to_equal(77)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.deref()).to_equal(77)  # oracle: authoritative expected value documented by this spec's contract

    describe "swap":
        it "applies function and returns new value":
            step("Verify: applies function and returns new value")
            var a = Atom.new(10)
            val result = a.swap(_1 + 5)
            expect(result).to_equal(15)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.deref()).to_equal(15)  # oracle: authoritative expected value documented by this spec's contract

        it "increments version on swap":
            step("Verify: increments version on swap")
            var a = Atom.new(0)
            a.swap(_1 + 1)
            expect(a.version()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            a.swap(_1 + 1)
            expect(a.version()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract

        it "applies multiplication":
            step("Verify: applies multiplication")
            var a = Atom.new(5)
            val result = a.swap(_1 * 3)
            expect(result).to_equal(15)  # oracle: authoritative expected value documented by this spec's contract

        it "applies identity function":
            step("Verify: applies identity function")
            var a = Atom.new(42)
            val result = a.swap(\x: x)
            expect(result).to_equal(42)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.version()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

    describe "reset":
        it "sets new value unconditionally":
            step("Verify: sets new value unconditionally")
            var a = Atom.new(1)
            a.reset(99)
            expect(a.deref()).to_equal(99)  # oracle: authoritative expected value documented by this spec's contract

        it "increments version":
            step("Verify: increments version")
            var a = Atom.new(0)
            a.reset(42)
            expect(a.version()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

        it "overwrites previous value":
            step("Verify: overwrites previous value")
            var a = Atom.new(10)
            a.reset(20)
            a.reset(30)
            expect(a.deref()).to_equal(30)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.version()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract

        it "can reset to nil":
            step("Verify: can reset to nil")
            var a = Atom.new(42)
            a.reset(nil)
            expect(a.deref()).to_be_nil()

    describe "compare_and_set":
        it "succeeds when expected matches":
            step("Verify: succeeds when expected matches")
            var a = Atom.new(10)
            val ok = a.compare_and_set(10, 20)
            expect(ok).to_equal(true)
            expect(a.deref()).to_equal(20)  # oracle: authoritative expected value documented by this spec's contract

        it "fails when expected does not match":
            step("Verify: fails when expected does not match")
            var a = Atom.new(10)
            val ok = a.compare_and_set(5, 20)
            expect(ok).to_equal(false)
            expect(a.deref()).to_equal(10)  # oracle: authoritative expected value documented by this spec's contract

        it "does not increment version on failure":
            step("Verify: does not increment version on failure")
            var a = Atom.new(10)
            a.compare_and_set(999, 20)
            expect(a.version()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract

        it "increments version on success":
            step("Verify: increments version on success")
            var a = Atom.new(10)
            a.compare_and_set(10, 20)
            expect(a.version()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

    describe "swap_returning_old":
        it "returns old value":
            step("Verify: returns old value")
            var a = Atom.new(100)
            val old = a.swap_returning_old(_1 * 2)
            expect(old).to_equal(100)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.deref()).to_equal(200)  # oracle: authoritative expected value documented by this spec's contract

        it "increments version":
            step("Verify: increments version")
            var a = Atom.new(0)
            a.swap_returning_old(_1 + 1)
            expect(a.version()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

        it "returns old on multiple swaps":
            step("Verify: returns old on multiple swaps")
            var a = Atom.new(1)
            val old1 = a.swap_returning_old(_1 + 10)
            val old2 = a.swap_returning_old(_1 + 10)
            expect(old1).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(old2).to_equal(11)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.deref()).to_equal(21)  # oracle: authoritative expected value documented by this spec's contract

    describe "multiple operations":
        it "tracks version across operations":
            step("Verify: tracks version across operations")
            var a = Atom.new(0)
            a.swap(_1 + 1)
            a.reset(50)
            a.compare_and_set(50, 100)
            expect(a.version()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.deref()).to_equal(100)  # oracle: authoritative expected value documented by this spec's contract

        it "failed CAS does not affect version":
            step("Verify: failed CAS does not affect version")
            var a = Atom.new(0)
            a.swap(_1 + 1)
            a.compare_and_set(999, 0)
            expect(a.version()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.deref()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

        it "sequence of resets":
            step("Verify: sequence of resets")
            var a = Atom.new(0)
            a.reset(1)
            a.reset(2)
            a.reset(3)
            expect(a.deref()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(a.version()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
