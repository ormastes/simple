# PersistentList — Cons List Tests
#
# Tests for the PersistentList class from nogc_async_immut/persistent_list.
# Immutable singly-linked list with structural sharing.
# O(1) prepend, O(1) head/tail, O(n) indexed access.

"""
## Purpose and audience
Purpose: Verify PersistentList.
Audience: compiler and tooling engineers who maintain this spec.
"""

# @req: REQ-LIB-PERSISTENT-LIST-001 — stable requirement identity for this spec.

use std.persistent_list.{PersistentList, PersistentList__empty, PersistentList__of, PersistentList__from_array}

use std.spec.step

describe "PersistentList":

    describe "empty list":
        it "has zero length":
            step("Verify: has zero length")
            # @req: REQ-LIB-PERSISTENT-LIST-001
            val l = PersistentList.empty()
            expect(l.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract

        it "is empty":
            step("Verify: is empty")
            val l = PersistentList.empty()
            expect(l.is_empty()).to_equal(true)

        it "head is nil":
            step("Verify: head is nil")
            val l = PersistentList.empty()
            expect(l.head()).to_be_nil()

        it "tail is empty":
            step("Verify: tail is empty")
            val l = PersistentList.empty()
            val t = l.tail()
            expect(t.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract
            expect(t.is_empty()).to_equal(true)

    describe "prepend":
        it "adds element to front":
            step("Verify: adds element to front")
            val l = PersistentList.empty().prepend(1)
            expect(l.len()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.head()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.is_empty()).to_equal(false)

        it "preserves order":
            step("Verify: preserves order")
            val l = PersistentList.empty().prepend(3).prepend(2).prepend(1)
            expect(l.head()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.get(1)).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.get(2)).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.len()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract

        it "does not modify original":
            step("Verify: does not modify original")
            val l1 = PersistentList.empty().prepend(1)
            val l2 = l1.prepend(2)
            expect(l1.len()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(l1.head()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(l2.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(l2.head()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract

        it "supports multiple branches from same base":
            step("Verify: supports multiple branches from same base")
            val base = PersistentList.empty().prepend(1)
            val branch_a = base.prepend(10)
            val branch_b = base.prepend(20)
            expect(base.len()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(branch_a.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(branch_b.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(branch_a.head()).to_equal(10)  # oracle: authoritative expected value documented by this spec's contract
            expect(branch_b.head()).to_equal(20)  # oracle: authoritative expected value documented by this spec's contract

    describe "PersistentList__of":
        it "builds list preserving order":
            step("Verify: builds list preserving order")
            val l = PersistentList.of([10, 20, 30])
            expect(l.len()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.head()).to_equal(10)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.get(1)).to_equal(20)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.get(2)).to_equal(30)  # oracle: authoritative expected value documented by this spec's contract

        it "handles single element":
            step("Verify: handles single element")
            val l = PersistentList.of([42])
            expect(l.len()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.head()).to_equal(42)  # oracle: authoritative expected value documented by this spec's contract

    describe "from_array":
        it "builds list from array":
            step("Verify: builds list from array")
            val l = PersistentList.from_array([5, 10, 15])
            expect(l.len()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.head()).to_equal(5)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.get(2)).to_equal(15)  # oracle: authoritative expected value documented by this spec's contract

        it "empty array gives empty list":
            step("Verify: empty array gives empty list")
            val l = PersistentList.from_array([])
            expect(l.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.is_empty()).to_equal(true)

    describe "get":
        it "returns element at index":
            step("Verify: returns element at index")
            val l = PersistentList.of([5, 10, 15])
            expect(l.get(0)).to_equal(5)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.get(1)).to_equal(10)  # oracle: authoritative expected value documented by this spec's contract
            expect(l.get(2)).to_equal(15)  # oracle: authoritative expected value documented by this spec's contract

        it "returns nil for out of bounds":
            step("Verify: returns nil for out of bounds")
            val l = PersistentList.of([1, 2])
            expect(l.get(5)).to_be_nil()

        it "returns nil for negative index":
            step("Verify: returns nil for negative index")
            val l = PersistentList.of([1, 2])
            expect(l.get(-1)).to_be_nil()

    describe "contains":
        it "finds existing element":
            step("Verify: finds existing element")
            val l = PersistentList.of(["a", "b", "c"])
            expect(l.contains("b")).to_equal(true)

        it "returns false for missing element":
            step("Verify: returns false for missing element")
            val l = PersistentList.of(["a", "b", "c"])
            expect(l.contains("z")).to_equal(false)

        it "works with integers":
            step("Verify: works with integers")
            val l = PersistentList.of([1, 2, 3, 4, 5])
            expect(l.contains(3)).to_equal(true)
            expect(l.contains(6)).to_equal(false)

        it "returns false for empty list":
            step("Verify: returns false for empty list")
            val l = PersistentList.empty()
            expect(l.contains(1)).to_equal(false)

    describe "tail":
        it "returns rest of list":
            step("Verify: returns rest of list")
            val l = PersistentList.of([1, 2, 3])
            val t = l.tail()
            expect(t.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(t.head()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract

        it "tail of single element is empty":
            step("Verify: tail of single element is empty")
            val l = PersistentList.of([42])
            val t = l.tail()
            expect(t.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract
            expect(t.is_empty()).to_equal(true)

        it "chained tail calls":
            step("Verify: chained tail calls")
            val l = PersistentList.of([1, 2, 3])
            val t = l.tail().tail()
            expect(t.len()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(t.head()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract

    describe "head":
        it "returns first element":
            step("Verify: returns first element")
            val l = PersistentList.of([99, 88, 77])
            expect(l.head()).to_equal(99)  # oracle: authoritative expected value documented by this spec's contract

        it "returns nil for empty":
            step("Verify: returns nil for empty")
            val l = PersistentList.empty()
            expect(l.head()).to_be_nil()

    describe "reverse":
        it "reverses the list":
            step("Verify: reverses the list")
            val l = PersistentList.of([1, 2, 3])
            val r = l.reverse()
            expect(r.head()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(r.get(1)).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(r.get(2)).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

        it "preserves length":
            step("Verify: preserves length")
            val l = PersistentList.of([1, 2, 3, 4])
            val r = l.reverse()
            expect(r.len()).to_equal(4)  # oracle: authoritative expected value documented by this spec's contract

        it "reverse of single element is same":
            step("Verify: reverse of single element is same")
            val l = PersistentList.of([42])
            val r = l.reverse()
            expect(r.head()).to_equal(42)  # oracle: authoritative expected value documented by this spec's contract
            expect(r.len()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

        it "reverse of empty is empty":
            step("Verify: reverse of empty is empty")
            val l = PersistentList.empty()
            val r = l.reverse()
            expect(r.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract

        it "does not modify original":
            step("Verify: does not modify original")
            val l = PersistentList.of([1, 2, 3])
            val r = l.reverse()
            expect(l.head()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(r.head()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract

    describe "to_array":
        it "converts to array":
            step("Verify: converts to array")
            val l = PersistentList.of([10, 20, 30])
            val arr = l.to_array()
            expect(arr.len()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(arr[0]).to_equal(10)  # oracle: authoritative expected value documented by this spec's contract
            expect(arr[1]).to_equal(20)  # oracle: authoritative expected value documented by this spec's contract
            expect(arr[2]).to_equal(30)  # oracle: authoritative expected value documented by this spec's contract

        it "empty list gives empty array":
            step("Verify: empty list gives empty array")
            val l = PersistentList.empty()
            val arr = l.to_array()
            expect(arr.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract

    describe "concat":
        it "joins two lists":
            step("Verify: joins two lists")
            val l1 = PersistentList.of([1, 2])
            val l2 = PersistentList.of([3, 4])
            val joined = l1.concat(l2)
            expect(joined.len()).to_equal(4)  # oracle: authoritative expected value documented by this spec's contract
            expect(joined.head()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract
            expect(joined.get(1)).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(joined.get(2)).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(joined.get(3)).to_equal(4)  # oracle: authoritative expected value documented by this spec's contract

        it "concat with empty list":
            step("Verify: concat with empty list")
            val l1 = PersistentList.of([1, 2])
            val l2 = PersistentList.empty()
            val joined = l1.concat(l2)
            expect(joined.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(joined.head()).to_equal(1)  # oracle: authoritative expected value documented by this spec's contract

        it "empty concat with list":
            step("Verify: empty concat with list")
            val l1 = PersistentList.empty()
            val l2 = PersistentList.of([3, 4])
            val joined = l1.concat(l2)
            expect(joined.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(joined.head()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract

        it "does not modify originals":
            step("Verify: does not modify originals")
            val l1 = PersistentList.of([1, 2])
            val l2 = PersistentList.of([3, 4])
            val joined = l1.concat(l2)
            expect(l1.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(l2.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(joined.len()).to_equal(4)  # oracle: authoritative expected value documented by this spec's contract

    describe "map":
        it "applies function to each element":
            step("Verify: applies function to each element")
            val l = PersistentList.of([1, 2, 3])
            val doubled = l.map(_1 * 2)
            expect(doubled.len()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract
            expect(doubled.get(0)).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(doubled.get(1)).to_equal(4)  # oracle: authoritative expected value documented by this spec's contract
            expect(doubled.get(2)).to_equal(6)  # oracle: authoritative expected value documented by this spec's contract

        it "preserves length":
            step("Verify: preserves length")
            val l = PersistentList.of([10, 20, 30])
            val result = l.map(_1 + 1)
            expect(result.len()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract

        it "empty list map is empty":
            step("Verify: empty list map is empty")
            val l = PersistentList.empty()
            val result = l.map(_1 * 2)
            expect(result.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract

    describe "filter":
        it "keeps matching elements":
            step("Verify: keeps matching elements")
            val l = PersistentList.of([1, 2, 3, 4, 5])
            val evens = l.filter(_1 % 2 == 0)
            expect(evens.len()).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(evens.get(0)).to_equal(2)  # oracle: authoritative expected value documented by this spec's contract
            expect(evens.get(1)).to_equal(4)  # oracle: authoritative expected value documented by this spec's contract

        it "returns empty when nothing matches":
            step("Verify: returns empty when nothing matches")
            val l = PersistentList.of([1, 3, 5])
            val evens = l.filter(_1 % 2 == 0)
            expect(evens.len()).to_equal(0)  # oracle: authoritative expected value documented by this spec's contract

        it "returns all when everything matches":
            step("Verify: returns all when everything matches")
            val l = PersistentList.of([2, 4, 6])
            val evens = l.filter(_1 % 2 == 0)
            expect(evens.len()).to_equal(3)  # oracle: authoritative expected value documented by this spec's contract

    describe "fold":
        it "accumulates sum":
            step("Verify: accumulates sum")
            val l = PersistentList.of([1, 2, 3, 4])
            val sum = l.fold(0, \acc, x: acc + x)
            expect(sum).to_equal(10)  # oracle: authoritative expected value documented by this spec's contract

        it "accumulates product":
            step("Verify: accumulates product")
            val l = PersistentList.of([1, 2, 3, 4])
            val product = l.fold(1, \acc, x: acc * x)
            expect(product).to_equal(24)  # oracle: authoritative expected value documented by this spec's contract

        it "returns init for empty list":
            step("Verify: returns init for empty list")
            val l = PersistentList.empty()
            val result = l.fold(42, \acc, x: acc + x)
            expect(result).to_equal(42)  # oracle: authoritative expected value documented by this spec's contract

        it "single element":
            step("Verify: single element")
            val l = PersistentList.of([5])
            val result = l.fold(0, \acc, x: acc + x)
            expect(result).to_equal(5)  # oracle: authoritative expected value documented by this spec's contract
