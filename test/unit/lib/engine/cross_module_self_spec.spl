use std.spec.{describe, it, expect}
use test.unit.lib.engine.helper_mod.{HelperClass}

describe "cross-module me method calling self.fn":
    it "bump (me method calling self.find) works after import":
        var obj = HelperClass.new()
        obj.bump()
        expect(obj.x).to_equal(1)
