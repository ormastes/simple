class HelperClass:
    x: i32

impl HelperClass:
    static fn new() -> HelperClass:
        HelperClass(x: 0)

    fn find(self) -> i32:
        self.x

    me bump():
        val h = self.find()
        self.x = h + 1
