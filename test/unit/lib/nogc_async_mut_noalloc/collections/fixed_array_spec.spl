describe "Fixed Array":
    it "skipped":
        skip("pre-existing test failures - functions/imports not available")

# # FixedArray - Capacity-Bounded Array Tests
# 
# describe "FixedArray":
# 
#     describe "construction":
#         it "creates empty array with given capacity":
#             var arr = FixedArray.new(8)
#             expect(arr.size()).to_equal(0)
#             expect(arr.is_empty()).to_equal(true)
#             expect(arr.is_full()).to_equal(false)
# 
#     describe "push and get":
#         it "pushes values and retrieves them":
#             var arr = FixedArray.new(4)
#             expect(arr.push(10)).to_equal(true)
#             expect(arr.push(20)).to_equal(true)
#             expect(arr.push(30)).to_equal(true)
#             expect(arr.get(0)).to_equal(10)
#             expect(arr.get(1)).to_equal(20)
#             expect(arr.get(2)).to_equal(30)
#             expect(arr.size()).to_equal(3)
# 
#         it "returns false when pushing to full array":
#             var arr = FixedArray.new(2)
#             expect(arr.push(1)).to_equal(true)
#             expect(arr.push(2)).to_equal(true)
#             expect(arr.push(3)).to_equal(false)
#             expect(arr.size()).to_equal(2)
# 
#     describe "set":
#         it "updates value at index":
#             var arr = FixedArray.new(4)
#             arr.push(10)
#             arr.push(20)
#             arr.set(0, 99)
#             expect(arr.get(0)).to_equal(99)
#             expect(arr.get(1)).to_equal(20)
# 
#     describe "remove":
#         it "removes element and shifts remaining":
#             var arr = FixedArray.new(8)
#             arr.push(10)
#             arr.push(20)
#             arr.push(30)
#             val removed = arr.remove(1)
#             expect(removed).to_equal(20)
#             expect(arr.size()).to_equal(2)
#             expect(arr.get(0)).to_equal(10)
#             expect(arr.get(1)).to_equal(30)
# 
#     describe "pop and last":
#         it "pops last element":
#             var arr = FixedArray.new(4)
#             arr.push(10)
#             arr.push(20)
#             val v = arr.pop()
#             expect(v).to_equal(20)
#             expect(arr.size()).to_equal(1)
# 
#         it "returns last element without removing":
#             var arr = FixedArray.new(4)
#             arr.push(10)
#             arr.push(20)
#             expect(arr.last()).to_equal(20)
#             expect(arr.size()).to_equal(2)
# 
#     describe "contains and index_of":
#         it "finds existing value":
#             var arr = FixedArray.new(4)
#             arr.push(10)
#             arr.push(20)
#             arr.push(30)
#             expect(arr.contains(20)).to_equal(true)
#             expect(arr.index_of(20)).to_equal(1)
# 
#         it "returns false for missing value":
#             var arr = FixedArray.new(4)
#             arr.push(10)
#             expect(arr.contains(99)).to_equal(false)
#             expect(arr.index_of(99)).to_equal(-1)
# 
#     describe "clear":
#         it "removes all elements":
#             var arr = FixedArray.new(4)
#             arr.push(10)
#             arr.push(20)
#             arr.clear()
#             expect(arr.size()).to_equal(0)
#             expect(arr.is_empty()).to_equal(true)
# 
#     describe "factory functions":
#         it "creates arrays with standard capacities":
#             val a8 = fixed_array_8()
#             val a16 = fixed_array_16()
#             val a32 = fixed_array_32()
#             val a64 = fixed_array_64()
#             val a128 = fixed_array_128()
#             val a256 = fixed_array_256()
#             expect(a8.capacity).to_equal(8)
#             expect(a16.capacity).to_equal(16)
#             expect(a32.capacity).to_equal(32)
#             expect(a64.capacity).to_equal(64)
#             expect(a128.capacity).to_equal(128)
#             expect(a256.capacity).to_equal(256)
# 
#     describe "capacity enforcement":
#         it "respects capacity limit across operations":
#             var arr = FixedArray.new(3)
#             arr.push(1)
#             arr.push(2)
#             arr.push(3)
#             expect(arr.is_full()).to_equal(true)
#             arr.pop()
#             expect(arr.is_full()).to_equal(false)
#             expect(arr.push(4)).to_equal(true)
#             expect(arr.is_full()).to_equal(true)
