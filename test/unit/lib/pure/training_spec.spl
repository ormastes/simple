describe "Training":
    it "skipped":
        val pending_reason = "interpreter runtime issue: semantic: class `Tensor` has no field named `data`"
        expect(pending_reason.len()).to_be_greater_than(0)
