use std.gc_async_immut.{PersistentVec}

describe "gc_async_immut vector push empty native probe":
    it "pushes one value":
        val base = PersistentVec.empty().push(10)
        expect(base.len()).to_equal(1)
        expect(base.get(0)).to_equal(10)
