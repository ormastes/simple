use std.gc_async_immut.{Atom}

describe "gc_async_immut atom native probe":
    it "constructs an atom":
        var atom = Atom.new(5)
        expect atom.deref() == 5

    it "resets an atom":
        var atom = Atom.new(5)
        atom.reset(8)
        expect atom.deref() == 8
