use std.gc_async_immut.{pmap, nogc_async_immut_version}

describe "gc_async_immut root native probe":
    it "runs root helpers":
        expect pmap([1, 2, 3], _1 + 1) == [2, 3, 4]
        expect nogc_async_immut_version() == "0.1.0"
