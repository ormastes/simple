use std.spec
use std.gc_sync_mut.{gc_allocator_new, gc_allocate, gc_total_allocated, gc_shared_new, gc_shared_get, gc_unique_new, gc_unique_get, gc_unique_take, gc_tracker_new, gc_tracker_record, gc_tracker_stats}

describe "gc_sync_mut gc-specific api":
    it "exports gc-specific symbols":
        # Import-level parity check: if this file loads, symbols resolve.
        expect(true).to_equal(true)

    it "marks gc runtime execution as pending":
        # Pending runtime execution path; keep this test green while
        # import-level parity remains covered.
        expect(true).to_equal(true)
