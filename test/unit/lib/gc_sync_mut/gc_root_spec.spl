use std.spec
use std.gc_sync_mut.{gc_root_pin, gc_root_unpin, gc_with_pinned, GcRoot}

describe "gc_sync_mut gc_root":
    it "exports gc_root symbols":
        expect(true).to_equal(true)

    it "marks gc_root runtime path as pending":
        expect(true).to_equal(true)
