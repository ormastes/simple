use std.spec.{describe, it, expect}
use app.test_daemon.qemu_broker.{QemuBroker, qemu_broker_new}

describe "throwaway broker import check":
    it "loads broker module":
        expect(1).to_equal(1)
