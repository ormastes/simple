use std.spec.{describe, it, expect}

fn inline(f) -> i64:
    return 999

@inline
fn zz_target(x: i64) -> i64:
    return x + 1

describe "zz shadow probe":
    it "shows whether @inline silently binds to a user fn named inline":
        expect(zz_target(1)).to_equal(2)
