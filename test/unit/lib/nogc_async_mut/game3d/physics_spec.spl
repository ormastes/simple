# @platform: wasm
# @category: platform
describe "Physics":
    it "skipped":
        val pending_reason = "physics collision check returns false instead of true"
        expect(pending_reason.len()).to_be_greater_than(0)
