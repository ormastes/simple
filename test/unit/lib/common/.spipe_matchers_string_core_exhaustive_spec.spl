# @covers src/lib/common/string_core.spl 80%
"""
# String Core Exhaustive Char Table Specification

**Feature IDs:** #LIB-STRING-CORE
**Category:** Stdlib
**Status:** Implemented

## Overview

Exhaustive per-character branch coverage for char_code_inline and
char_from_code_inline. Each digit, uppercase letter, and lowercase letter
is tested individually to cover every if-branch in the lookup tables.
"""

use common.string_core.{char_code_inline, char_from_code_inline}


# ============================================================================
# Test Group 13: Exhaustive char_code_inline branch coverage
# ============================================================================

describe "string_core - char_code_inline exhaustive digits":
    """
    ## char_code_inline - all digit branches

    Every digit 0-9 must be tested individually to cover each if-branch.
    """

    context "all digits":
        it "returns 49 for 1":
            expect char_code_inline("1") == 49

        it "returns 50 for 2":
            expect char_code_inline("2") == 50

        it "returns 51 for 3":
            expect char_code_inline("3") == 51

        it "returns 52 for 4":
            expect char_code_inline("4") == 52

        it "returns 54 for 6":
            expect char_code_inline("6") == 54

        it "returns 55 for 7":
            expect char_code_inline("7") == 55

        it "returns 56 for 8":
            expect char_code_inline("8") == 56

describe "string_core - char_code_inline exhaustive uppercase":
    """
    ## char_code_inline - all uppercase letter branches

    Every uppercase letter A-Z must be tested to cover each if-branch.
    """

    context "uppercase B through L":
        it "returns 66 for B":
            expect char_code_inline("B") == 66

        it "returns 67 for C":
            expect char_code_inline("C") == 67

        it "returns 68 for D":
            expect char_code_inline("D") == 68

        it "returns 69 for E":
            expect char_code_inline("E") == 69

        it "returns 70 for F":
            expect char_code_inline("F") == 70

        it "returns 71 for G":
            expect char_code_inline("G") == 71

        it "returns 72 for H":
            expect char_code_inline("H") == 72

        it "returns 73 for I":
            expect char_code_inline("I") == 73

        it "returns 74 for J":
            expect char_code_inline("J") == 74

        it "returns 75 for K":
            expect char_code_inline("K") == 75

        it "returns 76 for L":
            expect char_code_inline("L") == 76

    context "uppercase N through Y":
        it "returns 78 for N":
            expect char_code_inline("N") == 78

        it "returns 79 for O":
            expect char_code_inline("O") == 79

        it "returns 80 for P":
            expect char_code_inline("P") == 80

        it "returns 81 for Q":
            expect char_code_inline("Q") == 81

        it "returns 82 for R":
            expect char_code_inline("R") == 82

        it "returns 83 for S":
            expect char_code_inline("S") == 83

        it "returns 84 for T":
            expect char_code_inline("T") == 84

        it "returns 85 for U":
            expect char_code_inline("U") == 85

        it "returns 86 for V":
            expect char_code_inline("V") == 86

        it "returns 87 for W":
            expect char_code_inline("W") == 87

        it "returns 88 for X":
            expect char_code_inline("X") == 88

        it "returns 89 for Y":
            expect char_code_inline("Y") == 89

describe "string_core - char_code_inline exhaustive lowercase":
    """
    ## char_code_inline - all lowercase letter branches

    Every lowercase letter a-z must be tested to cover each if-branch.
    """

    context "lowercase b through l":
        it "returns 98 for b":
            expect char_code_inline("b") == 98

        it "returns 99 for c":
            expect char_code_inline("c") == 99

        it "returns 100 for d":
            expect char_code_inline("d") == 100

        it "returns 101 for e":
            expect char_code_inline("e") == 101

        it "returns 102 for f":
            expect char_code_inline("f") == 102

        it "returns 103 for g":
            expect char_code_inline("g") == 103

        it "returns 104 for h":
            expect char_code_inline("h") == 104

        it "returns 105 for i":
            expect char_code_inline("i") == 105

        it "returns 106 for j":
            expect char_code_inline("j") == 106

        it "returns 107 for k":
            expect char_code_inline("k") == 107

        it "returns 108 for l":
            expect char_code_inline("l") == 108

    context "lowercase n through y":
        it "returns 110 for n":
            expect char_code_inline("n") == 110

        it "returns 111 for o":
            expect char_code_inline("o") == 111

        it "returns 112 for p":
            expect char_code_inline("p") == 112

        it "returns 113 for q":
            expect char_code_inline("q") == 113

        it "returns 114 for r":
            expect char_code_inline("r") == 114

        it "returns 115 for s":
            expect char_code_inline("s") == 115

        it "returns 116 for t":
            expect char_code_inline("t") == 116

        it "returns 117 for u":
            expect char_code_inline("u") == 117

        it "returns 118 for v":
            expect char_code_inline("v") == 118

        it "returns 119 for w":
            expect char_code_inline("w") == 119

        it "returns 120 for x":
            expect char_code_inline("x") == 120

        it "returns 121 for y":
            expect char_code_inline("y") == 121


# ============================================================================
# Test Group 14: Exhaustive char_from_code_inline branch coverage
# ============================================================================

describe "string_core - char_from_code_inline exhaustive digits":
    """
    ## char_from_code_inline - all digit code branches

    Every digit code 48-57 must be tested individually.
    """

    context "all digit codes":
        it "returns 1 for 49":
            expect char_from_code_inline(49) == "1"

        it "returns 2 for 50":
            expect char_from_code_inline(50) == "2"

        it "returns 3 for 51":
            expect char_from_code_inline(51) == "3"

        it "returns 4 for 52":
            expect char_from_code_inline(52) == "4"

        it "returns 6 for 54":
            expect char_from_code_inline(54) == "6"

        it "returns 7 for 55":
            expect char_from_code_inline(55) == "7"

        it "returns 8 for 56":
            expect char_from_code_inline(56) == "8"

describe "string_core - char_from_code_inline exhaustive uppercase":
    """
    ## char_from_code_inline - all uppercase letter code branches

    Every uppercase code 65-90 must be tested individually.
    """

    context "uppercase codes B through L":
        it "returns B for 66":
            expect char_from_code_inline(66) == "B"

        it "returns C for 67":
            expect char_from_code_inline(67) == "C"

        it "returns D for 68":
            expect char_from_code_inline(68) == "D"

        it "returns E for 69":
            expect char_from_code_inline(69) == "E"

        it "returns F for 70":
            expect char_from_code_inline(70) == "F"

        it "returns G for 71":
            expect char_from_code_inline(71) == "G"

        it "returns H for 72":
            expect char_from_code_inline(72) == "H"

        it "returns I for 73":
            expect char_from_code_inline(73) == "I"

        it "returns J for 74":
            expect char_from_code_inline(74) == "J"

        it "returns K for 75":
            expect char_from_code_inline(75) == "K"

        it "returns L for 76":
            expect char_from_code_inline(76) == "L"

    context "uppercase codes N through Y":
        it "returns N for 78":
            expect char_from_code_inline(78) == "N"

        it "returns O for 79":
            expect char_from_code_inline(79) == "O"

        it "returns P for 80":
            expect char_from_code_inline(80) == "P"

        it "returns Q for 81":
            expect char_from_code_inline(81) == "Q"

        it "returns R for 82":
            expect char_from_code_inline(82) == "R"

        it "returns S for 83":
            expect char_from_code_inline(83) == "S"

        it "returns T for 84":
            expect char_from_code_inline(84) == "T"

        it "returns U for 85":
            expect char_from_code_inline(85) == "U"

        it "returns V for 86":
            expect char_from_code_inline(86) == "V"

        it "returns W for 87":
            expect char_from_code_inline(87) == "W"

        it "returns X for 88":
            expect char_from_code_inline(88) == "X"

        it "returns Y for 89":
            expect char_from_code_inline(89) == "Y"

describe "string_core - char_from_code_inline exhaustive lowercase":
    """
    ## char_from_code_inline - all lowercase letter code branches

    Every lowercase code 97-122 must be tested individually.
    """

    context "lowercase codes b through l":
        it "returns b for 98":
            expect char_from_code_inline(98) == "b"

        it "returns c for 99":
            expect char_from_code_inline(99) == "c"

        it "returns d for 100":
            expect char_from_code_inline(100) == "d"

        it "returns e for 101":
            expect char_from_code_inline(101) == "e"

        it "returns f for 102":
            expect char_from_code_inline(102) == "f"

        it "returns g for 103":
            expect char_from_code_inline(103) == "g"

        it "returns h for 104":
            expect char_from_code_inline(104) == "h"

        it "returns i for 105":
            expect char_from_code_inline(105) == "i"

        it "returns j for 106":
            expect char_from_code_inline(106) == "j"

        it "returns k for 107":
            expect char_from_code_inline(107) == "k"

        it "returns l for 108":
            expect char_from_code_inline(108) == "l"

    context "lowercase codes n through y":
        it "returns n for 110":
            expect char_from_code_inline(110) == "n"

        it "returns o for 111":
            expect char_from_code_inline(111) == "o"

        it "returns p for 112":
            expect char_from_code_inline(112) == "p"

        it "returns q for 113":
            expect char_from_code_inline(113) == "q"

        it "returns r for 114":
            expect char_from_code_inline(114) == "r"

        it "returns s for 115":
            expect char_from_code_inline(115) == "s"

        it "returns t for 116":
            expect char_from_code_inline(116) == "t"

        it "returns u for 117":
            expect char_from_code_inline(117) == "u"

        it "returns v for 118":
            expect char_from_code_inline(118) == "v"

        it "returns w for 119":
            expect char_from_code_inline(119) == "w"

        it "returns x for 120":
            expect char_from_code_inline(120) == "x"

        it "returns y for 121":
            expect char_from_code_inline(121) == "y"