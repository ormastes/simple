"""
## Purpose and audience
Verifies the time utils behaviour end to end.
Audience: engineers maintaining this component and its specs.
"""

use std.spec.{describe, it, expect}
use std.common.time_utils.{
    is_leap_year,
    days_in_month,
    day_of_week,
    timestamp_get_year,
    timestamp_get_month,
    timestamp_get_day,
    timestamp_get_hour,
    timestamp_get_minute,
    timestamp_get_second,
    timestamp_get_microsecond,
    timestamp_from_components,
    timestamp_add_days,
    timestamp_diff_days
}

# @req: REQ-TIMEUTIL-TimeUtils-001
describe "time_utils — pure arithmetic civil-date library":

    context "is_leap_year":
        it "2000 is a leap year (divisible by 400)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2000 is a leap year (divisible by 400)")
            expect(is_leap_year(2000)).to_equal(true)

        it "1900 is not a leap year (divisible by 100 but not 400)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 1900 is not a leap year (divisible by 100 but not 400)")
            expect(is_leap_year(1900)).to_equal(false)

        it "2024 is a leap year (divisible by 4 not 100)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2024 is a leap year (divisible by 4 not 100)")
            expect(is_leap_year(2024)).to_equal(true)

        it "2023 is not a leap year":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2023 is not a leap year")
            expect(is_leap_year(2023)).to_equal(false)

        it "1 AD is not a leap year":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 1 AD is not a leap year")
            expect(is_leap_year(1)).to_equal(false)

    context "days_in_month":
        it "January has 31 days":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: January has 31 days")
            expect(days_in_month(2024, 1)).to_equal(31)  # oracle: value fixed by the spec contract

        it "April has 30 days":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: April has 30 days")
            expect(days_in_month(2024, 4)).to_equal(30)  # oracle: value fixed by the spec contract

        it "February 2024 has 29 days (leap year)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: February 2024 has 29 days (leap year)")
            expect(days_in_month(2024, 2)).to_equal(29)  # oracle: value fixed by the spec contract

        it "February 2023 has 28 days (non-leap)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: February 2023 has 28 days (non-leap)")
            expect(days_in_month(2023, 2)).to_equal(28)  # oracle: value fixed by the spec contract

        it "February 1900 has 28 days (century not divisible by 400)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: February 1900 has 28 days (century not divisible by 400)")
            expect(days_in_month(1900, 2)).to_equal(28)  # oracle: value fixed by the spec contract

        it "February 2000 has 29 days (400-year leap)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: February 2000 has 29 days (400-year leap)")
            expect(days_in_month(2000, 2)).to_equal(29)  # oracle: value fixed by the spec contract

        it "December has 31 days":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: December has 31 days")
            expect(days_in_month(2024, 12)).to_equal(31)  # oracle: value fixed by the spec contract

    context "timestamp_get_year":
        it "epoch (micros=0) is year 1970":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch (micros=0) is year 1970")
            expect(timestamp_get_year(0)).to_equal(1970)  # oracle: value fixed by the spec contract

        it "2000-01-01 00:00:00 UTC is year 2000":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2000-01-01 00:00:00 UTC is year 2000")
            # 946684800 seconds * 1_000_000
            expect(timestamp_get_year(946684800000000)).to_equal(2000)  # oracle: value fixed by the spec contract

        it "one second before epoch is year 1969":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one second before epoch is year 1969")
            expect(timestamp_get_year(-1000000)).to_equal(1969)  # oracle: value fixed by the spec contract

        it "2024-02-29 is year 2024":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2024-02-29 is year 2024")
            # 2024-02-29 00:00:00 UTC = 1709164800 seconds
            expect(timestamp_get_year(1709164800000000)).to_equal(2024)  # oracle: value fixed by the spec contract

    context "timestamp_get_month":
        it "epoch (micros=0) is month 1":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch (micros=0) is month 1")
            expect(timestamp_get_month(0)).to_equal(1)  # oracle: value fixed by the spec contract

        it "2000-01-01 is month 1":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2000-01-01 is month 1")
            expect(timestamp_get_month(946684800000000)).to_equal(1)  # oracle: value fixed by the spec contract

        it "one second before epoch is month 12 (December 1969)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one second before epoch is month 12 (December 1969)")
            expect(timestamp_get_month(-1000000)).to_equal(12)  # oracle: value fixed by the spec contract

        it "2024-02-29 is month 2":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2024-02-29 is month 2")
            expect(timestamp_get_month(1709164800000000)).to_equal(2)  # oracle: value fixed by the spec contract

    context "timestamp_get_day":
        it "epoch (micros=0) is day 1":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch (micros=0) is day 1")
            expect(timestamp_get_day(0)).to_equal(1)  # oracle: value fixed by the spec contract

        it "2000-01-01 is day 1":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2000-01-01 is day 1")
            expect(timestamp_get_day(946684800000000)).to_equal(1)  # oracle: value fixed by the spec contract

        it "one second before epoch is day 31 (1969-12-31)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one second before epoch is day 31 (1969-12-31)")
            expect(timestamp_get_day(-1000000)).to_equal(31)  # oracle: value fixed by the spec contract

        it "2024-02-29 is day 29":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2024-02-29 is day 29")
            expect(timestamp_get_day(1709164800000000)).to_equal(29)  # oracle: value fixed by the spec contract

    context "timestamp_get_hour":
        it "epoch is hour 0":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch is hour 0")
            expect(timestamp_get_hour(0)).to_equal(0)  # oracle: value fixed by the spec contract

        it "3600 seconds past epoch is hour 1":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 3600 seconds past epoch is hour 1")
            expect(timestamp_get_hour(3600000000)).to_equal(1)  # oracle: value fixed by the spec contract

        it "one second before epoch is hour 23":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one second before epoch is hour 23")
            expect(timestamp_get_hour(-1000000)).to_equal(23)  # oracle: value fixed by the spec contract

    context "timestamp_get_minute":
        it "epoch is minute 0":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch is minute 0")
            expect(timestamp_get_minute(0)).to_equal(0)  # oracle: value fixed by the spec contract

        it "1830 seconds past epoch is minute 30":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 1830 seconds past epoch is minute 30")
            # 30 * 60 + 30 = 1830 seconds = 30m30s → minute 30
            expect(timestamp_get_minute(1830000000)).to_equal(30)  # oracle: value fixed by the spec contract

        it "one second before epoch is minute 59":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one second before epoch is minute 59")
            expect(timestamp_get_minute(-1000000)).to_equal(59)  # oracle: value fixed by the spec contract

    context "timestamp_get_second":
        it "epoch is second 0":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch is second 0")
            expect(timestamp_get_second(0)).to_equal(0)  # oracle: value fixed by the spec contract

        it "45 seconds past epoch is second 45":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 45 seconds past epoch is second 45")
            expect(timestamp_get_second(45000000)).to_equal(45)  # oracle: value fixed by the spec contract

        it "one second before epoch is second 59":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one second before epoch is second 59")
            expect(timestamp_get_second(-1000000)).to_equal(59)  # oracle: value fixed by the spec contract

    context "timestamp_get_microsecond":
        it "epoch has 0 microseconds":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch has 0 microseconds")
            expect(timestamp_get_microsecond(0)).to_equal(0)  # oracle: value fixed by the spec contract

        it "500000 micros past epoch is 500000 sub-second micros":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 500000 micros past epoch is 500000 sub-second micros")
            expect(timestamp_get_microsecond(500000)).to_equal(500000)  # oracle: value fixed by the spec contract

        it "one microsecond before epoch is 999999":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one microsecond before epoch is 999999")
            expect(timestamp_get_microsecond(-1)).to_equal(999999)  # oracle: value fixed by the spec contract

    context "day_of_week":
        it "epoch 1970-01-01 is Thursday (4)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: epoch 1970-01-01 is Thursday (4)")
            expect(day_of_week(0)).to_equal(4)  # oracle: value fixed by the spec contract

        it "1970-01-04 is Sunday (0)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 1970-01-04 is Sunday (0)")
            # 3 days after epoch
            expect(day_of_week(3 * 86400000000)).to_equal(0)  # oracle: value fixed by the spec contract

        it "2000-01-01 is Saturday (6)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2000-01-01 is Saturday (6)")
            expect(day_of_week(946684800000000)).to_equal(6)  # oracle: value fixed by the spec contract

        it "one day before epoch 1969-12-31 is Wednesday (3)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one day before epoch 1969-12-31 is Wednesday (3)")
            expect(day_of_week(-86400000000)).to_equal(3)  # oracle: value fixed by the spec contract

    context "timestamp_from_components":
        it "Unix epoch from components":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: Unix epoch from components")
            expect(timestamp_from_components(1970, 1, 1, 0, 0, 0, 0)).to_equal(0)  # oracle: value fixed by the spec contract

        it "2000-01-01 00:00:00 from components":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2000-01-01 00:00:00 from components")
            expect(timestamp_from_components(2000, 1, 1, 0, 0, 0, 0)).to_equal(946684800000000)  # oracle: value fixed by the spec contract

        it "one second before epoch from components":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one second before epoch from components")
            expect(timestamp_from_components(1969, 12, 31, 23, 59, 59, 0)).to_equal(-1000000)  # oracle: value fixed by the spec contract

        it "2024-02-29 from components":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 2024-02-29 from components")
            expect(timestamp_from_components(2024, 2, 29, 0, 0, 0, 0)).to_equal(1709164800000000)  # oracle: value fixed by the spec contract

    context "round-trip: from_components then get_*":
        it "round-trips 2023-07-15 14:30:45.123456":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: round-trips 2023-07-15 14:30:45.123456")
            val micros = timestamp_from_components(2023, 7, 15, 14, 30, 45, 123456)
            expect(timestamp_get_year(micros)).to_equal(2023)  # oracle: value fixed by the spec contract
            expect(timestamp_get_month(micros)).to_equal(7)  # oracle: value fixed by the spec contract
            expect(timestamp_get_day(micros)).to_equal(15)  # oracle: value fixed by the spec contract
            expect(timestamp_get_hour(micros)).to_equal(14)  # oracle: value fixed by the spec contract
            expect(timestamp_get_minute(micros)).to_equal(30)  # oracle: value fixed by the spec contract
            expect(timestamp_get_second(micros)).to_equal(45)  # oracle: value fixed by the spec contract
            expect(timestamp_get_microsecond(micros)).to_equal(123456)  # oracle: value fixed by the spec contract

        it "round-trips 1800-03-01 (pre-1970 distant past)":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: round-trips 1800-03-01 (pre-1970 distant past)")
            val micros = timestamp_from_components(1800, 3, 1, 0, 0, 0, 0)
            expect(timestamp_get_year(micros)).to_equal(1800)  # oracle: value fixed by the spec contract
            expect(timestamp_get_month(micros)).to_equal(3)  # oracle: value fixed by the spec contract
            expect(timestamp_get_day(micros)).to_equal(1)  # oracle: value fixed by the spec contract

    context "timestamp_add_days":
        it "adding 0 days is identity":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: adding 0 days is identity")
            expect(timestamp_add_days(0, 0)).to_equal(0)  # oracle: value fixed by the spec contract

        it "adding 1 day to epoch gives 86400000000 micros":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: adding 1 day to epoch gives 86400000000 micros")
            expect(timestamp_add_days(0, 1)).to_equal(86400000000)  # oracle: value fixed by the spec contract

        it "adding negative days moves backward":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: adding negative days moves backward")
            expect(timestamp_add_days(86400000000, -1)).to_equal(0)  # oracle: value fixed by the spec contract

    context "timestamp_diff_days":
        it "same timestamp gives 0 days difference":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: same timestamp gives 0 days difference")
            expect(timestamp_diff_days(0, 0)).to_equal(0)  # oracle: value fixed by the spec contract

        it "one day apart gives 1":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: one day apart gives 1")
            expect(timestamp_diff_days(86400000000, 0)).to_equal(1)  # oracle: value fixed by the spec contract

        it "reversed gives -1":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: reversed gives -1")
            expect(timestamp_diff_days(0, 86400000000)).to_equal(-1)  # oracle: value fixed by the spec contract

        it "365 days apart":
            # @req: REQ-TIMEUTIL-TimeUtils-001
            step("Verify: 365 days apart")
            val one_year = 365 * 86400000000
            expect(timestamp_diff_days(one_year, 0)).to_equal(365)  # oracle: value fixed by the spec contract
