describe "Iterable":
    it "skipped":
        skip("pre-existing test failures - functions/imports not available")

# # iterable stdlib spec
# #
# # Tests for src/lib/iterable.spl
# 
# use std.iterable.{each, each_with_index}
# 
# describe "iterable stdlib":
#     describe "each":
#         it "calls action for every element":
#             fn run():
#                 var count = 0
#                 each([1, 2, 3], fn(x):
#                     count = count + 1
#                 )
#                 count
#             expect(run()).to_equal(3)
# 
#         it "receives each element value":
#             fn run():
#                 var sum = 0
#                 each([10, 20, 30], fn(x):
#                     sum = sum + x
#                 )
#                 sum
#             expect(run()).to_equal(60)
# 
#         it "works on empty array":
#             fn run():
#                 var called = 0
#                 each([], fn(x):
#                     called = called + 1
#                 )
#                 called
#             expect(run()).to_equal(0)
# 
#     describe "each_with_index":
#         it "receives element and 0-based index":
#             fn run():
#                 var last_idx = -1
#                 each_with_index(["a", "b", "c"], fn(item, i):
#                     last_idx = i
#                 )
#                 last_idx
#             expect(run()).to_equal(2)
# 
#         it "indices start at 0":
#             fn run():
#                 var first_idx = -1
#                 each_with_index([99], fn(item, i):
#                     first_idx = i
#                 )
#                 first_idx
#             expect(run()).to_equal(0)
# 
#         it "accumulates all elements":
#             fn run():
#                 var total = 0
#                 each_with_index([1, 2, 3, 4], fn(item, i):
#                     total = total + item
#                 )
#                 total
#             expect(run()).to_equal(10)
