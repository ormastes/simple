"""
## Purpose and audience
Purpose: Verify Debug bridge API surface through executable runtime oracles against the real implementation.
Audience: library and compiler engineers who run the spec suite.

## Operator workflow
Run `bin/simple test /tmp/mod13/test/unit/std/complete/debug_3_complete_spec.spl` and require the final Results line to report 0 failed.

## Compatibility and limitations
Executed on the interpreter test lane; asserts observable values only and makes no performance claims.
"""

# @req: REQ-LIB-DEBUG-001 — stable requirement identity for this spec.
# doc-path: doc/01_research/local/debug_behavior.md
# doc-path: doc/03_plan/sys_test/debug_behavior.md
# doc-path: doc/04_architecture/lib/debug.md
# doc-path: doc/05_design/lib/debug_oracles.md
# @manual: primary

use std.spec
use std.debug.remote.t32_ffi.t32_breakpoint.{t32_breakpoint_function_names}
use std.spec.step

describe "Debug bridge API surface":
    it "the debug bridge advertises its exported function surface":
        step("Verify: the debug bridge advertises its exported function surface")
        # @req: REQ-LIB-DEBUG-001
        expect(t32_breakpoint_function_names().len() > 0).to_equal(true)  # oracle: the breakpoint API list is non-empty

