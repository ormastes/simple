# @tag:unit, spec

use std.spec.*

describe "std.spec boolean expectation shortcuts":
    """
    Verifies concise boolean assertions:
    `expect(condition)` asserts true and `expect_not(condition)` asserts false.
    Matcher chains such as `expect(value).to_equal(value)` remain supported for
    non-boolean equality.
    """

    it "accepts bare expect for true boolean expressions":
        expect(true)
        val condition = 2 + 2 == 4
        expect(condition)

    it "accepts expect_not for false boolean expressions":
        expect_not(false)
        val condition = "abc".contains("z")
        expect_not(condition)

    it "keeps matcher equality for non-boolean values":
        expect(42).to_equal(42)
        expect("simple").to_equal("simple")
