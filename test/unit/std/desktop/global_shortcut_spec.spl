# Desktop global shortcut API specification

use std.nogc_sync_mut.desktop.global_shortcut.{Shortcut}

describe "Desktop Global Shortcut API":
    it "creates Shortcut struct":
        val s = Shortcut(modifiers: ["ctrl", "shift"], key: "p")
        expect s.key == "p"
