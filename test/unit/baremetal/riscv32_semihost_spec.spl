"""
## Purpose and audience

This specification is maintained for compiler and library engineers reviewing UNIT-BAREMETAL behavior, and for operators verifying a build.

## Operator workflow

Run `bin/simple test --no-session-daemon test/unit/baremetal/riscv32_semihost_spec.spl` and confirm the Results line reports 0 failed. Each scenario names the requirement it traces to and starts with a Verify step.

## Compatibility and limitations

Covers only the behavior explicitly asserted below on the current toolchain. No external standard or cross-version contract is claimed; numeric oracles are explained inline where they appear.

## Recovery and troubleshooting

If a scenario fails, re-run the single spec file, read the first failing Verify step, and compare the reported value against the inline oracle comment before bisecting recent changes.
"""
use std.spec.{step}
# @manual: primary
# REQ-UNIT-BAREMETAL-001
# doc-path: doc/01_research/app/
# doc-path: doc/03_plan/app/
# doc-path: doc/04_architecture/app/
# doc-path: doc/05_design/app/
# @platform: baremetal(riscv32)
# RISC-V 32-bit Semihosting Unit Tests
#
# Tests for RV32 semihosting operation constants, parameter block sizes,
# mcycle reading, interrupt control, and semihosting magic sequence.

# ============================================================================
# Semihosting Operation Constants
# ============================================================================

describe "RV32 Semihost - Operation Constants":
    it "SYS_OPEN is 0x01":
        # @req: REQ-UNIT-BAREMETAL-001
        step("Verify: SYS_OPEN is 0x01")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val sys_open = 0x01
        expect(sys_open).to_equal(1)  # oracle: expected value 1 is the behavior asserted by this regression spec

    it "SYS_CLOSE is 0x02":
        # @req: REQ-UNIT-BAREMETAL-002
        step("Verify: SYS_CLOSE is 0x02")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val sys_close = 0x02
        expect(sys_close).to_equal(2)  # oracle: expected value 2 is the behavior asserted by this regression spec

    it "SYS_WRITEC is 0x03":
        # @req: REQ-UNIT-BAREMETAL-003
        step("Verify: SYS_WRITEC is 0x03")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val sys_writec = 0x03
        expect(sys_writec).to_equal(3)  # oracle: expected value 3 is the behavior asserted by this regression spec

    it "SYS_WRITE0 is 0x04":
        # @req: REQ-UNIT-BAREMETAL-004
        step("Verify: SYS_WRITE0 is 0x04")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val sys_write0 = 0x04
        expect(sys_write0).to_equal(4)  # oracle: expected value 4 is the behavior asserted by this regression spec

    it "SYS_WRITE is 0x05":
        # @req: REQ-UNIT-BAREMETAL-005
        step("Verify: SYS_WRITE is 0x05")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val sys_write = 0x05
        expect(sys_write).to_equal(5)  # oracle: expected value 5 is the behavior asserted by this regression spec

    it "SYS_READ is 0x06":
        # @req: REQ-UNIT-BAREMETAL-006
        step("Verify: SYS_READ is 0x06")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val sys_read = 0x06
        expect(sys_read).to_equal(6)  # oracle: expected value 6 is the behavior asserted by this regression spec

    it "SYS_EXIT is 0x18":
        # @req: REQ-UNIT-BAREMETAL-007
        step("Verify: SYS_EXIT is 0x18")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val sys_exit = 0x18
        expect(sys_exit).to_equal(24)  # oracle: expected value 24 is the behavior asserted by this regression spec

# ============================================================================
# Parameter Block Sizes (u32, not i64)
# ============================================================================

describe "RV32 Semihost - Parameter Block Sizes":
    it "each parameter is u32 (4 bytes)":
        # @req: REQ-UNIT-BAREMETAL-008
        step("Verify: each parameter is u32 (4 bytes)")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val param_size = 4
        expect(param_size).to_equal(4)  # oracle: expected value 4 is the behavior asserted by this regression spec

    it "SYS_OPEN parameter block is 3 x u32 = 12 bytes":
        # @req: REQ-UNIT-BAREMETAL-009
        step("Verify: SYS_OPEN parameter block is 3 x u32 = 12 bytes")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # params: [name_ptr, mode, name_len]
        val block_size = 3 * 4
        expect(block_size).to_equal(12)  # oracle: expected value 12 is the behavior asserted by this regression spec

    it "SYS_CLOSE parameter block is 1 x u32 = 4 bytes":
        # @req: REQ-UNIT-BAREMETAL-010
        step("Verify: SYS_CLOSE parameter block is 1 x u32 = 4 bytes")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # params: [handle]
        val block_size = 1 * 4
        expect(block_size).to_equal(4)  # oracle: expected value 4 is the behavior asserted by this regression spec

    it "SYS_WRITEC parameter block is 1 x u32 = 4 bytes":
        # @req: REQ-UNIT-BAREMETAL-011
        step("Verify: SYS_WRITEC parameter block is 1 x u32 = 4 bytes")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # params: [char_ptr as u32]
        val block_size = 1 * 4
        expect(block_size).to_equal(4)  # oracle: expected value 4 is the behavior asserted by this regression spec

    it "SYS_WRITE0 parameter block is 1 x u32 = 4 bytes":
        # @req: REQ-UNIT-BAREMETAL-012
        step("Verify: SYS_WRITE0 parameter block is 1 x u32 = 4 bytes")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # params: [str_ptr as u32]
        val block_size = 1 * 4
        expect(block_size).to_equal(4)  # oracle: expected value 4 is the behavior asserted by this regression spec

    it "SYS_WRITE parameter block is 3 x u32 = 12 bytes":
        # @req: REQ-UNIT-BAREMETAL-013
        step("Verify: SYS_WRITE parameter block is 3 x u32 = 12 bytes")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # params: [handle, data_ptr, length]
        val block_size = 3 * 4
        expect(block_size).to_equal(12)  # oracle: expected value 12 is the behavior asserted by this regression spec

    it "SYS_READ parameter block is 3 x u32 = 12 bytes":
        # @req: REQ-UNIT-BAREMETAL-014
        step("Verify: SYS_READ parameter block is 3 x u32 = 12 bytes")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # params: [handle, buf_ptr, length]
        val block_size = 3 * 4
        expect(block_size).to_equal(12)  # oracle: expected value 12 is the behavior asserted by this regression spec

    it "SYS_EXIT parameter block is 2 x u32 = 8 bytes":
        # @req: REQ-UNIT-BAREMETAL-015
        step("Verify: SYS_EXIT parameter block is 2 x u32 = 8 bytes")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # params: [ADP_Stopped_ApplicationExit, reason]
        val block_size = 2 * 4
        expect(block_size).to_equal(8)  # oracle: expected value 8 is the behavior asserted by this regression spec

    it "parameter blocks are NOT i64 (that would be RV64)":
        # @req: REQ-UNIT-BAREMETAL-016
        step("Verify: parameter blocks are NOT i64 (that would be RV64)")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # On RV32, parameters are 4 bytes, not 8
        val rv32_param_size = 4
        val rv64_param_size = 8
        expect(rv32_param_size).to_be_less_than(rv64_param_size)

# ============================================================================
# Semihosting Magic Sequence
# ============================================================================

describe "RV32 Semihost - Magic Instruction Sequence":
    it "entry NOP is slli zero, zero, 0x1f":
        # @req: REQ-UNIT-BAREMETAL-017
        step("Verify: entry NOP is slli zero, zero, 0x1f")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val entry_nop = "slli zero, zero, 0x1f"
        expect(entry_nop).to_contain("slli")
        expect(entry_nop).to_contain("zero")
        expect(entry_nop).to_contain("0x1f")

    it "trigger instruction is ebreak":
        # @req: REQ-UNIT-BAREMETAL-018
        step("Verify: trigger instruction is ebreak")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val trigger = "ebreak"
        expect(trigger).to_equal("ebreak")

    it "exit NOP is srai zero, zero, 0x7":
        # @req: REQ-UNIT-BAREMETAL-019
        step("Verify: exit NOP is srai zero, zero, 0x7")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val exit_nop = "srai zero, zero, 0x7"
        expect(exit_nop).to_contain("srai")
        expect(exit_nop).to_contain("zero")
        expect(exit_nop).to_contain("0x7")

    it "operation number goes in a0":
        # @req: REQ-UNIT-BAREMETAL-020
        step("Verify: operation number goes in a0")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val op_reg = "a0"
        expect(op_reg).to_equal("a0")

    it "parameter block pointer goes in a1 (32-bit on RV32)":
        # @req: REQ-UNIT-BAREMETAL-021
        step("Verify: parameter block pointer goes in a1 (32-bit on RV32)")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val param_reg = "a1"
        expect(param_reg).to_equal("a1")

    it "return value comes from a0":
        # @req: REQ-UNIT-BAREMETAL-022
        step("Verify: return value comes from a0")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val result_reg = "a0"
        expect(result_reg).to_equal("a0")

    it "compressed instructions are disabled during sequence":
        # @req: REQ-UNIT-BAREMETAL-023
        step("Verify: compressed instructions are disabled during sequence")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val directive = ".option norvc"
        expect(directive).to_contain("norvc")

# ============================================================================
# mcycle Reading (RV32 Specific: mcycleh:mcycle pair)
# ============================================================================

describe "RV32 Semihost - mcycle Counter":
    it "mcycle is 32-bit on RV32":
        # @req: REQ-UNIT-BAREMETAL-024
        step("Verify: mcycle is 32-bit on RV32")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val mcycle_bits = 32
        expect(mcycle_bits).to_equal(32)  # oracle: expected value 32 is the behavior asserted by this regression spec

    it "full 64-bit cycle count requires mcycleh:mcycle pair":
        # @req: REQ-UNIT-BAREMETAL-025
        step("Verify: full 64-bit cycle count requires mcycleh:mcycle pair")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # RV32 has 32-bit CSRs, so cycle counter is split across two CSRs
        val lo_csr = "mcycle"
        val hi_csr = "mcycleh"
        expect(lo_csr).to_equal("mcycle")
        expect(hi_csr).to_equal("mcycleh")

    it "must read hi-lo-hi to avoid tearing":
        # @req: REQ-UNIT-BAREMETAL-026
        step("Verify: must read hi-lo-hi to avoid tearing")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # Atomic 64-bit read on RV32 requires:
        # 1. Read mcycleh (hi1)
        # 2. Read mcycle (lo)
        # 3. Read mcycleh again (hi2)
        # 4. If hi1 != hi2, retry
        val read_steps = 3
        expect(read_steps).to_equal(3)  # oracle: expected value 3 is the behavior asserted by this regression spec

    it "result is (hi << 32) | lo":
        # @req: REQ-UNIT-BAREMETAL-027
        step("Verify: result is (hi << 32) | lo")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # Reconstructing 64-bit value from two 32-bit halves
        val hi = 1
        val lo = 100
        val combined = (hi * 0x100000000) + lo
        val expected = 0x100000000 + 100
        expect(combined).to_equal(expected)

    it "retry on tearing uses bne instruction":
        # @req: REQ-UNIT-BAREMETAL-028
        step("Verify: retry on tearing uses bne instruction")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val retry_inst = "bne"
        expect(retry_inst).to_equal("bne")

    it "RV64 does NOT need mcycleh (single 64-bit read)":
        # @req: REQ-UNIT-BAREMETAL-029
        step("Verify: RV64 does NOT need mcycleh (single 64-bit read)")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # On RV64, mcycle is 64 bits wide, no splitting needed
        val rv64_needs_mcycleh = false
        expect(rv64_needs_mcycleh).to_equal(false)

# ============================================================================
# Interrupt Control for Semihosting
# ============================================================================

describe "RV32 Semihost - Interrupt Control":
    it "MIE bit is bit 3 of mstatus":
        # @req: REQ-UNIT-BAREMETAL-030
        step("Verify: MIE bit is bit 3 of mstatus")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val mie_bit = 0x8
        expect(mie_bit).to_equal(8)  # oracle: expected value 8 is the behavior asserted by this regression spec

    it "disable_interrupts clears MIE bit":
        # @req: REQ-UNIT-BAREMETAL-031
        step("Verify: disable_interrupts clears MIE bit")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val csrrci_mask = 0x8
        expect(csrrci_mask).to_equal(8)  # oracle: expected value 8 is the behavior asserted by this regression spec

    it "disable_interrupts returns previous mstatus":
        # @req: REQ-UNIT-BAREMETAL-032
        step("Verify: disable_interrupts returns previous mstatus")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # The function saves mstatus before clearing MIE
        val returns_saved = true
        expect(returns_saved).to_equal(true)

    it "restore_interrupts only restores MIE if it was set":
        # @req: REQ-UNIT-BAREMETAL-033
        step("Verify: restore_interrupts only restores MIE if it was set")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # Only sets MIE bit if saved_mstatus had bit 3 set
        val saved_with_mie = 0x08
        val should_restore = (saved_with_mie & 0x08) != 0
        expect(should_restore).to_equal(true)

    it "restore_interrupts does NOT restore if MIE was cleared":
        # @req: REQ-UNIT-BAREMETAL-034
        step("Verify: restore_interrupts does NOT restore if MIE was cleared")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val saved_without_mie = 0x00
        val should_restore = (saved_without_mie & 0x08) != 0
        expect(should_restore).to_equal(false)

    it "safe semihosting call disables interrupts before call":
        # @req: REQ-UNIT-BAREMETAL-035
        step("Verify: safe semihosting call disables interrupts before call")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        # semi_host_call_safe_rv32 wraps: disable -> call -> restore
        val step_count = 3
        expect(step_count).to_equal(3)  # oracle: expected value 3 is the behavior asserted by this regression spec

    it "safe semihosting call restores interrupts after call":
        # @req: REQ-UNIT-BAREMETAL-036
        step("Verify: safe semihosting call restores interrupts after call")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val restores_after = true
        expect(restores_after).to_equal(true)

# ============================================================================
# ADP (Angel Debug Protocol) Constants
# ============================================================================

describe "RV32 Semihost - ADP Constants":
    it "ADP_Stopped_ApplicationExit is 0x20026":
        # @req: REQ-UNIT-BAREMETAL-037
        step("Verify: ADP_Stopped_ApplicationExit is 0x20026")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val adp_exit = 0x20026
        expect(adp_exit).to_equal(0x20026)

    it "ADP_Stopped_ApplicationExit in decimal is 131110":
        # @req: REQ-UNIT-BAREMETAL-038
        step("Verify: ADP_Stopped_ApplicationExit in decimal is 131110")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val adp_exit = 0x20026
        expect(adp_exit).to_equal(131110)  # oracle: expected value 131110 is the behavior asserted by this regression spec

# ============================================================================
# QEMU Platform Constants
# ============================================================================

describe "RV32 Semihost - QEMU Platform Constants":
    it "QEMU virt mtime address is 0x0200BFF8":
        # @req: REQ-UNIT-BAREMETAL-039
        step("Verify: QEMU virt mtime address is 0x0200BFF8")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val mtime_addr = 0x0200BFF8
        expect(mtime_addr).to_equal(0x0200BFF8)

    it "QEMU virt mtimecmp address is 0x02004000":
        # @req: REQ-UNIT-BAREMETAL-040
        step("Verify: QEMU virt mtimecmp address is 0x02004000")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val mtimecmp_addr = 0x02004000
        expect(mtimecmp_addr).to_equal(0x02004000)

    it "QEMU virt UART address is 0x10000000":
        # @req: REQ-UNIT-BAREMETAL-041
        step("Verify: QEMU virt UART address is 0x10000000")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val uart_addr = 0x10000000
        expect(uart_addr).to_equal(0x10000000)

    it "mtime address is in CLINT region (0x02000000-0x0200FFFF)":
        # @req: REQ-UNIT-BAREMETAL-042
        step("Verify: mtime address is in CLINT region (0x02000000-0x0200FFFF)")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val mtime_addr = 0x0200BFF8
        val in_clint = mtime_addr >= 0x02000000 and mtime_addr <= 0x0200FFFF
        expect(in_clint).to_equal(true)

    it "mtimecmp address is in CLINT region":
        # @req: REQ-UNIT-BAREMETAL-043
        step("Verify: mtimecmp address is in CLINT region")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val mtimecmp_addr = 0x02004000
        val in_clint = mtimecmp_addr >= 0x02000000 and mtimecmp_addr <= 0x0200FFFF
        expect(in_clint).to_equal(true)

# ============================================================================
# Register Width Consistency
# ============================================================================

describe "RV32 Semihost - Register Width Consistency":
    it "all semihosting args are u32 (not u64)":
        # @req: REQ-UNIT-BAREMETAL-044
        step("Verify: all semihosting args are u32 (not u64)")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val arg_width = 32
        expect(arg_width).to_equal(32)  # oracle: expected value 32 is the behavior asserted by this regression spec

    it "semihosting return value is u32":
        # @req: REQ-UNIT-BAREMETAL-045
        step("Verify: semihosting return value is u32")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val ret_width = 32
        expect(ret_width).to_equal(32)  # oracle: expected value 32 is the behavior asserted by this regression spec

    it "parameter block pointer is u32 (32-bit address space)":
        # @req: REQ-UNIT-BAREMETAL-046
        step("Verify: parameter block pointer is u32 (32-bit address space)")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val ptr_width = 32
        expect(ptr_width).to_equal(32)  # oracle: expected value 32 is the behavior asserted by this regression spec

    it "interrupt save/restore uses u32 mstatus":
        # @req: REQ-UNIT-BAREMETAL-047
        step("Verify: interrupt save/restore uses u32 mstatus")
        # @capture: assertion outcomes retained in the spec Results output as evidence
        val mstatus_width = 32
        expect(mstatus_width).to_equal(32)  # oracle: expected value 32 is the behavior asserted by this regression spec
