"""
## Purpose and audience
Purpose: Prove that OpenOCD feature registration.
Audience: compiler and tooling engineers who maintain this spec.
"""
# @req: REQ-APP-DEBUG-001 — stable requirement identity for this spec.

# OpenOCD Feature Registration Tests
# Tests that OpenOCD features are registered at correct ranks.

# --- Local copies ---

class FeatureRank:
    static fn NATIVE() -> i32:
        0
    static fn BRIDGE() -> i32:
        1
    static fn EXTERNAL() -> i32:
        3
    static fn EMULATED() -> i32:
        4

class FeatureEntry:
    name: text
    rank: i32
    backend: text

# Simulates what register_openocd_features would register
fn openocd_registered_features() -> [FeatureEntry]:
    [
        FeatureEntry(name: "Halt", rank: 0, backend: "openocd"),
        FeatureEntry(name: "Resume", rank: 0, backend: "openocd"),
        FeatureEntry(name: "SingleStep", rank: 0, backend: "openocd"),
        FeatureEntry(name: "ReadMemory", rank: 0, backend: "openocd"),
        FeatureEntry(name: "SetBreakpoint", rank: 0, backend: "openocd"),
        FeatureEntry(name: "FlashProgram", rank: 0, backend: "openocd"),
        FeatureEntry(name: "SystemReset", rank: 0, backend: "openocd"),
        FeatureEntry(name: "OpenocdMonitor", rank: 0, backend: "openocd")
    ]

# --- Tests ---

use std.spec.step

describe "OpenOCD feature registration":
    it "registers execution control at rank NATIVE":
        step("Verify: registers execution control at rank NATIVE")
        # @req: REQ-APP-DEBUG-001
        val features = openocd_registered_features()
        val halt = features[0]
        expect(halt.name).to_equal("Halt")
        expect(halt.rank).to_equal(FeatureRank.NATIVE())

    it "registers memory ops at rank NATIVE":
        step("Verify: registers memory ops at rank NATIVE")
        val features = openocd_registered_features()
        val mem = features[3]
        expect(mem.name).to_equal("ReadMemory")
        expect(mem.rank).to_equal(0)  # oracle: 0 — named expected value from the requirement

    it "registers FlashProgram at rank NATIVE":
        step("Verify: registers FlashProgram at rank NATIVE")
        val features = openocd_registered_features()
        val flash = features[5]
        expect(flash.name).to_equal("FlashProgram")
        expect(flash.rank).to_equal(0)  # oracle: 0 — named expected value from the requirement

    it "registers SystemReset at rank NATIVE":
        step("Verify: registers SystemReset at rank NATIVE")
        val features = openocd_registered_features()
        val reset = features[6]
        expect(reset.name).to_equal("SystemReset")
        expect(reset.rank).to_equal(0)  # oracle: 0 — named expected value from the requirement

    it "registers OpenocdMonitor at rank NATIVE":
        step("Verify: registers OpenocdMonitor at rank NATIVE")
        val features = openocd_registered_features()
        val mon = features[7]
        expect(mon.name).to_equal("OpenocdMonitor")
        expect(mon.rank).to_equal(0)  # oracle: 0 — named expected value from the requirement

    it "all features use openocd backend name":
        step("Verify: all features use openocd backend name")
        val features = openocd_registered_features()
        for f in features:
            expect(f.backend).to_equal("openocd")
