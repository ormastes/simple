use app.package.types.{Version, VersionConstraint}
use app.package.semver.{parse_version, parse_constraint, satisfies, version_equal, version_greater}

describe "test1":
    it "test1a":
        val (ok1, v1, err1) = parse_version("1.2.3")
        expect(ok1).to_equal(true)
    it "test1b":
        val (ok2, v2, err2) = parse_version("2.0.0")
        expect(ok2).to_equal(true)
