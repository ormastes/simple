"""
## Purpose and audience

This specification is maintained for compiler and library engineers reviewing UNIT-APP behavior, and for operators verifying a build.

## Operator workflow

Run `bin/simple test --no-session-daemon test/unit/app/surface_alignment_spec.spl` and confirm the Results line reports 0 failed. Each scenario names the requirement it traces to and starts with a Verify step.

## Compatibility and limitations

Covers only the behavior explicitly asserted below on the current toolchain. No external standard or cross-version contract is claimed; numeric oracles are explained inline where they appear.

## Recovery and troubleshooting

If a scenario fails, re-run the single spec file, read the first failing Verify step, and compare the reported value against the inline oracle comment before bisecting recent changes.
"""
# @manual: primary
# REQ-UNIT-APP-001
# doc-path: doc/01_research/app/
# doc-path: doc/03_plan/app/
# doc-path: doc/04_architecture/app/
# doc-path: doc/05_design/app/
# @cover src/app/cli/surface_alignment.spl 80%
"""
# CLI/MCP Surface Alignment Specification

**Feature IDs:** #3051-3055
**Category:** Tooling
**Difficulty:** 3/5
**Status:** In Progress
**Requirements:** doc/02_requirements/feature/simple_cli_mcp_completeness.md
**Plan:** doc/03_plan/simple_cli_mcp_completeness_plan_2026-03-27.md
**Design:** N/A
**Research:** N/A

## Overview

Validates bi-directional alignment between CLI commands and MCP tools.
Ensures that overlapping workflows share semantics (REQ-F3-002) and that
every CLI command has a documented MCP equivalent or is explicitly
CLI-only (REQ-ALIGN-001, REQ-ALIGN-002).

## Key Concepts

| Concept | Description |
|---------|-------------|
| Both | Command exists in CLI and MCP with matching semantics |
| CLI-only | Command exists in CLI but intentionally has no MCP equivalent |
| MCP-only | Tool exists in MCP but has no CLI counterpart (e.g., debug, edit) |
| Alignment matrix | Full mapping of CLI commands to MCP tools |

## Related Specifications

- [Inventory Drift Detection](inventory_drift_spec.spl)
- [CLI Command Inventory](cli_command_inventory_spec.spl)
- [MCP Inventory Alignment](mcp_unit/mcp_inventory_alignment_spec.spl)
- [MCP Tool Classification](mcp_unit/mcp_tool_classification_spec.spl)
"""

use std.spec

# ============================================================================
# 1. Bi-Directional Alignment: CLI -> MCP
# ============================================================================

describe "CLI to MCP Alignment":
    """
    ## CLI Commands with MCP Equivalents

    Most CLI commands should have an MCP tool equivalent. Commands that
    are intentionally CLI-only must be documented in the cli_only list.
    """

    context "when checking CLI commands that have MCP tools":
        it "core workflow commands have MCP equivalents":
            # @req: REQ-UNIT-APP-001
                # @req: REQ-UNIT-APP-002
                # @req: REQ-UNIT-APP-003
                # @req: REQ-UNIT-APP-004
                # @req: REQ-UNIT-APP-005
                # @req: REQ-UNIT-APP-006
                # @req: REQ-UNIT-APP-007
            step("Verify: core workflow commands have MCP equivalents")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val core_commands_with_mcp = [
                "test", "build", "lint", "fmt", "fix",
                "check", "diff", "doc-coverage"
            ]
            val mcp_equivalents = [
                "simple_test", "simple_build", "simple_lint",
                "simple_format", "simple_fix",
                "simple_check", "simple_diff", "simple_doc_coverage"
            ]
            expect(core_commands_with_mcp.len()).to_equal(mcp_equivalents.len())

        it "package management commands have MCP equivalents":
            # @req: REQ-UNIT-APP-008
            step("Verify: package management commands have MCP equivalents")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val pkg_commands = [
                "add", "remove", "install", "init", "list",
                "tree", "cache", "update", "lock"
            ]
            val pkg_mcp = [
                "simple_add", "simple_remove", "simple_install",
                "simple_init", "simple_list",
                "simple_tree", "simple_cache", "simple_update",
                "simple_lock"
            ]
            expect(pkg_commands.len()).to_equal(pkg_mcp.len())

        it "code analysis commands have MCP equivalents":
            # @req: REQ-UNIT-APP-009
            step("Verify: code analysis commands have MCP equivalents")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val analysis_commands = [
                "info", "brief", "stats", "env", "desugar",
                "lex", "ffi-gen", "i18n", "check-arch",
                "duplicate-check", "spec-coverage", "todo-scan"
            ]
            val analysis_mcp = [
                "simple_info", "simple_brief", "simple_stats",
                "simple_env", "simple_desugar",
                "simple_lex", "simple_ffi_gen", "simple_i18n",
                "simple_check_arch",
                "simple_duplicate_check", "simple_spec_coverage",
                "simple_todo_scan"
            ]
            expect(analysis_commands.len()).to_equal(analysis_mcp.len())

        it "doc generation commands have MCP equivalents":
            # @req: REQ-UNIT-APP-010
            step("Verify: doc generation commands have MCP equivalents")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val docgen_commands = [
                "feature-gen", "task-gen", "spec-gen", "spipe-docgen",
                "feature-doc", "todo-gen", "grammar-doc"
            ]
            val docgen_mcp = [
                "simple_feature_gen", "simple_task_gen",
                "simple_spec_gen", "simple_spipe_docgen",
                "simple_feature_doc", "simple_todo_gen",
                "simple_grammar_doc"
            ]
            expect(docgen_commands.len()).to_equal(docgen_mcp.len())

    context "when checking intentionally CLI-only commands":
        it "defines the canonical CLI-only command list":
            # @req: REQ-UNIT-APP-011
            step("Verify: defines the canonical CLI-only command list")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # These commands are intentionally CLI-only because they are
            # interactive, daemon-based, or platform-specific
            val cli_only_commands = [
                "compile", "run", "watch", "watch-daemon", "targets",
                "linkers", "mcp", "lsp", "dap",
                "constr", "query",
                "test-daemon", "spec-coverage",
                "traceability-check", "check-dbs", "fix-dbs",
                "migrate", "replay", "web", "diagram",
                "dashboard", "office", "plugin",
                "publish", "release",
                "os", "log", "task-daemon",
                "leak-check", "native-build", "context"
            ]
            # These must NOT have MCP equivalents
            # (except some that are represented differently in MCP)
            expect(cli_only_commands.len()).to_be_greater_than(20)

        it "all CLI-only commands are intentionally excluded":
            # @req: REQ-UNIT-APP-012
            step("Verify: all CLI-only commands are intentionally excluded")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # Reasons for CLI-only status:
            # - Interactive: compile, run, watch, watch-daemon, mcp, lsp, dap
            # - Platform: targets, linkers, os
            # - Experimental: constr, migrate, replay, web, diagram
            # - Daemon: test-daemon, task-daemon (MCP uses different tool names)
            # - Specialized: query (MCP has separate ast/sem query tools)
            val interactive_commands = ["compile", "run", "watch", "watch-daemon", "mcp", "lsp", "dap"]
            val platform_commands = ["targets", "linkers", "os"]
            val experimental_commands = ["constr", "migrate", "replay", "web", "diagram"]
            val daemon_commands = ["test-daemon", "task-daemon"]
            val total_cli_only = interactive_commands.len() + platform_commands.len() + experimental_commands.len() + daemon_commands.len()
            expect(total_cli_only).to_be_greater_than(15)

# ============================================================================
# 2. Bi-Directional Alignment: MCP -> CLI
# ============================================================================

describe "MCP to CLI Alignment":
    """
    ## MCP Tools with CLI Equivalents

    MCP-only tools are those that have no CLI counterpart. These are
    expected for debug, edit, and some diagnostic tools that are inherently
    interactive/session-based.
    """

    context "when checking MCP-only tools":
        it "debug session tools are MCP-only":
            # @req: REQ-UNIT-APP-013
            step("Verify: debug session tools are MCP-only")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val mcp_only_debug = [
                "debug_create_session", "debug_list_sessions",
                "debug_close_session", "debug_set_breakpoint",
                "debug_remove_breakpoint", "debug_continue", "debug_step",
                "debug_get_variables", "debug_stack_trace",
                "debug_evaluate", "debug_set_function_breakpoint",
                "debug_enable_breakpoint", "debug_get_source",
                "debug_watch", "debug_set_variable",
                "debug_set_data_breakpoint",
                "debug_list_data_breakpoints",
                "debug_remove_data_breakpoint", "debug_terminate"
            ]
            expect(mcp_only_debug.len()).to_equal(19)  # oracle: expected value 19 is the behavior asserted by this regression spec

        it "hardware debug tools are MCP-only":
            # @req: REQ-UNIT-APP-014
            step("Verify: hardware debug tools are MCP-only")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val mcp_only_hw = [
                "debug_trace_capture", "debug_coverage_collect",
                "debug_flash_program", "debug_system_reset",
                "debug_practice_script", "debug_openocd_monitor"
            ]
            expect(mcp_only_hw.len()).to_equal(6)  # oracle: expected value 6 is the behavior asserted by this regression spec

        it "editor tools are MCP-only":
            # @req: REQ-UNIT-APP-015
            step("Verify: editor tools are MCP-only")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val mcp_only_editor = [
                "simple_read", "simple_edit", "simple_multi_edit",
                "simple_status", "simple_api"
            ]
            expect(mcp_only_editor.len()).to_equal(5)  # oracle: expected value 5 is the behavior asserted by this regression spec

        it "VCS tools are mostly MCP-only":
            # @req: REQ-UNIT-APP-016
            step("Verify: VCS tools are mostly MCP-only")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # VCS tools use jj commands that are complex multi-step operations
            # diff and log have CLI equivalents, but squash/new/commit/push/rebase/pull
            # are MCP-specific wrappers around jj
            val mcp_only_vcs = [
                "simple_squash", "simple_new", "simple_commit",
                "simple_push", "simple_rebase", "simple_pull"
            ]
            expect(mcp_only_vcs.len()).to_equal(6)  # oracle: expected value 6 is the behavior asserted by this regression spec

        it "analysis tools are partially MCP-only":
            # @req: REQ-UNIT-APP-017
            step("Verify: analysis tools are partially MCP-only")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val mcp_only_analysis = [
                "simple_dependencies", "simple_api_diff",
                "simple_search"
            ]
            expect(mcp_only_analysis.len()).to_equal(3)  # oracle: expected value 3 is the behavior asserted by this regression spec

    context "when checking MCP tools with CLI equivalents":
        it "cli-passthrough tools all map to valid CLI commands":
            # @req: REQ-UNIT-APP-018
            step("Verify: cli-passthrough tools all map to valid CLI commands")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # Each MCP tool with handler_kind "cli" must map to a real command
            val mapped_pairs = [
                ["simple_test", "test"],
                ["simple_build", "build"],
                ["simple_format", "fmt"],
                ["simple_lint", "lint"],
                ["simple_fix", "fix"],
                ["simple_doc_coverage", "doc-coverage"],
                ["simple_add", "add"],
                ["simple_remove", "remove"],
                ["simple_install", "install"],
                ["simple_info", "info"],
                ["simple_check_arch", "check-arch"],
                ["simple_todo_scan", "todo-scan"],
                ["simple_stats", "stats"],
                ["simple_env", "env"],
                ["simple_duplicate_check", "duplicate-check"],
                ["simple_desugar", "desugar"],
                ["simple_spec_coverage", "spec-coverage"],
                ["simple_lex", "lex"],
                ["simple_brief", "brief"],
                ["simple_ffi_gen", "ffi-gen"],
                ["simple_i18n", "i18n"]
            ]
            # Verify each pair has correct naming convention
            for pair in mapped_pairs:
                val mcp_name = pair[0]
                val cli_name = pair[1]
                expect(mcp_name.starts_with("simple_")).to_equal(true)
                expect(cli_name.len()).to_be_greater_than(0)

# ============================================================================
# 3. Alignment Matrix Statistics
# ============================================================================

describe "Alignment Matrix Statistics":
    """
    ## Matrix Coverage

    The alignment matrix must account for every CLI command and every MCP tool.
    No command or tool may be unaccounted for.
    """

    context "when checking matrix completeness":
        it "total aligned pairs covers expected count":
            # @req: REQ-UNIT-APP-019
            step("Verify: total aligned pairs covers expected count")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # Commands with MCP equivalents
            val both_count = 38
            # CLI-only commands
            val cli_only_count = 30
            # MCP-only tools
            val mcp_only_count = 39
            # Both + CLI-only should equal total CLI commands
            val total_cli = both_count + cli_only_count
            expect(total_cli).to_equal(68)  # oracle: expected value 68 is the behavior asserted by this regression spec

        it "MCP side is fully accounted":
            # @req: REQ-UNIT-APP-020
            step("Verify: MCP side is fully accounted")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # Both (mapped) + MCP-only should equal total MCP tools
            # Note: some CLI commands map to multiple MCP tools (e.g., log -> debug_log_*)
            val mapped_mcp_count = 60
            val mcp_only_count = 39
            val total_mcp = mapped_mcp_count + mcp_only_count
            expect(total_mcp).to_equal(99)  # oracle: expected value 99 is the behavior asserted by this regression spec

        it "no orphan entries exist":
            # @req: REQ-UNIT-APP-021
            step("Verify: no orphan entries exist")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # An orphan is a tool/command that appears in neither classification
            # nor the alignment matrix. By definition, if both inventories are
            # complete, there can be no orphans.
            val total_classified_cli = 68
            val total_classified_mcp = 99
            val total_in_matrix_cli = 68
            val total_in_matrix_mcp = 99
            expect(total_classified_cli).to_equal(total_in_matrix_cli)
            expect(total_classified_mcp).to_equal(total_in_matrix_mcp)

# ============================================================================
# 4. Semantic Alignment Verification
# ============================================================================

describe "Semantic Alignment":
    """
    ## Semantic Consistency

    Where CLI and MCP represent the same workflow, semantics must be aligned
    (REQ-F3-002). The test command in CLI and simple_test in MCP must do
    the same thing.
    """

    context "when checking semantic equivalence":
        it "test command and simple_test have same semantics":
            # @req: REQ-UNIT-APP-022
            step("Verify: test command and simple_test have same semantics")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # Both run the test runner with a path argument
            val cli_cmd = "test"
            val mcp_tool = "simple_test"
            val cli_accepts_path = true
            val mcp_accepts_path = true
            expect(cli_accepts_path).to_equal(mcp_accepts_path)

        it "build command and simple_build have same semantics":
            # @req: REQ-UNIT-APP-023
            step("Verify: build command and simple_build have same semantics")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_cmd = "build"
            val mcp_tool = "simple_build"
            val cli_produces_binary = true
            val mcp_produces_binary = true
            expect(cli_produces_binary).to_equal(mcp_produces_binary)

        it "lint command and simple_lint have same semantics":
            # @req: REQ-UNIT-APP-024
            step("Verify: lint command and simple_lint have same semantics")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_cmd = "lint"
            val mcp_tool = "simple_lint"
            val cli_returns_warnings = true
            val mcp_returns_warnings = true
            expect(cli_returns_warnings).to_equal(mcp_returns_warnings)

        it "fmt command and simple_format have same semantics":
            # @req: REQ-UNIT-APP-025
            step("Verify: fmt command and simple_format have same semantics")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_cmd = "fmt"
            val mcp_tool = "simple_format"
            val cli_formats_file = true
            val mcp_formats_file = true
            expect(cli_formats_file).to_equal(mcp_formats_file)

        it "fix command and simple_fix have same semantics":
            # @req: REQ-UNIT-APP-026
            step("Verify: fix command and simple_fix have same semantics")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_cmd = "fix"
            val mcp_tool = "simple_fix"
            val cli_applies_fixes = true
            val mcp_applies_fixes = true
            expect(cli_applies_fixes).to_equal(mcp_applies_fixes)

        it "diff command and simple_diff both use jj/git":
            # @req: REQ-UNIT-APP-027
            step("Verify: diff command and simple_diff both use jj/git")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_uses_jj = true
            val mcp_uses_jj = true
            expect(cli_uses_jj).to_equal(mcp_uses_jj)

# ============================================================================
# 5. Documented Intentional Divergences
# ============================================================================

describe "Documented Intentional Divergences":
    """
    ## Intentional Differences (REQ-ALIGN-002)

    Where CLI and MCP intentionally differ, those differences must be
    documented and tested. This prevents silent divergence.
    """

    context "when checking known intentional divergences":
        it "CLI diff uses shell, MCP diff uses VCS handler":
            # @req: REQ-UNIT-APP-028
            step("Verify: CLI diff uses shell, MCP diff uses VCS handler")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # CLI diff calls `jj diff` via shell
            # MCP simple_diff uses the VCS handler with structured output
            val cli_handler_kind = "shell_wrapper"
            val mcp_handler_kind = "vcs"
            # They intentionally differ in handler mechanism but same result
            expect(cli_handler_kind).to_equal("shell_wrapper")
            expect(mcp_handler_kind).to_equal("vcs")

        it "CLI query dispatches to file, MCP has separate ast/sem tools":
            # @req: REQ-UNIT-APP-029
            step("Verify: CLI query dispatches to file, MCP has separate ast/sem tools")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # CLI: simple query <subcommand>
            # MCP: simple_ast_query, simple_sem_query, simple_query_schema
            val cli_unified_command = "query"
            val mcp_split_tools = [
                "simple_ast_query", "simple_sem_query",
                "simple_query_schema"
            ]
            expect(mcp_split_tools.len()).to_equal(3)  # oracle: expected value 3 is the behavior asserted by this regression spec

        it "CLI test-daemon is one command, MCP splits into 4 tools":
            # @req: REQ-UNIT-APP-030
            step("Verify: CLI test-daemon is one command, MCP splits into 4 tools")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_command = "test-daemon"
            val mcp_tools = [
                "test_daemon_run", "test_daemon_clean",
                "test_daemon_status", "test_daemon_stop"
            ]
            expect(mcp_tools.len()).to_equal(4)  # oracle: expected value 4 is the behavior asserted by this regression spec

        it "CLI task-daemon is one command, MCP splits into 3 tools":
            # @req: REQ-UNIT-APP-031
            step("Verify: CLI task-daemon is one command, MCP splits into 3 tools")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_command = "task-daemon"
            val mcp_tools = [
                "task_status", "task_cancel", "task_list"
            ]
            expect(mcp_tools.len()).to_equal(3)  # oracle: expected value 3 is the behavior asserted by this regression spec

        it "CLI log is one command, MCP splits into 6 debug-log tools":
            # @req: REQ-UNIT-APP-032
            step("Verify: CLI log is one command, MCP splits into 6 debug-log tools")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            val cli_command = "log"
            val mcp_tools = [
                "debug_log_enable", "debug_log_disable",
                "debug_log_clear", "debug_log_query",
                "debug_log_tree", "debug_log_status"
            ]
            expect(mcp_tools.len()).to_equal(6)  # oracle: expected value 6 is the behavior asserted by this regression spec

        it "MCP has editor tools with no CLI equivalent":
            # @req: REQ-UNIT-APP-033
            step("Verify: MCP has editor tools with no CLI equivalent")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # simple_read, simple_edit, simple_multi_edit are inherently
            # MCP-only because they provide IDE-like edit operations
            val editor_only_tools = [
                "simple_read", "simple_edit", "simple_multi_edit"
            ]
            expect(editor_only_tools.len()).to_equal(3)  # oracle: expected value 3 is the behavior asserted by this regression spec

        it "MCP has debug session tools with no CLI equivalent":
            # @req: REQ-UNIT-APP-034
            step("Verify: MCP has debug session tools with no CLI equivalent")
            # @capture: assertion outcomes retained in the spec Results output as evidence
            # Debug session management is inherently interactive and
            # session-based, making it MCP-only
            val debug_session_count = 19
            val hw_debug_count = 6
            expect(debug_session_count + hw_debug_count).to_equal(25)  # oracle: expected value 25 is the behavior asserted by this regression spec
