# @cover src/app/ui.web/html.spl 80%
# @tag: unit, ui, web, wm

use app.ui.web.html.{generate_wm_js, generate_css}

describe "generate_wm_js route":
    it "connects the WM boot script to the routed UI websocket":
        val js = generate_wm_js(3333)
        expect (js).contains("ws://localhost:")
        expect (js).contains("3333")
        expect (js).contains("/ui/ws")
        expect (js).contains("new SimpleWindowManager()")

describe "desktop glass CSS":
    it "keeps tablet widths on the desktop layout path":
        val css = generate_css("glass_dark")
        expect (css).contains(".widget-menubar::before")
        expect(css).not_to_contain("@media (max-width: 900px)")