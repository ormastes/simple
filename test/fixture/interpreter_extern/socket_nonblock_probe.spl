# Fixture for test/01_unit/compiler/interpreter_extern/fb_image_log_socket_registration_spec.spl.
# Calls the canonical Pure-Simple API. Its internal prepare shim receives an
# invalid descriptor, so F_GETFL fails deterministically without side effects.
use std.fs.nvfs_posix.socket_nonblock.{rt_socket_set_nonblocking}

fn main() -> i64:
    val ok = rt_socket_set_nonblocking(-1, true)
    print "socket_nonblock_result={ok}"
    0
