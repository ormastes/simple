# Fixture for test/01_unit/compiler/interpreter_extern/fb_image_log_socket_registration_spec.spl.
# A name in the rt_simpleos_log_* prefix with no C definition; should get the
# family-scoped guard text, not the generic "unknown extern function".

extern fn rt_simpleos_log_zzz_bogus() -> i64

fn main() -> i64:
    val r = rt_simpleos_log_zzz_bogus()
    print "unexpected success {r}"
    0
