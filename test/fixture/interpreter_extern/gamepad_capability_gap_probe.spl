# Fixture for test/01_unit/compiler/interpreter_extern/capability_gap_spec.spl.
# Calls a real rt_gamepad_* extern (declared in
# src/lib/nogc_sync_mut/io/gamepad_sffi.spl). rt_gamepad_* has 20 .spl call
# sites and ZERO native definitions anywhere (M3 capability gap).

extern fn rt_gamepad_count(context: i64) -> i64

fn main() -> i64:
    val n = rt_gamepad_count(0)
    print "unexpected success: rt_gamepad_count={n}"
    0
