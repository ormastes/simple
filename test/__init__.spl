# Test namespace root module.
#
# This module serves as the root of the test namespace hierarchy.
# Submodules are available as test.unit.*, test.system.*, etc.

# Module marker - no exports to avoid circular dependencies
