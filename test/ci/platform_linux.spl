# CI test: verify platform detection on Linux
extern fn rt_process_run(cmd: text, args: [text]) -> (text, text, i64)

val result = rt_process_run("/bin/sh", ["-c", "uname -s"])
val os = result[0].trim()
if os == "Linux":
    print "true"
else:
    print "false"
