# CI test: verify shell execution works
extern fn rt_process_run(cmd: text, args: [text]) -> (text, text, i64)

val result = rt_process_run("/bin/sh", ["-c", "echo test"])
val stdout = result[0].trim()
print stdout
